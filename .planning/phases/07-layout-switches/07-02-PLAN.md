---
phase: 07-layout-switches
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/layout/home/HomeHeroMinimal.tsx
  - components/layout/home/HomeHeroSplit.tsx
  - components/layout/home/HomeHeroFullBleed.tsx
  - app/page.tsx
  - tests/unit/components/layout/home/home-hero-variants.test.ts
  - tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt
autonomous: true
requirements: [LAYOUT-02, LAYOUT-04]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The home page resolves its hero through a typed lookup map keyed by the hero enum and renders the chosen named component server-side; no component receives a generic prop and none branches on the enum name (D-05, D-07). [LAYOUT-02, LAYOUT-04]"
    - "The centred-text hero is a verbatim extraction of today's home hero: every class string and every copy line of the pre-extraction section survives unchanged in the extracted component, and the only permitted difference is the added per-switch data attribute (D-07, D-14)."
    - "The split hero puts the copy left and the first featured product's image right at the large breakpoint, and stacks copy above image below it, using natural DOM order with no explicit ordering utilities (UI-SPEC populated row, D-07)."
    - "The full-bleed hero is a fixed-height band escaping the page's horizontal padding, with the first featured product's image behind a scrim and the copy overlaid, reusing the category page's existing band-height scale (UI-SPEC populated row, D-07)."
    - "The home page with zero featured products renders the centred-text hero for every stored hero value: the split and full-bleed components delegate to it rather than rendering their own markup with no image (UI-SPEC empty row, orchestrator decision over RESEARCH assumption A1). [LAYOUT-02 edge: empty]"
    - "The hero image source is always the first featured product's primary image, resolved through the shared product-image helper the product card already uses; it is never read from settings, from a store-config field, or from any other user-supplied input (D-07, RESEARCH Don't Hand-Roll). [LAYOUT-02 edge: ordering]"
    - "The featured-products grid below the hero is untouched by this phase: it keeps its existing markup, its existing order and its existing priority flag on the first card. [LAYOUT-02 edge: ordering]"
    - "All three heroes carry the same headline, body and CTA copy, reused verbatim; no new marketing copy is authored (UI-SPEC Copywriting Contract, UI-SPEC long-text row)."
    - "The home page is an async server component, so no hero variant has a client loading state of its own (UI-SPEC loading row)."
    - "No hero variant performs a fetch that can fail independently of the page, so no hero variant owns an error state (UI-SPEC error row)."
    - statement: "The unchanged headline and body copy still fit inside the full-bleed band's fixed height at the common breakpoints, without clipping or overlapping the CTA."
      verification: backstop
    - "The full-bleed scrim and its two overlaid text lines use literal, theme-independent colour values because no token pair guarantees a dark backdrop under every theme polarity; the exception is declared with an inline scanner region sentinel carrying a written reason, and the CTA button inside the same overlay stays fully token-driven and outside the sentinel region (UI-SPEC contrast row, D-09)."
    - "The whole-tree token scan still reports zero violations and exactly the two pre-existing manual-review rows; the scanner's named-file exception list is not extended (orchestrator decision, Phase 6 precedent)."
    - "The hero switch is independent of the other two: a hero value can never be read by the category or gallery switch because each is validated against its own enum array, and a stored hero value equal to the default renders identically to an absent one. [LAYOUT-02 edge: adjacency]"
    - "The hero lookup map's keys equal the hero enum, in order, with no duplicates, and every enum member resolves to a component that renders non-empty markup for both a present and an absent featured product. [LAYOUT-04 edge: empty, adjacency, ordering]"
    - "Each of the three hero variants has its own render test (D-13)."
  artifacts:
    - path: "components/layout/home/HomeHeroMinimal.tsx"
      provides: "The verbatim extraction of today's centred-text home hero"
    - path: "components/layout/home/HomeHeroSplit.tsx"
      provides: "The copy-left image-right hero, delegating to the centred hero when there is no featured product"
    - path: "components/layout/home/HomeHeroFullBleed.tsx"
      provides: "The full-width photo band hero with a scrim, delegating to the centred hero when there is no featured product"
    - path: "tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt"
      provides: "The pre-extraction hero block the verbatim extraction is proven against"
    - path: "app/page.tsx"
      provides: "Server-side hero resolution and the hero lookup map"
      contains: "getLayoutSettings"
  key_links:
    - from: "app/page.tsx"
      to: "lib/layout/settings.ts"
      via: "awaited call in the server page body, above the returned tree, never inside a Suspense child"
      pattern: "getLayoutSettings"
    - from: "app/page.tsx"
      to: "components/layout/home/HomeHeroFullBleed.tsx"
      via: "typed lookup map keyed by the hero enum, resolved server-side"
      pattern: "HOME_HERO_MAP"
    - from: "components/layout/home/HomeHeroSplit.tsx"
      to: "lib/utils/product-image.ts"
      via: "the shared product-image resolver, the same one the product card uses"
      pattern: "resolveProductImageSrc"
    - from: "components/layout/home/HomeHeroSplit.tsx"
      to: "components/layout/home/HomeHeroMinimal.tsx"
      via: "delegation when there is no featured product"
      pattern: "HomeHeroMinimal"
  prohibitions:
    - statement: "The three switches must NOT introduce a generic layout or variant prop on any page component."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition lifted from LAYOUT-04; enforced here by the server-side map lookup and the source-contract assertions in Task 3."
    - statement: "Layout resolution must NOT be cached in a module-level or isolate-level variable."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition carried from Phase 6; binds here because the home page is the most tempting place to hoist a settings read above the request."
    - statement: "The hero image URL must NOT be read from settings or any other user-supplied input."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition; this is the plan it binds in — the image comes from the first featured product through the shared resolver, asserted by the source-contract test in Task 2."
    - statement: "This phase must NOT change the 23-token contract or the theme mechanism."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition carried from Phase 5 D-01 and Phase 6; binds here because the full-bleed scrim is the only literal-colour exception the phase takes and it is fenced by a sentinel rather than by widening the contract."
    - statement: "The scanner's named-file exception list must NOT be extended for the scrim."
      status: unresolved
      verification: judgment
      reason: "Orchestrator decision overriding the UI-SPEC's own suggestion; the inline region sentinel is the Phase 6 precedent and keeps the exception visible at the line, not in a registry."
---

<objective>
Make the home hero a stored, server-chosen decision: extract today's centred-text hero verbatim,
add the split and full-bleed variants beside it, and let the home page pick one of the three from
the value plan 07-01's resolver returns.

Purpose: LAYOUT-02. This is the first expansion out from the tracer — the resolver, the enum module
and the map idiom are already proven on the category route, so this plan carries only what is new:
two genuinely new layouts, the first image-bearing hero surface, and the phase's single literal-colour
exception.

Output: three hero components, a rewired home page, a proven-verbatim default, and a live probe
showing the served home page change when the stored hero value changes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-switches/07-CONTEXT.md
@.planning/phases/07-layout-switches/07-PATTERNS.md
@.planning/phases/07-layout-switches/07-UI-SPEC.md
@.planning/phases/07-layout-switches/07-01-SUMMARY.md
</context>

<interfaces>
Consumed from plan 07-01, unchanged: `HOME_HEROES`, the `HomeHero` union, `DEFAULT_LAYOUTS` and
`LAYOUT_SETTING_KEYS` from `lib/layout/variants.ts`, and `getLayoutSettings()` from
`lib/layout/settings.ts`.

Created here:

`components/layout/home/*.tsx` — every hero variant takes exactly
`{ featuredProduct: Product | null }` and nothing else. The centred-text variant ignores the prop.
The three files are interchangeable behind
`Record<HomeHero, ComponentType<{ featuredProduct: Product | null }>>`.

Every variant's root element carries a `data-home-hero` attribute valued with its own enum member —
the same per-switch evidence attribute convention plan 07-01 established. Because the split and
full-bleed variants delegate to the centred variant when there is no featured product, a delegated
render legitimately serves the centred member's attribute value; that is the delegation being
observable, not a bug.

Consumed unchanged from the existing codebase: `resolveProductImageSrc(primaryImage, media,
placeholder?)` from `lib/utils/product-image.ts`, already used by `components/ProductCard.tsx`.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use the file form of
  `git commit`; redirect with `>|` when a target file may already exist.
- Every task is committed atomically before the next one starts.
</executor_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record the pre-extraction hero block and extract the centred-text hero verbatim</name>
  <read_first>
    - `app/page.tsx` — the whole file, 85 lines: the imports, the featured-products derivation, the hero section, the featured grid that stays, and the revalidation export at the end.
    - `.planning/phases/07-layout-switches/07-PATTERNS.md` — the HomeHeroMinimal section's verbatim JSX and the note that the copy strings are hardcoded English literals to move unchanged.
    - `.planning/phases/07-layout-switches/07-RESEARCH.md` — Pitfall 5, explaining why the page's revalidation export is already inert and must be left exactly as it is rather than "fixed".
    - `tests/unit/components/product-card-anchor-nesting.test.ts` — the render-test idiom and the next-module mocks.
  </read_first>
  <files>components/layout/home/HomeHeroMinimal.tsx, tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt, tests/unit/components/layout/home/home-hero-variants.test.ts</files>
  <behavior>
    - Every non-blank line of the recorded pre-extraction block, with leading whitespace normalised, appears in the extracted component's source in the same relative order.
    - The only recorded line that is not matched exactly is the opening section tag, which gained the data attribute; the test asserts that line's remainder still matches character for character.
    - Rendering the extracted component produces the headline, the body copy and the CTA label verbatim, and exactly one link to the featured category route.
    - The extracted component renders identically whether it is handed a featured product or null, because it ignores the prop.
  </behavior>
  <action>
Record first, extract second. Before editing anything, take the hero section out of the committed
version of the home page with a git read of the file at HEAD and write those lines, unmodified and
including their original indentation, into the recording file listed above. Add nothing to that file
— no header, no fence, no trailing note. It is a frozen copy, and the parity assertion is only worth
something if it was taken before the edit.

Then create the centred-text hero component. Copy the recorded block in as the component's returned
tree, add the imports it needs, give it the shared props type from the interfaces block even though
it ignores the prop, and add the per-switch data attribute to the section element valued with its own
enum member. Change nothing else: not the class strings, not the copy, not the element order, not
the whitespace inside the block. The recording is the acceptance gate.

Leave the home page itself completely alone in this task — the inline hero stays where it is until
Task 3 removes it. That keeps the recording and the extraction reviewable as a pure add.

Start the shared hero test file with the parity cases from the behavior block plus the centred
variant's render cases. Mock the two next modules the way the existing anchor-nesting test does.
Read both the recording and the component source through the filesystem inside the test, normalise
leading whitespace on each line, and assert order-preserving containment — this is a source-level
verbatim check rather than a render-level one because, unlike the category display, the
pre-extraction hero was never a standalone renderable unit. The page-level pixel proof for this
route is the volt-dark screenshot diff plan 07-05 runs against plan 07-01's baseline.
  </action>
  <acceptance_criteria>
    - `tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt` is non-empty and its content matches the hero section of `app/page.tsx` at the commit this task started from.
    - `components/layout/home/HomeHeroMinimal.tsx` exists and its returned tree contains every recorded line, in order.
    - `mise exec -- npx vitest run tests/unit/components/layout/home/` exits 0.
    - `mise exec -- npm run scan:tokens` reports 0 violations and exactly 2 manual-review rows.
    - `app/page.tsx` is unmodified by this task.
    - `mise exec -- npm run typecheck` and `mise exec -- npm run lint` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>test -s tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt && mise exec -- npx vitest run tests/unit/components/layout/home/ && test -z "$(git diff --name-only -- app/page.tsx)" && SCAN="$(mktemp)" && mise exec -- npm run scan:tokens >| "$SCAN" 2>&1 && test "$(/usr/bin/grep -c '^MANUAL-REVIEW' "$SCAN")" -eq 2 && /usr/bin/grep -q '0 violations' "$SCAN" && mise exec -- npm run typecheck && mise exec -- npm run lint</automated>
    <fails_when>the recording file is absent or zero bytes, meaning the pre-extraction state was never captured; or the parity case reports a line that does not appear in the component source, meaning the extraction was not verbatim; or the home page shows as modified, meaning the extraction and the rewiring were mixed into one reviewable change; or the scan prints a violation line or a manual-review count other than 2.</fails_when>
  </verify>
  <done>Today's hero exists as a standalone named component whose source is provably line-for-line the pre-extraction block, and the home page still renders exactly what it rendered before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The split and full-bleed heroes, their image source, and the scrim exception</name>
  <read_first>
    - `lib/utils/product-image.ts` lines 90-110 — `resolveProductImageSrc`'s signature, its handling of both stored image shapes, its R2-key normalisation and its placeholder fallback.
    - `components/ProductCard.tsx` lines 105-125 — the existing call site, including the placeholder path this repo passes.
    - `scripts/scan-hardcoded-colors.mjs` lines 145-185 — the region sentinel: it blanks whole lines between the start and end markers, runs before comment stripping, and throws on an unclosed region. Whole lines, not fragments, so anything that must stay scanned has to sit outside the region.
    - `app/category/[slug]/page.tsx` lines 120-135 — the existing hero band's height scale the full-bleed variant reuses rather than inventing new values.
    - `.planning/phases/07-layout-switches/07-UI-SPEC.md` — the Home hero rows of Storefront Variant Anatomy for both new variants' exact container, image and overlay contracts, the Color section's flagged scrim exception and its written reason, and the Copywriting Contract's hero rows.
    - `.planning/phases/07-layout-switches/07-RESEARCH.md` — Pitfall 3, the empty-featured-products case, and the Anti-Patterns entry forbidding a third image-resolution helper.
  </read_first>
  <files>components/layout/home/HomeHeroSplit.tsx, components/layout/home/HomeHeroFullBleed.tsx, tests/unit/components/layout/home/home-hero-variants.test.ts</files>
  <behavior>
    - The split variant handed a featured product renders the copy block, an image whose source came from the shared resolver, and the CTA, and carries its own enum member in the data attribute.
    - The full-bleed variant handed a featured product renders a band containing the image, the scrim element, the overlaid headline and body, and a CTA styled with contract tokens, and carries its own enum member in the data attribute.
    - Either new variant handed null renders exactly what the centred variant renders, including the centred variant's data attribute value — the delegation is observable in the markup.
    - Either new variant handed a product with no image data still renders an image element with the shared resolver's placeholder path rather than an empty source.
    - Neither new variant's source references the settings module, the layout settings module, or any store-config field for its image.
    - Neither new variant uses a raw-HTML sink of any kind.
    - Both new variants reuse the recorded headline, body and CTA copy verbatim.
  </behavior>
  <action>
Build the split variant first. Its container is the two-column grid at the large breakpoint from the
UI-SPEC row, with the copy column first in DOM order and the image column second, so the stacked
layout below that breakpoint falls out of natural order with no ordering utilities. The copy column
keeps the same size and weight classes the centred variant uses and drops only the centring
utilities. The image column is the aspect-square framed image the row specifies. Resolve the image
source by calling the shared product-image resolver with the featured product's primary image and
media fields — do not reach for the product display's own local media helper, which is narrower and
exists only to keep the gallery extraction byte-identical.

Then build the full-bleed variant. It is the fixed-height band from the UI-SPEC row, escaping the
page's horizontal padding with negative margins at each breakpoint step, reusing the height scale
the category page's own hero band already uses. The image fills the band. Above it sits the scrim,
and above that the overlay block holding the headline, the body and the CTA.

Both new variants must handle the no-featured-product case by delegating: when the prop is null,
return the centred variant rendered instead of their own markup. That is the UI-SPEC decision and it
supersedes the research assumption that suggested a placeholder image instead — record the
supersession in the summary. Delegation also means the served markup carries the centred variant's
attribute value, which is exactly how the empty case stays observable from a route probe.

The scrim and the two overlaid text lines are the phase's only literal-colour exception. Fence them
with the scanner's inline region sentinel, opening the region with a comment stating in plain words
why a token-relative scrim cannot work here — no token pair guarantees a dark backdrop under both a
light and a dark preset, and Phase 6 already proved that with its light-preset pass. Keep the region
as tight as the line-blanking behaviour allows: the sentinel blanks whole lines, so the CTA button
must sit on lines outside the region, because its contrast is guaranteed by the token contract and
it must stay under the scanner. Do not add either file to the scanner's named-file exception list
and do not edit the scanner at all: the run must still print exactly the two pre-existing
manual-review rows.

Extend the shared hero test file with the remaining behavior rows, including the two delegation
cases and the source-contract assertions about the image source and the absence of a raw-HTML sink.
Read the component sources inside the test the way the admin source-contract test reads its subject.
  </action>
  <acceptance_criteria>
    - `mise exec -- npx vitest run tests/unit/components/layout/home/` exits 0 with one passing case per behavior row.
    - The two delegation cases assert the rendered markup carries the centred variant's attribute value and the centred variant's copy.
    - The source-contract cases assert neither new variant's source names the settings modules or a store-config field, and that neither uses a raw-HTML sink.
    - `components/layout/home/HomeHeroFullBleed.tsx` contains one region-start sentinel and one region-end sentinel, and the start line carries a written reason.
    - `mise exec -- npm run scan:tokens` reports 0 violations, exactly 2 manual-review rows, and those two rows are the same two files as before this phase.
    - `scripts/scan-hardcoded-colors.mjs` is unmodified.
    - `mise exec -- npm run typecheck` and `mise exec -- npm run lint` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/layout/home/ && test "$(/usr/bin/grep -c 'gsd:scan-ignore-start' components/layout/home/HomeHeroFullBleed.tsx)" -eq 1 && test "$(/usr/bin/grep -c 'gsd:scan-ignore-end' components/layout/home/HomeHeroFullBleed.tsx)" -eq 1 && test -z "$(git diff --name-only -- scripts/scan-hardcoded-colors.mjs)" && SCAN="$(mktemp)" && mise exec -- npm run scan:tokens >| "$SCAN" 2>&1 && test "$(/usr/bin/grep -c '^MANUAL-REVIEW' "$SCAN")" -eq 2 && /usr/bin/grep -q 'image-placeholders.ts' "$SCAN" && /usr/bin/grep -q 'Promotion.ts' "$SCAN" && /usr/bin/grep -q '0 violations' "$SCAN" && mise exec -- npm run typecheck && mise exec -- npm run lint</automated>
    <fails_when>vitest exits non-zero; or either sentinel count is not exactly 1, meaning the region is unbalanced and the scanner will throw on the unclosed region; or the scanner script shows as modified, meaning the exception was registered in the named-file list instead of at the line; or the scan prints a violation line, a manual-review count other than 2, or a manual-review pair that is not the two pre-existing files.</fails_when>
  </verify>
  <done>Two genuinely new hero layouts exist, both take their image from the shared product-image resolver, both degrade to the centred hero when there is nothing to show, and the phase's one literal-colour exception is fenced at the line with a written reason while the scan stays clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the home page to the hero switch and prove it on a real server</name>
  <precondition>A local dev server can serve the storefront against a local D1 whose featured category has at least one active product, and the admin settings endpoint accepts the documented development bypass header — the same conditions plan 07-01 confirmed.</precondition>
  <read_first>
    - `app/page.tsx` — the whole file again, now to cut it: the featured-products derivation that stays, the inline hero that leaves, the featured grid that stays untouched, and the inert revalidation export at the end.
    - `.planning/phases/07-layout-switches/07-01-SUMMARY.md` — the exact call shape and return shape of the resolver, and the map idiom the category page established.
    - `app/category/[slug]/page.tsx` as it stands after plan 07-01 — the reference implementation of the awaited resolver call and the typed lookup map in a server page.
  </read_first>
  <files>app/page.tsx, tests/unit/components/layout/home/home-hero-variants.test.ts</files>
  <behavior>
    - The hero map's key list equals the hero enum array, in order, with no duplicates.
    - Every member of the hero enum resolves through the map to a component that renders non-empty markup with a featured product and with null.
    - The home page's source awaits the resolver directly in the component body, above the returned tree, and not inside a Suspense child.
    - The home page's source contains no comparison of a resolved value against any hero member name literal.
    - The featured-products grid's markup, its ordering and its priority flag on the first card are unchanged from the pre-phase source.
  </behavior>
  <action>
Remove the inline hero section from the home page and put the switch in its place. Await the
resolver directly in the page body, above the returned tree, never inside a Suspense child — a
streamed hero choice would paint the wrong layout first, which is the same reasoning Phase 6 applied
to theme resolution. Define the typed lookup map from the hero enum to a component taking the shared
hero props, look up the resolved value, and render the resolved component with the first featured
product or null when the array is empty.

Change nothing else on this page. The featured-products derivation stays, the featured grid stays
byte-for-byte, and the revalidation export at the bottom stays exactly as it is: research confirmed
with a real build that the root layout's dynamic-rendering directive already supersedes it and every
route renders per request, so there is no staleness to fix and nothing to clean up here. Removing it
would be an unrelated change inside a file this plan is already touching.

Extend the shared hero test file with the map and source-contract behavior rows.

Then prove it live. Start the dev server, and for each of the two non-default hero members in turn:
post that member for the hero setting key to the admin settings endpoint using the documented
development bypass header, fetch the home route, and count the hero data attribute set to that
member in the served HTML. Then restore the default member, refetch, and count the default
attribute. Stop the server. Record all three counts in the summary.
  </action>
  <acceptance_criteria>
    - `mise exec -- npx vitest run tests/unit/components/layout/home/` exits 0 with one passing case per behavior row.
    - The map-versus-enum case asserts key equality including order, and the every-member case asserts non-empty markup for both a present and an absent featured product.
    - The live probe prints 1 for each of the two non-default members while that member is stored, and 1 for the default member after restoring, with no server restart between them.
    - `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm test` and `mise exec -- npm run build` all exit 0.
    - `mise exec -- npm run scan:tokens` reports 0 violations and exactly 2 manual-review rows.
    - The stored hero value is left at the default member when the task finishes.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/layout/home/ && SCAN="$(mktemp)" && mise exec -- npm run scan:tokens >| "$SCAN" 2>&1 && test "$(/usr/bin/grep -c '^MANUAL-REVIEW' "$SCAN")" -eq 2 && /usr/bin/grep -q '0 violations' "$SCAN" && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run build && (mise exec -- npm run dev >| /tmp/07-02-dev.log 2>&1 & echo $! >| /tmp/07-02-dev.pid) && sleep 45 && /usr/bin/curl -s -X POST http://localhost:3000/api/admin/settings -H 'Content-Type: application/json' -H 'x-dev-admin: mercora-dev-bypass' -d '{"updates":[{"key":"appearance.home_hero","value":"split","category":"appearance","data_type":"string"}]}' >| /dev/null && test "$(/usr/bin/curl -s http://localhost:3000/ | /usr/bin/grep -c 'data-home-hero="split"')" -eq 1 && /usr/bin/curl -s -X POST http://localhost:3000/api/admin/settings -H 'Content-Type: application/json' -H 'x-dev-admin: mercora-dev-bypass' -d '{"updates":[{"key":"appearance.home_hero","value":"full-bleed","category":"appearance","data_type":"string"}]}' >| /dev/null && test "$(/usr/bin/curl -s http://localhost:3000/ | /usr/bin/grep -c 'data-home-hero="full-bleed"')" -eq 1 && /usr/bin/curl -s -X POST http://localhost:3000/api/admin/settings -H 'Content-Type: application/json' -H 'x-dev-admin: mercora-dev-bypass' -d '{"updates":[{"key":"appearance.home_hero","value":"minimal","category":"appearance","data_type":"string"}]}' >| /dev/null && test "$(/usr/bin/curl -s http://localhost:3000/ | /usr/bin/grep -c 'data-home-hero="minimal"')" -eq 1 && kill "$(cat /tmp/07-02-dev.pid)"</automated>
    <fails_when>any stage exits non-zero; or a map or source-contract case fails, meaning the page branches on the enum name or the map drifted from the enum; or any of the three attribute counts is not exactly 1 — a zero means the stored value did not reach the render, and a count above one means more than one hero rendered; or the scan prints a violation line or a manual-review count other than 2.</fails_when>
  </verify>
  <done>The home route renders one of three named hero components chosen server-side from a stored enum value, the featured grid below it is untouched, and flipping the stored value changes the served HTML with no restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D1 `admin_settings` -> home page render | The stored hero value is untrusted input by the time the home page resolves it. |
| D1 product rows -> `next/image` src | The featured product's stored image fields are merchant-authored data that reaches an image element's source attribute. |
| server render -> browser | The resolved member name is written into a DOM data attribute and the hero copy is rendered as text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | the hero image source in the split and full-bleed variants | high | mitigate | Both variants resolve the image only through the shared product-image resolver, which normalises both stored shapes, fixes an R2 key with no leading slash, and returns a placeholder when there is nothing usable. No hero reads an image path from settings, from store config, or from any request-supplied value. Asserted by the source-contract behavior rows and by the no-image-data row. |
| T-07-06 | Information Disclosure | the full-bleed overlay markup | medium | mitigate | The overlay renders fixed copy as text children; no raw-HTML sink is used anywhere in either new variant, asserted directly by a behavior row. The copy is compile-time literal, not merchant- or customer-authored. |
| T-07-07 | Denial of Service | the split and full-bleed variants with an empty featured-products array | medium | mitigate | Both variants branch on the null prop before touching any product field and delegate to the centred hero, so an empty or fully-inactive featured category renders a complete page rather than an image element with no source. Covered by the two delegation behavior rows. |
| T-07-08 | Tampering | the stored hero value reaching the map lookup and the data attribute | medium | mitigate | The map is only ever indexed with a value plan 07-01's resolver already validated against the hero enum array, and every member is a lowercase alphanumeric-and-hyphen literal, so the attribute value cannot carry a quote or an angle bracket. |
| T-07-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan or this phase. `07-RESEARCH.md` records zero packages added; there is nothing to gate. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| Component | `components/layout/home/HomeHeroMinimal.tsx` |
| Component | `components/layout/home/HomeHeroSplit.tsx` |
| Component | `components/layout/home/HomeHeroFullBleed.tsx` |
| Setting key | `appearance.home_hero` |
| DOM attribute | `data-home-hero` on every hero variant root |
| Scanner exception | one inline region sentinel with a written reason in `HomeHeroFullBleed.tsx` |
| Test | `tests/unit/components/layout/home/home-hero-variants.test.ts` |
| Recording | `tests/unit/components/layout/home/__snapshots__/home-hero-minimal-preextraction.txt` |
| Changed shape | `app/page.tsx` resolves and renders a named hero component instead of an inline section |
</artifacts_this_phase_produces>

<verification>
Commands the SUMMARY must record:

1. `mise exec -- npx vitest run tests/unit/components/layout/home/` with the per-case pass count, run at the end of each task.
2. `mise exec -- npm run scan:tokens` — the violation count and the two manual-review file names, recorded after Task 2 specifically, since that is the task that introduces the literal-colour region.
3. `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm test`, `mise exec -- npm run build`.
4. The Task 3 live probe — all three attribute counts and confirmation the stored value was restored to the default.

The SUMMARY must record that the empty-featured-products case delegates to the centred hero, which
supersedes the research document's assumption A1 recommending a placeholder image instead.
</verification>

<success_criteria>
- A stored value for the hero switch decides which of three named components the home route renders, server-side, per request.
- The centred-text default is a proven verbatim extraction and the featured grid below it is untouched.
- A home page with no featured products renders a complete hero under every stored value.
- The phase's only literal-colour exception is fenced at the line with a written reason, and the token scan still reports zero violations with the two pre-existing manual-review rows.
</success_criteria>

<output>
Create `.planning/phases/07-layout-switches/07-02-SUMMARY.md` when done.
</output>
