---
phase: 08.1-v2-tech-debt-closure
plan: 06
type: execute
wave: 3
depends_on: ["08.1-01", "08.1-05"]
files_modified:
  - scripts/build-themes.mjs
  - tests/fixtures/themes/duplicate-token/dupe.css
  - tests/unit/scripts/build-themes.test.ts
  - tests/unit/lib/themes/token-contract.test.ts
  - lib/themes/active-theme.ts
  - docs/theming.md
autonomous: true
requirements: [DEBT-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A theme file that declares the same token twice in its one rule fails the validator, naming the token, with a file and a line (D-08, 06-REVIEW IN-01)."
    - "The kebab-to-camel mapping has exactly one implementation in the repository, exported from the build script and imported by the contract test (D-08, 06-REVIEW IN-02)."
    - "The active-theme resolver's two unknown-value outcomes funnel through one telemetry call and one return, with identical observable behaviour to today (D-08, 06-REVIEW IN-04)."
    - "ADJACENCY: the seven shipped theme files still validate cleanly and the generated manifest is unchanged — the new check adds a failure mode, not a false positive."
    - "EMPTY: a theme file with no declarations still fails on the pre-existing missing-token messages, not on the new duplicate check."
    - "docs/theming.md's validator-message table gains the new check's message, quoted exactly as the script produces it."
    - "06-REVIEW IN-03 is accounted for with its recorded resolution — the review itself asks for no code change beyond the fix already applied — so the phase's review-Info list closes with nothing left dangling."
  artifacts:
    - tests/fixtures/themes/duplicate-token/dupe.css
  key_links:
    - "scripts/build-themes.mjs kebabToCamel -> tests/unit/lib/themes/token-contract.test.ts (one implementation, imported)"
    - "the duplicate-token fixture -> the validator's per-token declaration count -> a named error with a line"
  prohibitions:
    - "MUST NOT change the 23-token contract, any file under themes/, or the generated manifest and barrel CSS. [flagged-unverified beyond the manifest freshness check and a git-diff check]"
    - "MUST NOT change any observable behaviour of the active-theme resolver — the collapse is a restructuring, and its existing suite must pass unedited. [flagged-unverified beyond the git-diff check on that suite]"
    - "MUST NOT loosen or reword any existing validator message — the doc table quotes them verbatim and prior phases grep for them. [flagged-unverified]"
---

<objective>
Close the three Phase 6 review Info items that CONTEXT D-08 names, on three commits: the validator
silently accepting a duplicated token declaration, the kebab-to-camel helper reimplemented in a
test, and the two near-identical unknown-value branches in the active-theme resolver. Account for
the fourth Info item with its own recorded resolution.

Purpose: retires the last of the carried-forward review Info list.
Output: a stricter validator with a fixture proving it, one implementation of the naming helper,
and a resolver whose two unknown outcomes share one tail.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-CONTEXT.md
@.planning/phases/06-theme-file-mechanism-presets/06-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The validator rejects a token declared twice</name>
  <files>scripts/build-themes.mjs, tests/fixtures/themes/duplicate-token/dupe.css, tests/unit/scripts/build-themes.test.ts, docs/theming.md</files>
  <read_first>
    - scripts/build-themes.mjs lines 165-210 (the `declared` set, the per-declaration walk, the colour-hex check, and the missing-token loop that follows)
    - tests/fixtures/themes/valid/good.css (the clean control fixture to copy as the base for the new one)
    - tests/unit/scripts/build-themes.test.ts lines 1-60 (`runBuildThemes`, the fixture-driven `it` shape, and the error-shape assertions)
    - docs/theming.md §"What the validator rejects" (the message table this task extends)
    - .planning/phases/06-theme-file-mechanism-presets/06-REVIEW.md §Info IN-01
  </read_first>
  <behavior>
    - The clean control fixture still passes with zero errors and one manifest entry.
    - A fixture whose one rule declares the same token twice fails with an error naming that token, carrying the fixture's file path and a line number greater than zero.
    - A fixture declaring one token three times produces exactly one error for that token, not two.
    - A fixture missing a token still fails on the missing-token message, unaffected by the new check.
    - All seven shipped theme files still validate cleanly and the generated manifest is byte-identical.
  </behavior>
  <action>
In `scripts/build-themes.mjs`, replace the `declared` set inside the declaration walk with a
structure that counts declarations per token — a `Map<string, number>` is enough. Keep the
non-token-declaration guard and the colour-hex check exactly as they are, and keep the
missing-token loop reading membership from the new structure. After the walk, push one error per
token whose count exceeds one, using the same `{ file, line, message }` shape the file's other
errors use, with the line of that token's *second* declaration so the message points at the
duplicate rather than the original. Word the message in the same voice as the file's neighbours,
naming the token and stating it is declared more than once in the rule; note in a comment that
CSS's own cascade means the second declaration silently wins, which is why this must be an error
rather than a warning.

Create `tests/fixtures/themes/duplicate-token/dupe.css` by copying the clean control fixture,
renaming its stem and selector to `dupe`, and adding a second declaration of one existing token
with a different value. Adjust its `@theme` header label to describe what it is for.

Add two cases to `tests/unit/scripts/build-themes.test.ts` alongside the existing fixture-driven
ones: the duplicate fixture fails naming the duplicated token with a positive line number, and a
token declared three times yields exactly one error for that token. Follow the file's existing
`runBuildThemes` shape and error-shape assertions exactly.

Add one row to `docs/theming.md`'s validator-message table quoting the new message exactly as the
script produces it, including its template placeholder, and positioned next to the other
declaration-level checks. Change no other row.
  </action>
  <acceptance_criteria>
    - A duplicated token declaration produces exactly one error naming the token, with a file and a positive line number.
    - Three declarations of one token still produce exactly one error.
    - The clean control fixture and every other existing fixture case behave exactly as before.
    - The seven shipped theme files validate and the generated manifest and barrel CSS are unmodified.
    - The doc's validator table quotes the new message and every pre-existing row is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/scripts/build-themes.test.ts && mise exec -- node scripts/build-themes.mjs --check && test -z "$(/usr/bin/git diff --name-only -- lib/themes/manifest.generated.ts themes/)" && test "$(mise exec -- node scripts/build-themes.mjs --path tests/fixtures/themes/duplicate-token --json 2>/dev/null | mise exec -- node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);console.log(r.errors.length)})")" -eq 1 && mise exec -- npm run lint</automated>
    <fails_when>The build-themes suite reports a failing assertion (the duplicate fixture passing, or producing two errors for one thrice-declared token); or `--check` exits non-zero because a shipped theme file now trips the new rule, meaning it is a false positive; or `git diff` prints a generated path, proving the manifest drifted; or the fixture's error count is not exactly 1; or eslint reports an error.</fails_when>
  </verify>
  <done>A copy-paste duplicate in a theme file now fails the build instead of silently winning the cascade.</done>
</task>

<task type="auto">
  <name>Task 2: One kebab-to-camel implementation, imported rather than retyped</name>
  <files>scripts/build-themes.mjs, tests/unit/lib/themes/token-contract.test.ts</files>
  <read_first>
    - scripts/build-themes.mjs lines 66-76 (the helper's definition and the `TOKEN_TS_KEYS` derivation that uses it, plus the file's other `export function` declarations for the shape to match)
    - tests/unit/lib/themes/token-contract.test.ts lines 40-60 (the byte-identical reimplementation and the code that calls it)
    - .planning/phases/06-theme-file-mechanism-presets/06-REVIEW.md §Info IN-02
  </read_first>
  <action>
Add `export` to the `kebabToCamel` declaration in `scripts/build-themes.mjs`, matching how the
file's other exported helpers are declared. Change nothing about its body or its existing internal
use.

In `tests/unit/lib/themes/token-contract.test.ts`, delete the local reimplementation and its
docstring and import the helper from the build script instead, alongside whatever that file
already imports. Every call site and every assertion in the test stays as it is — this removes a
second copy, it does not change what the test checks.
  </action>
  <acceptance_criteria>
    - `kebabToCamel` is exported from the build script and defined nowhere else in the repository.
    - The token-contract test imports it and all of its assertions still pass.
    - No behaviour of the build script changes; the generated manifest is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -rl 'function kebabToCamel' scripts tests lib app components | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 1 && /usr/bin/grep -q 'export function kebabToCamel' scripts/build-themes.mjs && /usr/bin/grep -q 'kebabToCamel' tests/unit/lib/themes/token-contract.test.ts && mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/scripts/build-themes.test.ts && mise exec -- node scripts/build-themes.mjs --check && test -z "$(/usr/bin/git diff --name-only -- lib/themes/manifest.generated.ts)"</automated>
    <fails_when>The definition count is not 1, proving a second copy survives somewhere; or the export is missing; or the token-contract suite reports a failing assertion because the imported helper behaves differently from the deleted copy; or the manifest freshness check fails or the generated file drifted.</fails_when>
  </verify>
  <done>The kebab-to-camel convention lives in one place and a change to it breaks the test that depends on it.</done>
</task>

<task type="auto">
  <name>Task 3: The resolver's two unknown outcomes share one tail</name>
  <files>lib/themes/active-theme.ts</files>
  <read_first>
    - lib/themes/active-theme.ts (whole file as it stands after Plan 01 — the string branch, the trimmed-empty case, the manifest-membership check, and the two `recordTelemetry`/`return` pairs)
    - tests/unit/lib/themes/active-theme.test.ts (the suite that must pass unedited — read the cases so you know exactly which observable behaviours are pinned)
    - .planning/phases/06-theme-file-mechanism-presets/06-REVIEW.md §Info IN-04
    - .planning/phases/06-theme-file-mechanism-presets/06-REVIEW.md §Info IN-03 (read it so you can record its resolution accurately)
  </read_first>
  <action>
Restructure `getActiveTheme()`'s tail so the "present but unknown string" outcome and the "present
non-string value" outcome funnel through a single `recordTelemetry("theme.unknown_selection", {
outcome: "invalid" })` call followed by a single
`return resolveEnvOrManifestDefault(manifestNames)`. Determine a boolean — the review's own
suggestion is a `isKnownString`-style flag — and let both paths fall through to the shared tail.
The three silent cases (absent/null, empty or whitespace-only, and a manifest match) keep their own
early returns exactly as they are: a silent default must stay silent and a match must stay a match.

Keep every comment that explains *why* a case is silent or why the stored string is never put into
a telemetry field — those are the reasons a future reader needs, and one of them records a threat
this milestone closed. Merge the two duplicated explanations into one where they now sit above a
shared tail.

`tests/unit/lib/themes/active-theme.test.ts` must pass with zero edits. If it does not, the
restructuring changed behaviour and is wrong — fix the code, not the test.

Record 06-REVIEW IN-03's resolution in this plan's SUMMARY rather than in code: the review itself
concludes no change is required beyond the fix already applied, and the wrapping scenario it
mentions is covered by the admin-settings screenshot rows the phase-8 QA pass already captured.
Naming it explicitly is what closes the review's Info list with nothing dangling.
  </action>
  <acceptance_criteria>
    - Exactly one `recordTelemetry` call and one `resolveEnvOrManifestDefault` return remain in the unknown-value tail.
    - All three silent cases still return early without telemetry.
    - `tests/unit/lib/themes/active-theme.test.ts` passes unedited.
    - No stored string is passed into any telemetry field.
    - 06-REVIEW IN-03's resolution is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/lib/themes/active-theme.test.ts tests/unit/lib/layout/settings.test.ts && test -z "$(/usr/bin/git diff --name-only -- tests/unit/lib/themes/active-theme.test.ts)" && test "$(/usr/bin/grep -v '^[[:space:]]*[*/]' lib/themes/active-theme.ts | /usr/bin/grep -c 'recordTelemetry(')" -eq 1 && ! /usr/bin/grep -Eq 'unstable_cache|globalThis\.' lib/themes/active-theme.ts && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test</automated>
    <fails_when>The resolver suite reports a failing assertion, proving the restructuring changed an observable outcome; or `git diff` prints the test path, proving the suite was edited to fit the new code; or the telemetry-call count is not exactly 1, meaning the branches were not actually collapsed; or a forbidden cache identifier appeared; or lint, tsc, or the full suite report an error.</fails_when>
  </verify>
  <done>Both unknown-value outcomes share one telemetry call and one return, with the resolver's behaviour provably unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme author's CSS file → generated manifest → every rendered surface | A hand-written source file becomes the colour contract for the whole storefront |
| stored `appearance.theme` value → telemetry event fields | An admin-controlled string reaches a log line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-25 | Tampering | `scripts/build-themes.mjs` validator | medium | mitigate | A duplicated token declaration previously shipped silently with the second value winning the cascade, so the manifest could disagree with the file a reviewer read. The new per-token count makes that a build failure with a line number. |
| T-08.1-26 | Information Disclosure | `theme.unknown_selection` telemetry | high | mitigate | The collapse must preserve the rule that no part of the stored string enters a telemetry field — the milestone closed this exact disclosure (T-06-06). The existing suite pins it and must pass unedited. |
| T-08.1-27 | Tampering | the generated manifest and barrel CSS | medium | mitigate | Both tasks that touch the build script assert the generated files are unmodified and that `--check` still passes, so a validator change cannot silently rewrite the contract. |
| T-08.1-28 | Repudiation | one kebab-to-camel implementation | low | mitigate | Two copies could diverge with no compiler or test failure forcing a sync; one imported implementation makes a convention change break loudly at its single site. |
</threat_model>

<verification>
- `mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test`
- `mise exec -- node scripts/build-themes.mjs --check`
- `mise exec -- npm run scan:tokens` reports 0 violations
- `test -z "$(/usr/bin/git diff --name-only -- lib/themes/manifest.generated.ts themes/)"`
</verification>

<success_criteria>
- A theme file declaring a token twice fails the build with a named, located error.
- The kebab-to-camel convention exists once and is imported where it is needed.
- The resolver's unknown-value handling is one tail with unchanged behaviour, and every Phase 6 review Info item is accounted for.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

| Artifact | Kind | Purpose |
|---|---|---|
| Per-token declaration counting in the validator | behaviour change | Closes 06-REVIEW IN-01 (D-08) |
| `tests/fixtures/themes/duplicate-token/dupe.css` | new fixture | The failing case that proves the new check |
| Exported `kebabToCamel` | API change | Closes 06-REVIEW IN-02 (D-08) |
| Collapsed unknown-value tail in the resolver | refactor | Closes 06-REVIEW IN-04 (D-08) |
| Recorded resolution of 06-REVIEW IN-03 | SUMMARY section | Closes the review's Info list with nothing dangling |
</artifacts_produced>

<output>
Create `.planning/phases/08.1-v2-tech-debt-closure/08.1-06-SUMMARY.md` when done.
Record 06-REVIEW IN-01, IN-02, and IN-04 as closed in code and IN-03 as closed by recorded
resolution, completing D-08.
</output>
</content>
