---
phase: 08.1-v2-tech-debt-closure
plan: 05
type: execute
wave: 2
depends_on: ["08.1-01", "08.1-03"]
files_modified:
  - components/layout/product/ProductGalleryLeft.tsx
  - components/layout/product/ProductGalleryTop.tsx
  - components/layout/product/gallery-media-url.ts
  - app/product/[slug]/ProductDisplay.tsx
  - tests/unit/components/layout/product/product-gallery-variants.test.ts
  - tests/unit/app/layout-switch-contract.test.ts
  - docs/theming.md
files_deleted:
  - components/layout/product/gallery-media-url.ts
autonomous: true
requirements: [DEBT-01]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One image-URL resolver serves both product gallery variants and the product cards; the duplicated helper is gone from the tree (D-05)."
    - "The accepted output change is exactly one rule: a bare relative path gains a leading slash, which is the correct root-relative form (D-11). Absolute URLs, already-rooted paths, and the placeholder path are byte-identical to before."
    - "The pre-extraction parity recording is not regenerated; the two changed source lines are declared as named substitutions, so every other recorded line still has to match character for character."
    - "EMPTY: an empty image array still renders the main image at the placeholder path and no thumbnail buttons."
    - "ZERO-ONE-MANY: zero, one, and many images each render the counts the existing suite already pins."
    - "productGallery is a required prop with no default, and its only caller supplies it (D-08, 07-REVIEW IN-01) — a pure type-tightening with no runtime change."
    - "docs/theming.md no longer lists two coexisting image resolvers as a known limit."
  artifacts: []
  key_links:
    - "both gallery variants -> lib/utils/product-image.ts resolveProductImageSrc -> next/image src"
    - "app/product/[slug]/page.tsx -> ProductDisplay's required productGallery prop -> the gallery map"
  prohibitions:
    - "MUST NOT regenerate, edit, or delete the pre-extraction recording under __snapshots__ — it is the parity oracle. [flagged-unverified beyond the git-status check]"
    - "MUST NOT change lib/utils/product-image.ts — the surviving resolver is adopted as it is, not adjusted to match the retired one. [flagged-unverified beyond the git-diff check]"
    - "MUST NOT change either gallery's markup, class names, data attributes, ordering, or event wiring — only the src expression changes. [flagged-unverified]"
    - "MUST NOT change the 23-token contract, any file under themes/, or any other variant component. [flagged-unverified]"
---

<objective>
Retire the duplicated product-image helper in favour of the one resolver the rest of the app
already uses, accepting the single documented output difference (D-05, D-11), and make the
product gallery's layout prop required now that its only caller always supplies it
(D-08 / 07-REVIEW IN-01).

Purpose: closes two v2 audit lines that both live in the product display, on two commits.
Output: one resolver in the tree, a parity oracle that still holds, and a tightened prop type.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-CONTEXT.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-RESEARCH.md
@lib/utils/product-image.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One image resolver for both gallery variants</name>
  <files>components/layout/product/ProductGalleryLeft.tsx, components/layout/product/ProductGalleryTop.tsx, components/layout/product/gallery-media-url.ts, tests/unit/components/layout/product/product-gallery-variants.test.ts, tests/unit/app/layout-switch-contract.test.ts, docs/theming.md</files>
  <read_first>
    - lib/utils/product-image.ts lines 90-110 (`resolveProductImageSrc`: its three parameters, the leading-slash rule, and the default placeholder filename)
    - components/layout/product/ProductGalleryLeft.tsx (whole file — the two call sites at the main image and the thumbnail loop)
    - components/layout/product/ProductGalleryTop.tsx (whole file — the same two call sites)
    - components/layout/product/gallery-media-url.ts (the retiring helper, so you can confirm which of its branches are live under the real call pattern)
    - tests/unit/components/layout/product/product-gallery-variants.test.ts lines 27-180 (the helper's own describe block, the `KNOWN_SUBSTITUTIONS` map, and every render assertion whose fixture is a bare relative path)
    - tests/unit/components/layout/product/__snapshots__/product-gallery-left-preextraction.txt lines 1-30 (the two recorded `src=` lines)
    - tests/unit/app/layout-switch-contract.test.ts lines 230-250 (`PHASE_COMPONENT_FILES`, which names the retiring module)
    - docs/theming.md §"Known limits and backlog" (the two-resolvers row)
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-RESEARCH.md §Pitfall 2 and CONTEXT.md D-11
  </read_first>
  <behavior>
    - An absolute http(s) URL passes through unchanged.
    - A path that already starts with a slash passes through unchanged.
    - A bare relative path gains a leading slash — the one accepted output change (D-11).
    - A null, undefined, or unusable input renders the placeholder path `/placeholder.jpg`, byte-identical to today.
    - Both galleries still render one main image plus one thumbnail button per image, in the given order, with the accent border on the selected thumbnail and the neutral border on the others.
    - An empty image array still renders the placeholder main image and no thumbnail buttons.
    - Both galleries still carry their own `data-product-gallery` attribute exactly once.
  </behavior>
  <action>
<!-- planner-discipline-allow: gallery-media-url -->
Replace both `getMediaUrl(...)` call sites in `ProductGalleryLeft.tsx` and both in
`ProductGalleryTop.tsx` with `resolveProductImageSrc(<value>, undefined, "/placeholder.jpg")`
imported from `@/lib/utils/product-image`. Passing the placeholder explicitly is what keeps the
fallback filename identical; the surviving resolver's own default differs and must not be relied
on. Then delete `components/layout/product/gallery-media-url.ts`. Change nothing else in either
component: the markup, class names, `data-product-gallery` attributes, thumbnail ordering, and
`onSelect` wiring all stay byte-identical.

Do not modify `lib/utils/product-image.ts`. The leading-slash insertion on a bare relative path is
D-11's accepted, deliberate output change — a bare path resolves against the current route and is
not a valid image source, so the rooted form is the correction, not a regression.

In `tests/unit/components/layout/product/product-gallery-variants.test.ts`:
- Replace the retiring helper's own describe block with an equivalent one exercising
  `resolveProductImageSrc` through the exact three-argument call shape the components now use,
  covering all four behaviours above. The bare-path case asserts the rooted result per D-11.
- Add the two changed source lines to `KNOWN_SUBSTITUTIONS`, mapping each recorded `src=` line to
  its replacement, and extend that `it`'s description so it names four permitted substitutions
  instead of two. The recording file itself is not touched — every other recorded line must still
  match character for character, which is what keeps this a parity proof rather than a rewrite.
- Update the render assertions whose fixtures are bare relative paths so they expect the rooted
  form (`a.jpg` becomes `/a.jpg`, and likewise for the other bare fixtures). This is the same
  single D-11 behaviour change; list every updated expectation in the SUMMARY so none of it is
  silent. The placeholder assertion and every count, ordering, class, and attribute assertion stay
  exactly as they are.

In `tests/unit/app/layout-switch-contract.test.ts`, remove the retired module's path from
`PHASE_COMPONENT_FILES` — that list reads each file from disk and would otherwise fail on a
missing file. Leave every other entry and every assertion untouched.

In `docs/theming.md` §"Known limits and backlog", delete the row recording two coexisting
image resolvers. Leave the other rows alone.
  </action>
  <acceptance_criteria>
    - The retired helper file no longer exists and nothing under `app/`, `components/`, `lib/`, or `tests/` imports it.
    - Both galleries call `resolveProductImageSrc` with an explicit `/placeholder.jpg` third argument.
    - `lib/utils/product-image.ts` is unmodified.
    - No file under `__snapshots__` is modified.
    - The parity `it` declares four named substitutions and still passes.
    - Every render count, ordering, class, and attribute assertion passes unchanged; only bare-path `src` expectations gained a leading slash.
    - `docs/theming.md` no longer lists two coexisting image resolvers.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e components/layout/product/gallery-media-url.ts && ! /usr/bin/grep -rq 'gallery-media-url' app components lib tests && test -z "$(/usr/bin/git diff --name-only -- lib/utils/product-image.ts)" && test -z "$(/usr/bin/git status --porcelain -- 'tests/unit/components/layout/product/__snapshots__')" && test "$(/usr/bin/grep -v '^[[:space:]]*[*/]' components/layout/product/ProductGalleryLeft.tsx | /usr/bin/grep -c 'resolveProductImageSrc')" -eq 3 && test "$(/usr/bin/grep -v '^[[:space:]]*[*/]' components/layout/product/ProductGalleryTop.tsx | /usr/bin/grep -c 'resolveProductImageSrc')" -eq 3 && mise exec -- npx vitest run tests/unit/components/layout/product/product-gallery-variants.test.ts tests/unit/app/layout-switch-contract.test.ts tests/unit/lib/utils/product-image.test.ts && mise exec -- npm run typecheck && mise exec -- npm run lint</automated>
    <fails_when>The file still exists or a reference to it survives anywhere in the four source trees; or `git diff` shows the surviving resolver was edited to paper over the difference; or `git status --porcelain` prints a line, proving the parity recording was regenerated instead of substituted; or a `resolveProductImageSrc` count is not 3 (one import plus two call sites), meaning a call site was missed or duplicated; or the parity/render suite reports a failing assertion; or tsc/eslint report an error.</fails_when>
  </verify>
  <done>One resolver serves both galleries, the parity oracle still holds against the untouched recording, and the accepted leading-slash change is written down.</done>
  <reversibility rating="reversible">Deleting a small internal helper and swapping two import sites; restoring it is a revert of one commit with no data or contract consequences.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The product gallery prop becomes required</name>
  <files>app/product/[slug]/ProductDisplay.tsx, tests/unit/components/layout/product/product-gallery-variants.test.ts</files>
  <read_first>
    - app/product/[slug]/ProductDisplay.tsx lines 105-145 (the prop's docstring, its optional declaration, and the destructured default)
    - app/product/[slug]/page.tsx (confirm for yourself that the only caller always supplies the prop before removing the default)
    - tests/unit/components/layout/product/product-gallery-variants.test.ts lines 180-300 (the `ProductDisplay` source-contract assertions, which read the file's text and will see the changed lines)
    - .planning/phases/07-layout-switches/07-REVIEW.md §Info IN-01
  </read_first>
  <action>
In `app/product/[slug]/ProductDisplay.tsx`, drop the `?` from the `productGallery` prop
declaration and drop its destructured default. Rewrite the prop's docstring: it no longer explains
an optional fallback that exists because a later task will wire the caller — that wiring shipped in
Phase 7, so the prop is now required and every caller supplies it. Keep the sentence pinning it to
the frozen union rather than a bare string. If removing the default leaves `DEFAULT_LAYOUTS`
unused in this file, remove its import too; if it is still used elsewhere in the file, leave it.

Do not change any other prop, any rendering logic, or the gallery map. This is a type-tightening
with no runtime change in the shipped app — `app/product/[slug]/page.tsx` already always passes
the prop.

Update any source-contract assertion in the gallery test file that quotes the changed lines, so it
matches the required form. Do not weaken any of those assertions — in particular the one that
forbids a bare `layout`/`variant` prop name must keep matching.
  </action>
  <acceptance_criteria>
    - `productGallery` is declared without `?` and destructured without a default.
    - `app/product/[slug]/page.tsx` is unmodified.
    - No unused import remains in `ProductDisplay.tsx`.
    - Every source-contract assertion in the gallery test file still passes, including the anti-genericity one.
    - Typecheck, lint, and the full suite are green.
  </acceptance_criteria>
  <verify>
    <automated>! /usr/bin/grep -q 'productGallery?:' 'app/product/[slug]/ProductDisplay.tsx' && ! /usr/bin/grep -q 'productGallery = DEFAULT_LAYOUTS' 'app/product/[slug]/ProductDisplay.tsx' && /usr/bin/grep -q 'productGallery: ProductGallery' 'app/product/[slug]/ProductDisplay.tsx' && test -z "$(/usr/bin/git diff --name-only -- 'app/product/[slug]/page.tsx')" && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run scan:tokens</automated>
    <fails_when>The optional declaration or the destructured default survives; or the required declaration is absent; or `git diff` shows the page was edited to compensate, which would mean the prop was not actually always supplied; or eslint reports an unused import; or tsc reports a type error at a call site that omits the prop; or the full suite or the colour scan reports a failure.</fails_when>
  </verify>
  <done>The prop is required, the compiler enforces it, and no caller needed changing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product media records → `next/image` `src` | Merchant- and import-sourced URL strings become the source of a rendered image |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-21 | Tampering | image `src` resolution in both gallery variants | medium | mitigate | Consolidating on one resolver removes a second, differently-behaved code path that could be reasoned about incorrectly during review. The surviving resolver is adopted unmodified and its own suite (`tests/unit/lib/utils/product-image.test.ts`) runs in this task's verify. |
| T-08.1-22 | Tampering | the parity recording as an oracle | high | mitigate | The recording is never regenerated in this plan; the two changed lines are declared as named substitutions so every other line must still match character for character, and a `git status --porcelain` check over the snapshot directory fails the task if it was rewritten. |
| T-08.1-23 | Information Disclosure | a bare relative path resolving against the current route | low | mitigate | The accepted leading-slash change is the fix: a bare path previously resolved against whatever route the user was on, producing a wrong request; the rooted form always addresses the intended asset. |
| T-08.1-24 | Denial of Service | a missing image record | low | accept | Both galleries fall back to the placeholder path exactly as they do today; the empty-array case is pinned by an existing assertion. |
</threat_model>

<verification>
- `mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test`
- `mise exec -- npm run scan:tokens` reports 0 violations
- `mise exec -- node scripts/build-themes.mjs --check`
- `test -z "$(/usr/bin/git status --porcelain -- 'tests/unit/components/layout/*/__snapshots__')"`
- The visual proof of no regression is Plan 07's volt-dark product-route recapture (D-11)
</verification>

<success_criteria>
- One image resolver remains in the tree and both gallery variants use it.
- The parity recording is untouched and its suite still passes.
- The product gallery prop is required and the compiler enforces it.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

| Artifact | Kind | Purpose |
|---|---|---|
| Retired duplicate image helper | deletion | Closes the two-resolver audit line (D-05) |
| Two new `KNOWN_SUBSTITUTIONS` entries | parity contract | Keeps the untouched recording a real oracle across the swap |
| Required `productGallery` prop | type tightening | Closes 07-REVIEW IN-01 (D-08) |
| The written record of D-11's accepted output change | SUMMARY section | Every updated expectation listed, none silent |
</artifacts_produced>

<output>
Create `.planning/phases/08.1-v2-tech-debt-closure/08.1-05-SUMMARY.md` when done.
List every test expectation the leading-slash change touched, and record D-05 and the
07-REVIEW IN-01 half of D-08 as closed. Note that D-11's visual proof lands in Plan 07.
</output>
</content>
