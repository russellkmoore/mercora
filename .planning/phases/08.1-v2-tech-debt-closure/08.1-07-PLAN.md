---
phase: 08.1-v2-tech-debt-closure
plan: 07
type: execute
wave: 4
depends_on: ["08.1-01", "08.1-02", "08.1-03", "08.1-04", "08.1-05", "08.1-06"]
files_modified:
  - data/d1/seed-dev.sql
  - tests/unit/scripts/dev-seed-guard.test.ts
  - docs/theming.md
  - .planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md
  - .planning/v2-MILESTONE-AUDIT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEBT-01]
user_setup:
  - service: local-order-status-links
    why: "The guest order-status page is gated by a signed bearer token that only exists when two local environment variables are set; without them the route 404s before its first database read and cannot be screenshotted."
    env_vars:
      - name: ORDER_STATUS_GUEST_LINKS_ENABLED
        source: "Local `.env.local` — the literal value `true` (the shipped example sets it to false)"
      - name: ORDER_STATUS_SECRET
        source: "Local `.env.local` — 32+ random characters, generated with `openssl rand -hex 32`; never printed, never committed"

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A local development seed inserts one paid guest order with one line item at a fixed, documented id, so /order-status/[id] renders locally (D-09)."
    - "The fixture is development-only: it lives in the seed file that only the local database helper applies, and no production or preview seed path can reach it."
    - "The seed is idempotent — re-running it on every dev start neither duplicates the order nor errors."
    - "EMPTY: an order whose items array is empty still renders the order-status page rather than erroring; the guest projection maps an absent items array to an empty list."
    - "The order-status route is captured at both viewports and both states under all seven presets, recorded in 08.1-SCREENSHOTS.md (D-09)."
    - "D-11's visual proof: the product route captured under volt-dark with the default layout combination is compared against the Phase 8 volt-dark run's recorded capture, and the outcome — identical or a written judgement — is recorded."
    - "UI contrast contract: every one of the 35 preset-by-role cells in 08.1-UI-SPEC.md clears WCAG AA, and the faint Go-home border measured at 1.19–1.73:1 is a pre-existing sitewide hairline treatment, not a defect introduced by this phase."
    - "The bearer token is minted at capture time and never written into a seed file, a manifest, a document, or a summary."
    - "The v2 audit's tech-debt list is annotated line by line with the plan that closed each item, and DEBT-01 is marked satisfied."
  artifacts:
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md
    - tests/unit/scripts/dev-seed-guard.test.ts
  key_links:
    - "data/d1/seed-dev.sql -> scripts/db-local-ensure.mjs (--local only) -> the local orders table -> /order-status/<id>?token=<minted>"
    - "createOrderStatusToken(orderId) at capture time -> screenshot-routes.mjs --order-id -> the captured cell"
  prohibitions:
    - "MUST NOT print, log, commit, or record any value of ORDER_STATUS_SECRET or any minted token in a plan record, a manifest, a document, or a summary. [flagged-unverified beyond the secret-shaped grep]"
    - "MUST NOT add the development order fixture to data/d1/seed.sql or any migration — it is local-only. [flagged-unverified beyond the guard test]"
    - "MUST NOT modify .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md or 08-QA-MATRIX.md — the harness's default manifest path points at the first of those and has caused one real incident. [flagged-unverified beyond the git-diff check]"
    - "MUST NOT touch the broken products row in data/d1/seed.sql unless the fix is a single value on one line. [flagged-unverified]"
    - "MUST NOT change the 23-token contract, any file under themes/, or any variant component. [flagged-unverified]"
---

<objective>
Give the guest order-status route a local order to render, capture it across all seven presets,
carry out D-11's visual proof for the image-resolver swap, and close the phase: annotate the v2
audit's tech-debt list line by line and mark DEBT-01 satisfied.

Purpose: closes D-09, supplies D-11's visual evidence, and turns seven plans' worth of work into
a closed audit list.
Output: an idempotent dev-only order fixture with a guard test, a new screenshot manifest with 28
order-status cells, and an annotated audit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-CONTEXT.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-UI-SPEC.md
@.planning/v2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A dev-only, idempotent guest order fixture</name>
  <files>data/d1/seed-dev.sql, tests/unit/scripts/dev-seed-guard.test.ts, docs/theming.md</files>
  <read_first>
    - data/d1/seed-dev.sql (the existing single row and its `ON CONFLICT ... DO UPDATE` idempotency, and the file's opening comment stating production and preview never execute it)
    - scripts/db-local-ensure.mjs (confirm for yourself it is the only consumer, and that it passes `--local`)
    - lib/db/schema/order.ts (the orders columns, the JSON-mode fields, and the money shape)
    - lib/order-status/guest-projection.ts (exactly which order fields the guest page reads — nothing else needs to be realistic)
    - app/order-status/[id]/page.tsx lines 38-70 (the token gate that runs before the first database read)
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-RESEARCH.md §"data/d1/seed-dev.sql idempotency pattern to follow"
    - docs/theming.md §"The two gates and the screenshot harness" (where the local-capture note belongs)
  </read_first>
  <precondition>`npx wrangler d1 ... --local` can reach the local database; `scripts/db-local-ensure.mjs` runs on `predev` and applies this file on every dev start.</precondition>
  <behavior>
    - Applying the seed twice in a row inserts exactly one order row and raises no error.
    - The fixture order has a null customer id, a paid payment status, a shipped status, one line item with a product name and a quantity, a tracking number, and a created timestamp.
    - The guest projection built from the fixture exposes an order number, a placed date, a status, a tracking number, and one item.
    - `data/d1/seed.sql` and every file under `migrations/` contain no reference to the fixture's order id.
    - `data/d1/seed-dev.sql` is referenced by exactly one script, and that script targets the local database.
  </behavior>
  <action>
Append one `INSERT INTO orders (...) VALUES (...) ON CONFLICT(id) DO NOTHING;` statement to
`data/d1/seed-dev.sql`, using the fixed id `dev-order-001`. Follow the columns and JSON shapes the
schema defines: `total_amount` and the item prices are stored-money objects (`{"amount": <minor
units>, "currency": "USD"}`), `items` is a JSON array with one entry carrying at least
`product_name` and `quantity`, `shipping_address` is an address object, `customer_id` is `NULL` —
that null is what makes it a guest order, which is the only kind the token-gated page serves — and
`payment_status` is `'paid'` with `status` `'shipped'` plus a `tracking_number` so the page has a
shipment section to render. Give the statement a comment above it explaining that it exists so the
guest order-status route can be screenshotted locally, that the id is fixed and documented so the
capture command can hardcode it, and that the conflict clause is what keeps `predev` re-runnable.

Do not create a second seed file: the local database helper reads only this one, so a new file
would silently never run. Do not touch `data/d1/seed.sql`.

Write `tests/unit/scripts/dev-seed-guard.test.ts` asserting the four static guarantees: the dev
seed file contains the fixture id with an `ON CONFLICT` clause; `data/d1/seed.sql` and every file
under `migrations/` contain no occurrence of the fixture id; the dev seed file's path appears in
exactly one file under `scripts/`; and that referencing script passes `--local`. This is the guard
that keeps a development fixture out of any production seed path.

Add a short note to `docs/theming.md` §"The two gates and the screenshot harness" recording the
fixture's order id and that capturing `/order-status/<id>` locally also needs
`ORDER_STATUS_GUEST_LINKS_ENABLED=true` and an `ORDER_STATUS_SECRET` of at least 32 characters in
the developer's own `.env.local`, with the token minted at capture time via
`createOrderStatusToken`. Name the two variables; never write a value for either.
  </action>
  <acceptance_criteria>
    - Applying the seed twice yields exactly one `dev-order-001` row and no error.
    - The fixture is a guest order (null customer id), paid and shipped, with one item and a tracking number.
    - The fixture id appears in no production seed and no migration.
    - The dev seed file is referenced by exactly one script, which targets the local database.
    - `docs/theming.md` names the order id and the two required variables, and contains no secret value.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run db:prepare:local && mise exec -- npm run db:prepare:local && test "$(mise exec -- npx wrangler d1 execute mercora-db --local --json --command "SELECT COUNT(*) AS n FROM orders WHERE id='dev-order-001'" | mise exec -- node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log(j[0].results[0].n)})")" -eq 1 && mise exec -- npx vitest run tests/unit/scripts/dev-seed-guard.test.ts && ! /usr/bin/grep -rq 'dev-order-001' data/d1/seed.sql migrations/ && /usr/bin/grep -q 'dev-order-001' docs/theming.md && ! /usr/bin/grep -rqE '[0-9a-f]{32,}' docs/theming.md</automated>
    <fails_when>The second `db:prepare:local` run errors on a primary-key conflict, proving the statement is not idempotent; or the row count is not exactly 1; or the guard test reports a failing assertion (the fixture id found in a production seed or a migration, or the dev seed reachable from more than one script); or the fixture id is missing from the doc; or the long-hex grep matches, meaning something secret-shaped was written into the document.</fails_when>
  </verify>
  <done>A fixed, idempotent, guest order exists locally, provably unreachable from any production seed path, and the doc says how to use it.</done>
  <reversibility rating="reversible">One appended SQL statement in a development-only file; deleting it removes the row on the next local rebuild with no production consequence.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Capture order-status under all seven presets, and prove the resolver swap changed nothing visually</name>
  <files>.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md</files>
  <read_first>
    - .planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md lines 1-70 (the capture command with all four flags, the theme-switch method, the combination table, and why `--manifest` is always passed explicitly)
    - .planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md §Label `phase-08-a-volt-dark` (the recorded product row hashes this task compares against)
    - scripts/screenshot-routes.mjs lines 100-135 and 165-185 (the route grid, how `--order-id` is interpolated, and the localhost guard)
    - lib/order-status/token.ts (the `createOrderStatusToken(orderId)` signature and the two environment variables it requires)
    - lib/auth/admin-middleware.ts lines 20-35 (the development-only header bypass; read its value at run time, never write it down)
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-UI-SPEC.md §Per-preset contrast floor (the 35-cell contract and the flagged Go-home border assumption)
  </read_first>
  <action>
Create `.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md` with a header before any
capture runs, modelled on the Phase 8 matrix: what the file records, that captured images live
under the git-ignored screenshots directory and are never committed, the exact capture command
including the explicit `--manifest` flag pointing at this file, the theme-switch method, the
fixture order id, and the single layout combination used (the defaults — combination A — since the
order-status route is unaffected by all three layout switches, so crossing it with B and C would
add rows and no information). State plainly that the bearer token is minted per session and is
never written into this file.

Set up the local environment for the run. The guest-link variables must be visible to the Next.js
development server, and this project's environment loader lets the local environment file win over
a shell export, so append the two variables to `.env.local` rather than exporting them: generate
the secret with `openssl rand -hex 32` and append both lines with `>>` in a single command that
never reads the file and never echoes the value. Appending is safe to repeat — the last occurrence
of a key wins — and the file is git-ignored. Confirm afterwards that `git status --porcelain`
reports nothing for it.

Start the dev server, wait for it to answer, then mint the token by importing
`createOrderStatusToken` and calling it with the fixture id, capturing the result into a shell
variable only. Confirm the route works before capturing anything: request
`/order-status/dev-order-001?token=<token>` and check for a 200 rather than a 404 — a 404 means
the variables did not reach the server, and the run must stop and be fixed rather than recording
28 missing cells.

Then run seven captures, one per preset, in the order the Phase 8 matrix uses: volt-dark, luxe,
midnight, clinical, retro, atelier, market. Before each, write the theme and the three
combination-A layout values in one request to the admin settings endpoint with the development
header, read the appearance category back and confirm all four values, and fetch the storefront
root and confirm its rendered theme attribute matches — the same two confirmations the Phase 8
method requires. Label each run `phase-08.1-a-<theme>`, pass `--manifest` pointing at this file,
`--allow-missing`, `--include-content`, and `--order-id "dev-order-001?token=<token>"`.

For D-11's visual proof, compare this run's volt-dark `product__1280__resting.png` against the
Phase 8 volt-dark combination-A capture of the same cell, whose SHA-256 is recorded in the Phase 8
matrix and whose file is still on disk. Record the outcome in this file: if the hashes match, the
image-resolver swap is byte-identical proof; if they differ, compare the two images and write a
short judgement naming what changed and whether it is the accepted headless-rendering residual the
Phase 8 matrix already documents or a real regression. Do not treat a difference as automatically
acceptable, and do not overwrite the Phase 8 record.

Restore the appearance settings to the defaults recorded at the start, confirm the restore by
reading the category back, stop the dev server, and confirm port 3000 is free. Record in this file
the contrast-contract line from the UI spec: all 35 preset-by-role cells clear the AA threshold,
and the faint Go-home border is a pre-existing sitewide hairline treatment rather than a defect
this phase introduced — so a reader of these captures does not re-raise it.
  </action>
  <acceptance_criteria>
    - Exactly seven capture labels exist in the file, one per preset, all distinct.
    - Every label carries four order-status rows with a real image path and hash — 28 in total, none missing.
    - The token gate was confirmed working before capture began.
    - The D-11 comparison outcome is recorded with either a hash match or a written judgement.
    - The appearance settings were restored and confirmed, and port 3000 is free.
    - No secret value or minted token appears anywhere in the file.
    - `05-SCREENSHOTS.md` and `08-QA-MATRIX.md` are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>M=.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md; test "$(/usr/bin/grep -c '^## Label: ' "$M")" -eq 7 && test "$(/usr/bin/grep -o '^## Label: `[^`]*`' "$M" | /usr/bin/sort -u | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 7 && for t in volt-dark luxe midnight clinical retro atelier market; do /usr/bin/grep -qF "phase-08.1-a-$t" "$M" || { echo "MISSING RUN: $t"; exit 1; }; done && test "$(/usr/bin/grep -cE '^\| order-status \| (1280|390) \| (resting|nav-open) \| \.screenshots/[^|]*\.png \| [0-9a-f]{64} \|' "$M")" -eq 28 && ! /usr/bin/grep -qE '\?token=[A-Za-z0-9_.-]{10,}' "$M" && test -z "$(/usr/bin/git diff --name-only -- .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md .planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md)" && test "$(/usr/bin/ls -d .screenshots/phase-08.1-a-* 2>/dev/null | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 7 && ! /usr/bin/nc -z localhost 3000 2>/dev/null && /usr/bin/grep -q '71d237eb450c301fc0d7205b9cf03c40719904f938193c16ae617d57b48d4ff4' "$M"</automated>
    <fails_when>A label count other than 7, or a duplicate label, proving a run was skipped or repeated; or the loop prints MISSING RUN for a preset; or the order-status row count is not 28, which is the exact signal that the token gate was not working and the harness recorded missing cells; or the token-shaped grep matches, meaning a bearer token was written into a committed record; or `git diff` prints a prior phase's manifest path, proving captures landed in the wrong file again; or the screenshot directory count is not 7; or `nc` succeeds, proving the dev server was left running; or the Phase 8 comparison hash is absent, meaning D-11's proof was never recorded.</fails_when>
  </verify>
  <done>Twenty-eight order-status cells exist across all seven presets, the D-11 comparison outcome is written down, and the environment is back to its defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Close the audit list line by line and mark the requirement satisfied</name>
  <files>.planning/v2-MILESTONE-AUDIT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/v2-MILESTONE-AUDIT.md lines 1-50 (the `tech_debt` front-matter list, grouped by phase, and the `status: tech_debt` field)
    - .planning/REQUIREMENTS.md line 66 and the traceability table row for DEBT-01
    - The six preceding SUMMARY files in this phase directory (each names the audit lines it closed)
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-CONTEXT.md §Deferred Ideas (the items that stay open and must not be marked closed)
  </read_first>
  <action>
Annotate every entry in `.planning/v2-MILESTONE-AUDIT.md`'s `tech_debt` list with its outcome,
reading the six preceding SUMMARY files for what each plan actually closed rather than assuming
from this plan. For each item append either the plan that closed it and how, or an explicit
statement that it remains open and why — the deferred items in CONTEXT.md (the Cloudflare Workers
build variable, the Clerk admin walkthrough, the direction-doc contract extensions, the
accessibility screen-reader row, and the accepted two-pixel rendering residual) stay open and must
be marked as such, not quietly dropped. The order-status screenshot gap in the Phase 5 entry is
closed by this plan; the other three capture gaps in that same entry (the Stripe payment step and
the two Clerk-session routes) are not, and must stay listed.

Update the front-matter `status` only if every non-deferred item is closed; otherwise leave it and
say in the annotation which non-deferred item remains. Do not remove any list entry — an audit
that deletes its own history cannot be re-read.

In `.planning/REQUIREMENTS.md`, tick the DEBT-01 checkbox and set its traceability-table row to
Complete. Change nothing else in that file.

Write the phase SUMMARY so a reader can go from the audit's list to the commit that closed each
line without opening the code.
  </action>
  <acceptance_criteria>
    - Every `tech_debt` entry carries an annotation naming either the closing plan or the reason it stays open.
    - No entry was deleted.
    - Every CONTEXT deferred item is explicitly marked as still open.
    - DEBT-01 is ticked in `.planning/REQUIREMENTS.md` and its traceability row reads Complete.
    - No other requirement's state changed.
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/v2-MILESTONE-AUDIT.md; test "$(/usr/bin/grep -c '^      - "' "$A")" -ge 13 && for p in 08.1-01 08.1-02 08.1-03 08.1-04 08.1-05 08.1-06; do /usr/bin/grep -qF "$p" "$A" || { echo "PLAN NOT CITED IN AUDIT: $p"; exit 1; }; done && /usr/bin/grep -q '^- \[x\] \*\*DEBT-01\*\*' .planning/REQUIREMENTS.md && /usr/bin/grep -qE '^\| DEBT-01 \| Phase 8\.1 \| Complete \|' .planning/REQUIREMENTS.md && test "$(/usr/bin/grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md)" -eq 0 && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run scan:tokens && mise exec -- node scripts/build-themes.mjs --check && mise exec -- npm run build</automated>
    <fails_when>The entry count drops below the audit's original thirteen, proving a line was deleted rather than annotated; or the loop prints PLAN NOT CITED for a plan whose closed items are unrecorded; or the DEBT-01 checkbox or its traceability row is not Complete; or an unticked requirement remains in the file; or any full gate — lint, tsc, the unit suite, the colour scan, the manifest freshness check, or the production build — reports a failure, which would mean the phase does not actually close.</fails_when>
  </verify>
  <done>Every audit line names its outcome, the deferred items stay visibly open, DEBT-01 is satisfied, and every gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local `.env.local` → the developer's machine | A generated signing secret is written to a git-ignored file |
| minted bearer token → screenshot manifest / SUMMARY | A credential passes through a shell that also writes committed records |
| development seed file → any database | A fabricated order row could reach an environment it was never meant for |
| captured images → the repository | Screenshots of an order-status page depict order contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-29 | Information Disclosure | `ORDER_STATUS_SECRET` and the minted token | high | mitigate | The secret is generated and appended to a git-ignored file in one command that never reads or echoes it; the token lives only in a shell variable. Two greps gate the records: a long-hex match on the document and a token-shaped match on the manifest. `git status --porcelain` must report nothing for the environment file. |
| T-08.1-30 | Spoofing | the development-only admin header bypass used for theme switching | high | accept | Pre-existing, environment-gated infrastructure this phase only consumes; its value is read at run time and never written into any record, matching the Phase 8 method. The bypass is inert outside a development build. |
| T-08.1-31 | Tampering | the development order fixture reaching a production seed | high | mitigate | The fixture lives only in the file the local-only database helper applies; `tests/unit/scripts/dev-seed-guard.test.ts` asserts the id appears in no production seed and no migration, and that the dev seed is reachable from exactly one script that passes `--local`. |
| T-08.1-32 | Information Disclosure | captured order-status images | medium | mitigate | Images stay under the git-ignored screenshots directory and are never committed; only the manifest row — route, viewport, state, path, hash — is recorded, and the depicted order is a fabricated fixture with no real customer data. |
| T-08.1-33 | Tampering | prior phases' screenshot manifests | high | mitigate | The harness's default manifest path points at Phase 5's committed file and has caused one real incident, so `--manifest` is passed explicitly on every run and a `git diff` check over both prior manifests gates this task. |
| T-08.1-34 | Repudiation | the audit's own history | medium | mitigate | Entries are annotated, never deleted, and the deferred items are explicitly marked open — an audit that removes its own lines cannot be re-read later. |
</threat_model>

<verification>
- `mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run build`
- `mise exec -- npm run scan:tokens` reports 0 violations
- `mise exec -- node scripts/build-themes.mjs --check`
- `mise exec -- npm run test:workers`
- Port 3000 free; `.env.local` untracked; prior phases' manifests unmodified
</verification>

<success_criteria>
- The guest order-status route renders locally from a fixed, idempotent, development-only fixture and is captured across all seven presets.
- D-11's visual comparison outcome is recorded against the Phase 8 volt-dark capture.
- Every v2 audit tech-debt line names its outcome, the deferred items stay open, and DEBT-01 is satisfied with every gate green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07)

| Artifact | Kind | Purpose |
|---|---|---|
| `dev-order-001` in `data/d1/seed-dev.sql` | new fixture | Gives the guest order-status route something to render locally (D-09) |
| `tests/unit/scripts/dev-seed-guard.test.ts` | new test | Proves the fixture can never reach a production seed path |
| `.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md` | new manifest | 28 order-status cells across seven presets, plus the D-11 comparison record |
| Annotated `tech_debt` list in `.planning/v2-MILESTONE-AUDIT.md` | close-out | Every line maps to a closing plan or a stated reason it stays open |
| DEBT-01 marked satisfied | requirement close | The phase's single requirement |
</artifacts_produced>

<output>
Create `.planning/phases/08.1-v2-tech-debt-closure/08.1-07-SUMMARY.md` when done.
Record D-09 as closed, D-11's visual outcome, and a table mapping every v2 audit tech-debt line to
the plan that closed it or the reason it remains open.
</output>
