---
phase: 08.1-v2-tech-debt-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - docs/theming.md
  - tests/unit/helpers/parity-snapshot.ts
  - tests/unit/helpers/parity-snapshot.test.ts
  - tests/unit/components/layout/category/category-grid3-parity.test.ts
  - tests/unit/components/layout/home/home-hero-variants.test.ts
  - tests/unit/components/layout/product/product-gallery-variants.test.ts
  - app/api/admin/settings/route.ts
  - tests/unit/app/api/admin-settings-empty-category.test.ts
autonomous: true
requirements: [DEBT-01]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The hardcoded-colour scan runs in CI as its own named step, and docs/theming.md says so instead of recording it as a local-only convention (D-03)."
    - "A parity test whose baseline snapshot file is missing fails with a message naming the file and the opt-in flag, and never writes the baseline (D-06)."
    - "Setting UPDATE_SNAPSHOTS=1 is the only way a baseline gets written, and doing so is a deliberate, visible act."
    - "EMPTY: a snapshot file that exists but is empty is read and compared as empty — the helper does not treat an empty file as missing and does not regenerate it."
    - "The admin settings seed-insert fallback logs with a stable prefix instead of swallowing, and a failing insert is provably logged (D-07)."
    - "The empty-category settings test no longer configures a mock the code under test never calls (D-07)."
  artifacts:
    - tests/unit/helpers/parity-snapshot.ts
    - tests/unit/helpers/parity-snapshot.test.ts
  key_links:
    - "CI job -> npm run scan:tokens -> scripts/scan-hardcoded-colors.mjs"
    - "the three parity suites -> tests/unit/helpers/parity-snapshot.ts -> the committed __snapshots__ files"
    - "admin settings GET seed insert -> console.error with a stable prefix"
  prohibitions:
    - "MUST NOT regenerate, edit, or delete any committed file under a __snapshots__ directory in this plan — the baselines are the oracle and must stay byte-identical. [flagged-unverified beyond the git-diff check]"
    - "MUST NOT change the settings route's response shape, status codes, or the scoped-default-seeding behaviour introduced in Phase 8 — only the catch arm gains a log. [flagged-unverified]"
    - "MUST NOT add a watch-mode flag or a long-running command to the CI workflow. [flagged-unverified]"
    - "MUST NOT change the 23-token contract, any file under themes/, or any variant component. [flagged-unverified]"
---

<objective>
Close the three gate-and-hygiene debt items that need no production behaviour change: wire the
hardcoded-colour scan into CI and correct the doc that says it is not (D-03); make the
pre-extraction parity tests fail loudly on a missing baseline instead of quietly writing one
(D-06); and make the admin settings seed fallback log rather than swallow (D-07).

Purpose: three separately-recorded v2 audit lines, each closable on its own commit.
Output: a CI step, two corrected doc sections, a shared snapshot helper with its own test, and a
logged settings-seed failure with a regression test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-CONTEXT.md
@.planning/phases/08.1-v2-tech-debt-closure/08.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The hardcoded-colour scan becomes a CI step, and the doc stops saying otherwise</name>
  <files>.github/workflows/ci.yml, docs/theming.md</files>
  <read_first>
    - .github/workflows/ci.yml (whole file — copy the shape and placement of the "Check theme manifest freshness" step)
    - docs/theming.md lines 277-310 (the "The two gates and the screenshot harness" section, including the paragraph that records the scan as a local-only convention)
    - docs/theming.md lines 341-356 (the "Known limits and backlog" table, whose first and last rows this plan retires)
    - package.json scripts (confirm the exact script name before writing the workflow step)
  </read_first>
  <action>
Add one step to the `validate` job in `.github/workflows/ci.yml`, immediately after the
"Check theme manifest freshness" step, named "Scan for raw token usage", running
`npm run scan:tokens`. Match the surrounding steps' indentation and shape exactly; add no
condition, no continue-on-error, and no extra environment.

In `docs/theming.md` §"The two gates and the screenshot harness", replace the paragraph that
records the scan as a local-only convention with one that states it runs in CI as the step named
"Scan for raw token usage", alongside the manifest-freshness step. Keep the surrounding
description of what the scan covers and excludes exactly as it is. Adjust the manifest-freshness
paragraph's "**This one does run in CI**" emphasis so it no longer implies the other gate does
not — both now do.

In §"Known limits and backlog", delete the row about the scan not being wired into CI and the row
about the parity tests self-writing a missing baseline (Task 2 of this plan closes the second one
in the same wave). Leave every other row untouched — the deployment variable, the Clerk
walkthrough, the dropped direction-doc properties, and the two-resolver row are not this task's
business.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a step named "Scan for raw token usage" running `npm run scan:tokens`, positioned after the manifest-freshness step.
    - `docs/theming.md` states that the scan runs in CI and names the step.
    - Neither of the two retired known-limits rows remains in `docs/theming.md`.
    - The workflow file still parses as YAML and every other step is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>/usr/bin/grep -q 'Scan for raw token usage' .github/workflows/ci.yml && /usr/bin/grep -q 'npm run scan:tokens' .github/workflows/ci.yml && test "$(/usr/bin/grep -n 'Check theme manifest freshness' .github/workflows/ci.yml | /usr/bin/cut -d: -f1)" -lt "$(/usr/bin/grep -n 'Scan for raw token usage' .github/workflows/ci.yml | /usr/bin/cut -d: -f1)" && mise exec -- node -e "const y=require('node:fs').readFileSync('.github/workflows/ci.yml','utf8');if(!/^\s{6}- name: Scan for raw token usage$/m.test(y)){console.error('step indentation does not match its siblings');process.exit(1)}" && /usr/bin/grep -q 'Scan for raw token usage' docs/theming.md && ! /usr/bin/grep -q 'is not wired into CI' docs/theming.md && mise exec -- npm run scan:tokens</automated>
    <fails_when>A grep exits non-zero because the step or the doc sentence is missing; or the line-number comparison fails because the new step landed before the manifest gate; or the node check prints the indentation message because the step is nested at the wrong depth; or the stale doc sentence survives; or `scan:tokens` itself reports a violation, which would mean the new CI step is red on arrival.</fails_when>
  </verify>
  <done>The scan is a named CI step and the document describes the gates as they now are.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parity baselines fail loudly when missing; regenerating one is an explicit opt-in</name>
  <files>tests/unit/helpers/parity-snapshot.ts, tests/unit/helpers/parity-snapshot.test.ts, tests/unit/components/layout/category/category-grid3-parity.test.ts, tests/unit/components/layout/home/home-hero-variants.test.ts, tests/unit/components/layout/product/product-gallery-variants.test.ts</files>
  <read_first>
    - tests/unit/components/layout/category/category-grid3-parity.test.ts lines 1-75 (the only suite that currently writes a missing baseline, and its `SNAPSHOT_PATH`)
    - tests/unit/components/layout/home/home-hero-variants.test.ts lines 25-40 (its `source(RECORDING_PATH)` read, which throws a bare filesystem error today)
    - tests/unit/components/layout/product/product-gallery-variants.test.ts lines 20-60 (same pattern, plus `RECORDING_PATH`)
    - tests/integration/helpers/ (the existing precedent for a shared, non-collected test helper module)
  </read_first>
  <behavior>
    - readParitySnapshot on an existing file returns its contents unchanged, including when the file is empty.
    - readParitySnapshot on a missing file, with no opt-in flag set, throws an Error whose message contains the snapshot's path and the opt-in flag's name, and does not create the file.
    - readParitySnapshot on a missing file with the opt-in flag set to 1 creates the parent directory, writes the supplied content, and returns it.
    - readParitySnapshot on a missing file with the opt-in flag set but no content supplied throws rather than writing an empty baseline.
    - All three parity suites pass against their committed baselines with no baseline file modified.
  </behavior>
  <action>
Create `tests/unit/helpers/parity-snapshot.ts` exporting
`readParitySnapshot(relativePath: string, actual?: string): string` with the four behaviours
above. The opt-in is `process.env.UPDATE_SNAPSHOTS === "1"`. When the file is absent and the
opt-in is not set, throw an `Error` whose message names the missing path, states that a parity
baseline is the test's oracle and is never generated implicitly, and gives the exact command to
regenerate it deliberately. Use `existsSync` for the presence check so an existing-but-empty file
reads as present, not missing. Give the module a docstring explaining why writing-on-miss is the
dangerous default this helper removes: a baseline that regenerates itself proves nothing.

Create `tests/unit/helpers/parity-snapshot.test.ts` covering all four behaviours against a
temporary directory created under the OS temp dir, cleaned up afterwards. Save and restore the
opt-in environment variable around each case so the suite cannot leak state.

Rewire all three parity suites onto the helper. In the category suite, delete the
presence-check / directory-create / file-write block and its now-unused filesystem imports, and read the baseline
through the helper, passing the freshly rendered markup as the second argument so a deliberate
regeneration still works. In the home-hero and product-gallery suites, replace their
`source(RECORDING_PATH)` baseline read with the helper (the helper is only for the committed
baseline; their other `source(...)` calls, which read live component source files, stay as they
are). None of the three suites' assertions change.
  </action>
  <acceptance_criteria>
    - All three parity suites read their baseline through `readParitySnapshot` and no longer contain a write call of their own.
    - A missing baseline with no opt-in produces an error naming the path and the flag, and creates no file.
    - `UPDATE_SNAPSHOTS=1` with content supplied writes the baseline; with no content supplied it throws.
    - An existing empty baseline is read as empty, not treated as missing.
    - No file under any `__snapshots__` directory is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/helpers/parity-snapshot.test.ts tests/unit/components/layout/category/category-grid3-parity.test.ts tests/unit/components/layout/home/home-hero-variants.test.ts tests/unit/components/layout/product/product-gallery-variants.test.ts && test -z "$(/usr/bin/git status --porcelain -- 'tests/unit/components/layout/*/__snapshots__')" && ! /usr/bin/grep -q 'writeFileSync' tests/unit/components/layout/category/category-grid3-parity.test.ts && for f in tests/unit/components/layout/category/category-grid3-parity.test.ts tests/unit/components/layout/home/home-hero-variants.test.ts tests/unit/components/layout/product/product-gallery-variants.test.ts; do /usr/bin/grep -q 'readParitySnapshot' "$f" || { echo "NOT REWIRED: $f"; exit 1; }; done && mise exec -- npm run typecheck</automated>
    <fails_when>A helper or parity assertion reports a failing expectation (most tellingly, the missing-baseline case creating a file or failing to name the opt-in flag); or `git status --porcelain` prints a line, proving a committed baseline was rewritten; or the category suite still contains a write call; or the loop prints NOT REWIRED for a suite still reading its baseline directly; or tsc prints an error.</fails_when>
  </verify>
  <done>A deleted baseline now fails the suite with an actionable message, and regenerating one requires typing the flag.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The settings seed fallback logs instead of swallowing, and the unused mock goes</name>
  <files>app/api/admin/settings/route.ts, tests/unit/app/api/admin-settings-empty-category.test.ts</files>
  <read_first>
    - app/api/admin/settings/route.ts lines 50-80 (the scoped-defaults filter added in Phase 8 and the catch arm this task changes)
    - tests/unit/app/api/admin-settings-empty-category.test.ts lines 1-60 (the hoisted mocks object, the module mock, and the beforeEach stub)
    - .planning/phases/08-documentation-visual-qa-close-out/08-REVIEW.md §Info IN-01, IN-02 (the exact fix both items ask for)
    - lib/observability/telemetry.ts (confirm for yourself that no event exists for this case before deciding not to add one)
  </read_first>
  <behavior>
    - A successful seed insert produces no error log and the handler's response is unchanged.
    - A seed insert that rejects is logged once via console.error with the stable prefix, and the handler still proceeds to the re-select and returns its normal response rather than throwing.
    - The empty-category test's existing assertions about scoped default seeding still pass.
  </behavior>
  <action>
In `app/api/admin/settings/route.ts`, give the seed-insert catch arm a bound error parameter and a
single `console.error` call carrying the stable prefix
`'Seed insert failed (may be a benign concurrent race):'` and the caught value — the exact fix the
Phase 8 review prescribes. Keep the existing explanatory comment about the concurrent-seed race
and the deliberate re-read that follows; add to it that the log exists because the conflict clause
already absorbs the race this catch was written for, so anything reaching this arm is more likely
a real failure. Change nothing else in the handler: the scoped-defaults filter, the re-select, the
response shape, and the status codes all stay as they are. Do not introduce a telemetry event —
none exists for this case and the shared telemetry recorder already writes through the same
console channel.

<!-- planner-discipline-allow: isSuperAdminActor -->
In `tests/unit/app/api/admin-settings-empty-category.test.ts`, remove the `isSuperAdminActor`
entry from the hoisted mocks object, its export from the `@/lib/auth/admin-middleware` module
mock, and its `beforeEach` stub — this handler exercises only GET, which never reaches that check.
Add one case that makes the insert reject and asserts the error is logged once with the stable
prefix and that the handler still returns its normal successful response, using a spy on
`console.error` restored afterwards.
  </action>
  <acceptance_criteria>
    - The catch arm binds the error and calls `console.error` exactly once with the stable prefix.
    - A rejecting insert is logged and the handler still returns its normal response.
    - `isSuperAdminActor` appears nowhere in the empty-category test file.
    - The handler's scoped-default-seeding behaviour and response shape are unchanged; the file's pre-existing assertions still pass.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/app/api/admin-settings-empty-category.test.ts && ! /usr/bin/grep -q 'isSuperAdminActor' tests/unit/app/api/admin-settings-empty-category.test.ts && test "$(/usr/bin/grep -c 'Seed insert failed' app/api/admin/settings/route.ts)" -eq 1 && /usr/bin/grep -q 'defaultSettings.filter' app/api/admin/settings/route.ts && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test</automated>
    <fails_when>The empty-category suite reports a failing assertion (the new rejecting-insert case not seeing the log, or a Phase 8 scoped-seeding assertion breaking); or the unused mock name is still present; or the prefix count is not exactly 1, meaning the log is missing or duplicated; or the scoped-defaults filter was lost; or lint/tsc/the full suite report an error.</fails_when>
  </verify>
  <done>A non-race seed-insert failure is now visible in the logs, and the test file mocks only what it uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository source | An automated gate now decides whether raw palette values may land on `main` |
| admin request → settings seed insert | An admin-authenticated GET can trigger a write whose failure was previously invisible |
| test process → committed baseline files | Test code holds a filesystem write path into the repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.1-11 | Tampering | committed parity baselines | high | mitigate | Test code may write a baseline only when `UPDATE_SNAPSHOTS=1` is explicitly set; the default path throws. Enforced by the helper's own suite plus a `git status --porcelain` check over the snapshot directories in this task's verify. |
| T-08.1-12 | Information Disclosure | the new `console.error` on seed-insert failure | medium | mitigate | The log carries a fixed prefix and the caught error object only — no settings values, no admin identity, no request body. Reviewed against the Phase 8 review's own prescribed line. |
| T-08.1-13 | Denial of Service | the new CI step | low | accept | The scan is a bounded static pass over four source trees, already run locally on every phase of this milestone; it adds seconds, not minutes, and has no network dependency. |
| T-08.1-14 | Repudiation | a seed-insert failure that used to be silent | low | mitigate | This task's entire purpose: the failure is now attributable in the logs rather than being indistinguishable from success. |
</threat_model>

<verification>
- `mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test`
- `mise exec -- npm run scan:tokens` reports 0 violations (the gate this plan wires into CI)
- `mise exec -- node scripts/build-themes.mjs --check`
- `test -z "$(/usr/bin/git status --porcelain -- 'tests/unit/components/layout/*/__snapshots__')"`
</verification>

<success_criteria>
- CI runs the hardcoded-colour scan as its own named step and the document says so.
- A missing parity baseline fails its suite with an actionable message and writes nothing.
- A failing settings seed insert is logged with a stable prefix, and the empty-category test mocks only what it uses.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Artifact | Kind | Purpose |
|---|---|---|
| "Scan for raw token usage" CI step | workflow step | Makes the hardcoded-colour gate automatic rather than conventional (D-03) |
| `tests/unit/helpers/parity-snapshot.ts` | new helper | One loud-on-missing baseline reader for all three parity suites (D-06) |
| `tests/unit/helpers/parity-snapshot.test.ts` | new test | Proves the fail-loud default and the explicit opt-in |
| Logged seed-insert failure | behaviour change | Makes a previously silent admin-path failure attributable (D-07) |
</artifacts_produced>

<output>
Create `.planning/phases/08.1-v2-tech-debt-closure/08.1-03-SUMMARY.md` when done.
Record D-03, D-06, and D-07 as closed, naming the two `docs/theming.md` known-limits rows retired.
</output>
</content>
