---
phase: 15-subscription-address-in-place
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified: []
autonomous: true
requirements: [SUB-01, SUB-02, SUB-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full CI-mirroring gate suite is green on the phase's final tree before anything is pushed (D-08: scan:tokens is this phase's visual contract, since no UI-SPEC was produced)."
    - "The phase changed no API route, no migration, no Worker binding and no dependency — asserted by diff, not by assumption."
    - "The change is deployed to production and the subscription product page still serves anonymously with HTTP 200, checked read-only with no write of any kind."
    - "Russell has an explicit three-item browser list for the behaviours no automated check in this repository can reach (D-07: the unit suite is source-contract and pure-helper only; jsdom is unavailable)."
  artifacts:
    - "15-03-SUMMARY.md — gate evidence, deployed version evidence, and the human-check list"
  key_links:
    - "gate suite -> push to main -> Cloudflare Workers Builds -> production: the only deploy path for this repository (AGENTS.md; never npm run deploy)"
    - "phase-scope diff assertions -> the phase threat model: they are what closes T-15-06 and T-15-SC"
---

<objective>
Close the phase: run the gate suite, prove the phase's scope claims by diff, deploy by pushing `main`, verify production read-only, and hand Russell the short browser list.

Purpose: SUB-01..03 are implemented in waves 1 and 2 but only pinned by source contracts and pure-helper tests. This plan converts that into a shipped, checked state and names the residue a human still has to look at.
Output: `15-03-SUMMARY.md` with command evidence and the human-check list. No source file is modified.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-subscription-address-in-place/15-CONTEXT.md
@.planning/phases/15-subscription-address-in-place/15-01-SUMMARY.md
@.planning/phases/15-subscription-address-in-place/15-02-SUMMARY.md
</context>

<read_first>
- `AGENTS.md` "Gates" — the exact CI command list and order. `check:migrations` is pull-request-only and this phase adds no migration; `cf-typecheck` regenerates a tracked file and needs no run here because Task 1 asserts by diff that no binding or env surface changed.
- `AGENTS.md` "Rules" — `npm run deploy` and `npm run deploy:ci` are never run from a developer machine. Deployment happens because Cloudflare Workers Builds watches `main`.
- `.planning/STATE.md` "Blockers/Concerns" — the open production items carried from earlier phases (Stripe Tax unavailable, `STORE_SUPPORT_EMAIL` placeholder, no routing rule for the sender address). None of them is in this phase's scope; do not try to fix them here, and do not report them as new.

**Phase base commit:** `635cb28` (`docs(15): context, research, validation strategy`). Every phase-15 code commit is a descendant of it, so `635cb28..HEAD` is the phase range for every scope assertion below.

**Working tree note:** `.gsd/`, `MILESTONE-SEED.md`, `volt.png` and `volt.svg` are untracked and unrelated to this phase. Never run `git add .`; add named paths only, and leave those four alone.

**Precondition:** waves 1 and 2 are committed and the working tree carries no uncommitted change under `components/`, `lib/` or `tests/`. Halt if it does — the gate below must run on the tree that is about to be pushed.
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Full gate suite and phase-scope assertions</name>
  <files>(none — verification only)</files>
  <action>
Run the CI-mirroring gate suite on the phase's final tree, then assert the phase's scope claims by diff rather than by assumption.

Gate suite, in CI's order, one command chain so the first failure stops the run. `npm audit` and `build:themes:check` are included because CI runs them on the push Task 2 makes; `check:migrations` is skipped because it is pull-request-only and this phase adds no migration file; `cf-typecheck` is replaced by the binding-surface diff assertion below, which is a stronger statement than re-running the generator.

Scope assertions over `635cb28..HEAD`:
- `package.json` and `package-lock.json` unchanged — this phase installed nothing (T-15-SC).
- `app/api/` unchanged — no new or edited API surface (T-15-06, CONTEXT `<domain>`: "No API changes").
- `migrations/` unchanged — the expand-only policy is not engaged this phase.
- `wrangler.jsonc` and `cloudflare-env.d.ts` unchanged — no binding, var or secret surface moved.
- `app/account/addresses/page.tsx` unchanged (D-01).
- `components/checkout/` unchanged — the guest checkout shipping form is deliberately outside this phase's boundary and must not have been "consolidated" while the account form was extracted (see plan 15-01 Task 3's recorded D-07 narrowing).

If any gate command fails, fix the cause and re-run the whole chain — do not push a partially green tree. If a scope assertion fails, stop and report it; a scope violation is a planning-contract breach, not something to patch over.
  </action>
  <verify>
    <automated>npm audit --omit=dev --audit-level=high && npm run build:themes:check && npm run scan:tokens && npm run lint && npm run typecheck && mise exec -- npm test && npm run test:workers && npm run test:observability-worker && npm run build && git diff --quiet 635cb28..HEAD -- package.json package-lock.json app/api migrations wrangler.jsonc cloudflare-env.d.ts app/account/addresses/page.tsx components/checkout</automated>
    <fails_when>`scan:tokens` reports a violation (most likely a non-token class in the new dialog); the unit suite goes red on one of the three source contracts; `npm run build` fails on the new client component tree; or a scope assertion reports a diff — a changed `package-lock.json` means a dependency was pulled in, a changed `app/api` means the phase crossed its own boundary.</fails_when>
  </verify>
  <done>Every CI gate this phase can affect is green on the exact tree about to be pushed, and each of the six scope claims is proven by an empty diff.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy by pushing main, then check production read-only</name>
  <files>(none — deploy and observation only)</files>
  <action>
Deploy the phase and confirm production serves it. Every production interaction in this task is a `GET`; make no write of any kind — no admin action, no checkout, no address save against the live site (that is Russell's job in Task 3, with his own account).

1. Capture a pre-deploy fingerprint of the live subscription product page: fetch `https://voltique.russellkmoore.me/product/field-ration-resupply` and hash the sorted set of `/_next/static/...` asset paths it references. This page currently returns 200 (checked at planning time). Component source changed in waves 1 and 2, so the hashed chunk filenames must change once the new build is live — that is the deploy signal.
2. `git push origin main`. Push named branch and remote explicitly. Cloudflare Workers Builds picks it up; do not run any `deploy` script.
3. Poll the same page every 20 seconds, up to 20 minutes, until the asset fingerprint differs from the pre-deploy one.
4. Once it differs, confirm the page still returns HTTP 200 anonymously (no cookie, no auth header).
5. Record in the SUMMARY: the pushed commit SHA, the pre- and post-deploy fingerprints, the elapsed time, and the final status code.

If the fingerprint has not changed after 20 minutes, halt and report. Do not retry the push and do not run a deploy script — check the Cloudflare dashboard's Workers Builds log for the `mercora` Worker instead and report what it says.

Note for the SUMMARY: the shipping-address select is client-rendered inside a signed-in branch, so the anonymous HTML cannot and does not prove the select's new option exists. This step proves reachability and a successful deploy; the select itself is covered by the wave-2 source contracts and by Task 3's human list. Say that plainly rather than implying more.
  </action>
  <verify>
    <automated>BEFORE=$(curl -fsS https://voltique.russellkmoore.me/product/field-ration-resupply | grep -o '/_next/static/[^"]*' | sort -u | shasum | cut -c1-12) && git push origin main && AFTER="$BEFORE" && for i in $(seq 1 60); do sleep 20; AFTER=$(curl -fsS https://voltique.russellkmoore.me/product/field-ration-resupply | grep -o '/_next/static/[^"]*' | sort -u | shasum | cut -c1-12); [ "$AFTER" != "$BEFORE" ] && break; done && [ "$AFTER" != "$BEFORE" ] && test "$(curl -fsS -o /dev/null -w '%{http_code}' https://voltique.russellkmoore.me/product/field-ration-resupply)" = "200"</automated>
    <fails_when>the push is rejected (a non-fast-forward means `main` moved and the tree must be rebased and re-gated); the asset fingerprint is unchanged after 20 minutes, meaning Workers Builds did not deploy or the build failed; or the page returns a status other than 200 after the deploy, meaning the new client tree broke the product route.</fails_when>
  </verify>
  <done>The phase is live on `voltique.russellkmoore.me`, proven by a changed asset fingerprint plus a 200 on the subscription product page, with no write made against production.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-close record and the human-check list</name>
  <files>(none — the SUMMARY carries the record)</files>
  <action>
Write the phase-close record into `15-03-SUMMARY.md`. It must contain, with real command output rather than restated intent:

- The gate evidence from Task 1 (each command and its result) and the six scope assertions.
- The deploy evidence from Task 2 (SHA, fingerprints, elapsed time, status code).
- A requirement table mapping SUB-01, SUB-02 and SUB-03 each to the specific test or check that proves it, naming the test file and the assertion.
- The D-07 scoping narrowing recorded by plan 15-01 (the checkout shipping form is outside the one-form contract's walk) restated in one line, so a later reader does not re-discover it as a gap.
- The decision record for anything settled under Claude's discretion in wave 2: the two placeholder strings, the add-option label, and the modal title and description copy.
- The human-check list below, verbatim, as the last section.

Then state the phase's residue honestly: this repository's unit suite has no jsdom, so nothing in it renders the modal, opens the select or exercises a real save. Every behavioural claim in SUB-01..03 is pinned at the source-contract and pure-helper level. That is the reason the three items below need Russell.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md && test "$(grep -Fc 'SUB-01' .planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md)" -ge 1 && test "$(grep -Fc 'SUB-02' .planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md)" -ge 1 && test "$(grep -Fc 'SUB-03' .planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md)" -ge 1</automated>
    <fails_when>the SUMMARY is missing, or a requirement id has no row in it, meaning a requirement shipped with no named proof.</fails_when>
    <human-check>
Do these three on voltique.russellkmoore.me while signed in. Report pass or fail for each.

1. Open a subscription product (Field Ration Resupply). Pick a delivery schedule, set the quantity to 2, then choose "Add a new address…" in the shipping address dropdown. Save a real address. The modal should close, the new address should be selected, and the schedule and the quantity 2 should be exactly where you left them.

2. Open the same dropdown again, choose "Add a new address…", and enter a one-letter country code. Saving should show the error inside the modal and the modal should stay open.

3. Open the modal once more and press Escape. The dropdown should still show whichever address was selected before you opened it.

Also worth a glance while you are there: with no saved addresses at all, the dropdown must still be openable — it is no longer disabled on an empty list.
    </human-check>
  </verify>
  <done>`15-03-SUMMARY.md` exists with gate evidence, deploy evidence, a per-requirement proof table, the recorded discretion choices, and the three-item list for Russell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tree -> `main` -> production | Pushing `main` deploys. Everything that crosses this boundary is live for real shoppers immediately. |
| this agent -> production data | Read-only. No write of any kind against the live site is authorised in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-08 | Tampering | push to `main` | high | mitigate | The full CI-mirroring gate plus six scope assertions run on the exact tree before the push; a failure stops the chain before `git push` is reached, because Task 2's command chain begins only after Task 1 passed. |
| T-15-06 | Elevation of privilege | `app/api/**` | high | mitigate | Closed by diff: `git diff --quiet 635cb28..HEAD -- app/api` must be empty. The phase adds no API surface, so no authorisation or same-origin guard changed. |
| T-15-SC | Tampering | package manager | high | mitigate | Closed by diff: `package.json` and `package-lock.json` unchanged across the phase range. No package-legitimacy checkpoint is required because no install task exists anywhere in this phase. |
| T-15-09 | Information disclosure | production checks | medium | mitigate | Every production request in Task 2 is an anonymous `GET`; no cookie, no auth header, no write. The one flow that needs a real session is delegated to Russell's own account in Task 3. |
| T-15-10 | Denial of service | Workers Builds | low | accept | A failed build leaves the previous version serving; the fingerprint poll detects the non-deploy and halts rather than looping or forcing a manual deploy path that `AGENTS.md` forbids. |
</threat_model>

<verification>
- Task 1's chain exits zero end to end.
- Task 2's fingerprint changes and the page returns 200.
- `git log --oneline 635cb28..HEAD` lists only phase-15 commits, and `git status --short` shows no tracked source file left uncommitted.
</verification>

<success_criteria>
- All three requirements (SUB-01, SUB-02, SUB-03) are implemented, gated, deployed and mapped to a named proof.
- No API, migration, binding, dependency or checkout change happened anywhere in the phase — each proven by an empty diff.
- Russell has a three-item browser list and knows exactly why those three are not automated.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan's share)

| Path | New/Modified | Why |
|------|--------------|-----|
| `.planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md` | new | Gate evidence, deploy evidence, per-requirement proof table, human-check list |

No source file is modified by this plan.
</artifacts>

<output>
Create `.planning/phases/15-subscription-address-in-place/15-03-SUMMARY.md` when done, with the human-check list as its final section.
</output>
