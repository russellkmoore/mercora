---
phase: 05-token-contract-component-sweep
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - components/ProductCard.tsx
  - components/ProductRecommendations.tsx
  - app/category/[slug]/CategoryDisplay.tsx
  - app/category/[slug]/page.tsx
  - app/product/[slug]/ProductDisplay.tsx
  - app/product/[slug]/page.tsx
  - app/page.tsx
  - .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
autonomous: true
requirements: [TOKEN-03, TOKEN-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mise exec -- npm run scan:tokens -- --path app/product` and `-- --path app/category` report 0 violations"
    - "`mise exec -- npm run scan:tokens -- --path components/ProductCard.tsx` and the same for `ProductRecommendations.tsx` and `app/page.tsx` report 0 violations"
    - "Stock, sale, and availability badges reach the status quartet by meaning — in stock is success, low stock is warning, out of stock is danger"
    - "Product image blur placeholders are unchanged, and the manual-review registry still prints them on every scan run"
    - "Home, category, and product routes render identically to the baseline at both viewports apart from registered snaps"
  prohibitions:
    - statement: "MUST NOT modify any file under `app/admin/` or `components/admin/`. Admin's hardcoded palette is a deliberate scope boundary (D-14), not an oversight to tidy up."
      status: resolved
      verification: judgment
    - statement: "MUST NOT change any rendered pixel beyond the recorded snaps. No opportunistic restyling, no layout, spacing, typography, shadow, overlay, or light-versus-dark polarity change while substituting class names (D-16)."
      status: resolved
      verification: judgment
  artifacts:
    - path: "app/product/[slug]/ProductDisplay.tsx"
      provides: "Product detail template expressed entirely in contract tokens"
    - path: ".planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md"
      provides: "chunk-3-catalog coverage section for the home, category, and product routes"
  key_links:
    - from: "components/ProductCard.tsx"
      to: "themes/volt-dark.css"
      via: "card surface, price, and badge classes resolving through the Tailwind colour map"
      pattern: "bg-surface-elevated|text-primary"
---

<objective>
Sweep the three catalogue routes and the card components they share to the token contract.

Purpose: these are the highest-traffic storefront pages and the ones a theme change is most
visible on. They are also the first chunk swept against a stable, already-tokenised shell, so
their screenshot diffs isolate exactly this chunk's work.

Output: seven catalogue files scan-clean, and a screenshot set for the home, category, and
product routes proving no regression.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md
@.planning/phases/05-token-contract-component-sweep/05-CONTEXT.md
@.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
@.planning/phases/05-token-contract-component-sweep/05-05-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

All seven files here are main-set surfaces — dark panels in a dark app. The inverse token set
appears nowhere in this chunk; the only light panels in the storefront are the two drawers,
which plan 05-09 owns.

Measured scope: `ProductDisplay.tsx` 26 occurrences over 366 lines, `ProductCard.tsx` 14 over
205 lines, `app/category/[slug]/page.tsx` 5, `CategoryDisplay.tsx` 4, `app/page.tsx` 3,
`ProductRecommendations.tsx` 2, `app/product/[slug]/page.tsx` 1.

`lib/utils/image-placeholders.ts` is consumed by the card components as `blurDataURL`. It is on
the scan's manual-review registry and is deliberately NOT swept — its colour values live inside
a base64 data URI that cannot read a CSS custom property under any theme. See `05-TOKEN-MAP.md`
§6 for the written reason. Do not edit it, and do not remove it from the registry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the catalogue cards and the category route</name>
  <files>components/ProductCard.tsx, components/ProductRecommendations.tsx, app/category/[slug]/CategoryDisplay.tsx, app/category/[slug]/page.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the main-set substitution table including the status-token rows
    - `components/ProductCard.tsx` — the full file
    - `components/ProductRecommendations.tsx` — the full file
    - `app/category/[slug]/CategoryDisplay.tsx` and `app/category/[slug]/page.tsx` — the full files
    - `components/ui/card.tsx` and `components/ui/badge.tsx` as rewritten in plan 05-04 — the primitive vocabulary these compose against
  </read_first>
  <action>
    Sweep the four files with the §2 table, choosing tokens by role. Card surfaces and image
    wells are elevated surfaces; product names are foreground; secondary metadata, category
    counts, and descriptions are muted foreground; prices, sale accents, links, and hover
    highlights are primary with reduced alpha for the lighter and darker hover shades; card
    outlines and grid dividers are the border token.

    Map every stock or availability badge onto the status quartet by what it means rather than
    by the colour it currently uses: an in-stock or available badge is success, a low-stock or
    limited badge is warning, an out-of-stock or discontinued badge is danger. A tinted panel
    behind a badge — the very dark red, green, or amber backgrounds this codebase uses — becomes
    the same status token at a low alpha rather than a second colour.

    Leave `lib/utils/image-placeholders.ts` untouched and leave its `blurDataURL` imports as
    they are.

    Preserve every variant prefix, alpha modifier, aspect-ratio class, grid class, and
    transition. Change no spacing, sizing, or shadow class.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path components/ProductCard.tsx`, `-- --path components/ProductRecommendations.tsx`, and `-- --path app/category` each report 0 violations
    - `components/ProductCard.tsx` references at least two of the four status tokens for its badge states
    - No file in this task contains an inverse-set token class
    - `git diff --name-only lib/utils/image-placeholders.ts` returns nothing
    - `mise exec -- npm run build`, `lint`, and `typecheck` all succeed
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path components/ProductCard.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path components/ProductRecommendations.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path app/category</automated>
    <fails_when>non-zero exit from any of the three, meaning a palette value survives in that scope</fails_when>
    <automated>git diff --name-only lib/utils/image-placeholders.ts | /usr/bin/wc -l</automated>
    <fails_when>the printed count is anything other than 0, meaning the registered manual-review file was edited instead of left alone</fails_when>
    <automated>/usr/bin/grep -cE '(bg|text|border)-(surface-inverse|on-inverse|muted-on-inverse|border-inverse)' components/ProductCard.tsx components/ProductRecommendations.tsx | /usr/bin/grep -v ':0$' | /usr/bin/wc -l</automated>
    <fails_when>the printed count is anything other than 0, meaning an inverse-set token leaked onto a main-set surface</fails_when>
    <automated>mise exec -- npm run build &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a line matching "Failed to compile", "error TS", or "error" in the combined output</fails_when>
  </verify>
  <done>Catalogue cards and the category route are scan-clean, badges map to the status quartet by meaning, and the blur-placeholder module is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the product detail template and the home route</name>
  <files>app/product/[slug]/ProductDisplay.tsx, app/product/[slug]/page.tsx, app/page.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the substitution table
    - `app/product/[slug]/ProductDisplay.tsx` — the full file; the largest occurrence count in this chunk
    - `app/product/[slug]/page.tsx` and `app/page.tsx` — the full files
    - `components/ProductCard.tsx` as rewritten in Task 1 — the token vocabulary to stay consistent with
    - `components/ui/select.tsx` and `components/ui/button.tsx` as rewritten in plan 05-04 — the primitives the variant picker and add-to-cart button compose against
  </read_first>
  <action>
    Sweep `ProductDisplay.tsx` with the §2 table. Take the gallery well, the specification
    panels, and the accordion or tab surfaces as elevated surfaces; headings and the price as
    foreground; specification labels, shipping copy, and secondary descriptions as muted
    foreground; the add-to-cart affordance, selected variant state, rating stars, and links as
    primary; panel outlines and row dividers as the border token. Stock and shipping status text
    takes the status quartet by meaning, exactly as in Task 1.

    Sweep `app/product/[slug]/page.tsx` and `app/page.tsx` the same way. The home page's hero and
    section backgrounds are surface or elevated surface by role, and any call-to-action is primary.

    Preserve every variant prefix and alpha modifier. Change no layout, spacing, aspect ratio,
    shadow, or transition class — the product gallery's structure is Phase 7's concern and must
    be identical when this phase ends.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path app/product` and `-- --path app/page.tsx` each report 0 violations
    - No file in this task contains an inverse-set token class
    - The number of layout-affecting classes — grid, flex, gap, padding, margin, width, height, aspect — in `ProductDisplay.tsx` is unchanged from before this plan
    - `mise exec -- npm run test` passes with no new failures
    - `mise exec -- npm run build`, `lint`, and `typecheck` all succeed
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path app/product &amp;&amp; mise exec -- npm run scan:tokens -- --path app/page.tsx</automated>
    <fails_when>non-zero exit from either, meaning a palette value survives in that scope</fails_when>
    <automated>git stash list > /dev/null; test "$(git show HEAD:app/product/\[slug\]/ProductDisplay.tsx | /usr/bin/grep -coE '\b(grid|flex|gap|p|px|py|m|mx|my|w|h|aspect)-[a-z0-9\[\]/.-]+')" -eq "$(/usr/bin/grep -coE '\b(grid|flex|gap|p|px|py|m|mx|my|w|h|aspect)-[a-z0-9\[\]/.-]+' app/product/\[slug\]/ProductDisplay.tsx)" &amp;&amp; echo "layout-stable"</automated>
    <fails_when>non-zero exit, or the string "layout-stable" is absent, meaning the count of layout-affecting classes changed and the sweep touched more than colour</fails_when>
    <automated>mise exec -- npm run test &amp;&amp; mise exec -- npm run build &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test, or a line matching "error TS"</fails_when>
  </verify>
  <done>The product detail template and the home route are scan-clean, and the layout-class census proves the sweep changed colour only.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the catalogue routes did not move</name>
  <files>.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md</files>
  <read_first>
    - `scripts/screenshot-routes.mjs` — the capture flags
    - `.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md` — the `baseline`, `chunk-2-ui`, and `chunk-2-shell` sections
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §4 and §8 — the snap register and the coverage grid
  </read_first>
  <action>
    Start the dev server, capture with label `chunk-3-catalog`, and compare the home, category,
    and product cells against the `baseline` label by content hash at both viewports and in both
    states.

    Append a `chunk-3-catalog` section to the manifest with a per-cell verdict. Attribute every
    difference to a registered snap identifier, to a `chunk-2-ui` newly-visible interaction
    state, or fix it.

    Pay specific attention to the status-badge snaps. Consolidating the green, red, amber, and
    blue shade ladders onto four tokens will visibly shift some badges — those are S4, S5, S6,
    and S7, and every badge difference should map to one of them. A badge that changed to a
    different status *meaning* rather than a different shade of the same meaning is a defect:
    an in-stock badge that turned amber is a mapping error, not a snap.
  </action>
  <acceptance_criteria>
    - `.planning/.../05-SCREENSHOTS.md` contains a `chunk-3-catalog` section covering the home, category, and product routes at both viewports in both states
    - Every differing row names a snap identifier, a `chunk-2-ui` state, or a fix
    - No badge difference is annotated as a status-meaning change
    - `mise exec -- npm run scan:tokens -- --path components/ProductCard.tsx` still reports 0 violations after the capture
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run dev > /tmp/dev-05-06.log 2>&amp;1 &amp; DEV=$!; sleep 35; mise exec -- node scripts/screenshot-routes.mjs --label chunk-3-catalog --manifest .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md --allow-missing; RC=$?; kill $DEV; exit $RC</automated>
    <fails_when>non-zero exit, or the summary line reports 0 captured cells; if nothing captures, read /tmp/dev-05-06.log for the server startup reason</fails_when>
    <automated>/usr/bin/grep -c 'chunk-3-catalog' .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md</automated>
    <fails_when>the printed count is 0, meaning the chunk section was never appended</fails_when>
    <human-check>Put the `chunk-3-catalog` and `baseline` product-route captures side by side at 1280px and 390px. Confirm the price, the add-to-cart button, and every stock badge still say the same thing — a badge that changed shade is snap S4 through S7 and is fine; a badge that changed meaning is a bug.</human-check>
  </verify>
  <done>The catalogue routes render as they did, and every difference in the manifest is traced to a shade consolidation rather than a change in what a badge communicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| product data → shopper-facing status badge | Stock and availability badges carry commercial meaning, not just colour. |
| catalogue templates → every shopper session | These three routes are the storefront's highest-traffic surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Spoofing | stock and availability badges | high | mitigate | Badges are mapped onto the status quartet by meaning, not by nearest shade. A shopper who reads "in stock" as a caution colour makes a different purchase decision. Task 3's human check compares badge meaning, not just badge appearance, against the baseline. |
| T-05-06-02 | Tampering | layout drift smuggled in during a colour sweep | medium | mitigate | Task 2 counts the layout-affecting classes in the largest file before and after and requires the counts to match, so a "while I'm here" restructure cannot pass silently. Phase 7 owns gallery structure. |
| T-05-06-03 | Repudiation | blur placeholder silently skipped | medium | mitigate | `lib/utils/image-placeholders.ts` stays on the scan's manual-review registry and is printed on every run; Task 1 asserts the file is unmodified so the registry entry stays truthful. |
| T-05-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing; the only phase install happened in 05-02 behind the blocking human legitimacy checkpoint. |
</threat_model>

<verification>
- Scoped scan reports 0 violations for `app/product`, `app/category`, `app/page.tsx`, `ProductCard.tsx`, `ProductRecommendations.tsx`.
- No inverse-set token appears in any file in this chunk.
- The layout-class census on `ProductDisplay.tsx` is unchanged.
- `lib/utils/image-placeholders.ts` is unmodified and still on the manual-review registry.
- `mise exec -- npm run test && npm run typecheck && npm run lint` green.
- `05-SCREENSHOTS.md` carries a `chunk-3-catalog` section with a per-cell verdict.
</verification>

<success_criteria>
The three routes a shopper actually browses are token-driven, their status badges still mean
what they meant, and the screenshot record shows nothing moved but shade.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| 7 rewritten catalogue files | modification | home, category, product routes and the card components they share |
| `chunk-3-catalog` manifest section | new section | home, category, product cells at both viewports and states |
</artifacts_produced>

<output>
Create `.planning/phases/05-token-contract-component-sweep/05-06-SUMMARY.md` when done.
</output>
