---
phase: 11-production-enablement
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - wrangler.jsonc
  - cloudflare-env.d.ts
autonomous: false
requirements: [OPS-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A blocking-human checkpoint stops before the acquisition push in every mode, including an unattended run, and Russell chooses one of three recorded outcomes (D-07)."
    - "`wrangler.jsonc` `vars` contains both `STORE_FEATURE_GIFT_CARD_RECONCILIATION` and `STORE_FEATURE_GIFT_CARD_ACQUISITION` as the string `\"true\"`, in that order in the file, with reconciliation having been deployed and verified first (OPS-02, D-05)."
    - "`cloudflare-env.d.ts` is regenerated with the local env files out of the tree, contains no `GIFT_CARD_` name, and `mise exec -- npm run cf-typecheck` exits 0 under those same conditions (OPS-02)."
    - "A new Worker version appears in `mise exec -- npx wrangler deployments list` after the acquisition push, and one five-minute recovery cron cycle after it runs clean — proving capability resolution did not throw."
    - "The full CI gate suite is green from `npm audit` through `npm run build`."
    - "No secret value appears anywhere in the phase's pushed history, in any document, or in any planning artifact (OPS-01, re-checked at the phase gate)."
  artifacts:
    - "wrangler.jsonc — `STORE_FEATURE_GIFT_CARD_ACQUISITION: \"true\"`"
    - "cloudflare-env.d.ts — regenerated, adding only the one new var name"
    - "A recorded D-07 decision: which of the three options Russell chose, and on what evidence"
  key_links:
    - "The D-07 checkpoint -> the acquisition push: the one gate between this phase and gift cards being purchasable on the live site."
    - "`wrangler.jsonc` `vars` -> `lib/commerce/capabilities.ts`: acquisition enabled without reconciliation throws at capability resolution, so the post-push clean cron cycle is what proves the pair resolved."
    - "Phase 10's deferred human verification -> this gate: the code being switched on is the code that was never walked through in a browser."
  prohibitions:
    - "The acquisition flag is not committed, and not pushed, before the D-07 checkpoint returns a decision to proceed."
    - "The checkpoint is never auto-approved. `gate=\"blocking-human\"` holds in every mode, including an unattended run."
    - "No secret value in any output, commit, document, summary, or planning artifact."
    - "No secret name or value in `wrangler.jsonc`; only the one feature-flag string is added."
    - "No `npm run deploy` and no `npm run deploy:ci` from this machine."
    - "No `git add .`; only `wrangler.jsonc` and `cloudflare-env.d.ts` are staged."
    - "`cloudflare-env.d.ts` is never hand-edited."
    - "No code change under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`; no migration."
    - "The local env file is never read or named literally in a shell command; use a glob when moving it."
---

<objective>
Ask Russell the one question this phase cannot answer for him, then — if he says go — make
gift cards purchasable on the live site and prove the whole phase holds together.

Purpose: the second half of OPS-02, plus the phase gate. Enabling acquisition is the only
step that changes what a real shopper can do, and the code it switches on is Phase 10's,
whose human verification is still deferred.
Output: a recorded decision, one flag deployed, and a green CI-mirroring suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-enablement/11-CONTEXT.md
@.planning/phases/11-production-enablement/11-RESEARCH.md
@.planning/phases/11-production-enablement/11-04-SUMMARY.md
@.planning/phases/10-gift-card-purchase-flow/10-VERIFICATION.md
@.planning/phases/10-gift-card-purchase-flow/10-UAT.md
</context>

## Artifacts this phase produces

Phase-wide set; this plan delivers the rows marked **[11-05]**.

| Artifact | Plan |
|---|---|
| Five ring-shape invariants in `tests/unit/lib/gift-cards/config.test.ts` | 11-01 |
| `.dev.vars` — untracked local development ring | 11-01 |
| Two delivery-ring placeholder lines plus a read-path comment in `.env.example` | 11-02 |
| `tests/unit/scripts/env-example-gift-card-shape.test.ts` | 11-02 |
| Delivery-ring table row and paragraph in `docs/runtime-configuration.md` | 11-03 |
| `## 9. Gift Card Enablement` in `docs/DEPLOYMENT_SETUP.md` | 11-03 |
| Four production Worker secret **names** — values exist only inside Cloudflare | 11-04 |
| `STORE_FEATURE_GIFT_CARD_RECONCILIATION: "true"` + regenerated `cloudflare-env.d.ts` | 11-04 |
| **[11-05]** `STORE_FEATURE_GIFT_CARD_ACQUISITION: "true"` + regenerated `cloudflare-env.d.ts` | 11-05 |
| **[11-05]** A recorded D-07 decision | 11-05 |

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Turn on gift-card acquisition in production, with Phase 10's human verification still deferred?</decision>
  <context>
    Everything up to this point sells nothing. Reconciliation is live: existing gift-card
    reservations can be settled and released, and the five-minute delivery drain is running clean
    against a real key ring. No gift card can be bought.

    Setting `STORE_FEATURE_GIFT_CARD_ACQUISITION` to `"true"` changes that. From the moment
    Workers Builds finishes the deploy, a real shopper can buy a gift card on the live site with a
    real card.

    Two things are worth having in front of you before you answer.

    First, Phase 10 built that purchase flow — the recipient form, the cart line, the digital-only
    checkout, the confirmation and account views — and its human verification is still open. Its
    verification record stands at 15 of 15 non-visual truths verified, with 9 visual and
    interactive truths routed to a human, and `10-UAT.md` has 6 scenarios still marked pending.
    Nobody has walked that flow through a browser.

    Second, the reconciliation push in plan 11-04 already deployed that code — roughly 87
    accumulated commits, including all of Phase 10 — because you approved that on 2026-09-08
    ("Push as planned"). So Phase 10's code is already running in production. What this flag
    changes is not whether it is deployed but whether a shopper can reach it and pay.

    One check remains that only you can run, and it is worth doing before you answer: signed in as
    yourself on the live site, open the account gift-cards view (or call `GET /api/gift-cards` from
    that signed-in session). A 200 with a `cards` array — empty is correct, nothing has been sold —
    means the key ring and the database are healthy. A 503 saying gift cards are temporarily
    unavailable means they are not, and acquisition should not go on until that is understood.

    This is reversible by flag: set it back to `"false"` and push. It is not reversible for a
    shopper who has already paid.
  </context>
  <options>
    <option id="verified-10-passed">
      <name>Phase 10 has been verified — proceed</name>
      <pros>The purchase flow has been walked through by a human before real money can reach it. This is the intended order.</pros>
      <cons>Requires running `/gsd-verify-work 10` first, which is a separate session's worth of work.</cons>
    </option>
    <option id="enable-anyway">
      <name>Enable anyway, accepting the deferred verification</name>
      <pros>Unblocks Phase 12's live paid-order proof (SHOP-07), which cannot run until gift cards are purchasable. Reversible in one flag flip and one push.</pros>
      <cons>The first person to exercise the visual and interactive parts of the checkout flow may be a real shopper rather than you.</cons>
    </option>
    <option id="hold">
      <name>Hold — leave acquisition off for now</name>
      <pros>Nothing changes for shoppers. Reconciliation stays live and healthy; the secrets, docs, and example file from this phase all stand on their own.</pros>
      <cons>OPS-02 stays half-done and Phase 12 stays blocked until this is revisited.</cons>
    </option>
  </options>
  <resume-signal>Reply with `verified-10-passed`, `enable-anyway`, or `hold`. On `hold`, stop the plan here and record the decision — do not commit or push the acquisition flag.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Enable gift-card acquisition and deploy it</name>
  <reversibility rating="reversible">Set the var back to the string `"false"` and push; Workers Builds redeploys and new gift-card sales stop. Reconciliation stays on so existing balances and reservations keep working. Nothing schema-level changes, so there is no migration to undo.</reversibility>
  <precondition>The D-07 checkpoint above returned `verified-10-passed` or `enable-anyway`. If it returned `hold`, or if no decision was recorded, this task does not run.</precondition>
  <files>wrangler.jsonc, cloudflare-env.d.ts</files>
  <read_first>
    - `wrangler.jsonc` (the `vars` block as left by 11-04, with the reconciliation flag and its comment)
    - `cloudflare-env.d.ts` (lines 1-40 — the generated interface and the `ProcessEnv` pick list)
    - `lib/commerce/capabilities.ts` (lines 150-220 — the throw that fires if acquisition is on without reconciliation, and what acquisition additionally permits)
    - `lib/commerce/runtime.ts` (lines 20-35)
    - `lib/observability/scheduled.ts` (full file)
    - `lib/gift-cards/config.ts` (full file)
    - `lib/gift-cards/encryption.ts` (lines 1-25)
    - `app/api/payment-intent/route.ts` (lines 85-110 — where a gift-card token reaches the capability)
    - `docs/runtime-configuration.md` (the gift-card flag paragraph and the two ring paragraphs)
    - `docs/DEPLOYMENT_SETUP.md` (section 9 step 4 — this task performs what it documents)
    - `.env.example` (lines 30-50)
    - `package.json` (scripts block)
    - `AGENTS.md` (Rules section)
    - `.planning/phases/11-production-enablement/11-PATTERNS.md` (the `wrangler.jsonc` and `cloudflare-env.d.ts` analog sections; commit `24f964e` as the regenerate-only template)
    - `.planning/phases/11-production-enablement/11-04-SUMMARY.md` (the deployment version and cron result the second flag builds on)
  </read_first>
  <action>
    Implements D-05's second flag and D-07's outcome.

    Step 0 — start clean. `git diff --quiet -- wrangler.jsonc cloudflare-env.d.ts` must pass, and
    `git fetch origin` followed by `git rev-list --count origin/main..HEAD` must be zero: plan
    11-04's push landed and nothing has drifted since. If either check fails, halt and report.

    Step 1 — add one var. In `wrangler.jsonc`'s `vars` block, immediately after the reconciliation
    entry added in 11-04, add `"STORE_FEATURE_GIFT_CARD_ACQUISITION": "true"` — a string, matching
    the subscription pair's style. Add the comma the previous entry now needs. Do not restate the
    ordering rule; the comment block written in 11-04 already sits above both entries and covers
    them, exactly as the subscription block's single comment covers its pair.

    Step 2 — regenerate the types under CI's conditions. Same wrapper as 11-04: move `.env*.local`
    and `.dev.vars` into a directory under the system temp path, run `mise exec -- npm run cf-typegen`,
    then `mise exec -- npm run cf-typecheck`, then move both back, restoring even when a command in
    the middle fails. `wrangler types` reads both files, so leaving them in place would write local-only
    names into a committed file and diverge from what CI generates. Use the glob form for the local env
    file rather than naming it literally.

    Step 3 — commit exactly two files. `git add wrangler.jsonc cloudflare-env.d.ts` and nothing else.
    Never `git add .`.

    Step 4 — push and wait. Capture the newest version id from
    `mise exec -- npx wrangler deployments list`, then `git push origin main`, then poll until a
    newer version appears. Workers Builds performs the deploy; run no deploy script from this
    machine.

    Step 5 — prove the pair resolved. Acquisition enabled without reconciliation throws at
    capability resolution on the first request or cron tick after a deploy, so a clean cycle is the
    proof that both flags are consistent. Repeat 11-04 Task 3's cron watch against this deploy:
    start `mise exec -- npx wrangler tail --format json` in the background writing to a capture file
    under the system temp directory, poll until the recovery-drain success line appears, allow up to
    about seven minutes, stop the tail, assert the success line is present and the critical
    recovery-failure event is absent, then delete the capture.
  </action>
  <acceptance_criteria>
    - `grep -c '"STORE_FEATURE_GIFT_CARD_ACQUISITION": "true"' wrangler.jsonc` equals `1`.
    - `grep -c '"STORE_FEATURE_GIFT_CARD_RECONCILIATION": "true"' wrangler.jsonc` equals `1` — the first flag was not disturbed.
    - `grep -n 'STORE_FEATURE_GIFT_CARD' wrangler.jsonc` lists reconciliation before acquisition.
    - `grep -c 'STORE_FEATURE_GIFT_CARD_ACQUISITION' cloudflare-env.d.ts` is at least `2`.
    - `grep -c 'GIFT_CARD_C' cloudflare-env.d.ts` equals `0` — no secret name leaked into type generation from a local env file. <!-- planner-discipline-allow: GIFT_CARD_C -->
    - `mise exec -- npm run cf-typecheck`, run with the local env files moved aside, exits 0.
    - `git diff-tree --no-commit-id --name-only -r HEAD` lists exactly `wrangler.jsonc` and `cloudflare-env.d.ts`.
    - `git rev-list --count origin/main..HEAD` equals `0` after the push.
    - The post-push capture holds the drain success line at least once and the critical recovery-failure event zero times.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '"STORE_FEATURE_GIFT_CARD_ACQUISITION": "true"' wrangler.jsonc)" -eq 1 && test "$(grep -c '"STORE_FEATURE_GIFT_CARD_RECONCILIATION": "true"' wrangler.jsonc)" -eq 1 && test "$(grep -c 'GIFT_CARD_C' cloudflare-env.d.ts)" -eq 0 && git diff-tree --no-commit-id --name-only -r HEAD > "${TMPDIR:-/tmp}/gsd11-acq-files.txt" && test "$(sort "${TMPDIR:-/tmp}/gsd11-acq-files.txt" | tr '\n' ' ')" = "cloudflare-env.d.ts wrangler.jsonc " && git fetch origin && test "$(git rev-list --count origin/main..HEAD)" -eq 0 && test "$(grep -c 'recovery queues drained' "${TMPDIR:-/tmp}/mercora-11-cron-acq.jsonl")" -ge 1 && test "$(grep -c 'cron.recovery_failed' "${TMPDIR:-/tmp}/mercora-11-cron-acq.jsonl")" -eq 0</automated>
    <fails_when>Exits non-zero: the acquisition flag is missing or not the string `"true"`, the reconciliation flag was disturbed, a gift-card secret name leaked into the generated types, the commit touched other files, the push did not happen, or the post-deploy cron cycle shows a critical recovery-failure event — which after this particular deploy most likely means capability resolution is throwing because the two flags are inconsistent.</fails_when>
    <human-check>On the live site, open the gift card product page as a shopper and confirm the recipient form and add-to-cart controls are present and usable, and that a gift card can be added to the cart. This is the first moment a shopper-visible change exists; it is also the visual half of Phase 10's still-open verification, so anything that looks wrong here belongs in `/gsd-verify-work 10` rather than being fixed inline.</human-check>
  </verify>
  <done>Both flags are `"true"` in the deployed configuration, in the right order, the generated types match CI's, and one clean cron cycle after the deploy shows capability resolution did not throw.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full CI gate suite and re-check that no key reached git</name>
  <files>none — verification only</files>
  <read_first>
    - `.github/workflows/ci.yml` (the gate list and its order — this task mirrors it)
    - `AGENTS.md` (Gates section)
    - `CONTRIBUTING.md` (the pre-commit subset contributors run)
    - `package.json` (scripts block)
    - `wrangler.jsonc` (the `vars` block as left by Task 2)
    - `cloudflare-env.d.ts` (lines 1-40)
    - `.env.example` (lines 30-50)
    - `docs/runtime-configuration.md`, `docs/DEPLOYMENT_SETUP.md` (as edited in 11-03)
    - `lib/gift-cards/config.ts`, `lib/gift-cards/encryption.ts`, `lib/commerce/capabilities.ts`, `lib/observability/scheduled.ts` (unchanged by this phase — confirm that is still true)
    - `.planning/phases/11-production-enablement/11-VALIDATION.md` (the per-task verification map this suite closes out)
  </read_first>
  <action>
    Run the CI gate list in `.github/workflows/ci.yml` order, all under `mise exec --`:
    `npm audit --omit=dev --audit-level=high`, `npm run build:themes:check`, `npm run scan:tokens`,
    `npm run lint`, `npm run typecheck`, `npm run cf-typecheck`, `npm test`, `npm run test:workers`,
    `npm run test:observability-worker`, `npm run build`. Add `npm run docs:lint`, which this phase
    edited documentation for. Skip `npm run check:migrations` — it is pull-request-only and this
    phase adds no migration.

    Run `cf-typecheck` inside the same move-aside wrapper used in Task 2. It fails with the local
    env files present; that is a property of this machine, not of the change.

    Then re-run the two whole-phase security checks, now that every commit is pushed.

    First, scan the added lines of the phase's commits — restricted to `wrangler.jsonc`,
    `cloudflare-env.d.ts`, `.env.example`, and `docs/` — for an unbroken run of twenty or more
    base64 characters behind the delivery prefix. Expect zero. Scope it to those paths: the test
    files carry prefixed placeholder values on purpose, and a repository-wide scan would flag them
    and train everyone to ignore the check.

    Second, confirm this phase did not touch code it was forbidden to touch. `git diff --name-only`
    across the phase's commits must list no path under `lib/gift-cards/`, `lib/services/`,
    `app/api/`, `lib/commerce/`, or `migrations/`.

    Define the phase range once before running either scan: set `PHASE_BASE` to the commit
    immediately before this phase's first commit — take that SHA from `11-01-SUMMARY.md`, which
    records the commit it created — and use `"$PHASE_BASE"..HEAD` for both. Do not guess it with a
    fixed `HEAD~n` offset; the commit count varies with whether the deploy-state fallback fired in
    11-04.

    Finally, update `.planning/phases/11-production-enablement/11-VALIDATION.md`: mark each task row
    green or red with the command that was actually run, and set `nyquist_compliant: true` only if
    every task in the phase had a runnable automated verification that passed. Leave the two
    human-check rows as manual-only, listed with their instructions, since they are harvested at
    end-of-phase into the UAT flow.
  </action>
  <acceptance_criteria>
    - Every command in the CI gate list exits 0.
    - `mise exec -- npm run docs:lint` exits 0.
    - The scoped scan of added lines across `"$PHASE_BASE"..HEAD` for a key-shaped base64 run returns `0`.
    - `git diff --name-only "$PHASE_BASE"..HEAD` lists no path under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`, or `migrations/`.
    - `git status --porcelain` lists no `.dev.vars` entry and no leftover temp-aside file.
    - `11-VALIDATION.md` has every task row filled with a real command and a real status.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run docs:lint && mise exec -- npm run scan:tokens && mise exec -- npm test && mise exec -- npm run test:workers && mise exec -- npm run test:observability-worker && mise exec -- npm run build && test -n "$PHASE_BASE" && git diff --name-only "$PHASE_BASE"..HEAD > "${TMPDIR:-/tmp}/gsd11-phase-files.txt" && test "$(grep -cE '^(lib/gift-cards|lib/services|app/api|lib/commerce|migrations)/' "${TMPDIR:-/tmp}/gsd11-phase-files.txt")" -eq 0 && git log -p "$PHASE_BASE"..HEAD -- wrangler.jsonc cloudflare-env.d.ts .env.example docs/ > "${TMPDIR:-/tmp}/gsd11-phase-range.patch" && test "$(grep -E '^\+' "${TMPDIR:-/tmp}/gsd11-phase-range.patch" | grep -vE '^\+\+\+' | grep -cE 'base64:[A-Za-z0-9+/]{20,}')" -eq 0</automated>
    <fails_when>Exits non-zero: any CI gate fails (lint, typecheck, docs:lint, token scan, the three test suites, or the production build), `PHASE_BASE` is unset so the range scans are meaningless, the phase touched a forbidden path under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`, or `migrations/`, or an added line in the phase's commits carries a key-shaped base64 run — which means a value reached git and the push must be treated as a disclosure.</fails_when>
  </verify>
  <done>Every CI gate is green, the phase provably touched no forbidden path, no key-shaped material reached git, and `11-VALIDATION.md` records what was actually run.</done>
</task>

</tasks>

<!-- planner-discipline-allow: cron.recovery_failed -->
<!-- planner-discipline-allow: GIFT_CARD_C -->
<!-- planner-discipline-allow: base64:[A-Za-z0-9+/]{20,} -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the D-07 gate -> live commerce | The only boundary in this phase that a shopper can feel. Crossing it means real cards can be charged for gift cards. |
| `wrangler.jsonc` `vars` -> `resolveCommerceCapabilities` | An inconsistent flag pair throws at capability resolution, on the first request or cron tick after the deploy. |
| local repository -> `origin/main` -> production | Push is deploy. There is no staging step between them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-21 | Elevation of Privilege | Acquisition enabled without a human decision | critical | mitigate | `gate="blocking-human"` on the D-07 checkpoint, which is never auto-approved in any mode including an unattended run; Task 2 additionally carries a precondition that the checkpoint returned a proceed answer. |
| T-11-22 | Elevation of Privilege | An inconsistent flag pair reaching production | high | mitigate | Acceptance criteria require both flags present as the string `"true"` with reconciliation first; the code independently throws at capability resolution; the post-deploy cron watch is what detects that throw. |
| T-11-23 | Tampering | Selling through a purchase flow no human has walked | high | accept | Accepted only if Russell chooses `enable-anyway` at D-07, with the deferred verification stated plainly in the checkpoint copy. Reversible in one flag flip and one push. If he chooses `hold`, the task does not run. |
| T-11-24 | Information Disclosure | Secret names leaking into `cloudflare-env.d.ts` | medium | mitigate | Both local env files are moved out of the tree around `cf-typegen` and `cf-typecheck`, with an acceptance criterion requiring zero gift-card secret names in the generated file. |
| T-11-25 | Information Disclosure | A key value reaching git across the whole phase | critical | mitigate | Task 3 re-runs the scoped added-line scan across the entire phase range after everything is pushed, not just per-plan. |
| T-11-26 | Tampering | The phase silently editing code it was scoped out of | medium | mitigate | Task 3 asserts the phase's diff touches no path under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`, or `migrations/`. |
| T-11-20 | Information Disclosure | The production tail capture file | medium | mitigate | Written under the system temp directory, never inside the repository, deleted immediately after the assertion. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; no new dependency to vet. |
</threat_model>

<verification>
- The D-07 checkpoint returned a recorded decision, and the SUMMARY names which option and on what evidence.
- Both gift-card flags are `"true"` in `wrangler.jsonc`, reconciliation listed first.
- `mise exec -- npm run cf-typecheck`, with local env files moved aside, exits 0.
- `mise exec -- npx wrangler deployments list` shows the post-acquisition version as current.
- One five-minute cron cycle after the acquisition deploy runs clean.
- Every CI gate command exits 0, including `mise exec -- npm run build`.
- The phase-range scans return 0 for key-shaped base64 and 0 for forbidden paths.
</verification>

<success_criteria>
Gift cards are purchasable on the live site because Russell decided they should be, both flags
are deployed in the order the code enforces, and the full CI gate suite plus two whole-phase
security scans are green — or, if he chose to hold, acquisition stays off and everything else
this phase built still stands.
</success_criteria>

<output>
Create `.planning/phases/11-production-enablement/11-05-SUMMARY.md` when done. Record the D-07
option chosen and the evidence it rested on, the acquisition deployment version id, the cron-cycle
result, and the CI gate results. Record no value. If the decision was `hold`, say so plainly and
record OPS-02 as half-complete with the exact remaining step.
</output>
