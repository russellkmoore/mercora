---
phase: 11-production-enablement
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .env.example
  - tests/unit/scripts/env-example-gift-card-shape.test.ts
autonomous: true
requirements: [OPS-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`.env.example` names all four gift-card secrets — `GIFT_CARD_CODE_HMAC_CURRENT_VERSION`, `GIFT_CARD_CODE_HMAC_KEYS_JSON`, `GIFT_CARD_DELIVERY_CURRENT_VERSION`, `GIFT_CARD_DELIVERY_KEYS_JSON` — exactly once each (D-11)."
    - "The two delivery lines sit adjacent to the two HMAC lines: the four appear in that order with no other variable assignment between the first and the last (D-11)."
    - "The delivery placeholder is REJECTED by `parseGiftCardDeliveryKeyRing` exactly as written and ACCEPTED once its payload is replaced with a real base64 32-byte value; the existing HMAC placeholder is ACCEPTED as written because it is already over the 32-byte floor, so it is a working non-secret a developer must still replace. A test proves both statements (OPS-04 edge probe)."
    - "`.env.example` states that local development reads the gift-card values from `.dev.vars`, not `.env.local`, and says why (D-12)."
    - "No placeholder in `.env.example` can be mistaken for real key material: every placeholder payload contains hyphenated English words, never a run of 20 or more unbroken base64 characters."
  artifacts:
    - ".env.example — two delivery-ring placeholder lines plus a local-read-path comment"
    - "tests/unit/scripts/env-example-gift-card-shape.test.ts — source-contract test that reads the file and feeds its placeholders through the real parsers"
  key_links:
    - "`.env.example` text -> `lib/gift-cards/config.ts` parsers: the contract test reads the file, so the documented shape cannot drift from the enforced shape."
    - "The D-12 comment -> `next.config.ts` `initOpenNextCloudflareForDev()`: the reason `.dev.vars` and not `.env.local` is the local read path."
  prohibitions:
    - "No real or generated key value in `.env.example`, in the test, or in any commit."
    - "No change to the two existing HMAC placeholder lines — D-11 scopes this to two added lines plus one comment."
    - "No code change under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`; no migration."
    - "`.env.local` is never read, moved, opened, or referenced by name in a shell command in this plan."
    - "No `git add .`; no production command; no push."
---

<objective>
Give a developer everything needed to run the full gift-card flow locally: all four secret
names in `.env.example` with placeholders that cannot pass for real keys, the generation
command beside them, and a plain statement that these values are read from `.dev.vars`.
Then pin all of it with a test that reads the file and runs its placeholders through the
real parsers, so the example can never drift from what the code accepts.

Purpose: OPS-04. The example file is the only onboarding surface for these four values.
Output: an extended `.env.example` and a source-contract test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-enablement/11-CONTEXT.md
@.planning/phases/11-production-enablement/11-PATTERNS.md
@.planning/phases/11-production-enablement/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide set; this plan delivers the rows marked **[11-02]**.

| Artifact | Plan |
|---|---|
| Five ring-shape invariants in `tests/unit/lib/gift-cards/config.test.ts` | 11-01 |
| `.dev.vars` — untracked local development ring | 11-01 |
| **[11-02]** Two delivery-ring placeholder lines plus a `.dev.vars` read-path comment in `.env.example` | 11-02 |
| **[11-02]** `tests/unit/scripts/env-example-gift-card-shape.test.ts` | 11-02 |
| Delivery-ring table row and paragraph in `docs/runtime-configuration.md` | 11-03 |
| `## 9. Gift Card Enablement` in `docs/DEPLOYMENT_SETUP.md` | 11-03 |
| Four production Worker secret **names** — values exist only inside Cloudflare | 11-04 |
| `STORE_FEATURE_GIFT_CARD_RECONCILIATION: "true"` + regenerated `cloudflare-env.d.ts` | 11-04 |
| `STORE_FEATURE_GIFT_CARD_ACQUISITION: "true"` + regenerated `cloudflare-env.d.ts` | 11-05 |

<tasks>

<task type="auto">
  <name>Task 1: Add the delivery-ring placeholders and the local read-path comment to .env.example</name>
  <files>.env.example</files>
  <read_first>
    - `.env.example` (full file, 65 lines — especially the gift-card block at lines 34-42 and the closing Instructions block)
    - `lib/gift-cards/config.ts` (full file — the delivery-ring regex and the 32-byte decode check that decide whether a placeholder parses)
    - `lib/gift-cards/encryption.ts` (lines 1-25 — `GIFT_CARD_DELIVERY_KEY_BYTES`)
    - `lib/commerce/capabilities.ts` (lines 150-220 — the flag-order rule the existing comment block already states)
    - `lib/observability/scheduled.ts` (full file)
    - `next.config.ts` (lines 60-70 — `initOpenNextCloudflareForDev()`, the fact the new comment asserts)
    - `docs/runtime-configuration.md` (lines 20-70)
    - `docs/DEPLOYMENT_SETUP.md` (lines 446-479)
    - `wrangler.jsonc` (lines 108-126)
    - `package.json` (scripts block)
    - `AGENTS.md` (Setup step 4, Environment section)
    - `.planning/phases/11-production-enablement/11-PATTERNS.md` (the `.env.example` analog section — the exact comment style to copy)
  </read_first>
  <action>
    Implements D-11 and D-12.

    Edit 1 — the local read-path comment (D-12). Insert immediately ABOVE the existing line
    `# Server-only versioned HMAC lookup ring. Put real values in ...` (currently line 39) a
    five-line comment block stating, in the file's existing instructional tone: local development
    reads the four gift-card values below — and every other Cloudflare binding or secret — from
    `.dev.vars`, not from this file's copy; `next.config.ts` calls `initOpenNextCloudflareForDev()`,
    which backs `getCloudflareContext().env` with `.dev.vars`; the sibling local env file only
    supplies `NEXT_PUBLIC_*` and other plain `process.env` values.

    This wording is the corrected form. D-12 originally proposed a split by runner ("dev server
    reads one file, wrangler-driven runs read the other"), and 11-RESEARCH.md Pitfall 6 disproved
    it: every gift-card read in this codebase goes through `getCloudflareContext().env`, so
    `.dev.vars` is the read path under `npm run dev` as well. Write the corrected statement; do not
    write the original one.

    Edit 2 — the two delivery lines (D-11). Insert immediately AFTER the existing
    `# GIFT_CARD_CODE_HMAC_KEYS_JSON=...` line (currently line 42) a three-line comment followed by
    two commented assignments, matching the surrounding `# ` style exactly:

    Comment: server-only versioned AES-256 delivery ring; each value is the literal prefix
    `base64:` followed by a base64-encoded 32-byte key, generated with `openssl rand -base64 32`;
    the placeholder below is deliberately rejected by the parser until it is replaced.

    Line: `# GIFT_CARD_DELIVERY_CURRENT_VERSION=1`

    Line: `# GIFT_CARD_DELIVERY_KEYS_JSON={"1":"base64:replace-with-32-random-bytes-base64-encoded"}`

    The placeholder payload must stay hyphenated English. That is not cosmetic: hyphens make it
    fail the delivery-ring character-class regex, which is what makes it fail closed until a
    developer replaces it, and they also keep it from ever matching a leak-detection scan for an
    unbroken base64 run.

    Leave the two existing HMAC placeholder lines exactly as they are. D-11 scopes this edit to two
    added lines plus comments. Note for the SUMMARY, since it is a genuine asymmetry the next task
    proves: the existing HMAC placeholder is 33 UTF-8 bytes, over the 32-byte floor, so it PARSES as
    written — it is a working but publicly known value that a developer must still replace. The
    delivery placeholder does not parse. The comment on each block already carries the "put real
    values in `.dev.vars` or encrypted Workers secrets" instruction that covers both.

    Do not renumber, reorder, or reflow any other line in the file. Do not touch the `.env.local`
    file itself, and do not name it in a shell command — this runtime's secret-file guard refuses
    commands that do, and there is no reason to touch it here.
  </action>
  <acceptance_criteria>
    - `grep -c 'GIFT_CARD_DELIVERY_CURRENT_VERSION' .env.example` equals exactly `1`.
    - `grep -c 'GIFT_CARD_DELIVERY_KEYS_JSON' .env.example` equals exactly `1`.
    - `grep -c 'GIFT_CARD_CODE_HMAC_CURRENT_VERSION' .env.example` equals exactly `1`.
    - `grep -c 'GIFT_CARD_CODE_HMAC_KEYS_JSON' .env.example` equals exactly `1`.
    - `grep -c 'initOpenNextCloudflareForDev' .env.example` equals exactly `1`.
    - `grep -n 'GIFT_CARD_' .env.example` lists the four names in the order HMAC current, HMAC keys, delivery current, delivery keys, on four consecutive assignment lines with only comment lines between them.
    - `grep -cE 'base64:[A-Za-z0-9+/]{20,}' .env.example` equals `0` — no placeholder carries an unbroken base64 run that could pass for key material. <!-- planner-discipline-allow: base64:[A-Za-z0-9+/]{20,} -->
    - `git diff --stat -- .env.example` shows only additions, no deletions.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'GIFT_CARD_DELIVERY_CURRENT_VERSION' .env.example)" -eq 1 && test "$(grep -c 'GIFT_CARD_DELIVERY_KEYS_JSON' .env.example)" -eq 1 && test "$(grep -c 'GIFT_CARD_CODE_HMAC_KEYS_JSON' .env.example)" -eq 1 && test "$(grep -c 'initOpenNextCloudflareForDev' .env.example)" -eq 1 && test "$(grep -cE 'base64:[A-Za-z0-9+/]{20,}' .env.example)" -eq 0</automated>
    <fails_when>Exits non-zero: a delivery name is missing or duplicated, an HMAC name was accidentally duplicated by the insert, the local-read-path comment is missing (no `initOpenNextCloudflareForDev` mention), or a placeholder contains an unbroken 20-plus-character base64 run, which means something key-shaped was pasted in.</fails_when>
  </verify>
  <done>All four gift-card names appear once each in the documented order, the delivery placeholder is hyphenated English behind the required prefix, and the local read path is stated with its reason.</done>
</task>

<task type="auto">
  <name>Task 2: Pin .env.example against the real parsers with a source-contract test</name>
  <files>tests/unit/scripts/env-example-gift-card-shape.test.ts</files>
  <read_first>
    - `.env.example` (full file, as edited by Task 1)
    - `tests/unit/scripts/check-deploy-config.test.ts` (full file — the fixture-and-assert style analog named in 11-PATTERNS.md)
    - `tests/unit/lib/gift-cards/config.test.ts` (full file, as extended by 11-01 — reuse its assertion style, do not duplicate its cases)
    - `lib/gift-cards/config.ts` (full file — both parsers and the exported env-name constants)
    - `lib/gift-cards/encryption.ts` (lines 1-25)
    - `lib/commerce/capabilities.ts` (lines 150-220)
    - `lib/observability/scheduled.ts` (full file)
    - `docs/runtime-configuration.md` (lines 20-70)
    - `docs/DEPLOYMENT_SETUP.md` (lines 446-479)
    - `wrangler.jsonc` (lines 108-126)
    - `package.json` (scripts block, and the vitest config include globs in `vitest.config.mts`)
    - `AGENTS.md` (Gates section)
    - `.planning/phases/11-production-enablement/11-PATTERNS.md` (the source-contract-test section — this is a role-match with no exact prior art; the analog supplies style, not structure)
  </read_first>
  <behavior>
    The test reads `.env.example` from disk with `readFileSync`, strips the leading comment marker
    from the gift-card lines, and parses each into a name and a raw value. Then:

    - Each of the four gift-card names appears exactly once in the whole file.
    - The four appear in the documented order — HMAC current version, HMAC keys, delivery current
      version, delivery keys — and no other `NAME=` assignment line falls between the first and the
      last of them.
    - Feeding the two HMAC values to `parseGiftCardCodeKeyRing` returns a ring. The documented HMAC
      placeholder is over the 32-byte floor, so it parses as written. The test asserts this
      explicitly and carries a comment saying why that matters: it is a publicly known value, not a
      secret, and a developer must still replace it.
    - Feeding the two delivery values to `parseGiftCardDeliveryKeyRing` throws
      `"Gift-card runtime configuration is unavailable"`. The documented delivery placeholder is
      rejected as written.
    - Replacing only the delivery placeholder's payload with a canonical base64 encoding of 32
      bytes — built in memory with `btoa`, exactly as 11-01 does — makes the same parser return a
      ring. This is the "accepted only when replaced" half of the claim.
  </behavior>
  <action>
    Implements OPS-04's edge probe: state which placeholders the parsers accept and which they
    reject, and prove it rather than asserting it.

    Create `tests/unit/scripts/env-example-gift-card-shape.test.ts`. Put it under
    `tests/unit/scripts/` beside `check-deploy-config.test.ts` so it is picked up by the same
    vitest include globs; confirm that against `vitest.config.mts` before writing.

    Read the file with `readFileSync` relative to `process.cwd()`, which vitest roots at the
    repository root. Do not import `.env.example`, do not shell out, and do not read `.dev.vars`
    or the local env file — the point of this test is that it depends only on a committed,
    non-secret file.

    Extract assignments with a regex that tolerates the leading comment marker and captures the
    name and everything after the first `=`. For the ordering assertion, collect every assignment
    line in file order and check that the four gift-card names occupy four consecutive positions in
    that list. That definition is comment-tolerant, which is what "adjacent" has to mean in a file
    whose every line is a comment.

    Import `parseGiftCardCodeKeyRing` and `parseGiftCardDeliveryKeyRing` from
    `@/lib/gift-cards/config` — the real parsers, never a reimplementation. 11-RESEARCH.md's
    "Don't Hand-Roll" table is explicit that a second validator would drift from the enforced one.

    Give each case a name that states the claim, so a future failure reads as a sentence: the
    documented HMAC placeholder parses as written; the documented delivery placeholder is rejected
    as written; the delivery placeholder parses once its payload is replaced with a real 32-byte
    key.
  </action>
  <acceptance_criteria>
    - `tests/unit/scripts/env-example-gift-card-shape.test.ts` exists and `grep -c 'readFileSync' tests/unit/scripts/env-example-gift-card-shape.test.ts` is at least `1`.
    - `grep -c 'parseGiftCardDeliveryKeyRing' tests/unit/scripts/env-example-gift-card-shape.test.ts` is at least `2` (one rejection case, one post-replacement acceptance case).
    - `grep -cE 'GIFT_CARD_(CODE_HMAC|DELIVERY)_(CURRENT_VERSION|KEYS_JSON)' tests/unit/scripts/env-example-gift-card-shape.test.ts` is at least `4`.
    - `mise exec -- npx vitest run tests/unit/scripts/env-example-gift-card-shape.test.ts` exits 0 with at least 4 passing cases.
    - `mise exec -- npm run lint` and `mise exec -- npm run typecheck` both exit 0.
    - `mise exec -- npm test` exits 0 — the new file does not disturb the existing suite.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/scripts/env-example-gift-card-shape.test.ts && test "$(grep -c 'parseGiftCardDeliveryKeyRing' tests/unit/scripts/env-example-gift-card-shape.test.ts)" -ge 2 && mise exec -- npm test</automated>
    <fails_when>Exits non-zero: the four names are not present exactly once each or not consecutive (Task 1's edit landed in the wrong place), a placeholder's accept/reject behavior differs from what the test claims (the documented shape drifted from the enforced shape), the delivery parser is referenced fewer than twice (the "accepted when replaced" half is missing), or the full unit suite regressed.</fails_when>
  </verify>
  <done>A committed test reads `.env.example` and proves, against the real parsers, exactly which documented placeholder works as written and which fails closed until replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.example` -> every developer who clones the repo | Anything key-shaped here is copied into local environments and possibly beyond. |
| `.env.example` text -> `lib/gift-cards/config.ts` parsers | Documentation drift here produces a local environment that silently fails closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05 | Information Disclosure | `.env.example` placeholders | high | mitigate | Placeholder payloads are hyphenated English words, enforced by an acceptance criterion that no unbroken 20-plus-character base64 run exists anywhere in the file. |
| T-11-06 | Spoofing | A placeholder mistaken for a working secret | medium | mitigate | The delivery placeholder is rejected by the parser until replaced, proven by Task 2. The HMAC placeholder's accept-as-written behavior is stated explicitly in the test rather than left for a developer to discover. |
| T-11-07 | Tampering | Documented shape drifting from enforced shape | medium | mitigate | Task 2 reads the committed file and feeds it through the real parsers, so a parser change or an example edit breaks CI rather than a developer's afternoon. |
| T-11-08 | Information Disclosure | Local env file handling | medium | mitigate | This plan never reads, moves, or names the local env file; the only local file it discusses is `.dev.vars`, which `.gitignore` covers (proven in 11-01). |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; no new dependency to vet. |
</threat_model>

<verification>
- `mise exec -- npm test` exits 0.
- `mise exec -- npm run lint` exits 0.
- `mise exec -- npm run typecheck` exits 0.
- `git diff --stat -- .env.example` shows additions only.
- `git log -p -1 | grep -cE 'base64:[A-Za-z0-9+/]{20,}'` equals 0 for the commit this plan produces. <!-- planner-discipline-allow: base64:[A-Za-z0-9+/]{20,} -->
</verification>

<success_criteria>
A developer reading `.env.example` alone can name all four gift-card secrets, knows which
local file the runtime reads them from and why, has the generation command in front of them,
and cannot mistake either placeholder for a real key — and a test in CI keeps all of that true.
</success_criteria>

<output>
Create `.planning/phases/11-production-enablement/11-02-SUMMARY.md` when done. Record the
accept/reject verdict for each documented placeholder as a plain statement, since that is
OPS-04's open probe item. Record no value.
</output>
