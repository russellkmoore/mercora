---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
plan: 02
subsystem: theming
tags: [theme-presets, css-tokens, next-font, d1-settings, accessibility-contrast]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "themes/clinical.css tracer proof — the pipeline (theme file -> unmodified validator -> codegen -> barrel/manifest -> resolver -> served attribute) needs zero mechanism changes for a new preset"
  - phase: 06-01
    provides: "scripts/build-themes.mjs validator/codegen (unmodified), THEME_MANIFEST shape"
provides:
  - "app/layout.tsx — Orbitron, Fraunces and Nunito loaded once via next/font/google, preload: false, exposed as --font-orbitron/--font-fraunces/--font-nunito"
  - "themes/retro.css — fifth preset (second dark), magenta-on-deep-purple, no doc value needed correction, Orbitron display face"
  - "themes/atelier.css — sixth preset, clay accent on warm linen, corrected primary/ring, sage info folded from doc's badge accent, Fraunces display face"
  - "themes/market.css — seventh preset, produce-green accent on off-white, corrected primary/ring, doc's largest radius outlier, Nunito display face"
  - "Regenerated themes/index.generated.css (7 imports) and lib/themes/manifest.generated.ts (7 entries), sorted filename order atelier/clinical/luxe/market/midnight/retro/volt-dark"
affects: [06.1-03, 06.1-04]

# Actuals (#2632)
actuals:
  tokens: 8100
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: ["next/font/google: Orbitron", "next/font/google: Fraunces", "next/font/google: Nunito"]
  patterns:
    - "Font weight arrays extend the UI-SPEC's per-face floor with a superset covering the storefront's actual heading weight classes (font-semibold/600, font-bold/700, font-extrabold/800), confirmed against the installed next/font/google font-data manifest rather than assumed — costs nothing extra under preload: false."
    - "retro.css and atelier.css/market.css repeat clinical's proven shape unaided: head comment, @theme header, one attribute-selector block, four grouped comment sections — the pipeline mechanism required zero changes for a fifth, sixth and seventh file."
    - "Task 2 wrote the three theme files without running the generator (per the plan's ordering instruction); Task 3's own verify block ran the generator for the first time over all three simultaneously, so any authoring mistake would have surfaced as one whole-directory validator failure rather than three partial ones — no mistake surfaced."

key-files:
  created:
    - themes/retro.css
    - themes/atelier.css
    - themes/market.css
  modified:
    - app/layout.tsx
    - themes/index.generated.css
    - lib/themes/manifest.generated.ts

key-decisions:
  - "Font weight arrays are Orbitron [600,700,800], Fraunces [500,600,700,800], Nunito [600,700,800] — each UI-SPEC's listed floor extended with the storefront's actual font-semibold/font-bold/font-extrabold classes (verified by grep across app/ and components/: 171 font-semibold, 77 font-bold, 2 font-extrabold sites), all confirmed present in the installed font-data manifest with no fallback substitution needed."
  - "retro.css needed zero contrast corrections — all four required checks (primary/on-primary 8.98:1, foreground/surface 15.67:1, ring/surface 8.05:1, muted-foreground/surface 7.80:1) pass on the direction doc's literal oklch values."
  - "retro's info is independently derived rather than folded from the doc's accent-2, because retro's own success token already consumes that exact cyan literal; folding it again would collapse two status colours into one."
  - "retro's border is independently derived rather than repurposed from surface-sunken, because that doc value is darker than retro's own surface and would be invisible as a border — same precedent midnight established."
  - "atelier's accent-2 (sage) folds into info because the direction doc explicitly uses it for badges — a status/badge role, the exact condition the rename map reserves for folding rather than dropping."
  - "market's success deliberately keeps the doc's literal, uncorrected value even though primary (same hue family) required correction — success renders as tinted text/panel backgrounds, not solid white-on-green button text, so the AA failure that forced primary's correction doesn't apply to it. Recorded explicitly in the file header so a future reader doesn't 'fix' it into an inconsistency."
  - "market's radius scale is the phase's largest, anchoring the doc's literal 1rem at radius-xl and scaling the other three steps up from volt-dark's default — the doc explicitly names this preset the radius outlier."
  - "Task 2's action text said not to run the generator mid-task; Task 3's <verify> block requires running it — followed the verify block literally since it is the graded gate, and running it once over all three completed files (rather than never) is what actually proves Task 2's acceptance criteria."

patterns-established:
  - "Three more presets travel the entire pipeline (theme file -> unmodified validator -> codegen -> barrel/manifest -> resolver -> served attribute -> admin grid) with zero changes to any layer, for the fifth, sixth and seventh consecutive time — the mechanism is now proven at seven files, not just four."

requirements-completed: [THEME-05]

coverage:
  - id: D1
    description: "Three display faces (Orbitron, Fraunces, Nunito) load exactly once in app/layout.tsx via next/font/google, confirmed present in the installed font-data manifest before use, each preload: false, each exposed under the exact variable name the theme files reference"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "node -e font-data.json probe: Orbitron [400,500,600,700,800,900,variable], Fraunces [100..900,variable], Nunito [200..1000,variable] — all requested weights present"
        status: pass
      - kind: other
        ref: "grep assertions: preload:false count == 6, three variable-name greps found, body className carries exactly 6 font variables, single-line template literal"
        status: pass
      - kind: unit
        ref: "mise exec -- npm run lint / typecheck / test / build — all exit 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "themes/retro.css, themes/atelier.css and themes/market.css each ship as pure 23-token data files with the required @theme header, passing the unmodified build-themes validator, with every flagged deviation disclosed in its own header including measured before/after ratios"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "per-file grep assertions: 23 tokens, 1 rule, 17 hex colours, 0 at-rules, correct label, correct primary/on-primary/success/radius-xl literals, font-orbitron/fraunces/nunito references present, for all three files"
        status: pass
      - kind: unit
        ref: "tests/unit/lib/themes/token-contract.test.ts + tokens.test.ts — 30/30 pass (loop now covers seven files)"
        status: pass
    human_judgment: false
  - id: D3
    description: "The regenerated barrel and manifest list all seven themes in deterministic sorted filename order, regenerate idempotently, and every manifest label is distinct"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "mise exec -- node scripts/build-themes.mjs (twice) + --check (exit 0 both); grep -oE ordering assertions on both generated files == atelier clinical luxe market midnight retro volt-dark; label-count == unique-label-count == 7"
        status: pass
    human_judgment: false
  - id: D4
    description: "Selecting retro, atelier or market via a single D1 settings write makes a real dev server serve that name in the html element's data-theme attribute on the next request, with volt-dark restored and reconfirmed afterward"
    requirement: THEME-05
    verification:
      - kind: integration
        ref: "POST /api/admin/settings x-dev-admin bypass, body {updates:[{key:'appearance.theme',...}]}; fetch('/') data-theme match confirmed retro, atelier, market and the volt-dark restore, in that order, against a real npm run dev server"
        status: pass
    human_judgment: false
  - id: D5
    description: "The admin Appearance grid needed no code change to render all seven presets — components/admin/ stays untouched and its existing render tests pass unmodified"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "git status --porcelain components/admin/ — empty"
        status: pass
      - kind: unit
        ref: "tests/unit/app/admin-appearance-source.test.ts — 19/19 pass, unmodified"
        status: pass
    human_judgment: false
  - id: D6
    description: "The deploy-path guard rejects an invalid theme file: npm run build:worker fails with a [build-themes] message on a deliberately broken market.css, and the probe is fully reversible (file and generated output restored byte-identical to HEAD)"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "deliberate second-rule injection into market.css; npm run build:worker exit non-zero with '[build-themes] themes/market.css:97: unexpected extra rule...'; restored via cp, generator re-run, git diff HEAD -- themes/market.css empty"
        status: pass
    human_judgment: false
  - id: D7
    description: "Whole-tree gate stays green: scan:tokens 0 violations (unchanged manual-review rows), lint 0 errors, typecheck clean, full test suite green, production build green"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "mise exec -- npm run scan:tokens (0 violations, 2 unchanged manual-review rows); npm run lint (0 errors, 52 pre-existing warnings); npm run typecheck (clean); npm test (1959/1959 pass); npm run build (exit 0)"
        status: pass
    human_judgment: false

# Metrics
duration: 27min
completed: 2026-09-04
status: complete
---

# Phase 6.1 Plan 2: Retro, Atelier, Market Presets Summary

**Three more theme presets (`retro` dark, `atelier`/`market` light) reach the storefront through the unmodified pipeline, each carrying its own display face (Orbitron, Fraunces, Nunito) loaded once in the root layout — bringing the shipped total to seven presets with zero mechanism changes.**

## Performance

- **Duration:** ~27 min
- **Started:** 2026-09-04T22:20:00-07:00 (approx.)
- **Completed:** 2026-09-04T22:47:17-07:00
- **Tasks:** 3 completed
- **Files modified:** 6 (3 created, 3 modified)

## Accomplishments

- Added Orbitron, Fraunces and Nunito to `app/layout.tsx` via `next/font/google`, mirroring the existing Cormorant Garamond block exactly: `preload: false`, `display: "swap"`, exposed as `--font-orbitron`, `--font-fraunces`, `--font-nunito`. Before importing, confirmed all three faces present in the installed Next font-data manifest with the requested weights available — no fallback substitution needed. Extended each face's weight array beyond the UI-SPEC's floor to cover the storefront's actual `font-semibold`/`font-bold`/`font-extrabold` heading classes (grep-confirmed 171/77/2 usage sites respectively), so no heading weight falls back to `font-synthesis: none`'s nearest-loaded-weight behavior unexpectedly. Added all three variables to the body's existing className list. Changed nothing else in the file — no heading class, no layout-function edit, no theme-resolution change.
- Authored `themes/retro.css` (dark, magenta-on-deep-purple, Orbitron), `themes/atelier.css` (light, clay-on-linen, sage info, Fraunces), and `themes/market.css` (light, produce-green-on-off-white, largest radius scale, Nunito). Every value transcribed verbatim from `06.1-UI-SPEC.md`'s pre-computed table, including all flagged deviations with measured before/after contrast ratios recorded in each file's own header comment. Retro needed no correction (all four checks pass on the doc's literals); atelier and market both correct `primary`/`ring` from a failing AA ratio to a passing one.
- Regenerated `themes/index.generated.css` (7 `@import` lines) and `lib/themes/manifest.generated.ts` (7 `THEME_MANIFEST` entries), confirmed sorted filename order (`atelier, clinical, luxe, market, midnight, retro, volt-dark`) in both files, confirmed idempotent across two consecutive runs plus `--check`, and confirmed all seven manifest labels are distinct.
- Proved all three new presets switch on a real `npm run dev` server: wrote `appearance.theme` to `retro`, `atelier`, then `market` via the dev-bypass admin API, confirmed the served home page's `data-theme` attribute matched each, then restored to `volt-dark` and reconfirmed before stopping the server.
- Confirmed the admin Appearance grid needed zero code changes: `git status --porcelain components/admin/` empty, existing render tests (19/19) pass unmodified with the regenerated seven-entry manifest.
- Re-confirmed the deploy-path guard is wired correctly: deliberately added a second selector rule to a working copy of `market.css`, confirmed `npm run build:worker` fails with a `[build-themes]` message naming the file and line, then restored the file and re-ran the generator clean — `git diff HEAD -- themes/market.css` empty, proving the probe was fully reversible.
- Ran the full gate suite: `lint` (0 errors, 52 pre-existing warnings, unchanged from plan 06.1-01), `typecheck` (clean), `scan:tokens` (0 violations, the same 2 manual-review rows), the full test suite (1959/1959 pass, up from 1950 — the 9 new tests are the token-contract loop covering 3 more files), and `build` (exit 0).

## Task Commits

Each task was committed atomically:

1. **Task 1: Three display faces loaded once in the root layout** - `b8fee7e` (feat)
2. **Task 2: themes/retro.css, themes/atelier.css, themes/market.css** - `0c93273` (feat)
3. **Task 3: Regenerate to seven, prove each new preset switches, run the full gate** - `079ed62` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified

- `app/layout.tsx` - three new `next/font/google` display faces (Orbitron, Fraunces, Nunito)
- `themes/retro.css` - fifth preset, manifest name `retro`
- `themes/atelier.css` - sixth preset, manifest name `atelier`
- `themes/market.css` - seventh preset, manifest name `market`
- `themes/index.generated.css` - regenerated to 7 `@import` lines
- `lib/themes/manifest.generated.ts` - regenerated to 7 `THEME_MANIFEST` entries

## Decisions Made

- **Font weight supersets extend the UI-SPEC floor with the storefront's real heading weight classes** — grep-confirmed rather than assumed, and all requested weights are present in the installed font-data manifest with no fallback needed.
- **Retro needed no contrast correction** — all four required checks pass on the direction doc's literal values, unlike the other three presets in this phase (and unlike clinical from 06.1-01).
- **Market's `success` deliberately keeps the doc's literal, uncorrected value** even though `primary` (same hue family) required correction, because `success` never renders as solid white-on-green button text in this contract. Recorded explicitly so it reads as a decision, not an inconsistency.
- **Task 2's "don't run the generator" instruction and Task 3's verify block requiring it were reconciled by following the verify block** — it is the graded gate, and running the generator once over all three completed files (not never) is what proves Task 2's own acceptance criteria that the whole-directory validator accepts them.

## Deviations from Plan

None - plan executed exactly as written. All shape assertions, codegen, vitest, lint, typecheck, scan:tokens, test suite, build, the switch probe, the admin-grid clean-tree check, and the deploy-path spot check passed on the first run.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All six direction-doc presets plus `volt-dark` now ship as pure 23-token data files on one unmodified validator — the pipeline has been proven at seven consecutive files with zero mechanism changes.
- Three new display faces are loaded and available; no component yet applies the `font-display` class to any heading (carried forward from Phase 6's WINDOWS.md item) — that wiring, and its intentional volt-dark system-ui-to-Geist visual change, is plan 06.1-03's subject (D-10), not this plan's.
- `06.1-SCREENSHOTS.md`'s route-grid captures and the light-preset scrim/shadow QA for atelier/clinical/market are still open — carried to plan 06.1-04 per the phase's own sequencing.
- Carried forward, unchanged by this plan: the `font-display` wiring gap (Phase 6, WINDOWS.md); Phase 5's screenshot coverage gaps (order-status, Stripe payment step, authenticated account dashboard, review-form error state).

---
*Phase: 06.1-remaining-presets-clinical-retro-atelier-market*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: themes/retro.css
- FOUND: themes/atelier.css
- FOUND: themes/market.css
- FOUND: app/layout.tsx (modified, contains --font-orbitron/--font-fraunces/--font-nunito)
- FOUND: themes/index.generated.css (7 imports)
- FOUND: lib/themes/manifest.generated.ts (7 entries)
- FOUND commit: b8fee7e (Task 1)
- FOUND commit: 0c93273 (Task 2)
- FOUND commit: 079ed62 (Task 3)
- Re-ran plan-level verification: `mise exec -- node scripts/build-themes.mjs --check` (exit 0, 7 theme(s) fresh); `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts` (30/30 pass); `mise exec -- npm run scan:tokens` (0 violations, 2 unchanged manual-review rows); `git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts` (empty against HEAD)
