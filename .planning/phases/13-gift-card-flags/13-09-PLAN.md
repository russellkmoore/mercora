---
phase: 13-gift-card-flags
plan: 09
type: execute
wave: 4
depends_on: ["13-02", "13-03", "13-04", "13-06", "13-07", "13-08"]
files_modified: []
autonomous: true
requirements: [GCF-01, GCF-02, GCF-03, GCF-04, GCF-05]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every command in the AGENTS.md CI list passes locally, in that order, before anything is pushed"
    - "The push to main produces a new Cloudflare Workers Builds deployment"
    - "After deploy, the storefront behaves exactly as it did before — production has both flags on, so this phase changes nothing a shopper can see (GCF-01, GCF-03)"
    - "After one five-minute cron tick, the honor-guard row exists in production admin_settings with a zero outstanding total (GCF-02)"
    - "No gift_card.honor_disabled_with_balances event fires — the alarm is quiet, as it should be with honoring on"
  artifacts: []
  key_links:
    - "The production state is sell on and honor on with one zero-balance card, so a quiet deploy is the correct outcome and a noisy one is the signal that something is wrong"
  prohibitions:
    - "Never run npm run deploy or npm run deploy:ci from this machine — Cloudflare Workers Builds owns deployment (AGENTS.md)"
    - "Never git add . — stage named paths only"
    - "No production flag is changed by this phase; both stay \"true\" in wrangler.jsonc"
    - "No migration is applied; this phase adds none"
---

<objective>
Prove the phase in one pass, ship it, and confirm that nothing moved.

Production runs with both gift-card flags on and holds one gift card with a zero balance. That
means a correct deploy of this phase is invisible: the same pages render, the same product lists,
the same checkout. The one new observable is a row appearing in `admin_settings` after the first
cron tick, carrying a zero outstanding total — and the alarm staying silent.

Purpose: the phase gate for all five requirements.
Output: a green local CI run, a deployed version, and read-only production evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@CONTRIBUTING.md
@docs/DEPLOYMENT_SETUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local CI in the AGENTS.md order</name>

  <read_first>
    - `AGENTS.md` §Gates — the exact command list and its order.
    - `.github/workflows/ci.yml` — confirm the list has not drifted from AGENTS.md since this plan
      was written; if it has, follow the workflow file and note the difference in the summary.
    - `docs/DEPLOYMENT_SETUP.md` §9 Step 2 — the `.dev.vars` / `.env.local` caveat that applies
      around `cf-typegen` and `cf-typecheck`.
  </read_first>

  <precondition>`.dev.vars` exists in the repo root with the five required keys; `mise` resolves Node 24.18.1 (`mise exec -- node --version`).</precondition>

  <files>none — this task runs commands and changes nothing</files>

  <action>
    Run the CI list in order, stopping at the first failure and fixing it in the owning plan's
    files rather than papering over it here:

    1. `npm audit --omit=dev --audit-level=high`
    2. `npm run build:themes:check`
    3. `npm run scan:tokens`
    4. `npm run lint`
    5. `npm run typecheck`
    6. `npm run cf-typecheck`
    7. `mise exec -- npm test`
    8. `mise exec -- npm run test:workers`
    9. `mise exec -- npm run test:observability-worker`
    10. `npm run docs:lint`
    11. `npm run build`

    Before step 6, move `.dev.vars` and any `.env*.local` out of the working tree, and move them
    back immediately afterwards — `wrangler types` reads local env files and would otherwise
    generate names CI does not have. Use a shell command with no `$( )` substitution so it
    auto-approves.

    Also run `npm run check:migrations -- --base $(git merge-base HEAD origin/main)` once and
    confirm it reports no migration change. This phase adds none; if it reports one, stop and
    escalate — a migration would need to be expand-only and numbered `0024`, and no plan in this
    phase was authorised to add one.

    Record the wall-clock duration of the two slowest commands in the summary, so the next phase
    can size its own gate.
  </action>

  <acceptance_criteria>
    - Every command above exits 0.
    - `git status --porcelain cloudflare-env.d.ts` prints nothing after step 6 — the generated types did not drift.
    - `git status --porcelain migrations/` prints nothing.
    - `git status --porcelain` shows no unexpected file (the pre-existing modifications to `cloudflare-env.d.ts` and `wrangler.jsonc` noted at phase start are the only allowed carry-overs, and only if unchanged by this phase).
  </acceptance_criteria>

  <verify>
    <automated>npm run build:themes:check &amp;&amp; npm run scan:tokens &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; mise exec -- npm test &amp;&amp; mise exec -- npm run test:workers &amp;&amp; mise exec -- npm run test:observability-worker &amp;&amp; npm run docs:lint &amp;&amp; npm run build</automated>
    <fails_when>any gate is red, the generated Cloudflare types drift because a local env file was in place during cf-typecheck, or an unplanned migration appeared.</fails_when>
  </verify>

  <done>The whole CI list is green locally on the phase branch and the working tree is clean apart from the phase's own intended changes.</done>

  <reversibility rating="reversible">Read-only verification.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Push and confirm the deployment</name>

  <read_first>
    - `AGENTS.md` §Rules — deploys happen by pushing to `main`; `npm run deploy:ci` is Cloudflare's
      to run, never this machine's.
    - `CONTRIBUTING.md` — the commit and branch conventions for this repo.
    - `docs/DEPLOYMENT_SETUP.md` §9 Step 3 — the shape of a post-deploy verification.
  </read_first>

  <precondition>A Cloudflare account with access to the `mercora` Worker is authenticated for `wrangler` (`mise exec -- npx wrangler whoami` succeeds).</precondition>

  <files>none — this task pushes existing commits</files>

  <action>
    Stage and push only the paths this phase touched. Stage them by name; never `git add .`. If the
    commit message runs to more than one line, write it to a temp file and use `git commit -F
    <file>` — the heredoc form triggers a permission prompt every time.

    Push to `main`. Cloudflare Workers Builds picks up the push and deploys. Do not run
    `npm run deploy`, `npm run deploy:ci`, or `wrangler deploy` from this machine under any
    circumstances.

    Wait for the build, then confirm a new version exists with
    `mise exec -- npx wrangler deployments list` and record the new version id in the summary.

    Do not touch `wrangler.jsonc`. Both flags stay `"true"`; this phase changes behaviour behind
    the flags, not the flags themselves. That is why the deploy should be invisible.
  </action>

  <acceptance_criteria>
    - `git status --porcelain` is clean after the commit.
    - `git diff origin/main --stat -- wrangler.jsonc` prints nothing — no flag was changed.
    - `mise exec -- npx wrangler deployments list` shows a version created after the push.
  </acceptance_criteria>

  <verify>
    <automated>mise exec -- npx wrangler deployments list</automated>
    <fails_when>no new version appears, meaning the Workers Build failed or the push did not land on main.</fails_when>
  </verify>

  <human-check>Open the Cloudflare dashboard's build log for this deployment and confirm it finished green rather than falling back to a previous version.</human-check>

  <done>The phase is on `main`, Cloudflare has deployed it, and no flag value changed.</done>

  <reversibility rating="reversible">Both production flags are unchanged, so the deploy alters no observable storefront behaviour; a revert commit is the rollback and needs no data migration.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Read-only production confirmation</name>

  <read_first>
    - `lib/gift-cards/honor-guard.ts` — the settings key to query.
    - `docs/observability.md` — how to watch the tail for a critical event.
    - `.planning/phases/13-gift-card-flags/13-CONTEXT.md` §Specific Ideas — production holds one
      gift card with a zero balance and no open reservations, so the alarm must stay quiet.
  </read_first>

  <precondition>The deployment from task 2 is live and at least five minutes have passed, so one recovery cron tick has run.</precondition>

  <files>none — read-only checks against production</files>

  <action>
    Run these read-only checks and record each result in the summary. Every one is a `GET` or a
    `SELECT`; write nothing to production.

    1. Storefront unchanged, all of these expected to return 200:
       `curl -s -o /dev/null -w '%{http_code}' https://voltique.russellkmoore.me/`
       `curl -s -o /dev/null -w '%{http_code}' https://voltique.russellkmoore.me/product/gift-card`
       `curl -s -o /dev/null -w '%{http_code}' https://voltique.russellkmoore.me/api/products`
       With selling on, the gift-card page renders normally and the product list still contains
       the card.

    2. The gift card is still publicly listed, since selling is on:
       `curl -s https://voltique.russellkmoore.me/api/products?limit=100` and confirm the gift-card
       product is present. If it is absent, the listing filter is reading the flag wrongly and the
       phase must be reverted — that is the one regression that would be invisible in a status code.

    3. The honor-guard row exists and reads zero:
       `mise exec -- npx wrangler d1 execute mercora-db --remote --json --command "SELECT key, value, data_type FROM admin_settings WHERE key = 'gift_cards.honor_guard'"`
       Expect exactly one row, `data_type` of `object`, and a value whose outstanding total and
       open-reservation count are both zero and whose measurement timestamp is within the last
       fifteen minutes. If the row is missing after two cron cycles, the cron wiring is wrong.

    4. Cross-check the measurement against the source of truth:
       `mise exec -- npx wrangler d1 execute mercora-db --remote --json --command "SELECT COUNT(*) AS active_cards FROM gift_card_accounts WHERE status = 'active'"`
       Expect the count the context notes — one card — and a zero outstanding total in step 3.
       A nonzero outstanding total against a zero-balance card means the balance expression is
       being bound wrongly.

    If any check disagrees with its expectation, stop and report rather than adjusting production.
  </action>

  <acceptance_criteria>
    - All three storefront `curl` calls print `200`.
    - The gift-card product appears in the public `/api/products` response.
    - Exactly one `gift_cards.honor_guard` row exists, with `data_type` of `object` and a zero outstanding total.
    - The active-card count matches the one card recorded in the phase context.
  </acceptance_criteria>

  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' https://voltique.russellkmoore.me/ &amp;&amp; curl -s -o /dev/null -w '%{http_code}\n' https://voltique.russellkmoore.me/product/gift-card &amp;&amp; mise exec -- npx wrangler d1 execute mercora-db --remote --json --command "SELECT key, data_type FROM admin_settings WHERE key = 'gift_cards.honor_guard'"</automated>
    <fails_when>a storefront route stops returning 200, the gift card vanished from the public listing while selling is on, or the honor-guard row is missing after the cron has run.</fails_when>
  </verify>

  <human-check>Watch the Worker tail (or the observability tail worker's alert mailbox) across one five-minute window and confirm no `gift_card.honor_disabled_with_balances` alert arrived. With honoring on, this alarm must never fire — if it does, the emit condition is inverted.</human-check>

  <done>Production looks exactly as it did before the deploy, the guard row is present and reads zero, and the alarm is silent.</done>

  <reversibility rating="reversible">Read-only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → production | Only through a push to `main`; the deploy itself is Cloudflare's |
| Production D1 → verification queries | Read-only `SELECT` statements |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-37 | Elevation of Privilege | direct deploy from a developer machine | high | mitigate | Deployment is by push only; `npm run deploy*` and `wrangler deploy` are prohibited here and named in the plan's prohibitions |
| T-13-38 | Tampering | production data during verification | high | mitigate | Every production command in task 3 is a `GET` or a `SELECT`; no `INSERT`, `UPDATE` or `DELETE` appears anywhere in the plan |
| T-13-39 | Denial of Service | a silent listing regression | high | mitigate | Step 2 of task 3 inspects the response body rather than the status code, because a gift card wrongly filtered out of a live catalogue would still return 200 |
| T-13-40 | Tampering | accidental flag change in `wrangler.jsonc` | high | mitigate | An acceptance criterion diffs `wrangler.jsonc` against `origin/main` and requires an empty diff |
| T-13-41 | Repudiation | an inverted alarm condition | medium | mitigate | The human check watches one cron window and treats any alert as a failure, since honoring is on and no balance is outstanding |
</threat_model>

<verification>
- Every AGENTS.md CI command green locally
- A new version in `wrangler deployments list`
- Three production routes at 200, the gift card still listed, the guard row present and zero
- No critical gift-card alert across one cron window
</verification>

<success_criteria>
The phase ships and production is indistinguishable from before, apart from one new zero-valued
settings row and a quiet alarm.
</success_criteria>

<output>
Create `.planning/phases/13-gift-card-flags/13-09-SUMMARY.md` when done
</output>
