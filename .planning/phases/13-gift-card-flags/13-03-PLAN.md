---
phase: 13-gift-card-flags
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/product/[slug]/page.tsx
  - app/product/[slug]/ProductDisplay.tsx
  - tests/unit/app/product-slug-page.test.ts
  - tests/unit/components/gift-card-unavailable-source.test.ts
autonomous: true
requirements: [GCF-01, GCF-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "With sell off and honor on, GET /product/gift-card renders 200 with the unavailable notice and no recipient form and no add-to-cart control (GCF-01, D-07)"
    - "With both flags off, GET /product/gift-card calls notFound() (GCF-03, D-10)"
    - "With sell on, the gift-card product page renders the recipient form exactly as it does today"
    - "A non-gift product page is unaffected in every flag state"
  artifacts:
    - tests/unit/components/gift-card-unavailable-source.test.ts
  key_links:
    - "app/product/[slug]/page.tsx computes the unavailable state server-side and passes it to ProductDisplay as a prop; the client component does not re-derive flags"
  prohibitions:
    - "The existing inventory-driven `available` boolean is not reused for the flag state — they mean different things (13-PATTERNS.md Gotchas)"
    - "No hardcoded palette; the notice uses existing token classes only (D-13, AGENTS.md)"
    - "The 404 branch keys on the catalog product type, never the slug (D-08)"
---

<!-- planner-discipline-allow: giftCardSurfacesHidden, giftCardSalesDisabled -->

<objective>
Make the gift-card product page tell the truth in each flag state.

With selling off but honoring on, a bookmark or an old email pointing at `/product/gift-card`
must still land somewhere sensible: the page renders, with "Gift cards are not available right
now" where the recipient form used to be (D-07). With both flags off the product does not exist,
so the page 404s (D-10).

Purpose: the direct-link half of GCF-01 and the page half of GCF-03.
Output: a server-side branch in the page, a prop-driven unavailable state in `ProductDisplay`,
and tests for both.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-gift-card-flags/13-CONTEXT.md
@.planning/phases/13-gift-card-flags/13-PATTERNS.md
@lib/gift-cards/visibility.ts
@docs/theming.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Product page branches on the two flags</name>

  <read_first>
    - `app/product/[slug]/page.tsx` — the whole file (109 lines). The relevant part is lines 61-105:
      `getProductBySlug`, the `notFound()` for a missing or inactive product at line 65,
      `const store = getStoreConfig()` at line 67, and the `subscription={{ ... }}` prop object at
      lines 98-104 — that prop object is the exact shape to copy for the new prop.
      Note `export const revalidate = 0` at line 53, so this page reflects a flag change immediately.
    - `app/product/[slug]/ProductDisplay.tsx` lines 100-145 (the `ProductDisplayProps` interface
      and the destructured parameter list) and lines 356-381 (the gift-card branch:
      `product.type === "gift_card" ? available ? <GiftCardRecipientForm .../> : <p>Coming soon</p>
      : ...`). The `available` boolean at line 178 is inventory availability — do not reuse it.
    - `app/account/subscriptions/page.tsx` lines 1-8 — the established flag-off `notFound()` shape.
    - `lib/gift-cards/visibility.ts` — `giftCardSurfacesHidden` is the both-off predicate.
  </read_first>

  <files>app/product/[slug]/page.tsx, app/product/[slug]/ProductDisplay.tsx</files>

  <behavior>
    - Product type is not the gift-card type: nothing changes, in any flag state.
    - Gift-card product, sell on: `ProductDisplay` receives the new prop as false and renders the
      recipient form as today.
    - Gift-card product, sell off and honor on: the page renders; `ProductDisplay` receives the new
      prop as true and renders the unavailable notice in place of the recipient form; no
      add-to-cart control is reachable.
    - Gift-card product, both flags off: `notFound()` is called before any render work.
  </behavior>

  <action>
    In `app/product/[slug]/page.tsx`, after the existing product lookup and the existing
    `const store = getStoreConfig()`, add a branch that runs only when the fetched product's
    catalog type is the gift-card type. Inside it: call `notFound()` when the shared both-off
    predicate says the surfaces are hidden (D-10); otherwise compute a single boolean from the
    negation of `commerce.features.giftCardAcquisition`. Pass that boolean to `ProductDisplay` as a
    new prop named `giftCardSalesDisabled`, defaulting to false for every non-gift product. Place
    the new prop next to the existing `subscription` prop and follow the same object/boolean style
    already used there. Key the branch on the product type from the stored product, never on the
    slug (D-08).

    In `app/product/[slug]/ProductDisplay.tsx`, add `giftCardSalesDisabled?: boolean` to
    `ProductDisplayProps`, destructure it with a `= false` default, and change the gift-card branch
    so the new boolean is checked **before** the existing inventory `available` check. When it is
    true, render the notice instead of `GiftCardRecipientForm`: a primary line
    "Gift cards are not available right now" and a secondary line
    "Any gift card you already own still works at checkout." — the second line is accurate because
    the both-off state 404s before this component renders.

    Style the notice with the token classes already used by the sibling states in this file:
    `text-lg font-semibold text-warning sm:text-xl` for the primary line and
    `text-sm text-muted-foreground` for the secondary line. Do not introduce a new colour, a hex
    value, or a Tailwind palette class — `npm run scan:tokens` gates this (AGENTS.md).

    Keep the image gallery, price, variant selector and description above the notice untouched, per
    the discretion note in 13-CONTEXT.md. Do not change the `available`-driven "In stock" /
    "Currently unavailable" line at lines 357-361 and do not change the non-gift branches.
  </action>

  <acceptance_criteria>
    - `grep -c 'giftCardSalesDisabled' "app/product/[slug]/page.tsx"` prints at least `1`.
    - `grep -c 'giftCardSalesDisabled' "app/product/[slug]/ProductDisplay.tsx"` prints at least `2` (interface plus branch).
    - `grep -v '^ *[*/]' "app/product/[slug]/page.tsx" | grep -cF 'notFound()'` prints `2` — the pre-existing missing-product 404 plus the new both-off 404. Comment lines are stripped first because this file's header comment already mentions `notFound()`, and the import line does not call it, so an unfiltered count cannot fail.
    - `grep -c "Gift cards are not available right now" "app/product/[slug]/ProductDisplay.tsx"` prints `1`.
    - `grep -cE "'gift-card'|\"gift-card\"" "app/product/[slug]/page.tsx"` prints `0` — no slug-based branch.
    - `npm run scan:tokens` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npm run typecheck &amp;&amp; npm run scan:tokens &amp;&amp; npm run lint</automated>
    <fails_when>the flag boolean was folded into the inventory `available` variable, a raw colour class was used for the notice, or the branch keys on the slug so a renamed product bypasses it.</fails_when>
  </verify>

  <done>The page 404s when both flags are off, renders the notice when only selling is off, and is unchanged for every non-gift product.</done>

  <reversibility rating="reversible">One server-side branch and one prop-driven client branch; production has sell on, so neither fires today.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Product page and notice tests</name>

  <read_first>
    - `tests/unit/app/product-slug-page.test.ts` — the whole file. Its `vi.mock("next/navigation")`
      throws `NEXT_NOT_FOUND`, which is how a `notFound()` call is asserted, and its
      `vi.mock("@/lib/store-config")` at lines 23-31 returns a `commerce.features` object that
      currently carries only the two subscription booleans — that mock must gain the two gift-card
      booleans or every gift-card case will read them as undefined.
    - `tests/unit/components/checkout-digital-only-source.test.ts` — the source-contract shape.
    - `app/product/[slug]/ProductDisplay.tsx` — the branch written in task 1, for the strings the
      contract test pins.
  </read_first>

  <files>tests/unit/app/product-slug-page.test.ts, tests/unit/components/gift-card-unavailable-source.test.ts</files>

  <behavior>
    In `product-slug-page.test.ts`, four new cases with the store-config mock parameterised per case:
    - gift-card product, sell on / honor on → resolves, and the returned element tree carries the
      new prop as false.
    - gift-card product, sell off / honor on → resolves, new prop is true.
    - gift-card product, sell off / honor off → rejects with `NEXT_NOT_FOUND`.
    - non-gift product, sell off / honor off → resolves (the flags do not touch ordinary products).
    Every pre-existing case in the file keeps passing.

    In `gift-card-unavailable-source.test.ts`, three cases over the `ProductDisplay` source:
    the notice string appears exactly once; the notice branch is checked ahead of the inventory
    `available` check; and the notice markup contains no hex colour and no bare Tailwind palette
    class.
  </behavior>

  <action>
    Update the `vi.mock("@/lib/store-config")` block in `tests/unit/app/product-slug-page.test.ts`
    so the mocked `commerce.features` object also carries the two gift-card booleans, and make the
    mock readable per case (for example a module-level mutable object the mock returns, reset in
    `beforeEach` to both flags on so the existing cases are unaffected). Add the four cases above,
    giving the gift-card fixture the catalog gift-card type and the non-gift fixture no type, and
    asserting the `notFound()` case with `rejects.toThrow("NEXT_NOT_FOUND")` in the same style the
    file already uses for a missing product.

    Create `tests/unit/components/gift-card-unavailable-source.test.ts` reading
    `app/product/[slug]/ProductDisplay.tsx` and asserting the three cases above. For the
    ordering case, assert that the index of the new prop identifier inside the gift-card branch is
    lower than the index of the inventory branch's ternary. For the token case, strip comment lines
    from the source first, then assert the notice's class strings match no `#[0-9a-f]{3,8}` and no
    `\b(?:text|bg)-(?:red|green|blue|amber|yellow|orange|gray|slate|neutral)-\d{2,3}\b` pattern —
    build both patterns from `RegExp` literals declared in the test so the assertion cannot be
    satisfied by its own prose.
  </action>

  <acceptance_criteria>
    - `mise exec -- npx vitest run tests/unit/app/product-slug-page.test.ts tests/unit/components/gift-card-unavailable-source.test.ts` exits 0.
    - `grep -cF 'it(' tests/unit/components/gift-card-unavailable-source.test.ts` prints at least `3` (`-F` required — the default `grep` here is ripgrep).
    - Temporarily flipping the branch order in `ProductDisplay.tsx` makes the ordering case fail — confirm once by hand, then restore.
    - `mise exec -- npm test` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>mise exec -- npm test</automated>
    <fails_when>the both-off state renders instead of 404ing, the sell-off state 404s instead of rendering the notice, or the notice is reachable only after the inventory check so an in-stock card still shows the form.</fails_when>
  </verify>

  <done>All four page states are pinned by test, and the notice's ordering and token discipline are pinned by a source contract.</done>

  <reversibility rating="reversible">Test-only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous request → `/product/[slug]` | An unauthenticated visitor decides nothing; the server decides render vs 404 |
| Server prop → client component | `ProductDisplay` is a client component and must not be trusted to re-derive the flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Elevation of Privilege | `ProductDisplay` gift-card branch | high | mitigate | The unavailable check runs before the inventory check and removes the add-to-cart path; a source-contract test pins the ordering. The real guard is server-side: plan 13-04 rejects a gift-card line in `priceCheckout`, so a hand-crafted client request cannot buy |
| T-13-10 | Information Disclosure | `/product/[slug]` both-off branch | low | mitigate | `notFound()` returns the ordinary 404, identical to a product that never existed, so the flag state is not disclosed |
| T-13-11 | Tampering | both-off branch condition | medium | mitigate | The branch keys on the catalog product type, not the slug, so renaming or re-slugging the product cannot bypass it; pinned by a grep assertion |
</threat_model>

<verification>
- `mise exec -- npm test` green
- `npm run typecheck`, `npm run lint`, `npm run scan:tokens` green
</verification>

<success_criteria>
`/product/gift-card` renders a truthful notice when selling is off, 404s when gift cards do not
exist, and is unchanged when selling is on.
</success_criteria>

<output>
Create `.planning/phases/13-gift-card-flags/13-03-SUMMARY.md` when done
</output>
