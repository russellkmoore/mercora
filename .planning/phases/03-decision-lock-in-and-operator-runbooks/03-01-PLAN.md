---
phase: 03-decision-lock-in-and-operator-runbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/checkout-trust-boundary.md
  - docs/webhooks-refunds-inventory.md
  - docs/database-migrations.md
  - docs/subscriptions.md
  - gsd-ingest-manifest.yaml
autonomous: true
requirements: [ADR-01, ADR-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/checkout-trust-boundary.md states MCP checkout is inside the paid inventory boundary and the superseded sentence placing it outside is gone."
    - "docs/checkout-trust-boundary.md names lib/services/checkout-pricing.ts, lib/services/order-finalization.ts, create_payment_intent and place_order, and every one of those names exists in the code."
    - "All four ADR docs carry a dated Accepted status marker as their own markdown block directly under the H1."
    - "gsd-ingest-manifest.yaml is tracked by git and carries exactly four locked-true keys, one per ADR entry."
    - "EDGE (ADR-01, boundary): docs/checkout-trust-boundary.md places MCP checkout INSIDE the paid inventory boundary - the negative grep for the superseded phrase passes and the positive grep for 'inside the paid inventory boundary' matches."
    - "EDGE (ADR-01, precision): the supersession sentence carries the correction date 2026-09-02 while the same file's status marker carries the decision date 2026-08-05; the two dates are distinct and neither is substituted for the other."
    - "EDGE (ADR-02, boundary): exactly four docs carry a status marker and the manifest carries exactly four locked-true keys - three would leave an ADR unlocked, five would mean a SPEC/PRD/DOC entry was wrongly locked."
    - "EDGE (ADR-02, adjacency): in each ADR doc the marker is its own block - line 1 H1, line 2 blank, line 3 marker, line 4 blank, line 5 the document's original first paragraph line; the marker never merges into the H1 or the paragraph."
    - "EDGE (ADR-02, empty): gsd-ingest-manifest.yaml still parses as YAML and still lists all 26 path entries after the edit; no entry is dropped and no SPEC/PRD/DOC entry gains a locked key."
    - "EDGE (ADR-02, ordering): the four ADR entries keep their original order in the manifest (checkout-trust-boundary, webhooks-refunds-inventory, database-migrations, subscriptions); the edit inserts a key and never reorders entries."
    - "EDGE (ADR-02, precision): each marker carries that document's own first-commit date (2026-08-05, 2026-08-06, 2026-08-03, 2026-08-14) and none carries today's date; 2026-09-02 appears only in the manifest lock comment and in the supersession sentence."
  artifacts:
    - docs/checkout-trust-boundary.md
    - docs/webhooks-refunds-inventory.md
    - docs/database-migrations.md
    - docs/subscriptions.md
    - gsd-ingest-manifest.yaml
  key_links:
    - "docs/checkout-trust-boundary.md -> lib/services/checkout-pricing.ts and lib/services/order-finalization.ts: the doc's boundary claim is only true because both MCP tools import these two shared services."
    - "each ADR doc's Status line -> the GSD doc classifier: the classifier's semantic read of the doc's own status text is the real lock gate; the manifest key is a human-readable record."
    - "gsd-ingest-manifest.yaml -> the git index: the file is untracked today, so the lock record only survives if the task stages it."
  prohibitions:
    - statement: "MUST NOT mark a document Accepted while its text still asserts something the code does not do - a status marker on a false statement converts a stale doc into a binding one."
      status: resolved
      verification: judgment
    - statement: "MUST NOT erase the record that the earlier boundary statement was wrong; the correction must stay visible to a future reader rather than being silently overwritten."
      status: resolved
      verification: judgment
---

<objective>
Make the four binding-decision documents say what the code does, and mark them binding.

Purpose: ADR-01 removes the one statement in `docs/checkout-trust-boundary.md` that contradicts the
code. MCP checkout is served by the same pricing service and the same idempotent finalizer as the
storefront, so it is inside the paid inventory boundary. ADR-02 gives all four ADR docs a dated
`Accepted` marker and records the lock in `gsd-ingest-manifest.yaml`, so a future `/gsd-ingest-docs`
run classifies them as locked instead of re-opening them.

Output: four edited ADR docs, an edited and newly git-tracked `gsd-ingest-manifest.yaml`, and one
end-of-phase human verification item handing Russell the throwaway-branch ingest re-run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-CONTEXT.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-RESEARCH.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-PATTERNS.md
</context>

<!-- planner-discipline-allow: remains outside -->

## Artifacts this phase produces (Plan 01)

| Path | Change |
|---|---|
| `docs/checkout-trust-boundary.md` | adds `**Status:** Accepted (2026-08-05)`; replaces the two boundary sentences in "Scope and schema" |
| `docs/webhooks-refunds-inventory.md` | adds `**Status:** Accepted (2026-08-06)` |
| `docs/database-migrations.md` | adds `**Status:** Accepted (2026-08-03)` |
| `docs/subscriptions.md` | adds `**Status:** Accepted (2026-08-14)` |
| `gsd-ingest-manifest.yaml` | adds one lock-date comment and four `locked: true` keys; becomes git-tracked |

- **Symbols created:** none. This plan contains no code.
- **Symbols removed:** none.
- **New manifest keys:** `locked: true`, on the four ADR entries only.
- **New literal strings:** `**Status:** Accepted (2026-08-05)`, `**Status:** Accepted (2026-08-06)`, `**Status:** Accepted (2026-08-03)`, `**Status:** Accepted (2026-08-14)`, `inside the paid inventory boundary`.
- **Strings removed:** the sentence in `docs/checkout-trust-boundary.md` carrying the superseded phrase.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end ADR lock-in on one document - trust-boundary text, status marker, manifest, git</name>
  <files>docs/checkout-trust-boundary.md, gsd-ingest-manifest.yaml</files>
  <precondition>`gsd-ingest-manifest.yaml` exists at the repository root and is untracked by git; `git status --porcelain gsd-ingest-manifest.yaml` prints `?? gsd-ingest-manifest.yaml`. The task must stage it, otherwise the lock record is never committed and ADR-02 cannot be satisfied.</precondition>
  <reversibility rating="reversible">Every change is a documentation or config line that `git revert` undoes, with no data migration and no deployed effect.</reversibility>
  <read_first>
    - `docs/checkout-trust-boundary.md` - the file being modified. Read lines 1-5 and 95-110 before editing.
    - `gsd-ingest-manifest.yaml` - the file being modified. The ADR block is lines 15-23.
    - `lib/mcp/checkout.ts` - source of truth. Confirms the `create_payment_intent` path imports `priceCheckout` from `@/lib/services/checkout-pricing`.
    - `lib/mcp/tools/order.ts` - source of truth. Confirms the `place_order` path imports `finalizeOrderPayment` from `../../services/order-finalization`.
    - `.planning/phases/03-decision-lock-in-and-operator-runbooks/03-PATTERNS.md` - the exact marker shape and manifest entry shape to copy.
  </read_first>
  <action>
This is the phase tracer. Take ONE ADR document all the way through every layer this phase touches -
document prose, status marker, manifest lock record, git tracking, structural verification - before the
same pattern is applied to the other three documents in Task 2.

Work in this order.

**1. Verify the code names before writing prose.** Run `grep -rn "create_payment_intent" lib/mcp` and
`grep -rn "place_order" lib/mcp`. Both tool names are registered in `lib/mcp/auth.ts`. Confirm
`lib/services/checkout-pricing.ts` and `lib/services/order-finalization.ts` exist and that
`finalizeOrderPayment` is exported from the second. If any name differs from what is written below, use
the real name and record the difference in the SUMMARY. Docs matching code exactly is the standing rule
from Phase 1 SEC-04.

**2. Insert the status marker.** Line 1 of `docs/checkout-trust-boundary.md` is the H1
`# Order and checkout trust boundary` and line 2 is blank. Insert `**Status:** Accepted (2026-08-05)`
as the new line 3, then a blank line 4, so the document's existing first paragraph begins on line 5.
The date is this document's own first-commit date, not today's date. No frontmatter, no footer section.

You may optionally add a second line `**Binding:** changes require a new decision.` directly under the
status line - CONTEXT.md leaves that to discretion. If you add it, add it to all four documents in
Task 2 as well, and keep it on its own line so the status line stays the first content line after the H1.

**3. Replace the two sentences in the "Scope and schema" section.** The paragraph to replace currently
sits at lines 101-103 and opens with the words `This boundary does not expose trusted MCP payment
operations`. Replace that whole paragraph with two paragraphs, in this order - the positive statement of
what is shared, then the boundary conclusion, then the one-sentence correction record.

Paragraph one, target text: MCP `create_payment_intent` and `place_order` use the same shared checkout
pricing service (`lib/services/checkout-pricing.ts`) and the same idempotent finalizer
(`lib/services/order-finalization.ts`) as the storefront `POST /api/orders` route and the Stripe webhook
path. MCP checkout is inside the paid inventory boundary.

Paragraph two, target text: An earlier version of this document stated MCP checkout was outside the
boundary; that was corrected on 2026-09-02 after verifying the code.

Nothing else in the document moves. Leave the "U09 adds migrations `0008` through `0011`" sentence and
the "U13 must replace" sentence exactly as they are - a wider sweep is not ADR-01 and is deferred.

**4. Annotate the manifest.** In `gsd-ingest-manifest.yaml`, directly under the
`  # --- ADR: binding decisions ---` comment line, insert one comment line reading
`  # All four ADRs locked 2026-09-02 (Phase 3, ADR-02); the binding status marker lives in each doc.`
Then add `    locked: true` as a new line under the `type: ADR` line of the
`docs/checkout-trust-boundary.md` entry only. Indent the key with exactly four spaces so it aligns with
`type:`. Put no trailing comment on the `locked: true` line. Do not touch the SPEC, PRD or DOC entries.
Do not add `locked: false` anywhere.

Record in the SUMMARY that this manifest key is a human-readable record only: the GSD ingest classifier
does not read it, and the real lock gate is the document's own status line (RESEARCH.md Pitfall 2).
ADR-02 requires both edits; do not describe the manifest key as the thing that causes locking.

**5. Stage the manifest** with `git add gsd-ingest-manifest.yaml`. The file is untracked today, so
without this the lock record never reaches the commit.
  </action>
  <acceptance_criteria>
    - `sed -n '3p' docs/checkout-trust-boundary.md` prints exactly `**Status:** Accepted (2026-08-05)`.
    - Lines 2 and 4 of `docs/checkout-trust-boundary.md` are both empty, so the marker is its own markdown block.
    - `grep -c 'remains outside' docs/checkout-trust-boundary.md` prints `0`.
    - `docs/checkout-trust-boundary.md` contains all of: `inside the paid inventory boundary`, `lib/services/checkout-pricing.ts`, `lib/services/order-finalization.ts`, `create_payment_intent`, `place_order`, `2026-09-02`.
    - `git ls-files -- gsd-ingest-manifest.yaml` prints `gsd-ingest-manifest.yaml`.
    - The manifest parses as YAML and still has 26 entries under `docs:`; the `docs/checkout-trust-boundary.md` entry carries `locked: true`, and no SPEC, PRD or DOC entry carries a `locked` key.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '3p' docs/checkout-trust-boundary.md | grep -qxE '\*\*Status:\*\* Accepted \(2026-08-05\)'</automated>
    <fails_when>non-zero exit, meaning line 3 is not exactly the dated status marker - wrong line, wrong date, or extra text on the line</fails_when>

    <automated>test -z "$(sed -n '2p' docs/checkout-trust-boundary.md)" -a -z "$(sed -n '4p' docs/checkout-trust-boundary.md)"</automated>
    <fails_when>non-zero exit, meaning line 2 or line 4 is non-empty, so the marker is glued to the H1 or to the first paragraph</fails_when>

    <automated>test "$(grep -c 'remains outside' docs/checkout-trust-boundary.md)" = "0"</automated>
    <fails_when>non-zero exit, meaning the superseded boundary sentence is still in the file</fails_when>

    <automated>for s in 'inside the paid inventory boundary' 'lib/services/checkout-pricing.ts' 'lib/services/order-finalization.ts' 'create_payment_intent' 'place_order' '2026-09-02'; do grep -qF "$s" docs/checkout-trust-boundary.md || { echo "MISSING $s"; exit 1; }; done</automated>
    <fails_when>output contains a line starting with `MISSING`, or non-zero exit - one of the six required strings is absent from the doc</fails_when>

    <automated>test -n "$(git ls-files -- gsd-ingest-manifest.yaml)"</automated>
    <fails_when>non-zero exit, meaning the manifest is still untracked and `git add` was never run</fails_when>

    <automated>node -e "const y=require('yaml'),fs=require('fs');const d=y.parse(fs.readFileSync('gsd-ingest-manifest.yaml','utf8'));if(d.docs.length!==26){console.log('ENTRY COUNT '+d.docs.length);process.exit(1)}const e=d.docs.find(x=>x.path==='docs/checkout-trust-boundary.md');if(!e||e.locked!==true){console.log('TRACER ENTRY NOT LOCKED');process.exit(1)}const bad=d.docs.filter(x=>x.type!=='ADR'&&'locked' in x);if(bad.length){console.log('NON-ADR LOCKED '+bad.length);process.exit(1)}"</automated>
    <fails_when>non-zero exit, or output containing `ENTRY COUNT`, `TRACER ENTRY NOT LOCKED`, or `NON-ADR LOCKED` - the YAML failed to parse, an entry was dropped, the tracer entry is unlocked, or a non-ADR entry was locked</fails_when>
  </verify>
  <done>`docs/checkout-trust-boundary.md` carries its dated status marker as its own block, states that MCP checkout is inside the paid inventory boundary and names the two shared services and two MCP tools, and records the 2026-09-02 correction. `gsd-ingest-manifest.yaml` is git-tracked, still parses with all 26 entries, and its `docs/checkout-trust-boundary.md` entry carries `locked: true`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the proven marker and lock pattern to the other three ADR documents</name>
  <files>docs/webhooks-refunds-inventory.md, docs/database-migrations.md, docs/subscriptions.md, gsd-ingest-manifest.yaml</files>
  <reversibility rating="reversible">Four single-line documentation and config insertions; `git revert` undoes them.</reversibility>
  <read_first>
    - `docs/webhooks-refunds-inventory.md` - lines 1-4. H1 is `# Webhooks, refunds, and inventory operations`.
    - `docs/database-migrations.md` - lines 1-4. H1 is `# Database migrations`.
    - `docs/subscriptions.md` - lines 1-4. H1 is `# Subscriptions`.
    - `gsd-ingest-manifest.yaml` - the ADR block, already carrying the Task 1 lock comment and the first `locked: true`.
    - `docs/checkout-trust-boundary.md` lines 1-5 - the marker shape proven by Task 1; copy it exactly, changing only the date.
  </read_first>
  <action>
Expand the pattern Task 1 proved to the remaining three ADR documents. Each document gets one line;
nothing else in these three files changes.

**Status markers.** In each file, line 1 is the H1 and line 2 is blank. Insert the marker as the new
line 3, followed by a blank line 4, so the document's existing first paragraph begins on line 5. Use
each document's own first-commit date:

- `docs/webhooks-refunds-inventory.md` gets `**Status:** Accepted (2026-08-06)`
- `docs/database-migrations.md` gets `**Status:** Accepted (2026-08-03)`
- `docs/subscriptions.md` gets `**Status:** Accepted (2026-08-14)`

Do not substitute today's date on any of them. If Task 1 added the optional `**Binding:** changes
require a new decision.` line, add the identical line to all three of these files too, directly under
the status line.

**Manifest.** Add `    locked: true` under the `type: ADR` line of the three remaining ADR entries -
`docs/webhooks-refunds-inventory.md`, `docs/database-migrations.md`, `docs/subscriptions.md` - with the
same four-space indentation and no trailing comment. The lock-date comment added in Task 1 covers all
four entries; do not add a second comment. Keep the four ADR entries in their existing order and do not
touch the SPEC, PRD or DOC entries.

After the edits the manifest holds exactly four `locked: true` lines, one per ADR entry, and still holds
all 26 `- path:` entries in their original order.
  </action>
  <acceptance_criteria>
    - Line 3 of each of the four ADR docs matches the pattern `**Status:** Accepted (YYYY-MM-DD)`, and the four dates are 2026-08-05, 2026-08-06, 2026-08-03, 2026-08-14 in that file order.
    - Lines 2 and 4 of each of the three newly edited docs are empty.
    - `grep -c '^    locked: true$' gsd-ingest-manifest.yaml` prints `4`.
    - The manifest parses as YAML, has 26 entries under `docs:`, the four `type: ADR` entries each carry `locked: true`, and no non-ADR entry carries a `locked` key.
    - The four ADR `- path:` values appear in the manifest in the order checkout-trust-boundary, webhooks-refunds-inventory, database-migrations, subscriptions.
    - `git diff --stat docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md` shows only insertions - no line was deleted from any of the three.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -lE '^\*\*Status:\*\* Accepted \(20[0-9]{2}-[0-9]{2}-[0-9]{2}\)$' docs/checkout-trust-boundary.md docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md | wc -l | tr -d ' ')" = "4"</automated>
    <fails_when>the printed count is anything other than 4, or non-zero exit - at least one ADR doc has no dated status marker on a line of its own. Note the tolerant regex: a plain `Status: Accepted` grep matches none of these files because the marker is bold markdown</fails_when>

    <automated>paste -d' ' <(printf '%s\n' docs/checkout-trust-boundary.md docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md) <(printf '%s\n' 2026-08-05 2026-08-06 2026-08-03 2026-08-14) | while read -r f d; do sed -n '3p' "$f" | grep -qxF "**Status:** Accepted ($d)" || { echo "WRONG MARKER $f"; exit 1; }; test -z "$(sed -n '2p' "$f")" -a -z "$(sed -n '4p' "$f")" || { echo "NOT OWN BLOCK $f"; exit 1; }; done</automated>
    <fails_when>output contains `WRONG MARKER` or `NOT OWN BLOCK`, or non-zero exit - a doc carries the wrong date, the marker is not on line 3, or the marker is glued to the H1 or first paragraph</fails_when>

    <automated>test "$(grep -c '^    locked: true$' gsd-ingest-manifest.yaml)" = "4"</automated>
    <fails_when>the printed count is anything other than 4 - three means an ADR entry was missed, five or more means a non-ADR entry was locked</fails_when>

    <automated>node -e "const y=require('yaml'),fs=require('fs');const d=y.parse(fs.readFileSync('gsd-ingest-manifest.yaml','utf8'));if(d.docs.length!==26){console.log('ENTRY COUNT '+d.docs.length);process.exit(1)}const adr=d.docs.filter(x=>x.type==='ADR');const order=adr.map(x=>x.path).join(',');if(order!=='docs/checkout-trust-boundary.md,docs/webhooks-refunds-inventory.md,docs/database-migrations.md,docs/subscriptions.md'){console.log('ADR ORDER CHANGED '+order);process.exit(1)}if(adr.filter(x=>x.locked===true).length!==4){console.log('ADR LOCK COUNT '+adr.filter(x=>x.locked===true).length);process.exit(1)}const bad=d.docs.filter(x=>x.type!=='ADR'&&'locked' in x);if(bad.length){console.log('NON-ADR LOCKED '+bad.length);process.exit(1)}"</automated>
    <fails_when>non-zero exit, or output containing `ENTRY COUNT`, `ADR ORDER CHANGED`, `ADR LOCK COUNT`, or `NON-ADR LOCKED` - the YAML failed to parse, an entry was dropped, the ADR entries were reordered, fewer than four ADRs are locked, or a non-ADR entry was locked</fails_when>

    <automated>test "$(git diff --numstat docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md | awk '{s+=$2} END {print s+0}')" = "0"</automated>
    <fails_when>the printed number is greater than 0 - a line was deleted from one of the three status-marker-only docs, which the task forbids</fails_when>

    <human-check>
      Russell: confirm the doc ingest classifies all four ADRs as locked. Run this on a throwaway
      branch - ingest merge mode can write into `.planning/`.

      1. `git checkout -b throwaway/ingest-check`
      2. `/gsd-ingest-docs --manifest gsd-ingest-manifest.yaml --mode merge`
      3. Confirm all four ADR docs classify as locked, with no lock-status note for them and no warning
         that `docs/checkout-trust-boundary.md` contradicts a SPEC or PRD source.
      4. `git checkout main`, then `git branch -D throwaway/ingest-check`, then discard anything the run
         wrote (`git checkout -- .` and `git clean -fd .planning/` if needed).

      Why this is not automated: the in-phase gates are structural. They prove the marker text and the
      manifest shape, but the lock decision is an LLM classifier's semantic read of the document, which
      grep cannot reproduce (RESEARCH.md, "Don't Hand-Roll"). If any of the four still classifies as
      unlocked, name it and paste the line the run printed - that is a real gap and feeds a follow-up,
      not a false alarm.
    </human-check>
  </verify>
  <done>All four ADR docs carry a dated `Accepted` marker on line 3 as its own markdown block with the correct per-document date, and `gsd-ingest-manifest.yaml` carries exactly four `locked: true` keys on the four ADR entries with all 26 entries intact and in their original order. The throwaway-branch ingest re-run is harvested as an end-of-phase human verification item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository docs -> future reader (human or AI planner) | A binding-decision document is read as authoritative. A false statement marked `Accepted` is trusted input to future planning. |
| repository config -> GSD ingest pipeline | `gsd-ingest-manifest.yaml` routes documents into precedence buckets during ingest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Repudiation | four ADR docs + `gsd-ingest-manifest.yaml` | low | mitigate | Each marker carries the document's own decision date and the manifest carries the 2026-09-02 lock date, so a reader can tell when each decision became binding and when it was labelled. The ADR-01 supersession sentence keeps the correction on the record instead of silently rewriting history. |
| T-03-02 | Tampering | `docs/checkout-trust-boundary.md` boundary statement | low | mitigate | The replacement text names four real code identifiers, and Task 1 greps each against the source before writing. A claim the code does not support cannot be written without the grep failing first. |
| T-03-03 | Information Disclosure | all five edited files | low | mitigate | No edit introduces a credential value, endpoint secret, or account identifier. The Phase 1 SEC-01 placeholder rule still holds and the repository-wide credential scan is unaffected. |
| T-03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages. RESEARCH.md records "N/A - this phase installs no external packages", so the package-legitimacy gate has nothing to audit and no install checkpoint is required. |

No threat is rated `high` or `critical`, so nothing in this plan meets the configured blocking
threshold (`security_block_on: high`, ASVS level 1).
</threat_model>

<verification>
Run after both auto tasks are committed:

- `grep -lE '^\*\*Status:\*\* Accepted \(20[0-9]{2}-[0-9]{2}-[0-9]{2}\)$' docs/checkout-trust-boundary.md docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md | wc -l` prints `4`.
- `grep -c '^    locked: true$' gsd-ingest-manifest.yaml` prints `4`.
- `grep -c 'remains outside' docs/checkout-trust-boundary.md` prints `0`.
- `git ls-files -- gsd-ingest-manifest.yaml` is non-empty.
- `npm run lint` passes. No source file changed in this plan, so this is a cheap regression guard only.

Deferred to the end-of-phase human verification item on Task 2: the full `/gsd-ingest-docs` re-run on a
throwaway branch. The structural checks above are a proxy for the classifier's semantic judgment, never
a substitute for it.
</verification>

<success_criteria>
1. `docs/checkout-trust-boundary.md` states that MCP `create_payment_intent` and `place_order` share the
   storefront's pricing service and idempotent finalizer and that MCP checkout is inside the paid
   inventory boundary; the superseded sentence is gone and the 2026-09-02 correction is recorded.
2. All four ADR docs show `Accepted` with their own decision date on line 3, as their own markdown block.
3. `gsd-ingest-manifest.yaml` is git-tracked and carries exactly four `locked: true` keys on the four ADR
   entries, with all 26 entries intact and in their original order.
4. The throwaway-branch `/gsd-ingest-docs` re-run is recorded as an end-of-phase human verification item
   with its exact command and branch-cleanup steps, ready for Russell to run and report.
</success_criteria>

<output>
Create `.planning/phases/03-decision-lock-in-and-operator-runbooks/03-01-SUMMARY.md` when done.

Record in the SUMMARY: whether the optional `**Binding:**` line was added, any code identifier that
differed from the plan text, and the note that the manifest `locked:` key is a human-readable record
rather than the mechanism that causes locking.
</output>

