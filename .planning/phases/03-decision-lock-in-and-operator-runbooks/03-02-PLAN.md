---
phase: 03-decision-lock-in-and-operator-runbooks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DEPLOYMENT_SETUP.md
  - docs/CLAUDE.md
  - docs/STRIPE_INTEGRATION.md
autonomous: true
requirements: [RUN-01, RUN-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator reading docs/CLAUDE.md or docs/DEPLOYMENT_SETUP.md finds only guarded remote migration commands; no unguarded production wrangler migration command remains in either file."
    - "Both runbooks state that npm run deploy never applies remote migrations and that npm run deploy:ci (Cloudflare Workers Builds) applies production migrations before upload."
    - "docs/DEPLOYMENT_SETUP.md requires Node.js 24.18.1 and points at .nvmrc and the package.json engines field."
    - "The Stripe webhook event lists in docs/DEPLOYMENT_SETUP.md and docs/STRIPE_INTEGRATION.md are identical, carry the sixteen handled events in two labelled groups, and no longer list the checkout-session event."
    - "Both runbooks point at docs/webhooks-refunds-inventory.md as the binding source for the required event set and at docs/database-migrations.md as the binding source for migration policy."
    - "EDGE (RUN-01, idempotency): each runbook carries exactly one deploy-paths note and exactly one guarded migration command block, so re-applying the RUN-01 edit cannot duplicate either."
    - statement: "EDGE (RUN-01, concurrency): docs/CLAUDE.md and docs/DEPLOYMENT_SETUP.md both state that npm run deploy never applies remote migrations, so a deploy cannot race an operator-run production migration; the underlying concurrency guarantee lives in scripts/d1-migrate.mjs and scripts/lib/d1-migrate-plan.mjs and is not re-tested by this phase."
      verification: backstop
    - "EDGE (RUN-02, adjacency): refund.updated and charge.refund.updated stay distinct - refund.updated appears as a Required bullet in both docs, charge.refund.updated appears only in the compatibility sentence and never as a Required bullet."
    - "EDGE (RUN-02, ordering): the two runbooks list the same sixteen events in the same order, Required group first and Subscriptions group second, in both files."
  artifacts:
    - docs/DEPLOYMENT_SETUP.md
    - docs/CLAUDE.md
    - docs/STRIPE_INTEGRATION.md
  key_links:
    - "runbook migration commands -> package.json scripts db:migrate:status:* and db:migrate:apply:* -> scripts/lib/d1-migrate-plan.mjs canApply(): the documented production command only works because it carries MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1, which the gate equality-checks against the string 1."
    - "runbook event lists -> app/api/webhooks/stripe/route.ts dispatch switch: every event the docs tell an operator to subscribe to must have a case in that switch."
    - "runbook event lists -> docs/webhooks-refunds-inventory.md: that ADR stays the binding source; the runbooks carry the full list plus a pointer to it."
  prohibitions:
    - statement: "MUST NOT instruct an operator to stop subscribing to a Stripe event the route still handles - dropping a refund or subscription event from the runbook list silently stops reconciliation and loses customer money."
      status: resolved
      verification: judgment
---

<objective>
Make the three operator runbooks describe the procedures the repository actually enforces.

Purpose: RUN-01 replaces the unguarded production migration commands in `docs/CLAUDE.md` and
`docs/DEPLOYMENT_SETUP.md` with the guarded npm scripts, adds the `deploy` versus `deploy:ci`
distinction, and corrects the Node requirement. RUN-02 replaces the Stripe webhook event lists in
`docs/DEPLOYMENT_SETUP.md` and `docs/STRIPE_INTEGRATION.md` with the sixteen events the route actually
handles, in two labelled groups, identical in both files.

Output: three edited runbooks. No code changes; the matching webhook route change is Plan 03.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-CONTEXT.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-RESEARCH.md
@.planning/phases/03-decision-lock-in-and-operator-runbooks/03-PATTERNS.md
</context>

<!-- planner-discipline-allow: wrangler d1 migrations apply -->

## Artifacts this phase produces (Plan 02)

| Path | Change |
|---|---|
| `docs/DEPLOYMENT_SETUP.md` | Node prerequisite line, Step 1 migration block, Step 2 deploy-paths note, Step 4 event list |
| `docs/CLAUDE.md` | deploy-paths note in the command list, Migration Commands block, Database Changes step 3 |
| `docs/STRIPE_INTEGRATION.md` | section 4 event list, identical to the one in `docs/DEPLOYMENT_SETUP.md` |

- **Symbols created:** none. This plan contains no code.
- **Symbols removed:** none.
- **New literal strings:** `npm run db:migrate:status:preview`, `npm run db:migrate:apply:preview`, `npm run db:migrate:status:production`, `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production`, `npm run deploy:ci`, `**Deploy paths:**`, `Node.js 24.18.1`, `**Required**`, `**Subscriptions**`, and the eleven subscription event names.
- **Strings removed:** every unguarded remote `wrangler d1 migrations apply mercora-db` instruction, the `Node.js 18+` prerequisite, and the checkout-session event bullet in both webhook lists.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end runbook correction on one file - Node, migrations, deploy paths, webhook events in DEPLOYMENT_SETUP</name>
  <files>docs/DEPLOYMENT_SETUP.md</files>
  <reversibility rating="reversible">Documentation edits only; `git revert` restores the previous text with no deployed effect.</reversibility>
  <read_first>
    - `docs/DEPLOYMENT_SETUP.md` - the file being modified. Read line 29, lines 200-215, lines 240-290.
    - `package.json` - source of truth for the script names. `deploy` is `npm run clean && npm run build:worker && opennextjs-cloudflare deploy`; `deploy:ci` is `node scripts/d1-migrate.mjs --target production --apply --confirm-production && opennextjs-cloudflare deploy`; `engines.node` is `>=24.18.1 <25`.
    - `scripts/lib/d1-migrate-plan.mjs` lines 130-144 - source of truth for the gate. Production apply requires the `--confirm-production` flag AND `MERCORA_ALLOW_PRODUCTION_MIGRATIONS` equal to the string `1`.
    - `docs/database-migrations.md` - the binding migration policy this file must point at, not contradict.
    - `docs/webhooks-refunds-inventory.md` lines 15-23 - the binding required-event wording to echo.
    - `app/api/webhooks/stripe/route.ts` lines 213-264 - the dispatch switch, the source of truth for which events have handlers.
  </read_first>
  <action>
This is the plan tracer. Take ONE runbook all the way through every correction this plan makes -
prerequisite, migration commands, deploy paths, webhook event list - and prove all four gates on it
before the same corrections are applied to `docs/CLAUDE.md` and `docs/STRIPE_INTEGRATION.md`.

**1. Node prerequisite, line 29.** Replace the Node bullet with:
`- **Node.js 24.18.1** (pinned in `.nvmrc` and `engines` in `package.json`) and npm/yarn/pnpm`
Leave every other bullet in that prerequisites list alone.

**2. Step 1: Run Migrations, lines 245-252.** Replace the whole fenced command block. The local command
stays exactly as it is, because it is what `npm run db:prepare:local` wraps. The remote commands become
the guarded npm scripts only. The block reads, in this order:

- comment `# Local (development)`, then `npx wrangler d1 migrations apply mercora-db --local`
- blank line, comment `# Preview - check, then apply`, then `npm run db:migrate:status:preview`, then `npm run db:migrate:apply:preview`
- blank line, comment `# Production - check, then apply behind the gate`, then `npm run db:migrate:status:production`, then `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production`

Immediately after the fenced block add one line:
`See `docs/database-migrations.md` for the binding migration policy.`

No remote migration command may remain that does not go through one of those npm scripts.

**3. Step 2 deploy-paths note, at the first `npm run deploy` occurrence (line 282).** Insert a
three-line note immediately above that command block:

`**Deploy paths:** `npm run deploy` builds and uploads the Worker and never applies remote migrations.`
`` `npm run deploy:ci` (used by Cloudflare Workers Builds) applies production migrations before upload. ``
`Apply migrations yourself with the guarded `db:migrate:*` scripts; `docs/database-migrations.md` is the binding source.`

Insert this note exactly once. The two later `npm run deploy` mentions at roughly lines 330 and 404 stay
untouched - they describe a correct manual deploy and CONTEXT.md scopes them out.

**4. Step 4 webhook event list, lines 205-211.** Replace the six-item list under `4. Select events:` with
two labelled groups. Every bullet is indented three spaces and wrapped in backticks, matching the
existing style. The exact content and order:

Heading `   **Required** (core checkout and refunds):` then the bullets, in this order:
`payment_intent.succeeded`, `payment_intent.payment_failed` with the trailing parenthetical
`(telemetry only; the subscription is deliberately retained)`, `charge.refunded`, `refund.updated`,
`refund.failed`.

Blank line, then heading `   **Subscriptions** (required once acquisition is enabled):` then the
bullets, in this order: `invoice.paid`, `invoice.payment_succeeded`, `invoice.payment_failed`,
`invoice.payment_attempt_required`, `customer.subscription.created`, `customer.subscription.updated`,
`customer.subscription.deleted`, `customer.subscription.paused`, `customer.subscription.resumed`,
`customer.subscription.pending_update_applied`, `customer.subscription.pending_update_expired`.

Blank line, then two sentences of prose:
`   `charge.refund.updated` is accepted for compatibility with older Stripe event configurations but is not required; `refund.updated` is the preferred lifecycle event.`
`   `docs/webhooks-refunds-inventory.md` is the binding source for the required set.`

`charge.refund.updated` belongs in that prose sentence only - never as a Required bullet, because it is
the legacy alias for `refund.updated` and listing both as required would double-subscribe the same
lifecycle. The final list drops the checkout-session event entirely: it has no working handler, and
Plan 03 removes its dead branch from the route. Do not drop any other event - each of the sixteen has a
case in the dispatch switch, and removing one from the list would tell an operator to unsubscribe from
an event that still does real work.

Cross-check every one of the sixteen event names against the `switch (event.type)` block in
`app/api/webhooks/stripe/route.ts` before committing. If any listed event has no case, stop and report
it rather than documenting it.
  </action>
  <acceptance_criteria>
    - `docs/DEPLOYMENT_SETUP.md` contains `Node.js 24.18.1`, `.nvmrc` and `engines`, and does not contain the string `Node.js 18`.
    - Every line in `docs/DEPLOYMENT_SETUP.md` that mentions a wrangler migration apply command also carries `--local`; the file contains all four `npm run db:migrate:*` script names and the `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production` line.
    - `grep -c '^\*\*Deploy paths:\*\*' docs/DEPLOYMENT_SETUP.md` prints `1`, and the file contains `npm run deploy:ci` and `never applies remote migrations`.
    - `grep -c 'database-migrations.md' docs/DEPLOYMENT_SETUP.md` is at least `2` - one pointer after the migration block, one in the deploy-paths note.
    - `grep -c '^   - `' docs/DEPLOYMENT_SETUP.md` prints `16`, and the file contains `**Required**`, `**Subscriptions**`, `charge.refund.updated` and `webhooks-refunds-inventory.md`.
    - `grep -c 'checkout.session.completed' docs/DEPLOYMENT_SETUP.md` prints `0`.
    - `charge.refund.updated` appears in `docs/DEPLOYMENT_SETUP.md` only in prose, never on a line matching `^   - `.
  </acceptance_criteria>
  <verify>
    <automated>grep -qF 'Node.js 24.18.1' docs/DEPLOYMENT_SETUP.md && grep -qF '.nvmrc' docs/DEPLOYMENT_SETUP.md && ! grep -qF 'Node.js 18' docs/DEPLOYMENT_SETUP.md</automated>
    <fails_when>non-zero exit - the pinned version or the .nvmrc pointer is missing, or the superseded Node prerequisite is still present</fails_when>

    <automated>test "$(grep -n 'wrangler d1 migrations apply' docs/DEPLOYMENT_SETUP.md | grep -vc -- '--local')" = "0"</automated>
    <fails_when>non-zero exit, meaning the counted number of wrangler migration lines without a `--local` flag is greater than zero - an unguarded remote migration instruction survives. Written as a count rather than `! grep | grep` because pipeline negation is unreliable in this repo's shell</fails_when>

    <automated>for s in 'npm run db:migrate:status:preview' 'npm run db:migrate:apply:preview' 'npm run db:migrate:status:production' 'MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production' 'npm run deploy:ci' 'never applies remote migrations'; do grep -qF "$s" docs/DEPLOYMENT_SETUP.md || { echo "MISSING $s"; exit 1; }; done</automated>
    <fails_when>output contains a line starting with `MISSING`, or non-zero exit - one of the guarded scripts, the CI deploy path, or the deploy-paths wording is absent</fails_when>

    <automated>test "$(grep -c '^\*\*Deploy paths:\*\*' docs/DEPLOYMENT_SETUP.md)" = "1"</automated>
    <fails_when>the printed count is 0 (the note was never added) or 2 or more (the note was duplicated by a re-run)</fails_when>

    <automated>test "$(grep -c '^   - `' docs/DEPLOYMENT_SETUP.md)" = "16"</automated>
    <fails_when>the printed count is anything other than 16 - the list is short an event, carries an extra one, or the bullet indentation drifted from three spaces</fails_when>

    <automated>test "$(grep -c 'checkout.session.completed' docs/DEPLOYMENT_SETUP.md)" = "0"</automated>
    <fails_when>the printed count is greater than 0 - the removed event is still listed for operators to subscribe to</fails_when>

    <automated>test "$(grep '^   - `' docs/DEPLOYMENT_SETUP.md | grep -c 'charge.refund.updated')" = "0"</automated>
    <fails_when>non-zero exit, meaning the legacy alias was counted among the event bullets instead of staying in the compatibility sentence</fails_when>

    <automated>for s in '**Required**' '**Subscriptions**' 'charge.refund.updated' 'webhooks-refunds-inventory.md'; do grep -qF "$s" docs/DEPLOYMENT_SETUP.md || { echo "MISSING $s"; exit 1; }; done</automated>
    <fails_when>output contains a line starting with `MISSING`, or non-zero exit - a group heading, the compatibility note, or the pointer to the binding ADR is absent</fails_when>

    <automated>test "$(grep -c 'database-migrations.md' docs/DEPLOYMENT_SETUP.md)" -ge 2</automated>
    <fails_when>non-zero exit - fewer than two pointers to the binding migration policy, so either the migration block or the deploy-paths note lost its pointer</fails_when>
  </verify>
  <done>`docs/DEPLOYMENT_SETUP.md` requires Node 24.18.1, shows only guarded remote migration commands with a pointer to `docs/database-migrations.md`, carries exactly one deploy-paths note naming `npm run deploy:ci`, and lists the sixteen handled Stripe events in two labelled groups with the compatibility note and the pointer to `docs/webhooks-refunds-inventory.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration and deploy-path corrections to docs/CLAUDE.md</name>
  <files>docs/CLAUDE.md</files>
  <reversibility rating="reversible">Documentation edits only; `git revert` restores the previous text.</reversibility>
  <read_first>
    - `docs/CLAUDE.md` - the file being modified. Read lines 52-70, 212-232, 450-465.
    - `docs/DEPLOYMENT_SETUP.md` as edited by Task 1 - the migration block and deploy-paths note to copy word for word.
    - `package.json` - source of truth for the `deploy`, `deploy:ci` and `db:migrate:*` script names.
    - `docs/database-migrations.md` - the binding policy this file must point at.
  </read_first>
  <action>
Apply the same two corrections Task 1 proved, to `docs/CLAUDE.md`. Copy the wording from
`docs/DEPLOYMENT_SETUP.md` so the two runbooks agree.

**1. Deploy-paths note in the command list (around line 61).** The `# Deployment` block currently lists
`npm run deploy`, `npm run clean` and `npm run preview`. Add `npm run deploy:ci` to that list with the
inline comment `# Workers Builds path: applies production migrations, then deploys`, and add the same
three-line deploy-paths note used in `docs/DEPLOYMENT_SETUP.md` directly under the fenced command block:

`**Deploy paths:** `npm run deploy` builds and uploads the Worker and never applies remote migrations.`
`` `npm run deploy:ci` (used by Cloudflare Workers Builds) applies production migrations before upload. ``
`Apply migrations yourself with the guarded `db:migrate:*` scripts; `docs/database-migrations.md` is the binding source.`

Insert the note exactly once.

**2. Migration Commands block (lines 219-223).** Replace the two-line block. The local line stays; the
production line is replaced by the guarded npm scripts. Use the identical block from
`docs/DEPLOYMENT_SETUP.md` Step 1: `# Local (development)` then
`npx wrangler d1 migrations apply mercora-db --local`; then the preview pair
`npm run db:migrate:status:preview` and `npm run db:migrate:apply:preview`; then the production pair
`npm run db:migrate:status:production` and
`MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production`. Immediately after the fenced
block add the line: `See `docs/database-migrations.md` for the binding migration policy.`

**3. Database Changes step 3 (around line 459).** Step 3 currently tells the reader to apply migrations
with a bare wrangler command. Replace step 3 with: apply through the guarded scripts -
`npm run db:migrate:status:preview` then `npm run db:migrate:apply:preview` for preview, and
`npm run db:migrate:status:production` then
`MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production` for production - and end the
step with a pointer to `docs/database-migrations.md`. Leave steps 1 and 2 of that list unchanged.

Do not add a Node-version line to this file. It states no Node version today and RUN-01's Node edit is
`docs/DEPLOYMENT_SETUP.md` only; adding one here would be a no-op task the executor cannot ground.
  </action>
  <acceptance_criteria>
    - Every line in `docs/CLAUDE.md` that mentions a wrangler migration apply command also carries `--local`; nothing remains that applies remote migrations outside the npm scripts.
    - `docs/CLAUDE.md` contains all four `npm run db:migrate:*` script names and the `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production` line.
    - `grep -c '^\*\*Deploy paths:\*\*' docs/CLAUDE.md` prints `1`, and the file contains `npm run deploy:ci` and `never applies remote migrations`.
    - `grep -c 'database-migrations.md' docs/CLAUDE.md` is at least `3` - the deploy-paths note, the migration block pointer, and the Database Changes step.
    - The deploy-paths note text in `docs/CLAUDE.md` matches the note in `docs/DEPLOYMENT_SETUP.md`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -n 'wrangler d1 migrations apply' docs/CLAUDE.md | grep -vc -- '--local')" = "0"</automated>
    <fails_when>non-zero exit, meaning the counted number of wrangler migration lines without a `--local` flag is greater than zero - an unguarded instruction survives, most likely the bare `Apply with ...` step in the Database Changes list</fails_when>

    <automated>for s in 'npm run db:migrate:status:preview' 'npm run db:migrate:apply:preview' 'npm run db:migrate:status:production' 'MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production' 'npm run deploy:ci' 'never applies remote migrations'; do grep -qF "$s" docs/CLAUDE.md || { echo "MISSING $s"; exit 1; }; done</automated>
    <fails_when>output contains a line starting with `MISSING`, or non-zero exit - one of the guarded scripts, the CI deploy path, or the deploy-paths wording is absent</fails_when>

    <automated>test "$(grep -c '^\*\*Deploy paths:\*\*' docs/CLAUDE.md)" = "1"</automated>
    <fails_when>the printed count is 0 (never added) or 2 or more (duplicated by a re-run)</fails_when>

    <automated>test "$(grep -c 'database-migrations.md' docs/CLAUDE.md)" -ge 3</automated>
    <fails_when>non-zero exit - fewer than three pointers to the binding migration policy, so one of the three edited blocks lost its pointer</fails_when>

    <automated>test "$(grep -c '^\*\*Deploy paths:\*\*' docs/CLAUDE.md)" = "1" && test "$(grep -A2 '^\*\*Deploy paths:\*\*' docs/CLAUDE.md)" = "$(grep -A2 '^\*\*Deploy paths:\*\*' docs/DEPLOYMENT_SETUP.md)"</automated>
    <fails_when>non-zero exit - either the note is missing or duplicated in docs/CLAUDE.md, or the three-line note text differs between the two runbooks. The count guard runs first so two absent notes cannot compare equal and pass vacuously. Written as a string comparison rather than `diff` because `diff` is shadowed by a broken shell function in this environment</fails_when>
  </verify>
  <done>`docs/CLAUDE.md` shows only guarded remote migration commands in both the Migration Commands block and the Database Changes step, carries exactly one deploy-paths note identical to the one in `docs/DEPLOYMENT_SETUP.md`, lists `npm run deploy:ci`, and points at `docs/database-migrations.md` from all three edited places. No Node-version line was added.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the webhook event list into docs/STRIPE_INTEGRATION.md, byte-identical</name>
  <files>docs/STRIPE_INTEGRATION.md</files>
  <reversibility rating="reversible">Documentation edit only; `git revert` restores the previous list.</reversibility>
  <read_first>
    - `docs/STRIPE_INTEGRATION.md` - the file being modified. The list to replace is section 4, lines 60-68.
    - `docs/DEPLOYMENT_SETUP.md` Step 4 as edited by Task 1 - the exact block to mirror.
    - `app/api/webhooks/stripe/route.ts` lines 213-264 - the dispatch switch, to cross-check every listed event has a case.
  </read_first>
  <action>
Replace the six-item list under `4. Select events to listen for:` in `docs/STRIPE_INTEGRATION.md` with
the same two labelled groups Task 1 wrote into `docs/DEPLOYMENT_SETUP.md` Step 4. Copy the block from
that file rather than retyping it: the two event lists must be byte-identical, same groups, same events,
same order, same three-space bullet indentation, same compatibility sentence, same pointer line to
`docs/webhooks-refunds-inventory.md`.

The numbered lead-in line differs between the two files - this file says "Select events to listen for:"
and the other says "Select events:" - and that difference stays. Only the bullets, group headings and
the two prose sentences below them are mirrored.

Cross-check the sixteen event names against the `switch (event.type)` block in
`app/api/webhooks/stripe/route.ts` before committing. If any listed event has no case, stop and report
it rather than documenting it.

Leave the rest of the file alone. In particular leave the reference to `docs/API_STRUCTURE.md` at line 99
exactly as it is - that file does not exist, and fixing the reference is Phase 4 REF-02, not this plan.
  </action>
  <acceptance_criteria>
    - `grep -c '^   - `' docs/STRIPE_INTEGRATION.md` prints `16`.
    - The event bullet lines in `docs/STRIPE_INTEGRATION.md` and `docs/DEPLOYMENT_SETUP.md` are identical, line for line and in the same order.
    - `docs/STRIPE_INTEGRATION.md` contains `**Required**`, `**Subscriptions**`, `charge.refund.updated` and `webhooks-refunds-inventory.md`.
    - `grep -c 'checkout.session.completed' docs/STRIPE_INTEGRATION.md` prints `0`.
    - `charge.refund.updated` appears only in prose, never on a line matching `^   - `.
    - `grep -c 'API_STRUCTURE.md' docs/STRIPE_INTEGRATION.md` still prints `1` - the Phase 4 item was not touched.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^   - `' docs/STRIPE_INTEGRATION.md)" = "16"</automated>
    <fails_when>the printed count is anything other than 16 - the list is short an event, carries an extra one, or the bullet indentation drifted from three spaces</fails_when>

    <automated>test "$(grep -c '^   - `' docs/STRIPE_INTEGRATION.md)" = "16" && test "$(grep '^   - `' docs/DEPLOYMENT_SETUP.md)" = "$(grep '^   - `' docs/STRIPE_INTEGRATION.md)"</automated>
    <fails_when>non-zero exit - the two docs list different events, or the same events in a different order. The count guard runs first so two empty lists cannot compare equal and pass vacuously. Written as a string comparison rather than `diff` because `diff` is shadowed by a broken shell function in this environment</fails_when>

    <automated>test "$(grep -c 'checkout.session.completed' docs/STRIPE_INTEGRATION.md)" = "0"</automated>
    <fails_when>the printed count is greater than 0 - the removed event is still listed for operators to subscribe to</fails_when>

    <automated>test "$(grep '^   - `' docs/STRIPE_INTEGRATION.md | grep -c 'charge.refund.updated')" = "0"</automated>
    <fails_when>non-zero exit, meaning the legacy alias was counted among the event bullets instead of staying in the compatibility sentence</fails_when>

    <automated>for s in '**Required**' '**Subscriptions**' 'charge.refund.updated' 'webhooks-refunds-inventory.md'; do grep -qF "$s" docs/STRIPE_INTEGRATION.md || { echo "MISSING $s"; exit 1; }; done</automated>
    <fails_when>output contains a line starting with `MISSING`, or non-zero exit - a group heading, the compatibility note, or the pointer to the binding ADR is absent</fails_when>

    <automated>test "$(grep -c 'API_STRUCTURE.md' docs/STRIPE_INTEGRATION.md)" = "1"</automated>
    <fails_when>the printed count is not 1 - the out-of-scope Phase 4 reference was edited or removed by this plan</fails_when>

    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or `error` lines in the eslint output - a cheap regression guard, since this plan changes no source file</fails_when>
  </verify>
  <done>`docs/STRIPE_INTEGRATION.md` section 4 lists the same sixteen events in the same two groups and the same order as `docs/DEPLOYMENT_SETUP.md`, with the compatibility sentence and the pointer to `docs/webhooks-refunds-inventory.md`, and the Phase 4 `API_STRUCTURE.md` reference is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production D1 | A runbook command an operator copies and runs is a direct, destructive path into the production database. |
| operator -> Stripe dashboard config | The documented event list decides which events Stripe delivers, and therefore which money movements get reconciled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | `docs/CLAUDE.md` and `docs/DEPLOYMENT_SETUP.md` migration blocks | medium | mitigate | The runbooks currently hand an operator a bare remote wrangler apply command that bypasses `canApply()` entirely. Replacing it with `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1 npm run db:migrate:apply:production` puts every production apply behind the env gate and the `--confirm-production` flag. This is a net reduction in risk: the phase removes an unguarded destructive instruction rather than adding one. |
| T-03-05 | Tampering | `docs/DEPLOYMENT_SETUP.md` and `docs/STRIPE_INTEGRATION.md` event lists | medium | mitigate | An event dropped from the runbook list is an event an operator never subscribes to, so refunds or subscription state stop reconciling with no error anywhere. Tasks 1 and 3 cross-check all sixteen names against the dispatch switch before committing, and the `diff` gate proves the two lists agree. |
| T-03-06 | Information Disclosure | all three runbooks | low | mitigate | No edit introduces a credential value, key, or account identifier. `MERCORA_ALLOW_PRODUCTION_MIGRATIONS` is a gate flag, not a secret. The Phase 1 SEC-01 placeholder rule is unaffected. |
| T-03-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages. RESEARCH.md records "N/A - this phase installs no external packages", so the package-legitimacy gate has nothing to audit. |

No threat is rated `high` or `critical`, so nothing in this plan meets the configured blocking
threshold (`security_block_on: high`, ASVS level 1).
</threat_model>

<verification>
Run after all three tasks are committed:

- `grep -n 'wrangler d1 migrations apply' docs/CLAUDE.md docs/DEPLOYMENT_SETUP.md | grep -vc -- '--local'` prints `0`.
- `grep -c '^   - `' docs/DEPLOYMENT_SETUP.md` and `grep -c '^   - `' docs/STRIPE_INTEGRATION.md` both print `16`, and the two bullet sets compare equal as strings.
- `grep -rc 'checkout.session.completed' docs/DEPLOYMENT_SETUP.md docs/STRIPE_INTEGRATION.md` prints `0` for both files.
- `grep -c '^\*\*Deploy paths:\*\*' docs/CLAUDE.md` and the same for `docs/DEPLOYMENT_SETUP.md` both print `1`.
- `npm run lint` passes.
</verification>

<success_criteria>
1. An operator following `docs/CLAUDE.md` or `docs/DEPLOYMENT_SETUP.md` finds only the guarded
   `db:migrate:*` commands, with production behind `MERCORA_ALLOW_PRODUCTION_MIGRATIONS=1`, and each
   block points at `docs/database-migrations.md`.
2. Both runbooks state that `npm run deploy` never applies remote migrations and that `npm run deploy:ci`
   applies production migrations before upload, once each.
3. `docs/DEPLOYMENT_SETUP.md` requires Node.js 24.18.1 and names `.nvmrc` and the `engines` field.
4. The Stripe webhook event lists in `docs/DEPLOYMENT_SETUP.md` and `docs/STRIPE_INTEGRATION.md` are
   identical, list the sixteen handled events in two labelled groups, keep `charge.refund.updated` in the
   compatibility sentence only, and no longer list the checkout-session event.
</success_criteria>

<output>
Create `.planning/phases/03-decision-lock-in-and-operator-runbooks/03-02-SUMMARY.md` when done.

Record in the SUMMARY: the final sixteen-event list as written, confirmation that each name was matched
against a case in the dispatch switch, and the exact deploy-paths wording, so Plan 03's route change and
any later phase can be checked against it.
</output>

