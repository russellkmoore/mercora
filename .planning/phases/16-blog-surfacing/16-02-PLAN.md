---
phase: 16-blog-surfacing
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - components/Header.tsx
  - components/HeaderClient.tsx
  - tests/unit/components/header-blog-nav.test.ts
autonomous: true
requirements: [BLOG-01]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A shopper on a storefront with at least one published article sees a blog link in the desktop header nav and in the mobile menu (D-07, BLOG-01)."
    - "A shopper on a storefront with no published article sees no blog link in either place — the markup is absent, not hidden with CSS (D-06, D-08)."
    - "Both nav entries carry the same admin-set label from the same prop, so desktop and mobile can never disagree (D-07)."
    - "The existence check and the label are both resolved on the server and arrive as props; the client component performs no fetch and no settings read (D-08, D-18)."
    - "The header only ever learns a boolean about post existence — no post row, draft or published, crosses into the client component (D-03)."
    - "The label travels as a server-resolved prop, not through the store config: `StoreConfig` stays purely environment-derived and gains no database read (D-09 as resolved by D-18)."
    - "`components/Footer.tsx`'s own hardcoded blog link is untouched — it keeps its own label and its own visibility rule (D-14)."
    - "The show/hide rule and the label are both pinned by an automated test mirroring the account-navigation gate pattern, and the mobile width contract test stays green unmodified (D-15)."
  artifacts:
    - tests/unit/components/header-blog-nav.test.ts
  key_links:
    - "`getContentSettings()` + `getPublishedBlogPosts({ limit: 1 })` in `components/Header.tsx` -> `showBlogNav`/`blogNavLabel` props -> two conditionals in `components/HeaderClient.tsx`."
    - "`tests/unit/components/mobile-header-contract.test.ts` pins raw source substrings of `HeaderClient.tsx` and must stay green unmodified."
---

<objective>
Put the blog on the storefront's map. `components/Header.tsx` resolves two server-side facts — does any
published article exist, and what should the link be called — and hands them to `HeaderClient`, which renders
one conditional link in the desktop nav and one in the mobile sheet.

Purpose: BLOG-01. Today the blog index exists at `/blog` with no route into it from anywhere a shopper looks.

Output: additive edits to `components/Header.tsx` and `components/HeaderClient.tsx`, plus a new test file
proving both the server gate and desktop/mobile parity.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-blog-surfacing/16-CONTEXT.md
@.planning/phases/16-blog-surfacing/16-RESEARCH.md
@.planning/phases/16-blog-surfacing/16-PATTERNS.md
@.planning/phases/16-blog-surfacing/16-01-SUMMARY.md
</context>

<interface_context>
Consumed from 16-01 (frozen):

```typescript
import { getContentSettings } from "@/lib/content/settings";
const { blogNavLabel } = await getContentSettings();   // always a non-empty string
```

Published by this plan, consumed by nothing else in this phase:

```typescript
// components/HeaderClient.tsx
interface HeaderClientProps {
  categories: MACHCategory[];
  showBlogNav: boolean;
  blogNavLabel: string;
}
```
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Header.tsx resolves blog visibility and label server-side</name>
  <files>components/Header.tsx</files>
  <read_first>
    - `components/Header.tsx` — the whole file (55 lines). The `getCachedCategories` wrapper and the five-line `Header()` body are the exact fetch-then-prop-pass shape to extend.
    - `lib/models/blog.ts` `getPublishedBlogPosts` (around line 198) — confirm the signature, the `status = 'published' AND publishedAt <= now` filter, and that nothing wraps it in a cache.
    - `.planning/phases/16-blog-surfacing/16-01-SUMMARY.md` — the exact export names 16-01 landed.
  </read_first>
  <action>
Add two awaited values and two props to `Header()`.

Import `getContentSettings` from `@/lib/content/settings` and `getPublishedBlogPosts` from
`@/lib/models/blog`. Inside `Header()`, after the existing categories line, resolve the label from
`getContentSettings()` and the visibility boolean from `getPublishedBlogPosts({ limit: 1 })`, treating a
non-empty result as "articles exist" (D-03, D-08). Pass both down as `showBlogNav` and `blogNavLabel`
alongside the existing `categories` prop.

Convert the value to a boolean at the point it is computed. `HeaderClient` must receive a boolean and
nothing else — never the row, never the array, never a length. The header's existence check is the only
place in this phase where a post row is touched outside the home block, and a draft post must never become
observable through it; `getPublishedBlogPosts` already excludes drafts and future-dated posts, and collapsing
its result to a boolean before it crosses the server/client boundary means even a future change to that
query cannot leak a row into the client bundle (T-16-11).

Do not wrap either new read in `unstable_cache`. Nothing in this repository caches the published-posts list,
the root layout is already fully dynamic, and adding a cache here would need a new tag plus a revalidation
call site on every admin publish and unpublish. Leave `getCachedCategories` exactly as it is.

Extend the file's header comment block: add the two new resolved values to the `Data Flow` list and name the
blog entry in the `Architecture` line, matching the comment style already there.
  </action>
  <acceptance_criteria>
    - `grep -c "unstable_cache" components/Header.tsx` returns `1` — the pre-existing categories wrapper, and nothing new.
    - `grep -c "limit: 1" components/Header.tsx` returns `1`.
    - The value passed as `showBlogNav` is a boolean expression, not an array or a number: `grep -cE "showBlogNav=\{[a-zA-Z]+\}" components/Header.tsx` returns `1` and the bound identifier is assigned from a comparison or a boolean coercion.
    - `git diff -- components/Header.tsx` removes no existing line except the single-prop `HeaderClient` element it replaces.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run typecheck</automated>
    <fails_when>`HeaderClient` rejects the two new props because Task 2 has not run yet — expected on the first commit of this task; it passes once Task 2 lands. If it still fails after Task 2, the prop names or types disagree between the two files.</fails_when>
  </verify>
  <done>`Header()` awaits three values and renders `HeaderClient` with three props; the blog existence fact is a boolean before it leaves the server.</done>
</task>

<task type="auto">
  <name>Task 2: HeaderClient renders the conditional link in both navs</name>
  <files>components/HeaderClient.tsx</files>
  <read_first>
    - `components/HeaderClient.tsx` lines 69-75 (`HeaderClientProps`), 130-134 (the component signature and destructure), 357-411 (the desktop nav `div`, starting at the `Home` link and ending after `CartTrigger`), and 449-485 (the mobile sheet's `space-y-6` `div`, from the mobile `Home` link through the `Help & Search` block). Read the real current line numbers — do not trust these.
    - `tests/unit/components/mobile-header-contract.test.ts` — the whole file (26 lines). Note exactly which raw substrings and which regex-captured element bodies it pins.
    - `components/account/AccountNav.tsx` — the config-gated-link shape D-07 names as the template.
  </read_first>
  <action>
Add the two props and two conditional links. Nothing else in this file changes.

Add `showBlogNav: boolean;` and `blogNavLabel: string;` to `HeaderClientProps`, and destructure both in the
component signature alongside `categories`.

Desktop: inside the existing `hidden md:flex` nav `div`, insert a new sibling JSX expression between the
`Categories` `DropdownMenu` element and the `ClientOnly`-wrapped agent drawer. It renders, only when
`showBlogNav` is true, a `next/link` to `/blog` with `prefetch={true}` and the label as its text child. Copy
the `Home` link's `className` string verbatim so the entry sits in the nav row identically. Per D-07's
discretion note, use no icon — the entry is plain text, like the `Categories` trigger's own label.

Mobile: inside the sheet's `space-y-6` `div`, insert the same conditional between the
`SimpleMobileCategoryList` wrapper `div` and the `border-t border-border pt-6 space-y-3` block. It renders a
`next/link` to `/blog` with the mobile `Home` link's `className` copied verbatim, an
`onClick` that closes the sheet exactly as the mobile `Home` link does, and the label wrapped in a `span` to
match its sibling's shape.

Both conditionals read the same two props. That is the whole mechanism keeping desktop and mobile from
drifting apart — do not compute a second label, do not re-derive visibility, do not add a separate mobile
prop.

Do not create `components/header-links.ts`. 16-RESEARCH.md's project-structure sketch proposed it and
16-PATTERNS.md flags it as possibly unnecessary; D-07's own text settles it — "a single inline conditional is
fine", and one boolean gating one link does not need an array builder. The account-navigation helper is the
template for *how* to gate, not a module to mirror. Planned, considered, deliberately not built.

Render the label as a JSX text child. This is the only place an admin-authored string reaches the storefront
header, and passing it as a child is what makes React escape it; no raw-markup prop is introduced anywhere in
this file (T-16-12).

Do not open `components/Footer.tsx`. Its own blog link is hardcoded, is conditioned on the CMS page list
rather than on article existence, and stays exactly as it is — D-14 records that as a deliberate leave-alone,
not an oversight. Re-labelling it from this setting would make it disagree with its own visibility rule.

Touch nothing else. Specifically: leave the logo `Link` and its `min-w-0 flex-1 truncate` class alone, leave
the mobile controls wrapper and its `flex shrink-0 items-center gap-1` class alone, leave both
`SheetTrigger` and `DropdownMenuTrigger` element bodies alone, and do not open `components/cart/CartDrawer.tsx`
at all. `mobile-header-contract.test.ts` asserts on this file's raw source text and on the first
regex-captured `DropdownMenuTrigger` body, so an insertion in the wrong place breaks it even when the
rendered output is correct.

Extend the file's header comment: add the blog entry to the `Navigation Structure` diagram line and add the
two new props to the `Props` list, matching the existing comment style.
  </action>
  <acceptance_criteria>
    - `grep -c "showBlogNav &&" components/HeaderClient.tsx` returns exactly `2` — one desktop, one mobile.
    - `grep -c "{blogNavLabel}" components/HeaderClient.tsx` returns exactly `2`.
    - `grep -c 'href="/blog"' components/HeaderClient.tsx` returns exactly `2`.
    - `grep -c "dangerouslySetInnerHTML" components/HeaderClient.tsx` returns `0`.
    - `git diff -- components/cart/CartDrawer.tsx components/Footer.tsx` is empty.
    - `git diff -- components/HeaderClient.tsx` shows no removed line other than the props interface and the destructure it replaces.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/mobile-header-contract.test.ts</automated>
    <fails_when>An insertion disturbed a pinned region — the failure names which substring or trigger body no longer matches, most likely the first `DropdownMenuTrigger` capture if the blog link was placed inside the dropdown rather than beside it.</fails_when>
  </verify>
  <verify>
    <automated>mise exec -- npm run typecheck && mise exec -- npm run lint && mise exec -- npm run scan:tokens</automated>
    <fails_when>The props do not match what `Header.tsx` passes, or the copied `className` introduced a literal colour instead of a token class — `scan:tokens` reports the offending file and class.</fails_when>
  </verify>
  <done>The desktop nav and the mobile sheet each carry one blog link, both gated on the same boolean and both labelled from the same string; the mobile width contract test is green without being edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the server gate and desktop/mobile parity</name>
  <files>tests/unit/components/header-blog-nav.test.ts</files>
  <read_first>
    - `tests/unit/components/account/account-subscriptions-navigation.test.ts` — the whole file. The `vi.hoisted` mutable-mocks object and the mock-then-import ordering are the pattern to mirror.
    - `tests/unit/app/checkout-honor-gate.test.ts` lines 43-62 — the `clientProps(node)` tree-walking helper that reads a client boundary's props out of an awaited server component's returned element. This is the technique for asserting on `Header()` without rendering the Radix/Clerk tree.
    - `vitest.config.mts` — confirm `include` is `tests/unit/**/*.test.ts` (not `.tsx`), so this file must build elements with `React.createElement` rather than JSX, and confirm `clearMocks: true`.
  </read_first>
  <behavior>
    - With `getPublishedBlogPosts` resolving an empty array, `Header()`'s client boundary receives `showBlogNav: false`.
    - With it resolving one post, the boundary receives `showBlogNav: true`.
    - The boundary receives `blogNavLabel` equal to whatever `getContentSettings()` returned, verbatim.
    - `showBlogNav` is strictly a boolean in both cases — never an array, a number, or a truthy object.
    - The client boundary's props contain no post object and no array of posts under any key.
    - `getPublishedBlogPosts` is called exactly once, with a limit of 1.
    - `HeaderClient.tsx`'s source contains exactly two `showBlogNav &&` conditionals, exactly two `{blogNavLabel}` reads, and exactly two links to `/blog` — desktop and mobile, sourced identically.
  </behavior>
  <action>
Create `tests/unit/components/header-blog-nav.test.ts`.

Use `vi.hoisted` for a mutable mocks object holding the posts array and the label, then mock, before any
import of the component under test:
  - `@/lib/models` with `listCategories` resolving an empty array
  - `next/cache` with `unstable_cache` implemented as a pass-through that returns the function it was handed,
    so the real `getCachedCategories` wrapper resolves without a Next request scope
  - `@/lib/models/blog` with `getPublishedBlogPosts` as a `vi.fn()` reading the hoisted posts array
  - `@/lib/content/settings` with `getContentSettings` resolving the hoisted label plus the other four
    documented defaults
  - `@/components/HeaderClient` with a default export that is a plain identity function, so the tree walk has
    a stable identity to match and the real client component is never rendered

Then import `Header` from `@/components/Header` and the mocked `HeaderClient` default export.

Copy the `clientProps` helper from `checkout-honor-gate.test.ts`, retargeted to the mocked `HeaderClient`
identity. Write one `describe` covering the first six bullets of this task's `<behavior>` block. For the
no-post-data bullet, assert over `Object.values` of the captured props that no value is an array of objects
and that no value has a `slug` or `title` member — the point is that the header learns a boolean and nothing
else, so a future widening of the prop set fails here rather than shipping.

Add a second `describe` for desktop/mobile parity that reads `components/HeaderClient.tsx` with
`readFileSync(join(process.cwd(), ...), "utf8")` — the same technique `mobile-header-contract.test.ts` uses —
and asserts the three occurrence counts from the last bullet. Rendering the real `HeaderClient` would drag in
Radix, Clerk and the cart store for no additional signal; the counts prove both call sites exist and read the
same two props, which is the invariant D-07 actually asks for.

Commit the test before the implementation is complete if you are working task-by-task; otherwise run it
against the finished Tasks 1 and 2.
  </action>
  <acceptance_criteria>
    - The file ends in `.test.ts` and builds every element with `React.createElement` — `grep -c "</" tests/unit/components/header-blog-nav.test.ts` returns `0`.
    - `grep -c "  it(" tests/unit/components/header-blog-nav.test.ts` is at least 7.
    - The suite fails if either conditional is deleted from `HeaderClient.tsx`: temporarily removing one and re-running reports a count mismatch. Restore the file afterwards and confirm with `git diff -- components/HeaderClient.tsx` being limited to this plan's intended changes.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/header-blog-nav.test.ts tests/unit/components/mobile-header-contract.test.ts tests/unit/components/account/account-subscriptions-navigation.test.ts</automated>
    <fails_when>The gate case fails because `showBlogNav` arrived as the posts array rather than a boolean, the parity counts are not both 2 because a link was added to only one nav, or the mobile width contract broke from where the insertion landed.</fails_when>
  </verify>
  <verify>
    <automated>mise exec -- npm test</automated>
    <fails_when>The header change broke an unrelated suite — most likely a snapshot or source-contract test elsewhere that reads `HeaderClient.tsx` or `Header.tsx`.</fails_when>
  </verify>
  <done>BLOG-01's show/hide rule and its label are both pinned by automated assertions, and the desktop and mobile entries are proven to come from the same two props.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| D1 `blog_posts` -> `Header.tsx` -> client bundle | A post-existence check runs on every route; whatever it passes down is serialized into the client payload for every visitor, signed in or not |
| D1 `admin_settings` -> rendered header text | An admin-authored free-text label is rendered into the storefront chrome on every page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-11 | Information Disclosure | `components/Header.tsx` blog existence check | high | mitigate | Use `getPublishedBlogPosts` only — its filter already excludes drafts and future-dated posts — and collapse the result to a boolean before it becomes a prop, so no row of any status crosses the server/client boundary. Pinned by the "props contain no post object" case in `tests/unit/components/header-blog-nav.test.ts` |
| T-16-12 | Tampering | `blogNavLabel` rendered in `HeaderClient.tsx` | low | mitigate | The label is a JSX text child, which React escapes; no raw-markup prop is introduced in this file, asserted by an occurrence count of 0. Write access to the value is already admin-gated (16-04's T-16-21), so this is defence in depth |
| T-16-13 | Denial of Service | Two extra D1 reads on every route's header | low | accept | One indexed single-row read (`blog_posts_status_published_idx`) plus one settings-category read, both on an already fully dynamic app. The repository has an explicit Phase 07 precedent for accepting a per-request settings read rather than building cross-component memoisation to avoid it |
| T-16-14 | Tampering | An over-long or whitespace-only label breaking the header layout | low | mitigate | Resolved upstream: 16-01's `resolveContentText` falls back to `Blog` for a whitespace-only or over-60-character value, so `HeaderClient` can rely on receiving a short non-empty string |

No package-manager install occurs in this plan.
</threat_model>

<verification>
1. `mise exec -- npx vitest run tests/unit/components` — the new suite plus every existing component suite, including the mobile width contract.
2. `mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens` — no literal colour entered the storefront header.
3. `git diff --stat -- tests/unit/components/mobile-header-contract.test.ts components/cart/CartDrawer.tsx` is empty.
</verification>

<success_criteria>
- With published articles present, both navs render a link to `/blog` carrying the admin-set label.
- With none present, neither nav renders any blog markup at all.
- Desktop and mobile read the same two props, proven by occurrence counts rather than by inspection.
- `mobile-header-contract.test.ts` passes unmodified.
</success_criteria>

<artifacts>
## Artifacts this phase produces

| Artifact | Produced by | Consumed by |
|----------|-------------|-------------|
| `showBlogNav` / `blogNavLabel` props on `HeaderClient` | 16-02 | The storefront header on every route |
| `tests/unit/components/header-blog-nav.test.ts` | 16-02 | 16-04's phase gate (full suite) |
| `getContentSettings()` (from 16-01) | 16-01 | 16-02, 16-03 |
</artifacts>

<output>
Create `.planning/phases/16-blog-surfacing/16-02-SUMMARY.md` when done.
</output>
