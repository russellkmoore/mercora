---
phase: 08-documentation-visual-qa-close-out
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - .planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md
autonomous: true
requirements: [DOCS-02]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 21 preset-by-combination pairs are captured: seven shipped presets crossed with the three packed layout combinations Phase 7 defined, one harness run each (D-03). [DOCS-02]"
    - "Every run writes into this phase's own manifest, never the harness's default target, which is an earlier phase's file (D-03, RESEARCH Pitfall 1). [DOCS-02]"
    - "Each run's coverage rows are appended under a uniquely named label section, and the 21 label names are distinct and greppable (D-05)."
    - "Every run captures the same cell grid, so a difference between two runs is a real difference and not a different question being asked (D-03)."
    - "A cell whose prerequisite cannot be resolved is recorded as a MISSING row carrying the harness's own reason string, never omitted from the grid and never given invented placeholder copy (08-UI-SPEC.md `## UI Considerations`, error and partial rows; Copywriting Contract)."
    - "The coverage grid can never render empty: the 21 runs are planned before capture begins, not discovered from the environment (08-UI-SPEC.md `## UI Considerations`, empty and zero-one-many rows)."
    - "The record is a static document written as each capture completes; it has no loading state of its own (08-UI-SPEC.md `## UI Considerations`, loading row)."
    - "Rows are ordered preset-major then combination and are never re-sorted after they are written, so a reader diffing two capture sessions has stable row identity (08-UI-SPEC.md `## UI Considerations`, ordering-stability row)."
    - "The document uses one subsection per run rather than one flat table, so no single table needs horizontal scrolling in a standard markdown viewer (08-UI-SPEC.md `## UI Considerations`, overflow row, marked 🧪 backstop)."
    - "The four screenshot-coverage gaps carried since Phase 5 — no seeded order, the Stripe payment step, an authenticated account dashboard, and the review-form error state — are recorded as carried forward with their reasons, not fought (orchestrator decision, RESEARCH Open Q2)."
    - "Runs are strictly sequential: one preset-and-combination pair is written, confirmed, and captured before the next pair is written, so no two runs can interleave settings (DOCS-01 concurrency edge, resolved explicit)."
    - "The storefront is left on the four default appearance values and the dev server is stopped when capture finishes; the restore step is idempotent and can be re-run safely (RESEARCH capture-loop close-out)."
    - "Captures are taken only against localhost; the harness's own remote guard is never overridden (security contribution)."
  artifacts:
    - path: ".planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md"
      provides: "The visual QA record: framing, capture method, the 21-run coverage grid, and one appended label subsection per run"
      contains: "phase-08-a-volt-dark"
      min_lines: 200
  key_links:
    - from: ".planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md"
      to: "scripts/screenshot-routes.mjs"
      via: "every label subsection is appended by the harness under an explicit manifest flag"
      pattern: "## Label:"
    - from: "the capture loop"
      to: "app/api/admin/settings/route.ts"
      via: "one settings POST per run writes the theme key and the three layout keys under the appearance category"
      pattern: "appearance.theme"
  prohibitions:
    - statement: "This phase must NOT change any theme file, any layout variant component, any contract token, or the theme/layout resolution mechanism."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition from CONTEXT's phase boundary; binds here because this plan is the one that drives all seven themes and all eight variants and is therefore where a tempting 'quick fix' would land. Fixes belong in plan 08-04, judged and committed one at a time."
    - statement: "The capture must NOT run against a non-localhost base URL."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less security prohibition from the phase's security contribution; the harness refuses a remote host unless a flag overrides it, and that flag must not be passed."
    - statement: "The harness's manifest flag must NOT be omitted on any run."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition derived from RESEARCH Pitfall 1 and the documented 06-05 incident; the flag's default writes into Phase 5's committed screenshot file."
---

<objective>
Capture the whole matrix: seven shipped presets crossed with the three packed layout combinations,
21 harness runs, every run over the same cell grid, all recorded in one document.

Purpose: DOCS-02's evidence half. This plan produces the pixels and the coverage grid; plan 08-04
judges them against the six per-cell criteria and records findings.

Output: `08-QA-MATRIX.md` with its framing, its capture method, a 21-row coverage grid, and 21
appended label subsections — plus a storefront left on its defaults and a stopped dev server.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-documentation-visual-qa-close-out/08-CONTEXT.md
@.planning/phases/08-documentation-visual-qa-close-out/08-PATTERNS.md
@.planning/phases/08-documentation-visual-qa-close-out/08-UI-SPEC.md
</context>

<interfaces>
Nothing is exported. This plan consumes two existing tools unchanged:

- `scripts/screenshot-routes.mjs` via `npm run screenshot:routes`, with four flags on every
  invocation: a unique label, an explicit manifest path pointing at this phase's own file, the
  allow-missing flag, and the include-content flag.
- `POST /api/admin/settings` with a body of `{ updates: [{ key, value, category }] }`, four updates
  per request, all under the `appearance` category — the same endpoint and body shape the admin
  islands use.

Produced for plan 08-04: `08-QA-MATRIX.md` with framing, coverage grid and 21 label subsections.
Plan 08-04 appends its findings table and its summary table below them. Do not write a findings
table here — a findings row with no inspection behind it is worse than an absent one.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use `git commit -F`;
  redirect with `>|` when a target file may already exist.
- Captured images live under the git-ignored `.screenshots/` directory and are never committed.
  Only the manifest document is committed.
- **Correction to RESEARCH.md:** its per-run cell-count math (22 base, 28 with content) does not
  match the script as read this session. The base grid is home 4 + category 4 + product 4 + cart 2 +
  checkout 4 + account 4 + order-status 4 = 26 attempted, and the content flag adds 6 more, for 32.
  Do not assert a hardcoded count anywhere. Assert instead that all 21 runs produced the *same*
  count, and record the real number in the summary.
- **Runtime is an estimate, not a measurement.** RESEARCH flags roughly 2-4 minutes of capture per
  run as an extrapolation with no direct data behind it. That is why the 21 runs are split across
  two tasks. If task 1's first three runs come in far slower than that, say so in the summary rather
  than silently running long.
- The development bypass header name is `x-dev-admin`; its value is defined in
  `lib/auth/admin-middleware.ts` and is gated to the development environment. Read the value out of
  that file at run time — do not hardcode it into a committed script or into the manifest document.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Open the QA record and capture the first twelve runs (volt-dark, luxe, midnight, clinical)</name>
  <precondition>A local dev server can be started against the existing local D1 fixture and the settings endpoint accepts the development bypass header — the same method Phase 6, 6.1 and 7 used, because there is no Clerk session in this environment. If the bypass is rejected, the environment is not in development mode and capture cannot proceed; stop rather than working around it.</precondition>
  <read_first>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` lines 1-60 — the framing prose, the capture-command block, the theme-switch method paragraph, the carried-forward-gap note, and the per-run coverage-grid table shape. This is the format to reproduce.
    - `.planning/phases/07-layout-switches/07-SCREENSHOTS.md` — the combination-run naming precedent and the variant coverage table.
    - `scripts/screenshot-routes.mjs` lines 1-60 and its coverage-grid function — the flag list, the manifest flag's default (which points at Phase 5's file), the two viewports, the per-route state lists, and the MISSING reason strings it emits.
    - `lib/auth/admin-middleware.ts` lines 20-35 — the development bypass check and the environment gate, so the header is used correctly and its value is read rather than remembered.
    - `lib/layout/variants.ts` — the three enum arrays, so every combination value written is a real member.
    - `.planning/phases/08-documentation-visual-qa-close-out/08-CONTEXT.md` decision D-03 — the three combination definitions, verbatim.
  </read_first>
  <files>.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md</files>
  <action>
Create the record first, before capturing anything, so the harness has a file to append to and a
reader has the framing before the data.

Give it an H1 and a status line. Then a framing paragraph in the same voice the Phase 6.1 record
uses: why this is its own file rather than an append to a prior phase's, that captured images live
under the ignored screenshots directory and are never committed, and what the scope is — seven
presets crossed with three packed layout combinations, 21 runs, the full route grid plus the
content cells.

Then the capture-command block, showing the exact invocation with all four flags, and a sentence
saying why the manifest flag is passed explicitly on every single run: its default points at an
earlier phase's committed file, and relying on that default has already caused one real incident.

Then the switch-method paragraph: a local dev server against the existing local fixture; theme and
all three layout keys written in one request to the settings endpoint using the documented
development bypass header, whose value is read from the auth middleware rather than written down
here; and the storefront's rendered theme attribute plus a read-back of the appearance category
confirming the switch before every capture.

Then the combination table — three rows, A, B and C, with their three variant values each, copied
from the phase context verbatim.

Then the 21-row coverage grid table, ordered preset-major then combination, with columns for the run
number, the label, the theme written, the three layout values written, whether the switch was
confirmed, and the cells captured and missing. Write all 21 rows now with their run numbers, labels,
themes and layout values filled in and the result columns blank; fill each row's results as its run
completes. The ordering is fixed here and must never be re-sorted afterwards.

Then a carried-forward note listing the four gaps this milestone has carried since Phase 5 — no
seeded order, the Stripe payment step, an authenticated account dashboard, and the review-form error
state — each with its reason and a statement that they are carried forward deliberately, not
discovered here.

Then capture. Start the dev server in the background, redirecting its output to a temporary log, and
wait for it to actually serve before the first request rather than sleeping a fixed time and hoping.
Run strictly one pair at a time, in coverage-grid order: for each of the first four presets in the
grid's order, run its combination A, then B, then C. For each run: write all four appearance keys in
one request; read the appearance category back and confirm all four stored values; fetch the
storefront root and confirm the rendered theme attribute matches; then invoke the harness with the
unique label, the explicit manifest path, and both flags. Never start a run before the previous
run's capture has finished — interleaved settings would make both runs meaningless.

Label each run with the phase, the combination letter and the theme name, so the 21 labels are
unique and greppable.

If a run's confirmation step disagrees with what was written, stop and record it. Do not capture
against an unconfirmed state and do not retry blindly.

Leave the dev server running at the end of this task; task 2 continues against it and performs the
restore and shutdown.
  </action>
  <acceptance_criteria>
    - The record's framing, capture command, switch method, combination table, 21-row coverage grid and carried-forward note are all present before the first capture runs.
    - Twelve label subsections exist, one per run, with distinct label names.
    - Every one of the twelve runs produced the same number of coverage rows as the first.
    - Every MISSING row's note is the harness's own reason string, not invented copy.
    - The coverage grid's first twelve rows have their result columns filled.
    - No capture was taken against a base URL other than localhost.
    - Phase 5's screenshot manifest is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>M=.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md; test "$(/usr/bin/grep -c '^## Label: ' "$M")" -eq 12 && test "$(/usr/bin/grep -o '^## Label: `[^`]*`' "$M" | /usr/bin/sort -u | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 12 && test -z "$(git diff --name-only -- .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md)"</automated>
    <fails_when>the label count is not 12, meaning a run was skipped or double-appended; or the unique-label count is under 12, meaning two runs shared a label and one overwrote the other's identity; or Phase 5's manifest shows as modified, meaning a run was invoked without the explicit manifest flag.</fails_when>
    <automated>M=.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md; N=$(/usr/bin/awk '/^\| [a-z-]+ \| (1280|390) \|/ {c++} END {print c+0}' "$M"); L=$(/usr/bin/grep -c '^## Label: ' "$M"); echo "rows=$N labels=$L per-run=$((N/L))"; test "$L" -gt 0 && test $((N % L)) -eq 0 && test $((N / L)) -ge 26</automated>
    <fails_when>the printed per-run figure is not a whole number — the row total does not divide evenly by the label count, meaning one run captured a different grid than the others; or the per-run figure is under 26, meaning a run captured a partial grid and was recorded as if complete.</fails_when>
    <automated>test "$(/usr/bin/ls -d .screenshots/phase-08-* 2>/dev/null | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 12 && for d in .screenshots/phase-08-*; do test "$(/usr/bin/ls "$d"/*.png 2>/dev/null | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -ge 20 || { echo "THIN CAPTURE: $d"; exit 1; }; done</automated>
    <fails_when>the directory count is not 12, meaning a label was written to the manifest without a capture directory behind it; or "THIN CAPTURE:" prints, meaning a run recorded rows but wrote almost no images — a browser or dev-server failure that the manifest alone would not reveal.</fails_when>
  </verify>
  <done>Twelve of the twenty-one runs are captured against confirmed settings, recorded under distinct labels in this phase's own manifest, with an identical grid in each.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the remaining nine runs (retro, atelier, market), restore defaults, and prove the matrix is complete</name>
  <precondition>Task 1's dev server is still running and still serving; if it is not, restart it the same way before continuing. The appearance category currently holds task 1's last written combination, not the defaults — the restore at the end of this task is what returns it.</precondition>
  <read_first>
    - `.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md` — the coverage grid rows still unfilled, and the label naming already used, so the last nine runs match it exactly.
    - `lib/layout/variants.ts` — the defaults object, so the restore writes the real default members rather than remembered ones.
    - `lib/themes/manifest.generated.ts` — the default theme name, for the same reason.
    - `.planning/phases/07-layout-switches/07-05-SUMMARY.md` — how the previous phase closed its capture session: restore, confirm, stop.
  </read_first>
  <files>.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md</files>
  <action>
Continue the loop for the remaining three presets in coverage-grid order, each across combinations A,
B and C, using the identical per-run sequence: write all four keys in one request, read the category
back and confirm, confirm the storefront's rendered theme attribute, then capture with the unique
label, the explicit manifest path and both flags. Fill each coverage-grid row's result columns as its
run completes. Strictly one pair at a time.

Then restore. Write the four default appearance values — the manifest default theme and the three
default layout members, read from their own modules rather than typed from memory — in a single
request, read the category back and confirm all four, and confirm the storefront's rendered theme
attribute is the default again. This restore is idempotent: running it twice writes the same four
values and leaves the same state, so it is safe to repeat if anything looks wrong. Then stop the dev
server and confirm nothing is still listening on its port.

Then prove the matrix is complete rather than assuming it. Assert 21 label subsections with 21
distinct names, that every run captured the same number of rows, and that each label has a capture
directory with images in it. Record the real per-run cell count and the real missing count in the
coverage grid's own summary line and in the plan summary — these are measurements, not the numbers
any planning document predicted.

Finally, add a short "capture session closed" note to the record: when it ran, that all four
appearance keys were restored to their defaults and confirmed, and that the dev server was stopped.
Do not write a findings table — that is plan 08-04's work and it needs inspection behind it.
  </action>
  <acceptance_criteria>
    - Twenty-one label subsections exist with twenty-one distinct names covering all seven presets across all three combinations.
    - Every coverage-grid row has its result columns filled.
    - The four appearance keys read back as their defaults after the restore.
    - The dev server is stopped and its port is free.
    - Every run captured the same number of coverage rows.
    - The recorded per-run cell count and missing count are measured values, stated as such.
    - Phase 5's screenshot manifest is still unmodified.
  </acceptance_criteria>
  <verify>
    <automated>M=.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md; test "$(/usr/bin/grep -c '^## Label: ' "$M")" -eq 21 && test "$(/usr/bin/grep -o '^## Label: `[^`]*`' "$M" | /usr/bin/sort -u | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 21 && RC=0; for t in volt-dark luxe midnight clinical retro atelier market; do for c in a b c; do /usr/bin/grep -qF "phase-08-$c-$t" "$M" || { echo "MISSING RUN: $c/$t"; RC=1; }; done; done; test "$RC" -eq 0 && test -z "$(git diff --name-only -- .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md)"</automated>
    <fails_when>the label count or the unique-label count is not 21; or "MISSING RUN:" names a preset-and-combination pair that was never captured, which is the failure the run-number grid alone would hide; or Phase 5's manifest shows as modified, meaning a run omitted the explicit manifest flag.</fails_when>
    <automated>M=.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md; N=$(/usr/bin/awk '/^\| [a-z-]+ \| (1280|390) \|/ {c++} END {print c+0}' "$M"); echo "rows=$N per-run=$((N/21))"; test $((N % 21)) -eq 0 && test $((N / 21)) -ge 26 && test "$(/usr/bin/ls -d .screenshots/phase-08-* 2>/dev/null | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -eq 21</automated>
    <fails_when>the row total does not divide evenly by 21, meaning one run captured a different grid; or the per-run figure is under 26, meaning at least one run captured a partial grid; or the capture-directory count is not 21, meaning a manifest label has no images behind it.</fails_when>
    <automated>! /usr/bin/nc -z localhost 3000 2>/dev/null && echo "port 3000 free"</automated>
    <fails_when>the command exits non-zero without printing "port 3000 free", meaning something is still listening and the dev server was left running.</fails_when>
  </verify>
  <done>All 21 runs are captured under distinct labels with an identical grid, the storefront is back on its four default appearance values, and the dev server is stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture loop → `POST /api/admin/settings` | The loop writes real settings rows using a development-only bypass of the admin permission check |
| headless browser → storefront routes | The harness renders `checkout`, `account` and `order-status`, which display customer-shaped data |
| harness → committed manifest file | The harness appends to whatever path its manifest flag names, defaulting to an earlier phase's committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Elevation of Privilege | development bypass on `POST /api/admin/settings` | medium | mitigate | The bypass is gated to the development environment inside the auth middleware and is unchanged by this phase; the value is read from that file at run time and never written into a committed script or into the manifest document |
| T-08-10 | Information Disclosure | headless capture of `checkout`, `account`, `order-status` | high | mitigate | The harness refuses any non-localhost base URL unless an override flag is passed; that flag is prohibited by this plan, so captures can only ever contain the local fixture's data. Images stay under the ignored screenshots directory and are never committed |
| T-08-06 | Tampering | `.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md` | medium | mitigate | The manifest flag is passed explicitly on all 21 invocations, and both tasks assert Phase 5's manifest is unmodified in the working tree |
| T-08-11 | Tampering | `admin_settings` appearance rows left non-default | low | mitigate | Task 2 restores all four keys to values read from their own modules, confirms the read-back, and the restore is idempotent so a partial run can be re-closed safely |
| T-08-SC | Tampering | npm installs | low | accept | No plan in this phase runs a package-manager install; RESEARCH.md's Package Legitimacy Audit records zero packages installed, upgraded, or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `08-QA-MATRIX.md` | 08-03, extended by 08-04 | framing, capture method, combination table, 21-run coverage grid, 21 label subsections, session-closed note |
| `.screenshots/phase-08-*` image sets | 08-03 | all 21, uncommitted by design |
| `docs/theming.md` | 08-01, 08-02, 08-04, 08-05 | — |
| `app/api/admin/settings/route.ts` fix and its test | 08-01 | — |
| `.planning/codebase/*` edits and the close-out record | 08-05 | — |

<verification>
- 21 distinct label subsections covering all seven presets across all three combinations.
- Identical cell grid in every run, with the real per-run count recorded as a measurement.
- Every label has a capture directory with images behind it.
- Phase 5's screenshot manifest unmodified.
- Appearance settings restored to defaults and confirmed; dev server stopped.
</verification>

<success_criteria>
- The full preset-by-variant matrix exists as evidence, captured under confirmed settings, in one
  document with stable row identity.
- Nothing was judged yet — this plan produced evidence, and plan 08-04 is where judgement happens.
</success_criteria>

<output>
Create `.planning/phases/08-documentation-visual-qa-close-out/08-03-SUMMARY.md` when done.
</output>
