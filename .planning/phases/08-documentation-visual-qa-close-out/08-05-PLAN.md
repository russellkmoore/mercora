---
phase: 08-documentation-visual-qa-close-out
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - docs/theming.md
  - .planning/STATE.md
autonomous: true
requirements: [DOCS-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The architecture document describes theme resolution and layout resolution as first-class request-time behaviour, not as an unmentioned detail: where each resolver runs, what it reads, and what it falls back to (D-06). [DOCS-03]"
    - "The structure document's directory tree, directory purposes, key file locations and where-to-add-new-code sections all account for the theme directory, the two library directories, the layout components directory and the three theme and QA scripts (D-06). [DOCS-03]"
    - "The conventions document states the token-class convention: the storefront uses token classes and no hardcoded palette, the admin is excluded by path and keeps its own palette, and a polarity-neutral literal gets the scanner's sentinel pair with a written reason rather than a blanket exception (D-06). [DOCS-03]"
    - "The testing document names the two gates and the screenshot harness, and its unit-test count is the number a real run reports at the time of the edit rather than a number carried forward (D-06). [DOCS-03]"
    - "The three codebase documents the phase boundary excludes — the stack, integrations and concerns documents — are left alone unless a claim in one is actually false; research found none, so the default is no edit (D-06). [DOCS-03]"
    - "The refresh is a targeted edit into named sections, not a regenerated document: no section is rewritten wholesale and no full remap is run (D-06, CONTEXT Deferred Ideas). [DOCS-03]"
    - "The close-out record exists in both places D-08 names: the theming document's known-limits section and the project state's blockers and concerns (D-08). [DOCS-03]"
    - "Every close-out item D-08 lists is present with where it is tracked: the deploy-time theme default still needing a Workers Build variable, the un-run real-browser admin walkthrough, the deferred direction-document properties, the two image-URL resolvers to consolidate, and the parity tests that regenerate a deleted snapshot (D-08). [DOCS-03]"
    - "Any finding plan 08-04 recorded as unresolved is carried into the same close-out record rather than left only in the QA document (D-08)."
    - "The three phase requirements are marked ready through the repository's own tooling, not by hand-editing the requirements document. [DOCS-01, DOCS-02, DOCS-03]"
    - "The whole gate suite passes at the end of the phase: linter, typechecker, token scanner at zero violations, full test suite, theme-manifest freshness, and a real production build."
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Theme and layout resolution described in the layers, abstractions and responsibilities sections"
      contains: "getActiveTheme"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "The theme and layout directories and the three scripts, in the tree and in the prose sections"
      contains: "build-themes.mjs"
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "The token-class convention and the scanner's exception rules"
      contains: "scan:tokens"
    - path: ".planning/codebase/TESTING.md"
      provides: "The two gates, the screenshot harness, and a current unit-test count"
      contains: "build:themes:check"
    - path: "docs/theming.md"
      provides: "A known-limits section carrying every close-out item and any unresolved QA finding"
    - path: ".planning/STATE.md"
      provides: "Blockers and concerns updated with the milestone close-out items"
  key_links:
    - from: ".planning/codebase/ARCHITECTURE.md"
      to: "lib/themes/active-theme.ts"
      via: "the documented resolution chain names the resolver that implements it"
      pattern: "getActiveTheme"
    - from: ".planning/codebase/CONVENTIONS.md"
      to: "scripts/scan-hardcoded-colors.mjs"
      via: "the documented convention names the scanner that enforces it and its sentinel"
      pattern: "scan-hardcoded-colors"
    - from: "docs/theming.md"
      to: ".planning/todos/pending/theme-direction-doc-backlog-06.1.md"
      via: "the known-limits row for deferred direction-document properties points at the backlog note"
      pattern: "theme-direction-doc-backlog"
  prohibitions:
    - statement: "This phase must NOT run a full codebase remap."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from CONTEXT Deferred Ideas and D-06's explicit no-full-remap instruction; binds hardest here, because this is the plan that edits the mapped documents and a regeneration would look like thoroughness."
    - statement: "The stack, integrations and concerns documents must NOT be edited unless a claim in one is actually false."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from D-06's own scope sentence; research found no false claim in any of the three, so the expected outcome is no edit."
    - statement: "This phase must NOT change any theme file, any layout variant component, any contract token, or the theme/layout resolution mechanism."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition from CONTEXT's phase boundary; carried on every plan in the phase and asserted here one last time at the phase gate."
    - statement: "Documentation must NOT print a secret value or the development bypass header value."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less security prohibition from the phase's security contribution; asserted here at the phase gate over the finished theming document."
---

<objective>
Refresh the four mapped codebase documents that describe a storefront the token sweep and the theme
mechanism made them wrong about, then close the milestone: record every carry-over where D-08 says
it goes, mark the three requirements ready, and run the whole gate suite.

Purpose: DOCS-03 in full, plus the close-out this phase owns as the last phase of the milestone.

Output: targeted edits to four codebase documents, a complete known-limits record in the theming
document, updated project state, three requirements marked ready, and a green gate suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-documentation-visual-qa-close-out/08-CONTEXT.md
@.planning/phases/08-documentation-visual-qa-close-out/08-PATTERNS.md
@.planning/phases/08-documentation-visual-qa-close-out/08-04-SUMMARY.md
</context>

<interfaces>
Consumed from plans 08-01 through 08-04, unchanged: the finished `docs/theming.md`, the finished
`08-QA-MATRIX.md`, and the settings-GET fix and its test.

Nothing here exports code. The four codebase documents are edited in place at named sections; the
theming document gains rows in its existing known-limits section and is not otherwise touched.

Verified insertion points, read this session — confirm each before editing, since a prior task in
this phase may have shifted a line number:

- Architecture document: the layers section, the key abstractions section, the component
  responsibilities section.
- Structure document: the directory layout tree, the directory purposes section, the key file
  locations section, the where-to-add-new-code section. Its scripts entry currently names only the
  Shopify migration directory.
- Conventions document: the constants and configuration section, the last section in the file.
- Testing document: the test file organization section, whose unit-test count line is stale.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use `git commit -F`.
- The four codebase documents currently have zero real mentions of the theme or layout system. This
  is a gap, not a stale claim — the edits are additions, with one exception: the testing document's
  unit-test count is genuinely stale and gets corrected.
- Do not regenerate any of these documents. Splice into the named sections and leave everything else
  byte-identical.
- Use the repository's own tooling for the broken-windows ledger and for marking requirements ready.
  Hand-editing either file is how their counters drift.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Add theme and layout resolution to the architecture document and the theme paths to the structure document</name>
  <read_first>
    - `.planning/codebase/ARCHITECTURE.md` — the layers section, the key abstractions section and the component responsibilities section, plus enough of the surrounding prose to match the document's entry style (purpose, location, contains, depends on, used by).
    - `.planning/codebase/STRUCTURE.md` — the directory layout tree with its exact glyph style, the scripts entry, the directory purposes section's entry format, the key file locations section, and the where-to-add-new-code section's numbered-recipe format.
    - `lib/themes/active-theme.ts` and `lib/layout/settings.ts` — both resolvers, so the architecture entry describes what they actually do, including that they issue two independent reads of the same settings category by deliberate design.
    - `lib/themes/manifest.generated.ts` and `themes/index.generated.css` — enough to say correctly which files are generated and by what.
    - `docs/theming.md` — the finished document, so the codebase documents point at it rather than restating it.
  </read_first>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md</files>
  <action>
Architecture document, three targeted additions.

In the layers section, add an entry for the theme and layout resolution layer in the document's own
entry format: it runs server-side and blocking inside the root layout and the page components, it
reads one settings category from the database, it resolves through a three-tier fallback per value,
and it never caches. Name both resolver modules. Say the resolved theme name is stamped as a data
attribute on the root element, and that the same values are also handed to the four consumers that
cannot read a CSS custom property.

In the key abstractions section, add two entries in the document's existing shape. One for the token
contract: a frozen set of custom properties that every storefront surface reads through utility
classes, with a build-time validator and a generated manifest, and a pointer to the theming document
for the full table. One for the layout variants: three enumerated switches, each resolved against its
own member array rather than by key lookup, each mapping to a named component.

In the component responsibilities section, add the admin appearance surface — the two islands and
what each writes — and the storefront variant components directory.

Structure document, four targeted additions.

In the directory layout tree, add the theme directory with a note that its barrel file is generated,
the themes library directory with its three modules and which two are generated, the layout library
directory with its two modules, and the layout components directory. Add the two admin appearance
components under the existing admin components entry. Add the three theme and QA scripts under the
scripts entry, which today names only the Shopify migration directory. Match the tree's existing
glyphs and trailing-comment alignment exactly; do not redraw the tree.

In the directory purposes section, add entries for the theme directory and the two library
directories in the document's existing purpose-contains-key-files format.

In the key file locations section, add the resolver modules, the variant enums module and the three
scripts.

In the where-to-add-new-code section, add a short numbered recipe for adding a theme that mirrors the
five-step recipe in the theming document rather than inventing a different one, and points at that
document as the full version.

Everything else in both documents stays byte-identical.
  </action>
  <acceptance_criteria>
    - The architecture document names both resolvers, states the three-tier fallback, and states that nothing is cached.
    - The architecture document has entries for the token contract and the layout variants in its abstractions section.
    - The structure tree contains the theme directory, both library directories, the layout components directory, both admin appearance components and all three scripts.
    - The structure document's purposes, key file locations and where-to-add-new-code sections each gained their additions.
    - Both documents point at the theming document rather than restating its tables.
    - No section outside the named insertion points changed.
  </acceptance_criteria>
  <verify>
    <automated>A=.planning/codebase/ARCHITECTURE.md; S=.planning/codebase/STRUCTURE.md; RC=0; for s in 'getActiveTheme' 'getLayoutSettings' 'data-theme' 'docs/theming.md'; do /usr/bin/grep -qF "$s" "$A" || { echo "ARCH MISSING: $s"; RC=1; }; done; for s in 'themes/' 'lib/themes/' 'lib/layout/' 'components/layout/' 'ThemePresetGrid.tsx' 'LayoutSwitches.tsx' 'build-themes.mjs' 'scan-hardcoded-colors.mjs' 'screenshot-routes.mjs' 'docs/theming.md'; do /usr/bin/grep -qF "$s" "$S" || { echo "STRUCT MISSING: $s"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>"ARCH MISSING:" prints, meaning a resolver, the data attribute or the pointer to the theming document never made it into the architecture document; or "STRUCT MISSING:" prints, meaning a directory, component or script is still absent from the structure document — which is the exact gap DOCS-03 exists to close.</fails_when>
    <automated>test -z "$(git diff --name-only HEAD -- .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md)" && test -z "$(/usr/bin/grep -lE 'getActiveTheme|data-theme|--store-' .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md 2>/dev/null)" && test "$(git diff --numstat HEAD -- .planning/codebase/ARCHITECTURE.md | /usr/bin/awk '{print $2+0}')" -lt 40</automated>
    <fails_when>the first check prints a path, meaning one of the three out-of-scope codebase documents was edited despite no false claim being found in it; or the deleted-line count on the architecture document is 40 or more, meaning the edit rewrote sections rather than splicing into them.</fails_when>
  </verify>
  <done>The architecture and structure documents describe the theme and layout system a reader would otherwise have to discover from source, added by splice and not by regeneration.</done>
</task>

<task type="auto">
  <name>Task 2: Record the token-class convention and refresh the testing document's gates and counts</name>
  <read_first>
    - `.planning/codebase/CONVENTIONS.md` — the constants and configuration section and the document's entry style (a bolded pattern label, bullets, then a short code example).
    - `.planning/codebase/TESTING.md` — the test framework section, the test file organization section with its counts, and the continuous-integration line that lists what CI runs.
    - `scripts/scan-hardcoded-colors.mjs` lines 1-120 — the excluded paths, the named manual-review registry and its printed reasons, and the exact sentinel comment pair, so the convention is described in the scanner's own terms.
    - `scripts/screenshot-routes.mjs` lines 1-60 — the flags, so the harness is described accurately.
    - `.github/workflows/ci.yml` — which of the two gates actually runs there.
    - `.planning/phases/07-layout-switches/07-05-SUMMARY.md` — the previously reported suite size, for context only; the number written into the document must come from a run performed during this task.
  </read_first>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <action>
Conventions document: add a styling and colour-token section in the document's own entry style. State
the rule as a rule. The storefront tree uses token utility classes and carries no hardcoded palette
value. The admin tree is excluded from the scanner by path and deliberately keeps its own fixed
palette; that is a decision, not an oversight, and the section should say so. A colour literal that is
genuinely polarity-neutral — a scrim that must read dark under a light preset as well as a dark one —
is wrapped in the scanner's sentinel comment pair with a written reason at the site, never added as a
blanket exception. A whole file may be excluded only by being named in the scanner's manual-review
registry, which prints its reason on every run. Name the scanner and its command, and point at the
theming document for the token table itself.

Testing document: three targeted edits. Add the two gates to whatever section lists what runs — the
theme-manifest validator, which runs in CI under its real step name, and the token scanner, which does
not run in CI and is a local gate run by convention. Be explicit about that difference; a testing
document that implies both are enforced automatically is worse than one that mentions neither. Add the
screenshot harness as a manual capture tool with its command and its required label and manifest
flags, and say its captures live in an ignored directory and are never committed.

Then fix the stale unit-test count. Run the full suite during this task, read the real file and test
counts out of the runner's own summary, and write those numbers. Do not carry forward a number from a
prior phase's summary — it was already stale before this phase added tests, and it will be stale again
the moment it is copied rather than measured.

Leave the stack, integrations and concerns documents alone. Research found no false claim in any of
them, and the phase boundary permits editing them only to fix one.
  </action>
  <acceptance_criteria>
    - The conventions document has a styling and colour-token section naming the scanner, its command, the admin exclusion, the sentinel pair and the manual-review registry.
    - The testing document names both gates and states which one runs in CI and which does not.
    - The testing document describes the screenshot harness with its required flags.
    - The unit-test count in the testing document matches a run performed during this task.
    - The stack, integrations and concerns documents are unmodified.
  </acceptance_criteria>
  <verify>
    <automated>C=.planning/codebase/CONVENTIONS.md; T=.planning/codebase/TESTING.md; RC=0; for s in 'scan:tokens' 'scan-hardcoded-colors' 'docs/theming.md'; do /usr/bin/grep -qF "$s" "$C" || { echo "CONV MISSING: $s"; RC=1; }; done; for s in 'build:themes:check' 'scan:tokens' 'screenshot:routes'; do /usr/bin/grep -qF "$s" "$T" || { echo "TESTING MISSING: $s"; RC=1; }; done; test "$RC" -eq 0 && ! /usr/bin/grep -q '233 files / 1701 tests' "$T"</automated>
    <fails_when>"CONV MISSING:" or "TESTING MISSING:" prints, meaning a gate, the scanner or the harness never made it into the document; or the final negative grep fails, meaning the stale unit-test count line survived the edit.</fails_when>
    <automated>test -z "$(git diff --name-only HEAD -- .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md)" && test -z "$(/usr/bin/grep -lE 'getActiveTheme|data-theme|--store-' .planning/codebase/STACK.md .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md 2>/dev/null)" && COUNT=$(/usr/bin/grep -oE '[0-9]+ files / [0-9]+ tests' .planning/codebase/TESTING.md | /usr/bin/head -1) && test -n "$COUNT" && echo "recorded: $COUNT"</automated>
    <fails_when>the git check prints a path, meaning an out-of-scope codebase document was edited; or `test -n` fails because no counts line was found at all, meaning the refresh deleted the line instead of updating it.</fails_when>
  </verify>
  <done>The conventions document states the token-class rule the whole sweep enforced, and the testing document names both gates honestly and carries a count someone actually measured.</done>
</task>

<task type="auto">
  <name>Task 3: Close the milestone — known limits, project state, requirements, and the full gate suite</name>
  <read_first>
    - `docs/theming.md` — the known-limits section written by plan 08-02, so this task verifies and appends rather than rewrites.
    - `.planning/phases/08-documentation-visual-qa-close-out/08-QA-MATRIX.md` — the close-out rollup and any unresolved findings recorded by plan 08-04.
    - `.planning/STATE.md` — the blockers and concerns section and its existing carried-out-of-phase grouping style.
    - `.planning/WINDOWS.md` — the ledger after plan 08-04 marked the settings item fixed, so the state record agrees with it.
    - `.planning/todos/pending/theme-direction-doc-backlog-06.1.md` and `.planning/todos/pending/theme-contract-dropped-properties.md` — the two backlog notes the deferred-properties row points at.
  </read_first>
  <files>docs/theming.md, .planning/STATE.md</files>
  <action>
First, verify the known-limits section rather than assuming it. Check it carries all five items D-08
names — the deploy-time theme default still needing a Cloudflare Workers Build variable, the real
browser walkthrough of the admin appearance page still un-run, the deferred direction-document
properties with both backlog notes cited, the two image-URL resolvers to consolidate, and the parity
tests that regenerate a deleted baseline snapshot — plus the token scanner not running in CI. Add
anything missing. Then append any finding plan 08-04 recorded as unresolved, each with its own row and
its own reason. If plan 08-04 recorded none, say so explicitly in a sentence — an absent statement
reads as an omission, a present one reads as a result.

Second, update the project state's blockers and concerns with the same items, grouped as carried out
of Phase 8 in the file's existing style, and note that the settings seeding item is now closed by this
phase's own fix. Do not delete the earlier phases' carried items; this is an addition and one status
change.

Third, mark the three phase requirements ready through the repository's tooling, pointing it at this
plan. Do not hand-edit the requirements document.

Fourth, run the whole gate suite in one pass: linter, typechecker, token scanner, full test suite, and
a real production build, plus the theme-manifest freshness check. This is the last plan of the last
phase of the milestone, so this is the run that has to be green — not a spot check. Record the real
numbers in the summary: violation count, manual-review count, test totals, and whether the build
succeeded.

If any gate fails, fix the cause and re-run rather than recording a partial pass. A close-out that
reports a red gate as acceptable is the failure this step exists to prevent.
  </action>
  <acceptance_criteria>
    - The theming document's known-limits section carries all six close-out items, each pointing at where it is tracked.
    - Any unresolved QA finding appears in the known-limits section, or the section states in a sentence that there were none.
    - The project state's blockers and concerns carry the same items, with the settings seeding item marked closed by this phase.
    - The three requirements are marked ready through the tooling, with this plan as the source.
    - Linter, typechecker, token scanner, full test suite, theme-manifest freshness check and production build all pass in one run.
    - No theme file, contract token or variant enum changed anywhere in the phase.
  </acceptance_criteria>
  <verify>
    <automated>RC=0; for s in 'NEXT_PUBLIC_THEME_DEFAULT' 'theme-direction-doc-backlog-06.1.md' 'theme-contract-dropped-properties.md' 'scan:tokens'; do /usr/bin/grep -qF "$s" docs/theming.md || { echo "LIMITS MISSING: $s"; RC=1; }; done; for s in 'NEXT_PUBLIC_THEME_DEFAULT' 'Phase 8'; do /usr/bin/grep -qF "$s" .planning/STATE.md || { echo "STATE MISSING: $s"; RC=1; }; done; test "$RC" -eq 0 && test -f .planning/todos/pending/theme-direction-doc-backlog-06.1.md && test -f .planning/todos/pending/theme-contract-dropped-properties.md</automated>
    <fails_when>"LIMITS MISSING:" prints, meaning a close-out item D-08 named is absent from the known-limits section; or "STATE MISSING:" prints, meaning the project state never recorded the phase's carry-overs; or a `test -f` fails, meaning a cited backlog note does not exist and the row points nowhere.</fails_when>
    <automated>RC=0; for f in themes/volt-dark.css themes/luxe.css themes/midnight.css themes/clinical.css themes/retro.css themes/atelier.css themes/market.css; do N=$(/usr/bin/grep -c -- '--store-' "$f"); test "$N" -eq 23 || { echo "TOKEN COUNT $f = $N"; RC=1; }; done; for m in '"grid-3", "grid-2", "list"' '"full-bleed", "split", "minimal"' '"left", "top"'; do /usr/bin/grep -qF "$m" lib/layout/variants.ts || { echo "ENUM DRIFT: $m"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>"TOKEN COUNT" prints for any theme file whose declaration count is not 23, meaning the frozen contract gained or lost a member; or "ENUM DRIFT:" prints, meaning one of the three variant member lists changed — either is the contract change the phase boundary forbids, caught by content rather than by a commit-range guess.</fails_when>
    <automated>mise exec -- npm run lint && mise exec -- npm run typecheck && SCAN="$(mktemp)" && mise exec -- npm run scan:tokens >| "$SCAN" 2>&1 && /usr/bin/grep -q '0 violations' "$SCAN" && test "$(/usr/bin/grep -c '^MANUAL-REVIEW' "$SCAN")" -eq 2 && mise exec -- npm run build:themes:check && mise exec -- npm test && mise exec -- npm run build</automated>
    <fails_when>any command exits non-zero; or the scanner output has no zero-violation line, or its manual-review row count is not 2; or the theme-manifest check reports stale generated files; or the vitest summary reports a failed file; or the build stops with a compile or type error.</fails_when>
  </verify>
  <done>The milestone's carry-overs are recorded in both places D-08 names, the three requirements are marked ready through the tooling, and every gate is green on one run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → future planners | The mapped codebase documents are what a future phase's planner reads instead of the source |
| close-out record → milestone completion | An item omitted here is an item nobody sees again |
| repository tooling → requirements and ledger state | Hand edits to generated counters silently desynchronise them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-15 | Repudiation | `.planning/codebase/TESTING.md` gate description | high | mitigate | The document must state which gate runs in CI and which does not; the verify asserts both gate names are present and that the stale count line is gone, so nobody can later claim the scanner was believed enforced |
| T-08-16 | Tampering | the four mapped codebase documents | medium | mitigate | Edits are splices into named sections; the verify asserts the three out-of-scope documents are unmodified and caps deleted lines on the architecture document, so a regeneration fails the gate |
| T-08-17 | Repudiation | close-out record | high | mitigate | Every D-08 item is asserted present in the theming document and in the project state, and every cited backlog note is asserted to exist; unresolved QA findings must be carried over or their absence stated in a sentence |
| T-08-18 | Tampering | requirements and broken-windows counters | medium | mitigate | Both are changed through the repository's own tooling rather than by editing the files; plan 08-04 asserts the ledger counter, this plan uses the tooling for the requirement IDs |
| T-08-03 | Information Disclosure | `docs/theming.md` | medium | mitigate | No credential is added by this plan; the phase-gate run re-checks the finished document through the same extraction-based assertion plans 08-01 and 08-02 used |
| T-08-SC | Tampering | npm installs | low | accept | No plan in this phase runs a package-manager install; RESEARCH.md's Package Legitimacy Audit records zero packages installed, upgraded, or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `.planning/codebase/ARCHITECTURE.md` edits | 08-05 | layers, abstractions, responsibilities |
| `.planning/codebase/STRUCTURE.md` edits | 08-05 | tree, purposes, key file locations, where-to-add |
| `.planning/codebase/CONVENTIONS.md` edits | 08-05 | the token-class convention |
| `.planning/codebase/TESTING.md` edits | 08-05 | both gates, the harness, a measured test count |
| `docs/theming.md` known limits | 08-02, verified and extended by 08-05 | verification and any unresolved QA finding |
| `.planning/STATE.md` blockers and concerns | 08-05 | the phase's carry-overs and one status change |
| `08-QA-MATRIX.md`, the settings fix, the theming document body | 08-01 to 08-04 | — |

<verification>
- All four mapped codebase documents name the theme and layout system, its scripts and its gates.
- The three out-of-scope codebase documents are unmodified.
- The testing document's unit-test count comes from a run performed in this plan.
- The known-limits section and the project state both carry every D-08 item, each pointing somewhere real.
- The three requirements are marked ready through the tooling.
- Linter, typechecker, token scanner at zero violations with two manual-review rows, theme-manifest freshness, full test suite and production build all green in one run.
</verification>

<success_criteria>
- A future planner reading `.planning/codebase/` learns the theming system exists and where it lives, instead of rediscovering it from source.
- Nothing this milestone knowingly left open is left only in someone's memory.
- The milestone closes on a green gate suite, with the numbers recorded rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/08-documentation-visual-qa-close-out/08-05-SUMMARY.md` when done.
</output>
