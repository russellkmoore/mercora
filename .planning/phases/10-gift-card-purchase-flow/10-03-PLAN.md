---
phase: 10-gift-card-purchase-flow
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - components/checkout/ProgressBar.tsx
  - components/checkout/ShippingForm.tsx
  - tests/unit/components/checkout-step-props-source.test.ts
autonomous: true
requirements: [SHOP-06]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ProgressBar accepts an optional steps array and defaults to the exact four labels it hardcodes today, so every existing caller that passes only a step index renders byte-identically (D-02, SHOP-06)."
    - "The default four labels are Shipping Address, Shipping Method, Payment Information and Order Submitted, in that order, unchanged from the pre-phase file (D-02)."
    - "ProgressBar's fill, completed and current logic is already generic over the label count, so accepting the array is the whole change — no other line inside the component moves (D-02)."
    - "The bar renders either exactly three or exactly four labels and no other cardinality, because the only two arrays that reach it are the default and the digital-only one (UI-SPEC zero-one-many row)."
    - "Labels are fixed strings from the Copywriting Contract and never shopper text, so no label can overflow from user input (UI-SPEC long-text row)."
    - "Labels are a pure function of the array passed in; a step index outside the array's range fills nothing extra rather than throwing, so an unexpected index degrades quietly (UI-SPEC error row)."
    - "The bar has no async source and no loading state; it derives from props on render (UI-SPEC loading row)."
    - "ShippingForm accepts an optional heading defaulting to Shipping Address and an optional helperText that renders no paragraph at all when absent, so the physical checkout flow is byte-identical to today (D-01)."
    - "ShippingForm's isSubmitDisabled and its seven required fields are unchanged: line1, city, region, postal code, country, recipient and email all stay required for both the physical and the digital-only case, because Stripe Tax and the payment-intent route need a real address either way (D-01, RESEARCH Pitfall 2)."
    - "Every field, its order, every placeholder, the country Select's three options and the Use Address button are unchanged; only the two new strings can differ between the physical and digital-only renders (D-01, UI-SPEC copy row)."
    - "Long address input is already bounded by the server's own bounded-string limits on line1, city, region and postal code, and this plan adds no client-side length rule of its own (UI-SPEC long-text row)."
    - "The helper paragraph, when present, renders under the heading with text-sm text-muted-foreground mb-4 — the same secondary-text treatment used elsewhere in checkout (UI-SPEC color and typography rows)."
    - "Neither component learns anything about gift cards: both take strings and arrays, and the decision about which to pass lives in the caller (D-02)."
    - statement: "At 360px the three-label bar keeps Billing details on one line; if it wraps, the label shortens to Billing."
      verification: backstop
  artifacts:
    - "components/checkout/ProgressBar.tsx — new module-level const DEFAULT_STEPS"
    - "components/checkout/ProgressBar.tsx — new optional prop steps?: string[]"
    - "components/checkout/ShippingForm.tsx — new optional prop heading?: string"
    - "components/checkout/ShippingForm.tsx — new optional prop helperText?: string"
    - "tests/unit/components/checkout-step-props-source.test.ts — new file"
  key_links:
    - "ProgressBar's default steps array is the single place the four physical labels live; the caller in plan 10-05 passes the three-label array only for a digital-only cart."
    - "ShippingForm's heading default keeps every existing call site correct without editing it; only plan 10-05 passes the two new props."
    - "Both props are optional and additive, so this plan can land and ship green before any caller uses them."
  prohibitions:
    - statement: "ShippingForm's required-field gating is not relaxed for any case."
      status: enforced
      verification: "git diff -- components/checkout/ShippingForm.tsx shows no change inside the isSubmitDisabled expression"
    - statement: "No file under lib/gift-cards/ is edited by this plan."
      status: enforced
      verification: "git diff --stat -- lib/gift-cards is empty"
    - statement: "No change to lib/stores/cart-store.ts, app/api/, lib/services/ or the Stripe webhook."
      status: enforced
      verification: "git diff --stat -- lib app/api is empty"
    - statement: "No migration file is added or edited."
      status: enforced
      verification: "git status --porcelain -- migrations/ is empty"
    - statement: "No secret value is written into any committed file."
      status: enforced
      verification: "the plan's diff touches only two component files and one test file"
    - statement: "No hex colour, rgb() call or hardcoded palette value in either edited file."
      status: enforced
      verification: "mise exec -- npm run scan:tokens reports zero violations"
    - statement: "No deploy script is run from this machine."
      status: enforced
      verification: "no npm run deploy, deploy:ci, predeploy or wrangler deploy appears in any task command"
    - statement: "No blanket git add; every commit stages the exact paths it changes."
      status: enforced
      verification: "each task's commit names its files explicitly"
    - statement: "No new npm dependency is installed."
      status: enforced
      verification: "git diff --stat -- package.json package-lock.json is empty"
    - statement: "Neither component branches on gift cards, product type, a product id or a URL segment."
      status: enforced
      verification: "grep -c 'giftCard' over both files returns 0"
---

<objective>
Give the two leaf checkout components the optional props the digital-only flow needs, without changing what either renders for a physical cart.

Purpose: plan 10-05 rewires the checkout step machine. Landing the prop surface first means that plan is a pure caller change, and it means the "physical checkout is byte-identical" claim gets tested here, on its own, before the step machine moves.

Output: an optional steps array on ProgressBar with the existing four labels as its default; optional heading and helperText strings on ShippingForm; one new source-contract test proving the defaults and proving the required-field gating did not move.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gift-card-purchase-flow/10-CONTEXT.md
@.planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md
@.planning/phases/10-gift-card-purchase-flow/10-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProgressBar accepts an optional label array</name>
  <files>components/checkout/ProgressBar.tsx, tests/unit/components/checkout-step-props-source.test.ts</files>
  <read_first>
    - components/checkout/ProgressBar.tsx (the whole 73-line file — the hardcoded local array at lines 7 to 12, the fillWidths effect at 16 to 24, the isCompleted and isCurrent derivation at 29 and 30)
    - components/checkout/CheckoutClient.tsx line 267 (the only current call site, read-only in this plan)
    - .planning/phases/10-gift-card-purchase-flow/10-PATTERNS.md (the ProgressBar section, which notes this prop does not exist today and must be added net-new)
    - .planning/phases/10-gift-card-purchase-flow/10-RESEARCH.md (Pitfall 4 — the index math the caller must shift alongside the array)
    - .planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md (the Copywriting Contract's digital-only step-label row)
    - tests/unit/components/cart-line-source.test.ts (the source-contract test style)
  </read_first>
  <behavior>
    - The new test asserts ProgressBar declares a module-level default label array holding the four existing strings in their existing order
    - It asserts the component signature accepts an optional steps array alongside the required numeric step
    - It asserts the default is applied in the parameter destructuring, so a caller passing only step gets the four labels
    - It asserts the fillWidths initialiser and the map both read the resolved array rather than a local literal
    - It asserts the component still imports CheckCircle2 from lucide-react and still contains its existing primary and border token classes
  </behavior>
  <action>
Lift ProgressBar's hardcoded local array out of the function body into a module-level const named DEFAULT_STEPS holding the same four strings in the same order, then change the component's parameter to accept an object of a required numeric step and an optional string array named steps, defaulting steps to DEFAULT_STEPS in the destructuring.

Everything else stays. The useState initialiser that maps the array to zeros, the useEffect that fills by index, the isCompleted and isCurrent comparisons, the CheckCircle2 render, every className, and the label span all already read from the local identifier and are already generic over its length — they need no edit beyond now resolving to the prop. Do not add a length assertion, a clamp, or a fallback branch: an index outside the array's range simply fills nothing extra, which is the quiet degradation the contract wants.

Do not touch the call site in CheckoutClient.tsx in this plan. With the default in place that call site is unchanged and still correct.

Create tests/unit/components/checkout-step-props-source.test.ts in the readFileSync source-contract style, covering every item in this task's behavior block against ProgressBar.tsx.
  </action>
  <acceptance_criteria>
    - `grep -c "DEFAULT_STEPS" components/checkout/ProgressBar.tsx` returns at least 2 — the declaration and the default
    - `grep -c "steps = DEFAULT_STEPS" components/checkout/ProgressBar.tsx` returns 1
    - `grep -c "Shipping Address" components/checkout/ProgressBar.tsx` returns 1 and the same file still contains Shipping Method, Payment Information and Order Submitted
    - `grep -c "giftCard" components/checkout/ProgressBar.tsx` returns 0
    - `git diff -- components/checkout/ProgressBar.tsx > /tmp/gsd-10-03-bar.diff` exits 0, then `grep -c "^[-+].*fillWidths" /tmp/gsd-10-03-bar.diff` returns 0 — the fill logic is untouched
    - `mise exec -- npm run typecheck` exits 0 with the existing CheckoutClient call site unchanged
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/checkout-step-props-source.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or the default-array assertion finds fewer than four labels</fails_when>
    <automated>mise exec -- npm run typecheck &amp;&amp; mise exec -- npm run lint</automated>
    <fails_when>non-zero exit from either, or a TS error at the CheckoutClient ProgressBar call site, which would mean the default was not applied</fails_when>
  </verify>
  <done>ProgressBar takes an optional steps array defaulting to the four existing labels, its existing caller compiles untouched, and the new source-contract test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ShippingForm accepts an optional heading and helper line</name>
  <files>components/checkout/ShippingForm.tsx, tests/unit/components/checkout-step-props-source.test.ts</files>
  <read_first>
    - components/checkout/ShippingForm.tsx (the whole 151-line file — the Props interface at 14 to 21, the isSubmitDisabled expression at 31 to 41, the heading at line 49, the seven Inputs, the country Select and the Use Address button)
    - app/api/payment-intent/route.ts (read-only — normalizeAddress and the fields it requires unconditionally; this is why the required-field set must not be relaxed)
    - .planning/phases/10-gift-card-purchase-flow/10-CONTEXT.md (D-01 in full)
    - .planning/phases/10-gift-card-purchase-flow/10-RESEARCH.md (Pitfall 2)
    - .planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md (Component Anatomy, Billing details step; the Copywriting Contract's Billing details rows)
    - tests/unit/components/checkout-step-props-source.test.ts as it stands after Task 1
  </read_first>
  <behavior>
    - The test asserts the Props interface declares an optional heading string and an optional helperText string
    - It asserts the heading falls back to the exact string Shipping Address when no heading is passed
    - It asserts the helper paragraph is rendered inside a truthiness guard, so nothing at all renders when helperText is absent
    - It asserts the helper paragraph carries text-sm, text-muted-foreground and mb-4
    - It asserts all seven required-field names still appear inside the submit-disabled expression
    - It asserts the form still contains the Use Address button text and the three country option values
  </behavior>
  <action>
Add two optional string members to ShippingForm's Props interface, heading and helperText, and destructure both in the component signature. Replace the literal text inside the existing h2 with the heading value falling back to the exact string it holds today, keeping the h2's className unchanged. Immediately after the h2, render a paragraph guarded on helperText being truthy, carrying text-sm text-muted-foreground mb-4 and holding that string; render nothing when the prop is absent.

Change nothing else. The submit-disabled expression keeps all seven of its conjuncts — a form labelled for billing collects the same address as one labelled for shipping, because the payment-intent route's address normaliser and Stripe Tax both need every one of those fields whether or not anything physically ships. Every Input, its name, its placeholder, its autoComplete value and its ordering stay as they are; the country Select keeps its three options; the Use Address button keeps its text, its classes and its disabled binding; the form-level error div stays where it is.

Extend tests/unit/components/checkout-step-props-source.test.ts with a describe block for ShippingForm covering every item in this task's behavior block.
  </action>
  <acceptance_criteria>
    - `grep -c "heading?: string" components/checkout/ShippingForm.tsx` returns 1
    - `grep -c "helperText?: string" components/checkout/ShippingForm.tsx` returns 1
    - `grep -c "Shipping Address" components/checkout/ShippingForm.tsx` returns 1 — the fallback keeps the original string in the file exactly once
    - `grep -c "text-muted-foreground" components/checkout/ShippingForm.tsx` returns 1 — the one new helper paragraph
    - `grep -cE "^ +address\.[a-z_]+ &&$" components/checkout/ShippingForm.tsx` returns 6, and `grep -c "address.country" components/checkout/ShippingForm.tsx` returns 1 — all seven conjuncts of the submit-disabled expression survive untouched
    - `grep -c "Use Address" components/checkout/ShippingForm.tsx` returns 1
    - `grep -c "giftCard" components/checkout/ShippingForm.tsx` returns 0
    - `mise exec -- npm run scan:tokens` reports zero violations
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/checkout-step-props-source.test.ts tests/unit/app/checkout-recovery-source.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or the seven-required-fields assertion finds a missing conjunct</fails_when>
    <automated>mise exec -- npm run typecheck &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run scan:tokens</automated>
    <fails_when>non-zero exit from any of the three, or scan:tokens prints a violation count above 0</fails_when>
    <automated>mise exec -- npm test</automated>
    <fails_when>non-zero exit, or any previously passing checkout or cart suite reports a new failure</fails_when>
  </verify>
  <done>ShippingForm takes optional heading and helperText props, defaults the heading to its original string, renders no helper paragraph when the prop is absent, keeps all seven required fields, and the full unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkout client state → ShippingForm inputs | the address a shopper types enters here and is later posted to the payment-intent route |
| ShippingForm submit gate → POST /api/payment-intent | the client-side required-field gate sits in front of the server's address normaliser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | relaxing the address gate for a digital-only cart | medium | mitigate | The submit-disabled expression is unchanged and an acceptance criterion asserts its diff is empty. A digital-only checkout collects the same real, shopper-entered address the physical flow does; no substitute or filler value is introduced anywhere in this plan. |
| T-10-09 | Spoofing | a relabelled heading misleading a shopper about what is collected | low | mitigate | The helper line states plainly that nothing ships and why the address is needed, taken verbatim from the approved Copywriting Contract rather than invented at implementation time. |
| T-10-10 | Denial of Service | a caller passing an out-of-range step index to ProgressBar | low | accept | The bar fills nothing extra and throws nothing; a wrong index is a cosmetic defect on a purely presentational component with no data or money consequence. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; RESEARCH.md's Package Legitimacy Audit records zero new packages, and an empty package.json / package-lock.json diff is a plan prohibition. |
</threat_model>

<artifacts_this_phase_produces>
This plan's contribution to the phase's artifact set:

| Kind | Name | Location |
|---|---|---|
| module const | `DEFAULT_STEPS` (the four existing labels) | `components/checkout/ProgressBar.tsx` |
| prop | `steps?: string[]` (defaults to `DEFAULT_STEPS`) | `ProgressBar` |
| prop | `heading?: string` (defaults to `"Shipping Address"`) | `ShippingForm` |
| prop | `helperText?: string` (renders no paragraph when absent) | `ShippingForm` |
| new test file | `checkout-step-props-source.test.ts` | `tests/unit/components/` |
</artifacts_this_phase_produces>

<verification>
- `mise exec -- npm test` is green with no new failure in any checkout or cart suite
- `mise exec -- npm run lint`, `mise exec -- npm run typecheck` and `mise exec -- npm run scan:tokens` all pass
- `git diff --stat -- lib app/api migrations package.json package-lock.json` is empty
- The `isSubmitDisabled` expression in `components/checkout/ShippingForm.tsx` is unchanged
</verification>

<success_criteria>
ProgressBar renders any label array it is given and the four existing labels when given none. ShippingForm renders a caller-supplied heading and an optional helper line, and is otherwise the same form with the same seven required fields. A physical checkout renders exactly as it did before this plan, proved by the full unit suite staying green with no call site edited.
</success_criteria>

<output>
Create `.planning/phases/10-gift-card-purchase-flow/10-03-SUMMARY.md` when done
</output>
