---
phase: 10-gift-card-purchase-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/gift-cards/customization.ts
  - components/product/GiftCardRecipientForm.tsx
  - app/product/[slug]/ProductDisplay.tsx
  - tests/unit/lib/gift-cards/customization-field-validators.test.ts
  - tests/unit/components/gift-card-recipient-form-source.test.ts
autonomous: true
requirements: [SHOP-01, SHOP-02, SHOP-03, SHOP-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On /product/gift-card the shopper sees a Recipient details block in place of the plain Add to Cart button, rendered because product.type equals gift_card and for no other reason (D-05, SHOP-01)."
    - "The amount Select, the price/on-sale display and the In stock / Currently unavailable line above the form are unchanged for the gift card, exactly as they render for every other product (D-05)."
    - "lib/gift-cards/customization.ts exports GiftCardFieldError plus four per-field validators that wrap the existing private normalisers; parseGiftCardCustomization and canonicalGiftCardCustomization are byte-identical to their pre-phase form (D-06)."
    - "Empty input: an empty recipient email returns the required code; an empty or whitespace-only name, message or delivery date returns null because those fields are optional (SHOP-01 empty, SHOP-02 empty)."
    - "Length is counted the server's way, not the client's: each validator delegates to the normaliser that applies NFC normalisation, newline folding and whitespace collapse before measuring, so the client can never accept a string the server would reject for length (SHOP-01 encoding, SHOP-02 encoding)."
    - "The character counters under the name and message fields count the raw typed string's code-unit length, which is always greater than or equal to the normalised length the validator measures, so a counter that has turned red can never sit above a validator that still passes in the permissive direction (planner assumption, recorded)."
    - "Add to Cart is disabled until the recipient email validator returns null and every optional field that currently holds a value also returns null; a valid email alone is enough (SHOP-01, SHOP-02, UI-SPEC partial row)."
    - "No error text renders on first paint; a field turns red only after it has been blurred once, and after that first blur it re-validates on every keystroke (UI-SPEC empty and error rows)."
    - "Every invalid field carries aria-invalid and aria-describedby pointing at a sibling paragraph with role alert holding the Copywriting Contract string for that error code (UI-SPEC a11y row)."
    - "The delivery date is a native date input bounded to today through one year out, with helper text that promises email delivery as soon as payment completes and describes the date as printed on the card rather than as a scheduled send (D-07)."
    - "The out_of_range delivery-date code is a client-only rule stricter than the server: normalizedDeliveryDate accepts any real calendar date, so the bound is a UX guard and never a place the client is more permissive than the server (planner assumption, recorded)."
    - "Send to myself renders only when Clerk reports both isLoaded and isSignedIn, so a guest never sees it and it never flashes during the loading tick (D-08, SHOP-03)."
    - "Ticking Send to myself fills the recipient email from the signed-in user's primary email address and the recipient name from the user's full name; when Clerk holds no full name only the email is filled, and when Clerk holds no primary email address neither is (SHOP-03 empty, UI-SPEC empty row)."
    - "Both fields stay editable after Send to myself is ticked, and unticking never clears an already-typed value (D-08, UI-SPEC selection-state row)."
    - "The customization handed to addItem carries the shopper's raw typed strings with no trimming, lower-casing or NFC applied on the client; only keys whose trimmed value is empty are omitted, because an empty optional key is what the server rejects (D-09, SHOP-03 encoding)."
    - "The gift-card add path never produces a cart line without a giftCardCustomization, because the button that calls addItem exists only inside the recipient form and is gated on a valid email (SHOP-04 empty)."
    - "Two adds whose customizations normalise to exactly equal values merge into one line at the same quantity; any differing field produces a separate line; this is already true of the store and this plan changes nothing about it (SHOP-04 adjacency, D-09)."
    - "A merge increments the matched line's quantity in place, leaving the cart's line order unchanged (SHOP-04 ordering)."
    - "Add to Cart fires the existing Added to Cart toast with the same description, icon and View Cart action the physical button already uses (UI-SPEC copy row)."
    - "The form is pure client state with no async load: no spinner, no skeleton and no disabled-while-loading treatment is introduced; the toast fires synchronously on add (UI-SPEC loading row)."
    - "The message Textarea carries a maxLength attribute as a soft backstop, with the per-field validator — not that attribute — as the authority on whether Add to Cart enables (UI-SPEC long-text row)."
    - statement: "The recipient form stacks full-width in a single column at 360px with no horizontal overflow, matching ShippingForm's mobile-first stacking."
      verification: backstop
  artifacts:
    - "lib/gift-cards/customization.ts — new exported type GiftCardFieldError"
    - "lib/gift-cards/customization.ts — new exported function validateGiftCardRecipientEmail(value: string): GiftCardFieldError | null"
    - "lib/gift-cards/customization.ts — new exported function validateGiftCardRecipientName(value: string): GiftCardFieldError | null"
    - "lib/gift-cards/customization.ts — new exported function validateGiftCardMessage(value: string): GiftCardFieldError | null"
    - "lib/gift-cards/customization.ts — new exported function validateGiftCardDeliveryDate(value: string, todayIso?: string): GiftCardFieldError | null"
    - "components/product/GiftCardRecipientForm.tsx — new file, new directory components/product/, default export GiftCardRecipientForm"
    - "components/product/GiftCardRecipientForm.tsx — prop contract { available: boolean; onAdd: (customization: GiftCardCustomization) => void }"
    - "app/product/[slug]/ProductDisplay.tsx — gift-card branch around the existing add-to-cart block plus a new handleGiftCardAdd callback"
    - "tests/unit/lib/gift-cards/customization-field-validators.test.ts — new file"
    - "tests/unit/components/gift-card-recipient-form-source.test.ts — new file"
  key_links:
    - "GiftCardRecipientForm imports the four validators from lib/gift-cards/customization — it never restates the email pattern, the control-character class or any of the three length caps."
    - "ProductDisplay's handleGiftCardAdd passes the form's raw customization straight into useCartStore.getState().addItem, whose normalizeCartItemForStore already runs parseGiftCardCustomization; nothing normalises in between."
    - "The gift-card branch keys off product.type only; ProductDisplay and GiftCardRecipientForm contain no product id or URL-segment comparison."
    - "Send to myself reads Clerk through useUser() from @clerk/nextjs, the same import shape components/agent/AgentDrawer.tsx already uses."
  prohibitions:
    - statement: "No file under lib/gift-cards/ other than customization.ts is edited, and inside customization.ts the existing exports parseGiftCardCustomization and canonicalGiftCardCustomization are unchanged."
      status: enforced
      verification: "git diff --name-only -- lib/gift-cards/ lists only lib/gift-cards/customization.ts, and git diff -- lib/gift-cards/customization.ts shows no removed or altered line inside either existing exported function"
    - statement: "No change to lib/stores/cart-store.ts, lib/gift-cards/line-identity.ts, app/api/, lib/services/, or the Stripe webhook."
      status: enforced
      verification: "git diff --stat -- lib/stores/cart-store.ts lib/gift-cards/line-identity.ts app/api lib/services is empty"
    - statement: "No migration file is added or edited."
      status: enforced
      verification: "git status --porcelain -- migrations/ is empty"
    - statement: "No secret value is written into any committed file."
      status: enforced
      verification: "the plan's diff contains no key material; .dev.vars and wrangler.jsonc are untouched"
    - statement: "No hex colour, rgb() call or hardcoded palette value in any new or edited storefront file."
      status: enforced
      verification: "mise exec -- npm run scan:tokens reports zero violations"
    - statement: "No deploy script is run from this machine."
      status: enforced
      verification: "no npm run deploy, deploy:ci, predeploy or wrangler deploy appears in any task command"
    - statement: "No blanket git add; every commit stages the exact paths it changes."
      status: enforced
      verification: "each task's commit names its files explicitly"
    - statement: "No new npm dependency is installed."
      status: enforced
      verification: "git diff --stat -- package.json package-lock.json is empty"
    - statement: "No template code keys off the gift card product's id or URL segment; only product.type and the presence of giftCardCustomization decide behaviour."
      status: enforced
      verification: "grep -c 'prod_33' and grep -c 'product.slug' over ProductDisplay.tsx and GiftCardRecipientForm.tsx both return 0"
---

<objective>
Prove the whole gift-card recipient path end to end on one field, then fill it out. This is the phase's tracer: a shopper opens /product/gift-card, sees a recipient form that validates against the same rules the server enforces, and adds a cart line that carries the recipient details unaltered into the existing line-identity pipeline.

Purpose: every later plan in this phase reads recipient data out of a cart line or an order record. If the write path is wrong, every read surface is wrong. Proving the write path first — validators, form, product-page wiring, real cart line, real rendered page — settles the architecture on the executor's best early-context tokens.

Output: four exported per-field validators and one exported error-code type on lib/gift-cards/customization.ts (additive, the whole-object gate untouched); a new GiftCardRecipientForm client component; a data-keyed branch in ProductDisplay.tsx; two new test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-gift-card-purchase-flow/10-CONTEXT.md
@.planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md
@.planning/phases/10-gift-card-purchase-flow/10-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a shopper fills a recipient email and gets a customised cart line" — one field, one path</name>
  <files>lib/gift-cards/customization.ts, components/product/GiftCardRecipientForm.tsx, app/product/[slug]/ProductDisplay.tsx, tests/unit/lib/gift-cards/customization-field-validators.test.ts, tests/unit/components/gift-card-recipient-form-source.test.ts</files>
  <read_first>
    - lib/gift-cards/customization.ts (the whole 125-line file; the four private normalisers and the two existing exports)
    - lib/gift-cards/line-identity.ts (normalizeCartItemForStore, sameCartLineFacts, createCartLineId)
    - lib/stores/cart-store.ts (addItem; read-only, never edited by this phase)
    - lib/types/cartitem.ts (GiftCardCustomization, CartItem)
    - app/product/[slug]/ProductDisplay.tsx (the "use client" boundary at line 34; the variant Select, price block, availability line and the add-to-cart block that starts at line 326)
    - components/checkout/ShippingForm.tsx (the controlled-input plus submit-disabled-until-valid shape this form copies)
    - components/agent/AgentDrawer.tsx (the useUser() import and destructure shape)
    - .planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md (Component Anatomy for GiftCardRecipientForm; the Copywriting Contract table)
    - .planning/phases/10-gift-card-purchase-flow/10-PATTERNS.md (the customization.ts and GiftCardRecipientForm sections)
    - tests/unit/lib/gift-cards/customization.test.ts and tests/unit/components/cart-line-source.test.ts (the two test styles this phase uses)
    - AGENTS.md (mise exec, token contract, hard fences)
  </read_first>
  <precondition>The local D1 database holds the Phase 9 gift-card rows, so /product/gift-card resolves on the dev server. Slice the block out of data/d1/seed.sql and apply it before running this task's dev-server check; halt if the page 404s.</precondition>
  <behavior>
    - validateGiftCardRecipientEmail("") returns 'required'
    - validateGiftCardRecipientEmail("   ") returns 'required'
    - validateGiftCardRecipientEmail("not-an-email") returns 'invalid_format'
    - validateGiftCardRecipientEmail("  Recipient@Example.COM ") returns null — the normaliser trims and lower-cases before testing
    - validateGiftCardRecipientEmail of a 250-character local part at a real domain, total length above 254, returns 'too_long'
    - validateGiftCardRecipientEmail of a string containing a U+0000 control character returns 'control_characters'
    - validateGiftCardRecipientEmail("a@b.co@d.com") returns 'invalid_format' — more than one at-sign
  </behavior>
  <action>
Add the error-code type and the email validator to lib/gift-cards/customization.ts, below canonicalGiftCardCustomization, then build the thinnest form that uses it and wire it into the product page.

In lib/gift-cards/customization.ts, export a union type named GiftCardFieldError with exactly these five string members: required, invalid_format, too_long, control_characters, out_of_range. Export a function validateGiftCardRecipientEmail taking a string and returning GiftCardFieldError or null. It returns required when the trimmed input is empty. Otherwise it calls the existing private normalizedEmail inside a try block and returns null on success. In the catch it distinguishes the reason without re-deriving any rule: if CONTROL_CHARACTERS matches the raw input return control_characters; else if the NFC-normalised, trimmed, lower-cased input is longer than GIFT_CARD_RECIPIENT_EMAIL_MAX_LENGTH return too_long; else return invalid_format. Do not touch ALLOWED_KEYS, EMAIL_PATTERN, CONTROL_CHARACTERS, the three max-length constants, GiftCardCustomizationValidationError, isPlainRecord, any of the four normalisers, parseGiftCardCustomization, or canonicalGiftCardCustomization — this edit is append-only below the existing last line.

Create components/product/GiftCardRecipientForm.tsx as a new file in a new components/product/ directory. Mark it "use client". Its props are exactly `{ available: boolean; onAdd: (customization: GiftCardCustomization) => void }`, importing the GiftCardCustomization type from @/lib/types/cartitem. For this task it renders only three things inside a `space-y-4` column: an `h3` heading reading exactly "Recipient details" styled `text-lg font-semibold text-foreground`; a Label plus an Input for the recipient email, label text exactly "Recipient email", `type="email"`, `inputMode="email"`, `name="recipientEmail"`, and the exact placeholder string from the Copywriting Contract's recipient-email row; and a Button of the default variant reading exactly "Add to Cart" with `className="w-full sm:w-auto"`. Hold the email in local useState. Hold a separate boolean useState recording whether the email field has been blurred. Compute the current error by calling validateGiftCardRecipientEmail on the current value. Show the error paragraph only when the field has been blurred and the error is non-null; render it as a `p` with `role="alert"`, `id="gift-card-recipient-email-error"`, and `className="mt-1 text-xs text-danger"`, whose text is chosen by a lookup from the error code to the Copywriting Contract's four email strings. Set `aria-invalid` on the Input to true only when the error paragraph is showing, and set `aria-describedby` to that same id in that case and to undefined otherwise. Disable the Button when `available` is false or the email error is non-null. On click, build the customization object as `{ recipientEmail: <the raw state value, untrimmed and unaltered> }` and hand it to `onAdd`. Import Input, Label and Button from their existing components/ui paths. Style with token classes only.

In app/product/[slug]/ProductDisplay.tsx, extract the body of the existing add-to-cart button's onClick into a named callback that accepts an optional GiftCardCustomization and spreads it into the addItem argument when present, keeping the productName/variantDisplay/fullName derivation, the Money.fromMinor(price).toJSON() price, quantity 1, the existing primaryImageUrl try/catch expression and the existing toast call all byte-identical. Then replace the existing `{available ? <button …Add to Cart…/> : <p>Coming soon</p>}` expression with a branch that renders GiftCardRecipientForm when `product.type === "gift_card"`, passing `available` and an `onAdd` that calls that shared callback, and otherwise renders the existing expression unchanged. The comparison is against product.type and nothing else; do not read the product's id or URL segment anywhere in this file. Import GiftCardRecipientForm from @/components/product/GiftCardRecipientForm.

Create tests/unit/lib/gift-cards/customization-field-validators.test.ts as a sibling of the existing customization test — do not edit that existing file. Import the new type and the email validator from @/lib/gift-cards/customization and cover every case in this task's behavior block with an it.each boundary table in the style of the existing file.

Create tests/unit/components/gift-card-recipient-form-source.test.ts using the readFileSync source-contract style of tests/unit/components/cart-line-source.test.ts. Assert that GiftCardRecipientForm.tsx contains the "use client" directive, imports validateGiftCardRecipientEmail from the customization module, contains the literal heading and label strings, and contains `role="alert"`. Assert that ProductDisplay.tsx contains the literal `product.type === "gift_card"` comparison and renders GiftCardRecipientForm. Assert that neither file contains the string `prod_33` or the string `product.slug`.
  </action>
  <acceptance_criteria>
    - `grep -c "export type GiftCardFieldError" lib/gift-cards/customization.ts` returns 1
    - `grep -c "export function validateGiftCardRecipientEmail" lib/gift-cards/customization.ts` returns 1
    - `git diff -- lib/gift-cards/customization.ts > /tmp/gsd-10-01-cust.diff` exits 0, then `grep -c '^-[^-]' /tmp/gsd-10-01-cust.diff` returns 0 — the edit removes no existing line
    - `git diff --stat -- lib/stores/cart-store.ts lib/gift-cards/line-identity.ts lib/gift-cards/checkout.ts app/api lib/services` produces no output
    - `grep -c 'product.type === "gift_card"' 'app/product/[slug]/ProductDisplay.tsx'` returns at least 1
    - `grep -c "prod_33" 'app/product/[slug]/ProductDisplay.tsx' components/product/GiftCardRecipientForm.tsx` returns 0 for both files
    - `grep -c "product.slug" 'app/product/[slug]/ProductDisplay.tsx' components/product/GiftCardRecipientForm.tsx` returns 0 for both files
    - `grep -c "giftCardCustomization" 'app/product/[slug]/ProductDisplay.tsx'` returns at least 1
    - A locally running dev server serves /product/gift-card containing the heading text and the email placeholder text
    - `mise exec -- npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/lib/gift-cards/customization-field-validators.test.ts tests/unit/lib/gift-cards/customization.test.ts tests/unit/components/gift-card-recipient-form-source.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or any assertion in the pre-existing customization.test.ts starts failing</fails_when>
    <automated>sed -n '/^-- BEGIN gift-card-block (Phase 9)$/,/^-- END gift-card-block (Phase 9)$/p' data/d1/seed.sql > /tmp/gsd-10-gift-card-apply.sql && mise exec -- npm run db:prepare:local && mise exec -- npx wrangler d1 execute mercora-db --local --file /tmp/gsd-10-gift-card-apply.sql -y && (mise exec -- npm run dev > /tmp/gsd-10-dev.log 2>&1 & echo $! > /tmp/gsd-10-dev.pid) && for i in $(seq 1 60); do sleep 3; curl -sf -o /tmp/gsd-10-pdp.html http://localhost:3000/product/gift-card && break; done; kill "$(cat /tmp/gsd-10-dev.pid)"; grep -c "Recipient details" /tmp/gsd-10-pdp.html</automated>
    <fails_when>the curl loop never succeeds (no /tmp/gsd-10-pdp.html written), or the final grep prints 0, or the dev log contains a module-resolution error for @/components/product/GiftCardRecipientForm</fails_when>
    <automated>mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or any TS error naming GiftCardRecipientForm, ProductDisplay or customization.ts</fails_when>
  </verify>
  <done>lib/gift-cards/customization.ts exports GiftCardFieldError and validateGiftCardRecipientEmail with both pre-existing exports untouched; /product/gift-card renders the Recipient details block with a working email field; clicking Add to Cart with a valid email produces a cart line carrying giftCardCustomization; all three verify commands pass and the work is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The remaining three per-field validators</name>
  <files>lib/gift-cards/customization.ts, tests/unit/lib/gift-cards/customization-field-validators.test.ts</files>
  <read_first>
    - lib/gift-cards/customization.ts as it stands after Task 1
    - tests/unit/lib/gift-cards/customization-field-validators.test.ts as it stands after Task 1
    - .planning/phases/10-gift-card-purchase-flow/10-CONTEXT.md (D-06 and D-07)
    - .planning/phases/10-gift-card-purchase-flow/10-RESEARCH.md (Pattern 1)
  </read_first>
  <behavior>
    - validateGiftCardRecipientName("") and validateGiftCardRecipientName("   ") both return null — the field is optional
    - validateGiftCardRecipientName("  Ada   Lovelace ") returns null
    - validateGiftCardRecipientName of 101 repeated letters returns 'too_long'
    - validateGiftCardRecipientName of 101 characters that collapse to 100 after whitespace folding returns null — the normaliser's count, not the raw count, decides
    - validateGiftCardRecipientName of a string holding a U+0000 control character returns 'control_characters'
    - validateGiftCardMessage("") returns null; a 501-character message returns 'too_long'; a message containing a U+0000 control character returns 'control_characters'
    - validateGiftCardMessage of a message with CRLF line breaks returns null — the normaliser folds them
    - validateGiftCardDeliveryDate("") returns null
    - validateGiftCardDeliveryDate("24/12/2026") returns 'invalid_format'
    - validateGiftCardDeliveryDate("2026-02-29") returns 'invalid_format' — not a real calendar date
    - validateGiftCardDeliveryDate("2026-09-07", "2026-09-08") returns 'out_of_range' — yesterday
    - validateGiftCardDeliveryDate("2026-09-08", "2026-09-08") returns null — today is allowed
    - validateGiftCardDeliveryDate("2027-09-08", "2026-09-08") returns null — exactly one year out is allowed
    - validateGiftCardDeliveryDate("2027-09-09", "2026-09-08") returns 'out_of_range'
  </behavior>
  <action>
Append three more validators to lib/gift-cards/customization.ts, each following the shape Task 1 established. Again this is an append-only edit below the existing content; nothing already in the file changes.

validateGiftCardRecipientName takes a string and returns GiftCardFieldError or null. It returns null when the trimmed input is empty, because the field is optional. Otherwise it calls the existing private normalizedOptionalText with GIFT_CARD_RECIPIENT_NAME_MAX_LENGTH inside a try block and returns null on success; in the catch it returns control_characters when CONTROL_CHARACTERS matches the raw input and too_long otherwise.

validateGiftCardMessage has the identical shape but delegates to the existing private normalizedMessage, which carries its own cap.

validateGiftCardDeliveryDate takes a string plus an optional second parameter todayIso and returns GiftCardFieldError or null. It returns null when the trimmed input is empty. Otherwise it calls the existing private normalizedDeliveryDate inside a try block; the catch returns invalid_format. On success it applies the client-only calendar bound: resolve the reference day from todayIso when supplied and otherwise from the current UTC date rendered as a ten-character ISO day string; compute the same day one calendar year later; return out_of_range when the input sorts before the reference day or after that upper bound, and null otherwise. Compare the three ten-character day strings lexicographically — that is exact for this format and needs no date arithmetic beyond adding one to the year. Document in a short comment above the function that this bound is stricter than the server's, which accepts any real calendar day, so the client is never the more permissive of the two.

Extend tests/unit/lib/gift-cards/customization-field-validators.test.ts with a describe block per validator covering every case in this task's behavior block. Pass todayIso explicitly in every delivery-date case so the suite is deterministic under any clock.
  </action>
  <acceptance_criteria>
    - `grep -c "export function validateGiftCardRecipientName" lib/gift-cards/customization.ts` returns 1
    - `grep -c "export function validateGiftCardMessage" lib/gift-cards/customization.ts` returns 1
    - `grep -c "export function validateGiftCardDeliveryDate" lib/gift-cards/customization.ts` returns 1
    - `grep -cE '^export const GIFT_CARD_[A-Z_]+_MAX_LENGTH' lib/gift-cards/customization.ts` returns 3 — the three caps are still declared exactly once each, and every new validator reaches them through the private normalisers rather than restating one
    - `git diff -- lib/gift-cards/customization.ts > /tmp/gsd-10-01-cust.diff` exits 0, then `grep -c '^-[^-]' /tmp/gsd-10-01-cust.diff` returns 0 across the whole plan so far
    - `mise exec -- npx vitest run tests/unit/lib/gift-cards/customization.test.ts` still passes unchanged
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/lib/gift-cards/customization-field-validators.test.ts tests/unit/lib/gift-cards/customization.test.ts tests/unit/lib/gift-cards/line-identity.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or any delivery-date boundary case reports the wrong code</fails_when>
    <automated>mise exec -- npm run typecheck &amp;&amp; mise exec -- npm run lint</automated>
    <fails_when>non-zero exit from either command, or a lint error naming lib/gift-cards/customization.ts</fails_when>
  </verify>
  <done>All four validators are exported, every boundary case in the behavior block passes, the pre-existing whole-object suite is untouched and still green, and the work is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand the recipient form to all four fields plus Send to myself</name>
  <files>components/product/GiftCardRecipientForm.tsx, tests/unit/components/gift-card-recipient-form-source.test.ts</files>
  <read_first>
    - components/product/GiftCardRecipientForm.tsx as it stands after Task 1
    - lib/gift-cards/customization.ts as it stands after Task 2
    - components/ui/textarea.tsx, components/ui/checkbox.tsx, components/ui/label.tsx, components/ui/input.tsx (the aria-invalid styling already built in)
    - components/agent/AgentDrawer.tsx (the useUser() call shape)
    - node_modules/@clerk/shared/dist/types/index.d.ts around line 2078 (UserResource.primaryEmailAddress and fullName)
    - .planning/phases/10-gift-card-purchase-flow/10-UI-SPEC.md (Component Anatomy field order 1 through 6; the whole Copywriting Contract table)
    - tests/unit/components/gift-card-recipient-form-source.test.ts as it stands after Task 1
  </read_first>
  <behavior>
    - The source contract test asserts the form imports all four validators, not just the email one
    - It asserts the checkbox render is gated on both isLoaded and isSignedIn together
    - It asserts the Clerk read paths user.primaryEmailAddress and user.fullName both appear
    - It asserts the customization built for onAdd includes an optional key only when that key's trimmed value is non-empty
    - It asserts the delivery-date input carries both min and max attributes
    - It asserts the exact delivery-date helper sentence from the Copywriting Contract appears verbatim
    - It asserts the form contains no numeric literal 254, 100 or 500 outside a counter denominator, so no cap is restated
  </behavior>
  <action>
Grow GiftCardRecipientForm.tsx from one field to the full six-element anatomy the UI-SPEC specifies, keeping everything Task 1 established.

Hold the four values, and a blurred-once flag per field, in local state. Compute each field's error by calling its validator on the current value; show a field's error paragraph only once that field has been blurred. Each error paragraph keeps the Task 1 shape — role alert, an id derived from the field name, `className="mt-1 text-xs text-danger"` — and each control carries aria-invalid and aria-describedby wired to it exactly as the email field already does.

Field order top to bottom, in the existing `space-y-4` column. First, the Send to myself row: a Checkbox plus Label in a `flex items-center gap-2` wrapper, label text exactly "Send to myself", rendered only when `useUser()` reports both isLoaded and isSignedIn true. Destructure all three of isLoaded, isSignedIn and user. When the box is ticked, set the email state from the signed-in user's primary email address's emailAddress when that address object is present, and set the name state from the user's fullName when it is a non-empty string; leave either untouched when Clerk has nothing. Unticking clears nothing. Both fields remain ordinary editable inputs afterwards.

Second, the existing recipient email field, unchanged. Third, a recipient name field: Label text exactly "Recipient name (optional)" and the Copywriting Contract's name placeholder, laid out as a `flex justify-between` row with the Label on the left and a counter on the right reading the current raw length over 100, `text-xs text-muted-foreground` switching to `text-xs text-danger` when the raw length exceeds that denominator. Fourth, a Gift message field: Label text exactly "Gift message (optional)", the Contract's message placeholder, a Textarea with `rows={3}` and a maxLength attribute set to the message cap constant imported from the customization module rather than a fresh literal, and the same counter treatment against 500. Fifth, a delivery date field: Label text exactly "Delivery date (optional)" over a native `input` of `type="date"` carrying `min` set to today's ten-character ISO day and `max` set to that same day one year later, both computed once per render, and styled with the same border, radius and focus token classes the Input primitive uses; under it a helper paragraph `className="mt-1 text-xs text-muted-foreground"` holding the Contract's delivery-date helper sentence verbatim. Sixth, the existing Add to Cart Button, now disabled when `available` is false or when any of the four validators returns a non-null code for its current value.

On click, build the customization with recipientEmail always set to the raw email state, and each of recipientName, message and deliveryDate included only when that field's trimmed value is non-empty, in which case the value stored is the raw untrimmed state. Hand that object to `onAdd`. Do not trim, lower-case or NFC-normalise any value — the cart store's normalizeCartItemForStore already does that once.

Extend tests/unit/components/gift-card-recipient-form-source.test.ts to cover every item in this task's behavior block against the component source.
  </action>
  <acceptance_criteria>
    - `grep -c "validateGiftCardRecipientName\|validateGiftCardMessage\|validateGiftCardDeliveryDate" components/product/GiftCardRecipientForm.tsx` returns at least 3
    - `grep -c "isLoaded" components/product/GiftCardRecipientForm.tsx` returns at least 1 and the same file matches `isLoaded && isSignedIn`
    - `grep -c "primaryEmailAddress" components/product/GiftCardRecipientForm.tsx` returns at least 1
    - `grep -c 'type="date"' components/product/GiftCardRecipientForm.tsx` returns 1
    - `grep -c "Delivered by email as soon as payment completes" components/product/GiftCardRecipientForm.tsx` returns 1
    - `grep -c "@/lib/gift-cards/customization" components/product/GiftCardRecipientForm.tsx` returns 1 — every validation rule the form applies arrives through that single import and none is restated locally
    - `mise exec -- npm run scan:tokens` reports zero violations
    - `git diff --stat -- lib/gift-cards lib/stores app/api lib/services` shows only lib/gift-cards/customization.ts
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/components/gift-card-recipient-form-source.test.ts tests/unit/lib/gift-cards/customization-field-validators.test.ts tests/unit/lib/stores/cart-store-lines.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 passed, or the cart-store merge/separate suite regresses</fails_when>
    <automated>mise exec -- npm run typecheck &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run scan:tokens</automated>
    <fails_when>non-zero exit from any of the three, or scan:tokens prints a violation count above 0</fails_when>
    <human-check>Open /product/gift-card at 360px width and confirm the six form elements stack full-width in one column with no horizontal scrollbar, and that the Add to Cart button is grey until a valid email is typed and filled once it is. Signed out, confirm Send to myself is absent and never flashes on load.</human-check>
  </verify>
  <done>The recipient form renders all four fields plus the signed-in-only Send to myself checkbox, gates Add to Cart on all four validators, hands raw values with empty optional keys omitted to onAdd, restates no server rule, passes scan:tokens, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shopper keyboard → client component state | free-text recipient email, name and message enter the app here |
| client component → zustand cart store → localStorage | recipient PII is persisted on the device by the existing persist middleware |
| Clerk session → client render | signed-in identity is read for the Send to myself prefill |
| client cart → POST /api/payment-intent | every value re-validated server-side; unchanged this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | zustand persist of giftCardCustomization in localStorage | medium | accept | The cart already persists product and price data on the device. parseGiftCardCustomization rejects every key outside the four-field allowlist on write, so no bearer code or redemption token can be persisted alongside. Recorded as accepted, not overlooked. |
| T-10-02 | Tampering | client per-field validators as a security control | low | mitigate | The validators delegate to the same private normalisers parseGiftCardCustomization uses, so they can never be more permissive than the server. The delivery-date bound is deliberately stricter. A shopper who bypasses the form entirely still hits normalizeCartItemForStore, which returns null and drops the add. |
| T-10-03 | Information Disclosure | Send to myself reading Clerk data | medium | mitigate | The checkbox and every Clerk read are gated on isLoaded and isSignedIn together, so no user object is dereferenced during the loading tick and a guest render never touches Clerk identity. |
| T-10-06 | Information Disclosure | gift-card bearer code or redemption token surfacing in the form | high | mitigate | The form's onAdd contract is typed as GiftCardCustomization, whose four fields are the entire allowlist; the component imports nothing from lib/gift-cards/code.ts, encryption.ts, domain.ts or repository.ts, and the acceptance criteria hold the lib/gift-cards diff to customization.ts alone. |
| T-10-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; RESEARCH.md's Package Legitimacy Audit records zero new packages, and an empty package.json / package-lock.json diff is a plan prohibition. No [ASSUMED] or [SUS] package to gate. |
</threat_model>

<artifacts_this_phase_produces>
This plan's contribution to the phase's artifact set:

| Kind | Name | Location |
|---|---|---|
| exported type | `GiftCardFieldError` (`'required' \| 'invalid_format' \| 'too_long' \| 'control_characters' \| 'out_of_range'`) | `lib/gift-cards/customization.ts` |
| exported function | `validateGiftCardRecipientEmail(value: string): GiftCardFieldError \| null` | `lib/gift-cards/customization.ts` |
| exported function | `validateGiftCardRecipientName(value: string): GiftCardFieldError \| null` | `lib/gift-cards/customization.ts` |
| exported function | `validateGiftCardMessage(value: string): GiftCardFieldError \| null` | `lib/gift-cards/customization.ts` |
| exported function | `validateGiftCardDeliveryDate(value: string, todayIso?: string): GiftCardFieldError \| null` | `lib/gift-cards/customization.ts` |
| new directory | `components/product/` | repo root |
| new component | `GiftCardRecipientForm` (default export) | `components/product/GiftCardRecipientForm.tsx` |
| prop | `available: boolean` | `GiftCardRecipientForm` |
| prop | `onAdd: (customization: GiftCardCustomization) => void` | `GiftCardRecipientForm` |
| new test file | `customization-field-validators.test.ts` | `tests/unit/lib/gift-cards/` |
| new test file | `gift-card-recipient-form-source.test.ts` | `tests/unit/components/` |
</artifacts_this_phase_produces>

<verification>
- `mise exec -- npx vitest run tests/unit/lib/gift-cards/ tests/unit/components/gift-card-recipient-form-source.test.ts tests/unit/lib/stores/cart-store-lines.test.ts` is green
- `mise exec -- npm run lint`, `mise exec -- npm run typecheck` and `mise exec -- npm run scan:tokens` all pass
- `git diff --name-only -- lib/gift-cards/` lists only `customization.ts`
- `git diff --stat -- lib/stores/cart-store.ts app/api lib/services migrations package.json package-lock.json` is empty
</verification>

<success_criteria>
A shopper on /product/gift-card sees a Recipient details form with four fields and, when signed in, a Send to myself checkbox. Add to Cart is disabled until the recipient email validates and every filled optional field validates, against the exact rules the server enforces. Adding produces a cart line carrying the recipient details verbatim, which the existing line-identity pipeline normalises, merges and separates without any change to it.
</success_criteria>

<output>
Create `.planning/phases/10-gift-card-purchase-flow/10-01-SUMMARY.md` when done
</output>
