# Docs Inventory

One-line purpose: the keep/merge/retire verdict for every file this phase's documentation
overhaul touches, so plans 02 and 03 execute a work list instead of re-deciding anything.

**Status:** In progress (08.2-01 seeds the first row; 08.2-02 fills the rest).

Mercora is a themeable reference storefront on Cloudflare — Next.js, D1, R2, Workers AI, Stripe
and Clerk — that can render any product catalogue. The shipped sample catalogue is outdoor gear,
and the default look, `volt-dark`, is one of seven presets. Wherever a document describes the
product, that outdoor-gear framing is sample data, not the product's identity, and this phase's
rewrites demote it accordingly.

## Verdict table

A document nothing links to is still classified below — "nothing links to it" is not a reason to
retire something on its own, since an unlinked document may be the only place a fact is written
down. Every retirement row's reason names what supersedes the file or why the need it served has
ended.

| File | Lines | Manifest Type | Historical Banner | Verdict | Reason |
|---|---:|---|---|---|---|
| `docs/api-architecture.md` | 557 | SPEC | No | RETIRE | Documents roughly 9 endpoints against 88 real `route.ts` files today, and its closing "security layers" Mermaid diagram (WAF, bot management, CSRF) is generic, code-unverified, and duplicated with `docs/architecture.md`'s own closing diagram. |

## Retirement queue

| File | Verdict | Executing plan |
|---|---|---|
| `docs/api-architecture.md` | RETIRE | 08.2-01 (this plan — done) |

</content>
