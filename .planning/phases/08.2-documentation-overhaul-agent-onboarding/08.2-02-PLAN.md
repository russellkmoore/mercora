---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["08.2-01"]
files_modified:
  - gsd-ingest-manifest.yaml
  - README.md
  - docs/README.md
  - docs/mobile-lighthouse-baseline.md
  - docs/CHANGELOG-docs.md
  - scripts/docs-lint.mjs
  - package.json
files_deleted:
  - docs/admin-dashboard-specification.md
  - docs/mobile-improvements-actionable.md
  - docs/mobile-testing-automation.md
  - docs/mobile-ux-assessment.md
  - docs/ROADMAP.md
  - docs/o07-gift-cards-plan.md
  - docs/migration-reservations.md
autonomous: true
requirements: [DOCS-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Seven stale documents are gone from the tree and from every surface that pointed at them: the docs index, the root README, the mobile Lighthouse baseline and the ingest manifest (D-03, D-04). [DOCS-04]"
    - "No commit in this plan leaves a reference to a file that does not exist: each retirement lands the delete, the inbound-link fixes, the manifest entry removal and the changelog line together (DOCS-04 concurrency edge). [DOCS-04]"
    - "Every retirement has a written reason in `docs/CHANGELOG-docs.md` naming what supersedes it or why the need it served ended — a link count of zero is never the reason (DOCS-04 empty edge). [DOCS-04]"
    - "`scripts/docs-lint.mjs` runs on plain Node with no dependency and fails the moment a doc references a missing file, an npm script that does not exist, a credential-shaped string, a dangling or unregistered manifest entry, a missing locked ADR, or a doc without a level-one title on its first line (D-05, D-06). [DOCS-04]"
    - "`npm run docs:lint` exits 0 against the tree at the end of this plan and exits non-zero against a deliberately broken probe file, so the gate is proven in both directions rather than assumed. [DOCS-04]"
    - "`gsd-ingest-manifest.yaml` still agrees with `docs/` in both directions after seven entries are removed, and the four locked ADRs are untouched (D-10). [DOCS-04]"
    - "The mobile Lighthouse baseline still reads correctly after losing its two pointers to retired documents: each follow-on sentence names where the work is now tracked instead of pointing at a deleted file. [DOCS-04]"
  artifacts:
    - path: "scripts/docs-lint.mjs"
      provides: "Zero-dependency documentation gate: link resolution, manifest reconciliation, locked-ADR guard, npm script existence, credential shapes, header convention"
      contains: "node:fs"
      min_lines: 90
    - path: "docs/CHANGELOG-docs.md"
      provides: "Eight retirement entries under this phase's dated heading, one per file, each with a reason"
      contains: "migration-reservations.md"
    - path: "package.json"
      provides: "A docs:lint script entry so the gate is runnable by name from every later plan"
      contains: "docs:lint"
  key_links:
    - from: "package.json"
      to: "scripts/docs-lint.mjs"
      via: "the docs:lint script invokes the validator"
      pattern: "docs-lint.mjs"
    - from: "scripts/docs-lint.mjs"
      to: "gsd-ingest-manifest.yaml"
      via: "the manifest reconciliation check reads the path entries and compares them to the docs directory"
      pattern: "gsd-ingest-manifest.yaml"
    - from: "docs/mobile-lighthouse-baseline.md"
      to: ".planning/"
      via: "the follow-on sentences name where the work is tracked now that the two proposal docs are retired"
      pattern: "backlog"
  prohibitions:
    - statement: "This phase must NOT delete, rename or move any of the four locked ADR files."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from D-10; binds hardest here because this plan performs seven deletions in two tasks and a mistyped path list is the plausible failure."
    - statement: "This phase must NOT change application code; the only permitted non-documentation change is a zero-dependency docs-lint script and its npm script entry."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from the CONTEXT.md phase boundary; binds hardest here because this is the one plan that writes a script and edits package.json."
    - statement: "The documentation gate must NOT be wired into CI in this phase."
      status: unresolved
      verification: judgment
      reason: "Planner decision following the Phase 8 precedent: a documentation phase that changes CI falsifies its own claim about what CI runs. Recorded as a follow-up instead."
    - statement: "The documentation gate must NOT add a devDependency."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less constraint from the phase security contribution and RESEARCH.md's zero-new-dependency finding; binds on the one task that writes code."
---

<objective>
Retire the remaining seven stale documents, then turn the checks that proved plan 01's retirement
into a script the rest of the phase runs by name.

Purpose: the bulk of DOCS-04's prune. Plan 01 proved the path on one document; this plan walks it
seven more times and then stops the checks from being retyped by hand in every later plan.

Output: seven documents deleted with every inbound reference fixed, seven manifest entries removed,
seven changelog lines added, and `scripts/docs-lint.mjs` plus `npm run docs:lint` green on the tree
and red on a broken probe.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-CONTEXT.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-PATTERNS.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-01-SUMMARY.md
</context>

<interfaces>
Consumed from plan 01, unchanged: the retirement contract (delete, fix every inbound reference,
remove the manifest entry, add the changelog line, all in one commit), the manifest invariant (the
set of manifest doc paths equals the set of files under `docs/`), and the retirement queue in
`08.2-DOCS-INVENTORY.md`, which names exactly the seven files this plan retires.

Produced here for later plans:

- `npm run docs:lint` — the phase's documentation gate. Plans 03, 04, 05 and 06 call it by name in
  their verify blocks rather than retyping grep loops. It is a local gate; it is not wired into CI
  in this phase and `CONTRIBUTING.md`'s gate list, fixed in plan 03, does not include it.
- `docs/CHANGELOG-docs.md` gains seven entries under the heading plan 01 created. Plan 03 appends
  the merge entry to the same heading.

Nothing in this plan exports application code or changes a runtime contract.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`. Bare `node` is v26.
- The shell has `noclobber` and an interactive `rm` alias. Delete tracked files with `git rm` and
  untracked files with `/bin/rm -f`. Write commit messages to a `mktemp` file and use `git commit -F`.
- Two of the seven retired files are cross-referenced by `docs/ROADMAP.md`, which is itself one of the
  seven. Retire them in an order where that does not matter, or retire the roadmap copy in the same
  commit; either is fine, but no commit may end with a live document pointing at a dead path.
- `docs/CHANGELOG-docs.md` is the one file allowed to name a retired document. Name retired files in
  backticks there, never as a markdown link, and exclude that file from every dead-reference scan.
- The seven verdicts are already decided in `08.2-DOCS-INVENTORY.md`. Do not re-litigate one because
  the file is long; length is not a verdict.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Retire the four superseded admin and mobile documents and repoint the Lighthouse baseline</name>
  <read_first>
    - `08.2-DOCS-INVENTORY.md` — the retirement queue rows for the admin dashboard specification and the three mobile documents, and their reasons.
    - `docs/mobile-lighthouse-baseline.md` lines 125-141 — the "Two follow-ons" block, which names two of the documents being retired; you are rewriting those two sentences, not deleting the block.
    - `docs/README.md` lines 18-24 and 46-56 and 75-90 — the admin bullet, the assessments group, and the "where do I look" list, all of which reference these files.
    - `README.md` lines 116-136 — the docs-index block and the admin bullet inside it.
    - `gsd-ingest-manifest.yaml` — the SPEC, PRD and DOC groups holding the four entries to remove.
    - `.planning/STATE.md` Blockers/Concerns — the mobile Lighthouse backlog item, so the repointed sentences name where that work is actually tracked.
  </read_first>
  <files>docs/admin-dashboard-specification.md, docs/mobile-improvements-actionable.md, docs/mobile-testing-automation.md, docs/mobile-ux-assessment.md, docs/mobile-lighthouse-baseline.md, docs/README.md, README.md, gsd-ingest-manifest.yaml, docs/CHANGELOG-docs.md</files>
  <reversibility rating="costly">Four documents leave the working tree. Git history keeps them, so recovery is a `git show` away, but their paths and inbound links would have to be rebuilt by hand.</reversibility>
  <action>
Retire the admin dashboard specification and the three mobile documents, following the contract plan
01 proved.

For each of the four: delete it with `git rm`, remove its entry from the ingest manifest, and add one
line to the docs changelog naming the file in backticks with the reason from the inventory. The
admin specification describes modules that were never built and its own banner points readers at the
live project record instead. The mobile improvements guide describes work that has shipped. The
mobile testing proposal describes scripts that do not exist in the package manifest. The mobile UX
assessment says on its own face that the Lighthouse baseline supersedes it.

Then fix every inbound reference. The docs index loses four bullets — the admin bullet from the
business group and three bullets from the assessments group — and the mention of the admin
specification in the "where do I look" list; reword that sentence so it still reads correctly,
pointing a reader after admin capability at the admin authentication document and the live project
record instead. The root README loses its admin-specification bullet from the docs index.

Then repoint the Lighthouse baseline. Its closing "Two follow-ons" block names two of the retired
documents. Keep the block and keep both follow-ons — the work is still real — but rewrite each
sentence so it names where that work is tracked now rather than pointing at a deleted file: the
measurement follow-up and the automation follow-up both live in the planning backlog, and the
baseline's own scores remain the current source of truth for mobile performance status. Do not add a
link to any `.planning/` path from a published document; name the tracker in prose.

Commit once, with a message naming the four retired files and the surfaces that lost a reference.
  </action>
  <acceptance_criteria>
    - None of the four documents exists in the working tree.
    - No file under `docs/`, no root markdown file and not the ingest manifest mentions any of the four stems, except the changelog, which names each in backticks and not as a link.
    - The docs changelog has one new line per retired file, each with a reason that names what supersedes it.
    - Every manifest doc path resolves and every file under `docs/` has exactly one entry.
    - The Lighthouse baseline still has a "Two follow-ons" block with two entries, neither of which names a deleted file.
    - The four locked ADR files still exist and the manifest still carries exactly four locked markers.
  </acceptance_criteria>
  <verify>
    <automated>RC=0; for f in admin-dashboard-specification mobile-improvements-actionable mobile-testing-automation mobile-ux-assessment; do test ! -f "docs/$f.md" || { echo "STILL PRESENT: $f"; RC=1; }; H=$(/usr/bin/grep -rn "$f" README.md docs AGENTS.md CONTRIBUTING.md SECURITY.md gsd-ingest-manifest.yaml 2>/dev/null | /usr/bin/grep -v '^docs/CHANGELOG-docs\.md:'); test -z "$H" || { echo "DEAD REF $f:"; echo "$H"; RC=1; }; /usr/bin/grep -qF "$f.md" docs/CHANGELOG-docs.md || { echo "NO CHANGELOG LINE: $f"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>"STILL PRESENT:" prints, meaning a file was de-linked but never deleted; or "DEAD REF" prints, meaning a live surface still points at a deleted path; or "NO CHANGELOG LINE:" prints, meaning a retirement happened with no written reason, which is the empty-case the changelog exists to close.</fails_when>
    <automated>RC=0; for p in $(/usr/bin/grep -oE 'path: docs/[A-Za-z0-9_.-]+\.md' gsd-ingest-manifest.yaml | /usr/bin/sed 's/^path: //'); do test -f "$p" || { echo "MANIFEST DANGLES: $p"; RC=1; }; done; for p in docs/*.md; do /usr/bin/grep -qF "path: $p" gsd-ingest-manifest.yaml || { echo "UNREGISTERED: $p"; RC=1; }; done; L=$(/usr/bin/grep -v '^[[:space:]]*#' gsd-ingest-manifest.yaml | /usr/bin/grep -c 'locked: true'); test "$L" -eq 4 || { echo "LOCKED COUNT: $L"; RC=1; }; for p in docs/checkout-trust-boundary.md docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md; do test -f "$p" || { echo "LOCKED ADR MISSING: $p"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>"MANIFEST DANGLES:" or "UNREGISTERED:" prints, meaning the manifest and the tree disagree after four removals; or "LOCKED COUNT:" is not 4 or "LOCKED ADR MISSING:" prints, meaning a binding decision document was caught in the sweep. Comment lines are stripped before counting so the banner comment cannot satisfy the gate.</fails_when>
    <automated>B=docs/mobile-lighthouse-baseline.md; /usr/bin/grep -qiF 'follow-on' "$B" && test "$(/usr/bin/grep -c '^- ' "$B")" -ge 2 && ! /usr/bin/grep -qE 'mobile-(improvements-actionable|testing-automation|ux-assessment)' "$B" && ! /usr/bin/grep -qE '\]\(\.\./\.planning' "$B"</automated>
    <fails_when>the follow-ons block or its bullets were deleted rather than repointed, losing two real open items; or the negative grep fires, meaning a sentence still names a retired document; or the last negative grep fires, meaning a published document was made to link into the planning tree.</fails_when>
  </verify>
  <done>The admin specification and the three mobile documents are gone, nothing points at them, and the Lighthouse baseline still tells a reader where the two open mobile items live.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the stale roadmap copy, the gift-cards handoff record and the migration-number ledger</name>
  <read_first>
    - `08.2-DOCS-INVENTORY.md` — the retirement queue rows for these three files and their reasons.
    - `docs/ROADMAP.md` lines 80-95 — the two lines referencing documents retired in task 1, so you know this file is both a source and a target of dead references.
    - `docs/README.md` lines 10-14, 36-40, 44-48 and 75-90 — the roadmap bullet in Getting Started, the migration-reservations bullet in Operations, the gift-cards bullet in Specs, and the "where do I look" mention of the roadmap.
    - `README.md` lines 116-124 — the roadmap bullet in the docs index.
    - `docs/runtime-configuration.md` and `docs/checkout-trust-boundary.md` — where the gift-card feature flags and the optional-capability rules actually live, so the changelog line can name the successor rather than leaving the topic unowned.
    - `migrations/0023_add_order_effects_payload.sql` header — the evidence that the ledger's own numbering promise is already broken.
  </read_first>
  <files>docs/ROADMAP.md, docs/o07-gift-cards-plan.md, docs/migration-reservations.md, docs/README.md, README.md, gsd-ingest-manifest.yaml, docs/CHANGELOG-docs.md</files>
  <reversibility rating="costly">Three documents leave the working tree. Git history keeps them; the paths and inbound links would have to be rebuilt by hand.</reversibility>
  <action>
Retire the docs-tree roadmap copy, the gift-cards completion record and the migration-number ledger,
using the same contract.

Delete each with `git rm`, remove its manifest entry, and add one changelog line naming the file in
backticks with the reason. The roadmap copy is framed around a milestone numbering that the live
planning record contradicts, and the planning tree is the roadmap now. The gift-cards file is a
completion and handoff record for a feature that shipped; its living facts — the feature flags and
the rollback order — are already owned by the runtime configuration reference and the checkout trust
boundary ADR, so name both in the changelog line. The migration ledger's own promise about the next
free number is contradicted by an applied migration, and the multi-branch coordination need it
served has ended; say that.

Then fix inbound references. The docs index loses three bullets and the roadmap mention in the
"where do I look" list; reword that sentence to point a reader at the project's planning record for
current status. The root README loses its roadmap bullet. Check for any remaining mention of the
gift-cards file or the ledger anywhere in the tree, and fix whatever the grep finds.

Commit once, with a message naming the three retired files.
  </action>
  <acceptance_criteria>
    - None of the three documents exists in the working tree.
    - No file under `docs/`, no root markdown file and not the ingest manifest mentions any of the three stems, except the changelog, which names each in backticks and not as a link.
    - The changelog line for the gift-cards record names both documents that now own its living facts.
    - The changelog line for the ledger names the applied migration that contradicts it.
    - Every manifest doc path resolves and every file under `docs/` has exactly one entry; the count of manifest doc entries equals the count of files under `docs/`.
    - The four locked ADR files still exist and the manifest still carries exactly four locked markers.
  </acceptance_criteria>
  <verify>
    <automated>RC=0; for f in ROADMAP o07-gift-cards-plan migration-reservations; do test ! -f "docs/$f.md" || { echo "STILL PRESENT: $f"; RC=1; }; H=$(/usr/bin/grep -rn "docs/$f" README.md docs AGENTS.md CONTRIBUTING.md SECURITY.md gsd-ingest-manifest.yaml 2>/dev/null | /usr/bin/grep -v '^docs/CHANGELOG-docs\.md:'); test -z "$H" || { echo "DEAD REF $f:"; echo "$H"; RC=1; }; /usr/bin/grep -qF "$f.md" docs/CHANGELOG-docs.md || { echo "NO CHANGELOG LINE: $f"; RC=1; }; done; H2=$(/usr/bin/grep -rnE '\]\((\./)?(ROADMAP|o07-gift-cards-plan|migration-reservations)\.md\)' docs README.md 2>/dev/null); test -z "$H2" || { echo "RELATIVE DEAD LINK:"; echo "$H2"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"STILL PRESENT:" prints, meaning a file was de-linked but not deleted; "DEAD REF" prints, meaning a live surface still points at a deleted path; "NO CHANGELOG LINE:" prints, meaning a retirement was recorded nowhere; or "RELATIVE DEAD LINK:" prints, meaning a same-directory link inside the docs tree survived the absolute-path sweep.</fails_when>
    <automated>RC=0; /usr/bin/grep -qF 'runtime-configuration.md' docs/CHANGELOG-docs.md || { echo "GIFT CARD SUCCESSOR UNNAMED"; RC=1; }; /usr/bin/grep -qF 'checkout-trust-boundary.md' docs/CHANGELOG-docs.md || { echo "GIFT CARD ADR UNNAMED"; RC=1; }; /usr/bin/grep -qF '0023' docs/CHANGELOG-docs.md || { echo "LEDGER EVIDENCE UNNAMED"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>any of the three markers prints, meaning a retirement line was written as a bare "removed" note without naming the successor document or the evidence that made the file wrong — which is exactly the reasonless retirement the empty-case rule forbids.</fails_when>
    <automated>RC=0; for p in $(/usr/bin/grep -oE 'path: docs/[A-Za-z0-9_.-]+\.md' gsd-ingest-manifest.yaml | /usr/bin/sed 's/^path: //'); do test -f "$p" || { echo "MANIFEST DANGLES: $p"; RC=1; }; done; for p in docs/*.md; do /usr/bin/grep -qF "path: $p" gsd-ingest-manifest.yaml || { echo "UNREGISTERED: $p"; RC=1; }; done; N=$(/usr/bin/grep -cE '^[[:space:]]*- path: docs/' gsd-ingest-manifest.yaml); M=$(ls docs/*.md | /usr/bin/wc -l | /usr/bin/tr -d ' '); test "$N" = "$M" || { echo "COUNT MISMATCH manifest=$N docs=$M"; RC=1; }; L=$(/usr/bin/grep -v '^[[:space:]]*#' gsd-ingest-manifest.yaml | /usr/bin/grep -c 'locked: true'); test "$L" -eq 4 || { echo "LOCKED COUNT: $L"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"MANIFEST DANGLES:", "UNREGISTERED:" or "COUNT MISMATCH" prints, meaning the manifest and the tree disagree after seven total removals; or "LOCKED COUNT:" is not 4, meaning a locked marker was lost while editing neighbouring entries.</fails_when>
  </verify>
  <done>Seven of the nine retirements are done, the manifest matches the tree exactly, and every removal has a reason on the record.</done>
</task>

<task type="auto">
  <name>Task 3: Turn the checks into scripts/docs-lint.mjs and prove it fails on a broken probe</name>
  <read_first>
    - `scripts/scan-hardcoded-colors.mjs` lines 1-120 — the zero-dependency convention to copy exactly: the shebang, the JSDoc block naming the phase that gates on it, `node:fs` and `node:path` imports only, the violation-collection shape, the printed report, and the non-zero exit.
    - `package.json` scripts block — where a new entry goes and the surrounding formatting.
    - `08.2-RESEARCH.md` "Style Contract Check Method" — the four grep-based checks this script replaces, and the note that no markdown linter exists in the project's dependencies.
    - `08.2-RESEARCH.md` "Security Domain" — the credential shapes the scan must reject.
    - `gsd-ingest-manifest.yaml` — the entry shape the manifest check parses.
  </read_first>
  <files>scripts/docs-lint.mjs, package.json</files>
  <action>
Write a zero-dependency documentation validator and wire it to a script name. Import nothing but
`node:fs` and `node:path`. Add no dependency and no devDependency. Follow the colour scanner's
structure: a JSDoc header naming this phase and what the script gates, a set of check functions each
returning violations, one report at the end, exit 1 when the list is non-empty and exit 0 when it is
empty.

The scan surface is every file under `docs/` plus the root README, the contributing guide and the
security policy. The docs changelog is excluded from the dead-reference and link checks — naming
retired files is its entire job — but is included in the header-convention check.

Implement seven checks:

1. Documentation references resolve. For every mention of a path under the docs directory in the
   scanned files and in the ingest manifest, assert the file exists.
2. Relative markdown links resolve. For every markdown link target inside a docs file that is a
   relative path to a markdown file, resolve it against that file's directory and assert it exists.
   Do the same for relative links in the scanned root files.
3. Manifest reconciliation, both directions. Every path entry in the ingest manifest names a file
   that exists, every markdown file under the docs directory has exactly one entry, and the two
   counts are equal.
4. Locked ADR guard. The four locked ADR paths exist, each is listed in the manifest, and the count
   of locked markers is exactly four, counted after comment lines are stripped so the manifest's own
   explanatory comment cannot satisfy the check.
5. Script names resolve. Every `npm run` invocation mentioned in a scanned file names a script that
   exists in the package manifest.
6. Credential shapes. Reject a hexadecimal identifier in the eight-four-four-four-twelve shape, and
   reject a Stripe-style key prefix followed by twenty or more token characters. Labeled placeholders
   that end in an obviously fake word are the intended style and must pass.
7. Header convention. Every markdown file directly under the docs directory begins with a
   level-one heading on its first line.

Each violation prints one line: the check name, the file, the line number where it is available, and
what is wrong. End with a count and a one-line pass or fail statement.

Add a script entry named for documentation linting to the package manifest so later plans call the
gate by name. Do not add it to the CI workflow, and do not add it to the contributing guide's gate
list, which plan 03 fixes to the locked order — this is a local gate, exactly as the token scanner
was when it was introduced. Record wiring it into CI as a follow-up in the summary.

Then prove it fails. Create a throwaway markdown file under the docs directory containing a link to a
document that does not exist, run the gate, confirm it exits non-zero and names both the broken link
and the unregistered file, then delete the probe with `/bin/rm -f` and confirm the gate exits zero.
Never leave the probe committed.
  </action>
  <acceptance_criteria>
    - The script imports only from the Node built-in filesystem and path modules and adds no dependency to the package manifest.
    - The script exits 0 against the tree as it stands at the end of this task.
    - The script exits non-zero when a docs file links to a file that does not exist.
    - All seven checks are implemented and each prints an identifiable violation line.
    - The locked-marker count is computed after comment lines are stripped.
    - A script entry for documentation linting exists in the package manifest and runs the new file.
    - The CI workflow file is unchanged and no probe file remains in the tree.
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/docs-lint.mjs && BAD=$(/usr/bin/grep -oE "^import .* from ['\"][^'\"]+['\"]" scripts/docs-lint.mjs | /usr/bin/sed -E "s/.*from ['\"]([^'\"]+)['\"]/\1/" | /usr/bin/grep -v '^node:'); test -z "$BAD" || { echo "NON-BUILTIN IMPORT: $BAD"; false; }; /usr/bin/grep -qE "from ['\"]node:fs['\"]" scripts/docs-lint.mjs && ! /usr/bin/grep -q 'require(' scripts/docs-lint.mjs && mise exec -- npm run docs:lint</automated>
    <fails_when>"NON-BUILTIN IMPORT:" prints, meaning the validator reached for a package and broke the zero-dependency constraint; or the `node:fs` grep fails, meaning it does not read the filesystem at all and cannot be checking anything; or `docs:lint` exits non-zero, meaning the tree the seven retirements left behind does not actually satisfy the gate this plan claims it satisfies.</fails_when>
    <automated>mise exec -- node -e "const p=require('./package.json'); if(!p.scripts['docs:lint']){console.log('NO SCRIPT ENTRY');process.exit(1);} if(!/docs-lint\.mjs/.test(p.scripts['docs:lint'])){console.log('WRONG TARGET');process.exit(1);} const d=Object.keys(p.dependencies||{}).length+Object.keys(p.devDependencies||{}).length; console.log('deps',d);" && test -z "$(git diff --name-only .github/workflows/ci.yml)" && test -z "$(git diff package.json | /usr/bin/grep -E '^\+[[:space:]]+\"[a-zA-Z@][^\"]*\": \"[\^~0-9]')"</automated>
    <fails_when>"NO SCRIPT ENTRY" or "WRONG TARGET" prints, meaning later plans cannot call the gate by name; or the CI workflow shows a diff, meaning this documentation plan changed CI and falsified what the docs say CI runs; or the package-manifest diff contains an added version-pinned entry, meaning a dependency was introduced.</fails_when>
    <automated>PROBE=docs/__docs-lint-probe.md; printf '# Probe\n\nSee [missing](does-not-exist-probe.md).\n' >| "$PROBE"; mise exec -- npm run docs:lint >/dev/null 2>&1; RC=$?; /bin/rm -f "$PROBE"; test "$RC" -ne 0 || { echo "GATE DID NOT FAIL ON BROKEN PROBE"; false; }; test ! -f "$PROBE" && mise exec -- npm run docs:lint</automated>
    <fails_when>"GATE DID NOT FAIL ON BROKEN PROBE" prints, meaning the validator returns success against a file that links to a document that does not exist and is absent from the manifest — a gate that passes everything; or the final run fails, meaning the probe was not cleaned up and the tree is left dirty.</fails_when>
  </verify>
  <done>Every later plan can run one named command to prove the documentation set still hangs together, and that command has been shown to fail when it should.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published documentation | Everything under `docs/` and every root markdown file is committed and readable by anyone with repository access |
| documentation registry → tooling | `gsd-ingest-manifest.yaml` is read by `/gsd-ingest-docs`; a dangling path silently breaks that tool |
| validator script → build surface | A new script in `scripts/` runs on developer machines; anything it imports becomes part of the project's supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.2-02 | Tampering | `gsd-ingest-manifest.yaml`, the four locked ADR files | high | mitigate | Both retirement tasks assert all four ADR paths exist, are listed, and that exactly four locked markers survive with comment lines stripped; task 3 makes that assertion a permanent check |
| T-08.2-03 | Spoofing | inbound documentation links | low | mitigate | Each retirement task runs a dead-reference scan over every published surface, and task 3 turns it into a repeatable gate |
| T-08.2-05 | Repudiation | undocumented removals | low | mitigate | Every retirement adds a changelog line naming the file and its successor; the verify asserts the successor is named, not just the removal |
| T-08.2-06 | Tampering | `scripts/docs-lint.mjs`, `package.json` | medium | mitigate | The validator imports only Node built-ins; the verify extracts every import source and rejects anything outside `node:`, and rejects a version-pinned addition to the package manifest diff |
| T-08.2-SC | Tampering | npm/pip/cargo installs | low | accept | No plan in this phase runs a package-manager install; `08.2-RESEARCH.md` "Package Legitimacy Audit" records zero packages installed, upgraded or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `08.2-DOCS-INVENTORY.md` | 08.2-01 | consumed, not changed |
| `docs/CHANGELOG-docs.md` | 08.2-01, appended by 08.2-02 and 08.2-03 | seven retirement entries |
| `gsd-ingest-manifest.yaml` reconciliation | 08.2-01, maintained here and in 08.2-03 | seven entries removed, invariant preserved |
| Nine retired documents | 08.2-01 (one), 08.2-02 (seven), 08.2-03 (one merged) | seven |
| `scripts/docs-lint.mjs` and `npm run docs:lint` | 08.2-02 | all of it |
| `docs/DEPLOYMENT_SETUP.md` consolidation, `CONTRIBUTING.md` gate list | 08.2-03 | — |
| `AGENTS.md`, root `CLAUDE.md`, `docs/CLAUDE.md` trim | 08.2-04 | — |
| `README.md` rewrite, `docs/images/presets/`, `docs/README.md` index | 08.2-05 | — |
| Style-contract pass and phase close-out | 08.2-06 | — |

<verification>
- Seven documents are deleted, de-linked from every published surface, removed from the manifest and recorded in the changelog.
- The manifest and the `docs/` tree agree in both directions and the four locked ADRs are untouched.
- The mobile Lighthouse baseline keeps both follow-on items and names where each is tracked.
- `npm run docs:lint` exits 0 on the tree and non-zero on a deliberately broken probe.
- Neither the CI workflow nor the project's dependency set changed.
</verification>

<success_criteria>
- The docs directory holds only files someone decided to keep, each with a recorded reason.
- A reader who wants to know what happened to a removed document finds it in one place, with its successor named.
- Plans 03 through 06 verify their documentation work with one command instead of retyped grep loops.
</success_criteria>

<output>
Create `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-02-SUMMARY.md` when done.
</output>
