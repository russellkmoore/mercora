---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["08.2-02"]
files_modified:
  - docs/DEPLOYMENT_SETUP.md
  - docs/architecture.md
  - docs/README.md
  - README.md
  - docs/CLAUDE.md
  - docs/CHANGELOG-docs.md
  - gsd-ingest-manifest.yaml
  - CONTRIBUTING.md
files_deleted:
  - docs/STRIPE_INTEGRATION.md
autonomous: true
requirements: [DOCS-04]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/DEPLOYMENT_SETUP.md` is the one setup-and-deploy runbook: it holds the procedure and links the locked ADRs and the runtime configuration reference for the rules, rather than restating them, so no locked ADR is merged away or moved (D-04, D-10). [DOCS-04]"
    - "The build-variable precedence section survives verbatim: a reader still learns that `build:worker` copies every public variable from the worker config into the build and overrides a Dashboard value of the same name, and that the production-migration gate is a Dashboard build variable only (D-04, CONTEXT reusable assets). [DOCS-04]"
    - "The Stripe guide's unique content — account setup steps and the test-card table — lives in the setup runbook, its duplicated webhook-event list is replaced by a link to the ADR that owns it, and its path is retired (D-04, adjacency edge: one owner per topic). [DOCS-04]"
    - "`docs/architecture.md` no longer carries a section its own banner marks as historical, and no longer ends in a security diagram whose boxes have no code behind them (D-05, no duplicated content). [DOCS-04]"
    - "`CONTRIBUTING.md` lists the full gate order in the sequence D-11 fixes — lint, typecheck, scan:tokens, the theme-manifest check, the test suite, the workers suite, build — instead of a three-command subset (D-11). [DOCS-04]"
    - "Every local-secrets instruction in the setup runbook names one file, and that file is the one the running dev server actually reads, confirmed against the worker dev proxy the Next config initialises (D-06 claim check). [DOCS-04]"
    - "`npm run docs:lint` exits 0 after every task in this plan, so no commit leaves a dead reference, an unregistered doc or a missing manifest entry (DOCS-04 concurrency edge). [DOCS-04]"
  artifacts:
    - path: "docs/DEPLOYMENT_SETUP.md"
      provides: "The single setup-and-deploy runbook: Cloudflare, Clerk and Stripe account setup, environment, migrations by link, build variables verbatim, deploy and go-live"
      contains: "build-with-public-env.mjs"
    - path: "docs/architecture.md"
      provides: "System overview, AI assistant architecture, data flow, component architecture and deployment pipeline, with the historical schema section and the unverifiable security diagram removed"
      contains: "System Overview"
    - path: "CONTRIBUTING.md"
      provides: "The full gate order contributors run before opening a pull request"
      contains: "scan:tokens"
  key_links:
    - from: "docs/DEPLOYMENT_SETUP.md"
      to: "docs/database-migrations.md"
      via: "the migrations step links the locked ADR for the rule instead of restating it"
      pattern: "database-migrations.md"
    - from: "docs/DEPLOYMENT_SETUP.md"
      to: "docs/webhooks-refunds-inventory.md"
      via: "the Stripe webhook step links the ADR that owns the event list"
      pattern: "webhooks-refunds-inventory.md"
    - from: "docs/DEPLOYMENT_SETUP.md"
      to: "docs/runtime-configuration.md"
      via: "the environment step links the variable contract instead of duplicating the table"
      pattern: "runtime-configuration.md"
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/ci.yml"
      via: "the listed gate order is the workflow's step order"
      pattern: "scan:tokens"
  prohibitions:
    - statement: "This phase must NOT delete, rename or move any of the four locked ADR files."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from D-10; binds hardest here because this is the consolidation plan, and D-04's own example phrasing reads as an invitation to fold the migrations ADR into the setup runbook."
    - statement: "Documentation must NOT print a secret value, a real Cloudflare resource identifier, or the admin development bypass literal."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less security prohibition; binds here because absorbing account-setup steps is the moment a real key or identifier is most likely to be pasted in as an example."
    - statement: "This phase must NOT change application code; the only permitted non-documentation change is the zero-dependency docs-lint script and its npm script entry, both already written."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from the CONTEXT.md phase boundary; binds here because correcting a wrong instruction can tempt an executor to change the thing the instruction describes."
    - statement: "The documentation gate must NOT be wired into CI in this phase, and must NOT be added to the contributing guide's fixed gate list."
      status: unresolved
      verification: judgment
      reason: "D-11 fixes the gate list to a specific seven-command order; adding an eighth would contradict the decision this task implements."
---

<objective>
Make `docs/DEPLOYMENT_SETUP.md` the one setup-and-deploy runbook, retire the Stripe guide into it,
cut the two sections of `docs/architecture.md` that describe things that are not true, and give
`CONTRIBUTING.md` the real gate list.

Purpose: DOCS-04's consolidation half. D-04 asks for merges to be executed rather than proposed;
D-10 says the consolidation is trim-and-link because three of the candidates are locked ADRs or a
referenced contract. This plan does the executable part of that and stops there.

Output: a trimmed and linked setup runbook that absorbs the Stripe guide's unique content, one more
retirement recorded in the changelog and the manifest, an architecture document with no historical
section and no unverifiable diagram, and a contributing guide whose gate list matches CI.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-CONTEXT.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-PATTERNS.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-02-SUMMARY.md
</context>

<interfaces>
Consumed from plans 01 and 02, unchanged: the retirement contract, the manifest invariant, the topic
map in `08.2-DOCS-INVENTORY.md` (which names the refunds and webhooks ADR as the sole owner of the
Stripe event list), and `npm run docs:lint`, which every task here runs as its own gate.

Produced here for later plans:

- The final surviving document set. Plan 05 builds the docs index from exactly the files that exist
  after this plan; nothing is retired after it.
- `docs/DEPLOYMENT_SETUP.md` as the single deployment link target. `AGENTS.md` in plan 04 points its
  setup and deploy topics here, and nowhere else.
- `CONTRIBUTING.md`'s gate list, which `AGENTS.md` in plan 04 must match command for command.

Nothing in this plan exports application code or changes a runtime contract.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`. Bare `node` is v26.
- The shell has `noclobber` and an interactive `rm` alias. Delete tracked files with `git rm`. Write
  commit messages to a `mktemp` file and use `git commit -F`.
- Run `mise exec -- npm run docs:lint` before every commit. It is the gate plan 02 built and it will
  catch a dead link the moment a retirement is half done.
- **The local-secrets file is `.dev.vars`, and that is decided, not open.** `docs/runtime-configuration.md`
  already states it, `.gitignore` ignores `.dev.vars*`, and `next.config.ts` calls the OpenNext
  Cloudflare dev initialiser, which is what supplies worker bindings and reads that file during
  `next dev`. Read the initialiser call yourself so you can describe the mechanism accurately, then
  name exactly one local-secrets file in the runbook. Do not document two conventions for the same
  thing, and do not reintroduce the older filename anywhere in the file.
- The build-variable section is the single most valuable block in the file. Trim around it, never
  through it.
- Absorb the test-card table as documentation values; they are Stripe's published test numbers, not
  credentials.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Trim the deployment runbook to what is true here, and link out for every rule someone else owns</name>
  <read_first>
    - `docs/DEPLOYMENT_SETUP.md` — the whole file. You need every section heading, the resource-creation block, the environment sections, the migrations section, the build-variable section, and the four sections being cut.
    - `docs/runtime-configuration.md` — the variable contract table this file must link to instead of duplicating.
    - `docs/database-migrations.md` — the locked ADR that owns the migrations rule; the runbook links it, never absorbs it.
    - `docs/checkout-trust-boundary.md` and `docs/webhooks-refunds-inventory.md` — the two locked ADRs that own checkout and webhook behaviour.
    - `next.config.ts` lines 60-70 and `.gitignore` lines 25-50 — the dev initialiser and the ignored local-variable filenames, so the local-secrets instruction names the file the running dev server actually reads.
    - `package.json` scripts — the real bodies of the deploy, CI deploy and worker build commands, so every command in the runbook is one that exists.
  </read_first>
  <files>docs/DEPLOYMENT_SETUP.md</files>
  <action>
Cut what is generic, correct what is wrong, and link what someone else owns. Do not restructure the
numbered section sequence; a reader who has used this file before should still recognise it.

Cut four sections outright: monitoring and maintenance, the generic security checklist, the generic
troubleshooting list, and support resources. None of them says anything specific to this repository;
all four are the decorative filler D-05 rules out. If a single line inside one of them is genuinely
specific, move that line into the section it belongs to before cutting the rest.

Correct the environment instructions. Every place the file tells a reader where to put a local secret
must name one file, and it must be the file the dev server actually reads — confirm it against the
dev initialiser call in the Next config and the ignore rules before writing. Where the file lists
individual variables, replace the list with a link to the runtime configuration reference, which owns
that contract, and keep only the handful of values a first-time reader must set to boot: the Clerk
publishable and secret keys, the Stripe publishable and secret keys, and the Stripe webhook signing
secret. Keep every example value an obviously fake placeholder.

Replace restated rules with links. The migrations section keeps its commands but states the rule in
one sentence and links the migrations ADR for the reasoning. The Stripe webhook section keeps the
Dashboard steps but links the refunds and webhooks ADR for the event list rather than listing events
here — that list has exactly one owner. The checkout description, if any survives, links the checkout
trust boundary ADR.

Leave the build-variable section untouched. Its table, its precedence explanation and its checklist
stay exactly as they are, including the sentence naming the public-variable build script and the
sentence about the production-migration gate being a Dashboard build variable only.

Apply the header convention on the way past: a level-one title, a one-line purpose sentence, and a
status line. Drop decorative emoji from headings.
  </action>
  <acceptance_criteria>
    - The four generic sections no longer exist in the file.
    - The build-variable section is byte-identical to its previous state.
    - Exactly one local-secrets filename appears in the file, and it matches what the dev initialiser reads.
    - The runtime configuration reference, the migrations ADR and the refunds and webhooks ADR are each linked at least once.
    - No Stripe webhook event name is listed in this file.
    - Every command in the file names a script that exists in the package manifest.
    - The file opens with a level-one title followed by a purpose sentence and a status line.
    - `npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>D=docs/DEPLOYMENT_SETUP.md; RC=0; for s in 'Monitoring' 'Security Checklist' 'Troubleshooting' 'Support Resources'; do /usr/bin/grep -q "^#.*$s" "$D" && { echo "SECTION SURVIVED: $s"; RC=1; }; done; for s in 'runtime-configuration.md' 'database-migrations.md' 'webhooks-refunds-inventory.md' 'build-with-public-env.mjs' 'MERCORA_ALLOW_PRODUCTION_MIGRATIONS'; do /usr/bin/grep -qF "$s" "$D" || { echo "MISSING: $s"; RC=1; }; done; /usr/bin/head -1 "$D" | /usr/bin/grep -q '^# ' || { echo "NO TITLE"; RC=1; }; /usr/bin/grep -q '^\*\*Status:\*\*' "$D" || { echo "NO STATUS LINE"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"SECTION SURVIVED:" prints, meaning generic filler was left in place; or "MISSING:" prints, meaning a link-out target or the load-bearing build-variable text was dropped while trimming around it; or the title or status line check fails, meaning the header convention was not applied.</fails_when>
    <automated>D=docs/DEPLOYMENT_SETUP.md; RC=0; H=$(/usr/bin/grep -noE 'payment_intent\.[a-z_.]+|charge\.[a-z_.]+|checkout\.session\.[a-z_.]+' "$D"); test -z "$H" || { echo "EVENT LIST DUPLICATED:"; echo "$H"; RC=1; }; N=$(/usr/bin/grep -coE '\.env[a-z.]*\.local' "$D"); test "$N" -eq 0 || { echo "STALE LOCAL ENV MENTIONS: $N"; RC=1; }; /usr/bin/grep -qF '.dev.vars' "$D" || { echo "NO LOCAL SECRETS FILE NAMED"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"EVENT LIST DUPLICATED:" prints, meaning a Stripe event name is still restated in a file that is not the ADR that owns it; or "STALE LOCAL ENV MENTIONS:" reports a non-zero count, meaning the old local-variables filename survived the correction; or "NO LOCAL SECRETS FILE NAMED" prints, meaning the instruction was deleted rather than corrected and a reader now has nowhere to put a key.</fails_when>
    <automated>RC=0; for s in $(/usr/bin/grep -ohE 'npm run [a-zA-Z:_-]+' docs/DEPLOYMENT_SETUP.md | /usr/bin/awk '{print $3}' | /usr/bin/sort -u); do mise exec -- node -e "const p=require('./package.json'); process.exit(p.scripts['$s']?0:1)" || { echo "UNKNOWN SCRIPT: $s"; RC=1; }; done; test "$RC" -eq 0 && mise exec -- npm run docs:lint</automated>
    <fails_when>"UNKNOWN SCRIPT:" prints, meaning the runbook tells a reader to run something that does not exist; or `docs:lint` exits non-zero, meaning the trim broke a link, a header or the manifest agreement.</fails_when>
  </verify>
  <done>The deployment runbook says only what is true of this repository, states each rule once, and links the document that owns every rule it does not own itself.</done>
</task>

<task type="auto">
  <name>Task 2: Absorb the Stripe guide's unique content into the runbook and retire its path</name>
  <read_first>
    - `docs/STRIPE_INTEGRATION.md` — the whole file. You are keeping the account-setup steps, the tax-enablement steps, the test-card table and the test tax addresses; everything else duplicates the two ADRs or the runbook.
    - `docs/webhooks-refunds-inventory.md` — the ADR that owns the webhook event list, so you link instead of copying.
    - `docs/checkout-trust-boundary.md` — the ADR that owns the payment-intent and finalisation behaviour the Stripe guide describes in prose.
    - `docs/DEPLOYMENT_SETUP.md` Stripe section as it stands after task 1 — the insertion point.
    - `docs/README.md`, `README.md` and `docs/CLAUDE.md` — the three surfaces that link the Stripe guide today.
    - `08.2-DOCS-INVENTORY.md` — the merge row and the topic map's owner for the webhook event list.
  </read_first>
  <files>docs/DEPLOYMENT_SETUP.md, docs/STRIPE_INTEGRATION.md, docs/README.md, README.md, docs/CLAUDE.md, gsd-ingest-manifest.yaml, docs/CHANGELOG-docs.md</files>
  <reversibility rating="costly">The Stripe guide leaves the working tree once its unique content has been moved. Git history keeps the original; recovering it would mean rebuilding the path and its inbound links by hand.</reversibility>
  <action>
Move what only the Stripe guide has, then retire it in the same commit as the move.

Into the runbook's Stripe section, add: the account-creation and API-key steps, the tax-enablement
steps, the test-card table and the test tax addresses. Keep the table shape; these are Stripe's
published test values and belong in a setup document. Fold the steps into the runbook's existing
numbered style rather than pasting a second heading hierarchy underneath it.

Do not move: the API-endpoint descriptions, the component descriptions, the webhook-event list, the
error-handling notes or the file-structure tree. The endpoints and components are described by the
code; the event list is owned by the refunds and webhooks ADR; the payment behaviour is owned by the
checkout trust boundary ADR. Where the runbook's Stripe section needs any of those, add a link.

Then retire the guide with the contract from plan 01: delete it with `git rm`, remove its manifest
entry, add one changelog line naming it in backticks and saying its unique content moved into the
runbook and its duplicated content is owned by the two ADRs. Fix its three inbound links: the docs
index bullet in the business group, the root README bullet in the docs index, and the bullet in the
AI-context document's important-files list. In each case point the reader at the runbook rather than
deleting the entry, since the topic still exists.

Commit once. After this commit no document is retired again in this phase.
  </action>
  <acceptance_criteria>
    - The Stripe guide no longer exists and nothing outside the changelog mentions its stem.
    - The runbook contains the test-card table, the test tax addresses and the account-setup steps.
    - The runbook contains no Stripe webhook event name; it links the ADR that owns them.
    - The changelog line names both the runbook and the two ADRs.
    - Every manifest doc path resolves, every file under `docs/` has exactly one entry, and the counts are equal.
    - The four locked ADR files still exist and the manifest still carries exactly four locked markers.
    - `npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f docs/STRIPE_INTEGRATION.md && H=$(/usr/bin/grep -rn 'STRIPE_INTEGRATION' README.md docs AGENTS.md CONTRIBUTING.md SECURITY.md gsd-ingest-manifest.yaml 2>/dev/null | /usr/bin/grep -v '^docs/CHANGELOG-docs\.md:'); test -z "$H" || { echo "DEAD REF:"; echo "$H"; false; }; /usr/bin/grep -qF 'STRIPE_INTEGRATION.md' docs/CHANGELOG-docs.md && /usr/bin/grep -qF 'DEPLOYMENT_SETUP.md' docs/CHANGELOG-docs.md && /usr/bin/grep -qF 'webhooks-refunds-inventory.md' docs/CHANGELOG-docs.md</automated>
    <fails_when>the file still exists or "DEAD REF:" prints, meaning a surface still points at the retired guide; or a changelog grep fails, meaning the merge was recorded without naming where the content went or which ADR owns what was dropped.</fails_when>
    <automated>D=docs/DEPLOYMENT_SETUP.md; RC=0; for s in '4242' 'Stripe Tax'; do /usr/bin/grep -qF "$s" "$D" || { echo "UNIQUE CONTENT LOST: $s"; RC=1; }; done; H=$(/usr/bin/grep -noE 'payment_intent\.[a-z_.]+|charge\.[a-z_.]+|checkout\.session\.[a-z_.]+' "$D"); test -z "$H" || { echo "EVENT LIST DUPLICATED:"; echo "$H"; RC=1; }; /usr/bin/grep -qF 'webhooks-refunds-inventory.md' "$D" || { echo "NO ADR LINK"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"UNIQUE CONTENT LOST:" prints, meaning the merge deleted content instead of moving it — the test-card table and the tax-setup steps were the entire reason this was a merge and not a straight retirement; or "EVENT LIST DUPLICATED:" prints, meaning the copy pulled in the event names the ADR owns; or "NO ADR LINK" prints, meaning the topic was left unowned.</fails_when>
    <automated>RC=0; for p in $(/usr/bin/grep -oE 'path: docs/[A-Za-z0-9_.-]+\.md' gsd-ingest-manifest.yaml | /usr/bin/sed 's/^path: //'); do test -f "$p" || { echo "MANIFEST DANGLES: $p"; RC=1; }; done; for p in docs/*.md; do /usr/bin/grep -qF "path: $p" gsd-ingest-manifest.yaml || { echo "UNREGISTERED: $p"; RC=1; }; done; N=$(/usr/bin/grep -cE '^[[:space:]]*- path: docs/' gsd-ingest-manifest.yaml); M=$(ls docs/*.md | /usr/bin/wc -l | /usr/bin/tr -d ' '); test "$N" = "$M" || { echo "COUNT MISMATCH manifest=$N docs=$M"; RC=1; }; L=$(/usr/bin/grep -v '^[[:space:]]*#' gsd-ingest-manifest.yaml | /usr/bin/grep -c 'locked: true'); test "$L" -eq 4 || { echo "LOCKED COUNT: $L"; RC=1; }; test "$RC" -eq 0 && mise exec -- npm run docs:lint</automated>
    <fails_when>any of "MANIFEST DANGLES:", "UNREGISTERED:", "COUNT MISMATCH" or a locked count other than 4 prints, meaning the ninth and last retirement broke the registry invariant; or `docs:lint` exits non-zero, meaning a link, header or script reference broke.</fails_when>
  </verify>
  <done>Nine documents have been retired in total, the setup runbook owns the Stripe setup procedure, and the two ADRs remain the only place the payment and webhook rules are written down.</done>
</task>

<task type="auto">
  <name>Task 3: Cut the architecture document's historical and unverifiable sections, and fix the contributing gate list</name>
  <read_first>
    - `docs/architecture.md` lines 210-330 — the database schema section and the banner that already declares it historical, so you cut the section rather than caveat it further.
    - `docs/architecture.md` lines 495-597 — the closing security section and its diagram, whose boxes name controls that do not exist as code here.
    - `docs/architecture.md` lines 1-80 — the system overview, which stays, and the header shape to normalise.
    - `lib/db/schema/` directory listing and `migrations/` listing — the real current schema, so the replacement pointer names where a reader should actually look.
    - `lib/security-headers.ts` — what security this repository does implement in code, so the replacement paragraph describes something real or says nothing.
    - `CONTRIBUTING.md` "Development Workflow" step 6 — the three-command block being replaced, and the surrounding numbered-list prose that stays unchanged.
    - `08.2-CONTEXT.md` D-11 — the exact gate order this task writes, which is the decision, not CI's full step list.
    - `.github/workflows/ci.yml` — the workflow's step order, so the written order is consistent with it.
  </read_first>
  <files>docs/architecture.md, CONTRIBUTING.md</files>
  <action>
Two independent edits, one commit each is fine.

In the architecture document, delete the database schema section entirely, including its diagram and
its historical banner. Replace it with two sentences naming the schema modules directory and the
migrations directory as the current source of truth, and linking the migrations ADR for the
expand-only rule. A section that has been marked historical for a milestone is not being maintained;
carrying it forward with a warning label is worse than removing it, because a reader still has to
read it before learning it is wrong.

Then delete the closing security section's diagram. It depicts controls that do not exist in this
codebase, and its twin in the document retired in plan 01 was already flagged as duplicated,
unverifiable content. Replace it with a short paragraph describing the security surfaces that do
exist, naming the module that builds the response security headers and linking the admin
authentication document and the checkout trust boundary ADR. If you cannot name a file for a claim,
do not make the claim.

Normalise the document's header on the way past: a level-one title, a one-line purpose, a status
line. Leave the system overview, AI assistant architecture, data flow, component architecture and
deployment pipeline sections alone except for removing decorative emoji from their headings.

In the contributing guide, replace the three-command block in the development workflow with the full
gate order D-11 fixes: lint, typecheck, the token scan, the theme-manifest check, the test suite, the
workers suite, and build — in that order, as one fenced block, each on its own line. Keep the
numbered list, its wording and the rest of the file unchanged. Do not add the documentation gate to
this list; it is a local check and the fixed order has seven commands.
  </action>
  <acceptance_criteria>
    - The architecture document contains no historical banner and no entity-relationship diagram.
    - The architecture document contains no security diagram naming a control with no file behind it.
    - The replacement text names the schema directory, the migrations directory and the security headers module.
    - The architecture document opens with a level-one title, a purpose sentence and a status line.
    - The contributing guide's gate block contains exactly the seven commands in the fixed order.
    - The contributing guide's numbered-list structure and surrounding prose are otherwise unchanged.
    - `npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>A=docs/architecture.md; RC=0; /usr/bin/grep -qi 'Status: Historical' "$A" && { echo "HISTORICAL BANNER SURVIVED"; RC=1; }; /usr/bin/grep -qE 'erDiagram|CHAT_SESSIONS' "$A" && { echo "SCHEMA DIAGRAM SURVIVED"; RC=1; }; /usr/bin/grep -qiE 'WAF|Bot Management' "$A" && { echo "UNVERIFIABLE SECURITY BOX SURVIVED"; RC=1; }; for s in 'lib/db/schema' 'migrations/' 'lib/security-headers.ts' 'database-migrations.md'; do /usr/bin/grep -qF "$s" "$A" || { echo "POINTER MISSING: $s"; RC=1; }; done; /usr/bin/head -1 "$A" | /usr/bin/grep -q '^# ' || { echo "NO TITLE"; RC=1; }; /usr/bin/grep -q '^\*\*Status:\*\*' "$A" || { echo "NO STATUS LINE"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"HISTORICAL BANNER SURVIVED" or "SCHEMA DIAGRAM SURVIVED" prints, meaning the section was caveated again instead of removed; or "UNVERIFIABLE SECURITY BOX SURVIVED" prints, meaning the decorative diagram is still there; or "POINTER MISSING:" prints, meaning the removal left a reader with nowhere to look for the real schema or the real security surface.</fails_when>
    <automated>C=CONTRIBUTING.md; RC=0; for s in 'npm run lint' 'npm run typecheck' 'npm run scan:tokens' 'npm run build:themes:check' 'npm test' 'npm run test:workers' 'npm run build'; do /usr/bin/grep -qF "$s" "$C" || { echo "GATE MISSING: $s"; RC=1; }; done; /usr/bin/grep -qF 'docs:lint' "$C" && { echo "EXTRA GATE ADDED"; RC=1; }; L=$(/usr/bin/grep -c '^[0-9]\.' "$C"); test "$L" -ge 7 || { echo "NUMBERED LIST DAMAGED: $L"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"GATE MISSING:" prints, meaning the gate list is still a subset and a contributor would open a pull request that CI rejects; or "EXTRA GATE ADDED" prints, meaning the fixed seven-command order gained an eighth entry the decision does not include; or "NUMBERED LIST DAMAGED:" prints, meaning the edit reflowed the surrounding workflow list instead of replacing one code block.</fails_when>
    <automated>RC=0; for s in $(/usr/bin/grep -ohE 'npm run [a-zA-Z:_-]+' CONTRIBUTING.md docs/architecture.md | /usr/bin/awk '{print $3}' | /usr/bin/sort -u); do mise exec -- node -e "const p=require('./package.json'); process.exit(p.scripts['$s']?0:1)" || { echo "UNKNOWN SCRIPT: $s"; RC=1; }; done; test "$RC" -eq 0 && mise exec -- npm run docs:lint && mise exec -- npm run lint && mise exec -- npm run typecheck</automated>
    <fails_when>"UNKNOWN SCRIPT:" prints, meaning a documented gate command does not exist; or `docs:lint` fails, meaning a link or header broke; or lint or typecheck fails, meaning this documentation plan touched application code, which it is prohibited from doing.</fails_when>
  </verify>
  <done>The architecture document describes only things a reader can find in the code, and a contributor running the documented gates sees what CI sees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published documentation | Everything under `docs/` and every root markdown file is committed and readable by anyone with repository access |
| documentation → reader action | A setup runbook is executed literally; a wrong command or a wrong file name changes production configuration incorrectly |
| documentation registry → tooling | `gsd-ingest-manifest.yaml` is read by `/gsd-ingest-docs`; a dangling path silently breaks that tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.2-01 | Information Disclosure | `docs/DEPLOYMENT_SETUP.md` | high | mitigate | Every absorbed example stays an obviously fake placeholder; the docs gate rejects hexadecimal identifier shapes and key prefixes followed by a long token on every commit in this plan |
| T-08.2-02 | Tampering | the four locked ADR files, `gsd-ingest-manifest.yaml` | high | mitigate | Consolidation is trim-and-link by construction: the runbook links the migrations, checkout and webhooks ADRs and absorbs none of them; each task asserts all four paths exist and exactly four locked markers survive |
| T-08.2-04 | Tampering | reader following the runbook | high | mitigate | The local-secrets filename is confirmed against the dev initialiser before it is written, every command is asserted to exist in the package manifest, and the CI-only deploy path keeps its warning |
| T-08.2-05 | Repudiation | undocumented consolidation | low | mitigate | The changelog entry names the merge target and the two ADRs that own what was not moved |
| T-08.2-07 | Spoofing | an architecture diagram depicting controls that do not exist | medium | mitigate | The unverifiable security diagram is removed and replaced with a paragraph that names a real module, so nobody infers a control this repository does not have |
| T-08.2-SC | Tampering | npm/pip/cargo installs | low | accept | No plan in this phase runs a package-manager install; `08.2-RESEARCH.md` "Package Legitimacy Audit" records zero packages installed, upgraded or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `08.2-DOCS-INVENTORY.md` | 08.2-01 | consumed, not changed |
| `docs/CHANGELOG-docs.md` | 08.2-01, appended by 08.2-02 and 08.2-03 | the merge entry |
| `gsd-ingest-manifest.yaml` reconciliation | 08.2-01, maintained by 08.2-02 and 08.2-03 | the last entry removal |
| Nine retired documents | 08.2-01 (one), 08.2-02 (seven), 08.2-03 (one merged) | the ninth |
| `scripts/docs-lint.mjs` and `npm run docs:lint` | 08.2-02 | run as a gate, not changed |
| `docs/DEPLOYMENT_SETUP.md` consolidation | 08.2-03 | all of it |
| `docs/architecture.md` trim | 08.2-03 | all of it |
| `CONTRIBUTING.md` gate list | 08.2-03 | all of it |
| `AGENTS.md`, root `CLAUDE.md`, `docs/CLAUDE.md` trim | 08.2-04 | — |
| `README.md` rewrite, `docs/images/presets/`, `docs/README.md` index | 08.2-05 | — |
| Style-contract pass and phase close-out | 08.2-06 | — |

<verification>
- The setup runbook holds the procedure, links every rule it does not own, and keeps the build-variable section verbatim.
- The Stripe guide's unique content survives inside the runbook and its path is retired with the registry still consistent.
- The architecture document carries no historical section and no diagram without a file behind it.
- The contributing guide lists the full fixed gate order and nothing more.
- `npm run docs:lint`, `npm run lint` and `npm run typecheck` are all green.
</verification>

<success_criteria>
- A developer setting this project up follows one document and is sent to exactly one place for each rule.
- Nothing in the documentation set describes a control, a schema or an endpoint that a reader cannot find in the code.
- The document set is final: plan 05 can build an index from it without anticipating further removals.
</success_criteria>

<output>
Create `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-03-SUMMARY.md` when done.
</output>

