---
phase: "08.2"
slug: "documentation-overhaul-agent-onboarding"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-05"
---

# Phase 8.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This phase produces no application code. Its verification mechanism is a zero-dependency Node
validator plus two-sided grep and `test -f` claim checks against the files each document describes.
The existing CI gate suite covers the one runtime risk a documentation phase carries: accidentally
touching application code.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None applicable — no application code. Verification is `scripts/docs-lint.mjs` (zero-dependency Node) plus inline two-sided grep and `test -f` checks in each task's verify block |
| **Config file** | `scripts/docs-lint.mjs` — created in plan 08.2-02 task 3. Before it exists, plans 01 and 02 use inline shell checks that require no file |
| **Quick run command** | `mise exec -- npm run docs:lint` |
| **Full suite command** | `mise exec -- npm run docs:lint && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens && mise exec -- node scripts/build-themes.mjs --check && mise exec -- npm test && mise exec -- npm run test:workers && mise exec -- npm run build` |
| **Estimated runtime** | `docs:lint` under 5 seconds; full suite about 5 minutes |

---

## Sampling Rate

- **After every task commit:** `mise exec -- npm run docs:lint`. For the three tasks that run before the validator exists (08.2-01-01, 08.2-01-02, 08.2-01-03 and 08.2-02-01, 08.2-02-02), the task's own inline dead-reference, manifest-reconciliation and locked-ADR checks are the equivalent.
- **After every plan:** `docs:lint`, `lint` and `typecheck` — the last two prove no application code drifted.
- **Before `/gsd-verify-work`:** the full suite command above must be green.
- **Max feedback latency:** 5 seconds for the documentation gate, 5 minutes for the full suite.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 08.2-01-01 | 01 | 1 | DOCS-04 | T-08.2-02 / T-08.2-03 | All four locked ADRs survive with exactly four `locked: true` markers; no surface links a deleted path | script | dead-reference scan + manifest bidirectional check + locked-ADR guard + changelog shape check (inline) | ✅ no file needed | ✅ green |
| 08.2-01-02 | 01 | 1 | DOCS-04 | — | N/A | script | per-file classification scan + topic-owner scan + framing-record scan (inline) | ✅ no file needed | ✅ green |
| 08.2-01-03 | 01 | 1 | DOCS-04 | T-08.2-01 | No document shows a live resource identifier, a real-length provider key, or the admin bypass literal | script | UUID-shape scan + key-prefix scan + bypass literal extracted from `lib/auth/admin-middleware.ts` (inline) | ✅ no file needed | ✅ green |
| 08.2-02-01 | 02 | 2 | DOCS-04 | T-08.2-02 / T-08.2-03 | Four retirements land with links, manifest and changelog in one commit; locked ADRs untouched | script | per-file dead-reference scan + manifest reconciliation + follow-ons-block check (inline) | ✅ no file needed | ✅ green |
| 08.2-02-02 | 02 | 2 | DOCS-04 | T-08.2-02 / T-08.2-05 | Three more retirements, each with a successor named in the changelog | script | dead-reference + relative-link scan + successor-named scan + manifest count equality (inline) | ✅ no file needed | ✅ green |
| 08.2-02-03 | 02 | 2 | DOCS-04 | T-08.2-06 | The validator imports only Node built-ins and adds no dependency | script | `mise exec -- npm run docs:lint` (green on tree, non-zero on a broken probe) + import-source scan + dependency-diff scan | ❌ W2 creates `scripts/docs-lint.mjs` | ✅ green |
| 08.2-03-01 | 03 | 3 | DOCS-04 | T-08.2-04 | Every documented command exists; the local-secrets file named is the one the dev proxy reads | script | section-removal scan + link-out scan + event-list negative scan + script existence loop + `npm run docs:lint` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-03-02 | 03 | 3 | DOCS-04 | T-08.2-02 / T-08.2-05 | The ninth retirement preserves the manifest invariant and the four locked ADRs | script | dead-reference scan + unique-content-preserved scan + manifest reconciliation + `npm run docs:lint` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-03-03 | 03 | 3 | DOCS-04 | T-08.2-07 | No diagram depicts a control with no file behind it | script | historical-banner and diagram negative scans + pointer-present scan + gate-list scan + `lint` + `typecheck` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-04-01 | 04 | 4 | DOCS-05 | T-08.2-01 / T-08.2-04 / T-08.2-08 / T-08.2-09 | The managed block is preserved verbatim; the guide states the three production-safety rules; no secret value appears | script | Node block-comparison against the installed Next generator + command and path presence loops + rule-presence scan + secret-shape and bypass scans | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-04-02 | 04 | 4 | DOCS-05 | T-08.2-01 | Both root files become tracked; the stale example binding block is gone | script | `git ls-files --error-unmatch` on both root files + stale-block negative scan + link-present scan + size-range check + `npm run docs:lint` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-04-03 | 04 | 4 | DOCS-05 | T-08.2-01 / T-08.2-05 | Every documented secret is one the code reads, and none is shown with a value | script | command loop + backticked-path loop + binding-name loop + secret-read-by-code loop + validation-record placeholder scan | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-05-01 | 05 | 5 | DOCS-04 | T-08.2-10 | Only the seven resting home captures are committed; no customer-data route image lands in the repository | script | per-image width, size and tracked checks + directory total + route-name negative scan + `.gitignore` diff check | ✅ `sips`, `git`, `stat` | ⬜ pending |
| 08.2-05-02 | 05 | 5 | DOCS-04 | T-08.2-01 / T-08.2-05 | No credential shape in the most-read file; preset text matches its source of truth | script | length and framing scans + per-preset synopsis extracted from `themes/*.css` and asserted present + layout-member two-sided scan + script loop + `npm run docs:lint` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-05-03 | 05 | 5 | DOCS-04 | T-08.2-03 | Every index entry resolves and every living document is listed exactly once | script | header check + per-document entry-count check + link resolution + group-order check + `npm run docs:lint` | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-06-01 | 06 | 6 | DOCS-04 | T-08.2-02 / T-08.2-05 | The style pass reaches no locked ADR and reword no quoted string | script | Node heading-decoration scan across all docs + model-identifier two-sided scan + status-code scan + sentence-length ratio + protected-file diff check | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-06-02 | 06 | 6 | DOCS-04 | T-08.2-01 / T-08.2-05 | Every claim resolves against the code; the theming document's validator strings are unchanged | script | whole-set command loop + path loop + validator-string two-sided loop + variable-read-by-code loop + value-shown negative scan | ✅ `scripts/docs-lint.mjs` | ⬜ pending |
| 08.2-06-03 | 06 | 6 | DOCS-04, DOCS-05 | T-08.2-11 | The CI-wiring follow-up is recorded in project state, not only in a summary | script | placeholder scan of this record + requirement tick and status-row checks + follow-up-in-state check + full gate suite | ✅ `scripts/docs-lint.mjs` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `scripts/docs-lint.mjs` — the phase's documentation gate, created in plan 08.2-02 task 3. It is the only new verification file this phase needs.
- [x] No test framework install required — this phase produces no application code, and the existing CI gate suite already covers the "did documentation work touch code" risk.
- [x] No `MISSING` automated references — the five tasks that run before the validator exists carry inline shell checks that depend on no file.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| A coding assistant working from a fresh clone can follow `AGENTS.md` end to end without asking for a fact the repository already holds | DOCS-05 | Completeness of an onboarding document is judged by whether a reader gets stuck, which no grep can measure. The flagged assumption for this phase is that completeness is *defined* by the fact inventory in `08.2-RESEARCH.md` section 3 (prerequisites, accounts, variables, bindings, commands, gates, rules, do-not-edit paths, docs map); the dry run is the human's check on that assumption. | Clone the repository into an empty directory. Open a coding assistant with no prior project context. Say: "Read `AGENTS.md` and help me set this project up." Follow its instructions to the point where `npm run dev` serves the storefront. Record every question it had to ask that the repository could have answered; each one is a gap in `AGENTS.md`. |
| The first `next dev` run after this phase's commits leaves the tree clean | DOCS-05 | `next dev` rewrites the managed block on start; only an actual run proves the committed block matches what it writes. Plan 08.2-04 task 1 has a machine proxy — it compares the committed block against the text the installed generator produces — but the proxy reads the generator's source, not its output. | Run `mise exec -- npm run dev`, wait for the server to be listening, stop it, then run `git status --short`. `AGENTS.md` must not appear as modified. If it does, commit the appended block with the work, as the block's own text instructs. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency under 5 seconds for the documentation gate
- [ ] Both manual verifications performed and recorded
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — plan 08.2-06 task 3 fills the status column from the six plan summaries, ticks this checklist and sets the compliance flag.
