---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 05
subsystem: docs
tags: [documentation, readme, docs-index, presets, screenshots]

requires:
  - phase: 08.2-04
    provides: "AGENTS.md as the assistant-led setup path, docs/DEPLOYMENT_SETUP.md as the single setup-and-deploy runbook, CONTRIBUTING.md's fixed gate order, and npm run docs:lint as the gate"
provides:
  - "Seven optimised preset screenshots committed under docs/images/presets/ (896KB total), each ≤1200px wide and well under the 300KB per-file budget"
  - "A rewritten, product-neutral README.md (116 lines, down from 289): themeable-storefront framing, a verified five-key quick start, a preset/layout showcase with data copied verbatim from themes/*.css and lib/layout/variants.ts, an architecture-at-a-glance list naming Cloudflare bindings, a one-section MCP mention, and a docs index that agrees with docs/README.md's grouping"
  - "A regenerated docs/README.md index (42 lines, down from ~113): four fixed groups (setup, architecture, operations, reference), alphabetical within each, one link per surviving document, the ADR manifest-lock framing sentence preserved verbatim"
affects: ["08.2-06"]

actuals:
  tokens: 7300
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Preset showcase data is never hand-written: the README's industry/synopsis cells are copied byte-for-byte from each themes/*.css file's @theme label header comment, and the layout switch table is copied byte-for-byte from lib/layout/variants.ts's enum arrays and DEFAULT_LAYOUTS object — the plan's own verify re-extracts both at check time and fails on any hand-written drift"
    - "Docs index entries are one line each — link, dash, one sentence on what the document is for, not its title — grouped setup/architecture/operations/reference and alphabetical within each group, so a new document's placement is mechanical rather than a judgment call"
    - "Preset captures are copied out of the git-ignored .screenshots/ tree and downscaled with macOS sips (no new dependency), never referenced from the ignored path directly — the same pattern 08.2-PATTERNS.md prescribed and RESEARCH.md's Pitfall 4 warned against skipping"

key-files:
  created:
    - docs/images/presets/volt-dark.png
    - docs/images/presets/luxe.png
    - docs/images/presets/midnight.png
    - docs/images/presets/clinical.png
    - docs/images/presets/retro.png
    - docs/images/presets/atelier.png
    - docs/images/presets/market.png
  modified:
    - README.md
    - docs/README.md

key-decisions:
  - "README's docs-index section links a curated subset (setup/architecture/operations/reference groups, 4-5 entries each) rather than duplicating all 20 links from docs/README.md — the plan's verify only asserts six specific required link targets (AGENTS.md, CONTRIBUTING.md, SECURITY.md, and three docs paths) resolve, and duplicating the full index would push the file toward its 250-line ceiling for no reader benefit since docs/README.md is one click away and explicitly linked as 'the full index'."
  - "Used the AGENTS.md-proven wording for the README's opening paragraph ('themeable reference storefront on Cloudflare... built to render any catalogue... sample data, not the product') rather than drafting new phrasing, so the two files a reader or an assistant sees first do not describe the project two different ways."
  - "Reference group's ADR framing sentence ('all four carry a dated Accepted status and are locked in `gsd-ingest-manifest.yaml`') is placed as a lead-in above the alphabetical entry list rather than repeated per-ADR, matching the plan's instruction to keep the sentence verbatim once rather than fragment it across four bullet descriptions."

requirements-completed: [DOCS-04]

coverage:
  - id: D1
    description: "Seven optimised preset screenshots committed under docs/images/presets/, within the width and size budget"
    verification:
      - kind: other
        ref: "task 1 <verify> automated block: sips pixelWidth ≤1200, stat size <300KB per file, directory total <1.5MB, git ls-files tracked"
        status: pass
    human_judgment: false
  - id: D2
    description: "README.md rewritten in the fixed section order with preset/layout data copied verbatim from the code"
    verification:
      - kind: other
        ref: "task 2 <verify>: length ≤250 lines, title/framing checks, per-preset synopsis byte-match against themes/*.css, layout-member/source-drift checks, required-link resolution, npm run docs:lint"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/README.md regenerated with four fixed groups, alphabetical entries, and the ADR framing sentence preserved"
    verification:
      - kind: other
        ref: "task 3 <verify>: title/status-line checks, stale-section absence, ADR framing substring, per-file index-entry-count-of-one, link resolution, group order, npm run docs:lint"
        status: pass
    human_judgment: false
  - id: D4
    description: "A visitor learns in one paragraph that Mercora renders any catalogue and the outdoor-gear content is sample data"
    verification: []
    human_judgment: true
    rationale: "Whether the opening paragraph actually reads as clear and convincing to a first-time visitor is a qualitative judgment no grep-based check captures; the automated checks confirm the required words/phrases are present but not that the framing lands well in context."

duration: 35min
completed: 2026-09-05
status: complete
---

# Phase 8.2 Plan 05: README Rewrite and Docs Index Regeneration Summary

**Rewrote README.md as a themeable-storefront overview with a verified quick start and a seven-preset screenshot showcase, and regenerated docs/README.md as a four-group living index over the document set that survived plans 01-03.**

## Performance

- **Duration:** 35 min
- **Started:** 2026-09-05T23:44:00Z
- **Completed:** 2026-09-05T00:19:00Z
- **Tasks:** 3 completed
- **Files modified:** 2 (README.md, docs/README.md), 7 created (docs/images/presets/*.png)

## Accomplishments
- Copied and downscaled the seven phase-08-a resting home captures into `docs/images/presets/`, one per preset, all under 300KB and 896KB total — well inside the 1.5MB budget
- Replaced the outdoor-gear-framed README (289 lines) with a 116-line themeable-storefront overview in the fixed section order: what it is, quick start, themes and layouts, architecture, docs, contributing/gates, license
- Regenerated `docs/README.md` (42 lines, down from ~113) into four alphabetized groups that link every surviving document exactly once

## Task Commits

1. **Task 1: Copy and downscale the seven preset captures** - `b834799` (feat)
2. **Task 2: Rewrite README.md** - `5dcabd2` (docs)
3. **Task 3: Regenerate the docs index** - `ff7d09e` (docs)

**Plan metadata:** (recorded in next commit)

## Files Created/Modified
- `docs/images/presets/{volt-dark,luxe,midnight,clinical,retro,atelier,market}.png` - Committed, downscaled (≤1200px, <300KB each) resting-home captures, one per preset, combination A
- `README.md` - Full rewrite: themeable-storefront framing, five-key quick start, preset/layout showcase with verbatim code-sourced data, architecture-at-a-glance list, one-section MCP mention, curated docs index, contributing/gates pointer, license
- `docs/README.md` - Full rewrite: setup/architecture/operations/reference groups, alphabetical within each, one-line-per-entry index, ADR manifest-lock sentence preserved verbatim

## Decisions Made
See `key-decisions` in frontmatter: (1) README's docs-index section links a curated subset rather than duplicating the full 20-entry index; (2) reused AGENTS.md's proven opening-paragraph wording for consistency; (3) the ADR framing sentence is a single lead-in above the Reference group's alphabetical list rather than repeated per entry.

## Deviations from Plan

None - plan executed exactly as written. All source captures required by the precondition (`phase-08-a-{volt-dark,luxe,midnight,clinical,retro,atelier,market}/home__1280__resting.png`) were present; no substitution or gap was needed.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

`README.md` and `docs/README.md` are final for this milestone segment; plan 06's style-contract pass checks them against the same direct-language contract applied to the remaining documents rather than rewriting either. `docs/images/presets/` is a closed, committed asset directory — no later plan adds to it. `npm run docs:lint` remains green (0 violations) after every commit in this plan.

---
*Phase: 08.2-documentation-overhaul-agent-onboarding*
*Completed: 2026-09-05*
