---
phase: 12-content-assistant-live-proof
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - data/r2/knowledge_md/gift-cards.md
  - tests/unit/data/knowledge-gift-cards.test.ts
autonomous: true
requirements: [CONTENT-01]
user_setup: []

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The committed gift-card article names all four amounts ($25, $50, $100, $200), promises delivery 'as soon as payment' completes, states the card never expires, states it is not redeemable for cash, states it is not transferable for resale, tells the shopper the checkout step where a code is redeemed, and names Account -> Gift Cards as where a balance is visible (CONTENT-01, D-08)."
    - "The committed article no longer carries the stale sixty-minute delivery-window promise (D-08)."
    - "The article's front-matter tags literally contain gift, present and voucher, because the indexer embeds the whole raw file including front matter (12-RESEARCH.md Pitfall 4, D-08)."
    - "A source-contract vitest test pins every one of those promises and fails if the article drifts."
    - "curl -sI of https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md returns 200 and its ETag equals the MD5 of the committed file, proving the live object body is the committed body (D-03)."
  artifacts:
    - "data/r2/knowledge_md/gift-cards.md — rewritten to match what ships"
    - "tests/unit/data/knowledge-gift-cards.test.ts — source-contract test, no mocks"
    - "R2 object knowledge_md/gift-cards.md in voltique-images — the only object this phase puts"
  key_links:
    - "data/r2/knowledge_md/gift-cards.md -> wrangler r2 object put --remote -> voltique-images/knowledge_md/gift-cards.md -> the public CDN hostname"
    - "the uploaded R2 object -> plan 12-04's re-index -> the knowledge-gift-cards vector Volt retrieves"
  prohibitions:
    - "No R2 object put other than knowledge_md/gift-cards.md (D-03)."
    - "No edit to any other file under data/r2/knowledge_md/."
    - "No code change under lib/**, app/**, components/**."
    - "No edit to the committed wrangler.jsonc."
    - "No secret value read, printed, or written to any artifact."
    - "No promise in the article the storefront does not keep: no scheduled delivery date, no custom amounts."
    - "No npm run deploy, deploy:ci, or any deploy* script."
    - "No git add . — stage named paths only."
---

<objective>
Make the gift-card support article describe the product that actually ships, pin those promises with a test, and put the corrected article on the public bucket where Volt's indexer reads it.

Today the article is wrong in a way a customer would notice: it promises delivery "within" a sixty-minute window when issuance is immediate on payment, it never explains how to redeem a code at checkout, it never says where to see a balance, and its tags do not carry the words a shopper actually types. Per D-08 all of that is corrected in one rewrite.

Purpose: CONTENT-01, and the input plan 12-04 re-indexes.
Output: a rewritten committed article, a source-contract test that fails if it drifts, and a matching live R2 object.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-content-assistant-live-proof/12-CONTEXT.md
@.planning/phases/12-content-assistant-live-proof/12-PATTERNS.md
@.planning/phases/12-content-assistant-live-proof/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the failing source-contract test that pins the article's promises</name>
  <files>tests/unit/data/knowledge-gift-cards.test.ts</files>
  <read_first>
    - tests/unit/data/seed-gift-card.test.ts (the analog: `REPO_ROOT`, `readRepoFile`, real-file assertions, no mocks — copy its helper shape verbatim)
    - data/r2/knowledge_md/gift-cards.md (the current, wrong article)
    - data/r2/knowledge_md/returns.md and data/r2/knowledge_md/faq.md (the front-matter shape the indexer sees)
    - .planning/phases/12-content-assistant-live-proof/12-PATTERNS.md §"`tests/unit/data/knowledge-gift-cards.test.ts` (test, source-contract)"
    - AGENTS.md
  </read_first>
  <action>
Create `tests/unit/data/knowledge-gift-cards.test.ts`. Copy `tests/unit/data/seed-gift-card.test.ts`'s helper shape verbatim — `REPO_ROOT` resolved with `path.resolve(__dirname, "../../..")`, a `readRepoFile` wrapper around `readFileSync`, `describe`/`it` from vitest, no mocking and no fixtures. Read the real committed article and assert on its text.

Write it now, against the article as it will be after Task 2, so it fails on this commit. That failure is the point: it proves the test actually discriminates rather than passing vacuously against whatever happens to be on disk.

Assertions to include, one `it` block each:

<!-- planner-discipline-allow: 1 hour -->
- The stale delivery-window promise is gone: `expect(content).not.toMatch(/1 hour/i)`.
- All four amounts appear: `$25`, `$50`, `$100`, `$200`.
- Delivery is tied to payment completing, not to a clock: assert a case-insensitive match on `as soon as payment`.
- No expiry: a case-insensitive match on `never expire`.
- No cash redemption: a case-insensitive match on `cash`.
- Not transferable for resale: a case-insensitive match on `resold` or `resale`.
- The checkout redemption step is described: assert the text names the checkout field a code is entered into.
- The balance location is named: assert the text contains both `Account` and `Gift Cards`.
- Front-matter tags carry the retrieval vocabulary: three assertions matching the `tags:` line against the standalone words gift, present and voucher, in the style 12-PATTERNS.md records (`/tags:.*\bgift\b/i` and siblings). This matters because the indexer embeds the entire raw file, front matter included, with no YAML parsing (12-RESEARCH.md Pitfall 4).
- Nothing is promised that the storefront does not do: assert the text does not mention a scheduled or future send date, guarding against re-introducing SHOP-08's deferred behaviour as a promise.

Add a file-header comment explaining why the test reads the real file rather than a fixture: this article is uploaded verbatim to R2 and embedded verbatim into Volt's index, so the committed bytes are the contract.
  </action>
  <acceptance_criteria>
    - `tests/unit/data/knowledge-gift-cards.test.ts` exists and is git-tracked.
    - `mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts` exits nonzero at this task, with failures naming the missing promises.
    - The test file imports nothing from `lib/`, `app/` or `components/` and registers no mock.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts; test $? -ne 0 &amp;&amp; echo RED_AS_EXPECTED</automated>
    <fails_when>`RED_AS_EXPECTED` is not printed — the test passed against the current, known-wrong article, which means its assertions are too loose to discriminate and it would not catch future drift either.</fails_when>
    <automated>grep -cE "vi\.mock|vi\.fn|from \"@/(lib|app|components)" tests/unit/data/knowledge-gift-cards.test.ts</automated>
    <fails_when>The count is not 0 — the test reaches into application code or mocks something, when its whole job is to assert on committed bytes.</fails_when>
  </verify>
  <done>The test file exists, reads the real article, and fails today for exactly the reasons Task 2 will fix.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the article to match what ships</name>
  <files>data/r2/knowledge_md/gift-cards.md</files>
  <read_first>
    - data/r2/knowledge_md/gift-cards.md (the file being rewritten)
    - tests/unit/data/knowledge-gift-cards.test.ts (from Task 1 — the contract to satisfy)
    - data/r2/knowledge_md/returns.md (sibling article: heading rhythm and length to match)
    - components/checkout/CheckoutClient.tsx (read-only: confirm the checkout field a shopper enters a code into, so the article names the real one)
    - .planning/phases/12-content-assistant-live-proof/12-CONTEXT.md §D-08
    - AGENTS.md
  </read_first>
  <action>
Rewrite `data/r2/knowledge_md/gift-cards.md` in place. Keep the existing front-matter shape the sibling articles use (`id`, `title`, `category`, `tags`, then a blank line, then the `**AI NOTES:**` sentence, then `##` sections) and keep `id: faq-giftcards` so the vector id `knowledge-gift-cards` and the article identity stay stable.

Target state, section by section:

- Front matter: keep `id: faq-giftcards`, `title: Gift Cards`, `category: sales`. Widen `tags` to `[gift cards, gift, present, voucher, gift certificate, faq]`.
- AI NOTES: one sentence telling the assistant what the product is and when to recommend it, naming a gift, a present, a voucher and a gift certificate explicitly, plus the six facts below.
- `## Buying a Gift Card`: the Voltique Gift Card comes in four amounts, $25, $50, $100 and $200; the shopper picks an amount on the product page, enters the recipient's email address, and can add a name and a message; it can be bought as a guest or signed in.
- `## Delivery`: the card is delivered by email as soon as payment completes, to the recipient address entered at purchase, and the email carries the code. Replace the current sixty-minute-window sentence entirely; do not soften it, do not keep a time estimate of any kind.
- `## Redeeming at Checkout`: the code goes in the gift card field on the checkout shipping and billing step; the card's value comes off the order total and any remainder is charged to the order's payment method. Name the field the way the checkout UI labels it, confirmed from `CheckoutClient.tsx`.
- `## Checking a Balance`: signed-in shoppers see their cards and remaining balances under Account then Gift Cards; anyone holding a code can see its remaining balance by entering the code at checkout.
- `## Limits`: never expires; cannot be redeemed for cash; cannot be transferred or resold; stored value works across multiple orders until it runs out.

Write nothing the storefront does not do. There is no scheduled send date (SHOP-08 is deferred) and no custom amount (SHOP-09 is deferred), so neither appears, not even as a hint. Keep the article to roughly the length of `returns.md`; the whole file is embedded as one vector, so padding dilutes it.

Change no other file under `data/r2/knowledge_md/`.
  </action>
  <acceptance_criteria>
    - `mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts` exits 0.
    - `grep -c '\$25' data/r2/knowledge_md/gift-cards.md`, and the same for `\$50`, `\$100`, `\$200`, each return at least 1.
    - `grep -ic 'as soon as payment' data/r2/knowledge_md/gift-cards.md` returns at least 1.
    - `grep -ic 'never expire' data/r2/knowledge_md/gift-cards.md` returns at least 1.
    - `grep -ic 'cash' data/r2/knowledge_md/gift-cards.md` returns at least 1.
    - `grep -icE 'resold|resale' data/r2/knowledge_md/gift-cards.md` returns at least 1.
    - `grep -c 'Gift Cards' data/r2/knowledge_md/gift-cards.md` returns at least 1 and `grep -c 'Account' data/r2/knowledge_md/gift-cards.md` returns at least 1.
    - `git status --porcelain data/r2/knowledge_md/` lists exactly one path, `data/r2/knowledge_md/gift-cards.md`.
    - `mise exec -- npm test` exits 0 (the rewrite broke no other suite).
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts</automated>
    <fails_when>Exit code is nonzero — a named promise is missing or a forbidden one survived; the failing `it` block names which.</fails_when>
    <automated>WT=$(git status --porcelain data/r2/knowledge_md/) || { echo GIT_FAILED; exit 1; }; test "$(printf '%s\n' "$WT" | grep -c 'gift-cards.md')" = "1" &amp;&amp; test "$(printf '%s\n' "$WT" | grep -vc '^$')" = "1" &amp;&amp; echo ONLY_ARTICLE_CHANGED</automated>
    <fails_when>`ONLY_ARTICLE_CHANGED` is not printed. `GIT_FAILED` means the status could not be read; otherwise either the article is not the changed path or a sibling knowledge article was also modified, which would silently change what plan 12-04 re-indexes beyond this phase's scope.</fails_when>
    <automated>mise exec -- npm test</automated>
    <fails_when>Exit code is nonzero — some other suite reads this article or its tags and now disagrees.</fails_when>
  </verify>
  <done>The committed article states all six shipped facts, drops the stale delivery window, carries the widened tags, and the contract test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Upload the article to the public bucket and prove the live body matches the committed body</name>
  <files>none tracked — production R2 object voltique-images/knowledge_md/gift-cards.md</files>
  <read_first>
    - data/r2/knowledge_md/gift-cards.md (the file being uploaded)
    - .planning/phases/09-gift-card-catalogue/09-03-SUMMARY.md (the `wrangler r2 object put ... --remote` upload precedent and its curl proof)
    - .planning/phases/12-content-assistant-live-proof/12-01-SUMMARY.md (the recorded pre-upload ETag baseline)
    - AGENTS.md
  </read_first>
  <precondition>12-01 Task 2 recorded that the live R2 object's ETag equalled the MD5 of the committed file before this rewrite, so any post-upload difference is attributable to this upload alone.</precondition>
  <action>
Re-read the live object before writing, per D-03: `mise exec -- npx wrangler r2 object get voltique-images/knowledge_md/gift-cards.md --remote --pipe` piped to a scratch file, and record its MD5. It should still equal 12-01's recorded baseline.

Then perform the single R2 write this phase makes:

`mise exec -- npx wrangler r2 object put voltique-images/knowledge_md/gift-cards.md --file data/r2/knowledge_md/gift-cards.md --remote --content-type text/markdown`

Put no other key. `voltique-images` is a public bucket (project memory `voltique-r2-buckets-are-public`), so nothing sensitive may ever go into it; a markdown support article is exactly what belongs there.

Read back, per D-03: `curl -sI https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md`. Assert the status is 200, the content-type is `text/markdown`, and the `etag` equals `md5 -q data/r2/knowledge_md/gift-cards.md`. R2 returns the object body's MD5 as its ETag, verified at planning time against the pre-rewrite file, so this is exact body equality rather than a size heuristic.

Also fetch the body and confirm the stale delivery-window promise is absent from what the CDN actually serves, not just from the committed file — the CDN could serve a cached copy.

Record in the SUMMARY: the pre-upload MD5, the post-upload MD5, the ETag, the status code and the content-type.
  </action>
  <acceptance_criteria>
    - The `wrangler r2 object put` command exits 0.
    - `curl -s -o /dev/null -w '%{http_code}' https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md` prints 200.
    - The live ETag equals `md5 -q data/r2/knowledge_md/gift-cards.md`.
    - The fetched body contains `as soon as payment` and does not contain the stale delivery-window phrase.
    - The pre-upload MD5 recorded here equals the baseline 12-01 recorded, and differs from the post-upload MD5.
  </acceptance_criteria>
  <verify>
    <automated>test "$(curl -sI https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md | tr -d '\r' | awk -F'"' '/^etag:/{print $2}')" = "$(md5 -q data/r2/knowledge_md/gift-cards.md)" &amp;&amp; echo LIVE_BODY_MATCHES_COMMITTED</automated>
    <fails_when>Nothing is printed — the live object body is not the committed body, so plan 12-04 would embed stale text and CONTENT-02 would silently index the wrong article. A CDN cache is the likely cause; re-check after a short wait before concluding the upload failed.</fails_when>
    <automated>curl -s https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md | grep -ic 'as soon as payment'</automated>
    <fails_when>The count is 0 — the CDN is still serving the previous article body, and re-indexing now would embed the wrong text.</fails_when>
  </verify>
  <done>The corrected article is live on the public bucket, returns 200, and its served body hashes identically to the committed file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree -> public R2 bucket | `voltique-images` is world-readable; anything put there is published |
| committed article -> Volt's answers | the article's bytes become an embedded vector and are quoted back to shoppers as fact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Information Disclosure | public R2 bucket | high | mitigate | Exactly one key is put, and its content is a customer-facing support article reviewed in Task 2. No env file, no key material, and no other local path is passed to `r2 object put`. |
| T-12-08 | Tampering | overwriting an unrelated R2 object | medium | mitigate | The put names one fully-qualified key, `voltique-images/knowledge_md/gift-cards.md`, with an explicit `--file`. Task 2 asserts `git status --porcelain data/r2/knowledge_md/` lists exactly one path, so no sibling article can be swept along. |
| T-12-09 | Repudiation | live object silently diverging from the committed file | medium | mitigate | ETag-to-MD5 equality is asserted before and after, so the served bytes are provably the reviewed bytes. |
| T-12-10 | Tampering | the article promising behaviour the store does not deliver | medium | mitigate | The contract test pins each promise, and Task 2 forbids mentioning the deferred SHOP-08 scheduled delivery and SHOP-09 custom amounts. A future drift fails `npm test`. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed; the lockfile is untouched. `npm audit --omit=dev --audit-level=high` runs in the 12-06 phase gate. |
</threat_model>

## Artifacts this phase produces

| Artifact | Owning plan | Tracked? |
|----------|-------------|----------|
| Scratch remote-binding harness (`12-remote-env.mjs`) | 12-01 | no |
| `data/r2/knowledge_md/gift-cards.md` (rewritten) | **12-02** | yes |
| `tests/unit/data/knowledge-gift-cards.test.ts` | **12-02** | yes |
| R2 object `knowledge_md/gift-cards.md` (uploaded) | **12-02** | n/a — production object |
| `pages` row `terms-of-service` (updated) | 12-03 | n/a |
| Vectorize vector `knowledge-gift-cards` (upserted) | 12-04 | n/a |
| `12-PROOF-ORDER.md` | 12-05 | yes |
| `COVERAGE.md` | 12-05 | yes |
| `12-VALIDATION.md` (filled) | 12-06 | yes |

<verification>
- The contract test failed before the rewrite and passes after it.
- The article states the four amounts, immediate delivery on payment, no expiry, no cash, no resale, the checkout redemption step and the balance location, and carries gift/present/voucher tags.
- The live public object returns 200 and hashes identically to the committed file.
- The full unit suite is green.
</verification>

<success_criteria>
- `mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts` exits 0, having exited nonzero before Task 2.
- `mise exec -- npm test` exits 0.
- Exactly one path under `data/r2/knowledge_md/` is modified.
- The live ETag equals the committed file's MD5 and the served body carries the new delivery promise.
</success_criteria>

<output>
Create `.planning/phases/12-content-assistant-live-proof/12-02-SUMMARY.md` when done. Record the article's final section list, the exact grep counts, the pre- and post-upload MD5s, the ETag, and the RED-then-GREEN test transition.
</output>
