---
phase: 18-tech-debt-closure
plan: 07
type: execute
wave: 2
depends_on: ["18-01", "18-02", "18-03", "18-04", "18-05", "18-06"]
files_modified:
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-07, DEBT-08]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The stale token-scanner claim is confirmed already correct in the current documentation and its ledger entry is closed citing the confirming read (D-08)."
    - "The requirements-wide verify check is confirmed to live only in an archived plan file and its ledger entry is closed (D-09)."
    - "The retroactive gift-note question is ratified as already answered — a delayed delivery still sends the buyer's note — and recorded, so the thread closes instead of staying open (D-10)."
    - "The admin Appearance cards are confirmed to render each theme's industry and synopsis from the generated manifest, with no code change, and the matching ledger entry and todo are closed (D-12)."
    - "All eight DEBT requirements are marked complete and the roadmap shows the finished phase."
    - "The full CI-mirroring gate suite is green in one pass before anything is pushed."
    - "Production is confirmed after deploy by read-only checks only — the deleted tax route no longer answers and the new Worker version is live. No card, personal or secret data is read or written."
  artifacts:
    - .planning/WINDOWS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/18-tech-debt-closure/18-07-SUMMARY.md
  key_links:
    - "A push to main triggers Cloudflare Workers Builds, which runs deploy:ci — the only sanctioned deploy path. No deploy script is ever run from this machine (AGENTS.md)."
    - "This phase adds no migration, so deploy:ci's remote-migration step has nothing to apply."
    - "components/admin/ThemePresetGrid.tsx reads theme.meta from lib/themes/manifest.generated.ts, generated by scripts/build-themes.mjs from each themes/*.css header — one source of truth, already shipped."
---

<objective>
Close the phase. Six plans landed code; this one closes the documentation debt that needs no
code, confirms the one requirement that turned out to be already satisfied, runs the full gate
suite, deploys by pushing, and proves the result from the outside with read-only checks.

Purpose: DEBT-07's three remaining items and all of DEBT-08 are verification and record-keeping,
not implementation. Giving them their own plans would be ceremony. They belong next to the gate
that proves the rest of the phase.

Output: four ledger entries closed, two todos closed, one product decision recorded, eight
requirements marked complete, a green gate suite, a deployed Worker, and a read-only production
confirmation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/18-tech-debt-closure/18-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the four already-true claims, then close their records</name>
  <files>.planning/WINDOWS.md, .planning/todos/pending/theme-metadata-industry-synopsis-admin.md, .planning/todos/pending/migration-0023-duplicate-number.md</files>
  <read_first>
    - `.planning/WINDOWS.md` — entries 2, 5, 6 and 10, in the table and in the JSON block below it
    - `docs/CLAUDE.md` around line 123 and its Testing section — the subject of entry 5
    - `.github/workflows/ci.yml` — the step that runs the token scanner
    - `.planning/milestones/v2-phases/08.1-v2-tech-debt-closure/08.1-07-PLAN.md` — the archived plan that is entry 6's only home
    - `components/admin/ThemePresetGrid.tsx` lines 175-195 — the metadata render
    - `lib/themes/manifest.generated.ts` — the `meta` shape for every theme
    - `.planning/milestones/v2.1-phases/12-content-assistant-live-proof/12-REVIEW-FIX.md` around lines 575-581 and 687-689 — the deferred retroactive-note question
    - `.planning/phases/18-tech-debt-closure/18-04-SUMMARY.md` — for the commit that closes entry 10
    - `.planning/todos/pending/` — the two todo files being closed
  </read_first>
  <precondition>All six wave-1 plans are committed and their SUMMARY files exist in the phase directory. Plan 18-04's SUMMARY must name the commit that fixes the dropped cart line, because ledger entry 10 is closed citing it.</precondition>
  <action>
Verify each claim before closing its record. A claim that does not hold is a finding, not a
formality — stop and record it instead of closing the entry.

**Entry 5, the stale token-scanner claim (D-08).** Read `docs/CLAUDE.md` and confirm its current
text describes the scanner as wired into CI, then read `.github/workflows/ci.yml` and confirm
the step really runs. Both were fixed already by unrelated Phase 8.2 documentation work, so
there is no code change. Close with `gsd-tools windows fixed 5` and cite the two lines you read
as the confirming evidence in the SUMMARY.

**Entry 6, the requirements-wide verify check (D-09).** Confirm the check exists only inside the
archived Phase 8.1 plan file and is not a live pattern anywhere in `.planning/` outside
`milestones/`. Search before concluding. Close with `gsd-tools windows fixed 6`.

**Entry 10, the dropped cart line.** This is DEBT-03 and plan 18-04 fixed it. Read 18-04's
SUMMARY for the closing commit, then close with `gsd-tools windows fixed 10`, citing it.

**Entry 2 and DEBT-08, the admin Appearance metadata (D-12).** Confirm two facts directly: that
`components/admin/ThemePresetGrid.tsx` renders both metadata fields off the theme object, and
that every theme in `lib/themes/manifest.generated.ts` carries both. Then close entry 2 with
`gsd-tools windows fixed 2`. Entry 2 is an unrun human check carried from Phase 6, not a defect;
it closes on code-level evidence — the manifest data and the component's render logic read
directly — under the same convention this whole run has used. Say exactly that in the SUMMARY
rather than wording it as though someone clicked through the admin in a browser.

**The retroactive gift-note decision (D-10).** No code change. `deliverOne` already renders the
buyer's note on every send, including for delivery rows still pending from before the
note-rendering fix — which *is* "send the note". Record in the SUMMARY that this existing
behaviour is ratified as correct, and why: the buyer wrote the note intending it to go out, the
delivery was only delayed, and suppressing it would need a timestamp cutoff to tell old pending
rows from new ones — more code for a worse default, one where a note the buyer wrote silently
never appears. Flag it plainly as Russell's to override. The Phase 12 review declined to decide
this unilaterally; this run is answering it by default, not by mandate.

**Close the two todos** with `gsd-tools todo complete <filename>`:
`theme-metadata-industry-synopsis-admin.md` and `migration-0023-duplicate-number.md`. The cart
item has no todo file — it exists only as ledger entry 10.

Do not delete any ledger row. Entries close by status change with a note, never by removal.
  </action>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status --raw | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const e=JSON.parse(s).ledger.entries;const bad=[2,5,6,10].filter(id=>e.find(x=>Number(x.id)===id)?.status!=='fixed');if(bad.length){console.error('not fixed: '+bad.join(','));process.exit(1);}console.log('entries 2,5,6,10 fixed');})" && test ! -e .planning/todos/pending/theme-metadata-industry-synopsis-admin.md && test ! -e .planning/todos/pending/migration-0023-duplicate-number.md && grep -q 'theme.meta.industry' components/admin/ThemePresetGrid.tsx && grep -q 'theme.meta.synopsis' components/admin/ThemePresetGrid.tsx && node -e "const m=require('fs').readFileSync('lib/themes/manifest.generated.ts','utf8');const i=(m.match(/industry:/g)||[]).length,s=(m.match(/synopsis:/g)||[]).length;if(i<6||s<6||i!==s){console.error('manifest metadata incomplete: '+i+' industry, '+s+' synopsis');process.exit(1);}console.log('manifest metadata complete for '+i+' themes');"</automated>
    <fails_when>Any of the four ledger entries is still open or waived rather than fixed, either todo file is still in the pending directory, the admin grid stops reading either metadata field, or the generated manifest is missing industry or synopsis for one or more themes — which would mean DEBT-08 was closed on a false premise.</fails_when>
  </verify>
  <acceptance_criteria>
    - Ledger entries 2, 5, 6 and 10 all report status fixed; no row was deleted.
    - Both named todo files have left the pending directory.
    - The admin grid renders both metadata fields and the generated manifest supplies both for every theme.
    - The SUMMARY records the D-10 ratification, flagged as Russell's to override.
  </acceptance_criteria>
  <done>Every documentation-only item in DEBT-07 and DEBT-08 is verified true and its record closed, with the evidence written down.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full gate suite and mark the phase complete in the planning record</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `AGENTS.md` — the Gates section, which lists the CI command order this task mirrors
    - `CONTRIBUTING.md` — the subset contributors run before every commit
    - `.planning/REQUIREMENTS.md` — the eight DEBT checkboxes and the traceability table rows
    - `.planning/ROADMAP.md` — the Phase 18 section, its plan list, and the Progress table row
    - every `18-0*-SUMMARY.md` in this phase directory
  </read_first>
  <precondition>Task 1 is committed. `git status --short` shows no unexpected modification outside this phase's declared files — in particular `cloudflare-env.d.ts` and `wrangler.jsonc` carry pre-existing uncommitted changes from before this phase; leave them alone and note them, do not stage them.</precondition>
  <action>
Run the gate suite in the same order CI runs it, in one pass, and record each result:

```
npm audit --omit=dev --audit-level=high
npm run build:themes:check
npm run scan:tokens
npm run lint
npm run typecheck
npm run cf-typecheck
mise exec -- npm test
mise exec -- npm run test:workers
mise exec -- npm run test:observability-worker
npm run docs:lint
npm run check:migrations -- --base origin/main
npm run build
```

Two notes on commands that behave specially here. `npm run cf-typecheck` regenerates the
environment types and diffs them; CI generates without environment files present, so if a local
`.env.local` exists move it aside for the duration and restore it afterwards — this is a
recorded prior lesson, not a new discovery. And `check:migrations` will take its
no-migrations-added early exit, because this phase adds none; that is the expected result, not a
skipped gate, and plan 18-06's new collision rule is proven by its unit test rather than by this
run.

If any gate fails, fix it in the owning plan's files and say so in the SUMMARY. Do not weaken a
gate, skip a suite, or narrow a command to make it pass.

With the suite green, update the planning record:

  - mark all eight requirements complete with
    `gsd-tools requirements mark-complete DEBT-01,DEBT-02,DEBT-03,DEBT-04,DEBT-05,DEBT-06,DEBT-07,DEBT-08`,
    then read `.planning/REQUIREMENTS.md` and confirm both the checkboxes and the traceability
    rows moved — do not assume the command touched both;
  - in `.planning/ROADMAP.md`, tick every plan checkbox in the Phase 18 section, tick the
    Phase 18 line in the phase list, and add the Phase 18 row to the Progress table in the same
    shape as the Phase 13 through 17 rows;
  - leave `.planning/STATE.md`'s position fields to the executor's normal end-of-plan update.

Use scoped edits on `ROADMAP.md`. Never rewrite that file wholesale — it carries every phase of
four milestones and a whole-file write would destroy the sections outside the edit window.
  </action>
  <verify>
    <automated>npm run build:themes:check && npm run scan:tokens && npm run lint && npm run typecheck && mise exec -- npm test && mise exec -- npm run test:workers && mise exec -- npm run test:observability-worker && npm run docs:lint && npm run check:migrations -- --base origin/main && npm run build && node -e "const r=require('fs').readFileSync('.planning/REQUIREMENTS.md','utf8');const open=r.split('\n').filter(l=>/^- \[ \] \*\*DEBT-0[1-8]\*\*/.test(l));if(open.length){console.error('still unchecked: '+open.length);process.exit(1);}const pend=r.split('\n').filter(l=>/^\| DEBT-0[1-8] \|.*Pending/.test(l));if(pend.length){console.error('traceability still pending: '+pend.length);process.exit(1);}console.log('DEBT-01..08 complete');"</automated>
    <fails_when>Any gate command exits non-zero, the production build fails, a DEBT checkbox is still unchecked, or a DEBT traceability row still reads pending — each of which means the phase is not actually finished.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every gate command in the list above ran and passed, with its result recorded in the SUMMARY.
    - All eight DEBT checkboxes and all eight traceability rows show complete.
    - The Phase 18 roadmap section has every plan ticked and a Progress table row.
    - `ROADMAP.md` was edited by scoped replacement, and no phase section outside Phase 18 changed.
  </acceptance_criteria>
  <done>The whole phase passes the same checks CI runs, and the planning record says the phase is done.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy by pushing, then confirm production read-only</name>
  <files>No repository file. Push and read-only production confirmation.</files>
  <read_first>
    - `AGENTS.md` — the deploy rules: a push to `main` is the deploy; `npm run deploy` and `deploy:ci` are never run from this machine
    - `wrangler.jsonc` line 114 — the production site URL
    - `.planning/phases/18-tech-debt-closure/18-03-SUMMARY.md` — confirmation that the tax route was deleted
  </read_first>
  <precondition>Task 2's gate suite passed in this session and every phase commit is present on the local branch. The working tree's only uncommitted entries are the pre-existing `cloudflare-env.d.ts` and `wrangler.jsonc` modifications and untracked files that predate this phase; none of them are staged or pushed by this task.</precondition>
  <action>
**Before pushing**, capture the current production behaviour of the deleted route so the
after-check is a real signal rather than a guess:

```
curl -s -o /dev/null -w '%{http_code}\n' -X POST -H 'content-type: application/json' \
  -d '{}' https://voltique.russellkmoore.me/api/tax
```

Record whatever it returns now. An empty body carries no cart, no customer and no card data —
this is a read-only probe of a public endpoint and nothing else on production is touched.

Then push. `git push` to `main` is the deploy: Cloudflare Workers Builds picks it up and runs
`deploy:ci`. Do not run any deploy script locally, do not run a migration command, and do not
use `git add .` — stage only this phase's files by path.

Wait for the new version to go live, then run exactly three read-only confirmations:

  1. The deleted route no longer answers. Repeat the probe above and expect a not-found status.
     Compare it against the before-value you recorded; a status that did not change means the
     deploy has not landed yet, not that the deletion failed — wait and repeat rather than
     concluding.
  2. The storefront is healthy. A plain `GET` of the site root returns a success status.
  3. The deploy landed. `npx wrangler deployments list` shows a new version whose timestamp is
     after the push. This is a read-only listing; run no other wrangler subcommand.

Touch nothing else in production. Do not read a secret, list a secret's value, query D1, open an
order, or look at any customer record. Nothing in this phase changed a payment path, a gift-card
balance, or a stored credential, so nothing in this phase justifies looking at one.

If the deploy does not land or a check fails, stop and record the exact state. Do not attempt a
manual deploy as a workaround — that is explicitly forbidden by the project's own rules.

Record in the SUMMARY: the before and after status of the probe, the site-root status, the
deployed version identifier and its timestamp, and the count of commits the push carried.
  </action>
  <verify>
    <automated>test "$(curl -s -o /dev/null -w '%{http_code}' -X POST -H 'content-type: application/json' -d '{}' https://voltique.russellkmoore.me/api/tax)" = "404" && test "$(curl -s -o /dev/null -w '%{http_code}' https://voltique.russellkmoore.me/)" = "200"</automated>
    <fails_when>The deleted route still answers with anything other than a not-found status, meaning either the deploy has not landed or the route survived; or the storefront root stops returning success, meaning the deploy broke the site.</fails_when>
  </verify>
  <acceptance_criteria>
    - The probe returned a not-found status after the deploy and a different, recorded status before it.
    - The storefront root returns success.
    - A new Worker version is listed with a timestamp after the push.
    - No secret, database row, order or customer record was read; no deploy or migration command was run locally.
  </acceptance_criteria>
  <done>Phase 18's code is live and the one externally-visible change — the tax route being gone — is proven from outside the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine → `main` → Workers Builds | A push is the deploy trigger; whatever is on the branch ships |
| Operator → production Worker | Post-deploy confirmation reaches a live system holding real customer and payment data |
| Planning record → future decisions | A closed ledger entry is treated by later phases as settled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-31 | Information disclosure | production confirmation steps | high | mitigate | The post-deploy checks are restricted to three read-only probes: an empty-body POST to a now-deleted public route, a GET of the site root, and a deployment listing. The task explicitly forbids reading a secret, querying D1, or opening an order or customer record, and states the reason — nothing in this phase touched a payment path or a stored credential. |
| T-18-32 | Tampering | push to `main` | high | mitigate | A push deploys every commit on the branch. Task 3's precondition requires Task 2's full gate suite to have passed in the same session, requires staging by path, and forbids `git add .` — so the pre-existing uncommitted environment-type and wrangler changes cannot ride along unnoticed. |
| T-18-33 | Elevation of privilege | deploy path | high | mitigate | Running `deploy:ci` locally would apply remote migrations from a developer machine, which the project forbids outright. The task names the prohibition and the workaround it must not take, and instructs a halt-and-record on a failed deploy rather than a manual retry. |
| T-18-34 | Repudiation | `.planning/WINDOWS.md` | medium | mitigate | Closing an entry on a false premise makes a live defect invisible to every later phase. Task 1 requires each claim be independently verified before its entry closes and the confirming read cited, and its verify re-reads the admin grid and the generated manifest so DEBT-08 cannot be closed on an assumption. |
| T-18-35 | Repudiation | D-10 ratification | medium | accept | This is a product decision being answered by default in Russell's absence, not a technical one. It is accepted with the mitigation of being recorded explicitly and flagged as his to override; no behaviour is built that would be costly to reverse, since the decision is to leave the existing behaviour alone. |
| T-18-36 | Denial of service | production probe | low | accept | Two unauthenticated requests against a rate-limited public surface. Well inside normal traffic and no different from a browser visit. |

No package-manager install task exists in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
- The full gate suite listed in Task 2, in that order, in one pass.
- The three read-only production confirmations in Task 3.
- `gsd-tools windows status` showing entries 2, 5, 6 and 10 fixed.
</verification>

<success_criteria>
- Broken-windows entries 2, 5, 6 and 10 are closed with cited evidence, and no row was deleted.
- The theme-metadata and migration-number todos are closed.
- The retroactive gift-note decision is recorded and flagged for Russell.
- All eight DEBT requirements read complete in both the checklist and the traceability table.
- The full CI-mirroring gate suite passed in one pass.
- Production serves a not-found status for the deleted tax route, the storefront root is healthy, and a new Worker version is live.
</success_criteria>

<artifacts_this_phase_produces>
- `.planning/WINDOWS.md` — entries 2, 5, 6 and 10 closed with notes
- `.planning/REQUIREMENTS.md` — DEBT-01 through DEBT-08 complete in both the checklist and the traceability table
- `.planning/ROADMAP.md` — Phase 18 plans ticked, phase ticked, Progress row added
- `.planning/todos/pending/` — two files closed
- `.planning/phases/18-tech-debt-closure/18-07-SUMMARY.md` — the gate record, the D-10 decision, and the production evidence
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/18-tech-debt-closure/18-07-SUMMARY.md` when done. It must carry: each
gate command and its result; the before and after status of the production tax-route probe; the
deployed version identifier and timestamp; the evidence cited for each closed ledger entry; the
D-10 ratification flagged as Russell's to override; and a short list of anything left for him,
including the pre-existing uncommitted `cloudflare-env.d.ts` and `wrangler.jsonc` changes this
phase deliberately did not touch.
</output>
