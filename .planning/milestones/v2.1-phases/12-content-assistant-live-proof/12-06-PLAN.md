---
phase: 12-content-assistant-live-proof
plan: 06
type: execute
wave: 5
depends_on: ["12-01", "12-02", "12-03", "12-04", "12-05"]
files_modified:
  - .planning/phases/12-content-assistant-live-proof/12-VALIDATION.md
autonomous: true
requirements: [CONTENT-01, CONTENT-02, CONTENT-03, SHOP-07]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every command in the CI workflow's list passes locally, in CI's order, against the phase's final tree."
    - "12-VALIDATION.md is filled from commands that actually ran, with no skeleton placeholder left, status validated and nyquist_compliant true."
    - "Every phase-level truth re-checks green in one pass: the committed article's promises, the live R2 object, the gift-card knowledge vector, three Volt replies, the live Terms section, and the order, account and delivery rows."
    - "No tracked file is modified at the end of the phase, and main is pushed to origin (docs and data commits only)."
    - "No file under lib/, app/ or components/ changed anywhere in the phase, and no migration was added (D-11)."
    - "The committed wrangler.jsonc is byte-identical to its state before the phase began."
  artifacts:
    - ".planning/phases/12-content-assistant-live-proof/12-VALIDATION.md — filled, validated, nyquist_compliant"
  key_links:
    - "the CI workflow's command list -> the same commands run locally in the same order -> a green phase gate"
    - "each plan's SUMMARY coverage entries -> 12-VALIDATION.md's per-task verification map"
  prohibitions:
    - "No new production write of any kind — this plan only re-reads."
    - "No second purchase."
    - "No code change under lib/**, app/**, components/**."
    - "No migration file added or edited."
    - "No edit to the committed wrangler.jsonc."
    - "No npm run deploy, deploy:ci, or any deploy* script — pushing main is what triggers a Cloudflare build, and that is the only deploy path used."
    - "No secret value read, printed, or written to any artifact."
    - "No git add . — stage named paths only."
---

<objective>
Close the phase and the milestone. Run every gate CI runs, in CI's order, against the final tree; fill the phase's validation contract from commands that actually ran; re-check every phase-level truth in one pass; then push `main`.

This is the last phase of milestone v2.1, so the gate is the milestone's gate too. Anything left red here is left red in the shipped milestone.

Per D-11 this phase shipped no template or component changes, and this plan proves it from the diff rather than asserting it.

Purpose: CONTENT-01, CONTENT-02, CONTENT-03 and SHOP-07 all re-verified together.
Output: a green gate, a filled validation contract, and `main` on `origin`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-content-assistant-live-proof/12-01-SUMMARY.md
@.planning/phases/12-content-assistant-live-proof/12-02-SUMMARY.md
@.planning/phases/12-content-assistant-live-proof/12-03-SUMMARY.md
@.planning/phases/12-content-assistant-live-proof/12-04-SUMMARY.md
@.planning/phases/12-content-assistant-live-proof/12-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full CI-mirroring gate suite</name>
  <files>none tracked — gate commands only; exit codes recorded in 12-06-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml (the authoritative command list and its order)
    - AGENTS.md §Gates
    - CONTRIBUTING.md (the pre-commit subset contributors run)
    - .planning/phases/11-production-enablement/11-05-SUMMARY.md (the move-aside wrapper for cf-typegen and cf-typecheck, and the dotglob trap in the restore direction)
    - AGENTS.md
  </read_first>
  <precondition>`cf-typecheck` must run with the local env files moved aside, matching CI's no-env-file generation (project memory `cf-typecheck-regenerate-without-env-files`); the restore step must use the glob form for both directions, per 11-05's recorded dotglob trap.</precondition>
  <action>
Run the CI workflow's commands locally, in the same order, and record each exit code in the SUMMARY:

1. `npm audit --omit=dev --audit-level=high`
2. `npm run build:themes:check`
3. `npm run scan:tokens`
4. `npm run lint`
5. `npm run typecheck`
6. `npm run cf-typecheck` (env files moved aside)
7. `npm test`
8. `npm run test:workers`
9. `npm run test:observability-worker`
10. `npm run build`

Skip only `check:migrations`, which CI runs on pull requests against a base SHA. This phase adds no migration, so there is nothing for it to check; state that in the SUMMARY rather than silently omitting it.

Also run `npm run docs:lint`. No doc changed in this phase, so it should pass unchanged; run it anyway, because the phase's own gate list names it and a green run costs seconds.

Every command runs under `mise exec --`. If any fails, stop and report which one and why. Do not fix a failure by narrowing the gate.
  </action>
  <acceptance_criteria>
    - All ten CI commands exit 0, plus `docs:lint`.
    - `npm run cf-typecheck` exits 0 with `.env*.local` and `.dev.vars` moved aside, and both are restored to the repo root afterwards.
    - `npm audit --omit=dev --audit-level=high` reports no high or critical advisory.
    - `npm run build` completes and produces a build output.
    - Every exit code is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm audit --omit=dev --audit-level=high &amp;&amp; mise exec -- npm run build:themes:check &amp;&amp; mise exec -- npm run scan:tokens &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>Any command exits nonzero. An audit failure means a dependency advisory appeared since 11-05 and is unrelated to this phase's changes; a themes or token failure means generated theme output drifted; lint and typecheck failures point at the one new test file, the only TypeScript this phase added.</fails_when>
    <automated>mise exec -- npm test &amp;&amp; mise exec -- npm run test:workers &amp;&amp; mise exec -- npm run test:observability-worker</automated>
    <fails_when>Any suite exits nonzero. The unit suite is the one that would catch an article rewrite that broke another test's expectations; the workers and observability suites should be untouched by this phase, so a failure there is a signal that something outside the phase's declared scope moved.</fails_when>
    <automated>HOLD=$(mktemp -d); mv .env*.local .dev.vars "$HOLD"/ 2>/dev/null; mise exec -- npm run cf-typecheck; RC=$?; mv "$HOLD"/.env*.local "$HOLD"/.dev.vars . 2>/dev/null; rmdir "$HOLD" 2>/dev/null; test "$RC" = "0" &amp;&amp; echo CF_TYPECHECK_OK</automated>
    <fails_when>`CF_TYPECHECK_OK` is not printed — `cloudflare-env.d.ts` does not match what `wrangler types` generates under CI's conditions, which is a no-env-file generation. Run the restore step by hand and confirm `.env*.local` and `.dev.vars` are back in the repo root before doing anything else: 11-05 recorded a dotglob quirk that can strand them in the holding directory, and a stranded `.dev.vars` breaks local dev silently.</fails_when>
    <automated>mise exec -- npm run build &amp;&amp; mise exec -- npm run docs:lint</automated>
    <fails_when>Either exits nonzero. A build failure with no application code changed would be an environment problem rather than a code problem; a docs:lint failure would mean a doc changed when the phase declared none would.</fails_when>
  </verify>
  <done>Every gate CI runs is green locally against the phase's final tree.</done>
</task>

<task type="auto">
  <name>Task 2: Re-check every phase-level truth in one pass</name>
  <files>none tracked — read-only re-verification of all four requirements</files>
  <read_first>
    - .planning/REQUIREMENTS.md (CONTENT-01, CONTENT-02, CONTENT-03, SHOP-07)
    - .planning/ROADMAP.md §"Phase 12" (the four success criteria)
    - .planning/phases/12-content-assistant-live-proof/12-PROOF-ORDER.md (the order id and evidence from 12-05)
    - data/r2/knowledge_md/gift-cards.md
    - AGENTS.md
  </read_first>
  <action>
Re-verify each requirement from scratch, in one pass, rather than trusting the earlier plans' SUMMARYs. Anything that has drifted since its own plan ran shows up here.

CONTENT-01, the article:
- The committed file names all four amounts, promises delivery as soon as payment completes, states no expiry, mentions cash, describes the checkout redemption step, names where a balance is visible, and carries no sixty-minute delivery window.
- The public object returns 200 and its ETag equals the committed file's MD5.

CONTENT-02, the assistant:
- The index reports at or above the vector count 12-01 recorded, at 768 dimensions.
- The gift card knowledge vector is present with a knowledge source and text carrying the new delivery promise.
- The three D-10 questions each come back with a gift card mentioned in the `answer` field specifically. Parse the JSON and match on `answer` alone; the response's `products` array carries the gift card's catalogue name, so a whole-body grep would pass even if Volt's prose said nothing about it.

CONTENT-03, the terms:
- The stored D1 row contains the gift card heading exactly once, counted in SQL by the length lost to replacing it with nothing, divided by its own length.
- The live Terms page returns 200, contains all four promises, still contains the Recurring Orders section, and the gift card heading occurs there the same number of times as the known-single section-5 heading. Exactly-once is not claimed from the page: it ships its markup twice, once as HTML and once inside the React server-component payload, so counts taken from it are parity evidence rather than cardinality.

SHOP-07, the live proof:
- The order row reads paid.
- Exactly one gift card account exists, 2500, USD, active.
- Its delivery row reads sent.
- The account-listing clause is on record as a documented gap, not as a pass.

Record every result in the SUMMARY as a requirement-to-evidence table. Where a check is a human judgment rather than a command, mark it as such and name who owns it; do not record a human item as machine-verified.
  </action>
  <acceptance_criteria>
    - Each of CONTENT-01, CONTENT-02, CONTENT-03 has every one of its checks green.
    - SHOP-07's issuance and delivery checks are green and its account-listing clause is recorded as a gap with its evidence.
    - The SUMMARY carries a requirement-to-evidence table covering all four.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/data/knowledge-gift-cards.test.ts &amp;&amp; test "$(curl -sI https://voltique-images.russellkmoore.me/knowledge_md/gift-cards.md | tr -d '\r' | awk -F'"' '/^etag:/{print $2}')" = "$(md5 -q data/r2/knowledge_md/gift-cards.md)" &amp;&amp; echo CONTENT_01_OK</automated>
    <fails_when>`CONTENT_01_OK` is not printed: either the committed article drifted from its contract, or the live public object no longer matches the committed bytes.</fails_when>
    <automated>mise exec -- npx wrangler d1 execute mercora-db --remote --json --command "SELECT (length(content)-length(replace(content,'6. Gift Cards','')))/length('6. Gift Cards') AS heading_occurrences FROM pages WHERE slug='terms-of-service' AND status='published';"</automated>
    <fails_when>`heading_occurrences` is not exactly 1: zero means the row lost the section, and above one means a duplicate was appended. Exactly-once is asserted against the stored D1 string, not the served page — the page ships its markup twice, once as HTML and once inside the React server-component payload, so no count taken from it can mean "one section".</fails_when>
    <automated>curl -sf https://voltique.russellkmoore.me/terms-of-service -o /tmp/gsd-12-tos-gate.html || { echo FETCH_FAILED; exit 1; }; G=$(grep -o '6. Gift Cards' /tmp/gsd-12-tos-gate.html | wc -l | tr -d ' '); R=$(grep -o '5. Recurring Orders' /tmp/gsd-12-tos-gate.html | wc -l | tr -d ' '); rm -f /tmp/gsd-12-tos-gate.html; test "$G" -gt 0 &amp;&amp; test "$G" = "$R" &amp;&amp; echo "LIVE_TERMS_OK gift=$G recurring=$R"</automated>
    <fails_when>`LIVE_TERMS_OK` is not printed. `FETCH_FAILED` means the page did not return 200. A gift count of 0 means the section is not rendered. Counts that differ from the known-good section-5 heading mean the new section renders through different paths, so it is duplicated or half-rendered.</fails_when>
    <automated>mise exec -- npx wrangler d1 execute mercora-db --remote --json --command "SELECT status, issued_amount_minor, currency_code FROM gift_card_accounts;"</automated>
    <fails_when>Not exactly one row, or the row is not active at 2500 USD: the issued card is gone, wrong, or duplicated since 12-05 ran. This query is deliberately unfiltered — at the phase gate the whole-table count is the claim being made, that exactly one card exists in production.</fails_when>
    <automated>A=$(curl -sf -X POST https://voltique.russellkmoore.me/api/agent-chat -H 'content-type: application/json' -d '{"question":"do you have vouchers?"}' | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{try{process.stdout.write(String(JSON.parse(s).answer||""))}catch(e){process.exit(1)}})') || { echo CALL_FAILED; exit 1; }; printf '%s' "$A" | grep -ic 'gift card'</automated>
    <fails_when>`CALL_FAILED` is printed, or the count is 0: retrieval regressed since 12-04, or the index was re-cleared by something outside this phase. The count reads only the `answer` field, so the gift card appearing in the response's `products` array cannot mask a regression in Volt's prose.</fails_when>
  </verify>
  <done>All four requirements re-verify green in a single pass, with SHOP-07's unprovable clause recorded as a gap rather than a pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the validation contract, prove the scope held, and push main</name>
  <files>.planning/phases/12-content-assistant-live-proof/12-VALIDATION.md</files>
  <read_first>
    - .planning/phases/12-content-assistant-live-proof/12-VALIDATION.md (the skeleton to fill)
    - .planning/phases/09-gift-card-catalogue/09-04-SUMMARY.md (the precedent for filling a phase VALIDATION from commands that actually ran)
    - the five plan SUMMARYs from 12-01 through 12-05 (their coverage entries become the per-task verification map)
    - .planning/phases/11-production-enablement/11-05-SUMMARY.md (the drift check before pushing, and what a nonzero ahead-count usually is)
    - AGENTS.md
  </read_first>
  <precondition>All five earlier plans in this phase have committed SUMMARYs; the per-task verification map is built from what those SUMMARYs record, not from the plans' intentions.</precondition>
  <action>
Fill `.planning/phases/12-content-assistant-live-proof/12-VALIDATION.md` from commands that actually ran. Replace every placeholder; leave no brace-wrapped token behind.

- Test Infrastructure: vitest, the default config, the quick command being the single knowledge-article test, the full command being the unit suite, with the measured runtime.
- Sampling Rate: the quick test after each task commit, the full suite after each wave, and the CI-mirroring list before verification, which is what Task 1 just ran.
- Per-Task Verification Map: one row per task across 12-01 through 12-06, with its plan, wave, requirement, threat reference, test type, the automated command that actually ran, whether the file existed, and its status. Rows whose evidence is a production read rather than a test are typed accordingly, not dressed up as unit tests.
- Wave 0 Requirements: the one new test file, created in 12-02. No fixtures and no framework install were needed.
- Manual-Only Verifications: the two human checks harvested from this phase, each with why it is manual and what to do. One is the delivery email in Russell's inbox. The other is Account -> Gift Cards, which is not merely manual but structurally empty, and the row must say so rather than implying a person could confirm it.
- Set `status: validated` and `nyquist_compliant: true` only if every automated row is green.

Then prove the phase held its scope, from the diff rather than from assertion.

First pin the range explicitly. Read the `plan_head_before` field from `12-01-SUMMARY.md`'s frontmatter, which 12-01 Task 3 captured with `git rev-parse HEAD` before this phase committed anything, and export it as `PHASE_BASE`. That is the only source for this value; if the field is missing, stop and say so rather than guessing a base from dates or commit messages, because a wrong base makes every scope check below meaningless in the passing direction. A bare `git diff` with no range says nothing about committed work, so every scope check uses `$PHASE_BASE..HEAD`. Over that range:

- No file under `lib/`, `app/` or `components/` changed, which is D-11's claim made checkable.
- No file under `migrations/` was added or changed.
- `wrangler.jsonc` is byte-identical to its pre-phase state.
- No added line anywhere in the range matches a Stripe secret-key prefix, a payment client secret, or a gift-card code column name.

Finally, push. Check the ahead-count against `origin/main` first and confirm the commits about to go up are the phase's docs and data commits plus any unpushed docs commits from before it — at planning time `main` was already 5 commits ahead, all documentation. Pushing is what triggers a Cloudflare Workers build; that is the project's only sanctioned deploy path and it is harmless here, because no application code changed and no migration was added. Never run a deploy script directly.

Confirm afterwards that no tracked file is modified. The known untracked paths recorded in 12-01 will still be untracked; that is expected and is not something this plan cleans up.
  </action>
  <acceptance_criteria>
    - `PHASE_BASE` is set from `12-01-SUMMARY.md`'s `plan_head_before` frontmatter field and resolves to a valid commit.
    - `12-VALIDATION.md` has no brace-wrapped placeholder left, carries a per-task row for every task in the phase, and its frontmatter reads `status: validated` and `nyquist_compliant: true`.
    - Over `$PHASE_BASE..HEAD`, zero files changed under `lib/`, `app/`, `components/` or `migrations/`.
    - `git diff $PHASE_BASE..HEAD -- wrangler.jsonc` produces no output, with the diff's own exit status checked rather than inferred from empty output.
    - No added line in the range matches a Stripe secret prefix, a payment client secret, or a gift-card code column name.
    - `git rev-list --count origin/main..HEAD` returns 0 after the push.
    - `git status --porcelain --untracked-files=no` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '{[a-z]' .planning/phases/12-content-assistant-live-proof/12-VALIDATION.md</automated>
    <fails_when>The count is not 0: a skeleton placeholder survived, so the validation contract records intentions rather than what ran.</fails_when>
    <automated>SCOPE=$(git diff --name-only "$PHASE_BASE"..HEAD -- lib app components migrations wrangler.jsonc) || { echo GIT_FAILED; exit 1; }; test -z "$SCOPE" &amp;&amp; echo SCOPE_HELD</automated>
    <fails_when>`SCOPE_HELD` is not printed. `GIT_FAILED` means `PHASE_BASE` is unset or not a valid commit, so the check proved nothing and must be re-run with the SHA from 12-01's SUMMARY. Otherwise the named files are the ones the phase touched but declared it would not: application code, a migration, or the committed Cloudflare config.</fails_when>
    <automated>ADDED=$(git log -p "$PHASE_BASE"..HEAD) || { echo GIT_FAILED; exit 1; }; printf '%s\n' "$ADDED" | grep -E '^\+' | grep -vE '^\+\+\+' | grep -cE 'sk_(test|live)_|client_secret|code_hash|code_ciphertext|code_nonce'</automated>
    <fails_when>`GIT_FAILED` is printed, or the count is not 0. The count is the one that matters: a nonzero value means an added line carries a secret-shaped value or a gift-card code column name, and it must be removed from history before anything is pushed. The log's own exit status is captured first so a failed `git log` cannot read as a clean zero.</fails_when>
    <automated>git push origin main || { echo PUSH_FAILED; exit 1; }; AHEAD=$(git rev-list --count origin/main..HEAD) || { echo GIT_FAILED; exit 1; }; WT=$(git status --porcelain --untracked-files=no) || { echo GIT_FAILED; exit 1; }; test "$AHEAD" = "0" &amp;&amp; test -z "$WT" &amp;&amp; echo PHASE_CLOSED</automated>
    <fails_when>`PHASE_CLOSED` is not printed. `PUSH_FAILED` means the remote rejected it, in which case fetch and inspect rather than forcing. Otherwise commits remain unpushed or a tracked file is still modified.</fails_when>
  </verify>
  <human-check>Watch the Cloudflare Workers build triggered by this push and confirm it succeeds. Nothing in it should change behaviour: no application code and no migration is in the range, only docs and the one data file.</human-check>
  <done>The validation contract is filled and validated, the phase's scope claims are proven from the diff, and `main` is on `origin` with a clean tracked tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main -> origin/main | a push triggers a Cloudflare Workers build that deploys the Worker |
| phase commits -> public git history | anything committed in this phase becomes permanent and public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-29 | Information Disclosure | secrets entering git history | critical | mitigate | Every added line in the push range is grepped for a Stripe secret prefix, a payment client secret, and gift-card code column names, and must return zero before the push. |
| T-12-30 | Tampering | pushing unintended application changes | high | mitigate | The diff over the phase's range is asserted to contain zero files under `lib/`, `app/`, `components/` or `migrations/`, and `wrangler.jsonc` is asserted byte-identical. The push is only allowed once those pass. |
| T-12-31 | Tampering | a deploy applying an unexpected migration | high | mitigate | Deploys run only through Cloudflare Workers Builds on push, never from this machine. The migration-directory check above proves the range contains none, so the deploy's migration step is a no-op. |
| T-12-32 | Repudiation | a validation contract recording intentions rather than results | medium | mitigate | Placeholders are grepped for and must be zero; each row's command is one that actually ran, taken from the plan SUMMARYs; human-judgment rows are typed as such and never counted as machine-verified. |
| T-12-33 | Denial of Service | a failed build leaving production mid-state | low | accept | No application code changed, so the build is a rebuild of the same Worker. A human check watches it. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No packages were installed anywhere in this phase; the lockfile is unchanged. `npm audit --omit=dev --audit-level=high` runs first in Task 1 against that unchanged tree. |
</threat_model>

## Artifacts this phase produces

| Artifact | Owning plan | Tracked? |
|----------|-------------|----------|
| Scratch remote-binding harness (`12-remote-env.mjs`) | 12-01 | no |
| `data/r2/knowledge_md/gift-cards.md` (rewritten) | 12-02 | yes |
| `tests/unit/data/knowledge-gift-cards.test.ts` | 12-02 | yes |
| R2 object `knowledge_md/gift-cards.md` (uploaded) | 12-02 | n/a |
| `pages` row `terms-of-service` (updated) | 12-03 | n/a |
| Vectorize vector `knowledge-gift-cards` (upserted) | 12-04 | n/a |
| `12-PROOF-ORDER.md` | 12-05 | yes |
| `COVERAGE.md` | 12-05 | yes |
| `12-VALIDATION.md` (filled) | **12-06** | yes |

<verification>
- Every CI command passes locally in CI's order, plus `docs:lint`.
- All four requirements re-verify green in one pass, with SHOP-07's account clause recorded as a gap.
- The validation contract is filled from real commands, validated and nyquist-compliant.
- The diff proves no application code, no migration and no config change across the phase.
- No secret-shaped value is anywhere in the pushed range.
- `main` is on `origin` and the tracked tree is clean.
</verification>

<success_criteria>
- All ten CI commands plus `docs:lint` exit 0.
- `12-VALIDATION.md` has zero placeholders, a full per-task map, `status: validated` and `nyquist_compliant: true`.
- Zero files changed under `lib/`, `app/`, `components/` or `migrations/`, and `wrangler.jsonc` is unchanged.
- Zero secret-shaped added lines in the pushed range.
- `git rev-list --count origin/main..HEAD` is 0 and `git status --porcelain --untracked-files=no` is empty.
</success_criteria>

<output>
Create `.planning/phases/12-content-assistant-live-proof/12-06-SUMMARY.md` when done. Record: every gate command with its exit code, the requirement-to-evidence table for all four requirements, the scope-proof diff results, the pushed commit range, and the two human checks left for Russell — the delivery email and the Account -> Gift Cards gap decision. Note plainly that this closes milestone v2.1.
</output>

