---
phase: 11-production-enablement
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - docs/runtime-configuration.md
  - docs/DEPLOYMENT_SETUP.md
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`docs/runtime-configuration.md` carries a delivery-ring table row immediately after the HMAC-ring row and a delivery-ring paragraph immediately after the HMAC-ring paragraph, stating: versioned JSON object keyed by canonical positive integers, current version required, ring bounded to four, the literal `base64:` prefix in front of a payload that decodes to exactly 32 bytes, server-only, never a public variable, never in `wrangler.jsonc` (D-10)."
    - "`docs/DEPLOYMENT_SETUP.md` carries a `## 9. Gift Card Enablement` section, placed immediately before the file's closing tagline so the tagline remains the last line, with five ordered steps: generate and store the four secrets, enable reconciliation, verify reconciliation, enable acquisition, roll back (D-09)."
    - "Every secret command in section 9 uses the non-materializing pipeline form — generation inside a command substitution feeding a single command's stdin — and none uses the value-in-command-text form that section 8 still shows for Stripe."
    - "Section 9 states the rollout order and the reason: enabling acquisition without reconciliation throws at capability resolution, so the order is enforced by code, not convention."
    - "Section 9 tells an operator how to verify honestly: the signed-in gift-card listing distinguishes a healthy ring from a broken one, the public balance endpoint cannot, and a malformed delivery ring surfaces on the five-minute recovery cron even with nothing pending."
    - "`mise exec -- npm run docs:lint` exits 0 after both edits."
  artifacts:
    - "docs/runtime-configuration.md — delivery-ring table row and paragraph"
    - "docs/DEPLOYMENT_SETUP.md — section 9, Gift Card Enablement"
  key_links:
    - "Section 9's step order -> `lib/commerce/capabilities.ts` flag-order throw: the runbook mirrors a rule the code already enforces."
    - "Section 9's verification step -> `lib/observability/scheduled.ts` five-minute drain: the signal an operator watches is the one the code actually emits."
    - "Both docs -> `scripts/docs-lint.mjs`: header convention, resolving doc references, and resolving npm script names are all gated."
  prohibitions:
    - "No value, account id, database id, or worker id in either document."
    - "No copy of section 8's value-in-command-text secret example into section 9."
    - "No edit to any of the four locked ADRs, and no edit to `docs/gsd-ingest-manifest.yaml` — this plan adds no new documentation file."
    - "No `npm run deploy` or `deploy:ci` instruction in the new section; this project deploys by pushing to `main`."
    - "No code change under `lib/gift-cards/`, `lib/services/`, `app/api/`, `lib/commerce/`; no migration; no production command; no push."
---

<objective>
Write down what this phase does, in the two documents an operator actually opens: the
variable contract for the delivery key ring, and a five-step enablement recipe that can be
followed by someone who was not in the room. The recipe teaches the safe pipeline shape,
not the one the Stripe section still shows.

Purpose: OPS-03. A rollout nobody can repeat is a rollout that happens once.
Output: a table row and a paragraph in the runtime contract, and section 9 of the deploy runbook.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-production-enablement/11-CONTEXT.md
@.planning/phases/11-production-enablement/11-RESEARCH.md
@.planning/phases/11-production-enablement/11-PATTERNS.md
@.planning/phases/11-production-enablement/11-01-SUMMARY.md
</context>

## Artifacts this phase produces

Phase-wide set; this plan delivers the rows marked **[11-03]**.

| Artifact | Plan |
|---|---|
| Five ring-shape invariants in `tests/unit/lib/gift-cards/config.test.ts` | 11-01 |
| `.dev.vars` — untracked local development ring | 11-01 |
| Two delivery-ring placeholder lines plus a read-path comment in `.env.example` | 11-02 |
| `tests/unit/scripts/env-example-gift-card-shape.test.ts` | 11-02 |
| **[11-03]** Delivery-ring table row and paragraph in `docs/runtime-configuration.md` | 11-03 |
| **[11-03]** `## 9. Gift Card Enablement` in `docs/DEPLOYMENT_SETUP.md` | 11-03 |
| Four production Worker secret **names** — values exist only inside Cloudflare | 11-04 |
| `STORE_FEATURE_GIFT_CARD_RECONCILIATION: "true"` + regenerated `cloudflare-env.d.ts` | 11-04 |
| `STORE_FEATURE_GIFT_CARD_ACQUISITION: "true"` + regenerated `cloudflare-env.d.ts` | 11-05 |

<tasks>

<task type="auto">
  <name>Task 1: Document the delivery key ring beside the HMAC ring in the runtime contract</name>
  <files>docs/runtime-configuration.md</files>
  <read_first>
    - `docs/runtime-configuration.md` (full file, 84 lines — the variable table at lines 12-28, the gift-card flag paragraph at lines 55-59, the HMAC-ring paragraph at lines 61-68)
    - `lib/gift-cards/config.ts` (full file — the delivery-ring regex, the decode-to-32-bytes check, the four-version bound)
    - `lib/gift-cards/encryption.ts` (lines 1-25 — `GIFT_CARD_DELIVERY_KEY_BYTES`, `MAX_GIFT_CARD_DELIVERY_KEY_VERSIONS`)
    - `lib/commerce/capabilities.ts` (lines 150-220 — the flag-order throw)
    - `lib/observability/scheduled.ts` (full file — the five-minute drain that parses this ring)
    - `lib/services/gift-card-fulfillment.ts` (lines 205-230 — `drainGiftCardDeliveries` parsing the ring unconditionally)
    - `scripts/docs-lint.mjs` (the seven checks — header convention, doc-reference resolution, npm-script-name resolution, credential shapes)
    - `.env.example` (lines 30-50)
    - `docs/DEPLOYMENT_SETUP.md` (lines 446-479)
    - `wrangler.jsonc` (lines 108-126)
    - `package.json` (scripts block)
    - `AGENTS.md` (Rules section, Docs map)
    - `.planning/phases/11-production-enablement/11-PATTERNS.md` (the `docs/runtime-configuration.md` analog — the exact row and paragraph to mirror)
  </read_first>
  <action>
    Implements D-10.

    Edit 1 — the table row. Insert one row immediately after the existing
    `| Gift-card bearer lookup secrets | ... |` row. Purpose column reads
    `Gift-card delivery encryption secrets`. Variable column names
    `GIFT_CARD_DELIVERY_CURRENT_VERSION` and `GIFT_CARD_DELIVERY_KEYS_JSON` as server-only, and
    states in the same parenthetical style as the HMAC row: at most four versioned AES-256 keys,
    each carrying the literal prefix in front of a base64-encoded 32-byte value, never a public
    variable.

    Edit 2 — the paragraph. Insert one paragraph immediately after the existing
    `GIFT_CARD_CODE_HMAC_KEYS_JSON is a JSON object ...` paragraph and before the paragraph that
    begins `Core one-time checkout ...`. Mirror the HMAC paragraph's structure sentence for
    sentence, changing only what is genuinely different for this ring: canonical positive-integer
    property names are key versions; every value carries the literal `base64:` prefix and its
    remainder must decode to exactly 32 bytes, the AES-256 length exported as
    `GIFT_CARD_DELIVERY_KEY_BYTES` from `lib/gift-cards/encryption.ts`; the ring is versioned
    independently of the HMAC ring; the current version must be present; the ring is bounded to
    four keys; store the values in local `.dev.vars` or encrypted Cloudflare secrets, never in
    `wrangler.jsonc`; they are intentionally absent from `StoreConfig`, browser configuration,
    committed deployment files, telemetry, and errors.

    Close that paragraph with the operational fact the HMAC paragraph has no equivalent of, because
    it changes what an operator should watch: the delivery ring is parsed on every scheduled
    delivery drain once reconciliation is enabled, and the parse happens before the drain checks
    whether anything is pending — so a malformed value fails the recovery cron every five minutes
    rather than failing one request when someone finally buys a card.

    Leave the existing gift-card flag paragraph at lines 55-59 alone. It already states the
    reconciliation-before-acquisition rule; a second copy would be one more thing to keep in sync.

    Bump the `**Status:**` line at the top to `**Status:** Current (<today's date, ISO>).` The
    header check requires that line to exist and be non-empty; the date is the file's own
    convention.

    Name no value, no account id, and no database id. `scripts/docs-lint.mjs` only pattern-matches
    UUID-shaped and Stripe-shaped strings, so it would not catch a base64-shaped key pasted here —
    discipline is the control, not the linter.
  </action>
  <acceptance_criteria>
    - `grep -c 'GIFT_CARD_DELIVERY_KEYS_JSON' docs/runtime-configuration.md` is at least `2` (table row plus paragraph).
    - `grep -c 'GIFT_CARD_DELIVERY_CURRENT_VERSION' docs/runtime-configuration.md` is at least `1`.
    - `grep -c 'GIFT_CARD_DELIVERY_KEY_BYTES' docs/runtime-configuration.md` is at least `1`.
    - `grep -c '^| Gift-card delivery encryption secrets' docs/runtime-configuration.md` equals `1`.
    - `grep -c '^\*\*Status:\*\* Current' docs/runtime-configuration.md` equals `1`.
    - `head -1 docs/runtime-configuration.md` still begins with a level-one heading.
    - `mise exec -- npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'GIFT_CARD_DELIVERY_KEYS_JSON' docs/runtime-configuration.md)" -ge 2 && test "$(grep -c '^| Gift-card delivery encryption secrets' docs/runtime-configuration.md)" -eq 1 && test "$(grep -c '^\*\*Status:\*\* Current' docs/runtime-configuration.md)" -eq 1 && mise exec -- npm run docs:lint</automated>
    <fails_when>Exits non-zero: the delivery ring is mentioned fewer than twice (row or paragraph missing), the table row heading is absent or duplicated, the `**Status:**` header line was damaged by the date bump, or `docs:lint` reports a violation — a broken doc reference, an unresolvable npm script name, a credential-shaped string, or a header-convention break.</fails_when>
  </verify>
  <done>The delivery ring is documented beside the HMAC ring with its own shape rules, its own version space, and the one operational consequence that differs, and the docs gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the five-step gift card enablement recipe into the deploy runbook</name>
  <files>docs/DEPLOYMENT_SETUP.md</files>
  <read_first>
    - `docs/DEPLOYMENT_SETUP.md` (full file, 479 lines — especially section 6 step 1b at lines 358-390 for table style, section 4 step 2 at lines 279-296 for the fenced-block-with-inline-comments style, section 8 at lines 446-479 for the numbered-step structure and for the secret-put example this section must NOT copy)
    - `docs/runtime-configuration.md` (full file, as edited by Task 1)
    - `lib/gift-cards/config.ts` (full file — both parsers, so every shape claim in the recipe is checked against source)
    - `lib/gift-cards/encryption.ts` (lines 1-25)
    - `lib/commerce/capabilities.ts` (lines 150-220 — the throw that makes the step order load-bearing)
    - `lib/observability/scheduled.ts` (full file — the drain gate and the failure event, so the verify step names a real signal)
    - `app/api/gift-cards/route.ts` (full file, 32 lines — the authenticated listing that distinguishes a healthy ring from a broken one)
    - `app/api/gift-cards/balance/route.ts` (full file, 43 lines — the deliberately opaque public endpoint)
    - `scripts/docs-lint.mjs` (all seven checks)
    - `.env.example` (lines 30-50)
    - `wrangler.jsonc` (lines 108-126)
    - `package.json` (scripts block — only script names that exist may appear after `npm run`)
    - `AGENTS.md` (Rules section — deploy policy, secrets policy)
    - `.planning/phases/11-production-enablement/11-RESEARCH.md` (Code Examples; the endpoint-behavior section; the telemetry section)
    - `.planning/phases/11-production-enablement/11-PATTERNS.md` (the `docs/DEPLOYMENT_SETUP.md` analog — the three in-file style analogs and the explicit "do not copy section 8's secret-put line")
  </read_first>
  <action>
    Implements D-09.

    Insert a new `## 9. Gift Card Enablement` section immediately before the file's closing tagline
    line, so that tagline remains the true final line of the file. Precede the new section with the
    same `---` separator the other sections use.

    Open with two sentences: gift cards ship disabled; enabling them is four secrets and two feature
    flags applied in a fixed order, and `docs/runtime-configuration.md` owns the variable contract.
    Link that document by path so the docs linter's reference check exercises it.

    Step 1, generate and store the four secrets. Introduce the pipeline in one sentence: each value
    is generated and piped straight into Cloudflare in a single command, so it is never printed,
    never held in a shell variable, and never written to a file. Then a fenced bash block with the
    four commands, copied verbatim from 11-RESEARCH.md "Code Examples" — the two version variables
    fed by a `printf` of the digit one, and the two rings fed by a `printf` format string whose
    substitution is `"$(openssl rand -base64 32)"`, with the delivery ring's format string carrying
    the literal `base64:` prefix inside the JSON value and the HMAC ring's not. After the block,
    two sentences of why: the HMAC ring uses the generated string's raw bytes as key material so it
    takes no prefix; the delivery ring is an AES-256 key so it must carry the prefix in front of a
    payload that decodes to exactly 32 bytes. Then a second short block showing the names-only
    proof, `npx wrangler secret list`, with a sentence saying the proof is the four names and never
    a value. Close the step with the failure mode: if the command reports that the latest version of
    the Worker is not currently deployed, deploy the current `main` first and then retry the four
    commands.

    Do not copy section 8's Stripe example, which puts the literal value in the command text and
    therefore in shell history. Section 9 uses the substitution form throughout. Do not add a
    correction or a warning about section 8 — that is a separate change to a section this phase does
    not own.

    Step 2, enable reconciliation. State the exact `vars` entry to add to `wrangler.jsonc`:
    `"STORE_FEATURE_GIFT_CARD_RECONCILIATION": "true"`, a string, next to the subscription flags.
    Then: regenerate the generated Cloudflare types with `npm run cf-typegen` and confirm with
    `npm run cf-typecheck`, and note the one non-obvious requirement — `wrangler types` reads local
    env files, including `.dev.vars`, so both must be moved out of the working tree before either
    command runs and moved back afterwards, or the generated file will pick up local-only names that
    CI does not have. Then: commit `wrangler.jsonc` and the regenerated types together and push to
    `main`; Cloudflare Workers Builds deploys the push. Do not name `npm run deploy` or `deploy:ci`
    anywhere in this section.

    Step 3, verify reconciliation before going further. Three checks, as a short list. First, the
    new version appears in `npx wrangler deployments list`. Second, one five-minute recovery cron
    cycle completes cleanly: watch the Worker's tail and expect the recovery-drain success log,
    with no critical recovery-failure telemetry event in the same window — and explain why this is
    the sharp check, namely that the delivery ring is parsed on every tick before the drain checks
    whether anything is pending, so a malformed ring shows up within one cycle even with zero
    deliveries queued. Third, a signed-in request to the account gift-card listing endpoint returns
    a `cards` array; a 503 there means the ring or the database is unhealthy. Add the honest caveat:
    the public balance endpoint answers identically for a bad code, an unknown card, and a broken
    ring, by design, so it proves availability and nothing about ring health.

    Step 4, enable acquisition, and only after step 3. The exact entry is
    `"STORE_FEATURE_GIFT_CARD_ACQUISITION": "true"`; same regenerate-commit-push cycle. State the
    reason the order is not advisory: acquisition enabled without reconciliation throws at
    capability resolution, on the first request or cron tick after such a deploy.

    Step 5, rolling back. Set acquisition back to the string `"false"` and push to stop new
    gift-card sales; leave reconciliation enabled for as long as any balance or reservation exists,
    so existing cards can still be verified, settled, and released. To rotate a ring, add a second
    version to the JSON object and move the current-version pointer; never remove a key version that
    has issued cards under it.

    Throughout: name no value, no account id, no database id, no worker id. Use plain `npx wrangler`
    to match the command style already used in sections 6 and 8 of this file — the version-manager
    prefix this repository's agents use is a working convention, not part of the operator-facing
    runbook. Only `npm run` script names that exist in `package.json` may appear, because the docs
    linter resolves every one of them.
  </action>
  <acceptance_criteria>
    - `grep -c '^## 9\. Gift Card Enablement' docs/DEPLOYMENT_SETUP.md` equals `1`.
    - `tail -1 docs/DEPLOYMENT_SETUP.md` is still the file's closing tagline line.
    - `grep -cE 'GIFT_CARD_(CODE_HMAC|DELIVERY)_(CURRENT_VERSION|KEYS_JSON)' docs/DEPLOYMENT_SETUP.md` is at least `4` — all four names appear.
    - `grep -c 'STORE_FEATURE_GIFT_CARD_RECONCILIATION' docs/DEPLOYMENT_SETUP.md` is at least `1` and `grep -c 'STORE_FEATURE_GIFT_CARD_ACQUISITION' docs/DEPLOYMENT_SETUP.md` is at least `1`.
    - `grep -c 'wrangler secret list' docs/DEPLOYMENT_SETUP.md` is at least `1`.
    - `awk '/^## 9\./,0' docs/DEPLOYMENT_SETUP.md | grep -cE 'npm run (deploy|deploy:ci)'` equals `0` — the new section does not tell anyone to deploy from a developer machine.
    - `awk '/^## 9\./,0' docs/DEPLOYMENT_SETUP.md | grep -cE 'echo "[a-z]+_(live|test)_'` equals `0` — section 8's value-in-command-text form was not copied forward.
    - `mise exec -- npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '^## 9\. Gift Card Enablement' docs/DEPLOYMENT_SETUP.md)" -eq 1 && test "$(grep -cE 'GIFT_CARD_(CODE_HMAC|DELIVERY)_(CURRENT_VERSION|KEYS_JSON)' docs/DEPLOYMENT_SETUP.md)" -ge 4 && test "$(awk '/^## 9\./,0' docs/DEPLOYMENT_SETUP.md | grep -cE 'npm run (deploy|deploy:ci)')" -eq 0 && test "$(tail -1 docs/DEPLOYMENT_SETUP.md | grep -c 'ready for production')" -eq 1 && mise exec -- npm run docs:lint</automated>
    <fails_when>Exits non-zero: the section heading is missing or duplicated, fewer than four secret names appear (a step was summarised away), the new section instructs a machine-local deploy, the closing tagline is no longer the last line (the section was appended instead of inserted), or `docs:lint` reports a violation such as an unresolvable `npm run` name or a broken doc reference.</fails_when>
  </verify>
  <done>An operator who has never seen this phase can follow section 9 top to bottom and end with gift cards live, using the safe pipeline shape, in the order the code enforces.</done>
</task>

</tasks>

<!-- planner-discipline-allow: npm run deploy -->
<!-- planner-discipline-allow: cron.recovery_failed -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook text -> operator's shell | A command pattern documented here is the pattern that gets run, on production, by someone in a hurry. |
| public repository -> the internet | Both files are public; anything value-shaped in them is disclosed permanently. |
| runbook claims -> live system behavior | A verification step that cannot actually detect failure teaches false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-09 | Information Disclosure | Section 9's secret commands | high | mitigate | Section 9 documents only the substitution form, where the value exists solely inside a command substitution feeding one stdin. An acceptance criterion greps the new section for the value-in-command-text form and requires zero matches. |
| T-11-10 | Information Disclosure | Either document containing a real key | high | mitigate | No value, account id, database id, or worker id is written. Noted explicitly: the docs linter matches UUID and Stripe shapes only and would not catch a base64-shaped key, so this is enforced by review and by the phase's git-history scan in 11-04, not by tooling. |
| T-11-11 | Repudiation | A verification step that cannot detect failure | medium | mitigate | Step 3 names the signal the code actually emits — the recovery-cron drain — and states plainly that the public balance endpoint is opaque by design and proves nothing about ring health. |
| T-11-12 | Elevation of Privilege | Documented flag order encouraging the wrong sequence | medium | mitigate | Steps 2 through 4 fix the order and give the code-level reason: acquisition without reconciliation throws at capability resolution. Step 5 documents the reverse order for rollback. |
| T-11-13 | Tampering | Machine-local deploy instruction bypassing the CI migration gate | medium | mitigate | Acceptance criterion requires zero machine-local deploy script mentions inside section 9; the documented deploy action is a push to `main`. |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; no new dependency to vet. |
</threat_model>

<verification>
- `mise exec -- npm run docs:lint` exits 0.
- `mise exec -- npm run lint` exits 0.
- `tail -1 docs/DEPLOYMENT_SETUP.md` is the closing tagline.
- `git diff --stat -- docs/` shows changes confined to the two files in `files_modified`.
- `git log -p -1 -- docs/ | grep -cE '(sk|pk|rk)_(test|live)_[A-Za-z0-9]{20,}'` equals 0 for the commit this plan produces.
</verification>

<success_criteria>
The delivery key ring has a written contract next to the HMAC ring's, and the deploy runbook
has a five-step recipe that produces this phase's outcome — using the pipeline shape that does
not leak, in the order the code enforces, with a verification step that can actually fail.
</success_criteria>

<output>
Create `.planning/phases/11-production-enablement/11-03-SUMMARY.md` when done. Record the
inserted section number and its position relative to the closing tagline. Record no value.
</output>
