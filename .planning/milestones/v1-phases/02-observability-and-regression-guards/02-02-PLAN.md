---
phase: 02-observability-and-regression-guards
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - wrangler.jsonc
  - cloudflare-env.d.ts
  - lib/observability/route-template.ts
  - app/api/analytics/vitals/route.ts
  - tests/unit/lib/observability/route-template.test.ts
  - tests/unit/app/api/vitals-route.test.ts
autonomous: true
requirements: [OBS-02]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A production web-vitals beacon for a known metric results in exactly one Analytics Engine writeDataPoint call carrying exactly five values: metric name, value, rating, route template, and isMobile."
    - "An operator can group the dataset by route template and metric name and read mobile LCP, INP, and CLS from real traffic, because route template is the dataset index and isMobile is a stored blob."
    - "The vitals route never returns a status other than 200 — not for a malformed body, not for a missing binding, not for a writeDataPoint that throws."
    - "A beacon whose metric name is outside the fixed set CLS, FCP, INP, LCP, TTFB, touch-latency, or whose value is missing, non-numeric, non-finite, or negative, is dropped without a writeDataPoint call and the route still returns HTTP 200."
    - "An empty request body, a body that is not JSON, a JSON body that is null, and a JSON body that is an array each produce no writeDataPoint call and an HTTP 200 response."
    - "The route template written to Analytics Engine is one of a closed set of ASCII string literals produced by the mapper; it is never assembled from beacon text, so every possible value is well under the Analytics Engine 96-byte index cap and contains no control characters."
    - "Every path the mapper does not recognize — including a path with a query string, a fragment, a percent-encoded segment, an absolute URL, a path with more segments than any known route, and an empty or missing url field — maps to the single other bucket."
    - "The route holds no module-level mutable state and does not await the Analytics Engine write, so two beacons handled concurrently produce two independent rows and neither can delay or change the other's 200 response."
    - "When the WEB_VITALS binding is absent from env, the route emits one analytics.vitals_sink_unavailable warning event carrying only allow-listed enum values, performs no write, and still returns 200."
    - "The committed cloudflare-env.d.ts diff adds only the WEB_VITALS binding declaration; it introduces no variable name that exists only in the developer's local .env.local."
  artifacts:
    - path: "lib/observability/route-template.ts"
      provides: "Pure allowlist-based pathname to bounded route-template mapper with a single fallback bucket"
      exports: ["ROUTE_TEMPLATES", "OTHER_ROUTE_TEMPLATE", "toRouteTemplate"]
    - path: "app/api/analytics/vitals/route.ts"
      provides: "Validating beacon ingestion that writes the five permitted fields to Analytics Engine and always answers 200"
    - path: "wrangler.jsonc"
      provides: "The analytics_engine_datasets binding the route writes through"
      contains: "WEB_VITALS"
    - path: "tests/unit/lib/observability/route-template.test.ts"
      provides: "Bounded-cardinality coverage for the mapper, including the byte and ASCII bound on every template"
      min_lines: 40
    - path: "tests/unit/app/api/vitals-route.test.ts"
      provides: "Five-field write shape, drop-and-200 validation cases, and missing-binding behavior"
      min_lines: 60
  key_links:
    - from: "app/api/analytics/vitals/route.ts"
      to: "lib/observability/route-template.ts"
      via: "toRouteTemplate called on the beacon's url before anything is written"
      pattern: "toRouteTemplate\\("
    - from: "app/api/analytics/vitals/route.ts"
      to: "wrangler.jsonc"
      via: "getCloudflareContext().env.WEB_VITALS resolved against the analytics_engine_datasets binding"
      pattern: "WEB_VITALS"
    - from: "app/api/analytics/vitals/route.ts"
      to: "lib/observability/telemetry.ts"
      via: "recordTelemetry with the event plan 02-01 registered, emitted only on the missing-binding path"
      pattern: "recordTelemetry\\('analytics\\.vitals_sink_unavailable'"
  prohibitions:
    - statement: "MUST NOT store the beacon's url, userAgent, id, timestamp, IP address, or any other per-visitor value in the Analytics Engine dataset; the five permitted fields are the entire payload."
    - statement: "MUST NOT let attacker-supplied beacon content determine the Analytics Engine index value or introduce a new route-template bucket; the mapper's output set is fixed at build time."
    - statement: "MUST NOT commit a cloudflare-env.d.ts regenerated in a shell where .env.local was present, because wrangler folds local secret variable names into the generated interface."
    - statement: "MUST NOT return a non-200 status to a vitals beacon under any input, because the browser sends it during page unload and a failure is invisible to the user but noisy to the operator."
---

<objective>
Turn `/api/analytics/vitals` from a route that answers `ok` and throws the payload away into a real
queryable sink: one new Analytics Engine binding, a server-side route-template mapper with bounded
output, and a rewritten handler that validates, writes exactly five fields, and answers 200 no
matter what.

Purpose: OBS-02. Core Web Vitals instrumentation has been shipping to a black hole. After this
plan an operator can query mobile LCP, INP, and CLS by route template from real production traffic.

Output: the `WEB_VITALS` binding in `wrangler.jsonc`, a regenerated `cloudflare-env.d.ts`, a new
`lib/observability/route-template.ts`, a rewritten vitals route, and two new test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability-and-regression-guards/02-CONTEXT.md
@.planning/phases/02-observability-and-regression-guards/02-RESEARCH.md
@.planning/phases/02-observability-and-regression-guards/02-PATTERNS.md
@.planning/phases/02-observability-and-regression-guards/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

## Notes for the executor

Project commands run under Node 24 via mise. If a plain `npx`/`npm` command fails with a Node
version error, prefix it with `mise exec --`.

`npm run cf-typecheck` is authoritative in CI only. Do not run it locally as a gate.

Decision IDs referenced below are defined in the table in `02-01-PLAN.md`. This plan implements
**D-01**, **D-02**, **D-03**, and **D-04**.

Plan `02-01` already registered `analytics.vitals_sink_unavailable` in the closed taxonomy at
`severity: 'warning'`, `sampleRate: 0.01`. Do not add it again and do not change its rate.

`scripts/check-deploy-config.mjs` needs no change. `02-RESEARCH.md` Pitfall 3 records a full read of
that script: it validates placeholder strings, `ORDER_STATUS_SECRET`, and the two
`NEXT_PUBLIC_*` values only, and never inspects any binding array. `02-CONTEXT.md`'s "if it
validates bindings" hedge resolves to no.

The three negative gates in Task 3 are comment-filtered on purpose: the field names they forbid have
to be nameable in this plan so the executor knows which ones to remove, and a stray explanatory
comment in the route must not be able to fail a gate that is about executable code.

<!-- planner-discipline-allow: userAgent -->
<!-- planner-discipline-allow: timestamp -->
<!-- planner-discipline-allow: console\.(error|warn) -->
<!-- planner-discipline-allow: status:\s*[0-9]+ -->

<tasks>

<task type="auto">
  <name>Task 1: Add the Analytics Engine binding and regenerate the types safely</name>
  <precondition>The working tree has no uncommitted change to `cloudflare-env.d.ts` (`git diff --quiet -- cloudflare-env.d.ts` exits 0), so the regenerated diff is attributable to this task alone.</precondition>
  <reversibility rating="costly">Undoing the binding means another `cf-typegen` run and a redeploy to drop the dataset reference; the dataset itself is created lazily by the first write and holds only aggregate rows, so there is no migration and no consumer contract to break.</reversibility>
  <files>wrangler.jsonc, cloudflare-env.d.ts</files>
  <read_first>
    - `wrangler.jsonc` lines 26-51 — the existing `d1_databases`, `r2_buckets`, `vectorize`, and `ai` binding blocks. The new array follows the same top-level shape and belongs directly after `vectorize`.
    - `workers/observability-tail/producer-bindings.example.jsonc` — the already-documented `analytics_engine_datasets` entry shape for the sibling `COMMERCE_ANALYTICS` binding.
    - `cloudflare-env.d.ts` — the current generated `CloudflareEnv` interface, so you can recognize an unexpected addition in the diff.
    - `.planning/phases/01-security-and-admin-auth-truth/deferred-items.md`, the entry titled "cf-typecheck reports stale generated types" — the root-cause note explaining exactly how a local regeneration corrupts this file.
    - `package.json` `scripts.cf-typegen` — `wrangler types --env-interface CloudflareEnv --strict-vars false ./cloudflare-env.d.ts`.
  </read_first>
  <action>
    Add one `analytics_engine_datasets` array to `wrangler.jsonc`, immediately after the `vectorize`
    array and before `"ai"`, holding a single entry with binding `WEB_VITALS` and dataset
    `mercora_web_vitals` (per D-01; the exact names were left to Claude's Discretion and are fixed
    here — plan `02-02` Task 3 and the tests reference them verbatim).

    This is a second, separate binding. Do not touch, rename, or reuse `COMMERCE_ANALYTICS`:
    `lib/observability/telemetry.ts` writes a fixed four-blob severity shape through that name, and
    `02-RESEARCH.md` Pitfall 6 records that wiring it is a scope decision outside this phase.

    Then regenerate the types with this exact sequence, which is the whole point of the task:

    ```
    mv .env.local .env.local.bak && npm run cf-typegen; mv .env.local.bak .env.local
    ```

    The semicolon before the restore is deliberate: the restore must run even when `cf-typegen`
    exits non-zero, so a failed generation cannot leave the developer's local environment file
    parked under a backup name. If `.env.local` does not exist, run `npm run cf-typegen` alone.

    Inspect `git diff -- cloudflare-env.d.ts` before staging. The only additions you should see are
    the `WEB_VITALS` binding declaration and, if wrangler reorders, whitespace. If the diff contains
    any variable name that exists only in `.env.local` (secret-shaped names such as API keys or
    tokens), or a rewritten absolute `mainModule` import path, discard the regeneration with
    `git checkout -- cloudflare-env.d.ts` and repeat the move-aside sequence — a leaked local
    variable name in the tracked types is the exact failure Phase 1 recorded.
  </action>
  <acceptance_criteria>
    - `wrangler.jsonc` parses as JSONC and contains an `analytics_engine_datasets` array with exactly one entry whose `binding` is `WEB_VITALS` and whose `dataset` is `mercora_web_vitals`.
    - `wrangler.jsonc` still contains the `d1_databases`, `r2_buckets`, `vectorize`, `ai`, `send_email`, and `ratelimits` blocks unchanged.
    - `cloudflare-env.d.ts` declares `WEB_VITALS` as an `AnalyticsEngineDataset`.
    - `git diff --numstat -- cloudflare-env.d.ts` shows fewer than 10 added lines.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"analytics_engine_datasets"' wrangler.jsonc && grep -q '"WEB_VITALS"' wrangler.jsonc && grep -q '"mercora_web_vitals"' wrangler.jsonc</automated>
    <fails_when>non-zero exit, meaning the binding array, the binding name, or the dataset name is missing from the deploy configuration</fails_when>
    <automated>grep -q 'WEB_VITALS' cloudflare-env.d.ts</automated>
    <fails_when>non-zero exit, meaning the types were not regenerated and the route's binding access will not typecheck</fails_when>
    <automated>ADDED=$(git diff HEAD --numstat -- cloudflare-env.d.ts | awk '{print $1}'); test -n "$ADDED" && test "$ADDED" -ge 1 && test "$ADDED" -lt 10</automated>
    <fails_when>non-zero exit: an empty result means the file was never regenerated, and a count of 10 or more is the signature of local environment variable names being folded into the committed interface</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('wrangler.jsonc','utf8');const j=s.replace(/^\s*\/\/.*$/gm,'').replace(/\/\*[\s\S]*?\*\//g,'');const c=JSON.parse(j);if(c.analytics_engine_datasets.length!==1)process.exit(1);if(c.analytics_engine_datasets[0].binding!=='WEB_VITALS')process.exit(1);if(!c.d1_databases||!c.r2_buckets||!c.vectorize)process.exit(1)"</automated>
    <fails_when>non-zero exit, meaning wrangler.jsonc no longer parses, has more or fewer than one Analytics Engine dataset, names the wrong binding, or lost one of the pre-existing binding arrays</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit, or an "error" line appears in the tsc output</fails_when>
  </verify>
  <done>The Worker declares one Analytics Engine dataset binding named `WEB_VITALS`, the generated types know about it, and the committed type diff carries nothing from the developer's local environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded route-template mapper</name>
  <files>lib/observability/route-template.ts, tests/unit/lib/observability/route-template.test.ts</files>
  <read_first>
    - `lib/observability/telemetry.ts` lines 6-22 — `TELEMETRY_PATHS`, a `ReadonlySet<string>` allowlist used to bound cardinality. Lines 164-173 — `queryFreePath`, which splits on `[?#]`, length-bounds the result, rejects control characters, and returns `undefined` unless the value is in the allowlist. Copy this defensive shape.
    - `lib/hooks/useWebVitals.ts` lines 32-40 — the beacon builder. It already sends `url: window.location.pathname`, so the incoming value is normally a pathname, not an absolute URL; the mapper must still survive an absolute URL because the field is attacker-controllable.
    - `lib/hooks/useWebVitals.ts` lines 57-61 and 75-96 — the complete set of metric names the client can send: the five web-vitals library metrics plus the synthesized `touch-latency`.
    - The dynamic route inventory, confirmed by `find app -type d -name '[*]'`: `app/[slug]`, `app/blog/[slug]`, `app/category/[slug]`, `app/product/[slug]`, `app/order-status/[id]`, `app/account/orders/[id]`, `app/admin/blog/[id]`, `app/admin/categories/[id]`, `app/admin/orders/[id]`, `app/media/[...key]`. Only user-facing page routes matter for web vitals; API routes never produce a beacon.
    - `.planning/codebase/TESTING.md` "Test Structure" — flat `describe`, sentence-style `it` names.
  </read_first>
  <behavior>
    - `/` maps to the home template.
    - `/product/arctic-pulse-tool` and `/product/anything-else` both map to the same product template.
    - `/category/camping`, `/blog/some-post`, `/order-status/ord_123`, `/account/orders/ord_123`, `/admin/orders/ord_123`, `/admin/blog/7`, `/admin/categories/7` each map to their own single template.
    - `/checkout`, `/cart`, and any other known static page map to a template naming that page.
    - An unrecognized path maps to the fallback bucket: `/does/not/exist`, `/product` with no slug, `/product/a/b` with an extra segment, `/PRODUCT/x` in the wrong case, and a path with a traversal segment.
    - Non-pathname input maps to the fallback bucket without throwing: `undefined`, `null`, the empty string, a string that does not start with a slash, an absolute URL such as one with a scheme and host, and a value longer than 512 characters.
    - Query strings and fragments are stripped before matching, so a known path carrying a query string still maps to its own template rather than the fallback.
    - A path containing a control character maps to the fallback bucket.
    - A percent-encoded slug maps to the same template as an unencoded one, because only the segment count and the leading segments are matched.
    - Every member of the exported template set, including the fallback, is pure ASCII and at most 96 bytes when UTF-8 encoded.
    - The mapper is pure: calling it twice with the same input returns the same string, and it never returns a value outside the exported set.
  </behavior>
  <action>
    Create `lib/observability/route-template.ts` implementing D-03: a small pure module with no
    imports, no state, and no I/O.

    Export three things. `ROUTE_TEMPLATES` — a `ReadonlySet<string>` holding every template the
    mapper can return, including the fallback. `OTHER_ROUTE_TEMPLATE` — the single fallback bucket
    string. `toRouteTemplate(value: unknown): string` — the mapper, returning a member of
    `ROUTE_TEMPLATES` for every possible input.

    Implement `toRouteTemplate` in the same defensive order `queryFreePath` uses:

    1. Reject non-strings, values that do not start with a slash, and values longer than 512
       characters, returning the fallback. Rejecting anything that is not a leading-slash pathname
       is what makes an absolute URL fall through to the fallback rather than being parsed.
    2. Split on the first `?` or `#` and keep the left side.
    3. Reject the result if it is empty, longer than 128 characters, or matches
       the same control-character range `queryFreePath` rejects (U+0000 to U+001F plus U+007F).
    4. Match the remaining pathname against a static table. Use exact-string equality for static
       pages and a segment-count-plus-prefix match for dynamic ones — split the pathname on `/`,
       drop the leading empty element, and compare the leading segments and the total segment count
       against each known dynamic route. Do not build a regular expression per page; the whole point
       of D-03 is that the output set is fixed at build time, and a segment comparison makes that
       obvious to a reader.
    5. Return the fallback for anything unmatched.

    Cover, at minimum, the home route, the four public dynamic page routes
    (`/product/[slug]`, `/category/[slug]`, `/blog/[slug]`, `/order-status/[id]`), the account and
    admin dynamic page routes (`/account/orders/[id]`, `/admin/orders/[id]`, `/admin/blog/[id]`,
    `/admin/categories/[id]`), and the static pages that carry commerce traffic — at least
    `/checkout` and `/cart`. Choose the literal template strings so a reader of the dataset can map
    a row back to a route at a glance; the Next.js bracket form is the natural choice and keeps the
    strings ASCII.

    Do not add `app/[slug]` as its own template: it is the CMS catch-all and would match anything,
    which is precisely the unbounded behavior the fallback bucket exists to prevent. Unmatched CMS
    pages belong in the fallback.

    Then write `tests/unit/lib/observability/route-template.test.ts` covering every behavior above.
    For the ASCII and byte-length assertion, iterate `ROUTE_TEMPLATES` and assert each member
    satisfies `/^[\x20-\x7e]+$/` and has a `TextEncoder().encode(t).byteLength` of at most 96 — the
    Analytics Engine index cap cited in `02-RESEARCH.md`. That single loop is what keeps the index
    bound true as templates are added later.
  </action>
  <acceptance_criteria>
    - `lib/observability/route-template.ts` exists and exports `ROUTE_TEMPLATES`, `OTHER_ROUTE_TEMPLATE`, and `toRouteTemplate`.
    - The module has zero import statements.
    - `ROUTE_TEMPLATES` contains at least 11 members and every member is 96 bytes or fewer in UTF-8 and matches printable ASCII.
    - Every behavior bullet above has a corresponding assertion, and the test file contains at least 12 `it(` blocks.
    - `npx vitest run tests/unit/lib/observability/route-template.test.ts` reports 0 failed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/lib/observability/route-template.test.ts</automated>
    <fails_when>non-zero exit, or the word "failed" appears in the Test Files or Tests summary lines</fails_when>
    <automated>test "$(grep -c '  it(' tests/unit/lib/observability/route-template.test.ts)" -ge 12</automated>
    <fails_when>non-zero exit, meaning fewer than 12 cases were written and the behavior list is not fully covered</fails_when>
    <automated>! grep -Eq '^\s*import ' lib/observability/route-template.ts</automated>
    <fails_when>non-zero exit, meaning the mapper acquired a dependency and is no longer a self-contained pure module</fails_when>
    <automated>grep -q 'byteLength' tests/unit/lib/observability/route-template.test.ts && grep -q '96' tests/unit/lib/observability/route-template.test.ts && grep -q 'OTHER_ROUTE_TEMPLATE' tests/unit/lib/observability/route-template.test.ts</automated>
    <fails_when>non-zero exit, meaning the test file omits the UTF-8 byte-length bound assertion, the 96-byte Analytics Engine index cap, or the fallback-bucket assertion, so the passing vitest run above proves less than it appears to</fails_when>
  </verify>
  <done>Every possible beacon pathname, hostile or ordinary, resolves to one of a fixed, ASCII, index-safe set of route templates, and a test fails if that set ever stops being bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewrite the vitals route to write five fields and always answer 200</name>
  <precondition>`env.WEB_VITALS` is declared in `cloudflare-env.d.ts` (Task 1 complete), so the binding access typechecks without a cast to `any`.</precondition>
  <files>app/api/analytics/vitals/route.ts, tests/unit/app/api/vitals-route.test.ts</files>
  <read_first>
    - `app/api/analytics/vitals/route.ts` (whole file, 33 lines) — the current `AnalyticsPayload` interface, the non-production log block at lines 18-26, the `{ status: "ok" }` return at line 28, and the catch block at lines 29-32 that logs the caught exception through the console and returns HTTP 500.
    - `tests/unit/observability/instrumentation-source.test.ts` lines 40-54 and 77-82 — the AST rule that makes that catch block load-bearing: once this file calls `recordTelemetry`, it may no longer contain a console call whose argument list includes the bare identifier `error`.
    - `app/api/gift-cards/route.ts` lines 1 and 16 — the repository's async binding-access pattern, `const { env } = await getCloudflareContext({ async: true });`. Use this form, not the synchronous variant `lib/observability/telemetry.ts` uses internally.
    - `lib/observability/telemetry.ts` lines 144-150 — the `AnalyticsWriter` interface, which is the `writeDataPoint({ blobs, doubles, indexes })` shape. Lines 289-300 — `writeMetric`, the existing example of a guarded `writeDataPoint` call that swallows its own failure.
    - `lib/observability/route-template.ts` — the module created in Task 2.
    - `lib/hooks/useWebVitals.ts` lines 32-40 and 75-96 — the exact payload the client sends and the six possible metric names.
    - `tests/unit/app/api/stripe-webhook-refunds.test.ts` lines 1-45 — the repository's convention for route-handler tests: `vi.hoisted` mock holders, `vi.mock` of the collaborating modules, `import { POST } from '@/app/...'`, and a `NextRequest` built with an absolute test URL.
  </read_first>
  <behavior>
    - A production beacon with a known metric name, a finite non-negative value, a rating, a recognized url, and `isMobile: true` produces exactly one `writeDataPoint` call, and the route responds 200.
    - That single call carries the five permitted values and nothing else: metric name, rating, route template, and the mobile flag as blobs; the metric value as the only double; and the route template as the only index.
    - No `writeDataPoint` argument contains the beacon's `url`, `userAgent`, `id`, or `timestamp` values.
    - An unknown metric name produces no `writeDataPoint` call and a 200 response.
    - A missing `value`, a `value` that is a string, `NaN`, `Infinity`, or a negative number each produce no `writeDataPoint` call and a 200 response.
    - A missing or unrecognized `rating` is written as a fixed `unknown` literal rather than being dropped or forwarded raw.
    - An unrecognized url is written as the mapper's fallback bucket, not as the raw path.
    - An empty body, a non-JSON body, a JSON `null` body, and a JSON array body each produce no `writeDataPoint` call and a 200 response.
    - When `env.WEB_VITALS` is absent, the route emits `analytics.vitals_sink_unavailable` once, performs no write, and returns 200.
    - When `env.WEB_VITALS.writeDataPoint` throws, the route still returns 200.
    - When `getCloudflareContext` itself rejects, the route still returns 200.
    - The response body is `{ status: "ok" }` in every case above, so the client cannot distinguish a stored beacon from a dropped one.
  </behavior>
  <action>
    Rewrite `app/api/analytics/vitals/route.ts` to implement D-02, D-03, and D-04.

    Keep the module's existing shape: a single exported `async function POST(request: NextRequest)`
    returning `NextResponse.json({ status: "ok" })`. Keep the existing non-production
    `console.log` block exactly as it is (D-04 says so explicitly) — it logs a formatted object, not
    a caught exception, so it does not trip the AST contract.

    Add at module scope a `ReadonlySet<string>` of the six metric names the client can send (`CLS`,
    `FCP`, `INP`, `LCP`, `TTFB`, `touch-latency`, sourced from `lib/hooks/useWebVitals.ts`) and a
    `ReadonlySet<string>` of the three ratings web-vitals emits (`good`, `needs-improvement`,
    `poor`). Both are allow-lists: an unrecognized metric name is a drop, an unrecognized rating
    becomes a fixed `unknown` literal. This is what closes the beacon's cardinality, which is the
    only thing standing between an unauthenticated public endpoint and an unbounded blob dimension.

    Handler body, in order:

    1. Parse the body inside a `try`. On any parse failure, or when the parsed value is not a
       non-null non-array object, skip straight to the 200 response with no write.
    2. Validate: the metric name must be a string in the metric allow-list; the value must satisfy
       `typeof v === 'number' && Number.isFinite(v) && v >= 0`. If either fails, skip to the 200
       response with no write. Round the value with `Math.round` before storing so the double is an
       integer count of milliseconds for the timing metrics; CLS is unitless and sub-1, so store
       `Math.round(value * 1000)` for it — record that scaling in a one-line comment naming the
       metric so an operator reading the dataset divides correctly. State the unit choice once and
       apply it consistently.
    3. Derive `routeTemplate` by calling `toRouteTemplate(metric.url)`. Never read `metric.url`
       again after this line.
    4. Resolve the binding with `const { env } = await getCloudflareContext({ async: true });` inside
       its own `try`. A rejection is treated exactly like an absent binding.
    5. If the binding is absent or has no callable `writeDataPoint`, call
       `recordTelemetry('analytics.vitals_sink_unavailable', { operation: 'persist', outcome:
       'unavailable', provider: 'analytics' })` and skip the write. Those three values are already
       in `ALLOWED_FIELD_ENUMS` and need no taxonomy change. Pass no `path` field: `sanitizeTelemetryFields`
       only keeps a path that is a member of `TELEMETRY_PATHS`, and this route is not one, so the
       field would be silently dropped anyway.
    6. Otherwise call `writeDataPoint` once inside its own `try`/`catch` whose catch body is empty
       apart from a comment, mirroring `writeMetric`'s fail-open shape. Do not `await` it and do not
       let it gate the response.
    7. Return `NextResponse.json({ status: "ok" })`.

    Replace the existing catch block entirely. The route now returns 200 on every path (D-04: never
    a 500 for a beacon), so the outer catch has nothing to convert into an error response. It must
    also stop forwarding the caught exception to the console, because this file now calls
    `recordTelemetry` and the AST contract in `tests/unit/observability/instrumentation-source.test.ts`
    rejects that combination. Prefer no outer catch at all: with each risky step already wrapped,
    there is nothing left to catch.

    Update the `AnalyticsPayload` interface to describe only what the handler reads — name, value,
    rating, url, isMobile — and drop `id`, `timestamp`, and `userAgent` from it (D-02). The client
    still sends those fields; the point is that the server type no longer has a name for them, so
    no future edit can reach one by accident.

    Then write `tests/unit/app/api/vitals-route.test.ts` covering every behavior above. Mock
    `@opennextjs/cloudflare`'s `getCloudflareContext` with a `vi.hoisted` holder so each test can
    supply an env with a spy `writeDataPoint`, an env without the binding, or a rejection. Assert
    the write shape by reading the spy's first call argument and checking each array's exact
    contents and length, not just that it was called. For the no-identifier assertions, send a
    beacon whose `url`, `userAgent`, and `id` carry distinctive sentinel strings and assert
    `JSON.stringify(spy.mock.calls[0][0])` contains none of them.
  </action>
  <acceptance_criteria>
    - Every behavior bullet above has a passing test, and the test file contains at least 14 `it(` blocks.
    - `app/api/analytics/vitals/route.ts` contains exactly one `writeDataPoint(` call and exactly one `recordTelemetry(` call.
    - The route file contains no `console.error` and no `console.warn`, and retains its non-production `console.log` block.
    - The route file never returns a status other than 200: no `status: 500` and no other numeric status appears in it.
    - `AnalyticsPayload` declares no `userAgent`, `timestamp`, or `id` member.
    - `tests/unit/observability/instrumentation-source.test.ts` passes with 3 passing assertions.
    - `npm test`, `npm run lint`, and `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/app/api/vitals-route.test.ts tests/unit/lib/observability/route-template.test.ts tests/unit/observability/instrumentation-source.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than "3 passed" for Test Files, or the word "failed" appears in the summary</fails_when>
    <automated>test "$(grep -c 'writeDataPoint(' app/api/analytics/vitals/route.ts)" -eq 1 && test "$(grep -c 'recordTelemetry(' app/api/analytics/vitals/route.ts)" -eq 1</automated>
    <fails_when>non-zero exit, meaning the route writes to Analytics Engine more than once per beacon, writes zero times, or emits the missing-binding warning from more than one place</fails_when>
    <automated>! grep -v '^\s*[/*]' app/api/analytics/vitals/route.ts | grep -Eq 'console\.(error|warn)'</automated>
    <fails_when>non-zero exit, meaning the route retains a real (non-comment) raw-exception console logging call alongside its telemetry call and the instrumentation AST contract will fail the file</fails_when>
    <automated>! grep -v '^\s*[/*]' app/api/analytics/vitals/route.ts | grep -Eq 'status:\s*[0-9]+'</automated>
    <fails_when>non-zero exit, meaning the route can still answer a beacon with a numeric HTTP status other than the default 200</fails_when>
    <automated>! grep -v '^\s*[/*]' app/api/analytics/vitals/route.ts | grep -Eq 'userAgent|timestamp'</automated>
    <fails_when>non-zero exit, meaning a dropped beacon field is still named in executable route code and could reach the sink</fails_when>
    <automated>test "$(grep -c '  it(' tests/unit/app/api/vitals-route.test.ts)" -ge 14</automated>
    <fails_when>non-zero exit, meaning fewer than 14 cases were written and the behavior list is not fully covered</fails_when>
    <automated>npm test && npm run lint && npm run typecheck</automated>
    <fails_when>non-zero exit from any of the three, or a "failed" count above 0 in the vitest summary, or an "error" line in the eslint or tsc output</fails_when>
  </verify>
  <done>A production beacon lands in the Analytics Engine dataset as one five-field row keyed by a bounded route template, every malformed or unbindable case is dropped silently with a 200, and the browser can never tell the difference.</done>
</task>

</tasks>

## Artifacts this phase produces

Owned by this plan (`02-02`):

| Artifact | Kind | Notes |
|---|---|---|
| `WEB_VITALS` | new Analytics Engine binding | Dataset `mercora_web_vitals`; separate from the unconfigured `COMMERCE_ANALYTICS` |
| `lib/observability/route-template.ts` | new source file | Pure, import-free, bounded-output mapper |
| `ROUTE_TEMPLATES` | new exported constant | `ReadonlySet<string>` of every possible template |
| `OTHER_ROUTE_TEMPLATE` | new exported constant | The single fallback bucket |
| `toRouteTemplate` | new exported function | `(value: unknown) => string`, total over all inputs |
| `app/api/analytics/vitals/route.ts` | rewritten route | Five-field write, always 200 |
| `tests/unit/lib/observability/route-template.test.ts` | new test file | Cardinality and index-cap coverage |
| `tests/unit/app/api/vitals-route.test.ts` | new test file | Write shape, validation drops, missing binding |
| `cloudflare-env.d.ts` | regenerated | Adds only the `WEB_VITALS` declaration |

Consumed from `02-01`: the `analytics.vitals_sink_unavailable` taxonomy entry.
Owned by other plans: see the same section in `02-01-PLAN.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated internet → `POST /api/analytics/vitals` | Anyone can post any JSON body to this route; no auth, no origin check, no rate limit today |
| Beacon payload → Analytics Engine dataset | Everything that survives validation becomes an operator-queryable row with a Cloudflare-side storage cost |
| Beacon payload → Worker log stream | The missing-binding warning path turns a request into a log line |
| `wrangler.jsonc` / `.env.local` → committed `cloudflare-env.d.ts` | A local generation step can fold developer-machine variable names into a tracked file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Per-visitor data reaching the Analytics Engine dataset | high | mitigate | Exactly five values are written; `url`, `userAgent`, `id`, and `timestamp` are removed from `AnalyticsPayload` so the handler has no name for them; a sentinel-string test asserts none of them appears in the `writeDataPoint` argument |
| T-02-07 | Denial of Service | Index-cardinality abuse — a crafted `url` creating unbounded index values and driving Analytics Engine sampling and cost | high | mitigate | The index is `toRouteTemplate(url)`, whose output set is fixed at build time; a `node` gate asserts every member is ASCII and at most 96 bytes, and asserts a traversal path lands in the fallback bucket |
| T-02-08 | Denial of Service | Blob-dimension abuse via an arbitrary metric name or rating | medium | mitigate | Metric name is an allow-list drop; rating falls back to a fixed literal; both sets are module-scope `ReadonlySet` constants |
| T-02-09 | Denial of Service | Beacon flood turning the missing-binding path into the dominant log cost | medium | mitigate | `analytics.vitals_sink_unavailable` is registered at `sampleRate: 0.01` in `02-01`, so 99 of every 100 floods produce no log line while the condition still surfaces |
| T-02-10 | Tampering | Oversized or control-character payload reaching the sink or the log | medium | mitigate | The mapper rejects values over 512 characters before splitting and over 128 after, and rejects control characters, mirroring `queryFreePath`; the value is coerced through `Number.isFinite` and `Math.round` |
| T-02-11 | Denial of Service | A slow or throwing `writeDataPoint` blocking the unload-time beacon response | medium | mitigate | The write is not awaited and is wrapped in its own fail-open `try`/`catch`; a test asserts a throwing `writeDataPoint` still yields 200 |
| T-02-12 | Information Disclosure | Local secret variable names leaking into the committed `cloudflare-env.d.ts` | high | mitigate | The move-aside `cf-typegen` sequence is mandated in the action, the diff is inspected before staging, and a gate fails the task if the regenerated file grew by 10 or more lines |
| T-02-13 | Information Disclosure | Response shape revealing whether a beacon was stored | low | accept | The route answers `{ status: "ok" }` uniformly; an attacker learns nothing, and the operator's signal is the dataset, not the response |
| T-02-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages; `02-RESEARCH.md` "Package Legitimacy Audit" records no dependency added in this phase |
</threat_model>

<verification>
1. `npx vitest run tests/unit/app/api/vitals-route.test.ts tests/unit/lib/observability/route-template.test.ts tests/unit/observability/instrumentation-source.test.ts` — all pass.
2. `npm test` — 0 failed.
3. `npm run lint && npm run typecheck` — both exit 0.
4. `grep -c "WEB_VITALS" wrangler.jsonc cloudflare-env.d.ts app/api/analytics/vitals/route.ts` reports a non-zero count for all three.
5. Post-deploy, operator-only and outside this plan's automated gates: query the `mercora_web_vitals` dataset via the Cloudflare SQL API and confirm rows carry the five fields (recorded in `02-VALIDATION.md` "Manual-Only Verifications").
</verification>

<success_criteria>
- Mobile LCP, INP, and CLS are queryable by route template from real production traffic.
- The vitals route no longer answers `ok` while discarding the payload, and no longer answers 500 at all.
- No per-visitor value reaches the sink.
- The route-template output set is bounded, ASCII, and index-cap safe, with a test that fails if it stops being so.
- The unit suite, lint, and typecheck are green, and the committed type regeneration carries nothing local.
</success_criteria>

<output>
Create `.planning/phases/02-observability-and-regression-guards/02-02-SUMMARY.md` when done.
</output>
