---
phase: 02-observability-and-regression-guards
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/category/[slug]/page.tsx
  - app/product/[slug]/page.tsx
  - tests/unit/app/category-slug-page.test.ts
  - tests/unit/app/product-slug-page.test.ts
  - tests/unit/lib/services/checkout-allocation.test.ts
autonomous: true
requirements: [OBS-03, OBS-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Visiting /category/<unknown> returns HTTP 404 through Next's notFound() boundary instead of a 200 page whose body says the category was not found."
    - "Visiting /product/<unknown> returns HTTP 404, as it already did, and the page still reaches that branch after the params signature change."
    - "Both slug pages declare params as Promise<{ slug: string }> and await it; neither page has an any-typed params parameter."
    - "app/category/[slug]/page.tsx has no any type annotation anywhere: the products array and the caught error in the product-fetch catch are both typed."
    - "An empty, whitespace-only, or missing slug reaches the data layer unchanged and, because the data layer returns no record for it, the page calls notFound(); no branch renders a 200 body for a missing category."
    - "The slug is used exactly as Next decodes it into params: the pages perform no trimming, case folding, or Unicode normalization, so slug equality is the data layer's equality and nothing else."
    - "A category that exists but whose product fetch throws still renders its page with the existing error display; only the category-miss path changed to notFound()."
    - "allocateLargestRemainder returns exactly one part per weight and sum(parts) === total for line counts 1, 2, 10, and 100 across fixed weight tables including odd remainders."
    - "allocateDiscount raises the caller's existing array so that the sum of the increments equals min(amount, availableTotal) exactly, for line counts 1, 2, 10, and 100."
    - "Allocation operates on integer minor units only: every part allocateLargestRemainder returns is a safe integer, a non-integer or negative input throws, a zero weight total with a non-zero total throws, and a zero weight total with a zero total returns all zeros."
    - "allocateLargestRemainder distributes the remainder in descending remainder order and breaks ties by ascending input index, so two runs over identical inputs return identical arrays and a total smaller than the line count still sums exactly."
  artifacts:
    - path: "app/category/[slug]/page.tsx"
      provides: "Promise-typed params, a real 404 on an unknown category, and no any escapes"
      contains: "notFound"
    - path: "app/product/[slug]/page.tsx"
      provides: "Promise-typed params on the already-correct notFound path"
      contains: "Promise<{ slug: string }>"
    - path: "tests/unit/app/category-slug-page.test.ts"
      provides: "Unknown-slug 404 coverage and the empty-slug case for the category route"
      min_lines: 40
    - path: "tests/unit/app/product-slug-page.test.ts"
      provides: "Unknown-slug and inactive-product 404 coverage for the product route"
      min_lines: 40
    - path: "tests/unit/lib/services/checkout-allocation.test.ts"
      provides: "Deterministic sum-exactness tables at 1, 2, 10, and 100 lines plus penny and guard edge cases"
      min_lines: 80
  key_links:
    - from: "app/category/[slug]/page.tsx"
      to: "next/navigation"
      via: "notFound() called when getCategoryBySlug returns no record"
      pattern: "notFound\\(\\)"
    - from: "tests/unit/lib/services/checkout-allocation.test.ts"
      to: "lib/services/checkout-pricing.ts"
      via: "direct import of the two functions plan 02-01 exported"
      pattern: "allocateLargestRemainder"
  prohibitions:
    - statement: "MUST NOT change any behavior on the two slug pages beyond the params signature, the category-miss path, and the removal of the any annotations; the rendered output for an existing category or product must be byte-identical."
    - statement: "MUST NOT add a new test dependency; the allocation tables are fixed fixtures, not generated inputs."
---

<objective>
Close the two regression gaps the codebase map found on the read path: the category page that
answers 200 with a "not found" sentence, and the allocation functions whose penny-exactness rests on
a single existing test.

Purpose: OBS-03 and OBS-04. `app/category/[slug]/page.tsx:52-56` types `params` as `any`, reads
`params.slug` synchronously, and renders a 200 div when the category is missing — so a crawler, a
monitor, and a customer all get a success for a URL that does not exist. `allocateDiscount` and
`allocateLargestRemainder` decide where every rounded penny of tax and discount lands, and one
existing sum test covers them.

Output: two typed pages, two new page test files, and one new allocation table test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-observability-and-regression-guards/02-CONTEXT.md
@.planning/phases/02-observability-and-regression-guards/02-RESEARCH.md
@.planning/phases/02-observability-and-regression-guards/02-PATTERNS.md
@.planning/phases/02-observability-and-regression-guards/02-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

## Notes for the executor

Project commands run under Node 24 via mise. If a plain `npx`/`npm` command fails with a Node
version error, prefix it with `mise exec --`. Do not run `cf-typecheck` locally as a gate.

Decision IDs referenced below are defined in the table in `02-01-PLAN.md`. This plan implements
**D-09**, **D-10**, and **D-12**. **D-11** (the two exports) was completed in plan `02-01` Task 3;
if `allocateDiscount` and `allocateLargestRemainder` are not importable, stop and finish `02-01`
first.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category page — Promise params, a real 404, and no untyped escapes</name>
  <precondition>`app/category/[slug]/page.tsx` still shows the current shape at lines 52-68: an `any`-typed `params`, a synchronous `params.slug` read, and a returned `div` on the category miss. If it has already been migrated, stop and report rather than re-migrating.</precondition>
  <files>app/category/[slug]/page.tsx, tests/unit/app/category-slug-page.test.ts</files>
  <read_first>
    - `app/category/[slug]/page.tsx` (whole file) — the imports at lines 39-44, the signature and category-miss branch at lines 52-57, the `products: any[]` / `error` declarations and the `catch (e: any)` at lines 59-68, the `getCategoryImageUrl` helper from line 76, and the breadcrumb at line 105 which also reads `params.slug` and must be switched to the awaited local.
    - `app/order-status/[id]/page.tsx` — the repository's established Promise-params plus `notFound()` shape in a server component. Copy this structure rather than inventing one.
    - `lib/models/mach/product-serializer.ts` lines 1 and 53 — `toPublicProduct(product: Product): Product`, with `Product` imported from `@/lib/types`. That is the type for the products array.
    - `tests/unit/app/order-status-page.test.ts` lines 1-13 and 77-87 — the exact mocking convention: `vi.mock("next/navigation", () => ({ notFound: () => { throw new Error("NEXT_NOT_FOUND"); } }))`, module mocks for the data layer, then a direct call to the page's default export and a `.rejects.toThrow` assertion. The project deliberately does not assert against Next's real error digest; mirror the convention.
    - `vitest.config.mts` lines 1-14 — `environment: "node"`, `include: ["tests/unit/**/*.test.ts"]`, and the head comment explaining that unit tests import async React Server Components directly.
  </read_first>
  <behavior>
    - An unknown slug: `getCategoryBySlug` resolves to `null`, and calling the page's default export with `{ params: Promise.resolve({ slug: "does-not-exist" }) }` rejects with the mocked `notFound` sentinel.
    - `getCategoryBySlug` resolving to `undefined` produces the same rejection.
    - An empty-string slug and a whitespace-only slug are each passed to `getCategoryBySlug` verbatim — asserted on the mock's call argument — and, with the mock resolving to `null`, each rejects with the sentinel.
    - A percent-encoded slug and a mixed-case slug are each passed through byte-for-byte with no trimming, lowercasing, or normalization.
    - A known slug: `getCategoryBySlug` resolves to a category and `getProductsByCategory` resolves to a list, the page resolves without throwing, and `notFound` is never called.
    - A known slug whose `getProductsByCategory` rejects: the page still resolves without throwing, proving only the category-miss path became a 404.
    - Only active products are rendered: a list mixing an active and a non-active product yields one call to `toPublicProduct`-shaped output for the active one, matching the existing filter.
  </behavior>
  <action>
    Rewrite the signature and the miss path of `app/category/[slug]/page.tsx` per D-09. Four edits,
    nothing else in the file changes:

    1. Add `import { notFound } from "next/navigation";` to the import block, and import `Product`
       as a type from `@/lib/types`.
    2. Change the signature to `export default async function CategoryPage({ params }: { params:
       Promise<{ slug: string }> })` and destructure `const { slug } = await params;` as the first
       statement. Replace both later reads of `params.slug` — the data-layer call at line 53 and the
       breadcrumb href at line 105 — with the awaited `slug` local. Do not trim, lowercase, or
       normalize it; the data layer owns slug equality.
    3. Replace the returned `div` on the category miss with a bare `notFound()` call, matching
       `app/product/[slug]/page.tsx` and `app/order-status/[id]/page.tsx`. `notFound()` is typed
       `never`, so no `return` keyword is needed and the code below it narrows correctly.
    4. Type the two escapes: declare the products array as `Product[]` instead of `any[]`, and
       change `catch (e: any)` to a bare `catch (e)` with the message extracted through a
       `e instanceof Error ? e.message : 'Unknown error'` narrowing. Keep the existing `error` state
       variable and its rendering exactly as they are — a category that exists but whose product
       fetch fails must keep showing the current error display, not a 404.

    Leave `getCategoryImageUrl`, the hero markup, the breadcrumb structure, and `CategoryDisplay`
    untouched. The rendered output for a category that exists must be byte-identical to today's.

    Then write `tests/unit/app/category-slug-page.test.ts` covering every behavior above, as a
    module-import unit test per D-10 — mock the data layer, call the page's default export directly,
    and assert on the `notFound()` sentinel rather than standing up a request. Mock
    `next/navigation`, `@/lib/models` (for `getCategoryBySlug`), and
    `@/lib/models/mach/products` (for `getProductsByCategory`), plus `@/lib/models/mach/product-serializer`
    and any component module the page imports that would drag in a browser-only dependency under the
    node environment — check the page's full import list and mock exactly what the import graph
    requires, no more. Follow `tests/unit/app/order-status-page.test.ts` for the mock placement and
    the `.rejects.toThrow` idiom.
  </action>
  <acceptance_criteria>
    - Every behavior bullet above has a passing test, and the test file contains at least 8 `it(` blocks.
    - `app/category/[slug]/page.tsx` contains `Promise<{ slug: string }>` and exactly one `notFound()` call.
    - The file contains no `: any` and no `any[]` annotation.
    - The file contains no occurrence of `params.slug`.
    - `npx vitest run tests/unit/app/category-slug-page.test.ts` reports 0 failed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/app/category-slug-page.test.ts</automated>
    <fails_when>non-zero exit, or the word "failed" appears in the Test Files or Tests summary lines</fails_when>
    <automated>grep -q 'Promise<{ slug: string }>' "app/category/[slug]/page.tsx" && test "$(grep -c 'notFound()' "app/category/[slug]/page.tsx")" -eq 1</automated>
    <fails_when>non-zero exit, meaning params was not migrated to the awaited Promise form or the category miss does not resolve to exactly one 404 call</fails_when>
    <automated>! grep -v '^\s*[/*]' "app/category/[slug]/page.tsx" | grep -Eq ':\s*any|any\[\]'</automated>
    <fails_when>non-zero exit, meaning an untyped escape survives in executable code on the category page</fails_when>
    <automated>! grep -v '^\s*[/*]' "app/category/[slug]/page.tsx" | grep -q 'params\.slug'</automated>
    <fails_when>non-zero exit, meaning a synchronous read of the params object remains and will break under Next 16's Promise-typed params at runtime</fails_when>
    <automated>test "$(grep -c '  it(' tests/unit/app/category-slug-page.test.ts)" -ge 8</automated>
    <fails_when>non-zero exit, meaning fewer than 8 cases were written and the behavior list is not fully covered</fails_when>
  </verify>
  <done>An unknown category slug produces a real 404 through Next's boundary, an existing category renders exactly as before including its product-fetch error display, and the page has no untyped escape left.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Product page — Promise params with the 404 path pinned</name>
  <files>app/product/[slug]/page.tsx, tests/unit/app/product-slug-page.test.ts</files>
  <read_first>
    - `app/product/[slug]/page.tsx` lines 43-64 — the full import list (Clerk `auth`, `getProductBySlug`/`getProductReviews`/`getProductReviewEligibility` from `@/lib/models`, `notFound`, `ProductDisplay`, `toPublicProduct`, `getRecommendationsForProduct`, `buildServerUserContext`, `getStoreConfig`), the `revalidate` export, the `{ params }: any` signature at line 60, and the existing `notFound()` branch at line 63 which already covers both the missing-product and non-active-status cases.
    - `app/product/[slug]/page.tsx` lines 65-120 — everything downstream of the 404 branch, so the test knows which collaborators must be mocked for the happy path to resolve.
    - `tests/unit/app/order-status-page.test.ts` lines 1-13 — the mocking convention, as in Task 1.
    - `app/category/[slug]/page.tsx` as it stands after Task 1 — the sibling signature this page must now match.
  </read_first>
  <behavior>
    - An unknown slug: `getProductBySlug` resolves to `null` and the page rejects with the mocked `notFound` sentinel.
    - A product that exists but whose `status` is not active rejects with the same sentinel, pinning the second half of the existing guard.
    - The slug reaches `getProductBySlug` exactly as supplied, including an empty string, a whitespace-only string, and a mixed-case value — asserted on the mock's call argument.
    - An active product resolves without throwing and `notFound` is never called.
    - The page is invoked with a real Promise for `params` and awaits it, proving the new signature is exercised rather than merely declared: passing a plain object where the Promise is expected must not be how the tests call it.
  </behavior>
  <action>
    Change one thing in `app/product/[slug]/page.tsx` per D-09: replace `{ params }: any` at line 60
    with `{ params }: { params: Promise<{ slug: string }> }` and destructure `const { slug } = await
    params;` before the `getProductBySlug` call, then pass `slug` to it. The existing `notFound()`
    branch at line 63 already implements the required behavior and must not be touched. Change
    nothing else in the file — no import changes, no `revalidate` change, no restructuring of the
    `Promise.all`.

    Then write `tests/unit/app/product-slug-page.test.ts` covering every behavior above. This page
    has a wider import graph than the category page, so mock each of its collaborators: `@clerk/nextjs/server`
    for `auth`, `@/lib/models` for the three model functions, `@/lib/models/mach/product-serializer`,
    `@/lib/recommendations`, `@/lib/recommendations/user-context.server`, `@/lib/store-config`, and
    `next/navigation`. Read the page's import list first and mock exactly what the graph requires
    under the node test environment. Give each mock a minimal resolved value sufficient for the
    happy-path case to complete; the 404 cases short-circuit before most of them are reached.

    Always construct `params` as `Promise.resolve({ slug })` in the tests, never as a plain object.
    A plain object would still work today under a loose runtime and would silently stop proving the
    await once someone reverts the signature.
  </action>
  <acceptance_criteria>
    - Every behavior bullet above has a passing test, and the test file contains at least 6 `it(` blocks.
    - `app/product/[slug]/page.tsx` contains `Promise<{ slug: string }>`, contains no `: any`, and contains no occurrence of `params.slug`.
    - The test file constructs every `params` argument with `Promise.resolve(`.
    - `git diff --numstat -- 'app/product/[slug]/page.tsx'` shows at most 5 added and 3 removed lines.
    - `npx vitest run tests/unit/app/product-slug-page.test.ts` reports 0 failed.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/app/product-slug-page.test.ts tests/unit/app/category-slug-page.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than "2 passed" for Test Files, or the word "failed" appears in the summary</fails_when>
    <automated>grep -q 'Promise<{ slug: string }>' "app/product/[slug]/page.tsx"</automated>
    <fails_when>non-zero exit, meaning the product page still declares params in the pre-Next-16 form</fails_when>
    <automated>! grep -v '^\s*[/*]' "app/product/[slug]/page.tsx" | grep -Eq ':\s*any|params\.slug'</automated>
    <fails_when>non-zero exit, meaning an untyped params annotation or a synchronous params read survives in executable code on the product page</fails_when>
    <automated>! grep -q 'params: {' tests/unit/app/product-slug-page.test.ts</automated>
    <fails_when>non-zero exit, meaning at least one test passes params as a plain object literal instead of a Promise, so it would keep passing after a revert of the signature</fails_when>
    <automated>test "$(grep -c '  it(' tests/unit/app/product-slug-page.test.ts)" -ge 6</automated>
    <fails_when>non-zero exit, meaning fewer than 6 cases were written and the behavior list is not fully covered</fails_when>
  </verify>
  <done>Both slug pages share one Promise-typed params signature, and the product page's two 404 conditions are pinned by tests that would fail if either branch loosened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic allocation sum-exactness tables</name>
  <precondition>`allocateDiscount` and `allocateLargestRemainder` are exported from `lib/services/checkout-pricing.ts` (plan `02-01` Task 3 complete); `grep -q 'export function allocateLargestRemainder' lib/services/checkout-pricing.ts` exits 0.</precondition>
  <files>tests/unit/lib/services/checkout-allocation.test.ts</files>
  <read_first>
    - `lib/services/checkout-pricing.ts` lines 451-479 — `allocateLargestRemainder(total: number, weights: number[]): number[]`. Note four things the tables must respect: the guard that throws `Tax fallback allocation requires nonnegative integer minor units` for a non-safe-integer or negative `total` or weight; the zero-weight-total branch that throws `Cannot allocate tax across zero-value lines` when `total` is non-zero and returns all zeros when it is zero; the `BigInt` floor division producing each base share; and the tie-break `a.remainder === b.remainder ? a.index - b.index : (a.remainder > b.remainder ? -1 : 1)`, which is descending remainder, ascending index.
    - `lib/services/checkout-pricing.ts` lines 252-278 — `allocateDiscount(amount: Money, eligible: number[], lineTotals: Money[], existing: Money[]): void`. This one returns nothing and mutates `existing` in place. `eligible` is an array of indices into `lineTotals` and `existing`. `applied` is `min(amount, availableTotal)` where `availableTotal` is the sum of `lineTotals[i] - existing[i]` over the eligible indices; the function returns early with no mutation when `applied` or `availableTotal` is zero; and the last eligible position absorbs the whole remainder, which is what makes the sum exact.
    - `lib/money.ts` — the `Money` constructors and accessors the tables need: `Money.fromMinor`, `Money.zero`, `.toMinorUnits()`, `.add`, `.subtract`.
    - `tests/unit/lib/services/checkout-pricing.test.ts` — the sibling file this one lives beside. Do not duplicate its existing largest-remainder sum test near line 525; this file is the breadth OBS-04 asks for.
    - `.planning/codebase/TESTING.md` "Fixtures and Factories" — the repository builds test data with small inline factory functions and `Array.from({ length: n }, ...)`; use that for the 10- and 100-line tables rather than pasting a hundred literals.
  </read_first>
  <behavior>
    - `allocateLargestRemainder` sum exactness at 1, 2, 10, and 100 lines: for each line count, at least three fixed weight tables (uniform weights, ascending weights, and one deliberately awkward set that forces a non-zero remainder) with at least two totals each, asserting `parts.length === weights.length` and `parts.reduce((a,b)=>a+b,0) === total`.
    - Penny edge cases: a total of 1 across 2 lines, a total of 1 across 100 lines, a total of 99 across 100 equal weights, and a total smaller than the line count — each sums exactly and every part is a non-negative safe integer.
    - Zero-weight line: a weight table containing a zero alongside non-zero weights allocates 0 to that line and still sums exactly.
    - Single line: one weight receives the entire total.
    - Total of zero with non-zero weights returns all zeros.
    - Guards: a non-integer total, a negative total, a non-integer weight, and a negative weight each throw; a zero weight total with a non-zero total throws; a zero weight total with a zero total returns an array of zeros of the right length.
    - Determinism and tie-break: a weight table whose remainders tie assigns the extra units to the lowest input indices, and running the same inputs twice returns arrays that are deeply equal.
    - `allocateDiscount` sum exactness at 1, 2, 10, and 100 lines: for each count, build `lineTotals` and a zeroed `existing`, call the function, and assert the sum of `existing` in minor units equals the applied amount.
    - `allocateDiscount` clamping: an amount larger than the available total applies only the available total; an amount of zero mutates nothing; an `existing` array already carrying prior discounts reduces the available total and the applied sum accordingly.
    - `allocateDiscount` partial eligibility: with only a subset of indices in `eligible`, the ineligible entries of `existing` are untouched and the eligible ones absorb the whole applied amount.
    - `allocateDiscount` does not mutate its inputs other than `existing`: the `lineTotals` array and the `eligible` array are unchanged after the call.
  </behavior>
  <action>
    Create `tests/unit/lib/services/checkout-allocation.test.ts` implementing D-12: deterministic
    fixed tables, no new dependency, no property-based generation.

    Import `allocateDiscount` and `allocateLargestRemainder` directly from
    `@/lib/services/checkout-pricing` (exported by plan `02-01`) and `Money` from `@/lib/money`.

    Structure the file as one flat `describe` per function with sentence-style `it` names, per
    `.planning/codebase/TESTING.md`. Drive the line-count sweep with a table-plus-loop rather than
    four hand-written copies: define a `const CASES` array of `{ lines, weights, totals }` records
    covering 1, 2, 10, and 100, build the 10- and 100-line weight arrays with
    `Array.from({ length: n }, (_, i) => …)` so the fixtures stay readable, and iterate with
    `it.each` — that keeps every case named in the output while the fixtures stay fixed and
    inspectable.

    Two mechanics that matter for correctness of the tests themselves. First, `allocateDiscount`
    returns `void` and mutates: build a fresh zeroed `existing` array inside each case, call the
    function for its effect, then sum `existing`. A test that expects a return value will silently
    assert `undefined`. Second, for the clamping cases compute the expected applied amount as
    `Math.min(amount, availableTotal)` in the test from the same fixtures, rather than hard-coding a
    number — that keeps the assertion honest if a fixture is edited later.

    For the tie-break case, choose weights whose `total * weight % weightTotal` values are equal for
    at least two lines and assert the exact resulting array, not just its sum. The sum would pass
    under any tie-break; only the exact array pins the ascending-index rule the implementation
    actually uses.
  </action>
  <acceptance_criteria>
    - `tests/unit/lib/services/checkout-allocation.test.ts` exists and imports both functions from `@/lib/services/checkout-pricing`.
    - Every behavior bullet above has a passing assertion; the file exercises line counts 1, 2, 10, and 100 for both functions.
    - The file adds no import outside `vitest`, `@/lib/services/checkout-pricing`, and `@/lib/money`.
    - The file contains at least 16 test cases counting `it(` and `it.each(` blocks.
    - `npm test`, `npm run lint`, and `npm run typecheck` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/lib/services/checkout-allocation.test.ts tests/unit/lib/services/checkout-pricing.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any value other than "2 passed" for Test Files, or the word "failed" appears in the summary</fails_when>
    <automated>grep -q "from '@/lib/services/checkout-pricing'" tests/unit/lib/services/checkout-allocation.test.ts && grep -q 'allocateDiscount' tests/unit/lib/services/checkout-allocation.test.ts && grep -q 'allocateLargestRemainder' tests/unit/lib/services/checkout-allocation.test.ts</automated>
    <fails_when>non-zero exit, meaning the table file does not import and exercise both allocation functions directly</fails_when>
    <automated>for n in 1 2 10 100; do grep -qE "lines: *$n\b" tests/unit/lib/services/checkout-allocation.test.ts || exit 1; done</automated>
    <fails_when>non-zero exit, meaning one of the four line counts OBS-04 names is absent from the CASES fixture table</fails_when>
    <automated>test "$(grep -cE '  it(\.each)?\(' tests/unit/lib/services/checkout-allocation.test.ts)" -ge 16</automated>
    <fails_when>non-zero exit, meaning fewer than 16 cases were written and the behavior list is not fully covered</fails_when>
    <automated>! grep -q 'fast-check' tests/unit/lib/services/checkout-allocation.test.ts && test "$(grep -cE "^import " tests/unit/lib/services/checkout-allocation.test.ts)" -le 3</automated>
    <fails_when>non-zero exit, meaning a property-based testing dependency was introduced or the file imports more than the three permitted modules, contradicting the recorded decision to use deterministic tables with no new dependency</fails_when>
    <automated>npm test && npm run lint && npm run typecheck</automated>
    <fails_when>non-zero exit from any of the three, or a "failed" count above 0 in the vitest summary, or an "error" line in the eslint or tsc output</fails_when>
  </verify>
  <done>Discount and tax allocation are pinned to exact sums across 1, 2, 10, and 100 lines with penny remainders, zero weights, clamping, and a deterministic tie-break, so a rounding regression fails a test instead of shipping a cent short.</done>
</task>

</tasks>

## Artifacts this phase produces

Owned by this plan (`02-04`):

| Artifact | Kind | Notes |
|---|---|---|
| `app/category/[slug]/page.tsx` | modified page | Promise params, `notFound()` on miss, no `any` |
| `app/product/[slug]/page.tsx` | modified page | Promise params only; the 404 branch already existed |
| `tests/unit/app/category-slug-page.test.ts` | new test file | Unknown, empty, and encoded slug coverage |
| `tests/unit/app/product-slug-page.test.ts` | new test file | Unknown slug and non-active status coverage |
| `tests/unit/lib/services/checkout-allocation.test.ts` | new test file | 1/2/10/100-line sum-exactness tables for both functions |

Consumed from `02-01`: the `allocateDiscount` and `allocateLargestRemainder` exports.
Owned by other plans: see the same section in `02-01-PLAN.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated internet → `/category/[slug]` and `/product/[slug]` | The slug is fully attacker-controlled and reaches the data layer and, today, the rendered page body |
| Allocation functions → customer-visible money | These pure functions decide the per-line tax and discount amounts a customer is charged and a refund is computed against |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Information Disclosure | The category page reflecting the raw slug into the rendered 200 body | medium | mitigate | The reflecting branch is deleted outright: the miss path becomes `notFound()`, so no attacker-supplied slug reaches a rendered page body; a gate asserts no `params.slug` read survives |
| T-02-20 | Spoofing | An unknown URL answering 200, letting a crafted link look like a live storefront page to a crawler or a link preview | medium | mitigate | Both pages now answer 404 through Next's boundary; the category case is pinned by a test that rejects with the `notFound` sentinel |
| T-02-21 | Tampering | A rounding regression silently shifting a penny of tax or discount onto or off a line | high | mitigate | Sum-exactness tables at 1, 2, 10, and 100 lines with penny remainders and an exact-array tie-break assertion; the guard cases pin the throw behavior that keeps non-integer minor units out |
| T-02-22 | Denial of Service | A very long or deeply nested slug reaching the data layer | low | accept | The slug is passed through unchanged by design so the data layer owns equality; the route is already behind Cloudflare's request limits and the data layer's own parameter binding, and adding a second length policy here would diverge from the other slug routes |
| T-02-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages, and a gate fails the allocation task if a property-based testing dependency is introduced; `02-RESEARCH.md` "Package Legitimacy Audit" records no dependency added in this phase |
</threat_model>

<verification>
1. `npx vitest run tests/unit/app/category-slug-page.test.ts tests/unit/app/product-slug-page.test.ts tests/unit/lib/services/checkout-allocation.test.ts tests/unit/lib/services/checkout-pricing.test.ts` — all pass.
2. `npm test` — 0 failed.
3. `npm run lint && npm run typecheck` — both exit 0.
4. `grep -c 'Promise<{ slug: string }>' 'app/category/[slug]/page.tsx' 'app/product/[slug]/page.tsx'` reports a non-zero count for both.
</verification>

<success_criteria>
- `/category/<unknown>` and `/product/<unknown>` both return HTTP 404.
- Both slug pages await Promise-typed `params` and the category page carries no untyped escape.
- Tests cover the unknown-slug path for both routes.
- Discount and tax allocation fail a test if they ever stop summing to the total across 1, 2, 10, and 100 lines.
- The unit suite, lint, and typecheck are green.
</success_criteria>

<output>
Create `.planning/phases/02-observability-and-regression-guards/02-04-SUMMARY.md` when done.
</output>
