---
phase: 04-reference-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api-architecture.md
  - docs/CLAUDE.md
  - docs/STRIPE_INTEGRATION.md
autonomous: true
requirements: [REF-01, REF-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docs/CLAUDE.md and docs/api-architecture.md name the text model as @cf/openai/gpt-oss-20b, matching lib/ai/config.ts:29 (D-01, D-04)"
    - "docs/CLAUDE.md states the MCP tool count as 19 in both places it appears, and its enumerated tool list contains all 19 names from app/api/mcp/route.ts:193 (D-03)"
    - "docs/CLAUDE.md describes the three real vitest suites with their config files and npm script names, and the CI gates in the order .github/workflows/ci.yml runs them; the claim that no testing framework is configured is gone (D-05)"
    - "docs/CLAUDE.md's Key Dependencies block lists dependency names with no pinned version strings and points the reader at package.json for versions (D-06)"
    - "No document under docs/ references the nonexistent API architecture file; both former reference sites point at docs/api-architecture.md, which exists (D-07)"
    - "docs/api-architecture.md's PaymentProcessing note states that Stripe PaymentIntent creation and server-side retrieval are real, citing docs/checkout-trust-boundary.md; the note no longer describes the integration as unimplemented (D-08)"
    - "The Testing section names CI steps by their step name (for example 'Audit production dependencies'), not by any --audit-level value, so plan 04-05's change to that value in the same phase cannot make this section wrong regardless of which plan commits first (edge: concurrency)"
    - "The dated 'Recent Fixes & Issues Resolved (Aug 23, 2025)' and 'Current Git Status' sections of docs/CLAUDE.md are unchanged by this plan — 04-CONTEXT.md <deferred> places them outside REF-02"
  prohibitions:
    - statement: "MUST NOT delete or truncate documentation content solely to make a negative grep gate pass — a gate is satisfied by correcting the claim, never by removing the sentence that carries it"
      status: unverified
      verification: judgment
    - statement: "MUST NOT write a factual claim into docs/ that was not read from its named source file in this session — no npm script name, config filename, or CI step name paraphrased from memory"
      status: unverified
      verification: judgment
  artifacts:
    - path: "docs/api-architecture.md"
      provides: "API flow diagrams with the current model name and a truthful payment-processing note"
      contains: "checkout-trust-boundary.md"
    - path: "docs/CLAUDE.md"
      provides: "Contributor reference with the current model, correct tool count and list, real test/CI description, and unpinned dependency names"
      contains: "vitest.observability.config.mts"
    - path: "docs/STRIPE_INTEGRATION.md"
      provides: "Documentation tree entry pointing at a file that exists"
      contains: "api-architecture.md"
  key_links:
    - from: "docs/CLAUDE.md"
      to: "package.json"
      via: "Testing section quotes the three vitest script names and defers all dependency versions here"
      pattern: "test:observability-worker"
    - from: "docs/CLAUDE.md"
      to: ".github/workflows/ci.yml"
      via: "CI gate list copies the step names in file order"
      pattern: "Check Cloudflare binding types"
    - from: "docs/api-architecture.md"
      to: "docs/checkout-trust-boundary.md"
      via: "payment-processing note cites the accepted ADR for the real Stripe path"
      pattern: "checkout-trust-boundary"
---

<!-- planner-discipline-allow: mock implementation -->
<!-- planner-discipline-allow: API_STRUCTURE -->
<!-- planner-discipline-allow: No formal testing -->

<objective>
Bring `docs/CLAUDE.md` — the file a human or AI contributor reads first — in line with the code, and fix the two stale cross-references and the one false claim in its neighbours `docs/api-architecture.md` and `docs/STRIPE_INTEGRATION.md`.

Purpose: this file currently tells a contributor that no testing framework exists (three vitest suites and nine CI gates do), pins eight dependency versions that are all wrong, points at a documentation file that does not exist, names a model the platform no longer runs, and understates the MCP tool count in two places. `docs/api-architecture.md` separately describes the Stripe integration as unimplemented, which contradicts the accepted trust-boundary ADR.
Output: three corrected documents in three atomic commits.

## Decision map (D-NN → CONTEXT.md bullet)

From `04-CONTEXT.md` `<decisions>`:

- **D-01** (Model sweep) — exact id `@cf/openai/gpt-oss-20b`; `lib/ai/config.ts:29` is the source of truth.
- **D-03** (Tool count) — reads 19; `docs/CLAUDE.md` cites `app/api/mcp/route.ts` once as where the tool list lives; the executor re-counts from that file before writing.
- **D-04** (Scope) — every doc the sweep finds; only the model name changes in those sentences.
- **D-05** (Testing section) — `docs/CLAUDE.md` ~412-415 replaced by about ten lines naming the three vitest suites with config files and npm scripts, then the CI gates in `ci.yml` order; names copied verbatim from `package.json` and `.github/workflows/ci.yml`, nothing paraphrased.
- **D-06** (Key Dependencies) — drops every pinned version; names stay, followed by one line saying versions live in `package.json`.
- **D-07** (Stale doc references) — both references to the nonexistent API architecture file repointed to `docs/api-architecture.md`.
- **D-08** (Payment note) — `docs/api-architecture.md:435` states that Stripe PaymentIntent verification is real, citing `docs/checkout-trust-boundary.md`; the dated `docs/CLAUDE.md` sections stay untouched.

**Deferred, do not touch** (`04-CONTEXT.md` `<deferred>`): the "Recent Fixes & Issues Resolved (Aug 23, 2025)" and "Current Git Status" sections of `docs/CLAUDE.md`, and the feature claims that surround the model name.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reference-documentation-refresh/04-CONTEXT.md
@.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md
@.planning/phases/04-reference-documentation-refresh/04-PATTERNS.md
</context>

## Shell rules for every command in this plan

Confirmed live in `04-RESEARCH.md` "Common Pitfalls" #2: a shell hook intercepts bare `grep` and `find` here and returns wrong counts.

- Absolute paths in every gate: `/usr/bin/grep`, `/usr/bin/sed`, `/usr/bin/wc`, `/bin/ls`.
- Never `diff <(...) <(...)` — a shell function shadows `diff`.
- `/usr/bin/grep -c` prints `0` and exits 1 on a clean file; `test "$(...)" = "0"` is the correct idiom. No `|| echo 0` fallbacks.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end correction of one file carrying both requirements — api-architecture model name and payment note</name>
  <reversibility rating="reversible">Two prose edits inside one markdown file; `git revert` restores both.</reversibility>
  <files>docs/api-architecture.md</files>
  <read_first>
    - `docs/api-architecture.md` lines 258-268 (the AI sequence-diagram block; the model message is line 263)
    - `docs/api-architecture.md` lines 425-440 (the `note right of PaymentProcessing` block, lines 433-436)
    - `docs/checkout-trust-boundary.md` lines 1-30 — the accepted status marker and the invariants describing real PaymentIntent creation and server-side retrieval before finalization
    - `lib/ai/config.ts` lines 25-33 — the model id
  </read_first>
  <action>
This is the tracer for this plan: one file, carrying both a REF-01 edit and a REF-02 edit, through read-source → edit → gate → commit, before the two larger `docs/CLAUDE.md` tasks expand on it. It is production-quality, not a probe.

**Model name (D-01, D-04).** Line 263's sequence-diagram message ends with a parenthesised model family name. Replace the content of that parenthetical with `@cf/openai/gpt-oss-20b`. The `API->>AI: Generate response` prefix and the surrounding `alt`/`else` block stay byte-identical.

**Payment note (D-08).** Read `docs/checkout-trust-boundary.md:1-30` first, then rewrite the two-line body of the `note right of PaymentProcessing` block (lines 434-435) to state what that ADR actually establishes: Mercora creates the Stripe PaymentIntent and the order finalizer retrieves it server-side before finalizing, and `docs/checkout-trust-boundary.md` is the binding source. Keep it to two or three short lines — this is a mermaid note body, not prose; long lines break the diagram layout. Keep the `note right of PaymentProcessing` opener and the `end note` terminator exactly as they are.

The replacement text must not restate, negate, or reference the parenthetical it replaces in any form. "No longer a …" or "previously described as …" would reintroduce the exact string the gate below requires to be absent. State only what is true.

Commit: `docs(04-02): name the current model and describe the real Stripe path in the API architecture`.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'gpt-oss-20b' docs/api-architecture.md` prints `1`.
    - The case-insensitive superseded-model-name count in `docs/api-architecture.md` prints `0`.
    - `/usr/bin/grep -c 'mock implementation' docs/api-architecture.md` prints `0`.
    - `/usr/bin/grep -c 'checkout-trust-boundary' docs/api-architecture.md` prints `1` — the note cites the ADR by filename.
    - `/usr/bin/grep -c 'note right of PaymentProcessing' docs/api-architecture.md` still prints `1` and `end note` still closes it.
    - `git log -1 --name-only` shows exactly one file changed.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c 'gpt-oss-20b' docs/api-architecture.md)" = "1" && test "$(/usr/bin/grep -ic 'llama' docs/api-architecture.md)" = "0"</automated>
    <fails_when>non-zero exit from test — the model-id line count is not 1, or the case-insensitive superseded-name count is above 0</fails_when>
    <automated>test "$(/usr/bin/grep -c 'mock implementation' docs/api-architecture.md)" = "0" && test "$(/usr/bin/grep -c 'checkout-trust-boundary' docs/api-architecture.md)" = "1"</automated>
    <fails_when>non-zero exit from test — the removed parenthetical is still present anywhere in the file (including inside the replacement text), or the ADR citation is missing or duplicated</fails_when>
    <automated>test "$(/usr/bin/grep -c 'note right of PaymentProcessing' docs/api-architecture.md)" = "1"</automated>
    <fails_when>prints a number other than 1 — the mermaid note opener was deleted or duplicated, which would break the diagram</fails_when>
    <automated>test "$(git diff --name-only HEAD~1 HEAD | /usr/bin/grep -c .)" = "1"</automated>
    <fails_when>prints a number other than 1 — the tracer commit swept in files it does not own</fails_when>
  </verify>
  <done>`docs/api-architecture.md` names the current model id and its payment note describes the real Stripe path with an ADR citation; committed as one atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2: docs/CLAUDE.md model mentions, tool count, and the two missing tool names</name>
  <reversibility rating="reversible">Prose and list edits inside one markdown file.</reversibility>
  <files>docs/CLAUDE.md</files>
  <read_first>
    - `docs/CLAUDE.md` lines 30-35 (Backend & Infrastructure stack list; the AI bullet is line 33)
    - `docs/CLAUDE.md` lines 155-175 (admin analytics bullets on lines 158 and 172)
    - `docs/CLAUDE.md` lines 353-362 (the AI System pipeline arrow diagram on line 357, and the already-correct `**Language Model**` bullet just below it)
    - `docs/CLAUDE.md` lines 255-300 (the `MCP Tools` heading on line 258 and the four grouped tool lists below it)
    - `docs/CLAUDE.md` lines 540-550 (the MCP implementation feature list; the tool-count line is 545)
    - `app/api/mcp/route.ts` lines 100-195 — the 19 `case` arms and the `Available tools:` enumeration on line 193
    - `lib/ai/config.ts` lines 25-33 — the model id
    - `.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md` `## REF-01 Verified Facts` — the four stale model lines in this file with line numbers, and the full 19-name list
  </read_first>
  <action>
**Model name, four sites (D-01, D-04).** Lines 33, 158, 172, and 357 each carry the superseded model family name. Replace only that name with `@cf/openai/gpt-oss-20b`:

- Line 33 is a dense stack bullet — a backticked bare id reads best.
- Lines 158 and 172 are prose bullets — use the readable form `gpt-oss-20b (@cf/openai/gpt-oss-20b)` with the id in backticks, per D-01.
- Line 357 sits inside a fenced arrow diagram (`User Query → … → Response + Products`); replace the arrow node text with the bare id, no backticks, so the fence stays plain text.

Per D-04, leave the surrounding claims alone — "Real-time business intelligence", "Natural language insights", and the neighbouring stack bullets are unchanged. The `**Language Model**: @cf/openai/gpt-oss-20b (centrally configured in /lib/ai/config.ts)` bullet already below the arrow diagram is already correct; do not touch it, and count it when checking the post-edit total (1 existing + 4 new = 5).

**Tool count, two sites (D-03).** Re-count first: `/usr/bin/grep -c "case '" app/api/mcp/route.ts` must print 19. Plan 04-01 records the same measurement in its SUMMARY; if the numbers disagree, stop and report.

- **Line 258** is the heading over the enumerated tool list and states a total in parentheses. Set that total to 19.
- **Line 545** is the feature bullet stating a tool count. Set it to 19.

**The enumerated list under line 258 is short by two names.** Its four groups (Commerce Tools, Cart Management, Order Processing, Agent Administration) list 3 + 6 + 4 + 4 = 17 of the 19 names. `create_payment_intent` and `rotate_agent_key` are missing. Setting the heading to 19 over a list of 17 would replace one wrong number with an internal contradiction, so add both, matching the existing bullet shape (`` - `tool_name` - one-line description ``):

- `create_payment_intent` → under **Order Processing**, placed before `place_order` so the group reads in call order.
- `rotate_agent_key` → under **Agent Administration**, placed last after `update_agent_status`.

Write each description from what the corresponding handler in `app/api/mcp/route.ts` dispatches to; do not invent behaviour. Per D-03, add a single citation of `app/api/mcp/route.ts` as the place the authoritative tool list lives — one sentence directly under the line 258 heading is the right place for it, and one citation is enough for the whole file.

Do not restate the previous count anywhere in the file; the regex gate below rejects any two-digit tool count between 10 and 18.

Commit: `docs(04-02): name the current model and correct the MCP tool count and list`.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c "case '" app/api/mcp/route.ts` prints `19`, re-counted during this task.
    - `/usr/bin/grep -c 'gpt-oss-20b' docs/CLAUDE.md` prints `5`.
    - The case-insensitive superseded-model-name count in `docs/CLAUDE.md` prints `0`.
    - `/usr/bin/grep -c '(19 total)' docs/CLAUDE.md` prints `1` and `/usr/bin/grep -c '19 MCP tools' docs/CLAUDE.md` prints `1`.
    - A regex search for `\(1[0-8] total\)|1[0-8] MCP tools` in `docs/CLAUDE.md` prints `0`.
    - All 19 tool names from `app/api/mcp/route.ts:193` appear in `docs/CLAUDE.md`; the four category groups and their internal order are otherwise unchanged.
    - `app/api/mcp/route.ts` is named at least once in `docs/CLAUDE.md`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c "case '" app/api/mcp/route.ts)" = "19"</automated>
    <fails_when>prints a number other than 19 — the dispatch switch changed since RESEARCH.md counted it, so 19 is no longer the right figure; halt and report the new count</fails_when>
    <automated>test "$(/usr/bin/grep -c 'gpt-oss-20b' docs/CLAUDE.md)" = "5" && test "$(/usr/bin/grep -ic 'llama' docs/CLAUDE.md)" = "0"</automated>
    <fails_when>non-zero exit from test — a model-id line count other than 5 means a site was missed or the already-correct Language Model bullet was disturbed; a non-zero case-insensitive superseded-name count means an occurrence survived</fails_when>
    <automated>test "$(/usr/bin/grep -c '(19 total)' docs/CLAUDE.md)" = "1" && test "$(/usr/bin/grep -c '19 MCP tools' docs/CLAUDE.md)" = "1" && test "$(/usr/bin/grep -cE '\(1[0-8] total\)|1[0-8] MCP tools' docs/CLAUDE.md)" = "0"</automated>
    <fails_when>non-zero exit from test — a corrected count is absent or duplicated, or a stale two-digit count below 19 survives at either site</fails_when>
    <automated>for t in search_products assess_request get_recommendations add_to_cart update_cart remove_from_cart get_cart bulk_add_to_cart clear_cart create_payment_intent place_order get_order_status get_shipping_options validate_payment create_agent list_agents get_agent_details update_agent_status rotate_agent_key; do /usr/bin/grep -qF "$t" docs/CLAUDE.md || { echo "MISSING TOOL $t"; exit 1; }; done; echo ALL19_PRESENT</automated>
    <fails_when>output contains a "MISSING TOOL name" line instead of ALL19_PRESENT, or the loop exits 1</fails_when>
    <automated>test "$(/usr/bin/grep -c 'app/api/mcp/route.ts' docs/CLAUDE.md)" = "1"</automated>
    <fails_when>prints 0 (the authoritative-source citation D-03 requires was not added) or a number above 1 (the citation was repeated, which D-03 caps at one)</fails_when>
  </verify>
  <done>`docs/CLAUDE.md` names the current model at all five sites, states 19 tools in both places, and enumerates all 19 tool names with one citation of the dispatch file; committed.</done>
</task>

<task type="auto">
  <name>Task 3: docs/CLAUDE.md Testing section, Key Dependencies, and both stale doc references</name>
  <reversibility rating="reversible">Section replacements inside two markdown files; no code or config changes.</reversibility>
  <files>docs/CLAUDE.md, docs/STRIPE_INTEGRATION.md</files>
  <read_first>
    - `docs/CLAUDE.md` lines 36-52 (the `### Key Dependencies` heading and the fenced json block below it)
    - `docs/CLAUDE.md` lines 410-418 (the `## Testing` heading and its two-line body, followed by `## Deployment`)
    - `docs/CLAUDE.md` lines 585-595 (the "Important Files to Reference" list; the stale reference is line 589)
    - `docs/STRIPE_INTEGRATION.md` lines 110-120 (the documentation tree; the stale entry is line 115)
    - `package.json` lines 10-30 — the scripts block, for the exact `test`, `test:workers`, and `test:observability-worker` command strings, and the dependency names in `dependencies`
    - `.github/workflows/ci.yml` lines 28-60 — every step `name:` and its `run:` line, in file order
    - `.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md` `## REF-02 Verified Facts` — the verbatim script strings and the full CI step sequence, already captured
  </read_first>
  <action>
Three edits in `docs/CLAUDE.md` and one in `docs/STRIPE_INTEGRATION.md`, one commit.

**Testing section (D-05).** Replace `docs/CLAUDE.md` lines 412-415 — the `## Testing` heading and the two-line Status/Recommendation body under it — with the heading plus roughly ten lines of real content. Read `package.json` and `.github/workflows/ci.yml` in this task and copy names character-for-character; nothing here is written from memory.

Shape (per `04-CONTEXT.md` `<specifics>`): three bullets, then one ordered list.

- Three bullets, one per suite, each naming the suite, its config file, and its npm script: unit → `vitest.config.mts` → `npm test`; Workers integration → `vitest.workers.config.mts` → `npm run test:workers`; observability Durable Object → `vitest.observability.config.mts` → `npm run test:observability-worker`. Note that the bare `npm test` script resolves `vitest.config.mts` by Vitest's default discovery while the other two pass `--config` explicitly.
- Then an ordered list of the six non-test CI gates in `ci.yml` file order, **named by their step name**: Audit production dependencies, Check migration safety, Lint, Typecheck, Check Cloudflare binding types, Build.
- Then one sentence stating that the three test steps run between the Cloudflare binding-types step and the build step in the real pipeline, so a reader does not conclude the suites are outside CI.

Name the audit step by its step name only. Do **not** quote its `--audit-level` value: plan 04-05 changes that value in this same phase and the two plans have no ordering guarantee between them, so a quoted value here could be wrong the moment the sibling plan commits.

The sentence claiming no testing framework is configured must be gone, and no rephrasing of it may survive — a "formerly …" aside would trip the gate.

**Key Dependencies (D-06).** Replace the fenced json block at `docs/CLAUDE.md:38-50` with a plain bullet list of the same eight dependency names (`next`, `react`, `drizzle-orm`, `@clerk/nextjs`, `@opennextjs/cloudflare`, `zustand`, `@stripe/stripe-js`, `stripe`), each backticked, followed by one line stating that versions live in `package.json`. Keep the `### Key Dependencies` heading. No version string, no caret range, and no json fence remains in that section.

**Stale doc references (D-07).** Two sites name a documentation file that does not exist under `docs/` (confirmed absent in `04-RESEARCH.md` `## REF-03 Verified Facts`, which lists all 27 files):

- `docs/CLAUDE.md:589` — a bullet in "Important Files to Reference". Repoint it to `docs/api-architecture.md` and rewrite the description to match that file (RESTful API specifications and flows). Drop the `**NEW**:` marker; it has been there since 2025.
- `docs/STRIPE_INTEGRATION.md:115` — a tree entry inside a fenced directory listing. Repoint the filename to `api-architecture.md`, keeping the tree-drawing characters, the column alignment of the trailing `#` comment, and the comment text.

After this task no file under `docs/` names the nonexistent file. Do not leave a note explaining the rename — that would reintroduce the string.

Commit: `docs(04-02): describe the real test and CI setup, unpin dependency versions, repoint stale doc references`.
  </action>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'No formal testing' docs/CLAUDE.md` prints `0`.
    - `docs/CLAUDE.md` contains all three vitest config filenames, both explicit npm script names, and all six CI step names, matched with `grep -F` (fixed strings, not regex).
    - A regex search of `docs/CLAUDE.md` for `"(next|react|drizzle-orm|zustand|stripe|@clerk/nextjs|@opennextjs/cloudflare|@stripe/stripe-js)": "` prints `0` — no pinned dependency version survives (it prints `8` before the edit).
    - `docs/CLAUDE.md` names `package.json` at least once.
    - `/usr/bin/grep -rl 'API_STRUCTURE' docs/` matches no file.
    - `docs/STRIPE_INTEGRATION.md` names `api-architecture.md`.
    - `git diff` shows no change to the "Recent Fixes & Issues Resolved" or "Current Git Status" sections of `docs/CLAUDE.md` — both are deferred.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c 'No formal testing' docs/CLAUDE.md)" = "0"</automated>
    <fails_when>prints a number above 0 — the superseded testing-status sentence, or a rephrasing that retains its opening words, is still in the file</fails_when>
    <automated>for s in vitest.config.mts vitest.workers.config.mts vitest.observability.config.mts 'npm run test:workers' 'npm run test:observability-worker' 'Audit production dependencies' 'Check migration safety' 'Check Cloudflare binding types'; do /usr/bin/grep -qF "$s" docs/CLAUDE.md || { echo "MISSING FROM TESTING SECTION $s"; exit 1; }; done; echo TESTING_SECTION_OK</automated>
    <fails_when>output contains a "MISSING FROM TESTING SECTION string" line instead of TESTING_SECTION_OK, or the loop exits 1 — a suite, script, or CI step name was paraphrased instead of copied</fails_when>
    <automated>test "$(/usr/bin/grep -cE '"(next|react|drizzle-orm|zustand|stripe|@clerk/nextjs|@opennextjs/cloudflare|@stripe/stripe-js)": "' docs/CLAUDE.md)" = "0" && test "$(/usr/bin/grep -c 'package.json' docs/CLAUDE.md)" != "0"</automated>
    <fails_when>non-zero exit from test — a pinned dependency version line survives in the Key Dependencies block (it is 8 before the edit), or the pointer to package.json was not added</fails_when>
    <automated>test "$(/usr/bin/grep -rl 'API_STRUCTURE' docs/ | /usr/bin/grep -c .)" = "0" && test "$(/usr/bin/grep -c 'api-architecture.md' docs/STRIPE_INTEGRATION.md)" = "1"</automated>
    <fails_when>non-zero exit from test — a file under docs/ still names the nonexistent documentation file, or the Stripe integration tree entry was not repointed</fails_when>
    <automated>test "$(git diff HEAD~1 HEAD -- docs/CLAUDE.md | /usr/bin/grep -cE '^[+-].*(Recent Fixes|Current Git Status|Aug 23, 2025)')" = "0"</automated>
    <fails_when>prints a number above 0 — the diff touched a deferred dated section that 04-CONTEXT.md places outside this phase</fails_when>
  </verify>
  <done>`docs/CLAUDE.md` describes the three vitest suites and the CI gate order from source, lists dependency names without versions, and no doc under `docs/` references the nonexistent API architecture file; committed.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by **this plan**:

| Artifact | Kind | File |
|---|---|---|
| Rewritten `## Testing` section: three suite bullets + six-step CI gate list | new section body | `docs/CLAUDE.md` |
| `### Key Dependencies` as an unpinned name list plus a `package.json` pointer | replaced section body | `docs/CLAUDE.md` |
| `create_payment_intent` and `rotate_agent_key` tool bullets | new list entries | `docs/CLAUDE.md` |
| `(19 total)` tool-list heading and `19 MCP tools` feature line | corrected claims | `docs/CLAUDE.md:258,545` |
| Single citation of `app/api/mcp/route.ts` as the authoritative tool list | new claim | `docs/CLAUDE.md` |
| `@cf/openai/gpt-oss-20b` at four sites | corrected claims | `docs/CLAUDE.md:33,158,172,357` |
| `@cf/openai/gpt-oss-20b` in the AI sequence diagram | corrected claim | `docs/api-architecture.md:263` |
| PaymentProcessing note body citing `docs/checkout-trust-boundary.md` | replaced claim | `docs/api-architecture.md:434-435` |
| Both references to the nonexistent API architecture file | **removed / repointed** | `docs/CLAUDE.md:589`, `docs/STRIPE_INTEGRATION.md:115` |
| The "no testing framework configured" sentence and its Recommendation line | **removed** | `docs/CLAUDE.md:414-415` |
| The eight pinned dependency version strings and their json fence | **removed** | `docs/CLAUDE.md:38-50` |
| The unimplemented-integration parenthetical in the payment note | **removed** | `docs/api-architecture.md:435` |

Produced by sibling plans: the model-name sweep in three other files and the roadmap tool count (04-01); four README index groups and current status lines (04-03); four `Status: Historical` banners and 12 ticked checklist items (04-04); the `## Closed exceptions` subsection and the CI audit-level line (04-05).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/CLAUDE.md` → AI contributor | This file is loaded as agent context. A false operational claim here becomes an executed command later. |
| `docs/` → human contributor | Public, tracked documentation acted on directly. |
| documentation → CI pipeline | The Testing section describes the gates CI enforces; a wrong description can lead a contributor to skip a real gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | `docs/CLAUDE.md` | low | mitigate | No credential value is written. The `<ADMIN_VECTORIZE_TOKEN>` placeholders Phase 1 installed sit in the Admin Token Configuration and Deployment sections, neither of which this plan edits; the diff gate on the deferred sections also bounds the blast radius. |
| T-04-04 | Tampering | Testing / CI description in `docs/CLAUDE.md` | medium | mitigate | Suite names, config filenames, npm scripts, and CI step names are copied in-session from `package.json` and `.github/workflows/ci.yml`; the `grep -F` loop in Task 3 fails on any paraphrase. The audit gate is named by step name only, so plan 04-05's concurrent change to its `--audit-level` value cannot make this section false. |
| T-04-05 | Tampering | MCP tool list in `docs/CLAUDE.md` | medium | mitigate | The count is re-derived from `app/api/mcp/route.ts` during execution and cross-checked against plan 04-01's independent measurement; the 19-name loop fails if any name is absent, so an understated list cannot pass. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase; `04-RESEARCH.md` `## Package Legitimacy Audit` records the N/A disposition. |

Canon referral: reintroducing a literal credential value into `docs/` is canon security — covered by `/gsd-secure-phase` and the Phase 1 SEC-01 gate; not minted as a prohibition here.

Configured level: ASVS 1, block on `high`. No threat in this register rates `high`.
</threat_model>

<verification>
Scoped to the three files this plan owns. Run after Task 3:

```bash
test "$(/usr/bin/grep -ic 'llama' docs/CLAUDE.md docs/api-architecture.md | /usr/bin/grep -vc ':0$')" = "0"
test "$(/usr/bin/grep -rl 'API_STRUCTURE' docs/ | /usr/bin/grep -c .)" = "0"
test "$(/usr/bin/grep -c 'No formal testing' docs/CLAUDE.md)" = "0"
test "$(/usr/bin/grep -c 'mock implementation' docs/api-architecture.md)" = "0"
test "$(/usr/bin/grep -c '(19 total)' docs/CLAUDE.md)" = "1"
```

The repo-wide model-name sweep across all of `docs/` is a phase roll-up checked by `/gsd-verify-work`, not a gate for this plan — plans 01, 03, and 04 own the remaining files.
</verification>

<success_criteria>
- `docs/CLAUDE.md` names `@cf/openai/gpt-oss-20b` at five sites, states 19 MCP tools twice, and enumerates all 19 tool names.
- Its Testing section names three vitest suites with config files and scripts, and six CI gates in `ci.yml` order, every string copied from source.
- Its Key Dependencies block carries no version string and points at `package.json`.
- No file under `docs/` references the nonexistent API architecture document.
- `docs/api-architecture.md` names the current model and describes the real Stripe path with an ADR citation.
- The deferred dated sections of `docs/CLAUDE.md` are byte-identical in the diff.
- Three atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/phases/04-reference-documentation-refresh/04-02-SUMMARY.md` when done.
Record in it: the tool-arm count you measured from `app/api/mcp/route.ts` (must agree with plan 04-01's), the exact six CI step names you copied, and the descriptions you wrote for the two added tool bullets.
</output>
