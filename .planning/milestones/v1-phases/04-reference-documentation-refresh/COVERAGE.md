# API Coverage — Phase 4

> Full coverage by default. Opt-outs are explicit, reasoned decisions.

No external API integration: the phase edits Markdown under docs/ and one npm audit severity line in ci.yml; no SDK, route, or webhook changes. The detector matched doc titles containing "API" and "integration".
