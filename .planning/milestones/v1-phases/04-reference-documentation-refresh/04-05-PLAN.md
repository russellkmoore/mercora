---
phase: 04-reference-documentation-refresh
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - docs/dependency-security.md
autonomous: true
requirements: [DEP-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CI's production dependency audit runs at --audit-level=high; no --audit-level=critical remains in .github/workflows/ci.yml (D-14)"
    - "The raised gate was proven to pass locally before the CI change was committed: npm audit --omit=dev --audit-level=high exited 0 and its exit code is recorded in the plan summary (D-14)"
    - "Both Next-bundled production exceptions are closed with a dated exit-condition-met marker and moved to a Closed exceptions subsection of docs/dependency-security.md, so the history stays readable (D-13)"
    - "docs/dependency-security.md records the re-run evidence in its status header: re-run date, Node and npm versions, installed Next and PostCSS versions, and the audit totals (D-16)"
    - "The Development-only findings section is refreshed from a full npm audit run in the same session; any dev-only finding is listed with its severity and package path rather than suppressed (D-16)"
    - "docs/dependency-security.md carries a next-review date of 2026-12-01, which is in the future, and the owners are unchanged (D-15)"
    - "The document describes the CI gate as it now is, not as a future intention"
    - { statement: "The dependency-baseline requirement produced no applicable edge category from the probe taxonomy; instead the gate is bounded by two runnable commands whose exit codes are recorded — the production audit at the raised level and a full-tree audit including dev dependencies — so the document's claims are reproducible by a reviewer rather than asserted", verification: backstop }
  prohibitions:
    - statement: "MUST NOT weaken the dependency audit gate, suppress a finding, or narrow the audited scope to make the audit report clean — a blocking finding is recorded as a bounded exception with an owner and an exit condition, never hidden"
      status: unverified
      verification: test
    - statement: "MUST NOT write a factual claim into docs/dependency-security.md that was not produced by a command run in this session — no version number, advisory id, package path, or audit total carried over from the previous baseline without re-observation"
      status: unverified
      verification: judgment
    - statement: "MUST NOT mark an exception closed without recording the command output that satisfies its own stated exit condition"
      status: unverified
      verification: judgment
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Production dependency audit gate at the high severity level"
      contains: "audit-level=high"
    - path: "docs/dependency-security.md"
      provides: "Refreshed dependency baseline with both production exceptions closed, current dev-only findings, and a future review date"
      contains: "Closed exceptions"
  key_links:
    - from: "docs/dependency-security.md"
      to: ".github/workflows/ci.yml"
      via: "the Enforcement section states the gate level the workflow actually runs"
      pattern: "audit-level=high"
    - from: "docs/dependency-security.md"
      to: "package-lock.json"
      via: "the recorded Next, PostCSS, and Sharp versions are the versions the lockfile resolves"
      pattern: "8\\.5\\.[0-9]+"

flagged_assumptions:
  - id: FA-04-01
    source: "spec-less edge probe, DEP-01 row"
    statement: "The edge probe returned DEP-01 as unclassified and the row stays unresolved — no boundary, adjacency, empty, encoding, ordering, precision, or concurrency category applies to a dependency-baseline refresh. It is surfaced here rather than silently dropped. The compensating control is the backstop truth above: both audit commands are run and their exit codes recorded, so a reviewer can reproduce every number in the document."
    status: unresolved
---

<objective>
Raise CI's production dependency audit gate to `high` and refresh `docs/dependency-security.md` so it describes the dependency posture that exists today rather than the one that existed on 2026-08-11.

Purpose: the document's next-review date passed a week ago, its two production exceptions were written against Next 15.5.22 and both of their exit conditions have since been met under Next 16.3.1, and its Enforcement section describes the raised audit gate as a future intention while CI still runs at the lower level. The one non-documentation change in this phase closes that gap.
Output: one CI workflow line and one refreshed baseline document, in two atomic commits.

## Decision map (D-NN to CONTEXT.md bullet)

From `04-CONTEXT.md` `<decisions>` "Dependency Baseline (DEP-01)":

- **D-13** — both Next-bundled exceptions close as "exit condition met 2026-09-02" and move to a short "Closed exceptions" subsection so the history stays readable. Evidence recorded: under Next 16.3.1 and Node 24.18.1 the production audit reports 0 findings at every severity; Sharp and PostCSS resolutions are stated as observed.
- **D-14** — the CI gate rises to `high`. The executor runs that exact command locally and records the exit code before committing. This is the only non-documentation change in the phase.
- **D-15** — next review date is 2026-12-01; owners stay Russell K. Moore and Devon Hillard as the document has them.
- **D-16** — the refreshed document records the re-run evidence in its status header: re-run date, Node and npm versions, installed Next and PostCSS versions, and the audit totals. The Development-only findings section is refreshed from a full audit including dev dependencies run in the same session, and any findings are listed with severity and package path rather than suppressed.

Exact wording of the closed-exception entries is Claude's discretion per `04-CONTEXT.md`.

## Correction carried from research — read before writing the Sharp entry

`04-RESEARCH.md` `## DEP-01 Verified Facts` falsified one of the facts this plan was briefed with. The claim that Sharp does not resolve is **false**: it resolves from a top-level hoisted `node_modules/sharp`, declared as a direct dependency of Next, at a version in the 0.35 line. Only `node_modules/next/node_modules` is Sharp-free, because npm hoisted the dependency rather than nesting it.

That is stronger evidence for closing the exception, not weaker: the exception's own exit condition asks for Sharp 0.35 or newer, and the resolved version satisfies it. Re-observe the version yourself and write what you observe — do not copy the number out of RESEARCH.md, and do not repeat the falsified "does not resolve" framing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reference-documentation-refresh/04-CONTEXT.md
@.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md
@.planning/phases/04-reference-documentation-refresh/04-PATTERNS.md
</context>

## Shell rules for every command in this plan

Confirmed live in `04-RESEARCH.md` "Common Pitfalls" #2: a shell hook intercepts bare `grep` and `find` here and returns wrong counts.

- Absolute paths in every gate: `/usr/bin/grep`, `/usr/bin/sed`, `/usr/bin/wc`.
- Never `diff <(...) <(...)` — a shell function shadows `diff`.
- Never `npx vitest` inside a `bash -c` string — use `./node_modules/.bin/vitest run`.
- `npm` and `node` run directly from the executor shell; Phase 3 proved `npm run lint`, `npm run typecheck`, and `npm test` all resolve there.
- `/usr/bin/grep -c` prints `0` and exits 1 on a clean file; `test "$(...)" = "0"` is the correct idiom. No `|| echo 0` fallbacks.

<tasks>

<task type="tracer">
  <name>Task 1: Prove the raised audit gate locally, then raise it in CI — one line, end to end</name>
  <precondition>node_modules/ is installed from the committed package-lock.json in this checkout and `node --version` reports 24.x — the audit result is a property of the resolved dependency tree, so a stale or absent install would measure the wrong tree.</precondition>
  <reversibility rating="reversible">One value on one line of a tracked workflow file. Reverting the commit restores the previous gate level with no migration and no coordination; nothing outside CI reads it.</reversibility>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 28-40 — the Install dependencies step and the Audit production dependencies step on lines 32-33
    - `docs/dependency-security.md` lines 112-127 — the Enforcement and follow-up section, which already carries the target command as a forward-looking code block
    - `.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md` `## DEP-01 Verified Facts` — the audit results observed during research, for comparison against your own run
  </read_first>
  <action>
This is the tracer for the plan and the only non-documentation change in the phase: prove the gate passes, then make it real, then commit — before Task 2 writes anything about it.

**First, run the audit and record what happens.** Run `npm audit --omit=dev --audit-level=high` and capture both its stdout and its exit code. Do not skip this because research already ran it: the result is a property of the resolved dependency tree at this moment, and the lockfile may have moved.

If it exits non-zero, **stop and report** — do not lower the gate, do not add `--omit` scopes, and do not edit the workflow. A blocking finding is a real finding: `04-CONTEXT.md` and the ROADMAP success criterion both allow the alternative outcome of recording it as a bounded exception with an owner and an exit condition, but that is a decision for the developer, not a silent weakening. Surface the finding with its severity and package path and halt.

**Then raise the gate (D-14).** In `.github/workflows/ci.yml` line 33, change the audit severity threshold from the lower level to `high`. Change nothing else: the `- name: Audit production dependencies` line above it, its indentation, the `--omit=dev` flag, and every other step in the workflow stay byte-identical. The step name is load-bearing — plan 04-02 writes it into `docs/CLAUDE.md`'s CI gate list by name.

Also run a full-tree audit including dev dependencies (`npm audit`) in this same session and keep its output. Task 2 needs it for the Development-only findings section, and running both in one session is what keeps the document from being half-current (D-16).

Commit: `ci(04-05): raise the production dependency audit gate to high`.
  </action>
  <acceptance_criteria>
    - `npm audit --omit=dev --audit-level=high` exits 0, and that exit code is written into the plan summary.
    - `/usr/bin/grep -c 'audit-level=high' .github/workflows/ci.yml` prints `1`.
    - `/usr/bin/grep -c 'audit-level=critical' .github/workflows/ci.yml` prints `0`.
    - The `- name: Audit production dependencies` step name line is unchanged, at its original indentation.
    - The `--omit=dev` flag is still present on the audit command — the scope was not narrowed.
    - `git diff HEAD~1 HEAD -- .github/workflows/ci.yml` shows exactly one changed line.
    - The full-tree `npm audit` output is captured for Task 2.
  </acceptance_criteria>
  <verify>
    <automated>npm audit --omit=dev --audit-level=high</automated>
    <fails_when>non-zero exit, or the report lists any finding at high or critical severity in the production tree — in which case the workflow must not be edited and the finding is reported instead</fails_when>
    <automated>test "$(/usr/bin/grep -c 'audit-level=high' .github/workflows/ci.yml)" = "1" && test "$(/usr/bin/grep -c 'audit-level=critical' .github/workflows/ci.yml)" = "0"</automated>
    <fails_when>either test exits non-zero — the raised threshold is missing or duplicated, or the previous lower threshold survives somewhere in the workflow</fails_when>
    <automated>test "$(/usr/bin/grep -c '^      - name: Audit production dependencies$' .github/workflows/ci.yml)" = "1" && test "$(/usr/bin/grep -c 'audit --omit=dev' .github/workflows/ci.yml)" = "1"</automated>
    <fails_when>either test exits non-zero — the step name or its indentation changed (breaking the name plan 04-02 writes into the CI gate list), or the production-only scope flag was dropped, which would silently change what the gate measures</fails_when>
    <automated>test "$(git diff HEAD~1 HEAD -- .github/workflows/ci.yml | /usr/bin/grep -c '^+[^+]')" = "1"</automated>
    <fails_when>prints a number other than 1 — more than the single audit-level value was rewritten in the workflow</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit — inherited verbatim from Phase 3 plan 03-03, run here as a cheap confirmation that the phase's one non-documentation change did not disturb anything the linter sees</fails_when>
  </verify>
  <done>`npm audit --omit=dev --audit-level=high` passes locally with its exit code recorded, CI's audit step runs at that level, and the one-line change is committed as its own atomic commit.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the dependency baseline document from this session's audit output</name>
  <reversibility rating="reversible">Section rewrites in one tracked markdown file.</reversibility>
  <files>docs/dependency-security.md</files>
  <read_first>
    - `docs/dependency-security.md` — the whole file (127 lines): the status header on 1-6, the Scope section, the Remediated direct dependencies table, both exception entries on 63-98, Development-only findings on 100-110, and Enforcement and follow-up on 112-126
    - `package.json` — the `next` dependency declaration and the `engines` field
    - `.planning/phases/04-reference-documentation-refresh/04-PATTERNS.md` section on `docs/dependency-security.md` — the existing exception-entry field shape to reuse for the closed entries
    - `.planning/phases/04-reference-documentation-refresh/04-RESEARCH.md` `## DEP-01 Verified Facts` — the Sharp-resolution correction and the dev-only finding chains observed during research, for comparison against your own runs
  </read_first>
  <action>
Rewrite four parts of `docs/dependency-security.md` from the command output Task 1 captured. Every number written here must come from a command you ran in this session — no figure carried forward from the 2026-08-11 baseline without re-observation.

**Gather the facts first.** Alongside Task 1's two audit runs, observe and record: the Node and npm versions, the installed Next version, the resolved PostCSS version, and the resolved Sharp version with the path it resolves from. `npm ls <package>` and `node -e "console.log(require.resolve('<package>'))"` are the direct ways to get the last three.

**Status header (D-15, D-16).** Rewrite lines 1-6. Keep the `# Dependency Security Baseline` H1 and the existing bold-label field shape. The Status line now says no critical or high production findings and no open production exceptions.

Add these fields, using exactly these labels so a reviewer can grep for each one:

- `**Re-run date:** 2026-09-02`
- `**Node:** <version>` and `**npm:** <version>` — the runtime that produced the audit
- `**Installed Next:** <version>` and `**Installed PostCSS:** <version>` — bare semver values, each observed with `npm ls` or `require.resolve` in this session, not copied from the previous baseline
- the production audit totals by severity

Keep `**Owners:**` exactly as the document has it — Russell K. Moore and Devon Hillard — and set `**Next review:** 2026-12-01`.

**Exceptions (D-13).** Both entries under `## Time-bounded production exceptions` close. Leave that heading in place with a one-line body stating that no production exception is open as of the re-run date, then add a new `## Closed exceptions` section holding both entries.

Each closed entry keeps the field shape it already has — owners, advisories, package path, why it remained, exposure, compensating controls — and replaces the open `**Exit condition:**` framing with `**Closed:** exit condition met 2026-09-02`, followed by the evidence that satisfies that entry's own stated exit condition:

- PostCSS — the exit condition asked for a supported Next 16 release bundling a patched PostCSS, then raising the CI gate. Record the installed Next version, the resolved PostCSS version, and that the gate was raised in this same phase.
- Sharp — the exit condition asked for a supported Next 16 release using Sharp 0.35 or newer. Record the resolved Sharp version and the full `node_modules/sharp` path it resolves from, and state that it is hoisted to the top level as a direct dependency of Next. Per the correction above, write what your own `require.resolve` and `npm ls` output show; do not restate the briefing's claim about resolution failing.

**Development-only findings (D-16).** Rewrite the section from Task 1's full-tree audit output. List every finding with its severity and its package path, and trace each to the dev dependency that pulls it in. If the full run reports findings, they are listed, not suppressed; if it reports none, say so and say which command produced that result. Keep the existing closing guidance that these tools must only process trusted project input and must not expose their development servers to untrusted networks.

**Enforcement and follow-up (D-14).** The prose currently describes the raised gate as something CI should enforce once a follow-up lands. Rewrite it in the present tense: CI runs the audit at the `high` level now. Remove the forward-looking framing around the code block at the end of the section — the block's command is now what CI runs, not an aspiration. No `--audit-level=critical` string may remain anywhere in the file.

Do not touch the Remediated direct dependencies table or the historical pre-remediation figures; those are dated records and remain accurate as history.

Commit: `docs(04-05): refresh the dependency baseline and close both production exceptions`.
  </action>
  <acceptance_criteria>
    - `**Re-run date:** 2026-09-02` and `**Next review:** 2026-12-01` are both present; both owner names are unchanged.
    - A `## Closed exceptions` section exists and holds both entries, each carrying `**Closed:** exit condition met 2026-09-02`.
    - The Node version, npm version, installed Next version, and resolved PostCSS and Sharp versions all appear, each observed by a command in this session.
    - No `--audit-level=critical` string remains in the file; the `high` level appears at least once.
    - The Remediated direct dependencies table is unchanged in `git diff`.
    - The Development-only findings section names each finding's package path, or states explicitly that the full-tree run reported none and which command produced that.
  </acceptance_criteria>
  <verify>
    <automated>for s in '**Re-run date:** 2026-09-02' '**Next review:** 2026-12-01' '**Node:**' '**npm:**' 'Russell K. Moore' 'Devon Hillard'; do /usr/bin/grep -qF -e "$s" docs/dependency-security.md || { echo "MISSING HEADER FIELD $s"; exit 1; }; done; echo HEADER_OK</automated>
    <fails_when>output contains a "MISSING HEADER FIELD" line naming the absent label instead of HEADER_OK — the re-run date, the future review date, or the runtime that produced the audit is missing, or an owner was dropped</fails_when>
    <automated>test "$(/usr/bin/grep -c '^## Closed exceptions$' docs/dependency-security.md)" = "1" && test "$(/usr/bin/grep -c 'Closed:\*\* exit condition met 2026-09-02' docs/dependency-security.md)" = "2"</automated>
    <fails_when>either test exits non-zero — the Closed exceptions section is missing, or the number of dated closure markers is not 2, meaning an exception was closed without a marker or a marker was added without an entry</fails_when>
    <automated>test "$(/usr/bin/grep -c 'audit-level=critical' docs/dependency-security.md)" = "0" && test "$(/usr/bin/grep -c 'audit-level=high' docs/dependency-security.md)" != "0"</automated>
    <fails_when>either test exits non-zero — the document still describes CI as gating at the lower level, or it never states the level CI now runs at</fails_when>
    <automated>test "$(/usr/bin/grep -cE '^\*\*Installed (Next|PostCSS):\*\* [0-9]+\.[0-9]+\.[0-9]+' docs/dependency-security.md)" = "2"</automated>
    <fails_when>prints a number other than 2 — an Installed Next or Installed PostCSS field is missing, or its value is prose rather than a bare semver, meaning an exception was closed without recording the version evidence its own exit condition asks for. Neither label exists in the file before this task, so a passing count of 2 cannot be inherited from the previous baseline</fails_when>
    <automated>test "$(/usr/bin/grep -c 'node_modules/sharp' docs/dependency-security.md)" != "0"</automated>
    <fails_when>prints 0 — the Sharp entry was closed without recording the path the package actually resolves from, which is the evidence its exit condition turns on and the point research had to correct</fails_when>
    <automated>test "$(git diff HEAD~1 HEAD -- docs/dependency-security.md | /usr/bin/grep -cE '^[+-]\| `(@clerk/nextjs|drizzle-orm|wrangler)`')" = "0"</automated>
    <fails_when>prints a number above 0 — the diff altered rows of the Remediated direct dependencies table, which is a dated historical record and stays as written</fails_when>
    <automated>npm audit --omit=dev --audit-level=high</automated>
    <fails_when>non-zero exit, or the report lists a high or critical production finding — the document would then be describing a passing gate that does not pass</fails_when>
  </verify>
  <done>`docs/dependency-security.md` records this session's audit evidence in its header, closes both production exceptions with dated markers and observed versions, lists the current dev-only findings, and describes the CI gate in the present tense; committed.</done>
</task>

</tasks>

## Artifacts this phase produces

Produced by **this plan**:

| Artifact | Kind | File |
|---|---|---|
| `npm audit --omit=dev --audit-level=high` as the enforced CI gate | changed config value | `.github/workflows/ci.yml:33` |
| `## Closed exceptions` section holding both former production exceptions | new section | `docs/dependency-security.md` |
| `**Closed:** exit condition met 2026-09-02` markers (2) | new claims | `docs/dependency-security.md` |
| Status header fields: re-run date, Node and npm versions, Next and PostCSS versions, audit totals | new claims | `docs/dependency-security.md:1-6` |
| `**Next review:** 2026-12-01` | corrected claim | `docs/dependency-security.md` |
| Refreshed Development-only findings list with severities and package paths | replaced section body | `docs/dependency-security.md` |
| Present-tense Enforcement prose describing the gate CI now runs | replaced section body | `docs/dependency-security.md` |
| The two open time-bounded production exception entries | **removed from the open section** | `docs/dependency-security.md:63-98` |
| The lower audit-severity threshold | **removed** | `.github/workflows/ci.yml:33` |

Produced by sibling plans: the model-name sweep and roadmap tool count (04-01); the rewritten Testing section, de-pinned Key Dependencies, corrected tool count and list, and repointed doc references (04-02); four new README index groups and current status lines (04-03); four `Status: Historical` banners, the two `docs/architecture.md` diagram labels, and 12 ticked checklist items (04-04).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → production dependency tree | Untrusted upstream code enters the deployed Worker through the lockfile. The CI audit step is the gate on that boundary. |
| CI workflow → merge decision | `.github/workflows/ci.yml` decides what may merge; weakening a gate here silently widens every downstream boundary. |
| `docs/dependency-security.md` → operator | The document is the record an operator relies on to know which exceptions are open and when they expire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-12 | Tampering | supply chain via the CI audit gate | medium | mitigate | This change **tightens** the gate — the severity threshold moves down so more findings block. The gate is proven to pass locally before the workflow is edited, and the `--omit=dev` scope flag is asserted still present so the tightening cannot be offset by a quiet scope narrowing. |
| T-04-13 | Repudiation | closed exception entries | medium | mitigate | Each closure carries a dated marker and the observed version evidence that satisfies that entry's own stated exit condition; the version gate fails if the evidence is missing, so an exception cannot be closed on assertion alone. |
| T-04-14 | Information Disclosure | refreshed dev-only findings | low | mitigate | The section names package paths and advisory identifiers only — all public registry data. No token, no internal host, no deployment identifier is introduced. |
| T-04-15 | Denial of Service | CI availability after the gate raise | low | accept | A future high-severity advisory in a production dependency will now block merges. That is the intended behaviour of the gate, and the reversibility rating records that reverting one line restores the previous threshold if the developer chooses. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan or this phase — nothing is added to the dependency tree, only audited. `04-RESEARCH.md` `## Package Legitimacy Audit` records the N/A disposition explicitly and states the gate protocol was not run because there is nothing to audit. |

Canon referral: generic supply-chain advisory triage is canon security — covered by `/gsd-secure-phase` and Dependabot; not minted as a bespoke prohibition here. The prohibition that *is* minted is the project-specific one: the gate must not be weakened or its scope narrowed to manufacture a clean report.

Configured level: ASVS 1, block on `high`. No threat in this register rates `high`. ASVS V14 (configuration and dependency management) is the one category `04-RESEARCH.md` marks as applying to this phase, and this plan is its control.
</threat_model>

<verification>
Scoped to the two files this plan owns. Run after Task 2:

```bash
npm audit --omit=dev --audit-level=high
test "$(/usr/bin/grep -c 'audit-level=high' .github/workflows/ci.yml)" = "1"
test "$(/usr/bin/grep -c 'audit-level=critical' .github/workflows/ci.yml)" = "0"
test "$(/usr/bin/grep -c '^## Closed exceptions$' docs/dependency-security.md)" = "1"
test "$(/usr/bin/grep -c 'Closed:\*\* exit condition met 2026-09-02' docs/dependency-security.md)" = "2"
/usr/bin/grep -qF '**Next review:** 2026-12-01' docs/dependency-security.md
```

`docs/dependency-security.md` also gains a README index entry under plan 04-03. That plan was instructed to describe this document in wording that holds both before and after this refresh, so the two plans have no ordering dependency and neither blocks the other.
</verification>

<success_criteria>
- CI's production dependency audit runs at `--audit-level=high`, with the `--omit=dev` scope unchanged and the step name intact.
- The raised gate was proven to pass locally, with its exit code recorded, before the workflow was edited.
- Both production exceptions are closed with dated markers and the observed version evidence their own exit conditions require.
- The status header records the re-run date, runtime versions, package versions, and audit totals from this session.
- The next-review date is 2026-12-01 and the owners are unchanged.
- Development-only findings are listed with severity and package path, not suppressed.
- Two atomic commits, one per task.
</success_criteria>

<output>
Create `.planning/phases/04-reference-documentation-refresh/04-05-SUMMARY.md` when done.
Record in it: the exact exit code of `npm audit --omit=dev --audit-level=high`, the full-tree `npm audit` severity totals, the observed Node, npm, Next, PostCSS, and Sharp versions with the path Sharp resolved from, and — if the production audit did not exit 0 — the finding that blocked, so the developer can decide between a bounded exception and a fix.
</output>
