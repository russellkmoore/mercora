---
phase: 01-security-and-admin-auth-truth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CLAUDE.md
  - docs/admin-authentication.md
  - docs/DEPLOYMENT_SETUP.md
  - components/admin/AdminGuard.tsx
autonomous: true
requirements: [SEC-01, SEC-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No literal admin credential value remains anywhere in the tracked source tree outside .planning/; the published token value is replaced by a named placeholder in the one doc that carried it."
    - "The credential search is a byte-exact, case-sensitive literal match with no regex metacharacters, so a differently-cased or Unicode-normalized variant of the token would not be counted as a match and must be checked by eye during the edit."
    - "The credential search excludes .planning/ deliberately, because this phase's own planning artifacts quote the old value in order to specify the rotation and the probe; that value is dead once plan 01-04 completes. The exclusion is recorded here rather than silently applied."
    - "Zero matches from the credential search is the success state, not an error state; each negative gate is paired with a positive control match so a broken pathspec or a missing docs tree cannot produce a vacuous pass."
    - "No documentation example anywhere under docs/ transports an admin credential in a URL query string; every example uses an Authorization Bearer, X-API-Key, or x-dev-admin header."
    - "No document under docs/ claims admin authentication is switched off. The claims about admin-auth state are matched case-insensitively because they are English prose, while identifier literals are matched case-sensitively; that distinction is deliberate."
    - "docs/admin-authentication.md describes production admin access as Clerk sessionClaims.metadata.role equal to admin, or an active row in the adminUsers D1 table via isUserAdmin in lib/models/admin.ts, and names no environment-variable admin allowlist."
    - "No environment-variable admin allowlist is named as a live mechanism in docs/, components/, lib/, or app/. The historical reference inside migrations/0002_add_admin_users.sql is left untouched because it accurately records that the variable was superseded."
    - "The only documented bypass is the x-dev-admin header under a development NODE_ENV, and the documentation states that condition alongside every example that uses it."
    - "docs/CLAUDE.md points to docs/admin-authentication.md as the single source of truth for admin authentication rather than restating the mechanism at length."
  artifacts:
    - path: "docs/CLAUDE.md"
      provides: "Correct short summary of admin auth plus header-only credential examples"
      contains: "admin-authentication.md"
    - path: "docs/admin-authentication.md"
      provides: "The single source of truth for admin authentication, matching lib/auth and lib/models/admin"
      contains: "isUserAdmin"
    - path: "docs/DEPLOYMENT_SETUP.md"
      provides: "Deploy runbook whose vectorize examples authenticate with a header"
      contains: "Authorization: Bearer"
    - path: "components/admin/AdminGuard.tsx"
      provides: "Header comment describing the production admin check the code actually performs"
      contains: "adminUsers"
  key_links:
    - from: "docs/admin-authentication.md"
      to: "lib/models/admin.ts"
      via: "prose naming isUserAdmin and the adminUsers table as the production admin check"
      pattern: "isUserAdmin"
    - from: "docs/CLAUDE.md"
      to: "docs/admin-authentication.md"
      via: "pointer replacing the long inline authentication section"
      pattern: "admin-authentication\\.md"
    - from: "docs/DEPLOYMENT_SETUP.md"
      to: "lib/auth/admin-middleware.ts"
      via: "header-form curl examples matching the header-only extraction in the code"
      pattern: "Authorization: Bearer"
  prohibitions:
    - statement: "MUST NOT satisfy a documentation gate by deleting operator-necessary instructions instead of correcting them. Every removed example must be replaced by a working equivalent, never dropped."
    - statement: "MUST NOT write any real credential value into a document. Placeholders only, in angle-bracket form."
---

<objective>
Remove the published credential from the documentation and make all three admin-auth documents
describe the mechanism the code actually enforces.

Purpose: `docs/CLAUDE.md:416` publishes the live production admin token in a tracked file. Three
documents state that admin authentication is switched off, which is false — it has been enforced
since the database-backed admin table landed. Five documented examples send credentials in a URL
query string, which the code rejects and which leaks through logs, browser history, and the
Referer header. `docs/admin-authentication.md` describes an environment-variable admin allowlist
that does not exist anywhere in `lib/` or `app/`.

Output: a credential-free `docs/` tree, header-only credential examples everywhere, and one
authoritative description of admin authentication that matches `lib/auth/admin-middleware.ts`,
`lib/auth/unified-auth.ts`, and `lib/models/admin.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-and-admin-auth-truth/01-CONTEXT.md
@.planning/phases/01-security-and-admin-auth-truth/01-RESEARCH.md
@lib/auth/admin-middleware.ts
@lib/auth/unified-auth.ts
@lib/models/admin.ts
</context>

## Notes for the executor

<!-- planner-discipline-allow: ADMIN_USER_IDS -->

Every claim written into these documents must be checked against the three source files in
`<context>` first. This plan exists because the previous documentation was written from intent
rather than from code; repeating that is the one failure mode that matters here.

Placeholder convention, fixed by D-SEC-01: write `<ADMIN_VECTORIZE_TOKEN>` where a real token
value appeared, and `<DEV_ADMIN_BYPASS_TOKEN>` where the development bypass value appeared. Never
a real value.

Two things are deliberately **not** changed by this plan. The development bypass literal stays in
`lib/auth/admin-middleware.ts:22` — Russell decided on 2026-09-01 to keep it as a source literal,
and the `x-dev-admin` code path must not be removed or relocated. And the historical note inside
`migrations/0002_add_admin_users.sql:3` stays exactly as written, because it correctly records
which mechanism replaced which; migration files are immutable.

`git grep` respects `.gitignore`, so it will not report the local-only `.env.local` value or the
generated `.open-next/` build output. Do not "fix" either; both are out of scope by explicit
decision, and `.env.local` holds a different value that merely shares a prefix.

Project commands run under Node 24 via mise; prefix with `mise exec --` only if plain commands fail.

<tasks>

<task type="auto">
  <name>Task 1: Correct docs/CLAUDE.md — six regions</name>
  <files>docs/CLAUDE.md</files>
  <read_first>
    - `docs/CLAUDE.md` lines 100-115, 155-210, 313-345, 410-420, 450-460 — the six regions listed below, read before editing so the surrounding structure survives.
    - `lib/auth/admin-middleware.ts` lines 17-99 — the actual order of checks: guard, header bypass, Bearer / X-API-Key against the service token, same-origin check for mutating methods, Clerk session, `isUserAdmin` database lookup, Clerk metadata role fallback.
    - `lib/auth/unified-auth.ts` lines 73-81 — `extractToken`, which reads only the `authorization` and `x-api-key` headers.
  </read_first>
  <action>
    Six edits in one file. Line numbers are from the current tracked version; re-locate by
    surrounding text rather than trusting the numbers after your first edit shifts them.

    1. **Lines 106-107**, the project-structure tree entries for `admin-middleware.ts` and
       `unified-auth.ts`. Both trailing parenthetical comments assert the module is inactive. Replace
       each with a short accurate one, e.g. `# Admin auth choke point (enforced)` and
       `# Service token + Clerk session auth (enforced)`.

    2. **Lines 159-162**, the "Authentication Status" bullets under the Admin Dashboard section.
       All three assert the system is off and awaiting re-enablement. Replace the whole bullet group
       with two bullets: one stating that admin authentication is enforced in production, and one
       pointing at `docs/admin-authentication.md` as the source of truth.

    3. **Lines 190-204**, the whole `## Authentication System` section, including its "Current
       Implementation" preamble, both per-file subsections, and the "Re-enabling Authentication"
       list. Do not edit this in place — replace the entire section body with about six lines
       summarising the real mechanism, then a pointer to `docs/admin-authentication.md`. The six
       lines must cover, in order: the two entry points (`checkAdminPermissions` in
       `lib/auth/admin-middleware.ts` and `authenticateRequest` in `lib/auth/unified-auth.ts`);
       server-to-server access via an `Authorization: Bearer` or `X-API-Key` header carrying
       `<ADMIN_VECTORIZE_TOKEN>`; interactive access via a Clerk session where the user has
       `sessionClaims.metadata.role` equal to `admin` or an active row in the `adminUsers` table
       (`isUserAdmin` in `lib/models/admin.ts`); the same-origin requirement on mutating methods;
       and the single development-only bypass, the `x-dev-admin` header, which is honoured only when
       `NODE_ENV` is the development value. Note the "Capabilities" line in the old subsection lists
       a URL-parameter authentication method — that claim goes with the section.

    4. **Line 318**, in the Vectorization paragraph. The example authenticates by URL parameter.
       Rewrite it as a `curl` invocation using an `Authorization: Bearer <ADMIN_VECTORIZE_TOKEN>`
       header against the same endpoint. Keep the surrounding sentence about the token being managed
       as a Cloudflare secret in production; it is correct.

    5. **Line 341**, the first bullet under "Authentication Methods". It offers a URL-parameter form
       that `extractToken` does not accept. Delete that bullet entirely and leave the two header
       bullets below it. Add one short sentence under the list explaining that credentials are read
       from headers only, because a credential in a URL leaks through server logs, browser history,
       and the Referer header — the same reason recorded in the comment at
       `lib/auth/admin-middleware.ts:27`.

    6. **Line 416**, under "Live Environment": the `**Admin Token**` bullet publishes a real value.
       Replace the value with `<ADMIN_VECTORIZE_TOKEN>` and add a half-sentence noting it is set as a
       Cloudflare Worker secret and is never stored in the repository.

    7. **Line 456**, step 2 of "Adding New Products". Same URL-parameter form as edit 4; rewrite it
       to the header form or, since the admin UI is the ordinary path here, to a reference to the
       admin UI plus the header-form `curl` as the scripted alternative.
  </action>
  <acceptance_criteria>
    - `git grep -c "voltique-admin" -- docs/` returns no matches.
    - `git grep -n -e "?token=" -e "?dev=" -- docs/CLAUDE.md` returns no matches.
    - `git grep -ni -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/CLAUDE.md` returns no matches.
    - `docs/CLAUDE.md` references `admin-authentication.md` at least once.
    - `docs/CLAUDE.md` still documents how to reach the vectorize endpoint from a script; no example was deleted without a header-form replacement.
    - The angle-bracket placeholder appears where the real value was.
  </acceptance_criteria>
  <verify>
    <automated>git grep -q "ADMIN_VECTORIZE_TOKEN" -- docs/CLAUDE.md && ! git grep -q "voltique-admin" -- docs/</automated>
    <fails_when>non-zero exit — either the env-var control string vanished from the file (the edit went too far) or the published credential literal is still present somewhere under docs/</fails_when>
    <automated>git grep -q "Authorization: Bearer" -- docs/CLAUDE.md && ! git grep -q -e "?token=" -e "?dev=" -- docs/CLAUDE.md</automated>
    <fails_when>non-zero exit — either no header-form example remains in the file, or a query-string credential example is still present</fails_when>
    <automated>git grep -q "admin-authentication.md" -- docs/CLAUDE.md && ! git grep -qi -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/CLAUDE.md</automated>
    <fails_when>non-zero exit — either the pointer to the source-of-truth doc is missing, or a claim that admin auth is switched off survives in the file</fails_when>
  </verify>
  <done>`docs/CLAUDE.md` publishes no credential, describes admin authentication in six accurate lines with a pointer to the detailed document, and every credential example it retains uses a header.</done>
</task>

<task type="auto">
  <name>Task 2: Correct docs/admin-authentication.md — the source of truth</name>
  <files>docs/admin-authentication.md</files>
  <read_first>
    - `docs/admin-authentication.md` lines 85-145 (the environment-based access-control section, the environment-variable table, and the Clerk configuration steps), lines 190-225 (the testing examples and security warnings), lines 232-250 (the migration-from-previous-system section).
    - `lib/models/admin.ts` lines 1-40 — `isUserAdmin` as a Drizzle query against the `adminUsers` table filtered on `userId` and `isActive`.
    - `lib/auth/admin-middleware.ts` lines 49-82 — the real production branch order: Clerk session, `isUserAdmin` database lookup, then the `sessionClaims.metadata.role` fallback.
    - `lib/auth/unified-auth.ts` lines 158-172 — the equivalent branch in the other entry point.
  </read_first>
  <action>
    This file is designated the single source of truth by Task 1, so it has to be right. Five edits.

    1. **Lines 96-105**, the "Production Environment" fenced snippet. It shows a production check
       reading a comma-separated environment variable and testing membership. That variable is read
       nowhere in `lib/` or `app/` — it was superseded by the `adminUsers` table in migration 0002
       and the documentation was never updated. Replace the snippet with one that mirrors
       `lib/auth/admin-middleware.ts:64-77`: the `isUserAdmin(userId)` database lookup first, then the
       `sessionClaims.metadata.role` fallback. Name `lib/models/admin.ts` beneath it.

    2. **Line 124**, the row in the environment-variable table for that same non-existent variable.
       Delete the row. Replace it with a sentence under the table stating that the production admin
       list lives in the `adminUsers` D1 table, is managed through `/admin/users`, and is not
       configured by an environment variable.

    3. **Lines 130-140**, "Production Setup" step 1, which instructs the operator to set that
       variable. Replace step 1 with the real procedure: add an active row to `adminUsers`. Keep
       step 2 about Clerk public metadata — that fallback is real and still works.

    4. **Line 203**, the development-testing example. It sends the bypass value as a URL query
       parameter, which the code does not accept. Replace it with a `curl` using
       `-H "x-dev-admin: <DEV_ADMIN_BYPASS_TOKEN>"` against the same endpoint, and add one sentence
       immediately after stating that this header is honoured only when `NODE_ENV` is the
       development value and is inert in every deployed build. Scan the rest of the file for any
       other query-string bypass mention and give it the same treatment.

    5. **Line 220**, the security warning telling the operator to configure that variable before
       production. Replace it with a warning that matches reality: production admin access requires
       either an active `adminUsers` row or the Clerk metadata role, and neither is granted by
       default.

    6. **Lines 236-241**, the "Before (Development Only)" fenced snippet inside the migration
       section. The section itself is accurate history and should stay, but the snippet's log string
       asserts in the present tense a state that is no longer true, and it is what a text search for
       stale claims finds. Replace the fenced snippet with one prose sentence describing what the
       superseded implementation did — short-circuited the permission check and returned a synthetic
       admin identity — so the history survives without a present-tense claim.

    Leave the "Current Status: full production authentication enabled" framing alone; it is correct.
  </action>
  <acceptance_criteria>
    - `git grep -n "ADMIN_USER_IDS" -- docs/` returns no matches.
    - `docs/admin-authentication.md` names `isUserAdmin`, `adminUsers`, and `lib/models/admin.ts`.
    - `git grep -n -e "?token=" -e "?dev=" -- docs/admin-authentication.md` returns no matches.
    - `docs/admin-authentication.md` contains an `x-dev-admin` header example accompanied by the development-only condition.
    - `git grep -ni -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/admin-authentication.md` returns no matches.
    - The migration-history section still exists; only its code snippet became prose.
  </acceptance_criteria>
  <verify>
    <automated>git grep -q "isUserAdmin" -- docs/admin-authentication.md && git grep -q "adminUsers" -- docs/admin-authentication.md && ! git grep -q "ADMIN_USER_IDS" -- docs/</automated>
    <fails_when>non-zero exit — either the real mechanism is not named in the document, or the non-existent environment-variable allowlist is still described somewhere under docs/</fails_when>
    <automated>git grep -q "x-dev-admin" -- docs/admin-authentication.md && ! git grep -q -e "?token=" -e "?dev=" -- docs/admin-authentication.md</automated>
    <fails_when>non-zero exit — either the header-form bypass example is missing, or a query-string credential example survives in the file</fails_when>
    <automated>git grep -q "Migration from Previous System" -- docs/admin-authentication.md && ! git grep -qi -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/admin-authentication.md</automated>
    <fails_when>non-zero exit — either the history section was deleted rather than corrected, or a present-tense claim that admin auth is switched off survives</fails_when>
  </verify>
  <done>`docs/admin-authentication.md` describes exactly what `lib/auth` and `lib/models/admin.ts` enforce, names no mechanism the code lacks, and every example it shows would actually work.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the deploy runbook and the one stale source comment</name>
  <files>docs/DEPLOYMENT_SETUP.md, components/admin/AdminGuard.tsx</files>
  <read_first>
    - `docs/DEPLOYMENT_SETUP.md` lines 278-292 — the "Step 2: Deploy and Index Content" block: the note above the first `curl`, the unauthenticated `curl`, and the query-parameter `curl` below it.
    - `components/admin/AdminGuard.tsx` lines 20-40 — the header comment block describing the production admin rule.
    - `lib/models/admin.ts` — the real check the comment must describe.
  </read_first>
  <action>
    Two files, one small edit each.

    1. `docs/DEPLOYMENT_SETUP.md`, **lines 285-289**, edited as one block rather than line by line.
       The comment above the first `curl` asserts admin authentication is switched off; the first
       `curl` sends no credential at all and would fail today; the second sends a credential as a URL
       parameter, which the code rejects. Replace all three with a single working example: one
       `curl -X GET` against the vectorize endpoint carrying
       `-H "Authorization: Bearer <ADMIN_VECTORIZE_TOKEN>"`, preceded by a one-line comment saying the
       admin token is a Cloudflare Worker secret. Do not simply delete the block — an operator needs
       a runnable indexing command at this step.

    2. `components/admin/AdminGuard.tsx`, **line 30**. The header comment states that production
       admin access is granted to users with an admin role or listed in an environment-variable
       allowlist. The allowlist half is false — the server-side check is `isUserAdmin` against the
       `adminUsers` table with a Clerk metadata role fallback. Rewrite that one comment line to name
       the real pair. This file is not under `docs/`, but the comment is the same class of drift the
       phase exists to remove, and it is the last place in tracked source that names the phantom
       variable. Change only the comment; the component's runtime behaviour is out of scope.
  </action>
  <acceptance_criteria>
    - `git grep -ni -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/` returns no matches across the whole documentation tree.
    - `git grep -n -e "?token=" -e "?dev=" -- docs/` returns no matches across the whole documentation tree.
    - `git grep -n "ADMIN_USER_IDS" -- docs/ components/ lib/ app/` returns no matches.
    - `docs/DEPLOYMENT_SETUP.md` contains exactly one vectorize `curl` in that block and it carries an `Authorization: Bearer` header.
    - `git grep -n "voltique-admin" -- ':!.planning'` returns no matches.
    - `npm run lint` exits 0 after the `AdminGuard.tsx` comment edit.
  </acceptance_criteria>
  <verify>
    <automated>git grep -q "ADMIN_VECTORIZE_TOKEN" -- docs/ && ! git grep -q "voltique-admin" -- ':!.planning'</automated>
    <fails_when>non-zero exit — either the env-var name is gone from docs (the grep target is wrong or the edits went too far) or the published credential literal survives outside the planning directory</fails_when>
    <automated>git grep -q "Authorization: Bearer" -- docs/DEPLOYMENT_SETUP.md && ! git grep -q -e "?token=" -e "?dev=" -- docs/</automated>
    <fails_when>non-zero exit — either the deploy runbook lost its authenticated example, or a query-string credential example remains anywhere under docs/</fails_when>
    <automated>git grep -q "adminUsers" -- docs/ components/ && ! git grep -q "ADMIN_USER_IDS" -- docs/ components/ lib/ app/</automated>
    <fails_when>non-zero exit — either the real table name is not referenced in docs and components, or the phantom environment variable is still named in tracked application source or docs</fails_when>
    <automated>git grep -q "checkAdminPermissions" -- docs/ && ! git grep -qi -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/</automated>
    <fails_when>non-zero exit — either no document references the real entry point (the docs tree is wrong) or a claim that admin auth is switched off survives anywhere under docs/</fails_when>
    <automated>npm run lint && npm run typecheck</automated>
    <fails_when>non-zero exit from either command, or any "error" line in the eslint or tsc output</fails_when>
  </verify>
  <done>The deploy runbook gives an operator one working authenticated indexing command, no tracked file outside `.planning/` names the published credential or the phantom admin allowlist, and no document claims admin authentication is switched off.</done>
</task>

</tasks>

## Artifacts this phase produces

Owned by this plan (`01-03`): no new files and no new symbols. The changes are content
corrections to four existing tracked files — `docs/CLAUDE.md`, `docs/admin-authentication.md`,
`docs/DEPLOYMENT_SETUP.md`, and `components/admin/AdminGuard.tsx`.

Produced by sibling plans and referenced from these documents after wave 2:
`lib/auth/deployment-guard.ts`, `assertDeploymentPosture()`, `DEPLOYMENT_GUARD_STATUS`,
`DEPLOYMENT_GUARD_MESSAGE`, the `auth.deployment_guard_tripped` telemetry event (all `01-01`),
and the `docs/admin-authentication.md` Deployment Safety section (`01-02`).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → public reader | Every tracked file is readable by anyone with repository access; a credential in a tracked file is a published credential |
| Documentation → operator action | An operator follows a documented example verbatim; a bad example becomes a bad production action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Information Disclosure | The live admin token published at `docs/CLAUDE.md:416` | high | mitigate | Replaced with the named placeholder; verified by a repository-scoped negative search paired with a positive control. Rotation of the exposed value is plan `01-04` |
| T-01-10 | Spoofing | Five documented examples that send a credential in a URL query string | high | mitigate | Every example converted to a header form; the code already rejects query credentials, so the residual risk was operators leaking the value through logs, history, and Referer |
| T-01-11 | Repudiation | Documentation naming an environment-variable admin allowlist the code never reads | medium | mitigate | Replaced with the Clerk role plus `adminUsers` table description an operator can actually act on; the phantom variable is removed from docs, components, lib, and app |
| T-01-12 | Information Disclosure | The development bypass value advertised in a published document | medium | mitigate | Replaced with the named placeholder. The literal deliberately remains in `lib/auth/admin-middleware.ts:22` per the 2026-09-01 decision; it is harmless because the header is honoured only under a development `NODE_ENV`, and plan `01-01`'s guard closes the deployed-development-build case |
| T-01-20 | Repudiation | Documents asserting admin authentication is switched off invite an operator to skip access review | medium | mitigate | Every such claim corrected; matched case-insensitively because these are English prose claims, not identifiers |
| T-01-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages; `01-RESEARCH.md` records the phase's package legitimacy audit as not applicable |
</threat_model>

<verification>
1. `git grep -q "ADMIN_VECTORIZE_TOKEN" -- docs/ && ! git grep -q "voltique-admin" -- ':!.planning'` — exits 0.
2. `git grep -q "Authorization: Bearer" -- docs/ && ! git grep -q -e "?token=" -e "?dev=" -- docs/` — exits 0.
3. `git grep -q "adminUsers" -- docs/ components/ && ! git grep -q "ADMIN_USER_IDS" -- docs/ components/ lib/ app/` — exits 0.
4. `git grep -q "x-dev-admin" -- docs/ && ! git grep -q "mercora-dev-bypass" -- docs/` — exits 0.
5. `git grep -q "checkAdminPermissions" -- docs/ && ! git grep -qi -e "temporarily disabled" -e "currently disabled" -e "disabled for dev" -- docs/` — exits 0.
6. `npm run lint && npm run typecheck && npm test` — all exit 0.
</verification>

<success_criteria>
- No tracked file outside `.planning/` contains the published admin token value.
- No document under `docs/` shows a credential in a URL, claims admin authentication is off, or names an admin allowlist environment variable.
- `docs/admin-authentication.md` matches `lib/auth/admin-middleware.ts`, `lib/auth/unified-auth.ts`, and `lib/models/admin.ts` claim for claim.
- Every corrected example is replaced by a working one; nothing an operator needs was deleted.
</success_criteria>

<output>
Create `.planning/phases/01-security-and-admin-auth-truth/01-03-SUMMARY.md` when done.
</output>
