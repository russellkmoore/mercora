---
phase: 08-documentation-visual-qa-close-out
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - docs/theming.md
  - docs/CLAUDE.md
  - README.md
  - docs/runtime-configuration.md
autonomous: true
requirements: [DOCS-01]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A developer who has never seen this repo can read `docs/theming.md` and add a new theme without re-deriving the mechanism: theme-file anatomy, a five-step duplication recipe, and what the validator rejects are all present (D-01 §2, §3, §4). [DOCS-01]"
    - "Every validator rejection is documented with the exact message the script produces, so a reader who hits one can grep for it and land on the section that explains it (D-01 §4, CONTEXT specifics — quote, don't paraphrase). [DOCS-01]"
    - "The document states how `predev`, `build:worker` and the CI `--check` step each fail on an invalid theme, naming the real script bodies and the real CI step name (D-01 §4)."
    - "The document states plainly that `scan:tokens` is NOT wired into CI today and that only the theme-manifest freshness check is — and records adding it as a recommended follow-up rather than doing it here (D-01 §8, orchestrator decision). [DOCS-01]"
    - "The theme resolution order is documented end to end: D1, then the deploy-time environment default, then the manifest default; no caching; one telemetry signal on a stored name that is not a manifest member, carrying only an outcome and never the stored value (D-01 §5). [DOCS-01]"
    - "The three layout switches are documented with their real setting keys and their real enum members, and the document says the same appearance category holds all four keys (D-01 §7). [DOCS-01]"
    - "The known-limits section carries every close-out item D-08 names, each with a pointer to where it is tracked (D-01 §9, D-08). [DOCS-01]"
    - "`docs/CLAUDE.md` no longer claims a hardcoded dark background or a dark-by-default look, its structure tree names the theme and layout directories and the three scripts, its build-commands section names both gates, and it links the new document (D-02). [DOCS-01]"
    - "Every relative file path the theming document mentions resolves to a file that exists (orchestrator decision — link check). [DOCS-01]"
    - "All quoted validator text, command strings and setting keys are byte-identical between the document and the source file they came from, proved by a two-sided grep rather than by reading (orchestrator decision — claim check). [DOCS-01]"
    - "Quoted error strings and command invocations live inside fenced code blocks, which scroll horizontally in a standard renderer instead of wrapping or clipping (08-UI-SPEC.md `## UI Considerations`, long-text and code-block-scroll rows)."
    - "The `docs/CLAUDE.md` edits are additions and replacements inside a document already proven to render at its current length; nothing changes the document's overall shape (08-UI-SPEC.md `## UI Considerations`, long-text row for CLAUDE.md edits)."
    - "The document names no secret value and no development bypass header value anywhere in the eight sections added here (security contribution)."
  artifacts:
    - path: "docs/theming.md"
      provides: "Complete theming how-to: sections 2 through 9 appended below the token contract table"
      min_lines: 250
    - path: "docs/CLAUDE.md"
      provides: "Corrected styling claims, theme/layout entries in the structure tree, both gates named, and a link to the theming document"
      contains: "docs/theming.md"
    - path: "README.md"
      provides: "A docs-index bullet pointing at the theming document"
      contains: "docs/theming.md"
    - path: "docs/runtime-configuration.md"
      provides: "The theme row now lists the deploy-time theme default variable alongside the logo path"
      contains: "NEXT_PUBLIC_THEME_DEFAULT"
  key_links:
    - from: "docs/theming.md"
      to: "scripts/build-themes.mjs"
      via: "every documented rejection quotes that script's own message text"
      pattern: "missing required token"
    - from: "docs/theming.md"
      to: "lib/layout/variants.ts"
      via: "the documented switch members are that module's enum arrays"
      pattern: "full-bleed"
    - from: "docs/CLAUDE.md"
      to: "docs/theming.md"
      via: "a link in the Important Files to Reference list"
      pattern: "docs/theming.md"
    - from: "README.md"
      to: "docs/theming.md"
      via: "a bullet in the Technical Documentation docs index"
      pattern: "docs/theming.md"
  prohibitions:
    - statement: "This phase must NOT change any theme file, any layout variant component, any contract token, or the theme/layout resolution mechanism."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition from CONTEXT's phase boundary; binds hardest here, because writing the five-step recipe is the moment a writer is most tempted to 'improve' the thing being described."
    - statement: "Documentation must NOT print a secret value or the development bypass header value."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less security prohibition from the phase's security contribution; binds here because the gates-and-harness section describes the local capture method."
    - statement: "The token scanner must NOT be wired into CI in this phase."
      status: unresolved
      verification: judgment
      reason: "Planner decision recorded against the orchestrator's open option: the document is required to state the scanner is not in CI, so changing CI in the same phase would falsify the document it ships with, and CI is outside the phase boundary. Recorded as a recommended follow-up instead."
    - statement: "This phase must NOT run a full codebase remap."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from CONTEXT Deferred Ideas; binds here because the structure-tree edit to docs/CLAUDE.md is a targeted splice, not a regenerated tree."
---

<objective>
Finish `docs/theming.md` — sections 2 through 9 — and make `docs/CLAUDE.md` tell the truth about
how this storefront is styled. Then prove both, string by string, against the source files they
describe.

Purpose: DOCS-01 in full. Plan 08-01 proved the claim-check method on one table; this plan applies
it to the whole document and to the four repository documents that point at it.

Output: a complete theming how-to, five targeted edits to `docs/CLAUDE.md`, a README docs-index
bullet, one row added to the runtime-configuration reference, and a claim/link check that fails
loudly if any quoted string drifts from its source.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-documentation-visual-qa-close-out/08-CONTEXT.md
@.planning/phases/08-documentation-visual-qa-close-out/08-PATTERNS.md
@.planning/phases/08-documentation-visual-qa-close-out/08-UI-SPEC.md
@.planning/phases/08-documentation-visual-qa-close-out/08-01-SUMMARY.md
</context>

<interfaces>
Consumed from plan 08-01, unchanged: `docs/theming.md` with its H1, status line, section 0 (admin
switching) and section 1 (the 23-token contract table). This plan appends sections 2 through 9
below section 1 and does not rewrite anything above it.

Left for later plans: plan 08-04 appends the visual-QA result table as a subsection under section 8
once `08-QA-MATRIX.md` is complete; plan 08-05 appends any unresolved QA finding to section 9 and
verifies the close-out list. Neither rewrites what this plan writes. Do not leave a placeholder
heading for either — an absent subsection is correct until its data exists.

Nothing in this plan exports code or changes a runtime contract.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use `git commit -F`;
  redirect with `>|` when a target file may already exist.
- Every task is committed atomically before the next one starts.
- **Verified this session, use these numbers:** four shipped presets are light (`luxe`, `clinical`,
  `atelier`, `market`) and three are dark (`volt-dark`, `midnight`, `retro`), read from each theme's
  own surface token. RESEARCH.md's stale-claims table contradicts itself on this; trust the files.
- **Verified this session:** `.github/workflows/ci.yml` runs `npm run build:themes:check` as a step
  named "Check theme manifest freshness", between "Check migration safety" and "Lint". It does not
  run `npm run scan:tokens` anywhere. Confirm both yourself before writing the sentence.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Theme-file anatomy, the five-step duplication recipe, and what the validator rejects</name>
  <read_first>
    - `scripts/build-themes.mjs` — the whole file. You need the header-parsing function, the name pattern it enforces, the required-token list, the at-rule and single-rule checks, the hex check, the error-report format and the ABORT line, and the `--check` mode. Every message you quote comes from here, character for character.
    - `themes/volt-dark.css` lines 1-14 and `themes/luxe.css` lines 1-14 — two real headers, one dark and one light, plus the opening of each `[data-theme]` block.
    - Every `themes/*.css` file's own `@theme` line — read each one and copy it, rather than trusting any planning document's paraphrase of the six non-`volt-dark` headers. RESEARCH.md flags its own table as paraphrased for six of the seven.
    - `app/layout.tsx` — the font-loading block and the line that applies the font variable classes, including its inline comment explaining why the classes go on both the root element and the body.
    - `package.json` scripts section — the exact bodies of `predev`, `build:worker`, `build:themes` and `build:themes:check`.
    - `.github/workflows/ci.yml` — the theme-manifest step and its name, and the fact that the token scanner is absent from the file.
    - `docs/database-migrations.md` — the one-section-per-workflow-step, prose-then-fenced-command-block shape to follow.
  </read_first>
  <files>docs/theming.md</files>
  <action>
Append three sections below the token contract table.

The anatomy section describes what a theme file is: a comment header, one metadata line, and exactly
one selector block. Show the shape of the metadata line and name its three fields, saying which is
required and which two only feed the admin card. Say the selector must equal the filename stem. Say
the colour tokens are six-digit hex and nothing else, and that the radius and font tokens are not
hex-checked. Explain how a display font actually reaches a heading: a font token names a CSS variable
that the font loader in the root layout defines, and the variable classes are applied to the root
element as well as the body because a custom property whose value nests another variable reference
resolves where it is declared, not where it is consumed. That detail is load-bearing — a real bug was
found and fixed on it — so state it as a rule, not a footnote. Say plainly that a theme file is data,
not a stylesheet fragment: no imports, no font-face rules, no second selector.

The five-step section is a recipe, numbered, each step short and imperative, with the commands in
fenced blocks. Copy a theme file to a new name that satisfies the validator's name pattern; change
the metadata line; change the selector to match the new stem; set all 23 tokens; run the theme build
and the token scanner. Say what happens automatically after that — the new theme appears in the
admin's card grid with no other file edited — and say what does not: a new theme needs a deploy,
switching between shipped themes does not. Add a short callout warning that five of the seven shipped
presets carry a deliberate contrast correction away from the values in the design direction document,
because a direct transcription failed a contrast threshold; a converted preset should be re-checked
against its own surface rather than assumed to pass. Name the one preset that needed no correction.

The rejection section is a table with one row per check the validator performs: what it checks, and
the message it prints. Quote each message from the script character for character, in a code span or
a fenced block, including the interpolation placeholders where the script has them — a reader must be
able to copy a message out of a failing run and find it here. Then show the report format: the file
and line prefix on each error, the abort line with the error count, and the non-zero exit. State that
errors are collected rather than reported one at a time. Close with a short paragraph naming exactly
how each entry point fails: the pre-dev hook, the worker build, and the CI step, giving the real
script bodies and the real CI step name. Do not invent a step name.

Every one of these sections stays inside the document's existing second-person, imperative voice.
  </action>
  <acceptance_criteria>
    - Each of the validator's rejection messages appears in the document byte-identically to the script.
    - The document names the real CI step and the real script bodies for the pre-dev hook and the worker build.
    - The five-step recipe is numbered 1 through 5 and each step names a concrete file or command.
    - Every `@theme` metadata line quoted is copied from the theme file it belongs to, not paraphrased.
    - No quoted message is reworded, retyped from memory, or stripped of its interpolation placeholders.
    - No new heading is added for a section this task does not fill.
  </acceptance_criteria>
  <verify>
    <automated>set -- 'theme file is empty' 'unparseable CSS' 'a theme file may not use at-rules' 'no rule found; expected exactly one' 'a theme file may contain exactly one rule' 'must be lowercase alphanumerics and hyphens only' '(the filename stem)' 'non-token declaration' 'must be a 6-digit hex colour, got' 'missing required token' 'is not part of the frozen 23-token contract' 'missing required @theme header label' 'no theme files found in' 'stale generated file(s)'; RC=0; for s in "$@"; do /usr/bin/grep -qF "$s" docs/theming.md || { echo "DOC MISSING: $s"; RC=1; }; /usr/bin/grep -qF "$s" scripts/build-themes.mjs || { echo "SOURCE DRIFT: $s"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>the loop prints "DOC MISSING:" for any message, meaning a rejection was paraphrased or omitted; or it prints "SOURCE DRIFT:", meaning the literal was mistyped in this plan or the script changed and the doc is now quoting a message that no longer exists.</fails_when>
    <automated>set -- 'build:themes:check' 'Check theme manifest freshness' 'node scripts/build-themes.mjs && node scripts/db-local-ensure.mjs'; RC=0; for s in "$@"; do /usr/bin/grep -qF "$s" docs/theming.md || { echo "DOC MISSING: $s"; RC=1; }; done; /usr/bin/grep -qF 'Check theme manifest freshness' .github/workflows/ci.yml || { echo "CI STEP NAME DRIFT"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"DOC MISSING:" names a command or step the document failed to quote; or "CI STEP NAME DRIFT" prints, meaning the workflow step this document names no longer exists under that name.</fails_when>
  </verify>
  <done>A developer can duplicate a theme in five steps and, when the build rejects it, find the exact message in the document that explains why.</done>
</task>

<task type="auto">
  <name>Task 2: Resolution order, the admin surface, the layout switches, the gates, and known limits</name>
  <read_first>
    - `lib/themes/active-theme.ts` — the whole file: the header comment on why nothing is cached, the two exported setting constants, the fallback chain, the telemetry call and its payload, and the catch that degrades to the default.
    - `lib/layout/settings.ts` — the whole file: the setting-key object, the per-switch resolution against each enum array, the telemetry call, and the header comment explaining why this read is deliberately not shared with the theme resolver.
    - `lib/layout/variants.ts` — the three enum arrays and the defaults object, verbatim.
    - `components/admin/ThemePresetGrid.tsx` and `components/admin/LayoutSwitches.tsx` — skim each for what the surface actually shows: the card contents, the three radiogroups, the separate Save buttons. Do not document behaviour you have not seen in the file.
    - `scripts/scan-hardcoded-colors.mjs` lines 1-120 — the exclusion rules, the named manual-review registry, and the sentinel comment pair, so the gates section describes the scanner accurately.
    - `scripts/screenshot-routes.mjs` lines 1-60 — the flag list, and the manifest flag's default value, which points at Phase 5's file.
    - `.planning/WINDOWS.md` and `.planning/todos/pending/theme-direction-doc-backlog-06.1.md` — the open items and the deferred direction-document properties that section 9 must list.
    - `.planning/STATE.md` Blockers/Concerns — the two carry-overs about the image-URL resolvers and the self-writing parity snapshots.
    - `docs/runtime-configuration.md` lines 25-35 — the existing paragraph about storefront colours; agree with it, do not restate it differently.
  </read_first>
  <files>docs/theming.md</files>
  <action>
Append five more sections.

Resolution: describe the chain for the theme in order — the database read of the appearance category
and the theme key, then the deploy-time environment default if it is itself a manifest name, then the
manifest default. Say what each non-match does: an absent or empty stored value falls through
silently because that is the normal first-load state; a present value that is not a manifest name
emits exactly one telemetry signal carrying only an outcome, never the stored string, and falls
through; a database read failure degrades to the default rather than taking down every route. Say
there is no cache anywhere in the path, and say why, using the reasoning in the module's own header:
a worker isolate can be reused across requests, so caching here risks serving a stale look after an
admin save. Then say the layout resolver has the same shape, reads the same category, and is
deliberately a second independent read, quoting its own stated reason.

Admin surface: name the route, say what the theme grid shows per card and what the layout section
shows, and say each has its own Save. Say the admin keeps its own fixed palette and is deliberately
excluded from the token scanner by path. Keep this short — section 0 already covered the how-to.

Layout switches: name the module that owns the enums, give the three setting keys verbatim, and give
every member of each of the three enums verbatim along with the default. Say all four appearance keys
— the theme and the three switches — live in the same settings category, which is why one save can
change a whole look. Name the variant components each member renders, since a reader adding a variant
needs to know where they live.

Gates and the harness: two gates, each with its exact command in a fenced block. For the theme
validator, say it runs in CI and name the step. For the token scanner, state plainly that it is not
wired into CI at all today — it is a local gate every phase has run by convention — and record wiring
it into CI as a recommended follow-up rather than doing it. Describe the scanner's shape briefly: what
it excludes, that named files are excluded outright with a printed reason, and that a polarity-neutral
literal gets an explicit sentinel pair with a written reason rather than a blanket exception. Then
describe the screenshot harness: its command, its required label flag, and — as a warning, because it
has already caused one real cross-phase mistake — that its manifest flag silently defaults to an
earlier phase's file and must always be passed explicitly.

Known limits and backlog: a table, one row per open item, each with what it is, where it is tracked,
and what closing it would take. Include, at minimum: the deploy-time theme default still needing to be
added as a Cloudflare Workers Build variable; the admin Appearance page never having been walked
through in a real browser with a real session; the design-direction properties the seven presets
deliberately do not carry, pointing at both backlog notes; the two image-URL resolvers that should be
consolidated when the product display is next touched; the parity tests that will silently regenerate
a deleted baseline snapshot; and the token scanner not running in CI. Every row points at a real,
existing file — the link check will verify that.

Do not write a local switching recipe that includes a credential anywhere in these sections.
  </action>
  <acceptance_criteria>
    - Both telemetry signal names appear in the document, and the document states the payload carries only an outcome.
    - All three layout setting keys and all eight enum members appear verbatim.
    - Both gate commands appear in fenced code blocks.
    - The document states the token scanner does not run in CI, and does not claim otherwise anywhere.
    - The known-limits table has a row for each of the six items named in the action.
    - Every relative path in the known-limits table resolves to an existing file.
    - No development bypass value appears anywhere in the file.
  </acceptance_criteria>
  <verify>
    <automated>set -- 'appearance.theme' 'appearance.category_layout' 'appearance.home_hero' 'appearance.product_gallery' 'grid-3' 'grid-2' 'full-bleed' 'minimal' 'theme.unknown_selection' 'layout.unknown_selection' 'NEXT_PUBLIC_THEME_DEFAULT' 'DEFAULT_THEME_NAME' 'npm run scan:tokens' 'node scripts/build-themes.mjs --check' 'screenshot:routes' '--manifest'; RC=0; for s in "$@"; do /usr/bin/grep -qF "$s" docs/theming.md || { echo "DOC MISSING: $s"; RC=1; }; done; test "$RC" -eq 0 && /usr/bin/grep -qF 'appearance.category_layout' lib/layout/settings.ts && /usr/bin/grep -qF 'theme.unknown_selection' lib/themes/active-theme.ts</automated>
    <fails_when>"DOC MISSING:" prints for any key, enum member, telemetry name or command, meaning the section skipped it or paraphrased it; or one of the two trailing source greps fails, meaning the key or telemetry name this document quotes no longer exists in the module it came from.</fails_when>
    <automated>BYPASS="$(/usr/bin/sed -n 's/.*devToken === "\([^"]*\)".*/\1/p' lib/auth/admin-middleware.ts | /usr/bin/head -1)" && test -n "$BYPASS" && ! /usr/bin/grep -qF "$BYPASS" docs/theming.md && test -f .planning/todos/pending/theme-direction-doc-backlog-06.1.md && test -f .planning/WINDOWS.md</automated>
    <fails_when>`test -n` fails, meaning the extraction returned nothing and the credential check would have passed vacuously; or the grep succeeds, meaning the development credential was written into the document; or a `test -f` fails, meaning the known-limits table points at a backlog file that does not exist.</fails_when>
  </verify>
  <done>The theming document is complete through section 9: a reader knows how a look is chosen at request time, where to change it, what the two gates do, and every limit this milestone is knowingly shipping with.</done>
</task>

<task type="auto">
  <name>Task 3: Correct docs/CLAUDE.md, link the new document from README and the runtime reference, then claim-check and link-check everything</name>
  <read_first>
    - `docs/CLAUDE.md` lines 19-30 (the Frontend list), lines 49-73 (Build Commands and the deploy-paths note), lines 74-140 (the Project Structure tree, for its exact glyph and comment-alignment style), lines 385-395 (the Code Style list), and lines 597-610 (Important Files to Reference). Read the tree carefully before splicing: match its existing box-drawing glyphs and trailing-comment alignment exactly.
    - `README.md` lines 116-136 — the docs index, its group headings, and the exact emoji-bold-link-dash-description bullet shape.
    - `docs/runtime-configuration.md` lines 1-35 — the purpose/variable table and its Theme row.
    - `wrangler.jsonc` — the deploy-time theme default declaration and its own comment about needing a Workers Build variable.
    - `.planning/phases/08-documentation-visual-qa-close-out/08-PATTERNS.md` — the `docs/CLAUDE.md` targeted-edits section, which lists all five edit points.
  </read_first>
  <files>docs/CLAUDE.md, README.md, docs/runtime-configuration.md</files>
  <action>
Five targeted edits to the development-context document. Do not rewrite the file and do not reorder
anything.

First, the Styling bullet in the Frontend list. Replace it with one that describes the real model: a
23-token CSS custom-property contract under a prefixed namespace, one of seven preset files selected
by a data attribute on the root element, resolved server-side, with a pointer to the new theming
document. Do not put a hex value in the replacement — this bullet describes a mechanism, not a colour.

Second, the Styling bullet in the Code Style list. Replace it with one that says the same thing in one
line and gives the light/dark split by count, using the verified numbers in the executor notes. Do not
reuse the phrasing of the bullet you are replacing.

Third, the Project Structure tree. Splice in the theme directory with a note that the generated
barrel is generated, the themes library directory with its three modules and which are generated, the
layout library directory with its two modules, the layout components directory, the two admin
appearance components under the existing admin components entry, and the three theme/QA scripts under
the existing scripts entry. Match the tree's existing glyphs and comment alignment; do not introduce a
different tree-drawing convention and do not regenerate the tree.

Fourth, the Build Commands block. Add both gate commands with short trailing comments, and add one
sentence below the block saying which one runs in CI and that the other does not.

Fifth, the Important Files to Reference list. Add a bullet pointing at the theming document with a
one-line description, in the same shape as the existing bullets.

Then add one bullet to the README docs index, in the Technical Documentation group, matching the
existing emoji-bold-link-dash-description shape exactly.

Then add the deploy-time theme default variable to the Theme row of the runtime-configuration table.
That file's existing paragraph about storefront colours is already accurate — leave it alone.

Finally, run the full claim check and link check as one pass and record the result. The claim check
re-runs the two-sided greps from tasks 1 and 2 over the finished document. The link check extracts
every backtick-quoted relative path from the theming document and asserts each one exists on disk. If
a path does not exist, fix the document, not the check — a documentation link that does not resolve is
the defect the check is for. Record the counts in the summary: how many literals were cross-checked
and how many paths were link-checked.
  </action>
  <acceptance_criteria>
    - The Frontend Styling bullet no longer contains a hex colour value, and the Code Style Styling bullet no longer claims a default look.
    - The structure tree contains the theme directory, the two library directories, the layout components directory, both admin appearance components, and all three scripts.
    - The Build Commands block names both gate commands, and a sentence below it says which runs in CI.
    - The Important Files list and the README docs index each link the theming document.
    - The runtime-configuration Theme row names the deploy-time theme default variable.
    - Every backtick-quoted relative path in the theming document resolves to a file that exists.
    - The full two-sided claim check from tasks 1 and 2 passes against the finished document.
  </acceptance_criteria>
  <verify>
    <automated>! /usr/bin/grep -q '#000000' docs/CLAUDE.md && ! /usr/bin/grep -q 'dark theme by default' docs/CLAUDE.md && set -- 'themes/' 'lib/themes/' 'lib/layout/' 'components/layout/' 'ThemePresetGrid.tsx' 'LayoutSwitches.tsx' 'build-themes.mjs' 'scan-hardcoded-colors.mjs' 'screenshot-routes.mjs' 'scan:tokens' 'build:themes:check' 'docs/theming.md'; RC=0; for s in "$@"; do /usr/bin/grep -qF "$s" docs/CLAUDE.md || { echo "CLAUDE.md MISSING: $s"; RC=1; }; done; test "$RC" -eq 0 && /usr/bin/grep -qF 'docs/theming.md' README.md && /usr/bin/grep -qF 'NEXT_PUBLIC_THEME_DEFAULT' docs/runtime-configuration.md</automated>
    <fails_when>either negative grep succeeds, meaning a stale styling claim survived the edit; or "CLAUDE.md MISSING:" prints, meaning a required path, component, script or gate never made it into the structure tree or the commands block; or one of the two trailing greps fails, meaning the README bullet or the runtime-configuration row was not added.</fails_when>
    <automated>RC=0; N=0; for p in $(/usr/bin/grep -oE '`[A-Za-z0-9_.-]+/[A-Za-z0-9_./-]+\.(md|ts|tsx|mjs|css|json|jsonc|yml)`' docs/theming.md | /usr/bin/tr -d '`' | /usr/bin/sort -u); do N=$((N+1)); test -e "$p" || { echo "BROKEN LINK: $p"; RC=1; }; done; echo "link-checked $N paths"; test "$RC" -eq 0 && test "$N" -ge 10</automated>
    <fails_when>"BROKEN LINK:" prints, meaning the document points at a file that does not exist; or the reported count is under 10, meaning the extraction matched almost nothing and the check would have passed vacuously against a document that cites no paths at all.</fails_when>
  </verify>
  <done>The development-context document, the README index and the runtime reference all point at an accurate theming document, and every quoted string and every cited path in it is machine-verified against the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published documentation | `docs/theming.md`, `docs/CLAUDE.md` and `README.md` are committed and readable by anyone with repo access |
| documentation → reader action | A wrong instruction in the theming document causes a reader to change production configuration incorrectly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Information Disclosure | `docs/theming.md` | medium | mitigate | The gates-and-harness and admin sections describe the local switching method by mechanism only; tasks 2 and 3 extract the bypass literal from the middleware at run time and assert it is absent from the document |
| T-08-06 | Tampering | reader following a documented recipe | medium | mitigate | Every quoted command, key, enum member and error message is asserted byte-identical against its source file by a two-sided grep, so a recipe cannot silently drift from what the code does |
| T-08-07 | Spoofing | broken or misdirected documentation links | low | mitigate | The link check asserts every relative path the document cites exists, so a reader is never sent to a file that is not there |
| T-08-08 | Repudiation | undocumented gate coverage | low | mitigate | The document states explicitly which gate runs in CI and which does not, so nobody can later claim the token scanner was believed to be enforced automatically |
| T-08-SC | Tampering | npm installs | low | accept | No plan in this phase runs a package-manager install; RESEARCH.md's Package Legitimacy Audit records zero packages installed, upgraded, or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `docs/theming.md` | 08-01, extended by 08-02, 08-04, 08-05 | sections 2 through 9 |
| `docs/CLAUDE.md` edits | 08-02 | all five |
| `README.md` docs-index bullet | 08-02 | the whole bullet |
| `docs/runtime-configuration.md` theme row | 08-02 | the added variable |
| `app/api/admin/settings/route.ts` fix and its test | 08-01 | — |
| `08-QA-MATRIX.md` | 08-03, judged in 08-04 | — |
| `.planning/codebase/{ARCHITECTURE,STRUCTURE,CONVENTIONS,TESTING}.md` edits | 08-05 | — |
| Close-out record in `docs/theming.md` and `STATE.md` | 08-05 | — |

<verification>
- Every validator message, setting key, enum member, telemetry name and gate command in `docs/theming.md` is byte-identical to its source file, proved by two-sided grep.
- Every relative path cited in `docs/theming.md` exists on disk.
- `docs/CLAUDE.md` carries no stale styling claim, names both gates, lists the theme and layout paths, and links the theming document.
- `README.md` and `docs/runtime-configuration.md` carry their one-line additions.
- No development bypass value appears in any file this plan touches.
</verification>

<success_criteria>
- A developer with no prior context can add a theme from `docs/theming.md` alone.
- Nothing in the theming document or the development-context document contradicts the code, and a
  machine check says so rather than a reviewer's reading.
- The token scanner's absence from CI is documented as a fact and recorded as a follow-up, not
  quietly fixed inside a documentation phase.
</success_criteria>

<output>
Create `.planning/phases/08-documentation-visual-qa-close-out/08-02-SUMMARY.md` when done.
</output>
