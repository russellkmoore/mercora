---
phase: 08.1-v2-tech-debt-closure
verified: 2026-09-05T15:20:00Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification: null
---

# Phase 08.1: v2 Tech-Debt Closure Verification Report

**Phase Goal:** Close the milestone's recorded tech debt that code can close: transactional emails and the global-error page follow the admin-selected theme; `scan:tokens` runs in CI; the appearance settings are read once per request; the duplicated image resolver, silent seed catch, self-writing parity snapshots and review Info items are fixed; order-status gets a seeded local order so its screenshots exist.
**Verified:** 2026-09-05
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1a | With a non-default theme saved in D1, every transactional email builder renders that theme's tokens, proven by a test stubbing the active theme | ✓ VERIFIED | `tests/unit/lib/utils/order-status-email-tokens.test.ts` describe block `"a stored non-default theme reaches the rendered email HTML (D-01)"` stubs `getSettings` → `luxe`, asserts `luxeTokens.surfaceInverse`/`onInverse`/`mutedOnInverse` appear in rendered HTML. Ran this exact test: 1 passed. All six builders (`lib/utils/email.ts`, `lib/fulfillment/shipping-email.ts`, `lib/payments/refund-email.ts`, `lib/subscriptions/lifecycle-email.ts`, `lib/utils/review-notifications.ts`, `lib/email/footer.ts`) confirmed to take `tokens` as a parameter with no internal `getThemeTokens()` no-arg call; async senders resolve via `lib/email/theme.ts`'s `resolveEmailTheme()`. |
| SC1b | `app/global-error.tsx` renders the active theme's colours after mount via a public `GET /api/theme` route, with volt-dark as the pre-fetch fallback | ✓ VERIFIED | `app/global-error.tsx` reads: `useState(() => getThemeTokens())` (no-arg = manifest default = `volt-dark`, confirmed `DEFAULT_THEME_NAME = "volt-dark"` in `lib/themes/manifest.generated.ts`), then `useEffect` fetches `/api/theme`, parses via `parseThemeResponse`, and calls `setTokens` only on success — every failure path silently keeps the default. `app/api/theme/route.ts` is public (no auth guard), `force-dynamic`, returns `{name, tokens}` from `getActiveTheme()`/`getThemeTokens()` with `Cache-Control: no-store`. Ran `tests/unit/app/global-error.test.ts` + `tests/unit/app/api/theme-route.test.ts`: 18/18 passed. |
| SC2a | `.github/workflows/ci.yml` runs `npm run scan:tokens` and `docs/theming.md` says so | ✓ VERIFIED | CI step "Scan for raw token usage" running `npm run scan:tokens` present, positioned immediately after "Check theme manifest freshness". `docs/theming.md:297` states the scan runs in CI as this named step. Ran `npm run scan:tokens` live: `0 violations`. |
| SC2b | `getActiveTheme()` and `getLayoutSettings()` share one request-scoped read of the `appearance` category (`React.cache`), with no module/isolate cache | ✓ VERIFIED | Both `lib/themes/active-theme.ts` and `lib/layout/settings.ts` call `readAppearanceSettings()` from `lib/themes/appearance-read.ts`, which wraps a single `getSettings(APPEARANCE_SETTINGS_CATEGORY)` call in React's `cache()`. No top-level mutable binding, no `globalThis`, no `unstable_cache` in the module (verified by reading the file and by `tests/unit/app/layout-switch-contract.test.ts`'s guard describe block, which explicitly names `appearance-read.ts` as the one granted, explained exception rather than silently excluding it from the forbidden-pattern scan). |
| SC3a | One image-URL resolver serves both the product gallery variants and cards | ✓ VERIFIED | `components/layout/product/gallery-media-url.ts` confirmed deleted from the tree. `ProductGalleryLeft.tsx` and `ProductGalleryTop.tsx` both call `resolveProductImageSrc` from `lib/utils/product-image.ts`; `components/ProductCard.tsx` and `components/agent/ProductCard.tsx` already used the same resolver pre-phase — so one resolver now serves both gallery variants and cards. |
| SC3b | Parity tests fail loudly when a baseline snapshot is missing | ✓ VERIFIED | `tests/unit/helpers/parity-snapshot.ts`'s `readParitySnapshot()` throws (naming the path and the `UPDATE_SNAPSHOTS` flag) when the file is absent and `UPDATE_SNAPSHOTS=1` isn't set, and never writes in that case. All three suites (`category-grid3-parity`, `home-hero-variants`, `product-gallery-variants`) import and use it; no committed `__snapshots__` file was touched (confirmed via git diff scope on the phase commit range). |
| SC3c | The settings seed fallback logs instead of swallowing | ✓ VERIFIED | `app/api/admin/settings/route.ts`'s seed-insert catch arm: `console.error('Seed insert failed (may be a benign concurrent race):', err)`. The unused `isSuperAdminActor` mock removed from `tests/unit/app/api/admin-settings-empty-category.test.ts` (grep confirms zero occurrences). |
| SC3d | The Phase 6/7/8 review Info items are closed | ✓ VERIFIED | Cross-referenced against source reviews: 06-REVIEW.md IN-01 (duplicate-token validator — closed by plan 06, fixture `tests/fixtures/themes/duplicate-token/dupe.css` + `scripts/build-themes.mjs` per-token Map count), IN-02 (`kebabToCamel` reimplementation — closed, `grep -rl "function kebabToCamel"` returns exactly `scripts/build-themes.mjs`, contract test imports it), IN-03 (settings tab bar margin — closed by recorded resolution, no code change needed per the review's own text), IN-04 (active-theme unknown-value branches — closed, `grep -c "recordTelemetry("` in `active-theme.ts` == 1). 07-REVIEW.md IN-01 (`productGallery` optional prop — closed by plan 05, prop is now required with no default, compiler-enforced). 08-REVIEW.md IN-01 (unused mock — closed, same fix as above) and IN-02 (silent catch — closed, same as SC3c). |
| SC4a | A local D1 order fixture exists and the order-status route is captured across the seven-theme grid | ✓ VERIFIED | `data/d1/seed-dev.sql` contains fixed id `dev-order-001` with `ON CONFLICT(id) DO NOTHING`. `tests/unit/scripts/dev-seed-guard.test.ts` (3 cases) proves the fixture is absent from `data/d1/seed.sql` and every file under `migrations/`, and is referenced by exactly one `--local`-gated script. `08.1-SCREENSHOTS.md` contains 28 `order-status` rows (4 cells × 7 presets: volt-dark, luxe, midnight, clinical, retro, atelier, market). |
| SC4b | Gates stay green (scan 0, build-themes --check, full suite, build) | ✓ VERIFIED | Re-ran independently: `npm run scan:tokens` → 0 violations; `node scripts/build-themes.mjs --check` → fresh, 7 themes; `npm run typecheck` → clean; `npm test` → 265 files / 2187 tests passed; `npm run test:workers` → 27 files / 153 tests passed; `npm run lint` → 0 errors (52 pre-existing warnings, unrelated files). Git tree clean of tracked changes after the run. |
| D-10a | Staged effect carries `themeName`; drain renders from it | ✓ VERIFIED | `lib/services/order-effects.ts`: `stagePaidOrderEffects` resolves `themeName = options.themeName ?? await getActiveTheme()` and serializes `{themeName}` into the new `payload` column only for `confirmation_email`/`merchant_notification` rows. `executeEffect`'s two email branches call `emailThemeForStagedPayload(effect.payload)` and pass the result into the senders — never re-resolving `getActiveTheme()` at drain time. |
| D-10b | Migration `0023` is expand-only; no `getActiveTheme()` reachable from `scheduled()` | ✓ VERIFIED | `migrations/0023_add_order_effects_payload.sql` contains exactly one statement: `ALTER TABLE order_effects ADD COLUMN payload TEXT;` — nullable, no default, no backfill. `grep -rn "getActiveTheme\|getCloudflareContext" lib/observability/scheduled.ts worker.ts` returns no matches — the scheduled handler and worker entry point never touch the theme resolver or a request-context helper. |
| D-11 | Single accepted output change, product route hash-identical | ✓ VERIFIED | `resolveProductImageSrc` in `lib/utils/product-image.ts` (unmodified per the prohibition) only prepends `/` to a bare relative path (`!/^https?:\/\//i.test(url) && !url.startsWith('/')` → `/${url}`); absolute URLs and rooted paths pass through unchanged. `08.1-SCREENSHOTS.md`'s "D-11 comparison outcome" section records this run's volt-dark `product__1280__resting.png` hash as byte-identical to Phase 8's recorded capture of the same cell. |

**Score:** 12/12 truths verified (0 present, behavior-unverified)

### Prohibitions

| # | Prohibition | Status | Evidence |
|---|-------------|--------|----------|
| 1 | No secrets/dev-bypass literal in docs or records | ✓ VERIFIED | Grepped `08.1-SCREENSHOTS.md` and all `08.1-*.md` plan/summary files for `ORDER_STATUS_SECRET`/token-shaped strings — only variable *names* appear, never values. No 40+ char token-shaped string found in `08.1-SCREENSHOTS.md` besides the expected 64-char content hashes. `.env.local` confirmed untracked (`git status --porcelain .env.local` empty). |
| 2 | `/api/theme` returns manifest data only with no-store | ✓ VERIFIED | Route body is exactly `{name, tokens}` built from `getActiveTheme()`/`getThemeTokens()` (manifest data), `Cache-Control: no-store` header set, `dynamic = "force-dynamic"` prevents build-time baking. |
| 3 | No theme/variant/contract change | ✓ VERIFIED | `git diff afd9a06~1..HEAD -- lib/themes/manifest.generated.ts lib/themes/manifest.barrel.css themes/` (the phase's full commit range) produces zero output — these files are byte-identical to before the phase. |
| 4 | Dev fixture never in production seeds | ✓ VERIFIED | Same as SC4a — `dev-seed-guard.test.ts` proves `dev-order-001` is absent from `data/d1/seed.sql` and every migration file. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/themes/appearance-read.ts` | shared request-scoped reader | ✓ VERIFIED | Substantive, `React.cache`-wrapped, docstring explains request-scope, imported by both resolvers |
| `lib/themes/theme-response.ts` | manifest-validated response parser | ✓ VERIFIED | `parseThemeResponse` validates name against `THEME_MANIFEST`, never trusts wire token values |
| `app/api/theme/route.ts` | public theme route | ✓ VERIFIED | force-dynamic, no-store, `{name, tokens}` only |
| `lib/email/theme.ts` | email theme resolver + staged-payload trust boundary | ✓ VERIFIED | `resolveEmailTheme()` and `emailThemeForStagedPayload()` both present, substantive |
| `migrations/0023_add_order_effects_payload.sql` | expand-only migration | ✓ VERIFIED | One ALTER statement, nullable column, no default |
| `tests/unit/helpers/parity-snapshot.ts` | loud-on-missing baseline reader | ✓ VERIFIED | Throws naming path+flag on missing file with no opt-in; used by all 3 parity suites |
| `tests/fixtures/themes/duplicate-token/dupe.css` | duplicate-token fixture | ✓ VERIFIED | Declares `--store-primary` three times, used by `build-themes.test.ts` |
| `tests/unit/scripts/dev-seed-guard.test.ts` | dev fixture isolation guard | ✓ VERIFIED | 3 cases, all pass |
| `.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md` | order-status capture manifest | ✓ VERIFIED | 28 order-status rows across 7 presets, D-11 hash comparison recorded |
| `components/layout/product/gallery-media-url.ts` | retired (should NOT exist) | ✓ VERIFIED | File absent from tree, no remaining references anywhere in app/components/lib/tests |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| appearance settings row | `readAppearanceSettings()` | `React.cache` wrap of `getSettings()` | ✓ WIRED | Both `getActiveTheme()` and `getLayoutSettings()` call it |
| `readAppearanceSettings()` | `GET /api/theme` JSON | `getActiveTheme()` → `getThemeTokens()` | ✓ WIRED | Route handler chain confirmed by reading source |
| `/api/theme` JSON | `global-error` inline styles | `parseThemeResponse()` → `setTokens` | ✓ WIRED | Confirmed by source + passing test suite |
| appearance settings row | rendered email HTML | `getActiveTheme()` → `resolveEmailTheme()` → sender → generator's `tokens` param | ✓ WIRED | Confirmed by source across all 6 builder files + passing D-01 test |
| staged effect row | rendered cron-drain email | `getActiveTheme()` (staging) → `payload` column → `emailThemeForStagedPayload()` (drain) → sender | ✓ WIRED | Confirmed by source in `lib/services/order-effects.ts` + integration tests |
| CI job | `scripts/scan-hardcoded-colors.mjs` | `npm run scan:tokens` step | ✓ WIRED | Confirmed in `.github/workflows/ci.yml`, ran live: 0 violations |
| 3 parity suites | committed `__snapshots__` baselines | `readParitySnapshot()` | ✓ WIRED | All 3 suites import and call the helper |
| both gallery variants | `next/image` src | `resolveProductImageSrc()` | ✓ WIRED | Confirmed in both component files |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Non-default theme reaches email HTML | `npx vitest run tests/unit/lib/utils/order-status-email-tokens.test.ts -t "renders luxe colour values"` | 1 passed | ✓ PASS |
| Theme route + crash page two-state contract | `npx vitest run tests/unit/app/global-error.test.ts tests/unit/app/api/theme-route.test.ts` | 18 passed | ✓ PASS |
| `scan:tokens` clean | `npm run scan:tokens` | 0 violations | ✓ PASS |
| Theme manifest fresh | `node scripts/build-themes.mjs --check` | fresh, 7 themes | ✓ PASS |
| Full unit suite | `npm test` | 265 files / 2187 tests passed | ✓ PASS |
| Workers integration suite (migration 0023 + drain) | `npm run test:workers` | 27 files / 153 tests passed | ✓ PASS |
| Typecheck | `npm run typecheck` | clean | ✓ PASS |
| Lint | `npm run lint` | 0 errors, 52 pre-existing warnings | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|-------------|--------------|--------|----------|
| DEBT-01 | 08.1-01 through 08.1-07 | All code-closable v2 audit tech debt | ✓ SATISFIED | `.planning/REQUIREMENTS.md:93` marked Complete; `.planning/v2-MILESTONE-AUDIT.md`'s 14-item `tech_debt` list annotated line by line, each entry naming its closing plan or reason it stays open (2 items legitimately remain open — Stripe/Clerk-session screenshot gaps and the border-inverse dual-use item — both outside 08.1-CONTEXT.md's declared scope, correctly not claimed as closed) |

No orphaned requirements found — `DEBT-01` is the only requirement mapped to Phase 8.1 in REQUIREMENTS.md.

### Anti-Patterns Found

None. Scanned all 40 files touched across the phase's full commit range (`afd9a06~1..HEAD`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` — zero matches.

### Deferred Items (informational, not gaps)

Two v2-audit tech-debt items remain open by design, per `08.1-CONTEXT.md`'s explicit phase boundary (not code-closable in this phase):
- Three of the four Phase 5 screenshot-coverage gaps (Stripe payment step, authenticated account dashboard, review-form error state) — need a real Stripe/Clerk session
- The `border-inverse` dual-use item — no preset's email dividers have been judged to read badly; not part of this phase's named scope

The Cloudflare Workers Build variable item and the Clerk admin walkthrough are recorded as closed/open per the audit annotation, consistent with `08.1-CONTEXT.md`'s deferred list.

### Human Verification Required

None. All must-haves resolved to VERIFIED via source inspection, live test execution, and independent re-run of the full gate suite (lint, typecheck, full unit suite, Workers integration suite, scan:tokens, build-themes --check). The one item SUMMARY 05 flagged as `human_judgment: true` (D-11's visual proof) was independently closed by plan 07's byte-identical hash comparison, verified directly in this pass rather than deferred.

### Gaps Summary

None. All 12 ROADMAP/CONTEXT must-have truths, all 4 prohibitions, all 10 required artifacts, and all 8 key links verified against the actual codebase — not just SUMMARY claims. Full gate suite (lint, typecheck, 265-file/2187-test unit suite, 27-file/153-test Workers integration suite, scan:tokens, build-themes --check) re-run independently in this verification pass and all green. Git tree clean of tracked changes.

---

_Verified: 2026-09-05_
_Verifier: Claude (gsd-verifier)_
