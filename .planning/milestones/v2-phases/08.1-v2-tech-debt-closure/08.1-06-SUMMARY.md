---
phase: 08.1-v2-tech-debt-closure
plan: 06
subsystem: theming
tags: [build-themes, validator, active-theme, telemetry, review-followup]

# Dependency graph
requires:
  - phase: 08.1-01
    provides: lib/themes/appearance-read.ts (shared request-scoped reader active-theme.ts's collapse builds on)
  - phase: 08.1-05
    provides: the settled product-display/image-resolver surface this plan's file list does not overlap
provides:
  - A validator that rejects a theme rule declaring the same token twice, naming the token with a file and line
  - One kebabToCamel implementation, exported and imported rather than reimplemented in a test
  - active-theme.ts's two unknown-value branches collapsed into one telemetry call and one return
  - 06-REVIEW IN-01, IN-02, IN-03, IN-04 all closed (IN-03 by recorded resolution, no code change)
affects: [08.1-07, future-theme-file-work]

actuals:
  tokens: 5200
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Per-token declaration counting (Map<string, {count, secondLine}>) replaces membership-only Set when a validator must both detect presence and flag repetition."

key-files:
  created:
    - tests/fixtures/themes/duplicate-token/dupe.css
  modified:
    - scripts/build-themes.mjs
    - tests/unit/scripts/build-themes.test.ts
    - tests/unit/lib/themes/token-contract.test.ts
    - lib/themes/active-theme.ts
    - docs/theming.md

key-decisions:
  - "The duplicate-token fixture declares --store-primary three times (not twice), so one fixture file proves both facts the plan's <behavior> block asks for: the duplicate fails naming the token with a positive line, and three declarations still yield exactly one error, not two."
  - "The resolver's two unknown-value branches merge by natural fallthrough past the closing brace of the `typeof stored === \"string\"` block, rather than an explicit isKnownString variable — same single-tail outcome the review suggested, one fewer name to maintain."
  - "06-REVIEW IN-03 (no vertical-margin regression test for the settings tab bar) is closed by recorded resolution only, per the review's own conclusion that no code change is required beyond the already-applied WR-01 fix; the wrapping scenario it names is covered by the admin-settings screenshot rows the phase-8 QA pass already captured."

patterns-established:
  - "A validator that must catch a repeated declaration counts per-key occurrences (a Map), not just presence (a Set) — the missing/unknown-token checks keep reading membership from the same structure via .has()."

requirements-completed: [DEBT-01]

coverage:
  - id: D1
    description: "Validator rejects a theme file that declares one token twice in its rule, naming the token with a file and a positive line number; three declarations still yield exactly one error"
    requirement: "DEBT-01"
    verification:
      - kind: unit
        ref: "tests/unit/scripts/build-themes.test.ts#duplicate-token — a fixture that redeclares a token fails naming the token, with a positive line number"
        status: pass
      - kind: unit
        ref: "tests/unit/scripts/build-themes.test.ts#duplicate-token — a token declared three times still yields exactly one error for that token, not two"
        status: pass
      - kind: other
        ref: "mise exec -- node scripts/build-themes.mjs --check (all seven shipped themes still validate; manifest/barrel byte-identical)"
        status: pass
    human_judgment: false
  - id: D2
    description: "kebabToCamel exists once in the repo (exported from scripts/build-themes.mjs) and the token-contract test imports it instead of reimplementing it"
    requirement: "DEBT-01"
    verification:
      - kind: unit
        ref: "grep -rl 'function kebabToCamel' scripts tests lib app components — exactly 1 match"
        status: pass
      - kind: unit
        ref: "tests/unit/lib/themes/token-contract.test.ts (full suite, imported helper)"
        status: pass
    human_judgment: false
  - id: D3
    description: "active-theme.ts's two unknown-value outcomes (unknown string, non-string value) funnel through one recordTelemetry call and one resolveEnvOrManifestDefault return, with the resolver's existing suite passing unedited"
    requirement: "DEBT-01"
    verification:
      - kind: unit
        ref: "tests/unit/lib/themes/active-theme.test.ts (full suite, file unedited)"
        status: pass
      - kind: unit
        ref: "tests/unit/lib/layout/settings.test.ts"
        status: pass
      - kind: other
        ref: "grep -c 'recordTelemetry(' lib/themes/active-theme.ts (excluding comments) == 1"
        status: pass
    human_judgment: false
  - id: D4
    description: "06-REVIEW IN-03 recorded as closed by resolution (no code change) so the Phase 6 review's Info list has nothing left dangling"
    verification: []
    human_judgment: true
    rationale: "This deliverable is a documentation/record fact (the review's own conclusion, restated here), not a testable code behavior."

# Metrics
duration: 10min
completed: 2026-09-05
status: complete
---

# Phase 8.1 Plan 06: Phase 6 review Info-item closure — validator, kebabToCamel, resolver collapse Summary

**Validator now rejects a copy-paste duplicate token declaration by name and line; `kebabToCamel` lives in exactly one place; `active-theme.ts`'s two unknown-value branches share one telemetry call and return — closing all four Phase 6 review Info items.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-09-05T21:40:00Z
- **Completed:** 2026-09-05T21:44:16Z
- **Tasks:** 3 completed
- **Files modified:** 6 (1 created, 5 modified)

## Accomplishments

- `scripts/build-themes.mjs`'s `validateThemeFile` now counts declarations per token (a `Map`, not a `Set`), pushing one error per token declared more than once, pointed at the line of its second declaration — closes 06-REVIEW IN-01.
- New fixture `tests/fixtures/themes/duplicate-token/dupe.css` declares `--store-primary` three times, proving in one file both that the duplicate is caught and that repeats past the second don't fan out into extra errors.
- `docs/theming.md`'s validator-message table gained the new row, quoted verbatim; every other row unchanged.
- `kebabToCamel` is now `export`ed from `scripts/build-themes.mjs`; `tests/unit/lib/themes/token-contract.test.ts` imports it instead of carrying a byte-identical local copy — closes 06-REVIEW IN-02.
- `lib/themes/active-theme.ts`'s `getActiveTheme()` now has exactly one `recordTelemetry("theme.unknown_selection", ...)` call and one `resolveEnvOrManifestDefault(...)` return in its unknown-value tail; the three silent early returns (absent/null, empty/whitespace, manifest match) are untouched — closes 06-REVIEW IN-04.
- 06-REVIEW IN-03 recorded as closed by resolution (see Decisions) — closes the last item, so Phase 6's review Info list has nothing left dangling.

## Task Commits

Each task was committed atomically:

1. **Task 1: The validator rejects a token declared twice** - `2b6b942` (fix)
2. **Task 2: One kebab-to-camel implementation, imported rather than retyped** - `4c6e265` (refactor)
3. **Task 3: The resolver's two unknown outcomes share one tail** - `d1734cd` (refactor)

**Plan metadata:** commit pending (this SUMMARY + STATE/ROADMAP/REQUIREMENTS)

## Files Created/Modified

- `scripts/build-themes.mjs` - `validateThemeFile`'s per-rule declaration walk now counts occurrences per token via a `Map`, emitting a new "declared more than once" error; `kebabToCamel` exported.
- `tests/fixtures/themes/duplicate-token/dupe.css` - new fixture: a clean 23-token theme with `--store-primary` declared three times.
- `tests/unit/scripts/build-themes.test.ts` - two new fixture-driven cases for the duplicate-token check.
- `tests/unit/lib/themes/token-contract.test.ts` - deleted its local `kebabToCamel` reimplementation, imports the build script's export instead.
- `lib/themes/active-theme.ts` - `getActiveTheme()`'s two unknown-value branches collapsed to one shared telemetry/return tail.
- `docs/theming.md` - one new row in the validator-message table.

## Decisions Made

- The duplicate-token fixture declares `--store-primary` three times (not twice) so one fixture file proves both `<behavior>` facts (duplicate caught by name+line; three declarations still yield exactly one error, not two) without needing a second fixture the plan's `files_modified` list didn't include.
- The resolver's branch collapse uses natural fallthrough past the `typeof stored === "string"` block rather than an explicit `isKnownString` variable — both the unknown-string path and the non-string path land on the same shared tail with no extra state to maintain, satisfying the review's suggestion by outcome rather than by literal variable name.
- 06-REVIEW IN-03 is closed by recorded resolution only, per the review's own text: no code change required beyond the already-applied `gap-1` fix, and the wrapping scenario it names is already covered by the admin-settings screenshot rows from the Phase 8 QA pass.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

All four Phase 6 review Info items (IN-01 through IN-04) are closed — three in code with tests, one by recorded resolution. `mise exec -- node scripts/build-themes.mjs --check`, `npm run scan:tokens` (0 violations), full `npm test` (264 files / 2184 tests), lint, and typecheck all pass. No blockers for `08.1-07` (the order fixture + captures + close-out plan), which is the last plan in this phase.

## Self-Check: PASSED

- `tests/fixtures/themes/duplicate-token/dupe.css` — FOUND
- `.planning/phases/08.1-v2-tech-debt-closure/08.1-06-SUMMARY.md` — FOUND
- Commits `2b6b942`, `4c6e265`, `d1734cd`, `601f4d6` — all FOUND in `git log`
- All task-level `<acceptance_criteria>` re-verified (vitest, `--check`, grep counts, lint, typecheck) — PASS
- Plan-level `<verification>`: lint/typecheck/full test suite, `--check`, `scan:tokens` (0 violations), manifest/theme diff empty — all PASS

---
*Phase: 08.1-v2-tech-debt-closure*
*Completed: 2026-09-05*
