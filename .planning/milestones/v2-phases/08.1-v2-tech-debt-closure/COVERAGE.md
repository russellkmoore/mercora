# Phase 8.1 — API Capability Coverage

No external API integration: one new public internal route (`GET /api/theme`) returning manifest data, CI step, request-scoped read helper, test hygiene, a local seed fixture. No third-party API, SDK, or webhook is consumed.
