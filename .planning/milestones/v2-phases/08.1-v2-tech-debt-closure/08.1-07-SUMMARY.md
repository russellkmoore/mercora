---
phase: 08.1-v2-tech-debt-closure
plan: 07
subsystem: theming
tags: [d1, seed-fixture, screenshot-qa, order-status, milestone-audit]

requires:
  - phase: 08.1-01
    provides: shared request-scoped appearance read, GET /api/theme, global-error theming (D-02/D-04)
  - phase: 08.1-02
    provides: email builders theme-aware at send time (D-01)
  - phase: 08.1-03
    provides: scan:tokens in CI, loud parity snapshots, logged seed fallback (D-03/D-06/D-07)
  - phase: 08.1-04
    provides: staged-effect theme for cron-path emails (D-10)
  - phase: 08.1-05
    provides: single image-URL resolver, D-11's code change (D-05/D-11 code half)
  - phase: 08.1-06
    provides: theme validator duplicate-token detection, kebabToCamel export, active-theme branch collapse (D-08 second half)
provides:
  - "A fixed, idempotent, dev-only guest order fixture (dev-order-001) in data/d1/seed-dev.sql, guarded by a unit test that proves it can never reach a production seed or migration"
  - "08.1-SCREENSHOTS.md: 28 order-status cells across all seven presets, captured against a session-minted bearer token that is never recorded anywhere"
  - "D-11's visual proof: this run's volt-dark product/1280/resting capture hash is byte-identical to Phase 8's recorded capture of the same cell"
  - "v2-MILESTONE-AUDIT.md's 14-item tech_debt list annotated line by line with the closing plan or the reason each item stays open"
  - "DEBT-01 marked Complete in REQUIREMENTS.md, closing Phase 8.1"
affects: [Phase 8.2 (documentation overhaul, DOCS-04/DOCS-05), milestone v2 close-out]

actuals:
  tokens: 18051
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "Dev-only fixtures live in data/d1/seed-dev.sql (predev-applied, --local only) and are guarded by a static test asserting they never appear in a production seed or a migration"
    - "A bearer token needed only for capture-time verification is minted into a shell variable, never a file, and grepped for at multiple points (document, manifest, .env.local tracking status)"

key-files:
  created:
    - tests/unit/scripts/dev-seed-guard.test.ts
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md
  modified:
    - data/d1/seed-dev.sql
    - docs/theming.md
    - .planning/v2-MILESTONE-AUDIT.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/08.1-v2-tech-debt-closure/08.1-VALIDATION.md
    - .planning/WINDOWS.md

key-decisions:
  - "The order-status route is captured only at layout combination A (defaults) — the route renders none of the three layout-switch variants, so crossing it with combinations B/C would add rows with no new information."
  - "D-11's visual proof used a direct hash comparison against Phase 8's recorded capture rather than a fresh pixel-diff tool run — the hashes matched exactly, which is strictly stronger evidence than a visual pixel-diff would have been."
  - "The Cloudflare Workers Build variable tech-debt item (Phase 06) is recorded as closed by Russell directly on 2026-09-05, not by a plan in this phase — the audit annotation attributes it accurately rather than crediting a plan that didn't do the work."
  - "v2-MILESTONE-AUDIT.md's front-matter status is left as tech_debt: two non-deferred items (three of the four Phase 5 screenshot-coverage gaps; the border-inverse dual-use item) remain open and are not part of 08.1-CONTEXT.md's named deferred scope, so promoting status would misrepresent the phase's actual closure."

patterns-established:
  - "Audit annotation appends the closure/open status inline in the same quoted list entry rather than replacing or deleting it, preserving the audit's history per its own non-deletion rule."

requirements-completed: [DEBT-01]

coverage:
  - id: D1
    description: "A fixed, idempotent, dev-only guest order fixture (dev-order-001) renders /order-status/[id] locally, provably unreachable from any production seed path"
    requirement: "DEBT-01"
    verification:
      - kind: unit
        ref: "tests/unit/scripts/dev-seed-guard.test.ts"
        status: pass
      - kind: integration
        ref: "npm run db:prepare:local run twice — exactly one dev-order-001 row, no error"
        status: pass
    human_judgment: false
  - id: D2
    description: "The order-status route captured at both viewports and both states under all seven presets, plus D-11's visual comparison against Phase 8's volt-dark capture"
    requirement: "DEBT-01"
    verification:
      - kind: other
        ref: "08.1-SCREENSHOTS.md structural checks: 7 distinct labels, 28 order-status rows, no token-shaped string, prior manifests untouched, D-11 hash present and matching"
        status: pass
    human_judgment: true
    rationale: "08.1-VALIDATION.md's Manual-Only Verifications row calls for a human to open the seven captures and confirm they read correctly under each preset — the automated checks prove structural completeness and the D-11 hash match, not visual correctness of every rendered cell."
  - id: D3
    description: "Every v2 audit tech-debt line names its outcome (closing plan or reason it stays open), and DEBT-01 is marked satisfied with every gate green"
    requirement: "DEBT-01"
    verification:
      - kind: unit
        ref: "full gate suite: npm run lint, npm run typecheck, npm test (265 files / 2187 tests), npm run scan:tokens (0 violations), node scripts/build-themes.mjs --check, npm run build"
        status: pass
      - kind: other
        ref: "grep checks: audit entry count >=13 (14 actual, none deleted), all six plan IDs cited, DEBT-01 checkbox and traceability row Complete"
        status: pass
    human_judgment: false

duration: 22min
completed: 2026-09-05
status: complete
---

# Phase 8.1 Plan 07: Dev-Only Order Fixture, Order-Status Visual QA, and Audit Close-Out Summary

**A dev-only guest order fixture makes /order-status/[id] renderable locally; all seven presets are captured with byte-identical D-11 proof against Phase 8's volt-dark baseline; the v2 audit's 14-item tech-debt list is annotated line by line and DEBT-01 closes Phase 8.1.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-05T21:46:00Z
- **Completed:** 2026-09-05T22:08:05Z
- **Tasks:** 3 completed
- **Files modified:** 8

## Accomplishments

- Appended a fixed, idempotent `dev-order-001` guest order to `data/d1/seed-dev.sql` (`ON CONFLICT(id) DO NOTHING`), proven safe by a new guard test and by running `db:prepare:local` twice with exactly one row and no error.
- Set up `.env.local`'s two guest-link variables (never read or echoed), confirmed the token gate returned 200 not 404, then captured the order-status route across `volt-dark`, `luxe`, `midnight`, `clinical`, `retro`, `atelier`, `market` — 28 cells total, recorded in a new `08.1-SCREENSHOTS.md`.
- D-11's visual proof: this run's volt-dark `product__1280__resting.png` hash (`71d237eb450c301fc0d7205b9cf03c40719904f938193c16ae617d57b48d4ff4`) is byte-identical to Phase 8's recorded capture of the same cell — the image-resolver consolidation produced zero visible change.
- Annotated every one of the 14 items in `v2-MILESTONE-AUDIT.md`'s `tech_debt` list with its closing plan or the reason it stays open, without deleting any entry; marked `DEBT-01` Complete in `REQUIREMENTS.md`.

## Task Commits

Each task was committed atomically:

1. **Task 1: A dev-only, idempotent guest order fixture** - `acfc335` (feat)
2. **Task 2: Capture order-status under all seven presets, prove the resolver swap changed nothing visually** - `9228148` (docs)
3. **Task 3: Close the audit list line by line and mark the requirement satisfied** - `22d8aa8` (docs)

Additional docs commits made during close-out: `f555dde` (VALIDATION.md rows/sign-off), `42779d4` (WINDOWS.md deviation entry).

**Plan metadata:** committed separately after this summary.

## Files Created/Modified

- `data/d1/seed-dev.sql` - Adds the `dev-order-001` guest order fixture (Task 1)
- `tests/unit/scripts/dev-seed-guard.test.ts` - Proves the fixture never reaches a production seed or migration and is referenced by exactly one `--local` script
- `docs/theming.md` - Documents the fixture id and the two required local env vars, no values
- `.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md` - 28-cell order-status coverage grid plus the D-11 comparison outcome and close-out record
- `.planning/v2-MILESTONE-AUDIT.md` - Every `tech_debt` entry annotated; new "Tech-Debt Closure (Phase 8.1)" section
- `.planning/REQUIREMENTS.md` - `DEBT-01` checkbox ticked, traceability row set to Complete
- `.planning/phases/08.1-v2-tech-debt-closure/08.1-VALIDATION.md` - Remaining task rows marked done, Wave 0 checkboxes checked, sign-off note added
- `.planning/WINDOWS.md` - New deviation entry for the REQUIREMENTS.md zero-unchecked-requirements verify mismatch (see Deviations below)

## Decisions Made

- Order-status captured at layout combination A only (no new information from crossing with B/C) — see key-decisions in frontmatter.
- D-11 proof used a direct hash comparison (stronger than a pixel-diff), which happened to match exactly.
- The Cloudflare Workers Build variable audit item is attributed to Russell directly, not to a plan, since that's factually what closed it.
- The audit's front-matter `status: tech_debt` is left unchanged because non-deferred items remain open (see Deviations and key-decisions).

## Deviations from Plan

### Auto-fixed Issues

None — no bugs, missing critical functionality, or blocking issues were found in the plan's own scope during Tasks 1 and 2.

### Documented Plan-Verify Mismatch

**1. [Deviation — verify assumption mismatch] Task 3's automated verify required zero unchecked `- [ ] **` requirement lines in `REQUIREMENTS.md` after ticking only `DEBT-01`**
- **Found during:** Task 3, running the full literal verify command
- **Issue:** `REQUIREMENTS.md` also carries `DOCS-04` and `DOCS-05` (Phase 8.2, inserted after this phase but not yet executed), which remain legitimately unchecked. The verify's `grep -c '^- \[ \] \*\*' .planning/REQUIREMENTS.md -eq 0` check therefore fails as literally written (count is 2, not 0) — ticking those two requirements would misrepresent unstarted Phase 8.2 work and violate this same task's own acceptance criterion "No other requirement's state changed."
- **Resolution:** Left `DOCS-04`/`DOCS-05` unchecked (accurate), verified `DEBT-01`'s own checkbox and traceability row directly instead of relying on the whole-file zero-unchecked assertion, and ran every other literal sub-check (audit entry count, all six plan IDs cited, `DEBT-01` checkbox/row, full lint/typecheck/test/scan/build-themes/build gate suite) — all of which passed.
- **Files affected:** none (no code change; this is a plan-verify authoring gap, not a defect in the codebase)
- **Verification:** All other sub-checks of Task 3's verify command passed; see "Full Gate Suite" below.
- **Recorded:** `.planning/WINDOWS.md` entry id 6 (kind: deviation)

---

**Total deviations:** 1 documented (plan-verify assumption mismatch, no code impact). **Impact:** none — DEBT-01's actual completion criteria are all met; the mismatch is in an over-broad automated sub-check that predates knowledge of Phase 8.2's insertion.

## Full Gate Suite

- `npm run lint` — 0 errors, 52 pre-existing warnings (unrelated files)
- `npm run typecheck` — clean
- `npm test` — 265 files / 2187 tests passed
- `npm run scan:tokens` — 0 violations (2 manual-review rows, pre-existing)
- `node scripts/build-themes.mjs --check` — fresh, 7 themes
- `npm run build` — succeeded

## Issues Encountered

None beyond the documented deviation above.

## User Setup Required

None — no external service configuration required. The two local `.env.local` variables
(`ORDER_STATUS_GUEST_LINKS_ENABLED`, `ORDER_STATUS_SECRET`) were generated and appended by this
plan's own automation and remain git-ignored/untracked; no action needed from Russell for this
plan specifically (they only affect local screenshot capture, already done).

## Next Phase Readiness

Phase 8.1 is complete: all 7 plans executed, `DEBT-01` satisfied, every full gate green. Two
non-deferred tech-debt items remain open (three of the four Phase 5 screenshot-coverage gaps;
the border-inverse dual-use item) and five deferred items stay open per `08.1-CONTEXT.md` — none
block milestone close. Ready for Phase 8.2 (documentation overhaul, `DOCS-04`/`DOCS-05`) and then
`/gsd-complete-milestone v2`.

## Self-Check: PASSED

- `data/d1/seed-dev.sql` — FOUND
- `tests/unit/scripts/dev-seed-guard.test.ts` — FOUND
- `.planning/phases/08.1-v2-tech-debt-closure/08.1-SCREENSHOTS.md` — FOUND
- `git log --oneline --all --grep="08.1-07"` — no commits use that grep pattern in the subject (commits use `feat(08.1-07):`/`docs(08.1-07):` prefixes); verified directly instead: `acfc335`, `9228148`, `22d8aa8`, `f555dde`, `42779d4` all present in `git log --oneline -10`
- All three tasks' `<acceptance_criteria>` re-run and passing (see Task Commits and Full Gate Suite above)

---
*Phase: 08.1-v2-tech-debt-closure*
*Completed: 2026-09-05*
