---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
verified: 2026-09-05T00:05:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Sign in to /admin/settings/appearance with a real Clerk session, confirm all seven cards render (industry + synopsis text, correct swatch chips) for clinical/retro/atelier/market alongside luxe/midnight/volt-dark, click each of the four new cards, and drive the grid with arrow keys/space/enter."
    expected: "Seven cards render with correct copy; clicking a new preset shows the ring without moving the Active badge until Save; keyboard radiogroup navigation works."
    why_human: "No Clerk session exists in this environment (carried from Phase 6, WINDOWS #2, still open — unchanged by this phase). Automated evidence instead: ThemePresetGrid.tsx imports the real THEME_MANIFEST (now 7 entries, confirmed live) with zero code changes; the dev-bypass header method confirmed each new preset actually flips the served data-theme attribute; the pure-view render test (19/19) passes against the regenerated manifest. That proves the mechanism, not the live rendered page."
  - test: "With clinical/atelier/market active and a real Clerk session plus a completed Stripe checkout, open the OrderConfirmationModal (Dialog) and an admin confirmation AlertDialog directly and confirm the bg-black/80 / bg-black/50 scrims read as a darkened, content-obscuring backdrop; also view the category hero overlay against a real configured product-category image."
    expected: "Both scrims recede content behind them exactly as under volt-dark/luxe; the hero overlay's light wash keeps foreground text legible against a real photo, not just a synthetic gradient."
    why_human: "Stripe checkout fails locally (known Phase 5 gap), AlertDialog is admin-only (no Clerk session), and the local D1 fixture's one category has no configured image. This phase's own findings (F6/F7/F14/F20 in 06.1-SCREENSHOTS.md) substituted a live Sheet-drawer trigger plus live-DOM compositing injection (real compiled CSS, synthetic gradient) for Dialog/AlertDialog/hero — same class/mechanism, not independently observed live. Carried forward unchanged since Phase 6."
---

# Phase 6.1: Remaining Presets (Clinical, Retro, Atelier, Market) Verification Report

**Phase Goal:** The four remaining presets from `docs/voltique-theme-direction.md` — `clinical`, `retro`, `atelier`, `market` — ship as validated theme files on the frozen 23-token contract, each with header metadata and screenshot evidence, and the three light ones pass the same scrim/shadow QA Luxe did, so the admin Appearance page offers all six merchant looks plus volt-dark.
**Verified:** 2026-09-05T00:05:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `themes/clinical.css`, `themes/retro.css`, `themes/atelier.css`, `themes/market.css` exist, each a single `[data-theme]` block with exactly 23 `--store-*` tokens (17 hex colours) plus a `@theme` header (label, industry, synopsis); `build-themes.mjs --check` passes and the committed manifest lists seven themes | ✓ VERIFIED | Read all four files directly — one `[data-theme="<name>"]` rule each, 23 `--store-*` declarations (grep count confirmed), `/* @theme label: ... | industry: ... | synopsis: ... */` header present on each. Independently re-ran `mise exec -- node scripts/build-themes.mjs --check` → `check passed — generated output for 7 theme(s) is fresh`. `lib/themes/manifest.generated.ts` lists 7 distinct `name`/`label` entries (atelier, clinical, luxe, market, midnight, retro, volt-dark). `git status --porcelain themes/ lib/themes/` clean. |
| 2 | Display fonts named by the direction doc (Orbitron, Fraunces, Nunito) load via `next/font` with `preload: false` and reach `--store-font-display`; `volt-dark` pages pay no extra font bytes | ✓ VERIFIED | `app/layout.tsx` declares `Orbitron`, `Fraunces`, `Nunito` via `next/font/google`, each with `preload: false` and CSS variables (`--font-orbitron`, `--font-fraunces`, `--font-nunito`). `themes/retro.css`/`atelier.css`/`market.css`'s `--store-font-display` reference the matching variable. `themes/volt-dark.css` and `themes/midnight.css` contain no reference to any of the three new font variables (`grep` returned nothing) — no extra bytes for those pages. Live `document.fonts.check()` readings recorded in 06.1-SCREENSHOTS.md (lines 262-298, 482-513) confirm each loaded webfont — not its fallback — actually resolves on rendered `h1`s. |
| 3 | Each preset is captured across the seven-route grid; the three light presets (clinical, atelier, market) have their overlays and shadows inspected with every judgement recorded, fixes made only where something reads wrong | ✓ VERIFIED | 06.1-SCREENSHOTS.md records four canonical per-preset capture runs (`phase-06.1-04-{clinical,retro,atelier,market}`, 28/32 cells each, 4 missing for the same environment-documented reasons carried since Phase 5). The Findings table (F1-F22) records 22 judgements across clinical/atelier/market's acid tests (hover inversion, no-card-fills, radius-reach), heading-typography checks for all four new presets, scrim re-checks, and cross-checks — one real defect found and fixed (F4, `Select` dropdown highlight), every other site explicitly recorded "leave it" with its own reasoning, satisfying D-07's not-change-only requirement. Independently confirmed the F4 fix is live in `components/ui/select.tsx:119` (`focus:bg-primary/10`, not the old `focus:bg-surface-elevated`). |
| 4 | The admin Appearance page shows all seven cards with industry and synopsis; selecting any of the four new presets switches the storefront on the next request; `scan:tokens` stays at 0 and the 23-token contract is unchanged | ✓ VERIFIED (mechanism); live-render carve-out routed to human verification | `components/admin/ThemePresetGrid.tsx` imports and maps over the real `THEME_MANIFEST` from `lib/themes/manifest.generated.ts` (now 7 entries) with zero code changes (`git status --porcelain components/admin/` empty across the phase per SUMMARY). `tests/unit/app/admin-appearance-source.test.ts` (19/19, independently re-run) exercises the pure render view against a manifest fixture. Switch mechanism confirmed via the documented dev-bypass method flipping `data-theme` for each of the four new presets, then restored to `volt-dark`. Independently re-ran `scan:tokens` → `0 violations` (2 unchanged manual-review rows) and re-counted 23 `--store-*` tokens per file — contract unchanged. Live visual confirmation of the rendered 7-card grid needs a real Clerk session (WINDOWS #2, carried from Phase 6, unchanged) — see human verification below. |
| 5 (D-10) | Both type tokens are wired site-wide (`font-sans` on body, `font-display` on headings), `next/font` variable classes present on both `<html>` and `<body>`, the volt-dark system-ui→Geist change is recorded as intentional snap S-TYPE-01, and display faces render on headings per preset | ✓ VERIFIED | `app/globals.css` body rule sets `font-family: var(--store-font-sans)`; `html` rule no longer carries a literal `system-ui` stack (only `font-feature-settings`). `app/layout.tsx` applies the identical six-variable class string (`geistSans.variable geistMono.variable cormorantGaramond.variable orbitron.variable fraunces.variable nunito.variable`) to both the `<html>` and `<body>` elements. `tailwind.config.ts` maps the `display` Tailwind font family to `var(--store-font-display)`; 28 storefront files use the `font-display` utility class on heading elements. 06.1-SCREENSHOTS.md registers `S-TYPE-01` (line 260) with before/after capture labels and computed-font-family readings (system-ui→Geist under volt-dark, 27/27 comparable cells differ), and the per-preset display-face proof table (lines 262-276) shows `document.fonts.check()` returning `true` for the loaded webfont (not fallback) on all four new presets' `h1`s. |

**Score:** 5/5 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `themes/clinical.css` | 23-token light preset, Geist/Geist display, `@theme` header | ✓ VERIFIED | Exists, 23 tokens, header present, referenced by `themes/index.generated.css` |
| `themes/retro.css` | 23-token dark preset, Orbitron display | ✓ VERIFIED | Exists, 23 tokens, header present, `--store-font-display` references `--font-orbitron` |
| `themes/atelier.css` | 23-token light preset, Fraunces display | ✓ VERIFIED | Exists, 23 tokens, header present, `--store-font-display` references `--font-fraunces` |
| `themes/market.css` | 23-token light preset, Nunito display | ✓ VERIFIED | Exists, 23 tokens, header present, `--store-font-display` references `--font-nunito` |
| `app/layout.tsx` | Orbitron/Fraunces/Nunito `next/font` loads, variable classes on html+body | ✓ VERIFIED | All three fonts declared with `preload: false`; variable string applied to both `<html>` and `<body>` |
| `app/globals.css` | body carries `--store-font-sans`, html has no literal font stack | ✓ VERIFIED | Confirmed by direct read |
| `lib/themes/manifest.generated.ts` | 7 manifest entries | ✓ VERIFIED | Generated fresh, 7 distinct names/labels, matches `build-themes.mjs --check` |
| `components/ui/select.tsx` | F4 fix — highlight distinguishable from popover background | ✓ VERIFIED | `focus:bg-primary/10` confirmed present at line 119 |
| `.planning/phases/06.1-.../06.1-SCREENSHOTS.md` | Capture grids, findings table, S-TYPE-01, phase-close roll-up | ✓ VERIFIED | 830 lines; all required sections present with named evidence |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `themes/index.generated.css` | `themes/clinical.css` | generated `@import` | ✓ WIRED | `@import './clinical.css';` present in generated barrel |
| `themes/retro.css` | `app/layout.tsx` | `--store-font-display: var(--font-orbitron), ...` | ✓ WIRED | Variable declared in layout, consumed in theme file |
| body element | `themes/*.css --store-font-sans` | base-layer `font-family` declaration on body, next/font variable classes also on body | ✓ WIRED | Confirmed in `app/globals.css` + `app/layout.tsx` |
| `lib/themes/manifest.generated.ts` | admin Appearance card grid | `THEME_MANIFEST` import, `.map()` over 7 entries, no component change | ✓ WIRED | Confirmed by direct read of `ThemePresetGrid.tsx` |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Theme build validator passes fresh for 7 themes | `mise exec -- node scripts/build-themes.mjs --check` | `check passed — generated output for 7 theme(s) is fresh.` | ✓ PASS |
| Token scan stays at 0 | `mise exec -- npm run scan:tokens` | `0 violations` (2 pre-existing manual-review rows) | ✓ PASS |
| Admin render test suite | `mise exec -- npx vitest run tests/unit/app/admin-appearance-source.test.ts` | 19/19 passed | ✓ PASS |
| Full workspace test suite (run once) | `mise exec -- npm test` | 249 files / 1959 tests, all green | ✓ PASS |
| Typecheck | `mise exec -- npm run typecheck` | clean, no errors | ✓ PASS |
| Lint | `mise exec -- npm run lint` | 0 errors, 52 pre-existing warnings (unchanged) | ✓ PASS |
| Working tree left clean after all checks | `git status --porcelain themes/ lib/themes/ app/ components/ui/select.tsx tests/` | empty | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| THEME-05 | 06.1-01, 06.1-02, 06.1-03, 06.1-04 | Four remaining presets ship as validated theme files, fonts load, screenshot evidence, light-preset QA, admin lists all seven | ✓ SATISFIED | Marked `Complete` in `.planning/REQUIREMENTS.md`; all four ROADMAP success criteria independently confirmed above |

No orphaned requirements found mapped to Phase 6.1 beyond THEME-05.

### Anti-Patterns Found

None. Scanned all four new theme files, `app/layout.tsx`, `app/globals.css`, `components/ui/select.tsx`, `lib/themes/manifest.generated.ts`, and `themes/index.generated.css` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` and stub patterns — no matches (one false-positive grep hit on Radix's `data-[placeholder]` attribute selector in `select.tsx`, not a stub marker).

### Regression Check (Phase 6)

Phase 6's core mechanism files (`scripts/build-themes.mjs`, `lib/themes/active-theme.ts`, `components/admin/ThemePresetGrid.tsx`) show no uncommitted changes and no new commits since Phase 6 closed — confirmed via `git log`/`git status --porcelain`. Phase 6's own two open human-verification items (concurrent-request race on `getActiveTheme()`, live keyboard/screen-reader walkthrough) are unaffected by this phase's scope and remain exactly as Phase 6 left them.

### Human Verification Required

See frontmatter `human_verification`. Both items are environment-limited (no Clerk session, no completed Stripe checkout, no configured category image in this local D1 fixture) and were already open/carried-forward at Phase 6's close (WINDOWS #2; F7/F14/F20's compositing-injection substitute) — this phase did not introduce them and made no attempt to work around them beyond the same live-DOM compositing-injection technique Phase 6 established. They do not indicate a defect in this phase's work; the underlying mechanism (manifest-driven grid, dev-bypass theme switch, scrim class reuse) is independently confirmed by non-visual evidence recorded above.

### Gaps Summary

No gaps. All four ROADMAP success criteria and D-10 are independently verified against the codebase: four theme files exist and validate cleanly, three new display fonts load correctly and reach rendered headings, all four presets are captured with light-preset QA recorded (one real defect found and fixed), and the admin Appearance page's manifest-driven grid mechanism plus the storefront-wide type-token wiring are both confirmed wired. The two open items are pre-existing environmental limitations (no authenticated admin session, no live checkout/photo) carried unchanged from Phase 6, not new gaps this phase created.

---

_Verified: 2026-09-05T00:05:00Z_
_Verifier: Claude (gsd-verifier)_

---

**Human verification outcome (2026-09-05T07:05:14Z):** Russell accepted the two human-verification items (admin Appearance walkthrough with seven cards; Dialog/AlertDialog scrims under a real checkout) as passed on the strength of the code-level evidence above, via /gsd-autonomous. See 06.1-UAT.md.
