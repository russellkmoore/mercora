---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - app/layout.tsx
  - themes/retro.css
  - themes/atelier.css
  - themes/market.css
  - themes/index.generated.css
  - lib/themes/manifest.generated.ts
autonomous: true
requirements: [THEME-05]
user_setup: []

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The phase's remaining three preset files exist at the paths D-01 names — `themes/retro.css`, `themes/atelier.css`, `themes/market.css` — each exactly one `[data-theme]` rule holding exactly the 23 frozen tokens plus a `@theme` header carrying label, industry and synopsis copied verbatim from the UI-SPEC, with the theme name being the filename stem (D-01)."
    - "Three display faces load exactly once, in the root layout, through `next/font/google` with `preload: false`, each exposed as a CSS custom property that exactly one theme file references: Orbitron for `retro`, Fraunces for `atelier`, Nunito for `market` (D-05)."
    - "Every one of the three faces was confirmed present in the installed Next font-data manifest, with the requested static weights available, before it was imported — Orbitron, Fraunces and Nunito all resolve with the weights this plan requests (D-05's belt-and-suspenders check)."
    - "No theme file loads a font. All three new files reference a layout-exposed CSS variable in `--store-font-display` and declare no at-rule of any kind, which the unmodified validator enforces structurally (D-05)."
    - "`themes/retro.css` is a dark preset: its five inverse tokens go light (drawers, Stripe host, email panels stay light panels), mirroring `midnight`'s polarity, and its `border` is independently derived rather than repurposed from the doc's `surface-sunken`, which is darker than its own surface (D-04, rename map)."
    - "`themes/atelier.css` and `themes/market.css` are light presets: their five inverse tokens go dark, mirroring `luxe`'s polarity, and each repurposes the doc's `surface-sunken` as the hairline `border` tone (D-04, rename map)."
    - "Three of the four presets carry a flagged `primary` deviation from the direction doc's literal accent, darkened along the same hue so the doc's own `on-primary` reaches the 4.5:1 AA text threshold — clinical 3.38 to 4.61, atelier 4.27 to 4.64, market 3.68 to 4.63. Retro needs no correction and takes every value literally. Each deviation is disclosed in its own file's header comment with the measured before and after ratios (D-02)."
    - "`accent-2` folds into `info` only where the direction doc uses it for a status or badge role: atelier's sage badge accent folds in; retro's cyan is already consumed by `success` and market's orange is decorative, so both derive `info` independently and drop `accent-2` (D-03, rename map)."
    - "Each preset's four radius tokens scale from the doc's single figure by that preset's own stated reasoning — retro compressed below volt-dark's stock scale, market scaled up with its 1rem figure anchoring `radius-xl`, atelier reusing the stock progression anchored at its 0.375rem figure — and no shape token is added (D-03)."
    - "EDGE/ordering: after regeneration the barrel and manifest both list all seven themes in sorted filename order — atelier, clinical, luxe, market, midnight, retro, volt-dark — so the admin grid's card order is deterministic."
    - "EDGE/adjacency: all seven labels are distinct strings, and two theme files sharing an identical token value is not a conflict — identity is the filename stem, which a case-sensitive filesystem already makes unique."
    - "EDGE/concurrency: two consecutive generator runs leave `git status --porcelain` empty on both generated paths, and `--check` exits 0 against the committed output (D-08)."
    - "Setting `appearance.theme` to each of `retro`, `atelier` and `market` in turn makes the storefront serve that name in the html element's theme attribute on the next request, with no code change and no restart (D-09)."
    - "UI CONSIDERATION (populated): the admin Appearance grid renders seven cards, one per manifest entry, with `components/admin/ThemePresetGrid.tsx` unchanged — the existing render tests, whose expected count derives from the mocked manifest length rather than a literal, still pass (D-09)."
    - "UI CONSIDERATION (zero-one-many): the responsive card grid absorbs three-to-seven growth with no layout change, confirming Phase 6's decision not to hardcode a card count."
    - "The whole-tree token scan stays at 0 violations with its existing manual-review rows — theme files are the scanner's declared source directory and are skipped, so adding three changes nothing about the sweep's verdict (D-08)."
  artifacts:
    - path: "themes/retro.css"
      provides: "The second dark preset after midnight: deep purple surfaces, magenta primary, cream text, Orbitron display face"
      contains: "font-orbitron"
    - path: "themes/atelier.css"
      provides: "Light preset: warm linen surfaces, corrected clay primary, sage info folded from the doc's badge accent, Fraunces display face"
      contains: "font-fraunces"
    - path: "themes/market.css"
      provides: "Light preset: off-white surfaces, corrected produce-green primary, the phase's largest radius scale, Nunito display face"
      contains: "font-nunito"
    - path: "app/layout.tsx"
      provides: "Three additional next/font/google display faces exposed as CSS variables on the body element"
      contains: "--font-orbitron"
  key_links:
    - from: "themes/retro.css"
      to: "app/layout.tsx"
      via: "the display-font token references the CSS variable the root layout's Orbitron declaration exposes"
      pattern: "font-orbitron"
    - from: "themes/atelier.css"
      to: "app/layout.tsx"
      via: "the display-font token references the CSS variable the root layout's Fraunces declaration exposes"
      pattern: "font-fraunces"
    - from: "themes/market.css"
      to: "app/layout.tsx"
      via: "the display-font token references the CSS variable the root layout's Nunito declaration exposes"
      pattern: "font-nunito"
    - from: "lib/themes/manifest.generated.ts"
      to: "components/admin/ThemePresetGrid.tsx"
      via: "the grid maps every manifest entry to a card with no hardcoded count, so seven entries render seven cards with no component change"
      pattern: "THEME_MANIFEST"
  prohibitions:
    - statement: "This phase must NOT add, rename, or remove any of the 23 frozen tokens to express a preset."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. Retro's hard offset shadows and 2px borders, market's decorative promo strip, and atelier's shadow-on-hover all want a token the contract does not have; this is the plan where that pressure peaks."
    - statement: "A theme file must NOT contain an @import, an @font-face, a second selector, or any external resource reference — a theme is data, not code."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. Three of these files reference a display face by name, which is exactly the situation that tempts an in-file font declaration."
    - statement: "The validator, the manifest shape, the theme resolver, and the admin grid must NOT be changed to accommodate a preset."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. D-09 predicts zero admin changes; any diff under components/admin/ in this plan is the signal that a preset was accommodated instead of authored correctly."
    - statement: "A display font must NOT be shipped via a runtime third-party link or stylesheet import."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. This is the only plan in the phase that adds font loading, so it is the only place this can be violated."
---

<objective>
Author the remaining three presets and load the three display faces they name. `retro` is dark and
takes the direction doc's values literally; `atelier` and `market` are light and each carry one
contrast correction. All three reference a display face that the root layout loads once, on demand,
for every theme.

Purpose: THEME-05's bulk. Plan 06.1-01 already proved the pipeline end to end on `clinical`, so this
plan is repetition of a known-good shape plus the one genuinely new element — three `next/font`
declarations — which is why the font wiring is Task 1 and the files that reference it come after.

Output: `themes/retro.css`, `themes/atelier.css`, `themes/market.css`, three font declarations in
`app/layout.tsx`, and a regenerated seven-entry barrel and manifest.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-CONTEXT.md
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-PATTERNS.md
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-01-SUMMARY.md
</context>

<interfaces>
This plan defines three new CSS custom property names in Task 1 that Task 2's theme files consume by
name. Fixing them first is deliberate — a mismatch between the two is invisible to the validator and
to typecheck, and would surface only as a heading silently rendering in the fallback face.

Produced by Task 1, consumed by Task 2:

| Face | CSS variable | Theme file that references it |
|---|---|---|
| Orbitron | `--font-orbitron` | `themes/retro.css` |
| Fraunces | `--font-fraunces` | `themes/atelier.css` |
| Nunito | `--font-nunito` | `themes/market.css` |

Consumed unchanged from earlier work:

- `scripts/build-themes.mjs` — validator plus codegen, `--check` freshness gate, `[build-themes]`
  message prefix, `REQUIRED_TOKENS` as the canonical 23-name list, at-rules rejected outright.
- `themes/index.generated.css`, `lib/themes/manifest.generated.ts` — generated, never hand-edited;
  barrel imports carry an explicit `./` prefix.
- `APPEARANCE_SETTINGS_CATEGORY` / `APPEARANCE_THEME_SETTING_KEY` from `lib/themes/active-theme.ts`,
  and the `x-dev-admin: mercora-dev-bypass` header, for Task 3's switch probe.
- `components/admin/ThemePresetGrid.tsx` and `tests/unit/app/admin-appearance-source.test.ts` —
  both unchanged by this plan; the test's expected card count derives from its mocked manifest
  length, so it exercises the rendering contract rather than the real seven-entry count.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three display faces loaded once in the root layout</name>
  <read_first>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 355-408 — the Font Loading section: the three declaration blocks with their variable names, subsets, weights and `preload: false`, the instruction to add the three variables to the existing body class list, and the pre-use availability check.
    - `app/layout.tsx` lines 60-105 and 170-180 — the existing import line's alphabetical ordering, the three existing font declarations (Geist, Geist Mono, Cormorant Garamond), the comment convention above the Cormorant block explaining why preload stays off and which theme references it, and the body `className` template literal the three new variables join.
    - `app/globals.css` lines 5-32 — the base-layer rules. Note that `html` carries a literal system font stack and that nothing in the tree applies the `font-sans` class, so these variables are defined but not yet consumed by any element; that gap is plan 06.1-03's subject, not this task's.
    - `tailwind.config.ts` lines 36-45 — the `fontFamily` map showing `display` resolves to `var(--store-font-display)`, which is what the theme files' display tokens feed.
  </read_first>
  <files>app/layout.tsx</files>
  <behavior>
    - Before importing: each of Orbitron, Fraunces and Nunito is confirmed present in the installed `next/font/google` font-data manifest, with the static weights this task requests listed among its available weights. The check runs against the installed package, not from memory.
    - After the change: `npm run build` succeeds, proving Next accepted all three font declarations at build time (an unavailable face or weight is a build-time error in `next/font`, not a runtime fallback).
    - After the change: the three new CSS variable names appear in the compiled output and the body element's class list carries all six font variables.
    - Under `volt-dark`, no page requests any of the three new font files, because no element references `--store-font-display` yet and `preload` is off for all three.
  </behavior>
  <action>
Add three `next/font/google` display faces to the root layout, following the existing Cormorant
Garamond declaration exactly.

First, confirm availability against the installed package rather than from memory: read the three
face names out of Next's own bundled font-data manifest and check that each lists the static weights
this task requests. If a requested weight is absent for a face, fall back to the nearest available
static weight, and record that substitution in the SUMMARY and in the header comment of the theme
file that references it, following the disclosure discipline the existing preset files use.

Extend the existing font import from `next/font/google` to include the three new faces, keeping the
import list alphabetical the way it already is.

Add one declaration block per face, each mirroring the existing Cormorant Garamond block's shape:
the CSS variable name from the interfaces table above, the latin subset, a weight array, swap
display, and preload disabled. Above each block put a short comment in the same voice as the
existing one, naming which preset references the variable and restating why preload stays off — a
page that never renders text in that face pays nothing extra, so the browser fetches the file only
when a theme block referencing the variable actually renders text.

On weights, take the UI-SPEC's listed weight for each face as the floor and extend it to a superset
covering the weights the storefront's heading elements actually request. The storefront's headings
use semibold, bold and extrabold classes, and `app/globals.css` sets `font-synthesis: none` on the
body, so a weight that is not loaded renders at the nearest loaded weight rather than being
synthesised. Loading the extra static weights costs nothing under `preload: false` for any page that
does not render that face. Record the superset and this reasoning in the SUMMARY as a deliberate,
additive extension of the UI-SPEC's list — nothing the UI-SPEC named is removed.

Add the three new variables to the body element's existing class list alongside the three that are
already there, so each custom property is always defined regardless of which theme is active.

Change nothing else in this file: no change to the layout function body, no change to the theme
resolution calls, no new element, no class applied to a heading. This task exposes variables; making
any element consume them is plan 06.1-03's subject.
  </action>
  <acceptance_criteria>
    - The availability check ran against the installed Next font-data manifest and its result — face name, available weights, requested weights — is recorded in the SUMMARY for all three faces.
    - `app/layout.tsx` declares exactly three new font constants, each with `preload: false` and `display: "swap"`, exposing `--font-orbitron`, `--font-fraunces` and `--font-nunito`.
    - The body `className` carries all six font variables.
    - The import list from `next/font/google` is alphabetical and includes the three new faces.
    - Nothing else in `app/layout.tsx` changed: no heading class, no layout-function edit, no theme-resolution change.
    - `mise exec -- npm run build` exits 0, which is the real proof Next resolved all three faces and their weights.
    - `mise exec -- npm run lint`, `typecheck` and `test` all exit 0.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- node -e 'const d=require("next/dist/compiled/@next/font/dist/google/font-data.json");for(const k of ["Orbitron","Fraunces","Nunito"]){if(!d[k]){console.error("MISSING FACE "+k);process.exit(1);}console.log(k,JSON.stringify(d[k].weights));}' && test "$(/usr/bin/grep -c 'preload: false' app/layout.tsx)" -eq 6 && /usr/bin/grep -q '"--font-orbitron"' app/layout.tsx && /usr/bin/grep -q '"--font-fraunces"' app/layout.tsx && /usr/bin/grep -q '"--font-nunito"' app/layout.tsx && test "$(/usr/bin/grep -cE '\$\{(geistSans|geistMono|cormorantGaramond|orbitron|fraunces|nunito)\.variable\}' app/layout.tsx)" -eq 1 && test "$(/usr/bin/grep -oE '\$\{[a-zA-Z]+\.variable\}' app/layout.tsx | wc -l | tr -d ' ')" -eq 6 && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run build</automated>
    <fails_when>the availability probe prints `MISSING FACE` and exits 1, meaning a face this phase assumed is not in the installed font-data manifest; or the `preload: false` count is not 6, meaning a new declaration omitted it and would preload an off-theme face on every page; or a variable-name grep finds nothing, meaning a declaration exposes a different property name than the theme files will reference; or the body class list does not carry exactly six font variables, meaning a variable is defined but never attached to an element; or lint, typecheck, test or build exits non-zero — a build failure here is specifically how an unavailable face or weight surfaces.</fails_when>
  </verify>
  <done>Three display faces are loaded once in the root layout, confirmed present in the installed package, exposed under the exact variable names the next task's theme files will reference, and provably not preloaded.</done>
</task>

<task type="auto">
  <name>Task 2: themes/retro.css, themes/atelier.css, themes/market.css</name>
  <read_first>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 248-281 — the complete Retro token table, including the note that all four contrast checks pass on the doc's literal values, the independently derived `border` and its reasoning, the independently derived `info` and why it cannot reuse the doc's second accent, and the compressed radius scale. Transcribe these values.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 284-317 — the complete Atelier token table, including the flagged `primary` deviation with its measured before and after ratios, the sage `info` folded in from the doc's badge accent, and the stock radius progression anchored at the doc's figure.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 320-352 — the complete Market token table, including the flagged `primary` deviation, the deliberate decision to keep `success` at the doc's literal uncorrected value and why that is not an inconsistency, the derived cool-blue `info` and why orange was avoided, and the scaled-up radius progression anchoring the doc's 1rem at the extra-large step.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 184-208 — the rename map and the contrast-check method, specifically the conditions under which the doc's second accent folds into `info` versus being dropped, and the per-preset radius rule.
    - `themes/midnight.css` — the dark-preset file shape retro mirrors, including its inverse-group comment wording for a dark theme and its deviation-disclosure paragraphs.
    - `themes/clinical.css` (from plan 06.1-01) and `themes/luxe.css` — the light-preset file shape atelier and market mirror, including the inverse-group comment wording for a light theme.
  </read_first>
  <files>themes/retro.css, themes/atelier.css, themes/market.css</files>
  <action>
Author the three remaining preset files. Each mirrors the structure the existing preset files use:
head comment paragraphs, then the metadata line, then one selector block with four commented groups
in the order main colours, inverse colours, shape, type.

Take every value from the UI-SPEC's per-preset table. Do not re-derive any hex from an oklch value —
the UI-SPEC's conversions were computed programmatically and cross-checked against the two
already-shipped presets, and re-deriving by hand is how the corrections get silently reverted.

`themes/retro.css` — dark. Head comment records that this is the phase's only dark preset, that
every value is a direct transcription because all four contrast checks pass on the doc's literal
values with no correction needed, and two disclosure paragraphs: that `border` is derived
independently rather than repurposed from the doc's sunken surface, because that value is darker
than retro's own surface and would be invisible as a border; and that `info` is derived
independently rather than folded from the doc's second accent, because retro's `success` already
carries that exact cyan literal and reusing it would collapse two status colours into one. Use the
dark-theme inverse-group comment wording — light panels on a dark app. The shape group's comment
records that the doc's single figure anchors the medium step and the whole progression is compressed
below the stock scale, matching retro's chunky pop-art direction. The display token references the
Orbitron variable from Task 1 followed by the named family and a generic fallback.

`themes/atelier.css` — light. Head comment records the one flagged deviation with both measured
ratios: the doc's literal accent against the doc's own accent-ink falls just under the AA text
threshold, so `primary` is darkened along the same hue and chroma to the UI-SPEC's corrected value.
Second paragraph records that `info` is the doc's own second accent folded in, because the doc uses
it for badges — a status role, the exact condition the rename map reserves for folding rather than
dropping. Third paragraph records the light-preset inverse polarity. Use the light-theme
inverse-group comment wording — dark panels on a light app. The display token references the
Fraunces variable followed by the named family and serif fallbacks.

`themes/market.css` — light. Head comment records the flagged `primary` deviation with both measured
ratios, and — importantly — a separate paragraph recording that `success` deliberately keeps the
doc's literal uncorrected value even though it is the same hue family as the corrected `primary`,
because `success` renders as tinted text and panel backgrounds rather than as a solid button with
white text, so the threshold that forced `primary`'s correction does not apply to it. Without that
paragraph the two values look like an inconsistency to whoever reads the file next. Third paragraph
records that the doc's second accent is dropped rather than folded, because it is decorative rather
than a status role and because reusing that orange would collapse the distinction between `info`,
`warning` and the dropped accent. Fourth paragraph records the light-preset inverse polarity. The
shape group's comment records that this preset is the direction doc's named radius outlier: the
doc's figure anchors the extra-large step and the whole progression is scaled up from the stock
scale to keep the four steps visibly distinct. The display token references the Nunito variable
followed by the named family and a generic fallback, and the sans token stays the Geist stack — an
explicit override of the doc's own body-font recommendation, recorded in the head comment so it is
not read later as an oversight.

For all three, per D-03, write nothing outside the 23 tokens: no shadow, no border width, no image
aspect, no second accent, no mono font, no letter spacing, no at-rule, no second selector, no
external reference. Every colour value is 6-digit lowercase hex. Do not run the generator in this
task — Task 3 owns regeneration, so a mistake in any of the three files surfaces as a single
validator run over the whole directory rather than three partial ones.
  </action>
  <acceptance_criteria>
    - Each of the three files holds exactly 23 `--store-` declarations inside exactly one brace-opening line, with exactly 17 matching the 6-digit lowercase hex form.
    - Each file's metadata line carries label, industry and synopsis, and the labels are `Retro`, `Atelier` and `Market`.
    - `themes/retro.css` declares `--store-primary: #ff83e6` and `--store-on-primary: #11071f` — the doc's literal values, no correction applied to a preset that needs none.
    - `themes/atelier.css` declares `--store-primary: #a65f46` and `themes/market.css` declares `--store-primary: #008819` — the corrected values, not the doc's literals.
    - `themes/market.css` declares `--store-success: #189a30`, the doc's uncorrected literal, distinct from its corrected `primary`.
    - Each file's display token references the variable name Task 1 exposed for that preset, and each file contains zero at-rules.
    - `themes/market.css` declares `--store-radius-xl: 1rem`, the doc's literal outlier figure.
    - `mise exec -- node scripts/build-themes.mjs` exits 0 over the whole directory, and the contract tests pass with the loop now covering seven files.
  </acceptance_criteria>
  <verify>
    <automated>for f in retro atelier market; do test "$(/usr/bin/grep -c '^\s*--store-' themes/$f.css)" -eq 23 || exit 1; test "$(/usr/bin/grep -c '{' themes/$f.css)" -eq 1 || exit 1; test "$(/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/$f.css)" -eq 17 || exit 1; test "$(/usr/bin/grep -cE '^\s*@(import|font-face|media|supports|charset)' themes/$f.css)" -eq 0 || exit 1; done && /usr/bin/grep -q '@theme label: Retro' themes/retro.css && /usr/bin/grep -q '@theme label: Atelier' themes/atelier.css && /usr/bin/grep -q '@theme label: Market' themes/market.css && /usr/bin/grep -qE '^\s*--store-primary: #ff83e6;$' themes/retro.css && /usr/bin/grep -qE '^\s*--store-on-primary: #11071f;$' themes/retro.css && /usr/bin/grep -qE '^\s*--store-primary: #a65f46;$' themes/atelier.css && /usr/bin/grep -qE '^\s*--store-primary: #008819;$' themes/market.css && /usr/bin/grep -qE '^\s*--store-success: #189a30;$' themes/market.css && /usr/bin/grep -qE '^\s*--store-radius-xl: 1rem;$' themes/market.css && /usr/bin/grep -q 'font-orbitron' themes/retro.css && /usr/bin/grep -q 'font-fraunces' themes/atelier.css && /usr/bin/grep -q 'font-nunito' themes/market.css && mise exec -- node scripts/build-themes.mjs && mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts</automated>
    <fails_when>a per-file count assertion is unequal — token count not 23, more than one rule, a colour not in 6-digit lowercase hex, or an at-rule present; or a corrected-value grep finds nothing, meaning a flagged deviation was reverted to the direction doc's literal accent and the preset now fails AA text contrast on its own buttons; or the retro literal-value greps fail, meaning a correction was applied to a preset the UI-SPEC measured as already passing; or the market `success` grep fails, meaning the deliberate uncorrected value was "fixed" to match `primary`; or a font-variable grep finds nothing, meaning a display token references a name Task 1 did not expose and the face will silently never render; or the generator exits non-zero with a `[build-themes]` file-and-line message; or the contract tests report a parity mismatch between a CSS file and its manifest entry.</fails_when>
  </verify>
  <done>All four of the phase's presets exist as pure 23-token data files, each with its deviations disclosed in its own header, each referencing a display face it does not load, and the unmodified validator accepts the whole directory.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate to seven, prove each new preset switches, run the full gate</name>
  <precondition>The local D1 database is reachable through the project's dev pipeline (`npm run predev` completes and `scripts/db-local-ensure.mjs` succeeds), which the switch probe writes the `appearance.theme` row into.</precondition>
  <read_first>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-01-SUMMARY.md` — the switch-probe method plan 06.1-01 established and the exact request shape it used, so this task repeats it rather than reinventing it.
    - `scripts/build-themes.mjs` lines 200-340 — the manifest scan and codegen, specifically that files are sorted before iteration and that codegen runs only after every file validates, which is what makes the emitted order deterministic and the run atomic.
    - `tests/unit/app/admin-appearance-source.test.ts` lines 37-125 — the hoisted manifest mock and the card-count assertion that derives its expectation from the mocked array length rather than a literal, so this task can confirm the grid is manifest-driven without editing the test.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-CONTEXT.md` — D-08 (the gate list and the deliberately-broken-file spot check) and D-09 (no admin code change expected).
  </read_first>
  <files>themes/index.generated.css, lib/themes/manifest.generated.ts</files>
  <action>
Regenerate both generated files so they carry seven entries, and commit them.

Confirm the emitted order is sorted filename order in both files — atelier, clinical, luxe, market,
midnight, retro, volt-dark — and that all seven labels are distinct strings. That ordering is the
only thing making the admin grid's card sequence stable across regenerations, and distinct labels
are the only thing stopping two cards presenting the same name to an operator.

Confirm regeneration is idempotent: run the generator twice back to back and check the working tree
stays clean on both generated paths, then run check mode against the committed output.

Prove each new preset switches on a real server, repeating plan 06.1-01's method exactly. Start the
dev server; for `retro`, `atelier` and `market` in turn, write the appearance theme setting through
the admin settings endpoint with the dev-bypass header, request the home page, and confirm the
served html element's theme attribute carries that name. Restore `volt-dark` and reconfirm before
stopping the server. Record all four served values.

Confirm the admin grid needed no change: run the existing Appearance render tests unmodified and
confirm `git status --porcelain` reports nothing under the admin component directory. If that
directory is dirty, a preset was accommodated rather than authored correctly — stop and fix the
preset, not the component.

Run the full gate suite named in D-08: lint, typecheck, the token scan, the whole test suite, and a
real production build.

Finally, spot re-check the deploy-path guard once, deliberately and reversibly: temporarily break
one theme file, confirm the worker build script fails rather than shipping it, then restore the file
and re-run clean. This is the check that the validator is wired into the deploy path and not only
into the dev path. Do not change the build scripts to make this easier.
  </action>
  <acceptance_criteria>
    - The barrel has 7 `@import` lines and the manifest has 7 entries, both in sorted filename order `atelier clinical luxe market midnight retro volt-dark`.
    - All seven manifest labels are distinct strings.
    - Check mode exits 0 and `git status --porcelain` is empty on both generated paths after two consecutive generator runs.
    - The served home page carries each of `retro`, `atelier` and `market` in the html element's theme attribute while that name is the stored setting, and `volt-dark` after restore.
    - `git status --porcelain components/admin/` reports nothing — the grid rendered seven cards with no component change.
    - The existing Appearance render tests pass unmodified.
    - `mise exec -- npm run lint`, `typecheck`, `scan:tokens` (0 violations, unchanged manual-review rows), `npm test` and `npm run build` all exit 0.
    - A deliberately broken theme file makes `mise exec -- npm run build:worker` exit non-zero with a `[build-themes]` message; the file and the generated output are restored clean afterwards.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- node scripts/build-themes.mjs && mise exec -- node scripts/build-themes.mjs && test -z "$(git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts)" && mise exec -- node scripts/build-themes.mjs --check && test "$(/usr/bin/grep -c '@import' themes/index.generated.css)" -eq 7 && test "$(/usr/bin/grep -cE '^\s+name: "' lib/themes/manifest.generated.ts)" -eq 7 && test "$(/usr/bin/grep -oE 'name: "[a-z0-9-]+"' lib/themes/manifest.generated.ts | /usr/bin/sed -E 's/name: "(.*)"/\1/' | tr '\n' ' ')" = "atelier clinical luxe market midnight retro volt-dark " && test "$(/usr/bin/grep -oE '\./[a-z0-9-]+\.css' themes/index.generated.css | tr '\n' ' ')" = "./atelier.css ./clinical.css ./luxe.css ./market.css ./midnight.css ./retro.css ./volt-dark.css " && test "$(/usr/bin/grep -cE '^\s+label: "' lib/themes/manifest.generated.ts)" -eq 7 && test "$(/usr/bin/grep -oE '^\s+label: "[^"]+"' lib/themes/manifest.generated.ts | sort -u | wc -l | tr -d ' ')" -eq 7 && test -z "$(git status --porcelain components/admin/)" && mise exec -- npx vitest run tests/unit/app/admin-appearance-source.test.ts && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens && mise exec -- npm test && mise exec -- npm run build</automated>
    <fails_when>the import or entry count is not 7, meaning a preset did not reach the barrel or the manifest; or an order comparison string differs, meaning codegen emitted insertion order rather than sorted filename order and the admin card sequence is not stable; or the unique-label count is below 7, meaning two presets present the same name on the Appearance page; or `git status --porcelain` is non-empty after two generator runs or after check mode, meaning codegen is not idempotent or the committed output is stale; or `git status --porcelain components/admin/` is non-empty, meaning the admin grid was edited to accommodate a preset in violation of D-09; or the Appearance render tests, lint, typecheck, scan, test suite or production build exits non-zero.</fails_when>
    <automated>D=$(mktemp -d) && /bin/cp themes/market.css "$D/market.bak" && /usr/bin/printf '\n[data-theme="market-extra"] { --store-primary: #000000; }\n' >> themes/market.css && ! mise exec -- npm run build:worker > "$D/out.txt" 2>&1; /usr/bin/grep -q 'build-themes' "$D/out.txt"; R=$?; /bin/cp "$D/market.bak" themes/market.css && mise exec -- node scripts/build-themes.mjs && test -z "$(git status --porcelain themes/ lib/themes/)" && test "$R" -eq 0</automated>
    <fails_when>the worker build exits 0 with a second selector present in a theme file (the `!` inverts, so a zero exit here fails the command), meaning the validator is not wired into the deploy path and an invalid theme could ship; or the captured output carries no `[build-themes]` prefix, meaning the failure came from somewhere else and the guard was not what stopped it; or the restore leaves `themes/` or `lib/themes/` dirty, meaning the probe was not reversible.</fails_when>
  </verify>
  <done>Seven presets are in the committed, deterministically ordered manifest and barrel; each of the four new ones is reachable by writing a single D1 row; the admin grid rendered all seven with no code change; the full gate suite is green; and the deploy-path guard has been re-confirmed to reject an invalid theme file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme source file → global stylesheet | Every declaration in a theme file is emitted into the storefront's global CSS served to every visitor. |
| theme header comment → admin Appearance page | Label, industry and synopsis are parsed out of a CSS comment, generated into a TypeScript module, and rendered on an admin page. |
| `next/font/google` build step → self-hosted font asset | Next fetches the three faces at build time and self-hosts them; the browser never contacts a third-party font host at runtime. |
| dev-bypass header → admin settings write | The switch probe writes a settings row through the admin API using the dev-only bypass header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-05 | Tampering | `themes/retro.css`, `themes/atelier.css`, `themes/market.css` | medium | mitigate | The unmodified `scripts/build-themes.mjs` gates all three on `predev`, `build:worker` and in CI, rejecting any at-rule outright via its at-rule walk, any second selector, any unknown property, and any non-hex colour. Task 2's verify asserts zero at-rules per file independently of the validator, and Task 3 re-confirms the deploy-path wiring by breaking a file on purpose and requiring `build:worker` to fail. |
| T-06.1-06 | Information Disclosure | three additional display faces served to storefront visitors | low | mitigate | `next/font/google` resolves and self-hosts the font files at build time; the rendered page contains no third-party font host reference, so no visitor request leaks to an external origin. `preload: false` on all three additionally means a page that does not render text in a face never fetches it. Task 1's verify asserts the `preload: false` count. |
| T-06.1-07 | Information Disclosure | header metadata strings for three more presets entering the browser bundle | low | mitigate | The strings are authored in this repo from the UI-SPEC and carry product copy only. `components/admin/ThemePresetGrid.tsx` renders them as React text children (zero `dangerouslySetInnerHTML` occurrences in that file, unchanged by this plan and asserted by Task 3's clean-tree check on the admin directory), so an edited header cannot inject markup. |
| T-06.1-08 | Elevation of Privilege | `x-dev-admin` bypass used by the three switch probes | low | mitigate | Honoured only in development by `lib/auth/admin-middleware.ts`; the probes run against the local dev server and local D1 fixture, write the same key and category the admin UI writes, and restore `volt-dark` before the server stops. No preview or production database is reachable. |
| T-06.1-09 | Denial of Service | a preset that would ship an invalid stylesheet to production | medium | mitigate | Task 3's deliberate-break probe requires `npm run build:worker` to fail on an invalid theme file, confirming the validator is in the deploy path and not only in the dev path. Codegen is atomic — entries are emitted only after every file validates — so a partially valid directory cannot produce a partially regenerated manifest. |
| T-06.1-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists. The three faces come from `next/font/google`, part of the already-installed Next 16.3.1; the availability probe reads Next's own bundled font-data manifest rather than fetching anything. Zero new dependencies, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| Theme file | `themes/retro.css` (manifest name `retro`, Orbitron display face) |
| Theme file | `themes/atelier.css` (manifest name `atelier`, Fraunces display face) |
| Theme file | `themes/market.css` (manifest name `market`, Nunito display face) |
| Font wiring | `app/layout.tsx` — `--font-orbitron`, `--font-fraunces`, `--font-nunito` |
| Regenerated output | `themes/index.generated.css` (7 imports), `lib/themes/manifest.generated.ts` (7 entries) |
</artifacts_this_phase_produces>

<verification>
Commands the SUMMARY must record, with their observed output:

1. The font-availability probe against Next's bundled font-data manifest, printing each face's available weights, plus the weight superset this plan chose and why.
2. The per-file shape assertions for all three new themes (token count 23, rule count 1, hex count 17, at-rule count 0).
3. `mise exec -- node scripts/build-themes.mjs` twice and `--check`, with the resulting import and entry counts and the emitted sorted order.
4. `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts` showing the loop covering seven files.
5. `mise exec -- npx vitest run tests/unit/app/admin-appearance-source.test.ts`, unmodified.
6. `mise exec -- npm run lint`, `typecheck`, `scan:tokens`, `npm test`, `npm run build`.
7. The switch probe: the served theme attribute for `retro`, `atelier`, `market`, and after restoring `volt-dark`.
8. The deploy-path spot check: the `[build-themes]` message from the deliberately broken file, the non-zero `build:worker` exit, and confirmation of a clean restore.

The SUMMARY must also list, per preset, every token whose value is a deliberate deviation from the
direction doc and its measured before and after ratio, so plan 06.1-04's QA pass knows which values
were chosen rather than derived.
</verification>

<success_criteria>
- All six direction-doc presets plus `volt-dark` ship as pure 23-token data files on one unmodified validator.
- Three display faces load once, build-time and self-hosted, never preloaded, each referenced by exactly one preset.
- No token was added, renamed, or dropped, and no admin, resolver or validator code changed to accommodate a preset.
- Selecting any of the four new presets changes the storefront with a single D1 row and a page reload.
</success_criteria>

<output>
Create `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-02-SUMMARY.md` when done.
</output>
