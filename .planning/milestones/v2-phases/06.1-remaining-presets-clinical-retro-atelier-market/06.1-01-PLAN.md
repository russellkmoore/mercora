---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - themes/clinical.css
  - themes/index.generated.css
  - lib/themes/manifest.generated.ts
  - .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md
autonomous: true
requirements: [THEME-05]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TRACER: one new preset (`clinical`) travels the whole pipeline unaided — authored as a 23-token data file, accepted by the unmodified validator, emitted into the regenerated barrel and manifest, served as the storefront's `data-theme` value after a single D1 setting write, and captured across the route grid — before any other preset is authored (D-01, D-06, D-09)."
    - "`themes/clinical.css` is exactly one `[data-theme=\"clinical\"]` rule holding exactly the 23 frozen `--store-*` tokens, all 17 colour tokens 6-digit lowercase hex, plus a `@theme` header carrying label, industry and synopsis copied verbatim from 06.1-UI-SPEC.md line 244 (D-01)."
    - "Every clinical token value is transcribed from 06.1-UI-SPEC.md's pre-computed Clinical table (lines 210-245), including its one flagged deviation — `primary` darkened from the doc's `oklch(62% .12 200)` to `oklch(54% .12 200)` so white `on-primary` reaches 4.61:1 — and no hex is re-derived from oklch by hand (D-02)."
    - "Clinical's five inverse tokens go dark because its main surfaces are light, mirroring `luxe`'s polarity rather than copying a dark theme's light inverse values (D-04)."
    - "Clinical needs no new font: both `--store-font-sans` and `--store-font-display` carry the existing Geist stack byte-identically, so this preset proves the pipeline with zero `app/layout.tsx` involvement (D-05)."
    - "The direction doc's extra properties are absent from the file: no shadow, border-width, image-aspect, accent-2, mono-font or letter-spacing token; the doc's single 0.5rem radius anchors `radius-lg` and the other three steps reuse volt-dark's stock progression (D-03)."
    - "EDGE/empty: a preset file with zero tokens, an absent `@theme` header, or a non-hex colour is rejected by `scripts/build-themes.mjs` with a `[build-themes]` file-and-line message, and codegen does not run — the validator is exercised as-is, never relaxed to admit a preset."
    - "EDGE/ordering: the regenerated barrel and manifest list themes in sorted filename order — after this plan exactly `clinical, luxe, midnight, volt-dark` in both files — so the admin grid order is deterministic, not insertion-ordered."
    - "EDGE/concurrency: codegen is idempotent — running `build-themes.mjs` twice back to back leaves `git status --porcelain` empty on both generated paths, and `--check` exits 0 against the committed output."
    - "EDGE/adjacency: all shipped labels are distinct strings — the manifest's `label:` line count equals its unique `label:` line count, so no two cards on the admin Appearance page carry the same name."
    - "`.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` exists as this phase's own capture record, created from 06-SCREENSHOTS.md's shape, and the harness is invoked with `--manifest` pointed at it explicitly so no row lands in Phase 5's file (the 06-05 default-path defect is not repeated)."
    - "ASSUMPTION DELTA: `no-change` — this phase adds manifest entries under Phase 6's already-promoted theme-name identity; no assumption is promoted, demoted or contradicted."
    - "UI CONSIDERATION (populated / zero-one-many): the admin card grid renders one card per manifest entry with no hardcoded count, so going from 3 entries to 4 needs no `ThemePresetGrid` change — pinned by the existing render tests, not by a new assertion."
  artifacts:
    - path: "themes/clinical.css"
      provides: "The clinical preset — light, pure-white surfaces, corrected sea-teal primary, Geist for both font tokens"
      contains: "clinical"
    - path: ".planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md"
      provides: "This phase's own screenshot manifest and QA record, following 06-SCREENSHOTS.md's structure"
      contains: "phase-06.1-01-clinical"
  key_links:
    - from: "themes/index.generated.css"
      to: "themes/clinical.css"
      via: "generated relative @import line, regenerated by scripts/build-themes.mjs after the file is added"
      pattern: "\\./clinical\\.css"
    - from: "lib/themes/manifest.generated.ts"
      to: "themes/clinical.css"
      via: "generated manifest entry carrying the parsed header metadata and the 23 token values"
      pattern: "name: \"clinical\""
    - from: "lib/themes/active-theme.ts"
      to: "the served html element's data-theme attribute"
      via: "the appearance.theme D1 setting row resolved per request against the regenerated manifest allow-list"
      pattern: "data-theme=\"clinical\""
  prohibitions:
    - statement: "This phase must NOT add, rename, or remove any of the 23 frozen tokens to express a preset."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. The direction doc's Clinical entry names a border width and a shadow that have no contract slot; the temptation to add one lands on this task."
    - statement: "A theme file must NOT contain an @import, an @font-face, a second selector, or any external resource reference — a theme is data, not code."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. Enforced structurally by the validator, but restated because this is the first file this phase authors by hand."
    - statement: "The validator, the manifest shape, the theme resolver, and the admin grid must NOT be changed to accommodate a preset."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. If clinical fails validation the file is wrong, never the validator."
    - statement: "A display font must NOT be shipped via a runtime third-party link or stylesheet import."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less project prohibition, flagged unverified. Not binding on clinical (Geist only) but stated here because it governs every theme file this phase writes."
---

<objective>
Prove the whole preset pipeline end to end on one file. `clinical` is the thinnest complete slice
available: a light preset with a real contrast correction, an inverted inverse set, and no new font
dependency — so it touches every layer (theme file, validator, codegen, barrel, manifest, resolver,
served attribute, screenshot harness) without needing `app/layout.tsx` at all.

Purpose: if the pipeline is going to reject something about the way this phase authors presets, it
must reject it after one file, not after four. Everything plan 06.1-02 does afterwards is repetition
of a shape that is already known to work.

Output: `themes/clinical.css`, a regenerated four-entry barrel and manifest, and this phase's own
`06.1-SCREENSHOTS.md` carrying the first capture label.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-CONTEXT.md
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md
@.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-PATTERNS.md
@.planning/phases/06-theme-file-mechanism-presets/06-03-SUMMARY.md
</context>

<interfaces>
This plan authors data and consumes existing code unchanged. Fixed names it depends on:

- `scripts/build-themes.mjs` — the validator plus codegen. Flags: none (write), `--check`
  (freshness gate, no write). Message prefix `[build-themes]`. Its `REQUIRED_TOKENS` array is the
  canonical 23-name list; its `NAME_RE` restricts filename stems to lowercase alphanumerics and
  hyphens; its selector rule requires `[data-theme="<stem>"]` exactly once per file.
- `themes/index.generated.css` and `lib/themes/manifest.generated.ts` — generated, never
  hand-edited. Barrel imports use an explicit `./` prefix (06-01: a bare specifier breaks the real
  Next build because Tailwind resolves it as a Node module lookup).
- `APPEARANCE_SETTINGS_CATEGORY` (`"appearance"`) and `APPEARANCE_THEME_SETTING_KEY`
  (`"appearance.theme"`) from `lib/themes/active-theme.ts` — the setting category and key the
  switch probe writes.
- `x-dev-admin: mercora-dev-bypass` — the documented dev-only admin bypass header
  (`lib/auth/admin-middleware.ts`), the only way to reach `POST /api/admin/settings` in an
  environment with no Clerk session. This is the method 06-04 and 06-05 both used.
- `scripts/screenshot-routes.mjs` — flags `--label`, `--manifest`, `--allow-missing`,
  `--include-content`, `--base-url`. `--manifest` defaults to Phase 5's file and MUST be passed.

It produces one new manifest name, `clinical`, which plans 06.1-02 and 06.1-04 both build on.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end — one new preset reaches the served page unaided</name>
  <read_first>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 210-245 — the complete Clinical token table: every token, its value, its provenance, the one flagged `primary` deviation with its measured before/after ratio, the radius-anchoring reasoning, and the verbatim header comment string on line 244. Transcribe these values; do not re-derive any hex from oklch.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 184-208 — the rename map and the contrast-check method, specifically the row explaining that Clinical's `border` comes from the doc's own named border colour rather than the usual `surface-sunken` repurposing, and that `surface-sunken` is therefore dropped.
    - `themes/luxe.css` — the exact file shape to mirror for a light preset: head comment paragraphs (purpose, conversion source, deviation disclosure, inverse-polarity note, no-font-loading note), the `@theme` metadata line, then one selector block with four commented groups in order (Main colours, Inverse colours, Shape, Type).
    - `themes/volt-dark.css` — the stock four-step radius progression Clinical reuses (`0.25rem / 0.375rem / 0.5rem / 0.75rem`) and the exact Geist font stack string both of Clinical's font tokens copy byte-for-byte.
    - `scripts/build-themes.mjs` lines 34-58 and 165-220 — the `REQUIRED_TOKENS` list, the colour-token hex rule, the one-rule/one-selector structural rule, and the `@theme` header requirement, so the file is written to pass first time.
    - `lib/themes/active-theme.ts` lines 20-60 — the setting category and key constants and the resolution order the switch probe exercises.
  </read_first>
  <files>themes/clinical.css, themes/index.generated.css, lib/themes/manifest.generated.ts</files>
  <action>
Author `themes/clinical.css` mirroring `themes/luxe.css`'s structure exactly, then regenerate and
prove the result reaches a served page. This single task deliberately spans every layer — it is the
tracer, not a layer slice.

File contents, per D-01 and D-02: a head comment naming the theme, the 23-token contract, the
direction-doc section it converts from (section 3) and the UI-SPEC section that pre-computed the
conversion; then a dedicated paragraph disclosing the one deliberate deviation — the doc's literal
sea-teal accent measures only 3.38:1 against the doc's own white accent-ink and fails the AA text
threshold for a solid button label, so `primary` is darkened along the same hue and chroma to the
UI-SPEC's corrected value, measuring 4.61:1; then, following luxe's precedent, a paragraph stating
that because this theme's main surfaces are light its five inverse tokens go dark (the opposite
polarity from every dark theme, per Phase 5 D-06 and D-04); then a paragraph stating the file
declares no font-loading at-rule and that both font tokens carry the existing Geist stack because
the direction doc names Geist itself as an acceptable face for this preset.

Then the `@theme` metadata line copied verbatim from the UI-SPEC's Clinical header string — label,
industry and synopsis, no paraphrasing.

Then one `[data-theme="clinical"]` rule holding all 23 tokens in luxe's group order with luxe's group
comments. Use the light-preset inverse-group comment wording (dark panels on a light app: drawers,
Stripe, emails). The Shape group carries a one-line comment recording that the doc's single 0.5rem
figure lands exactly on volt-dark's own `radius-lg` step, so that step takes the doc's literal value
and the surrounding three reuse volt-dark's stock progression rather than inventing a scale. The
Type group carries a one-line comment recording that both tokens are the Geist stack because the doc
names Geist directly, matching the midnight precedent of not introducing a face.

Per D-03 write nothing outside the 23: no shadow, no border width, no image aspect, no second
accent, no mono font, no letter spacing, no at-rule, no second selector, no external reference.
Every colour value is 6-digit lowercase hex.

Then regenerate both generated files with the build script and commit them alongside the new theme
file. Confirm the barrel's four `@import` lines and the manifest's four entries are in sorted
filename order (clinical, luxe, midnight, volt-dark) — that ordering is what makes the admin grid's
card order deterministic rather than insertion-dependent.

Then prove the switch takes effect on a real server. Start the dev server, write `appearance.theme`
= `clinical` through `POST /api/admin/settings` using the dev-bypass header (the same mechanism
06-04's own end-to-end proof used, since no Clerk session exists here), request the home page, and
confirm the served HTML carries the clinical name in the html element's theme attribute. Restore the
setting to `volt-dark` and reconfirm before stopping the server. Record both served values in the
SUMMARY.

Exercise the validator's rejection path once, deliberately and reversibly, so the empty/invalid edge
is measured rather than assumed: temporarily strip one required token from the new file, run the
build script, capture the `[build-themes]` file-and-line message it prints and its non-zero exit,
then restore the file and re-run clean. Record the captured message text in the SUMMARY. Do not
change the validator to make this easier.
  </action>
  <acceptance_criteria>
    - `themes/clinical.css` holds exactly 23 `--store-` declarations inside exactly one brace-opening line, with exactly 17 of them matching the 6-digit lowercase hex form.
    - The file's `@theme` header carries label, industry and synopsis, and the label string is `Clinical`.
    - `primary` and `ring` both equal the UI-SPEC's corrected value, and `on-primary` is pure white — i.e. the flagged deviation was honoured, not silently reverted to the doc's literal accent.
    - The four radius declarations are `0.25rem`, `0.375rem`, `0.5rem`, `0.75rem` in that order.
    - Both font tokens are byte-identical to `themes/volt-dark.css`'s two font token values.
    - `scripts/build-themes.mjs` exits 0 and regenerates a barrel with 4 `@import` lines and a manifest with 4 entries, both in sorted filename order `clinical luxe midnight volt-dark`.
    - `--check` exits 0 and `git status --porcelain` is empty on both generated paths after two consecutive generator runs.
    - Manifest label strings are all distinct (label line count equals unique label line count).
    - The served home page carries `data-theme="clinical"` while the setting says clinical, and `data-theme="volt-dark"` after restore.
    - A deliberately invalidated copy of the file makes the build script exit non-zero and print a `[build-themes]` message naming the file and a line number; the file is restored afterwards and the tree is clean.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c '^\s*--store-' themes/clinical.css)" -eq 23 && test "$(/usr/bin/grep -c '{' themes/clinical.css)" -eq 1 && test "$(/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/clinical.css)" -eq 17 && /usr/bin/grep -q '@theme label: Clinical' themes/clinical.css && test "$(/usr/bin/grep -cE '^\s*--store-(primary|ring): #00828a;$' themes/clinical.css)" -eq 2 && test "$(/usr/bin/grep -cE '^\s*@(import|font-face|media|supports|charset)' themes/clinical.css)" -eq 0 && test "$(/usr/bin/grep -E '^\s*--store-font-(sans|display):' themes/clinical.css | /usr/bin/sed -E 's/^[[:space:]]*//')" = "$(/usr/bin/grep -E '^\s*--store-font-(sans|display):' themes/volt-dark.css | /usr/bin/sed -E 's/^[[:space:]]*//')" && mise exec -- node scripts/build-themes.mjs && mise exec -- node scripts/build-themes.mjs --check && test -z "$(git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts)" && test "$(/usr/bin/grep -c '@import' themes/index.generated.css)" -eq 4 && test "$(/usr/bin/grep -cE '^\s+name: "' lib/themes/manifest.generated.ts)" -eq 4 && test "$(/usr/bin/grep -oE 'name: "[a-z0-9-]+"' lib/themes/manifest.generated.ts | /usr/bin/sed -E 's/name: "(.*)"/\1/' | tr '\n' ' ')" = "clinical luxe midnight volt-dark " && test "$(/usr/bin/grep -oE '\./[a-z0-9-]+\.css' themes/index.generated.css | tr '\n' ' ')" = "./clinical.css ./luxe.css ./midnight.css ./volt-dark.css " && test "$(/usr/bin/grep -cE '^\s+label: "' lib/themes/manifest.generated.ts)" -eq "$(/usr/bin/grep -oE '^\s+label: "[^"]+"' lib/themes/manifest.generated.ts | sort -u | wc -l | tr -d ' ')" && mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens</automated>
    <fails_when>a count assertion is unequal — the token count is not 23, more than one rule was written, a colour is not 6-digit lowercase hex, or the barrel/manifest entry count is not 4; or the order comparison string differs, meaning codegen did not emit sorted filename order; or the corrected-value grep finds fewer than 2 matches, meaning the flagged `primary`/`ring` deviation was reverted to the doc's literal accent; or the at-rule grep count is non-zero, meaning a font-loading rule was written into a theme file; or `--check` exits non-zero or `git status --porcelain` is non-empty, meaning committed output is stale or codegen is not idempotent; or the label uniqueness comparison is unequal; or vitest reports a failing parity assertion between the CSS file and its manifest entry; or lint/typecheck/scan exits non-zero.</fails_when>
    <automated>D=$(mktemp -d) && /bin/cp themes/clinical.css "$D/clinical.bak" && /usr/bin/sed -i '' '/--store-ring:/d' themes/clinical.css && ! mise exec -- node scripts/build-themes.mjs > "$D/out.txt" 2>&1; /usr/bin/grep -q 'build-themes' "$D/out.txt" && /usr/bin/grep -q 'clinical.css' "$D/out.txt"; R=$?; /bin/cp "$D/clinical.bak" themes/clinical.css && mise exec -- node scripts/build-themes.mjs && test -z "$(git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts)" && test "$R" -eq 0</automated>
    <fails_when>the build script exits 0 on a file missing a required token (the `!` inverts, so a zero exit here fails the command), meaning the validator admitted an incomplete preset; or the captured output carries no `[build-themes]` prefix and no `clinical.css` filename, meaning the rejection message does not locate the fault; or the restore leaves the generated files dirty, meaning the probe was not reversible.</fails_when>
  </verify>
  <done>A fourth preset exists as a pure 23-token data file, the unmodified validator accepts it, codegen emits it into a deterministically ordered barrel and manifest, one D1 row makes the storefront serve it, and the validator's rejection path has been measured rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: This phase's own screenshot record, opened with the clinical capture grid</name>
  <precondition>Playwright's chromium browser is installed and launchable (the harness `browser.launch()`s it), and the local D1 fixture resolves at least one category slug and one product slug from `/sitemap.xml` — the same thin fixture Phase 6's capture runs used.</precondition>
  <read_first>
    - `.planning/phases/06-theme-file-mechanism-presets/06-SCREENSHOTS.md` lines 1-45 — the opening paragraph (why the file is phase-scoped, that images live under the git-ignored `.screenshots/` and only the manifest is committed), the capture-command line, the theme-switch method paragraph naming the dev-bypass header and the `curl | grep data-theme` confirmation, and the per-run table columns. Copy this structure.
    - `scripts/screenshot-routes.mjs` lines 20-50 and 103-145 — the flag list, the `--manifest` default that must be overridden, the seven-route grid with its two viewports and interactive states, the three additional `--include-content` cells, and the `missingReason` mechanism that makes an unresolvable route a MISSING row.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-UI-SPEC.md` lines 141-157 — the `## UI Considerations` table, whose rows this phase's record must eventually answer; this task records the two that are already answerable (populated card count, zero-one-many grid growth) and names the rest as open.
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-CONTEXT.md` — D-06 (capture method and record location) and D-09 (no admin code change; verify cards render).
  </read_first>
  <files>.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md</files>
  <action>
Create this phase's own screenshot record and open it with the tracer preset's capture grid.

Write the file following 06-SCREENSHOTS.md's structure: an opening paragraph explaining this is a
new phase-scoped record rather than an append to Phase 6's, that captured images live under the
git-ignored screenshots directory and are never committed, and that only the manifest rows are
committed. Include the capture command line with the manifest flag written out in full and pointed
at this file — the harness's default targets Phase 5's file, and 06-05 hit exactly that defect and
had to move rows before committing. Include the theme-switch method paragraph naming the settings
endpoint, the dev-bypass header, and the confirmation request made before each run. Include the
per-run table with the columns Phase 6 used: run number, label, theme written to D1, the confirmed
served theme attribute, cells captured and cells missing.

Then run the capture for clinical. Write the appearance setting to clinical, confirm the served theme
attribute, then run the harness with this file as the manifest, the content cells included, and
missing cells allowed — the local fixture seeds no order, so the order-status cells are the same
carried-forward gap Phase 6 recorded. Restore the setting to volt-dark afterwards and confirm.

Record in the run table the label used, the confirmed served attribute, and the captured and missing
cell counts the harness printed. Add a short carried-forward note naming the order-status gap and its
cause, worded the way Phase 6's record words it.

Then add two sections this record will grow through the phase:

A findings section, empty for now apart from its column headers, using the exact Findings-table
columns 06-SCREENSHOTS.md established (number, site, file and line, class in play, what it looks
like under the preset, judgement, reason). Plan 06.1-04 fills it for the three light presets. State
in one line that the judgement column records accepts as well as changes, per D-07.

A UI-considerations section answering the two rows from the UI-SPEC that this plan can already
answer with evidence — that the card grid renders one card per manifest entry with no hardcoded
count, now demonstrated at four entries; and that the responsive grid absorbed the growth with no
layout change — and listing the remaining rows (long-text overflow at the real synopses, the three
light-preset acid tests, the font-display assumption) as open, each naming the plan that closes it.

Finally record the assumption-delta result for this phase as `no-change`, with the one-line reason:
this phase adds manifest entries under the theme-name identity Phase 6 already promoted, so nothing
is promoted, demoted or contradicted.
  </action>
  <acceptance_criteria>
    - `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` exists and contains a `## Label: \`phase-06.1-01-clinical\`` section with a captured-cell table.
    - No row from this phase's capture run appears in `.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md` — that file is byte-identical to its committed state.
    - The record names the capture command including the explicit manifest flag, and the theme-switch method including the dev-bypass header.
    - The run table records the label, the confirmed served theme attribute, and the captured and missing cell counts.
    - A findings table with the Phase 6 column headers exists, and a line states that accepts are recorded as well as changes.
    - The UI-considerations section answers the populated and zero-one-many rows with evidence and names the plan that closes each remaining row.
    - The assumption-delta result is recorded as `no-change` with its reason.
  </acceptance_criteria>
  <verify>
    <automated>M=.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md && test -f "$M" && /usr/bin/grep -q 'phase-06.1-01-clinical' "$M" && /usr/bin/grep -q -- '--manifest' "$M" && /usr/bin/grep -q 'x-dev-admin' "$M" && /usr/bin/grep -q 'no-change' "$M" && test "$(/usr/bin/grep -c '^| home | 1280 |' "$M")" -ge 1 && test -z "$(git status --porcelain .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md)" && test "$(/usr/bin/grep -c 'phase-06.1' .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md)" -eq 0</automated>
    <fails_when>the record file is absent; or the clinical label section is missing, meaning the capture run did not append here; or the manifest-flag or dev-bypass strings are absent, meaning the method paragraph was not written; or no home-route capture row exists, meaning the harness recorded zero cells; or `git status --porcelain` on Phase 5's screenshot file is non-empty, or that file contains a `phase-06.1` label, meaning the harness's default manifest path was used and rows leaked into the wrong phase's record.</fails_when>
  </verify>
  <done>The phase has its own screenshot record, the tracer preset is captured across the route grid inside it, Phase 5's record is untouched, and the two answerable UI-consideration rows carry evidence instead of a promise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme source file → global stylesheet | Every declaration in a theme file is emitted into the storefront's global CSS served to every visitor. |
| theme header comment → admin Appearance page | Label, industry and synopsis are parsed out of a CSS comment, generated into a TypeScript module, and rendered on an admin page. |
| dev-bypass header → admin settings write | The switch probe writes a settings row through the admin API using the dev-only bypass header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-01 | Tampering | `themes/clinical.css` | medium | mitigate | The unmodified `scripts/build-themes.mjs` gates the file on `predev`, `build:worker` and in CI: one rule, one selector, no at-rule, no unknown property, hex-only colours. Task 1's own verify runs the generator and additionally measures the rejection path on a deliberately invalidated copy, so a file that could smuggle an external resource reference cannot pass green. |
| T-06.1-02 | Information Disclosure | header metadata strings entering `lib/themes/manifest.generated.ts` and the browser bundle | low | mitigate | The strings are authored in this repo and copied from the UI-SPEC; they carry product copy only. `components/admin/ThemePresetGrid.tsx` renders them as React text children (confirmed: zero `dangerouslySetInnerHTML` occurrences in that file), so an edited header cannot inject markup into the admin page. |
| T-06.1-03 | Elevation of Privilege | `x-dev-admin` bypass used by the switch probe | low | mitigate | The header is honoured only in development by `lib/auth/admin-middleware.ts`; the probe runs against the local dev server and the local D1 fixture, writes the same key and category the admin UI writes, and restores `volt-dark` before the server stops. No preview or production database is reachable from this task. |
| T-06.1-04 | Spoofing | a preset name colliding with or shadowing an existing theme | low | mitigate | The validator requires the selector name to equal the filename stem and restricts stems to lowercase alphanumerics and hyphens; a case-sensitive filesystem forbids two files with the same stem. Task 1 additionally asserts that all manifest labels are distinct, so no two admin cards can present the same name. |
| T-06.1-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan or this phase — it adds CSS data files, three `next/font/google` declarations for already-installed Next, and regenerated build output. Zero new dependencies, so the package-legitimacy gate has nothing to audit. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| Theme file | `themes/clinical.css` (manifest name `clinical`) |
| Manifest entry | `clinical` — joining `luxe`, `midnight`, `volt-dark` |
| Regenerated output | `themes/index.generated.css` (4 imports), `lib/themes/manifest.generated.ts` (4 entries) |
| Capture record | `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` |
| Capture label | `phase-06.1-01-clinical` |
</artifacts_this_phase_produces>

<verification>
Commands the SUMMARY must record, with their observed output:

1. The per-file shape assertions on `themes/clinical.css` (token count 23, rule count 1, hex count 17).
2. `mise exec -- node scripts/build-themes.mjs` and `--check`, with the resulting import and entry counts and the emitted sorted order.
3. The deliberate-invalidation probe: the exact `[build-themes]` message text and the non-zero exit, plus confirmation the file and generated output were restored clean.
4. `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts`.
5. `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm run scan:tokens` (expect 0 violations, unchanged manual-review rows).
6. The switch probe: the served theme attribute value with `clinical` stored, and again after restoring `volt-dark`.
7. `mise exec -- npm run screenshot:routes -- --label phase-06.1-01-clinical --allow-missing --include-content --manifest .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md`, with the captured and missing cell counts.

The SUMMARY must also state, in one line each: which clinical token value is a deliberate deviation
and its measured ratio, and that clinical introduced no new font dependency.
</verification>

<success_criteria>
- One new preset travels the entire pipeline unaided, on the unmodified validator, resolver and admin grid.
- The barrel and manifest are deterministically ordered and idempotently regenerated.
- The validator's rejection behaviour is measured, not assumed.
- This phase's screenshot record exists, is the harness's actual target, and Phase 5's record is untouched.
</success_criteria>

<output>
Create `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-01-SUMMARY.md` when done.
</output>
