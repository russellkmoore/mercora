---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
plan: 04
subsystem: theming
tags: [visual-qa, screenshot-harness, light-preset, accessibility-contrast, dropdown-highlight]

# Dependency graph
requires:
  - phase: 06.1-01
    provides: "themes/clinical.css, this phase's own 06.1-SCREENSHOTS.md opened with the clinical tracer capture"
  - phase: 06.1-02
    provides: "themes/retro.css, themes/atelier.css, themes/market.css; three new next/font display faces loaded in app/layout.tsx"
  - phase: 06.1-03
    provides: "Both frozen type tokens (--store-font-sans, --store-font-display) wired site-wide per D-10; S-TYPE-01 snap registered; WINDOWS #1 closed"
provides:
  - "Canonical capture grids for all four new presets (phase-06.1-04-clinical/retro/atelier/market), each confirmed served before capture, each with a live heading-face read-back distinguishing the loaded webfont from its fallback"
  - "22-row Findings table (F1-F22) covering Clinical's hover-inversion acid test, Atelier's no-card-fills acid test, Market's radius-reach acid test, all four presets' heading-typography judgements, the Phase 6 scrim re-checks under each light preset, and the cross-check pass"
  - "components/ui/select.tsx — SelectItem's highlighted-row background fixed from focus:bg-surface-elevated (byte-identical to its own popover, invisible on every theme) to focus:bg-primary/10 (token-driven, visibly distinct on every theme)"
  - ".planning/todos/pending/theme-direction-doc-backlog-06.1.md — per-preset dropped-properties backlog for clinical/retro/atelier/market, extending Phase 6's equivalent note"
  - "THEME-05 marked complete in REQUIREMENTS.md and ROADMAP.md; Phase 06.1 closed with named command evidence against all four ROADMAP success criteria"
affects: [7, 8]

# Actuals (#2632)
actuals:
  tokens: 16400
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Live-DOM hover/focus verification via Playwright's real page.hover()/focus() (not just a computed-style read) as the primary evidence for Clinical's hover-inversion acid test — catches cases a purely analytical class-name check would miss, and is what surfaced the SelectItem defect."
    - "Live-DOM compositing injection (Phase 6's method) reused for sites this environment has no live trigger for: the desktop mega-menu's child-category link (local fixture has 1 category, no children) and the category hero overlay (no configured category image)."
    - "Measure real content before trusting a plan's own stated estimate — the 06.1-04 plan's must_haves cited a 54-char longest industry line; the real manifest's longest is retro's at 72 chars. Recorded the correction rather than silently validating the wrong number."

key-files:
  created:
    - .planning/todos/pending/theme-direction-doc-backlog-06.1.md
  modified:
    - .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - components/ui/select.tsx

key-decisions:
  - "SelectItem's focus highlight was byte-identical to its own SelectContent container background (both focus:bg-surface-elevated / bg-surface-elevated) — a real, cross-theme bug (not clinical-specific) discovered while inspecting D-07's named 'dropdown items' checklist item. Fixed to focus:bg-primary/10, a token-driven utility swap requiring no sentinel and no theme-file change; confirmed live via the checkout country Select before and after."
  - "Atelier's ProductCard fill (bg-surface-elevated) is kept even though the direction doc wants 'no card fills' — per-theme component/markup overrides are rejected on principle (PROJECT.md Key Decision), so a borderless-card variant scoped to Atelier alone would violate that decision. Recorded as an accepted, reasoned 'leave it,' not a silent gap."
  - "Market's Badge/chip radius reach (doc's own named test) has no live consumer anywhere on the storefront today (Badge is dead code outside admin/) — accepted as 'leave it, no live consumer to exercise' rather than fabricating a component just to test it, since the identical rounded-md utility is already confirmed resolving correctly on Input/Button."
  - "The desktop nav mega-menu's child-category link hover (hover:bg-surface-elevated/50) and the category hero overlay under each light preset both used compositing-injection substitute evidence rather than a live trigger, since the local D1 fixture has exactly 1 category with no children and no configured image — flagged as lower-confidence evidence in the record rather than silently upgraded to a full pass."
  - "The plan's own must_haves estimate for the longest new industry line (54 chars) was wrong — retro's is actually 72 chars. Recorded the real measurement and the correction explicitly rather than validating against the plan's stated number."

patterns-established:
  - "A theme-file phase's own closing QA pass should hover/focus real interactive elements via a real browser automation tool, not just diff class names against a token map — the one real defect this plan found (the Select highlight) was invisible to every other gate (lint, typecheck, scan:tokens, the full test suite, and a production build all passed with the bug present)."

requirements-completed: [THEME-05]

coverage:
  - id: D1
    description: "All four new presets are captured across the full seven-route grid at both viewports and interactive states while genuinely served (confirmed data-theme attribute before capture), each with a heading-face read-back confirming the loaded webfont (not its fallback) actually rendered, and the long-text UI-consideration answered at real content size with the real 54-vs-72-char discrepancy recorded"
    requirement: THEME-05
    verification:
      - kind: e2e
        ref: "phase-06.1-04-clinical/retro/atelier/market route-grid captures (28 cells each, 4 missing order-status, unchanged carried-forward gap)"
        status: pass
      - kind: integration
        ref: "per-preset live heading-face read-back: document.fonts.check() at the heading's own rendered weight (800) confirms the loaded webfont (Geist/Orbitron/Fraunces/Nunito) resolved, not the fallback family, for all four presets"
        status: pass
      - kind: other
        ref: "live-DOM compositing measurement of the real ThemePresetGrid card markup against the real seven-entry manifest at both card widths -- no overflow or overlap for retro's 72-char industry line or market's 165-char synopsis"
        status: pass
      - kind: other
        ref: "mise exec -- npm run scan:tokens (0 violations, 2 unchanged manual-review rows); git status --porcelain app/ components/ themes/ lib/ empty for this task"
        status: pass
    human_judgment: false
  - id: D2
    description: "Each light preset (clinical, atelier, market) was tested against the acid test its own direction-doc entry named, using real mouse hover/focus against a live dev server plus compositing injection for sites with no reachable live trigger; every judgement -- change or accept -- is recorded with its evidence, and one real cross-theme defect (the Select dropdown highlight) was found and fixed"
    requirement: THEME-05
    verification:
      - kind: integration
        ref: "22-row Findings table (F1-F22) in 06.1-SCREENSHOTS.md, each with site, class, observed behavior under the preset, judgement, and reason; live hover/focus reads via Playwright for reachable sites, compositing injection for the two sites this environment cannot reach live (nav mega-menu child link, category hero overlay)"
        status: pass
      - kind: unit
        ref: "mise exec -- npm run lint (0 errors, 52 pre-existing warnings) / typecheck (clean) / scan:tokens (0 violations) / npm test (1959/1959 pass) / npm run build (exit 0) -- all re-run fresh after the select.tsx fix"
        status: pass
    human_judgment: true
    rationale: "The plan's own Task 2 <verify> carries an explicit <human-check> (open a real browser, hover header nav/product card/list row/dropdown item under each light preset, confirm the hovered state is obviously different from resting, especially under clinical; confirm modal/drawer backdrops recede; confirm each heading face reads right on its own background). This session substituted the closest available equivalent evidence -- real Playwright mouse hover/focus against a live dev server (not merely a computed-style diff), screenshots reviewed by the executor, and compositing injection for genuinely unreachable sites -- but a human's own eye on the live rendered UI has not confirmed it, so this is not auto-passable."
  - id: D3
    description: "The phase answers its own four ROADMAP success criteria with named command evidence, records the direction-doc properties and layout behaviours it deliberately did not carry per preset, closes every remaining UI-SPEC consideration row, and marks THEME-05 complete in both the requirements table and the roadmap"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "06.1-SCREENSHOTS.md Task 3 phase-close section: all four ROADMAP criteria quoted and answered; theme-direction-doc-backlog-06.1.md names every dropped property per preset plus the layout behaviours routed to Phase 7"
        status: pass
      - kind: other
        ref: "grep -qE '^- \\[x\\] \\*\\*THEME-05\\*\\*' REQUIREMENTS.md; grep -qE '^\\| THEME-05 \\|.*\\| Complete \\|' REQUIREMENTS.md; grep -c '06.1-0[1-4]-PLAN.md' ROADMAP.md == 4; mise exec -- node scripts/build-themes.mjs --check (7 themes, fresh, clean tree); sorted-order and distinct-label (7) assertions on the manifest"
        status: pass
      - kind: unit
        ref: "full gate suite re-run fresh at close: lint / typecheck / scan:tokens / npm test (1959/1959) / npm run build -- all exit 0"
        status: pass
    human_judgment: false

# Metrics
duration: 45min
completed: 2026-09-05
status: complete
---

# Phase 6.1 Plan 4: Canonical Captures, Light-Preset Acid Tests, and Phase Close Summary

**All four new presets are captured while genuinely served with their display faces proven live, the three light presets' hover/scrim/border acid tests are recorded across 22 findings (one real cross-theme bug found and fixed — a `Select` dropdown highlight invisible on every theme, not just clinical), and Phase 6.1 closes with THEME-05 marked complete against named command evidence.**

## Performance

- **Duration:** ~45 min
- **Started:** 2026-09-05T06:16:00Z (approx.)
- **Completed:** 2026-09-05T06:51:00Z
- **Tasks:** 3 completed
- **Files modified:** 5 (1 created, 4 modified)

## Accomplishments

- Captured `phase-06.1-04-clinical`, `-retro`, `-atelier`, `-market` across the full seven-route grid at both viewports and all interactive states, plus the `--include-content` cells (28 cells captured, 4 missing `order-status` per run — the unchanged carried-forward gap). Each preset's served `data-theme` attribute was confirmed via `curl` before capture, and while each preset was served, the page-title `h1`'s computed font family was read back live on the home and product routes and confirmed via `document.fonts.check()` at the heading's own rendered weight (800) — Geist (clinical), Orbitron (retro), Fraunces (atelier), and Nunito (market) all resolved as the loaded webfont, not the token's fallback family. Explained why `clinical` appears under two labels (`phase-06.1-01-clinical` predates plan 06.1-03's type-wiring; `phase-06.1-04-clinical` is this plan's like-for-like canonical grid).
- Measured — rather than trusted — the real longest industry/synopsis lines from the seven-entry manifest and found the plan's own `must_haves` estimate wrong: retro's industry line is 72 characters, not the cited 54. Confirmed via live-DOM compositing injection of the real `ThemePresetGrid` card markup at both 1280px and 390px widths that neither the industry line (no clamp/truncate class, contrary to the UI-SPEC's assumption) nor the synopsis (`line-clamp-3`) overflows or overlaps for any of the four new presets — judgement: leave it, with the corrected measurement recorded.
- Ran the full Light-Preset QA Checklist for `clinical` (light-theme hover inversion), `atelier` (no-card-fills), and `market` (radius-reach) against a live `npm run dev` server, using real Playwright mouse hover/focus (not just computed-style reads) wherever a live trigger existed, and live-DOM compositing injection for the two sites this environment cannot reach live (the desktop mega-menu's child-category link — the local fixture has 1 category with no children — and the category hero overlay, whose category has no configured image). Recorded 22 findings (F1-F22): the three named acid tests, all four presets' heading-typography judgements (including retro's, judged per the plan's own instruction even though it's a dark preset), the Phase 6 scrim re-checks under each light preset, and the cross-check pass. 21 of 22 findings are "leave it" with their own evidence; one is a real, fixed defect.
- **Found and fixed a real cross-theme bug** while inspecting D-07's named "dropdown items" checklist item: `components/ui/select.tsx`'s `SelectItem` highlighted-row background (`focus:bg-surface-elevated`) was byte-identical to its own `SelectContent` popover's container background (also `bg-surface-elevated`), so the "highlighted" row never visibly differed from its own popover on **any** shipped theme — confirmed live via the checkout country `Select` (hovered option background === listbox parent background, byte for byte, both before and independently re-confirmed after). Fixed to `focus:bg-primary/10`, a fully token-driven utility swap needing no sentinel and touching no theme file; confirmed live afterward that the highlighted option now visibly differs from its popover.
- Wrote `.planning/todos/pending/theme-direction-doc-backlog-06.1.md`, recording per preset what the direction doc asked for that the frozen 23-token contract does not carry (clinical's shadow scale; retro's hard-offset shadow/hover-lift, 2px border width, and its own accent-2's fate; atelier's shadow-on-hover and zero border width; market's dropped accent-2/promo strip and its overridden body-font recommendation), plus the per-theme layout behaviours (Atelier's masonry grid, Market's dense four-across grid with steppers/chips) routed to Phase 7's enumerated variants instead of per-theme markup.
- Closed the phase: wrote the phase-close roll-up quoting and answering all four ROADMAP success criteria with named command evidence, closed every remaining `06.1-UI-SPEC.md` UI-consideration row, updated the carried-forward table (closing the `font-display`/WINDOWS #1 item as already resolved at 06.1-03, keeping WINDOWS #2/#3 and the Phase 5 screenshot gaps accurate), and marked THEME-05 complete in both `REQUIREMENTS.md` (checkbox + coverage table row) and `ROADMAP.md`'s Phase 06.1 entry (all four plans checked). Re-ran the full gate suite fresh at close: `build-themes.mjs --check` (7 themes fresh, clean tree, sorted order, 7 distinct labels), the admin-appearance render test (19/19, unmodified), `scan:tokens` (0 violations), lint (0 errors), typecheck (clean), the full test suite (1959/1959 pass), and a real production build — all green.

## Task Commits

Each task was committed atomically:

1. **Task 1: Canonical capture grids for all four new presets** - `72d3b1c` (docs)
2. **Task 2: The light-preset acid tests — inspect, judge, record every call** - `d5aac2c` (fix)
3. **Task 3: Phase-close roll-up, per-preset backlog, THEME-05 marked complete** - `cec0b79` (docs)

**Plan metadata:** (this commit)

## Files Created/Modified

- `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` - four new capture-grid label sections, the 22-row Findings table, closed UI Considerations, and the phase-close roll-up
- `components/ui/select.tsx` - `SelectItem`'s highlight fixed from `focus:bg-surface-elevated` to `focus:bg-primary/10`
- `.planning/todos/pending/theme-direction-doc-backlog-06.1.md` - per-preset dropped-properties backlog (new file)
- `.planning/REQUIREMENTS.md` - THEME-05 marked complete (checkbox + coverage table)
- `.planning/ROADMAP.md` - Phase 06.1's plan list fully checked, success criteria answered

## Decisions Made

- **The `Select` dropdown highlight fix is a component-level, token-driven utility swap** (`focus:bg-primary/10`), not a token or theme-file change — confirmed live on `clinical` and applies identically to all seven themes since the underlying bug (item background matching its own container) was theme-independent.
- **Atelier's `ProductCard` fill is kept, not special-cased** — per-theme component/markup overrides are rejected on principle (PROJECT.md Key Decision); the direction doc's "no card fills" ideal is accepted as unreachable within this contract, not silently dropped.
- **Market's `Badge`/chip radius test has no live consumer** — recorded as "leave it, no live consumer to exercise" rather than inventing markup just to test it, since the identical `rounded-md` utility already resolves correctly on `Input`/`Button`.
- **The plan's own 54-char "longest industry line" estimate was corrected to 72 (retro's actual line)** — measured from the real manifest rather than trusted from the plan text.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `SelectItem`'s highlighted-row background was invisible against its own popover on every theme**
- **Found during:** Task 2, while inspecting D-07's "dropdown items" checklist item for Clinical
- **Issue:** `components/ui/select.tsx`'s `SelectItem` used `focus:bg-surface-elevated`, byte-identical to its own `SelectContent` container's `bg-surface-elevated` (line ~63). The "highlighted" state therefore never visibly differed from the popover it sits inside, on any shipped preset — confirmed live via the checkout country `Select` (hovered option background === listbox parent background, byte for byte).
- **Fix:** Swapped the highlight to `focus:bg-primary/10` — token-driven, no literal colour, no scanner sentinel needed. Confirmed live afterward: the highlighted option now visibly differs from the popover under `clinical`.
- **Files modified:** `components/ui/select.tsx`
- **Verification:** live Playwright hover/focus reads before and after (`getComputedStyle` background comparison against the popover's own background); full gate suite green (`scan:tokens` 0 violations, lint/typecheck clean, 1959/1959 tests, production build)
- **Committed in:** `d5aac2c` (Task 2's commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug). **Impact:** Necessary correctness fix for a defect discovered by this plan's own named QA checklist item; affects every theme (a universal UX improvement), not scope creep — no theme file touched, no shared overlay class introduced, `git status --porcelain themes/` stayed empty throughout.

## Issues Encountered

- **Two sites named by the light-preset checklist had no reachable live trigger in this environment**, matching the class of limitation Phase 6 hit for its own scrim sites: the desktop nav mega-menu's child-category link (`hover:bg-surface-elevated/50`) has no live example because the local D1 fixture seeds exactly 1 category with no children; the category hero overlay's re-check (`bg-surface/40`) has no live example because that category has no configured image. Both were substituted with live-DOM compositing injection (Phase 6's established method) and recorded as lower-confidence evidence rather than silently upgraded to a full pass. Neither is a defect — both are flagged for a real check once richer fixture data exists.
- **Market's `Badge`/chip radius check had no live consumer at all** — `Badge` is unused anywhere on the storefront outside `admin/`. Recorded honestly rather than fabricating markup; the identical `rounded-md` utility class is already confirmed reaching Market's `radius-md` (8px) on `Input`/`Button`, so the same deterministic CSS mechanism would apply the moment any component renders a `Badge`.

## User Setup Required

None - no external service configuration required. The `NEXT_PUBLIC_SITE_URL` and `NEXT_PUBLIC_THEME_DEFAULT` Workers Build variables remain outstanding from v1/Phase 6 (unchanged by this plan; already tracked in STATE.md's Blockers/Concerns).

## Next Phase Readiness

- Phase 6.1 is complete: all four requirements-bearing ROADMAP success criteria have named evidence in `06.1-SCREENSHOTS.md`'s phase-close section, and the full gate suite (`build-themes --check`, the admin-appearance render test, `scan:tokens`, lint, typecheck, `npm test`, `npm run build`) is green at close. THEME-05 is marked complete in `REQUIREMENTS.md` and `ROADMAP.md`.
- Seven presets now ship on the frozen 23-token contract with zero mechanism changes across two phases (06 and 06.1) — Phase 7 (Layout Switches) can build its enumerated `category_layout`/`home_hero`/`product_gallery` variants against a stable, fully-QA'd theme surface.
- Carried forward, listed with where to pick up in `06.1-SCREENSHOTS.md`'s Task 3 section: Phase 5's four screenshot coverage gaps (order-status, Stripe payment step, authenticated account dashboard, review-form error state — need a seeded order and a Clerk session), WINDOWS #2 (admin walkthrough) and #3 (`defaultSettings` re-insert bug), the category-hero overlay's synthetic-evidence caveat, the desktop mega-menu child-link's lower-confidence evidence, Market's `Badge` no-live-consumer note, and the two outstanding Workers Build variables.
- Dropped-properties backlog for all seven presets is now fully recorded across two notes: `.planning/todos/pending/theme-contract-dropped-properties.md` (Phase 6, luxe/midnight) and `.planning/todos/pending/theme-direction-doc-backlog-06.1.md` (this plan, clinical/retro/atelier/market).

---
*Phase: 06.1-remaining-presets-clinical-retro-atelier-market*
*Completed: 2026-09-05*

## Self-Check: PASSED

- FOUND: .planning/todos/pending/theme-direction-doc-backlog-06.1.md
- FOUND: .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md
- FOUND commit: 72d3b1c (Task 1)
- FOUND commit: d5aac2c (Task 2)
- FOUND commit: cec0b79 (Task 3)
- Re-ran plan-level verification: `mise exec -- node scripts/build-themes.mjs --check` (exit 0, 7 theme(s) fresh); `git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts` (empty); manifest sorted order and 7 distinct labels confirmed; `mise exec -- npx vitest run tests/unit/app/admin-appearance-source.test.ts` (19/19 pass); `mise exec -- npm run lint` (0 errors, 52 pre-existing warnings); `mise exec -- npm run typecheck` (clean); `mise exec -- npm run scan:tokens` (0 violations, 2 unchanged manual-review rows); `mise exec -- npm test` (249 files / 1959 tests, all green); `mise exec -- npm run build` (exit 0); `grep -qE '^- \[x\] \*\*THEME-05\*\*'` and the coverage-table `Complete` row both confirmed in REQUIREMENTS.md; stored `appearance.theme` confirmed back at `volt-dark` via a live curl before this summary was written.
