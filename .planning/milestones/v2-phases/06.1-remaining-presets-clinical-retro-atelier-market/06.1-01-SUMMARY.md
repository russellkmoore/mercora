---
phase: 06.1-remaining-presets-clinical-retro-atelier-market
plan: 01
subsystem: theming
tags: [theme-presets, css-tokens, d1-settings, accessibility-contrast, screenshot-harness]

# Dependency graph
requires:
  - phase: 06-01
    provides: "scripts/build-themes.mjs validator/codegen (unmodified), THEME_MANIFEST shape"
  - phase: 06-02
    provides: "getActiveTheme() (D1 -> env -> manifest default), APPEARANCE_SETTINGS_CATEGORY, APPEARANCE_THEME_SETTING_KEY"
  - phase: 06-03
    provides: "themes/luxe.css as the light-preset file shape to mirror"
provides:
  - "themes/clinical.css — fourth preset, 23-token contract, manifest name 'clinical'"
  - "Regenerated themes/index.generated.css (4 imports) and lib/themes/manifest.generated.ts (4 entries), sorted filename order clinical/luxe/midnight/volt-dark"
  - ".planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md — this phase's own capture record, opened with the clinical route-grid capture"
affects: [06.1-02, 06.1-03, 06.1-04]

# Actuals (#2632)
actuals:
  tokens: 4000
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "clinical.css mirrors luxe.css's exact shape (head comment, @theme label header, single attribute-selector block, four grouped comment sections) — no new pattern, confirms the mechanism travels to a fourth file unaided"
    - "The validator's rejection path is measured, not assumed: a required token is deliberately deleted, build-themes.mjs is run to capture its exact failure message and exit code, then the file is restored and the generator re-run clean — the same probe pattern this phase's remaining three plans should reuse rather than skip"

key-files:
  created:
    - themes/clinical.css
    - .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md
  modified:
    - themes/index.generated.css
    - lib/themes/manifest.generated.ts

key-decisions:
  - "Every clinical token value was transcribed verbatim from 06.1-UI-SPEC.md's pre-computed table, including its one flagged deviation (primary/ring darkened from oklch(62% .12 200) to oklch(54% .12 200) so white on-primary reaches 4.61:1) — no oklch value was re-derived by hand."
  - "Clinical's radius-lg anchors the doc's literal 0.5rem figure; the surrounding three radius steps reuse volt-dark's stock progression rather than inventing a new scale, since the doc gives no preference for a more/less aggressive scale."
  - "Both font tokens stay the existing Geist stack (byte-identical to volt-dark.css) — clinical introduces zero new font dependency and needed no app/layout.tsx change, proving the pipeline's thinnest possible slice."

patterns-established:
  - "A fourth preset travels the entire pipeline (theme file -> unmodified validator -> codegen -> barrel/manifest -> resolver -> served attribute -> screenshot harness) with zero changes to any of those layers — the mechanism is now proven repeatable, not just built."

requirements-completed: [THEME-05]

coverage:
  - id: D1
    description: "themes/clinical.css ships as a pure 23-token data file (one attribute-selector block, 17 hex colours, required @theme header), passing the unmodified build-themes validator and the token-contract parity test, with the doc's flagged accessibility correction honoured (not silently reverted)"
    requirement: THEME-05
    verification:
      - kind: unit
        ref: "tests/unit/lib/themes/token-contract.test.ts (loop now covers clinical.css) — 21/21 pass"
        status: pass
      - kind: other
        ref: "mise exec -- node scripts/build-themes.mjs --check (exit 0, 4 theme(s) fresh); grep -cE assertions for token count (23), rule count (1), hex count (17), and the corrected primary/ring value (2 matches)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The regenerated barrel and manifest list all four themes in deterministic sorted filename order (clinical, luxe, midnight, volt-dark), regenerate idempotently, and every manifest label is distinct"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "mise exec -- node scripts/build-themes.mjs (twice, idempotent, git-clean both runs); grep -oE ordering assertions on both generated files; label-count == unique-label-count"
        status: pass
    human_judgment: false
  - id: D3
    description: "The validator's rejection path is measured (not assumed): a copy of clinical.css missing --store-ring makes build-themes.mjs exit non-zero with a [build-themes] message naming the file and line, then the file and generated output are restored clean"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "deliberate-invalidation probe: sed-deleted --store-ring line, node scripts/build-themes.mjs exit 1, message '[build-themes] themes/clinical.css:30: missing required token \"--store-ring\"', file restored via cp, generator re-run, git status --porcelain empty on both generated paths"
        status: pass
    human_judgment: false
  - id: D4
    description: "One D1 setting write makes a real dev server serve data-theme=\"clinical\" on the next request, using the documented dev-bypass admin API method; the setting is restored to volt-dark and reconfirmed before the server stops"
    requirement: THEME-05
    verification:
      - kind: integration
        ref: "POST /api/admin/settings with x-dev-admin: mercora-dev-bypass, body {updates:[{key:'appearance.theme',value:'clinical',category:'appearance'}]}; curl http://localhost:3000/ | grep data-theme confirmed both clinical and the volt-dark restore"
        status: pass
    human_judgment: false
  - id: D5
    description: "This phase's own 06.1-SCREENSHOTS.md exists, is the harness's explicit --manifest target (not the default path), and carries the clinical route-grid capture; Phase 5's screenshot record is untouched"
    requirement: THEME-05
    verification:
      - kind: other
        ref: "mise exec -- npm run screenshot:routes -- --label phase-06.1-01-clinical --allow-missing --include-content --manifest .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md (28 captured, 4 missing order-status); git status --porcelain on 05-SCREENSHOTS.md empty, 0 'phase-06.1' matches in that file"
        status: pass
    human_judgment: false

# Metrics
duration: 20min
completed: 2026-09-04
status: complete
---

# Phase 6.1 Plan 1: Clinical Preset Tracer Summary

**The fourth theme preset (`clinical`, light, sea-teal accent) travels the entire pipeline — theme file, unmodified validator, codegen, barrel/manifest, resolver, served `data-theme` attribute, and screenshot harness — with zero changes to any layer, and the validator's rejection path was measured on a deliberately invalidated copy rather than assumed.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-09-04T22:30:00Z (approx.)
- **Completed:** 2026-09-04T22:50:00Z (approx.)
- **Tasks:** 2 completed
- **Files modified:** 4 (2 created, 2 modified)

## Accomplishments

- Authored `themes/clinical.css` mirroring `themes/luxe.css`'s exact shape: head comment (theme name, 23-token contract, direction-doc section, UI-SPEC section, deviation-disclosure paragraph, inverse-polarity paragraph, no-font-loading paragraph), the `@theme` header line copied verbatim from the UI-SPEC, and one `[data-theme="clinical"]` block with all 23 tokens in luxe's group order and group comments. `primary`/`ring` carry the UI-SPEC's corrected value (`#00828a`, 4.61:1 against white `on-primary`) rather than the doc's literal accent (3.38:1, fails AA). `radius-lg` anchors the doc's literal `0.5rem`; the other three radius steps reuse volt-dark's stock progression. Both font tokens are byte-identical to `volt-dark.css`'s Geist stack — clinical introduces no new font dependency and required no `app/layout.tsx` change.
- Regenerated `themes/index.generated.css` (4 `@import` lines) and `lib/themes/manifest.generated.ts` (4 `THEME_MANIFEST` entries) — confirmed sorted filename order `clinical, luxe, midnight, volt-dark` in both files, confirmed idempotent (two consecutive generator runs produce byte-identical output, `--check` exits 0, `git status --porcelain` empty), and confirmed all four manifest labels are distinct.
- Ran the full plan-level verify chain: shape assertions on `clinical.css` (23 tokens, 1 rule, 17 hex colours, corrected primary/ring, zero at-rules, font-token byte-match against `volt-dark.css`), `build-themes.mjs` + `--check`, the token-contract/tokens vitest suites (21/21 pass), `npm run lint` (0 errors, 52 pre-existing warnings only), `npm run typecheck` (clean), and `npm run scan:tokens` (0 violations, same 2 manual-review rows Phase 5/6 closed with).
- Exercised the validator's rejection path deliberately and reversibly: stripped `--store-ring` from a working copy of `clinical.css`, ran `build-themes.mjs`, captured its exact output — `[build-themes] themes/clinical.css:30: missing required token "--store-ring"` and a non-zero exit — then restored the file from backup and re-ran the generator clean (idempotent, git-clean).
- Proved the switch on a real `npm run dev` server: wrote `appearance.theme` = `"clinical"` via `POST /api/admin/settings` with the `x-dev-admin: mercora-dev-bypass` dev-bypass header (body shape `{updates:[{key, value, category}]}`, matching the route's actual contract), confirmed the served home page carried `data-theme="clinical"`, then restored the setting to `"volt-dark"` and reconfirmed before stopping the server.
- Created `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` (this phase's own capture record, following `06-SCREENSHOTS.md`'s shape) and opened it with the `phase-06.1-01-clinical` route-grid capture: 28 cells captured, 4 missing (the carried-forward `order-status` gap — no seeded order in the local D1 fixture, unchanged since Phase 5). The harness was invoked with `--manifest` pointed explicitly at this file; `05-SCREENSHOTS.md` is confirmed untouched (`git status --porcelain` empty, zero `phase-06.1` matches).

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end — one new preset reaches the served page unaided** - `c4a76ec` (feat)
2. **Task 2: This phase's own screenshot record, opened with the clinical capture grid** - `f8a4cb8` (docs)

**Plan metadata:** (this commit)

_Note: Task 1 is `type="tracer"` — executed and committed exactly like `type="auto"`, then the tracer feedback gate re-ran both `<automated>` verify blocks end-to-end (interactive, `human_verify_mode: end-of-phase`, verify carries only `<automated>` — no `<human-check>`) before Task 2 began. Both re-runs exited 0; no checkpoint was synthesized._

## Files Created/Modified

- `themes/clinical.css` - fourth preset, 23-token contract, manifest name `clinical`
- `themes/index.generated.css` - regenerated to 4 `@import` lines
- `lib/themes/manifest.generated.ts` - regenerated to 4 `THEME_MANIFEST` entries
- `.planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md` - this phase's own capture record, opened with `phase-06.1-01-clinical`

## Decisions Made

- **Every token value copied verbatim from the UI-SPEC's pre-computed table, including the flagged correction** — no oklch value was re-derived from `docs/voltique-theme-direction.md` by hand, per the plan's explicit instruction.
- **`radius-lg` anchors the doc's literal figure; the other three steps reuse volt-dark's stock progression** — the doc gives no preference for a more/less aggressive scale, matching Clinical's "medium-weight, a lot of air" direction.
- **No new font dependency** — both font tokens stay the Geist stack, matching the `midnight` precedent of reusing Geist when the doc's face choice is itself Geist-compatible.

## Deviations from Plan

None — plan executed exactly as written. All shape assertions, codegen, vitest, lint, typecheck, and scan:tokens gates passed on the first run; the deliberate-invalidation probe and the dev-server switch proof both matched the plan's described method (the settings API's actual `{updates: [...]}` body shape was discovered by reading `app/api/admin/settings/route.ts`, not a deviation from the plan — the plan named the mechanism, not the literal payload).

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The preset pipeline is proven end to end on a fourth file with zero mechanism changes — plans 06.1-02, 06.1-03, and 06.1-04 (`retro`, `atelier`, `market`) can now repeat this exact shape with confidence the pipeline itself will not reject anything about the way this phase authors presets.
- `06.1-SCREENSHOTS.md` exists with its Findings table stubbed (headers only) for plan 06.1-04's three-light-preset acid test, and its UI Considerations section already answers the populated/zero-one-many rows with evidence at 4 manifest entries.
- Carried forward, unchanged by this plan: the `font-display` wiring gap (Phase 6, WINDOWS.md) — `clinical` ships no new display face so this plan doesn't touch it; CONTEXT D-10's site-wide type wiring is scoped to a later plan in this phase.

---
*Phase: 06.1-remaining-presets-clinical-retro-atelier-market*
*Completed: 2026-09-04*

## Self-Check: PASSED

- FOUND: themes/clinical.css
- FOUND: .planning/phases/06.1-remaining-presets-clinical-retro-atelier-market/06.1-SCREENSHOTS.md
- FOUND commit: c4a76ec (Task 1)
- FOUND commit: f8a4cb8 (Task 2)
- Re-ran plan-level verification: `mise exec -- node scripts/build-themes.mjs --check` (exit 0, 4 theme(s) fresh); `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts tests/unit/lib/themes/tokens.test.ts` (21/21 pass); `mise exec -- npm run scan:tokens` (0 violations, 2 unchanged manual-review rows); `git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts` (empty)
