---
phase: 05-token-contract-component-sweep
plan: 07
type: execute
wave: 7
depends_on: ["05-06"]
files_modified:
  - components/reviews/ProductReviewsSection.tsx
  - components/reviews/ReviewForm.tsx
  - components/reviews/StarRating.tsx
  - components/subscriptions/SubscriptionAcquisitionPanel.tsx
  - components/subscriptions/SubscriptionSetupReturnHandler.tsx
  - .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
autonomous: true
requirements: [TOKEN-03, TOKEN-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mise exec -- npm run scan:tokens -- --path components/reviews` and `-- --path components/subscriptions` report 0 violations"
    - "Star rating fill and empty states are token-driven, with filled stars on the primary token and unfilled stars on muted foreground"
    - "Review form validation states reach the danger token, and submission success reaches the success token"
    - "Subscription plan cards distinguish the selected plan from the unselected ones using primary against elevated surface, with no raw palette value"
    - "The product route renders identically to the baseline apart from registered snaps, including the reviews section and the subscription panel"
  prohibitions:
    - statement: "MUST NOT modify any file under `app/admin/` or `components/admin/`. Admin's hardcoded palette is a deliberate scope boundary (D-14), not an oversight to tidy up. `components/admin/reviews/ReviewModerationDashboard.tsx` is the near-neighbour to leave alone here."
      status: resolved
      verification: judgment
    - statement: "MUST NOT change any rendered pixel beyond the recorded snaps. No opportunistic restyling, no layout, spacing, typography, shadow, overlay, or light-versus-dark polarity change while substituting class names (D-16)."
      status: resolved
      verification: judgment
  artifacts:
    - path: "components/reviews/ProductReviewsSection.tsx"
      provides: "Review list and rating summary expressed entirely in contract tokens"
    - path: "components/subscriptions/SubscriptionAcquisitionPanel.tsx"
      provides: "Subscription plan selection expressed entirely in contract tokens"
  key_links:
    - from: "components/reviews/StarRating.tsx"
      to: "themes/volt-dark.css"
      via: "star fill and empty colours resolving through the Tailwind colour map"
      pattern: "text-primary|text-muted-foreground"
---

<objective>
Sweep the two heaviest component clusters on the product route — reviews and subscription
acquisition — to the token contract.

Purpose: together these five files carry 128 palette occurrences, the largest concentration in
the component tree outside email. They also sit on the product route, which plan 05-06 has just
screenshotted, so their diffs land against a freshly-verified page.

Output: reviews and subscriptions scan-clean, with a product-route screenshot set covering both
the reviews section and the subscription panel.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md
@.planning/phases/05-token-contract-component-sweep/05-CONTEXT.md
@.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
@.planning/phases/05-token-contract-component-sweep/05-06-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

All five files are main-set surfaces. The inverse token set appears nowhere in this chunk.

Measured scope: `ProductReviewsSection.tsx` 47 occurrences over 363 lines,
`SubscriptionAcquisitionPanel.tsx` 41 over 523 lines, `ReviewForm.tsx` 29 over 265 lines,
`SubscriptionSetupReturnHandler.tsx` 9 over 161 lines, `StarRating.tsx` 2.

`components/admin/reviews/ReviewModerationDashboard.tsx` is a separate admin file that must not
be touched — it keeps its hardcoded palette under D-14 and is excluded from the scan by path.

`components/account/SubscriptionManager.tsx` is NOT part of this chunk; it belongs to plan
05-11 with the rest of the account surfaces.

These components compose against `components/ui/button.tsx`, `input.tsx`, `textarea.tsx`,
`select.tsx`, and `badge.tsx`, all rewritten in plan 05-04 — match their token vocabulary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the review surfaces</name>
  <files>components/reviews/ProductReviewsSection.tsx, components/reviews/ReviewForm.tsx, components/reviews/StarRating.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the main-set substitution table including the status rows
    - `components/reviews/ProductReviewsSection.tsx` — the full file
    - `components/reviews/ReviewForm.tsx` — the full file
    - `components/reviews/StarRating.tsx` — the full file, including any SVG fill or stroke attribute
    - `components/ui/input.tsx` and `components/ui/textarea.tsx` as rewritten in plan 05-04 — the form primitive vocabulary to match
  </read_first>
  <action>
    Sweep the three review files with the §2 table by role. Review cards, the rating summary
    panel, and the distribution bars sit on elevated surfaces; reviewer names and review bodies
    are foreground; timestamps, helpful counts, and verified-purchase notes are muted foreground;
    the rating bars' filled portion and any call to action are primary; card outlines and list
    dividers are the border token.

    In `StarRating.tsx`, check for SVG `fill` and `stroke` attributes as well as class names —
    the sweep covers colour wherever it lives, and a star component is a likely place for a raw
    attribute. Filled stars take the primary token; unfilled stars take muted foreground.

    In `ReviewForm.tsx`, map validation and submission feedback onto the status quartet by
    meaning: a field error or a failed submission is danger, a successful submission is success,
    a pending or rate-limited notice is warning. Preserve the `aria-invalid` handling the
    rewritten form primitives now render visibly.

    Preserve every variant prefix and alpha modifier. Change no spacing, sizing, shadow, or
    transition class.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path components/reviews` reports 0 violations
    - `components/reviews/StarRating.tsx` contains no SVG fill or stroke attribute holding a literal colour
    - `components/reviews/ReviewForm.tsx` references the danger token for its error state and the success token for its confirmation state
    - No file in this task contains an inverse-set token class
    - `mise exec -- npm run build`, `lint`, and `typecheck` all succeed
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path components/reviews</automated>
    <fails_when>non-zero exit, or the line "[scan-tokens] 0 violations" is absent from the output</fails_when>
    <automated>/usr/bin/grep -cE '(fill|stroke)="(#|rgb|hsl|[a-z]+")' components/reviews/StarRating.tsx</automated>
    <fails_when>the printed count is anything other than 0, meaning a literal colour survives in an SVG attribute where a className scan would never have found it</fails_when>
    <automated>/usr/bin/grep -q -- '-danger' components/reviews/ReviewForm.tsx &amp;&amp; /usr/bin/grep -q -- '-success' components/reviews/ReviewForm.tsx &amp;&amp; echo "feedback-tokens-present"</automated>
    <fails_when>non-zero exit, or the string "feedback-tokens-present" is absent, meaning the form's error or confirmation state does not reach a status token</fails_when>
    <automated>mise exec -- npm run build &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a line matching "Failed to compile", "error TS", or "error" in the combined output</fails_when>
  </verify>
  <done>The three review files are scan-clean, star colours come from tokens including in SVG attributes, and form feedback reaches the status quartet.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the subscription acquisition surfaces</name>
  <files>components/subscriptions/SubscriptionAcquisitionPanel.tsx, components/subscriptions/SubscriptionSetupReturnHandler.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the substitution table
    - `components/subscriptions/SubscriptionAcquisitionPanel.tsx` — the full file; the largest in this chunk
    - `components/subscriptions/SubscriptionSetupReturnHandler.tsx` — the full file
    - `components/ui/badge.tsx` and `components/ui/card.tsx` as rewritten in plan 05-04
    - `components/reviews/ReviewForm.tsx` as rewritten in Task 1 — the status-token vocabulary to stay consistent with
  </read_first>
  <action>
    Sweep the two subscription files with the §2 table. Plan cards are elevated surfaces; plan
    names and prices are foreground; billing-interval copy, savings notes, and terms are muted
    foreground; the selected-plan highlight, the recommended-plan accent, and the subscribe
    action are primary; card outlines are the border token, with the selected card's outline as
    primary rather than a brighter neutral.

    The selected-versus-unselected distinction must survive the shade consolidation. If today's
    difference is only between two neutral shades that both collapse onto the same token,
    express the selection with the primary token on the border or a primary tint on the surface
    instead — losing the affordance is a regression under D-16, not a snap under D-15.

    In `SubscriptionSetupReturnHandler.tsx`, map the setup outcome states onto the status
    quartet by meaning: a completed setup is success, a failed or declined one is danger, a
    pending or requires-action one is warning, an informational return notice is info.

    Preserve every variant prefix and alpha modifier. Change no spacing, sizing, shadow, or
    transition class.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path components/subscriptions` reports 0 violations
    - The selected-plan state in `SubscriptionAcquisitionPanel.tsx` is expressed with the primary token, not with a second neutral shade
    - `SubscriptionSetupReturnHandler.tsx` references at least three of the four status tokens
    - No file in this task contains an inverse-set token class
    - `mise exec -- npm run test` passes with no new failures
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path components/subscriptions</automated>
    <fails_when>non-zero exit, or the line "[scan-tokens] 0 violations" is absent from the output</fails_when>
    <automated>C=0; for t in success warning danger info; do /usr/bin/grep -q -- "-$t" components/subscriptions/SubscriptionSetupReturnHandler.tsx &amp;&amp; C=$((C+1)); done; test "$C" -ge 3 &amp;&amp; echo "status-coverage-ok"</automated>
    <fails_when>non-zero exit, or the string "status-coverage-ok" is absent, meaning fewer than three status tokens are reached and setup outcomes are collapsing into one colour</fails_when>
    <automated>/usr/bin/grep -cE '(border|bg|ring)-primary' components/subscriptions/SubscriptionAcquisitionPanel.tsx</automated>
    <fails_when>the printed count is 0, meaning the selected-plan affordance is not expressed with the primary token and may have been lost to shade consolidation</fails_when>
    <automated>mise exec -- npm run test &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test, or a line matching "error TS"</fails_when>
  </verify>
  <done>Both subscription files are scan-clean, the selected-plan affordance survives consolidation, and setup outcomes reach distinct status tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the product route still reads correctly</name>
  <files>.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md</files>
  <read_first>
    - `scripts/screenshot-routes.mjs` — the capture flags and the interactive-state selectors
    - `.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md` — the `baseline` and `chunk-3-catalog` sections
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §4 and §8
  </read_first>
  <action>
    Start the dev server, capture with label `chunk-3-engagement`, and compare the product-route
    cells against the `baseline` label at both viewports.

    The standard grid's single interactive state per route does not reach the two surfaces this
    plan changed, so add three supplementary cells: the reviews section scrolled into view, the
    review form with a validation error showing, and the subscription panel with a plan selected.
    Without them this chunk would ship 128 substitutions with no visual evidence at all.

    Append a `chunk-3-engagement` section to the manifest with a per-cell verdict, attributing
    every difference to a registered snap identifier or fixing it. Star-rating and status-badge
    differences should map to S4 through S7; a selected-plan card that is no longer visually
    distinguishable from an unselected one is a defect, not a snap.
  </action>
  <acceptance_criteria>
    - `.planning/.../05-SCREENSHOTS.md` contains a `chunk-3-engagement` section covering the product route at both viewports
    - Three supplementary rows exist for the reviews section, the review form error state, and the selected subscription plan
    - Every differing row names a snap identifier or a fix
    - `mise exec -- npm run scan:tokens -- --path components/reviews` and `-- --path components/subscriptions` still report 0 violations after the capture
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run dev > /tmp/dev-05-07.log 2>&amp;1 &amp; DEV=$!; sleep 35; mise exec -- node scripts/screenshot-routes.mjs --label chunk-3-engagement --manifest .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md --allow-missing; RC=$?; kill $DEV; exit $RC</automated>
    <fails_when>non-zero exit, or the summary line reports 0 captured cells; if nothing captures, read /tmp/dev-05-07.log for the server startup reason</fails_when>
    <automated>mise exec -- npm run scan:tokens -- --path components/reviews &amp;&amp; mise exec -- npm run scan:tokens -- --path components/subscriptions</automated>
    <fails_when>non-zero exit from either, meaning a palette value reappeared in this chunk's scope</fails_when>
    <human-check>Open the product page, scroll to the reviews, submit the review form empty to trigger validation, then select a subscription plan. Confirm the star rating still reads at a glance, the validation error is unmistakably an error, and the selected plan is obviously the selected one. These three affordances are exactly what a shade consolidation can quietly flatten.</human-check>
  </verify>
  <done>The product route's two heaviest component clusters are swept, and three supplementary screenshots prove the affordances that consolidation could have flattened still read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form validation → shopper | An invalid field's error affordance is the only signal a submission will fail. |
| plan selection → billing commitment | The selected-plan highlight is what tells a shopper which recurring charge they are agreeing to. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Spoofing | subscription plan selection affordance | high | mitigate | The selected state is required to be expressed with the primary token rather than a second neutral shade, asserted in Task 2 and eyeballed in Task 3. A shopper who cannot tell which recurring plan is selected can commit to the wrong charge. |
| T-05-07-02 | Spoofing | review-form validation state | medium | mitigate | Error and confirmation states are required to reach the danger and success tokens, asserted in Task 1. |
| T-05-07-03 | Information Disclosure | SVG attribute colours invisible to a className-only sweep | medium | mitigate | `StarRating.tsx` is checked for literal `fill` and `stroke` attributes explicitly, closing the gap RESEARCH Pitfall 1 names. |
| T-05-07-04 | Tampering | the admin review moderation dashboard | medium | mitigate | It is excluded by path in the scan and named in this plan's prohibitions as the near-neighbour to leave alone. |
| T-05-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing; the only phase install happened in 05-02 behind the blocking human legitimacy checkpoint. |
</threat_model>

<verification>
- Scoped scan reports 0 violations for `components/reviews` and `components/subscriptions`.
- `StarRating.tsx` carries no literal colour in an SVG attribute.
- `ReviewForm.tsx` reaches danger and success; `SubscriptionSetupReturnHandler.tsx` reaches at least three status tokens.
- The selected-plan affordance uses the primary token.
- `mise exec -- npm run test && npm run typecheck && npm run lint` green.
- `05-SCREENSHOTS.md` carries a `chunk-3-engagement` section with three supplementary cells.
</verification>

<success_criteria>
The 128 densest palette occurrences in the component tree are gone, and the three affordances a
shade consolidation could have flattened — star ratings, validation errors, plan selection — are
photographed still working.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| 5 rewritten review and subscription files | modification | the densest palette cluster outside email |
| `chunk-3-engagement` manifest section | new section | product route plus reviews, form-error, and selected-plan cells |
</artifacts_produced>

<output>
Create `.planning/phases/05-token-contract-component-sweep/05-07-SUMMARY.md` when done.
</output>
