---
phase: 05-token-contract-component-sweep
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - components/HeaderClient.tsx
  - components/Header.tsx
  - components/Breadcrumbs.tsx
  - components/Footer.tsx
  - components/PromotionalBanner.tsx
  - app/layout.tsx
  - .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
autonomous: true
requirements: [TOKEN-03, TOKEN-05]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`mise exec -- npm run scan:tokens -- --path components/HeaderClient.tsx` and the same for each of the five other swept files report 0 violations"
    - "The Sonner toaster reads its colours from token classes rather than a CSS-variable-in-class-name expression"
    - "The header's Suspense fallback block keeps the same visual weight it has today, expressed as an elevated-surface token"
    - "The promotional banner's variant colours map onto the status quartet — success, warning, danger, info — with the blue variant reaching the info token (D-03)"
    - "Header, footer, and breadcrumbs render identically to the baseline at both viewports apart from registered snaps"
    - "The Clerk provider's appearance prop is untouched by this plan — it belongs to the non-cascade consumer chunk"
  prohibitions:
    - statement: "MUST NOT modify any file under `app/admin/` or `components/admin/`. Admin's hardcoded palette is a deliberate scope boundary (D-14), not an oversight to tidy up."
      status: resolved
      verification: judgment
    - statement: "MUST NOT change any rendered pixel beyond the recorded snaps. No opportunistic restyling, no layout, spacing, typography, shadow, overlay, or light-versus-dark polarity change while substituting class names (D-16). The shell frames every screenshot in the phase, so drift here silently contaminates every later chunk's diff."
      status: resolved
      verification: judgment
  artifacts:
    - path: "components/HeaderClient.tsx"
      provides: "Storefront navigation chrome expressed entirely in contract tokens"
    - path: ".planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md"
      provides: "chunk-2-shell coverage section across all seven routes"
  key_links:
    - from: "app/layout.tsx"
      to: "themes/volt-dark.css"
      via: "Sonner toastOptions className resolving primary and on-primary tokens"
      pattern: "bg-primary/80"
---

<objective>
Sweep the shared shell — navigation, footer, breadcrumbs, promotional banner, and the two
remaining colour expressions in the root layout — to the token contract.

Purpose: the shell frames every screenshot the rest of the phase takes. Sweeping it before the
page chunks means each later diff is measured against a stable frame instead of a moving one.
It is also where the last CSS-variable-in-a-class-name expression lives: the Sonner toaster
still writes the raw custom property into its class string, which D-12 replaces with plain
token classes.

Output: six shell files scan-clean, and a full seven-route screenshot set proving the frame
did not move.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md
@.planning/phases/05-token-contract-component-sweep/05-CONTEXT.md
@.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md
@.planning/phases/05-token-contract-component-sweep/05-04-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

`app/layout.tsx` after plan 05-03 has `data-theme` on `<html>` and token classes on `<body>`.
Two colour expressions remain in it:
  - the header Suspense fallback, a fixed-height placeholder div carrying a raw neutral
    background class whose hex is the same value the elevated-surface token now holds;
  - the Sonner `<Toaster>` `toastOptions.className`, which today writes the raw custom property
    directly into a Tailwind arbitrary-value class and pairs it with a raw black text class.
    D-12 replaces both with plain token classes at the same eighty-percent alpha.

`ClerkProvider`'s `appearance` prop in the same file is NOT this plan's work — the Clerk
`variables` block belongs to plan 05-11, which owns every `getThemeTokens()` consumer.

Measured scope: `HeaderClient.tsx` 32 occurrences over 491 lines, `Footer.tsx` 15,
`PromotionalBanner.tsx` 8, `Breadcrumbs.tsx` 6, `app/layout.tsx` 2, `Header.tsx` 0 (confirm).

The promotional banner is one of the three surfaces D-03 cites as the reason the `info` token
exists at all; its blue variant maps to `info`, not to `primary`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the navigation chrome</name>
  <files>components/HeaderClient.tsx, components/Header.tsx, components/Breadcrumbs.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the main-set substitution table, and §2b for any residual shadcn class
    - `components/HeaderClient.tsx` — the full file; it carries the largest occurrence count in this chunk
    - `components/Header.tsx` — the server wrapper; confirm whether it carries any colour class
    - `components/Breadcrumbs.tsx` — the full file
    - `components/ui/navigation-menu.tsx` as rewritten in plan 05-04 — the primitive the header composes, so its token vocabulary is the one to match
  </read_first>
  <action>
    Sweep `HeaderClient.tsx`, `Header.tsx`, and `Breadcrumbs.tsx` using the §2 substitution
    table, choosing each token by the role the element plays rather than by matching shade
    numbers. The header bar and any dropdown panel are elevated surfaces; primary navigation
    labels are foreground; secondary and inactive labels are muted foreground; every accent,
    active state, and hover highlight is the primary token, with the lighter and darker hover
    shades expressed as reduced alpha on primary per the table.

    Watch for cart-count badges and any icon fill or stroke attribute — the sweep covers SVG
    colour attributes, not just className strings, and a badge is a common place for a raw
    status colour to hide.

    Preserve every variant prefix and alpha modifier exactly. Change no spacing, sizing, border
    width, shadow, or transition class.

    Re-read the substitution table between files rather than carrying a remembered mapping
    forward — the inverse set does not apply anywhere in this chunk, and mixing the two is the
    single most damaging mistake available here.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path components/HeaderClient.tsx` reports 0 violations, and the same for `components/Header.tsx` and `components/Breadcrumbs.tsx`
    - No file in this task contains any inverse-set token class — the shell is a main-set surface
    - Every hover state that previously used a lighter or darker orange now uses primary with a reduced-alpha modifier, not a second colour token
    - `mise exec -- npm run build` succeeds
    - `mise exec -- npm run lint` and `mise exec -- npm run typecheck` report no errors
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path components/HeaderClient.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path components/Header.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path components/Breadcrumbs.tsx</automated>
    <fails_when>non-zero exit from any of the three, meaning a palette value survives in that file</fails_when>
    <automated>/usr/bin/grep -cE '(bg|text|border)-(surface-inverse|on-inverse|muted-on-inverse|border-inverse)' components/HeaderClient.tsx components/Header.tsx components/Breadcrumbs.tsx | /usr/bin/grep -v ':0$' | /usr/bin/wc -l</automated>
    <fails_when>the printed count is anything other than 0, meaning an inverse-set token leaked onto a main-set surface</fails_when>
    <automated>mise exec -- npm run build &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a line matching "Failed to compile", "error TS", or "error" in the combined output</fails_when>
  </verify>
  <done>Navigation chrome and breadcrumbs are scan-clean, use only main-set tokens, and the build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the footer, promotional banner, and the layout's last two colour expressions</name>
  <files>components/Footer.tsx, components/PromotionalBanner.tsx, app/layout.tsx</files>
  <read_first>
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §2 — the substitution table, including the status-token rows
    - `components/Footer.tsx` — the full file
    - `components/PromotionalBanner.tsx` — the full file, especially its variant-to-colour mapping
    - `app/layout.tsx` — the header Suspense fallback line and the Sonner toaster block
    - `.planning/phases/05-token-contract-component-sweep/05-CONTEXT.md` D-03 and D-12 — why `info` exists and exactly what the toaster class becomes
  </read_first>
  <action>
    Sweep `Footer.tsx` with the §2 table by role: the footer surface, its link labels, its muted
    legal copy, its social icons, and its dividing borders.

    Sweep `PromotionalBanner.tsx`. Its variants map onto the status quartet rather than onto
    arbitrary hues: the affirmative variant is success, the cautionary one is warning, the
    urgent one is danger, and the blue informational one is info. D-03 names this banner as one
    of only three reasons the `info` token is in the contract at all, so it must actually reach
    that token rather than collapsing into primary.

    In `app/layout.tsx`, change the header Suspense fallback's background to the elevated-surface
    token — its current value is the same hex the token holds, so this is a pure no-op.

    Rewrite the Sonner toaster's class string per D-12: the background becomes the primary token
    at the same eighty-percent alpha it uses today, and the label colour becomes the on-primary
    token. The toaster's current black label is the strongest evidence behind snap S1's choice of
    black for on-primary, so this substitution changes nothing visually here. Leave the toaster's
    position, duration, margin, shadow, and animation classes untouched.

    Do not touch the Clerk provider's appearance prop. It belongs to plan 05-11.
  </action>
  <acceptance_criteria>
    - `mise exec -- npm run scan:tokens -- --path components/Footer.tsx`, `-- --path components/PromotionalBanner.tsx`, and `-- --path app/layout.tsx` each report 0 violations
    - `components/PromotionalBanner.tsx` references all four status tokens — success, warning, danger, and info
    - `app/layout.tsx` contains the Sonner class string with a primary token at eighty-percent alpha and an on-primary label token
    - `app/layout.tsx` contains no arbitrary-value class wrapping a CSS custom property
    - The Clerk `appearance` prop in `app/layout.tsx` is byte-identical to its state before this plan
    - `mise exec -- npm run test` passes with no new failures
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run scan:tokens -- --path components/Footer.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path components/PromotionalBanner.tsx &amp;&amp; mise exec -- npm run scan:tokens -- --path app/layout.tsx</automated>
    <fails_when>non-zero exit from any of the three, meaning a palette value survives in that file</fails_when>
    <automated>for t in success warning danger info; do /usr/bin/grep -q -- "-$t" components/PromotionalBanner.tsx || { echo "missing token: $t"; exit 1; }; done; echo "quartet-present"</automated>
    <fails_when>the string "quartet-present" is absent, or a "missing token:" line names any of the four status tokens</fails_when>
    <automated>/usr/bin/grep -cE 'bg-\(--store-' app/layout.tsx</automated>
    <fails_when>the printed count is anything other than 0, meaning the arbitrary-value custom-property class survived in the toaster string</fails_when>
    <automated>mise exec -- npm run test &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck</automated>
    <fails_when>non-zero exit, or a summary line reporting any failed test, or a line matching "error TS"</fails_when>
  </verify>
  <done>Footer, banner, and the root layout's remaining colour expressions are scan-clean, the status quartet is genuinely reached, and the Clerk block is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the shell frame did not move</name>
  <files>.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md</files>
  <read_first>
    - `scripts/screenshot-routes.mjs` — the capture flags and interactive-state selectors
    - `.planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md` — the `baseline` and `chunk-2-ui` sections
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` §4 and §8 — the snap register and the coverage grid
  </read_first>
  <action>
    Start the dev server, capture the full coverage grid with label `chunk-2-shell`, and compare
    every cell against the `baseline` label by content hash.

    Append a `chunk-2-shell` section to the manifest recording route, viewport, state, path,
    hash, and a verdict per cell. Because the shell appears in every screenshot, this section is
    the phase's checkpoint on cumulative drift: annotate each difference with either a registered
    snap identifier, a D-17 newly-visible interaction state already recorded in the `chunk-2-ui`
    section, or a defect to fix.

    Give the mobile viewport particular attention. The header collapses to a different layout
    below the desktop breakpoint, and a mobile-only class is the easiest thing to miss in a
    file this size.

    A difference in the footer or the breadcrumb trail has no legitimate explanation in this
    chunk — neither carries a newly-visible interaction state and neither is covered by a
    registered snap. Treat any such difference as a defect and fix it rather than annotating it.
  </action>
  <acceptance_criteria>
    - `.planning/.../05-SCREENSHOTS.md` contains a `chunk-2-shell` section whose row count equals the `baseline` section's row count
    - Every differing row carries a snap identifier, a reference to a `chunk-2-ui` newly-visible state, or a note that it was fixed
    - No footer-region or breadcrumb-region difference is annotated rather than fixed
    - Both the 1280px and the 390px viewport are represented for every route in the section
    - `mise exec -- npm run scan:tokens -- --path components` reports fewer violations than before this plan, and none in the six swept files
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npm run dev > /tmp/dev-05-05.log 2>&amp;1 &amp; DEV=$!; sleep 35; mise exec -- node scripts/screenshot-routes.mjs --label chunk-2-shell --manifest .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md --allow-missing; RC=$?; kill $DEV; exit $RC</automated>
    <fails_when>non-zero exit, or the summary line reports 0 captured cells; if nothing captures, read /tmp/dev-05-05.log for the server startup reason</fails_when>
    <automated>/usr/bin/grep -c 'chunk-2-shell' .planning/phases/05-token-contract-component-sweep/05-SCREENSHOTS.md</automated>
    <fails_when>the printed count is 0, meaning the chunk section was never appended to the manifest</fails_when>
    <human-check>Put the `chunk-2-shell` and `baseline` home-route captures side by side at both 1280px and 390px and confirm the header, footer, and breadcrumb trail are indistinguishable. Then resize a live browser through the mobile breakpoint and confirm the collapsed header still reads correctly — a mobile-only class missed in a 491-line file will not show up in a desktop screenshot.</human-check>
  </verify>
  <done>The shell renders identically to the pre-sweep baseline at both viewports, and the manifest records the comparison cell by cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → every later chunk's diff | The header and footer appear in every screenshot; drift here silently contaminates every subsequent comparison. |
| class string → CSS custom property | The toaster currently interpolates a raw custom property into a class name, the last such expression in the storefront. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-01 | Tampering | shell drift contaminating later baselines | high | mitigate | Task 3 compares the full seven-route grid at both viewports against the fixed 05-02 baseline and forbids annotating a footer or breadcrumb difference instead of fixing it. |
| T-05-05-02 | Spoofing | promotional banner variant colour collapsing into primary | medium | mitigate | Task 2 asserts all four status tokens are actually referenced in the banner source. A promotional banner that renders brand-orange where it meant to render a caution or an informational state misrepresents the message to the shopper. |
| T-05-05-03 | Tampering | Clerk appearance prop edited out of chunk order | low | mitigate | Task 2 asserts the Clerk `appearance` prop is byte-identical after this plan; its rewrite belongs to plan 05-11. |
| T-05-05-04 | Information Disclosure | mobile-only classes unverified | medium | mitigate | The coverage grid captures both viewports for every route, and Task 3 adds an explicit human check on the collapsed header. |
| T-05-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing; the only phase install happened in 05-02 behind the blocking human legitimacy checkpoint. |
</threat_model>

<verification>
- Scoped scan reports 0 violations for each of the six swept files.
- The promotional banner references all four status tokens.
- No arbitrary-value custom-property class remains in `app/layout.tsx`.
- The Clerk appearance prop is untouched.
- `mise exec -- npm run test && npm run typecheck && npm run lint` green.
- `05-SCREENSHOTS.md` carries a `chunk-2-shell` section with a per-cell verdict at both viewports.
</verification>

<success_criteria>
The frame around every remaining screenshot is token-driven and provably unmoved, so the seven
page chunks that follow measure their own changes and nothing else.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Symbol / path | Kind | Notes |
|---|---|---|
| 6 rewritten shell files | modification | header chrome, footer, breadcrumbs, banner, and the layout's last two colour expressions |
| `chunk-2-shell` manifest section | new section | full seven-route grid at both viewports |
| removed: the last arbitrary-value custom-property class | deletion | the Sonner toaster now uses plain token classes (D-12) |
</artifacts_produced>

<output>
Create `.planning/phases/05-token-contract-component-sweep/05-05-SUMMARY.md` when done.
</output>
