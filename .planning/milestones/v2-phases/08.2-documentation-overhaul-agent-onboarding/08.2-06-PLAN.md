---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 06
type: execute
wave: 6
depends_on: ["08.2-05"]
files_modified:
  - docs/ai-pipeline.md
  - docs/admin-authentication.md
  - docs/mcp-server-specification.md
  - docs/observability.md
  - docs/shopify-migration.md
  - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [DOCS-04, DOCS-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every surviving document follows one style contract: a level-one title, a one-line purpose, a status line where relevant, second-person imperative prose, short sentences, commands and paths in code formatting, and no decorative emoji in any heading (D-05). [DOCS-04]"
    - "No topic is covered twice: each document that mentions a topic it does not own links the owner named in the inventory's topic map rather than restating it (D-05, DOCS-04 adjacency edge). [DOCS-04]"
    - "Every command, script name, path and environment variable in every surviving document resolves against the code, proved by a machine check rather than a reading (D-06). [DOCS-04]"
    - "`docs/theming.md` still quotes the theme validator's messages byte-identically to `scripts/build-themes.mjs`; the style pass did not touch its verbatim strings (D-06). [DOCS-04]"
    - "`08.2-VALIDATION.md` carries a real per-task verification map with the commands each plan ran, both manual checks, and a sign-off — no template placeholder survives. [DOCS-04] [DOCS-05]"
    - "DOCS-04 and DOCS-05 are marked complete in `.planning/REQUIREMENTS.md`, and `.planning/STATE.md` records the phase as complete with what shipped (D-04, D-07). [DOCS-04] [DOCS-05]"
    - "The full gate suite is green at the end of the phase, proving a documentation-only phase changed no runtime behaviour. [DOCS-04]"
  artifacts:
    - path: ".planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md"
      provides: "The filled validation record: infrastructure, sampling, per-task map across all six plans, manual checks, sign-off"
      contains: "docs:lint"
    - path: "docs/ai-pipeline.md"
      provides: "The AI pipeline document in the phase style: headers normalised, decoration removed, sample-catalogue examples labelled as sample data"
      contains: "@cf/openai/gpt-oss-20b"
    - path: "docs/admin-authentication.md"
      provides: "The admin authentication document in the phase style, with its deployment-guard and status-code detail intact"
      contains: "503"
  key_links:
    - from: "docs/theming.md"
      to: "scripts/build-themes.mjs"
      via: "every documented rejection quotes that script's own message text, unchanged by this phase"
      pattern: "missing required token"
    - from: "docs/ai-pipeline.md"
      to: "lib/ai/config.ts"
      via: "the model identifiers named in the document are the ones the configuration module sets"
      pattern: "@cf/baai/bge-base-en-v1.5"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/STATE.md"
      via: "both record DOCS-04 and DOCS-05 as complete in the same commit"
      pattern: "DOCS-05"
  prohibitions:
    - statement: "The style pass must NOT change any quoted validator message, command string, setting key, model identifier or status code."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from D-06; a style pass is exactly the operation most likely to reword a quoted string into something that no longer matches its source."
    - statement: "This phase must NOT change application code."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition from the CONTEXT.md phase boundary; the closing gate suite is what proves it across the whole phase."
    - statement: "Documentation must NOT print a secret value, a real Cloudflare resource identifier, or the admin development bypass literal."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less security prohibition; the closing scan is the phase-level proof."
    - statement: "The four locked ADR files must NOT be rewritten by the style pass."
      status: unresolved
      verification: judgment
      reason: "D-10 makes consolidation trim-and-link only; the ADRs already carry their own header convention and rewriting one would change a binding decision's text."
---

<objective>
Apply one style contract to the documents that survived, prove every claim in the whole set against
the code, and close the phase.

Purpose: the last two thirds of DOCS-04 — the style contract and the claim check — plus the phase
close-out that marks both requirements and records what shipped.

Output: five documents brought to the phase style, a claim check across the whole surviving set, a
filled validation record, DOCS-04 and DOCS-05 marked complete, and a green gate suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-CONTEXT.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-PATTERNS.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md
@.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-05-SUMMARY.md
</context>

<interfaces>
Consumed from plans 01 through 05, unchanged: the final document set, the topic map that names one
owner per topic, `npm run docs:lint`, `AGENTS.md`, the rewritten `README.md` and docs index.

This is the last plan in the phase. Nothing consumes its output inside the phase; the milestone audit
and `/gsd-verify-work` read the validation record and the requirements file it updates.

Nothing in this plan exports application code or changes a runtime contract.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`. Bare `node` is v26.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use `git commit -F`.
- **Do not touch the four locked ADRs, `docs/theming.md`, `docs/content-publishing.md`,
  `docs/customer-communications.md`, `docs/dependency-security.md` or
  `docs/mobile-lighthouse-baseline.md`.** The inventory marks all of them as already meeting the bar.
  The theming document is the style model, not a style target.
- A style pass rewords prose. It never rewords a quoted string. If a sentence contains a command, an
  error message, a setting key, a model identifier or a status code, the quoted part is copied
  through unchanged.
- Run `mise exec -- npm run docs:lint` before every commit, and the full gate suite before the last
  one.
</executor_notes>

<tasks>

<task type="auto">
  <name>Task 1: Bring the five remaining documents to the phase style without changing a quoted string</name>
  <read_first>
    - `docs/theming.md` lines 1-10 and `docs/database-migrations.md` lines 1-7 — the two header conventions, which are the target style.
    - `docs/ai-pipeline.md` — the whole file. It is accurate but heavily decorated, and its example prompts are drawn from the sample catalogue.
    - `docs/admin-authentication.md` — the whole file. Accurate and detailed; it needs decoration removed, not content changed. Note its deployment-guard mechanism and the status-code split by route prefix, both of which are quoted facts.
    - `docs/mcp-server-specification.md`, `docs/observability.md`, `docs/shopify-migration.md` — headers and section openings only; these need the header convention and a decoration sweep, not a rewrite.
    - `lib/ai/config.ts` — the model identifiers the AI pipeline document names, so you can confirm they are unchanged after editing.
    - `08.2-DOCS-INVENTORY.md` topic map — for each of the five, which topics it mentions but does not own, so those mentions become links.
  </read_first>
  <files>docs/ai-pipeline.md, docs/admin-authentication.md, docs/mcp-server-specification.md, docs/observability.md, docs/shopify-migration.md</files>
  <action>
Bring five documents to one style. This is an editing pass, not a rewrite: no section is deleted for
being long, and no fact leaves the repository.

Give each file the header convention: a level-one title, a one-line purpose sentence directly under
it, and a status line where the document is a specification or a runbook. Remove decorative emoji
from every heading in all five files, and from bullet prefixes where they are pure decoration.

Then the prose pass, in second person and imperative where the document is telling a reader to do
something. Split any sentence over about twenty-five words. Put every command, path, variable name,
setting key and identifier in code formatting. Where a document restates a topic it does not own
according to the topic map, cut the restatement to one sentence and link the owner.

In the AI pipeline document specifically, keep the model identifiers exactly as they are and label
the example prompts as drawn from the sample catalogue, so a reader adapting the pipeline to a
different catalogue is not misled into thinking the domain is baked in.

In the admin authentication document specifically, keep the deployment-guard description and the
status-code split by route prefix word for word; those are the two facts a reader most needs to trust.

Do not touch the four locked ADRs, the theming document, or the four documents the inventory records
as already meeting the bar. Do not reword a quoted string anywhere.
  </action>
  <acceptance_criteria>
    - All five files open with a level-one title, a purpose sentence, and a status line where applicable.
    - No heading in any documentation file contains a decorative emoji.
    - The model identifiers in the AI pipeline document are unchanged and still match the AI configuration module.
    - The admin authentication document still names both status codes and the deployment guard.
    - Fewer than one sentence in twenty across the five files runs over thirty words.
    - No locked ADR and none of the already-conforming documents were modified.
    - `npm run docs:lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- node -e "const fs=require('node:fs');const files=fs.readdirSync('docs').filter(f=>f.endsWith('.md')).map(f=>'docs/'+f).concat(['README.md','CONTRIBUTING.md','SECURITY.md','AGENTS.md']);const emoji=/[\u{1F300}-\u{1FAFF}\u{2190}-\u{21FF}\u{2460}-\u{24FF}\u{2600}-\u{27BF}\u{2B00}-\u{2BFF}\u{FE0F}]/u;const bad=[];for(const f of files){if(!fs.existsSync(f))continue;fs.readFileSync(f,'utf8').split('\n').forEach((l,i)=>{if(/^#{1,6} /.test(l)&&emoji.test(l))bad.push(f+':'+(i+1)+': '+l.trim());});}if(bad.length){console.log('DECORATED HEADING:');console.log(bad.slice(0,15).join('\n'));process.exit(1);}console.log('headings clean across',files.length,'files');"</automated>
    <fails_when>"DECORATED HEADING:" prints, meaning a heading somewhere in the documentation set still carries decoration the style contract removes. The check covers every documentation file, not only the five edited here, so a heading missed in an earlier plan also fails.</fails_when>
    <automated>RC=0; for f in docs/ai-pipeline.md docs/admin-authentication.md docs/mcp-server-specification.md docs/observability.md docs/shopify-migration.md; do /usr/bin/head -1 "$f" | /usr/bin/grep -q '^# ' || { echo "NO TITLE: $f"; RC=1; }; done; for s in '@cf/openai/gpt-oss-20b' '@cf/baai/bge-base-en-v1.5'; do /usr/bin/grep -qF "$s" docs/ai-pipeline.md || { echo "MODEL ID LOST: $s"; RC=1; }; /usr/bin/grep -qF "$s" lib/ai/config.ts || { echo "MODEL ID SOURCE DRIFT: $s"; RC=1; }; done; for s in '503' '401'; do /usr/bin/grep -qF "$s" docs/admin-authentication.md || { echo "STATUS CODE LOST: $s"; RC=1; }; done; test "$RC" -eq 0</automated>
    <fails_when>"NO TITLE:" prints, meaning the header convention was not applied to one of the five; or "MODEL ID LOST:" prints, meaning the style pass reworded a quoted identifier; or "MODEL ID SOURCE DRIFT:" prints, meaning the configuration module no longer sets that model and the document is now quoting something that does not exist; or "STATUS CODE LOST:" prints, meaning the two facts a reader most needs from the auth document were edited away.</fails_when>
    <automated>mise exec -- node -e "const fs=require('node:fs');const files=process.argv.slice(1);let total=0,long=0,worst=[];for(const f of files){const text=fs.readFileSync(f,'utf8').split('\n').filter(l=>!/^\s*[|>#\`\-*]/.test(l)&&!/^\s*\d+\./.test(l)).join(' ');for(const s of text.split(/(?<=[.!?])\s+/)){const w=s.trim().split(/\s+/).filter(Boolean).length;if(!w)continue;total++;if(w>30){long++;if(worst.length<5)worst.push(f+' ('+w+' words): '+s.trim().slice(0,90));}}}console.log('sentences',total,'over-30-words',long);if(total<50){console.log('SAMPLE TOO SMALL');process.exit(1);}if(long/total>0.05){console.log('TOO MANY LONG SENTENCES');console.log(worst.join('\n'));process.exit(1);}" docs/ai-pipeline.md docs/admin-authentication.md docs/mcp-server-specification.md docs/observability.md docs/shopify-migration.md</automated>
    <fails_when>"SAMPLE TOO SMALL" prints, meaning the extraction found almost no prose and the length check would have passed vacuously; or "TOO MANY LONG SENTENCES" prints with examples, meaning the direct-language rule was applied to headings and formatting but not to the prose it is actually about.</fails_when>
    <automated>RC=0; for f in docs/checkout-trust-boundary.md docs/webhooks-refunds-inventory.md docs/database-migrations.md docs/subscriptions.md docs/theming.md docs/content-publishing.md docs/customer-communications.md docs/dependency-security.md docs/mobile-lighthouse-baseline.md; do test -z "$(git diff --name-only HEAD~1 -- "$f")" || { echo "PROTECTED FILE MODIFIED: $f"; RC=1; }; done; test "$RC" -eq 0 && mise exec -- npm run docs:lint</automated>
    <fails_when>"PROTECTED FILE MODIFIED:" prints, meaning the style pass reached a locked ADR or a document the inventory already marked as meeting the bar — the two categories this task is explicitly told not to touch; or `docs:lint` exits non-zero.</fails_when>
  </verify>
  <done>Every document in the set reads the same way, and nothing that was quoted from the code has changed a character.</done>
</task>

<task type="auto">
  <name>Task 2: Claim-check the whole surviving set against the code, including the theming document's verbatim strings</name>
  <read_first>
    - `scripts/build-themes.mjs` — the validator's message strings, which `docs/theming.md` quotes and which this task proves are still identical on both sides.
    - `package.json` scripts — the authority for every command mentioned anywhere in the documentation set.
    - `wrangler.jsonc` — the authority for binding names.
    - `docs/runtime-configuration.md` — the public variable contract, whose variable names are checked against the code that reads them.
    - `08.2-DOCS-INVENTORY.md` topic map — the ownership rule the duplicate check enforces.
    - `.planning/phases/08-documentation-visual-qa-close-out/08-02-PLAN.md` task 3 — the claim-and-link-check method this task reuses at whole-set scale.
  </read_first>
  <files>docs/ai-pipeline.md, docs/admin-authentication.md, docs/mcp-server-specification.md, docs/observability.md, docs/shopify-migration.md</files>
  <action>
Run one claim check over every surviving document and fix the documents, never the check.

Four passes. First, commands: extract every script invocation from every documentation file and the
root files, and confirm each names a script in the package manifest. Second, paths: extract every
backticked relative path and every markdown link target and confirm each resolves, except paths a
document explicitly describes as files the reader creates. Third, variables: extract every uppercase
environment variable name mentioned and confirm the code reads it somewhere under the library,
application, scripts or workers trees, and that none is shown with a value. Fourth, quoted strings:
confirm the theming document's validator messages are still byte-identical to the messages the theme
build script prints — the style pass in task 1 must not have reached them, and the script must not
have changed underneath the document.

Then the duplication pass. For each topic in the inventory's map, confirm the owner is the only
document that states the rule and that every other mention is a link. Where you find a restatement
that survived earlier plans, cut it to a link. Record in the summary which duplications you closed.

Record the counts: commands checked, paths checked, variables checked, validator strings checked, and
duplications closed. A check that reports zero of something is a check that is not running.
  </action>
  <acceptance_criteria>
    - Every command named anywhere in the documentation set exists in the package manifest.
    - Every backticked relative path and every markdown link target resolves, except explicitly reader-created files.
    - Every environment variable named is read by the code, and none is shown with a value.
    - The theming document's validator messages are byte-identical to the theme build script's.
    - No topic in the map is stated in full by more than one document.
    - The summary records a non-zero count for each of the four passes.
  </acceptance_criteria>
  <verify>
    <automated>RC=0; N=0; for s in $(/usr/bin/grep -rohE 'npm run [a-zA-Z:_-]+' docs README.md AGENTS.md CONTRIBUTING.md SECURITY.md 2>/dev/null | /usr/bin/awk '{print $3}' | /usr/bin/sort -u); do N=$((N+1)); mise exec -- node -e "const p=require('./package.json'); process.exit(p.scripts['$s']?0:1)" || { echo "UNKNOWN SCRIPT: $s"; RC=1; }; done; echo "commands checked: $N"; test "$N" -ge 12 || { echo "TOO FEW COMMANDS"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"UNKNOWN SCRIPT:" prints, meaning a document still tells a reader to run something that does not exist; or "TOO FEW COMMANDS" prints, meaning the extraction matched almost nothing and the pass would report success against a set it never read.</fails_when>
    <automated>RC=0; P=0; for p in $(/usr/bin/grep -rohE '`[A-Za-z0-9_.-]+/[A-Za-z0-9_./-]+\.(md|ts|tsx|mjs|css|json|jsonc|yml|sql)`' docs README.md AGENTS.md CONTRIBUTING.md 2>/dev/null | /usr/bin/tr -d '`' | /usr/bin/sort -u); do P=$((P+1)); test -e "$p" || { echo "PATH DANGLES: $p"; RC=1; }; done; echo "paths checked: $P"; test "$P" -ge 30 || { echo "TOO FEW PATHS"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"PATH DANGLES:" prints, meaning a document cites a file that is not in the repository; or "TOO FEW PATHS" prints, meaning the whole-set path extraction found fewer citations than a single document produced in the previous phase, which means it is not scanning what it claims to scan.</fails_when>
    <automated>set -- 'missing required token' 'is not part of the frozen 23-token contract' 'must be a 6-digit hex colour, got' 'missing required @theme header label' 'stale generated file(s)'; RC=0; C=0; for s in "$@"; do C=$((C+1)); /usr/bin/grep -qF "$s" docs/theming.md || { echo "DOC MISSING: $s"; RC=1; }; /usr/bin/grep -qF "$s" scripts/build-themes.mjs || { echo "SOURCE DRIFT: $s"; RC=1; }; done; echo "validator strings checked: $C"; test "$RC" -eq 0</automated>
    <fails_when>"DOC MISSING:" prints, meaning a validator message the theming document is required to quote verbatim was reworded or removed during this phase; or "SOURCE DRIFT:" prints, meaning the theme build script no longer prints that message and the document is quoting something that no longer exists.</fails_when>
    <automated>RC=0; V=0; for v in $(/usr/bin/grep -rohE '\b(NEXT_PUBLIC_[A-Z0-9_]+|CLERK_[A-Z0-9_]+|STRIPE_[A-Z0-9_]+|ADMIN_[A-Z0-9_]+|EMAIL_[A-Z0-9_]+|ORDER_STATUS_[A-Z0-9_]+|GIFT_CARD_[A-Z0-9_]+|RESEND_[A-Z0-9_]+|MERCORA_[A-Z0-9_]+)\b' docs README.md AGENTS.md 2>/dev/null | /usr/bin/sort -u); do V=$((V+1)); /usr/bin/grep -rqF "$v" lib app scripts workers wrangler.jsonc 2>/dev/null || { echo "VARIABLE NOT USED BY CODE: $v"; RC=1; }; done; echo "variables checked: $V"; test "$V" -ge 8 || { echo "TOO FEW VARIABLES"; RC=1; }; H=$(/usr/bin/grep -rnE '^[[:space:]]*[A-Z][A-Z0-9_]+=[^[:space:]$]' docs README.md AGENTS.md 2>/dev/null | /usr/bin/grep -vE '=(your|<|\$|"?y?our|pk_|sk_|whsec_|1$)' | /usr/bin/head -5); test -z "$H" || { echo "VARIABLE WITH VALUE:"; echo "$H"; RC=1; }; test "$RC" -eq 0 && mise exec -- npm run docs:lint</automated>
    <fails_when>"VARIABLE NOT USED BY CODE:" prints, meaning a document names a variable the repository never reads, which sends a reader to configure something that does nothing; or "TOO FEW VARIABLES" prints, meaning the extraction is not matching the documented variables; or "VARIABLE WITH VALUE:" prints, meaning an assignment carrying something other than an obvious placeholder reached a published file.</fails_when>
  </verify>
  <done>Every claim the documentation set makes about this repository has been machine-checked against the repository, and each topic is stated once.</done>
</task>

<task type="auto">
  <name>Task 3: Fill the validation record, mark both requirements complete, and run the phase's closing gate suite</name>
  <read_first>
    - `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` — the record to complete; the per-task map rows are already laid out and need their status column set and any gap filled.
    - The six plan summaries in this phase — the commands each task actually ran, which are the evidence the map records.
    - `.planning/REQUIREMENTS.md` lines 60-100 — the two unchecked requirement lines and the status table rows.
    - `.planning/STATE.md` — the current position block, the blockers and concerns list, and the performance metrics table.
    - `.github/workflows/ci.yml` — the gate order the closing suite mirrors.
  </read_first>
  <files>.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
Close the phase.

Complete the validation record. Set the status column of every row in the per-task map from the six
summaries: which command ran, whether it passed, and when. Add any row a plan added that the map does
not have. Confirm both manual checks are described with instructions a person can follow: the
fresh-clone dry run by a coding assistant against the agent guide, and the clean-tree check after a
dev-server run. Fill the sign-off checklist and set the compliance flag in the record's front matter
once every row is green. Leave no placeholder token anywhere in the file.

Mark both requirements. In the requirements file, tick DOCS-04 and DOCS-05 and set their rows in the
status table to complete. Change nothing else in that file.

Update the project state. Record the phase as complete, say in two or three lines what shipped —
nine documents retired, one merge executed, a documentation gate added, the README rewritten, the
agent guide written and tracked — and move the position to the next phase. Add one line to the
concerns list recording that the documentation gate is a local check and is not wired into CI, so the
follow-up is on the record rather than in a summary nobody re-reads.

Then run the closing gate suite in the CI order and record the result in the summary. This is a
documentation phase; the suite's job is to prove it changed no runtime behaviour. If anything fails,
fix the documentation or revert the change that caused it — do not adjust a gate.
  </action>
  <acceptance_criteria>
    - The validation record contains no template placeholder token.
    - Every row of the per-task map has a status and a command.
    - Both manual checks are described with followable instructions.
    - The sign-off checklist is complete and the compliance flag is set.
    - Both requirements are ticked and both status rows say complete.
    - The project state records the phase complete, names what shipped, and carries the documentation-gate follow-up.
    - The full gate suite passes.
  </acceptance_criteria>
  <verify>
    <automated>V=.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md; RC=0; H=$(/usr/bin/grep -nE '\{[a-zA-Z_ .0-9/-]+\}' "$V" | /usr/bin/head -5); test -z "$H" || { echo "PLACEHOLDER SURVIVED:"; echo "$H"; RC=1; }; /usr/bin/grep -q 'nyquist_compliant: true' "$V" || { echo "COMPLIANCE FLAG NOT SET"; RC=1; }; for s in 'docs:lint' 'fresh clone' 'next dev'; do /usr/bin/grep -qiF "$s" "$V" || { echo "MAP ENTRY MISSING: $s"; RC=1; }; done; R=$(/usr/bin/grep -c '| 08\.2-' "$V"); test "$R" -ge 15 || { echo "TOO FEW MAP ROWS: $R"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>"PLACEHOLDER SURVIVED:" prints, meaning the record was left partly as the template, which is what blocked the previous phase; or "COMPLIANCE FLAG NOT SET" prints; or "MAP ENTRY MISSING:" prints, meaning the gate or a manual check is absent from the map; or "TOO FEW MAP ROWS:" prints, meaning the map does not cover the phase's tasks.</fails_when>
    <automated>RC=0; /usr/bin/grep -qE '^- \[x\] \*\*DOCS-04\*\*' .planning/REQUIREMENTS.md || { echo "DOCS-04 NOT TICKED"; RC=1; }; /usr/bin/grep -qE '^- \[x\] \*\*DOCS-05\*\*' .planning/REQUIREMENTS.md || { echo "DOCS-05 NOT TICKED"; RC=1; }; /usr/bin/grep -qE '\| DOCS-04 \| Phase 8\.2 \| Complete' .planning/REQUIREMENTS.md || { echo "DOCS-04 STATUS ROW NOT COMPLETE"; RC=1; }; /usr/bin/grep -qE '\| DOCS-05 \| Phase 8\.2 \| Complete' .planning/REQUIREMENTS.md || { echo "DOCS-05 STATUS ROW NOT COMPLETE"; RC=1; }; /usr/bin/grep -qiF 'docs:lint' .planning/STATE.md || { echo "FOLLOW-UP NOT RECORDED IN STATE"; RC=1; }; test "$RC" -eq 0</automated>
    <fails_when>a "NOT TICKED" or "STATUS ROW NOT COMPLETE" message prints, meaning the phase's two requirements are still open on the record even though the work shipped; or "FOLLOW-UP NOT RECORDED IN STATE" prints, meaning the fact that the documentation gate is not in CI lives only in a summary nobody re-reads.</fails_when>
    <automated>mise exec -- npm run docs:lint && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens && mise exec -- node scripts/build-themes.mjs --check && mise exec -- npm test && mise exec -- npm run test:workers && mise exec -- npm run build</automated>
    <fails_when>any gate exits non-zero, meaning a phase that was supposed to touch only documentation changed something the build, the type system, the token scanner, the theme validator or a test suite cares about.</fails_when>
  </verify>
  <done>The phase is closed on the record: both requirements ticked, the validation record filled and signed off, the CI-wiring follow-up captured, and every gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published documentation | Everything under `docs/` and every root markdown file is committed and readable by anyone with repository access |
| documentation → reader action | A quoted command or error message is copied out and used; a reworded one sends the reader nowhere |
| planning record → future work | The requirements file and the project state are what a later milestone audit reads; an unrecorded follow-up is a lost follow-up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.2-01 | Information Disclosure | the whole documentation set | high | mitigate | The closing claim check rejects any variable shown with a value that is not an obvious placeholder, and the documentation gate's credential-shape check runs before every commit |
| T-08.2-05 | Repudiation | quoted validator messages and model identifiers | medium | mitigate | The style pass is bounded by two-sided greps: every quoted validator message must match the theme build script and every model identifier must match the AI configuration module, so a reworded quote fails rather than shipping |
| T-08.2-02 | Tampering | the four locked ADR files and the conforming documents | high | mitigate | Task 1 asserts none of the nine protected files changed in the style commit |
| T-08.2-11 | Repudiation | an unrecorded CI-wiring follow-up | low | mitigate | Task 3 writes the follow-up into the project state, not only into a plan summary |
| T-08.2-SC | Tampering | npm/pip/cargo installs | low | accept | No plan in this phase runs a package-manager install; `08.2-RESEARCH.md` "Package Legitimacy Audit" records zero packages installed, upgraded or newly imported |
</threat_model>

## Artifacts this phase produces

| Artifact | Created by | This plan's part |
|---|---|---|
| `08.2-DOCS-INVENTORY.md` | 08.2-01 | consumed, not changed |
| `docs/CHANGELOG-docs.md` | 08.2-01 through 08.2-03 | not changed |
| `gsd-ingest-manifest.yaml` reconciliation | 08.2-01 through 08.2-03 | not changed |
| Nine retired documents | 08.2-01 through 08.2-03 | — |
| `scripts/docs-lint.mjs` and `npm run docs:lint` | 08.2-02 | run as a gate |
| `docs/DEPLOYMENT_SETUP.md`, `docs/architecture.md`, `CONTRIBUTING.md` | 08.2-03 | claim-checked |
| `AGENTS.md`, root `CLAUDE.md`, `docs/CLAUDE.md` | 08.2-04 | claim-checked |
| `README.md`, `docs/README.md`, `docs/images/presets/` | 08.2-05 | claim-checked |
| Style pass over the five remaining documents | 08.2-06 | all of it |
| `08.2-VALIDATION.md` filled and signed off | 08.2-04 (manual rows), 08.2-06 | the full map and the sign-off |
| DOCS-04 and DOCS-05 marked complete, state updated | 08.2-06 | all of it |

<verification>
- Every documentation heading is free of decoration and every surviving document carries the header convention.
- Fewer than one sentence in twenty across the styled documents runs over thirty words.
- Every command, path and variable in the whole set resolves against the code, with non-zero counts reported for each pass.
- The theming document's validator messages still match the theme build script byte for byte.
- No locked ADR and no already-conforming document was modified by the style pass.
- Both requirements are ticked, the validation record is filled and signed off, and the full gate suite is green.
</verification>

<success_criteria>
- The documentation set reads as though one person wrote it in one sitting.
- Nothing in it disagrees with the code, and a machine says so.
- The phase closes with both requirements recorded complete and the one deliberate follow-up written down.
</success_criteria>

<output>
Create `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-06-SUMMARY.md` when done.
</output>

