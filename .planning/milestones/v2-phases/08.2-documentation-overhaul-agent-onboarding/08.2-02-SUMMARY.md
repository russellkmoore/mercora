---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 02
subsystem: docs
tags: [documentation, docs-lint, gsd-ingest-manifest, retirement]

requires:
  - phase: 08.2-01
    provides: "The proven retirement contract, the 08.2-DOCS-INVENTORY.md verdict table and retirement queue, and docs/CHANGELOG-docs.md's first entry"
provides:
  - "Seven stale documents retired end to end: docs/admin-dashboard-specification.md, docs/mobile-improvements-actionable.md, docs/mobile-testing-automation.md, docs/mobile-ux-assessment.md, docs/ROADMAP.md, docs/o07-gift-cards-plan.md, docs/migration-reservations.md"
  - "scripts/docs-lint.mjs — the phase's zero-dependency documentation gate, wired as `npm run docs:lint`"
  - "docs/mobile-lighthouse-baseline.md's follow-ons repointed at the planning backlog instead of two now-deleted documents"
affects: ["08.2-03", "08.2-04", "08.2-05", "08.2-06"]

actuals:
  tokens: 27200
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "docs-lint.mjs follows scan-hardcoded-colors.mjs's zero-dependency Node convention exactly: node:fs/node:path only, JSDoc header naming the phase/plan it gates, one check function per violation class, single report, exit 1 on any violation"
    - "Manifest bidirectional invariant re-proven twice more (28→24→21 as each retirement batch lands), plus the locked-ADR guard (exactly 4, comment-stripped) as a permanent script check instead of a one-off inline grep"
    - "Retirement commits that touch a file scheduled for deletion in a later commit: task 1 edited docs/ROADMAP.md (retired in task 2) to remove its two dead cross-references, so no commit in the plan ever leaves a live document pointing at a deleted path"

key-files:
  created:
    - scripts/docs-lint.mjs
  modified:
    - gsd-ingest-manifest.yaml
    - README.md
    - docs/README.md
    - docs/mobile-lighthouse-baseline.md
    - docs/CHANGELOG-docs.md
    - docs/ROADMAP.md
    - package.json
    - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md

key-decisions:
  - "Fixed docs/ROADMAP.md's two dead cross-references (to mobile-improvements-actionable.md and mobile-ux-assessment.md) inside task 1's commit, even though ROADMAP.md itself is retired in task 2 — the plan's own executor_notes flagged this concurrency edge and required no commit to leave a live document pointing at a dead path."
  - "docs-lint.mjs's scan surface follows the plan's exact spec: every markdown file under docs/ plus README.md/CONTRIBUTING.md/SECURITY.md, with docs/CHANGELOG-docs.md excluded only from the dead-reference and relative-link checks (naming retired files is its job) but included in the header-convention check."
  - "Credential-shape regexes (UUID 8-4-4-4-12 hex, Stripe-style key prefix + 20 alnum chars) were pre-verified against zero false positives in the current tree before wiring, so the gate's first real run stayed green rather than requiring a follow-up fix."
  - "npm script name is `docs:lint` (not `lint:docs`), matching the plan's own verify commands and D-11's precedent naming convention for local gates (`scan:tokens`, `build:themes:check`)."

patterns-established:
  - "scripts/docs-lint.mjs: 7-check zero-dependency documentation validator (dead-reference, relative-link resolution, manifest reconciliation, locked-ADR guard, npm-script-name resolution, credential-shape rejection, header convention), consumed by plans 03-06 via `npm run docs:lint` instead of retyped grep loops."

requirements-completed: [DOCS-04]

coverage:
  - id: D1
    description: "Four documents (admin-dashboard-specification.md and three mobile docs) retired in one commit: deleted, de-linked from docs/README.md/root README.md, removed from the manifest, logged in the changelog with a named successor; docs/mobile-lighthouse-baseline.md's Two follow-ons block repointed at the planning backlog instead of the two deleted docs."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 1 <verify>: per-file STILL PRESENT / DEAD REF / NO CHANGELOG LINE loop across README.md, docs, AGENTS.md, CONTRIBUTING.md, SECURITY.md, gsd-ingest-manifest.yaml"
        status: pass
      - kind: other
        ref: "task 1 <verify>: manifest bidirectional reconciliation + locked-ADR guard (4 locked markers, all 4 ADR paths present)"
        status: pass
      - kind: other
        ref: "task 1 <verify>: Lighthouse baseline follow-ons block check (>=2 bullets, no retired-doc mention, no .planning link)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Three more documents (docs/ROADMAP.md, docs/o07-gift-cards-plan.md, docs/migration-reservations.md) retired in one commit, each changelog line naming a successor or contradicting evidence; manifest and docs/ tree agree exactly (21 = 21) with all four locked ADRs untouched."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 2 <verify>: per-file STILL PRESENT / DEAD REF / NO CHANGELOG LINE / RELATIVE DEAD LINK loop"
        status: pass
      - kind: other
        ref: "task 2 <verify>: successor-named scan (runtime-configuration.md, checkout-trust-boundary.md, migration 0023 all present in the changelog)"
        status: pass
      - kind: other
        ref: "task 2 <verify>: manifest reconciliation + count equality (N=21, M=21) + locked-ADR count (4)"
        status: pass
    human_judgment: false
  - id: D3
    description: "scripts/docs-lint.mjs implements all seven required checks, imports only Node built-ins, adds no dependency, is wired as `npm run docs:lint`, exits 0 on the tree and non-zero on a deliberately broken probe (naming both the broken link and the unregistered manifest entry), and leaves neither the CI workflow nor package.json's dependency set changed."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 3 <verify>: import-source scan (zero non-node: imports) + `mise exec -- npm run docs:lint` green"
        status: pass
      - kind: other
        ref: "task 3 <verify>: package.json script-entry check (docs:lint -> scripts/docs-lint.mjs) + CI workflow diff empty + no version-pinned dependency diff"
        status: pass
      - kind: other
        ref: "task 3 <verify>: broken-probe round trip (docs/__docs-lint-probe.md with a dangling link) — gate exits non-zero, probe removed, gate green again"
        status: pass
    human_judgment: false

duration: 26min
completed: 2026-09-05
status: complete
---

# Phase 08.2 Plan 02: Seven Document Retirements and the docs:lint Gate Summary

**Retired the remaining seven stale documents from 08.2-DOCS-INVENTORY.md's queue in two atomic commits, then turned the phase's grep-based checks into `scripts/docs-lint.mjs`, a zero-dependency validator wired as `npm run docs:lint` and proven to fail on a broken probe.**

## Performance

- **Duration:** 26 min
- **Started:** 2026-09-05T23:03:00Z (approx.)
- **Completed:** 2026-09-05T23:16:13Z
- **Tasks:** 3 completed
- **Files modified:** 14 (1 created, 6 modified, 7 deleted)

## Accomplishments

- Retired `docs/admin-dashboard-specification.md`, `docs/mobile-improvements-actionable.md`, `docs/mobile-testing-automation.md`, and `docs/mobile-ux-assessment.md` in one commit — deleted, de-linked from `docs/README.md` and the root `README.md`, removed from `gsd-ingest-manifest.yaml`, each logged in `docs/CHANGELOG-docs.md` with a named successor. Also fixed `docs/ROADMAP.md`'s two dead cross-references to two of these files inside this same commit, since `docs/ROADMAP.md` (itself retired next) would otherwise have pointed at a deleted path mid-plan.
- Repointed `docs/mobile-lighthouse-baseline.md`'s "Two follow-ons" block so both open items name the project's planning backlog instead of the two now-deleted documents, while keeping the baseline's own scores as the source of truth for mobile performance status.
- Retired `docs/ROADMAP.md`, `docs/o07-gift-cards-plan.md`, and `docs/migration-reservations.md` in a second commit — each changelog line names either a successor document (`docs/runtime-configuration.md` and `docs/checkout-trust-boundary.md` for gift cards) or the contradicting evidence (`migration 0023` applied against the ledger's own numbering claim). Manifest and `docs/` tree now agree bidirectionally at 21 = 21, four locked ADRs untouched throughout all seven retirements.
- Wrote `scripts/docs-lint.mjs` (zero-dependency, `node:fs`/`node:path` only) implementing all seven required checks — dead-path references, relative markdown link resolution, bidirectional manifest reconciliation, the locked-ADR guard (comment-stripped, exactly 4), npm-script-name resolution, credential-shaped strings (UUID and Stripe-key shapes), and the docs header convention — wired as `npm run docs:lint`. Proved it fails on a deliberately broken probe (naming both the broken link and the unregistered manifest entry) before cleaning the probe up and confirming green again. Neither the CI workflow nor `package.json`'s dependency set changed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Retire the four superseded admin and mobile documents and repoint the Lighthouse baseline** - `9374427` (docs)
2. **Task 2: Retire the stale roadmap copy, the gift-cards handoff record and the migration-number ledger** - `4fa2893` (docs)
3. **Task 3: Turn the checks into scripts/docs-lint.mjs and prove it fails on a broken probe** - `3aba7e8` (feat)

**Plan metadata:** committed separately (this SUMMARY + STATE/ROADMAP/REQUIREMENTS/VALIDATION updates)

## Files Created/Modified

- `scripts/docs-lint.mjs` - New zero-dependency documentation validator (7 checks), wired as `npm run docs:lint`
- `package.json` - Added the `docs:lint` script entry
- `gsd-ingest-manifest.yaml` - Removed the 7 retired entries; 21 manifest doc paths now match 21 `docs/*.md` files
- `README.md` - Removed the admin-dashboard and roadmap bullets from the docs index
- `docs/README.md` - Removed 6 bullets (admin, 3 mobile, roadmap, migration-reservations, gift-cards) and reworded the "where do I look" admin and business-user mentions
- `docs/ROADMAP.md` - Fixed its two dead cross-references in task 1's commit, then deleted in task 2
- `docs/mobile-lighthouse-baseline.md` - Repointed "Two follow-ons" block at the planning backlog
- `docs/CHANGELOG-docs.md` - 7 new retirement entries, each naming a successor or contradicting evidence
- `docs/admin-dashboard-specification.md` - Deleted (retired)
- `docs/mobile-improvements-actionable.md` - Deleted (retired)
- `docs/mobile-testing-automation.md` - Deleted (retired)
- `docs/mobile-ux-assessment.md` - Deleted (retired)
- `docs/o07-gift-cards-plan.md` - Deleted (retired)
- `docs/migration-reservations.md` - Deleted (retired)
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` - Marked this plan's three task rows green

## Decisions Made

- Fixed `docs/ROADMAP.md`'s two dead cross-references inside task 1's commit (even though the file itself is retired in task 2), per the plan's own executor_notes on this concurrency edge
- docs-lint.mjs's scan surface excludes `docs/CHANGELOG-docs.md` only from the dead-reference and relative-link checks, per the plan's explicit instruction that naming retired files is the changelog's entire job
- Credential-shape regexes were pre-verified against zero false positives in the existing tree before wiring, so the gate's first run stayed green
- Named the script `docs:lint` to match the plan's own verify commands and the project's existing local-gate naming convention (`scan:tokens`, `build:themes:check`)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

A `git add` command listing both modified paths and an already-deleted-and-staged path failed atomically (`fatal: pathspec ... did not match any files`), which combined with a stale `noclobber`-blocked temp file to produce a first attempt at task 1's commit with the wrong message and an incomplete file set. Diagnosed immediately (compared `git status --short` staged-vs-unstaged markers against what had actually been intended), staged the remaining files, and used `git commit --amend` twice — first to restore the complete file set, then to fix the message — since this was a same-session tooling error on a commit that had not been shared, not a prior legitimate commit. Verified the final commit's diff and message before proceeding to task 2.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All seven of this plan's retirements are done; `gsd-ingest-manifest.yaml` and the `docs/` tree agree in both directions (21 = 21); the four locked ADRs are untouched
- `npm run docs:lint` is available for plans 03-06 to call by name instead of retyping grep loops
- Plan 03 has its one remaining retirement queue item (`docs/STRIPE_INTEGRATION.md` merge into `docs/DEPLOYMENT_SETUP.md`) and D-11's `CONTRIBUTING.md` gate-list fix
- DOCS-04 stays open in REQUIREMENTS.md per the shared-ID gate — sibling plans in this phase (03-06) still declare it
- No blockers

---
*Phase: 08.2-documentation-overhaul-agent-onboarding*
*Completed: 2026-09-05*

## Self-Check: PASSED

- `scripts/docs-lint.mjs` — FOUND
- `docs/admin-dashboard-specification.md` — deleted as expected (no longer present)
- `docs/mobile-improvements-actionable.md` — deleted as expected (no longer present)
- `docs/mobile-testing-automation.md` — deleted as expected (no longer present)
- `docs/mobile-ux-assessment.md` — deleted as expected (no longer present)
- `docs/ROADMAP.md` — deleted as expected (no longer present)
- `docs/o07-gift-cards-plan.md` — deleted as expected (no longer present)
- `docs/migration-reservations.md` — deleted as expected (no longer present)
- Commit `9374427` — FOUND in `git log`
- Commit `4fa2893` — FOUND in `git log`
- Commit `3aba7e8` — FOUND in `git log`
- All three tasks' `<acceptance_criteria>` re-verified via their `<verify>` commands: PASS
- Plan-level `<verification>` re-run: 7 documents deleted/de-linked/logged, manifest=docs tree agreement (21=21), 4 locked ADRs intact, Lighthouse baseline keeps 2 follow-ons naming the backlog, `npm run docs:lint` green on tree and non-zero on broken probe, CI workflow and dependency set unchanged — PASS
