---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 03
subsystem: docs
tags: [documentation, deployment-runbook, stripe, architecture, contributing-gates]

requires:
  - phase: 08.2-02
    provides: "scripts/docs-lint.mjs wired as npm run docs:lint, and the proven retirement contract"
provides:
  - "docs/DEPLOYMENT_SETUP.md as the single setup-and-deploy runbook: trimmed of four generic filler sections, corrected to name .dev.vars as the local-secrets file, linked instead of restating the migrations, webhooks and runtime-configuration rules, and absorbing the Stripe guide's test-card table, test tax addresses, and account/tax setup steps"
  - "The ninth and final document retirement: docs/STRIPE_INTEGRATION.md deleted, de-linked, de-registered, and logged"
  - "docs/architecture.md with its historical database-schema diagram and its unverifiable security diagram both removed and replaced with pointers to the real schema directories, the migrations ADR, and the real security-headers module"
  - "CONTRIBUTING.md's development-workflow gate list expanded from a 3-command subset to the full 7-command order D-11 fixes, matching CI"
affects: ["08.2-04", "08.2-05", "08.2-06"]

actuals:
  tokens: 7100
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Consolidation is trim-and-link, not absorb-and-restate: the runbook links docs/database-migrations.md, docs/webhooks-refunds-inventory.md, and docs/runtime-configuration.md for the rules those documents own, and states only the one sentence a reader needs before following the link"
    - "Merge-then-retire in one commit: the Stripe guide's unique content (test cards, test tax addresses) moved into the runbook and the guide was deleted in the same commit as its three inbound links were fixed, per the plan 01 retirement contract"
    - "A diagram that depicts a control with no file behind it is deleted, not caveated — the historical database ER diagram and the unverifiable security diagram were both replaced with prose naming the actual source-of-truth files/modules, matching the precedent already applied to the retired api-architecture.md"

key-files:
  created: []
  modified:
    - docs/DEPLOYMENT_SETUP.md
    - docs/architecture.md
    - CONTRIBUTING.md
    - docs/README.md
    - README.md
    - docs/CLAUDE.md
    - docs/CHANGELOG-docs.md
    - gsd-ingest-manifest.yaml
    - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md
  deleted:
    - docs/STRIPE_INTEGRATION.md

key-decisions:
  - "The Stripe guide's account-creation, API-key, and tax-enablement steps were judged non-unique (DEPLOYMENT_SETUP.md already had near-identical steps) and were not duplicated; only the genuinely unique test-card table and test tax addresses were moved, as a new Step 6 in the runbook's existing numbered style."
  - "The migrations section keeps its existing trailing link to docs/database-migrations.md and gained one new leading sentence stating the rule, rather than restructuring the section — the plan's own executor_notes said not to restructure the numbered sequence."
  - "The closing 'Security Architecture' heading in docs/architecture.md was kept (only its diagram was deleted) since the task's instruction was to delete the diagram, not the section, and a short paragraph now occupies that space naming lib/security-headers.ts and linking admin-authentication.md and checkout-trust-boundary.md."
  - "Root README.md and docs/README.md's Stripe bullets were repointed at DEPLOYMENT_SETUP.md rather than deleted, since the topic (Stripe setup) still exists — consistent with the retirement contract's 'point at the new owner, don't delete the entry' rule. Plan 05 rewrites README.md wholesale, so this is an interim fix, not a final one."

patterns-established:
  - "Header convention retrofit on a merge target: docs/DEPLOYMENT_SETUP.md and docs/architecture.md both now open with a level-one title, a one-line purpose sentence, and a **Status:** line, matching the convention docs-lint.mjs's header-convention check enforces structurally (first line is a heading) and this plan's own <verify> blocks enforce for the purpose/status lines specifically."

requirements-completed: [DOCS-04]

coverage:
  - id: D1
    description: "docs/DEPLOYMENT_SETUP.md trimmed: four generic filler sections (monitoring, security checklist, troubleshooting, support resources) deleted, .env.local corrected to .dev.vars everywhere, the Stripe webhook event list replaced with a link to docs/webhooks-refunds-inventory.md, the environment variable list replaced with a link to docs/runtime-configuration.md keeping only the five boot-time values, the migrations section given a one-sentence rule plus its existing ADR link, the build-variable section left byte-identical, and the header convention applied."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 1 <verify>: section-removal negative scan (Monitoring/Security Checklist/Troubleshooting/Support Resources absent) + link-out presence scan (runtime-configuration.md, database-migrations.md, webhooks-refunds-inventory.md, build-with-public-env.mjs, MERCORA_ALLOW_PRODUCTION_MIGRATIONS) + title/status-line check"
        status: pass
      - kind: other
        ref: "task 1 <verify>: Stripe event-name negative scan + stale .env.local negative scan + .dev.vars presence check"
        status: pass
      - kind: other
        ref: "task 1 <verify>: npm-script existence loop over every `npm run` reference + `npm run docs:lint`"
        status: pass
    human_judgment: false
  - id: D2
    description: "docs/STRIPE_INTEGRATION.md's unique content (test-card table, test tax addresses) absorbed into the runbook as a new Step 6; the file itself retired (git rm), its manifest entry removed, its three inbound links (root README, docs index, docs/CLAUDE.md important-files list) repointed at the runbook, and one changelog line logged naming the runbook and the two owning ADRs (webhooks-refunds-inventory.md, checkout-trust-boundary.md). Ninth and final retirement of the phase; manifest and docs/ tree agree at 20 = 20."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 2 <verify>: file-absence check + dead-reference grep across README.md/docs/AGENTS.md/CONTRIBUTING.md/SECURITY.md/gsd-ingest-manifest.yaml (excluding the changelog) + changelog naming-scan for both the runbook and the webhooks ADR"
        status: pass
      - kind: other
        ref: "task 2 <verify>: unique-content-preserved scan ('4242', 'Stripe Tax') + webhook-event negative scan + ADR-link presence check"
        status: pass
      - kind: other
        ref: "task 2 <verify>: manifest bidirectional reconciliation (dangling/unregistered/count-equality) + locked-ADR guard (4 markers, all 4 paths present) + `npm run docs:lint`"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/architecture.md's historical database-schema ER diagram and its unverifiable security diagram both deleted and replaced with prose naming lib/db/schema/, migrations/, docs/database-migrations.md, lib/security-headers.ts, docs/admin-authentication.md, and docs/checkout-trust-boundary.md; header convention applied. CONTRIBUTING.md's gate list expanded from lint/typecheck/build to the full seven-command D-11 order (lint, typecheck, scan:tokens, build:themes:check, test, test:workers, build), with the numbered-list structure and prose otherwise unchanged and no eighth (docs:lint) command added."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 3 <verify>: historical-banner and erDiagram/CHAT_SESSIONS negative scans + WAF/Bot-Management negative scan + pointer-presence scan (lib/db/schema, migrations/, lib/security-headers.ts, database-migrations.md) + title/status-line check"
        status: pass
      - kind: other
        ref: "task 3 <verify>: seven-gate presence loop in CONTRIBUTING.md + docs:lint negative scan (no eighth gate added) + numbered-list-intact count check"
        status: pass
      - kind: other
        ref: "task 3 <verify>: npm-script existence loop + `npm run docs:lint` + `npm run lint` + `npm run typecheck`, all green"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-09-05
status: complete
---

# Phase 08.2 Plan 03: Deployment Runbook Consolidation, Stripe Guide Retirement, and Gate-List Fix Summary

**Made `docs/DEPLOYMENT_SETUP.md` the single trim-and-link setup-and-deploy runbook — absorbing the Stripe guide's test-card table and retiring its path (the ninth and last retirement) — cut two now-false sections from `docs/architecture.md`, and gave `CONTRIBUTING.md` the real seven-command CI gate order.**

## Performance

- **Duration:** 25 min
- **Tasks:** 3 completed
- **Files modified:** 10 (9 modified, 1 deleted)

## Accomplishments

- Trimmed `docs/DEPLOYMENT_SETUP.md`: deleted four generic filler sections (Monitoring & Maintenance, Security Checklist, Troubleshooting, Support Resources), corrected every local-secrets instruction from the stale `.env.local` to the file the OpenNext Cloudflare dev initialiser actually reads (`.dev.vars`), replaced the restated Stripe webhook event list with a link to `docs/webhooks-refunds-inventory.md`, replaced the full environment-variable list with a link to `docs/runtime-configuration.md` (keeping only the five values a first-time reader needs to boot: Clerk and Stripe publishable/secret keys plus the webhook signing secret), added a one-sentence migrations rule ahead of the existing `docs/database-migrations.md` link, left the build-variable section byte-identical, and applied the header convention (title, purpose sentence, `**Status:**` line) while dropping decorative emoji from every heading.
- Absorbed `docs/STRIPE_INTEGRATION.md`'s genuinely unique content — the test-card table and test tax addresses — into the runbook as a new Step 6 (its account-creation, API-key, and tax-enablement steps were judged already near-duplicated in the runbook and were not re-copied), then retired the file in the same commit: `git rm`, manifest entry removed, its three inbound links (root README, docs index, `docs/CLAUDE.md`'s important-files list) repointed at the runbook, and one changelog line logged naming the runbook and the two ADRs that own what was not moved (`docs/webhooks-refunds-inventory.md`, `docs/checkout-trust-boundary.md`). This is the ninth and final retirement of the phase; `gsd-ingest-manifest.yaml` and `docs/` now agree at 20 = 20, with all four locked ADRs untouched.
- Cut `docs/architecture.md`'s historical database-schema ER diagram (already banner-marked historical, predating the variant and order-ledger tables) and its closing security diagram (depicting Cloudflare WAF/Bot Management controls with no code behind them in this repository), replacing both with prose naming the real current sources of truth: `lib/db/schema/`, `migrations/`, `docs/database-migrations.md`, `lib/security-headers.ts`, `docs/admin-authentication.md`, and `docs/checkout-trust-boundary.md`. Applied the header convention to the document's own opening.
- Replaced `CONTRIBUTING.md`'s three-command gate subset (`lint`, `typecheck`, `build`) with the full seven-command D-11 order (`lint`, `typecheck`, `scan:tokens`, `build:themes:check`, `test`, `test:workers`, `build`), matching `.github/workflows/ci.yml`'s real step order, without adding the documentation gate as an eighth command.

## Task Commits

Each task was committed atomically:

1. **Task 1: Trim the deployment runbook to what is true here, and link out for every rule someone else owns** - `396de41` (docs)
2. **Task 2: Absorb the Stripe guide's unique content into the runbook and retire its path** - `c77cc10` (docs)
3. **Task 3: Cut the architecture document's historical and unverifiable sections, and fix the contributing gate list** - `d5b471a` (docs)

**Plan metadata:** committed separately (this SUMMARY + STATE/ROADMAP/REQUIREMENTS/VALIDATION updates)

## Files Created/Modified

- `docs/DEPLOYMENT_SETUP.md` - Trimmed to four fewer generic sections, corrected local-secrets filename, linked instead of restated migrations/webhook/runtime-config rules, absorbed the Stripe guide's test-card table and test tax addresses as a new Step 6, header convention applied
- `docs/architecture.md` - Historical database-schema diagram and unverifiable security diagram both removed and replaced with pointers to real code, header convention applied
- `CONTRIBUTING.md` - Development-workflow gate list expanded from 3 to the full 7 D-11 commands
- `docs/README.md` - Stripe bullet repointed at `DEPLOYMENT_SETUP.md`
- `README.md` - Stripe bullet repointed at `docs/DEPLOYMENT_SETUP.md`
- `docs/CLAUDE.md` - Important-files bullet repointed from the retired Stripe guide to the runbook
- `docs/CHANGELOG-docs.md` - One new retirement entry naming the runbook and the two owning ADRs
- `gsd-ingest-manifest.yaml` - Removed the `docs/STRIPE_INTEGRATION.md` entry (20 = 20 with the `docs/` tree)
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` - Marked this plan's three task rows green
- `docs/STRIPE_INTEGRATION.md` - Deleted (retired; ninth and final retirement)

## Decisions Made

- Did not duplicate the Stripe guide's account-creation, API-key, and tax-enablement steps into the runbook since DEPLOYMENT_SETUP.md already carried near-identical steps; only the test-card table and test tax addresses were genuinely unique and were moved
- Kept the "Security Architecture" heading in `docs/architecture.md`, deleting only its diagram, since the task instruction was to delete the diagram, not the section
- Repointed (not deleted) the Stripe bullets in both READMEs and `docs/CLAUDE.md`, since the topic still exists at a new owner — consistent with the phase's retirement contract; plan 05 rewrites `README.md` wholesale so this is an interim, not final, fix

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The document set this phase's remaining plans build on is now final for setup/deploy, architecture, and contributing content: plan 04's `AGENTS.md` and root/`docs/CLAUDE.md` trim can point at `docs/DEPLOYMENT_SETUP.md` as the single setup-and-deploy link target and match `CONTRIBUTING.md`'s gate list command for command
- `gsd-ingest-manifest.yaml` and the `docs/` tree agree bidirectionally at 20 = 20; all nine planned retirements across plans 01-03 are complete
- `npm run docs:lint`, `npm run lint`, and `npm run typecheck` are all green
- DOCS-04 stays open in REQUIREMENTS.md per the shared-ID gate — sibling plans 04, 05, and 06 still declare it
- No blockers

---
*Phase: 08.2-documentation-overhaul-agent-onboarding*
*Completed: 2026-09-05*

## Self-Check: PASSED

- `docs/DEPLOYMENT_SETUP.md` — FOUND, contains "Step 6: Test Payments and Tax", ".dev.vars", "webhooks-refunds-inventory.md", "runtime-configuration.md", "database-migrations.md"
- `docs/architecture.md` — FOUND, no `erDiagram`/`CHAT_SESSIONS`, no `WAF`/`Bot Management`, contains `lib/db/schema`, `migrations/`, `lib/security-headers.ts`
- `CONTRIBUTING.md` — FOUND, contains all seven gate commands, no `docs:lint`
- `docs/STRIPE_INTEGRATION.md` — deleted as expected (no longer present)
- Commit `396de41` — FOUND in `git log`
- Commit `c77cc10` — FOUND in `git log`
- Commit `d5b471a` — FOUND in `git log`
- All three tasks' `<acceptance_criteria>` re-verified via their `<verify>` commands: PASS
- Plan-level `<verification>` re-run: runbook holds the procedure with every rule linked out and the build-variable section byte-identical; Stripe guide's unique content survives with the registry consistent (20=20, 4 locked ADRs); architecture document carries no historical section and no diagram without a file behind it; contributing guide lists the full seven-command gate order and nothing more; `npm run docs:lint`, `npm run lint`, and `npm run typecheck` all green — PASS
