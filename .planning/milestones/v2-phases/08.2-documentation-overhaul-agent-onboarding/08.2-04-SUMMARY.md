---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 04
subsystem: docs
tags: [documentation, agent-onboarding, agents-md, claude-md]

requires:
  - phase: 08.2-03
    provides: "docs/DEPLOYMENT_SETUP.md as the single setup-and-deploy runbook and CONTRIBUTING.md's fixed seven-command gate order, which AGENTS.md agrees with command for command"
provides:
  - "A tracked root AGENTS.md: framing, prerequisites, ordered setup, the full CI gate order, production-safety rules, do-not-edit paths, a secrets and bindings inventory, and a docs map — with the Next.js managed block preserved verbatim as the final section"
  - "A tracked one-line root CLAUDE.md (@AGENTS.md)"
  - "docs/CLAUDE.md cut from 655 to 236 lines: AI-assistant context only, linking rather than restating setup, gates, environment variables, deployment, and the MCP tool inventory"
affects: ["08.2-05", "08.2-06"]

actuals:
  tokens: 4500
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "AGENTS.md is a fact sheet, not narrative: every sentence is either an instruction or a verified fact, each one checked this session against package.json, wrangler.jsonc, .github/workflows/ci.yml, or a whole-tree grep — not against a planning document's paraphrase"
    - "Secrets are named, never valued: every variable in AGENTS.md's Environment section is a bare backticked name with a one-line purpose, and the verify gate rejects any UPPERCASE=value assignment line as well as UUID- and key-prefix-shaped strings"
    - "The Next.js managed block is copied, not retyped, and pasted back as the file's last section between its exact markers — the verify gate diffs the committed block's body against the sentence the installed generator's source actually emits, so a next dev run after this commit finds the block unchanged and leaves the tree clean"

key-files:
  created:
    - AGENTS.md
  modified:
    - CLAUDE.md
    - docs/CLAUDE.md
    - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md

key-decisions:
  - "Corrected the gift-card HMAC-keys secret name during the claim check: 08.2-RESEARCH.md's inventory listed GIFT_CARD_CODE_HMAC_CURRENT_KEYS_JSON, but lib/gift-cards/config.ts reads GIFT_CARD_CODE_HMAC_KEYS_JSON (no CURRENT). AGENTS.md was written with the correct name from the start, per task 3's own instruction to fix the guide against the code rather than trust a planning document's paraphrase."
  - "Dropped the CI gate order and the raw NEXT_PUBLIC_*/secret example block that used to live under docs/CLAUDE.md's Testing and Development Guidelines sections, rather than keeping a second, second-hand copy alongside AGENTS.md's — one owning list (AGENTS.md's Gates section) instead of two that can drift, matching the pattern D-05 and the plan's own success criteria ask for."
  - "Kept a trimmed AI System (Volt Assistant) section in docs/CLAUDE.md even though it isn't named in the task's explicit keep list — it is AI-assistant context that is neither setup nor owned by another document, which is exactly the file's post-trim job description (D-08)."
  - "Rewrote docs/CLAUDE.md's Project Overview paragraph from 'AI-powered outdoor gear eCommerce platform' to the same themeable-storefront framing AGENTS.md opens with, so the two files describing this repository to an AI assistant do not contradict each other (plan success criteria: 'the AI-context document and the agent guide do not say the same thing twice')."

patterns-established:
  - "Docs map convention: AGENTS.md's Docs map section is one line per topic, format '<topic> → `<path>`', naming exactly one owning document per topic — the pattern docs/theming.md and the retirement contract already used, now extended to the file a coding assistant reads first."

requirements-completed: [DOCS-05]

coverage:
  - id: D1
    description: "AGENTS.md written at the repository root: framing paragraph naming the seven presets and the sample catalogue, prerequisites, a five-step numbered setup with the predev-hook explanation (no separate seed command), a ten-command CI-ordered gates block, six production-safety/token rules, a four-path do-not-edit list, a nine-variable secrets inventory (names only) plus eight worker bindings by name and kind, an eleven-topic docs map, and the Next.js managed block preserved verbatim as the final section."
    requirement: DOCS-05
    verification:
      - kind: other
        ref: "task 1 verify: Node block-comparison against the installed Next.js generator source (marker count, body-sentence match, source-drift check)"
        status: pass
      - kind: other
        ref: "task 1 verify: 12-command / 8-path / 9-docs-map presence-and-existence loop"
        status: pass
      - kind: other
        ref: "task 1 verify: expand-only/deploy:ci/MERCORA_ALLOW_PRODUCTION_MIGRATIONS rule-presence scan + UUID/key-shape credential scan + bypass-literal extraction-and-absence check"
        status: pass
      - kind: other
        ref: "task 1 verify: whole-script-name resolution loop against package.json + line-count floor (157 >= 90) + npm run docs:lint (0 violations)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Root CLAUDE.md (the pre-existing untracked one-line @AGENTS.md pointer) staged and committed unchanged, becoming tracked; docs/CLAUDE.md cut from 655 to 236 lines — git-status and recent-fixes sections and the example worker-config block removed outright; build-commands, environment-variables, deployment, MCP endpoint/tool inventory, and database-schema sections replaced with one-sentence links; project structure, admin summary, authentication summary, state management, code style/key patterns, and the important-files list kept and trimmed; header convention (title, purpose, Status line) applied."
    requirement: DOCS-05
    verification:
      - kind: other
        ref: "task 2 verify: git ls-files --error-unmatch on both root files + pointer-file line-count and content checks"
        status: pass
      - kind: other
        ref: "task 2 verify: stale-block negative scan (branch name, git-status, recent-fixes, database_id) + five-link presence scan + 120-300 line-count range check (236)"
        status: pass
      - kind: other
        ref: "task 2 verify: title/status-line check + kept-section presence scan (Project Structure, Code Style, Important Files) + npm run docs:lint (0 violations)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Claim-checked the finished AGENTS.md against the repository: 13 npm-run commands resolved against package.json, 12 backticked paths resolved on disk, 6 worker bindings cross-checked against wrangler.jsonc, 5 required secrets confirmed both listed and read by the code, and no variable shown with a value. Confirmed the phase validation record's two manual rows (fresh-clone dry run, next-dev clean-tree check) and its flagged-assumption sentence were already present at plan-creation time; marked this plan's three task rows green in 08.2-VALIDATION.md."
    requirement: DOCS-05
    verification:
      - kind: other
        ref: "task 3 verify: script-existence loop (13 commands, >= 8 floor) + backticked-path existence loop (12 paths, >= 10 floor) + binding-name cross-check against wrangler.jsonc"
        status: pass
      - kind: other
        ref: "task 3 verify: 5-secret listed-and-read-by-code loop + UPPERCASE=value negative scan"
        status: pass
      - kind: other
        ref: "task 3 verify: manual-row and flagged-assumption presence scan + template-placeholder absence check + npm run docs:lint (0 violations)"
        status: pass
    human_judgment: true
    rationale: "The manual-only verification this task records — whether a coding assistant working from a fresh clone can follow AGENTS.md end to end without asking for a fact the repository already holds, and whether the first next dev run after this phase's commits leaves the tree clean — is explicitly a human's job (08.2-VALIDATION.md's Manual-Only Verifications table). Neither was performed as part of this plan's execution; both remain open for a human to run before the phase closes."

duration: 15min
completed: 2026-09-05
status: complete
---

# Phase 08.2 Plan 04: AGENTS.md Agent-Onboarding Guide, Tracked CLAUDE.md, docs/CLAUDE.md Trim Summary

**Wrote a 157-line root `AGENTS.md` that a coding assistant can follow end to end (setup, ten-command CI gate order, production-safety rules, do-not-edit paths, secrets/bindings inventory, docs map, Next.js managed block verbatim), tracked the one-line root `CLAUDE.md`, and cut `docs/CLAUDE.md` from 655 to 236 lines of AI-assistant context that links rather than restates.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-05T23:29:19Z
- **Completed:** 2026-09-05T23:41:36Z
- **Tasks:** 3 completed
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments

- Wrote `AGENTS.md` at the repository root — the file a coding assistant reads first: a framing paragraph naming the seven presets and the sample outdoor-gear catalogue; prerequisites (pinned Node in three files, npm, the local `wrangler` binary, three accounts scoped to which features need them); a five-step numbered setup ending in `npm run dev`, with an explicit note that the `predev` hook builds themes and prepares the local database automatically — no separate seed command; a single fenced block with the ten CI-ordered gate commands (dependency audit through build), annotated to point at `CONTRIBUTING.md` for the contributor subset instead of a second list; six rules (expand-only migrations, `deploy` vs `deploy:ci`, the production-migration gate's dashboard-only location, no secrets in `wrangler.jsonc`, token-classes-only in the storefront, the three-file Node pin); a four-item do-not-edit list; a secrets section naming nine variables with one line each and no values, plus eight worker bindings by name and kind with no resource identifiers; an eleven-line docs map; and the Next.js `<!-- BEGIN:nextjs-agent-rules -->` managed block, copied byte-for-byte from the pre-existing file into a temp file first and pasted back verbatim as the last section.
- Staged the pre-existing, untracked, one-line root `CLAUDE.md` (`@AGENTS.md`) so it becomes tracked, unchanged. Cut `docs/CLAUDE.md` from 655 to 236 lines: removed the stale `feature/mach-alliance-implementation` git-status section, the dated "Recent Fixes" log, and the example `wrangler.jsonc` binding block outright; replaced the build-commands, environment-variables, deployment, MCP endpoint/tool-inventory, and database-schema sections with one-sentence links to `AGENTS.md`, `docs/runtime-configuration.md`, `docs/DEPLOYMENT_SETUP.md`, `docs/mcp-server-specification.md`, and `docs/database-migrations.md` respectively; kept and trimmed the project structure tree, the admin dashboard summary, the authentication summary, state management, code style/key patterns, and the important-files list; applied the header convention (title, one-line purpose, `**Status:**` line); and rewrote the project-overview paragraph to match `AGENTS.md`'s themeable-storefront framing instead of the old "AI-powered outdoor gear eCommerce platform" description.
- Claim-checked the finished `AGENTS.md` against the repository: 13 `npm run` commands resolved against `package.json`'s scripts, 12 backticked paths resolved on disk, all worker bindings named cross-checked against `wrangler.jsonc`, and the five required secrets (`CLERK_SECRET_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `ADMIN_VECTORIZE_TOKEN`, `ORDER_STATUS_SECRET`) confirmed both listed in the guide and read somewhere under `lib/`, `app/`, `scripts/`, or `workers/`. The claim check caught one real discrepancy in the phase's own research: the gift-card HMAC-keys variable is `GIFT_CARD_CODE_HMAC_KEYS_JSON` in `lib/gift-cards/config.ts`, not `GIFT_CARD_CODE_HMAC_CURRENT_KEYS_JSON` as `08.2-RESEARCH.md`'s inventory had it; `AGENTS.md` was written with the correct name. Confirmed the phase validation record's two manual rows and flagged-assumption sentence were already present (written at plan-creation time) and marked this plan's three task rows green in `08.2-VALIDATION.md`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write AGENTS.md — framing, prerequisites, ordered setup, gates, rules, do-not-edit paths, docs map, managed block last** - `05afc83` (docs)
2. **Task 2: Track the root pointer file and cut docs/CLAUDE.md down to context that is not setup** - `1d5d2a2` (docs)
3. **Task 3: Claim-check AGENTS.md against the code and record the human check it cannot replace** - `7d6957e` (docs)

**Plan metadata:** committed separately (this SUMMARY + STATE/ROADMAP/REQUIREMENTS updates)

## Files Created/Modified

- `AGENTS.md` - New: 157-line agent-onboarding guide (framing, prerequisites, setup, gates, rules, do-not-edit, environment, docs map, Next.js managed block)
- `CLAUDE.md` - Newly tracked (content unchanged: the one-line `@AGENTS.md` pointer)
- `docs/CLAUDE.md` - Cut from 655 to 236 lines; AI-assistant context only, links instead of restating setup/gates/environment/deployment/MCP inventory
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` - Marked this plan's three task rows green

## Decisions Made

- Corrected the gift-card HMAC-keys secret name in `AGENTS.md` (`GIFT_CARD_CODE_HMAC_KEYS_JSON`, not `_CURRENT_KEYS_JSON`) after the claim check found `08.2-RESEARCH.md`'s inventory disagreed with `lib/gift-cards/config.ts`
- Dropped the duplicate CI gate list and the raw environment-variable example block that used to live in `docs/CLAUDE.md`'s Testing and Development Guidelines sections, rather than keeping a second copy alongside `AGENTS.md`'s
- Kept a trimmed "AI System (Volt Assistant)" section in `docs/CLAUDE.md` even though it wasn't named in the task's explicit keep list, since it is AI-assistant context that belongs nowhere else
- Rewrote `docs/CLAUDE.md`'s project-overview paragraph to match `AGENTS.md`'s themeable-storefront framing so the two files do not contradict each other

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected a wrong secret variable name inherited from the phase research document**
- **Found during:** Task 3 (claim check)
- **Issue:** `08.2-RESEARCH.md`'s "AGENTS.md Content Inventory" section named the gift-card HMAC-keys environment variable `GIFT_CARD_CODE_HMAC_CURRENT_KEYS_JSON`. The actual variable read by `lib/gift-cards/config.ts` (`GIFT_CARD_HMAC_KEYS_ENV = "GIFT_CARD_CODE_HMAC_KEYS_JSON"`) has no `CURRENT` segment.
- **Fix:** Wrote the correct name, `GIFT_CARD_CODE_HMAC_KEYS_JSON`, into `AGENTS.md`'s Environment section from the first draft (verified against the source file before writing, per task 3's own instruction to fix the guide rather than trust a planning document's paraphrase).
- **Files modified:** `AGENTS.md` (written correctly; no separate fix commit needed)
- **Verification:** `grep -rqF "GIFT_CARD_CODE_HMAC_KEYS_JSON" lib/gift-cards/config.ts` matches; the old, wrong name does not appear anywhere in `AGENTS.md`
- **Committed in:** `05afc83` (part of the task 1 commit, since the error was caught before writing rather than after)

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug fix, caught during the claim-check task before it ever reached a committed file)
**Impact on plan:** No scope creep. The fix corrected a fact this plan's own completeness definition (`08.2-RESEARCH.md`'s content inventory) got wrong; `AGENTS.md` itself was never wrong.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `AGENTS.md` and `docs/CLAUDE.md` are both final for this milestone; plan 05's `README.md` rewrite and docs index can link `AGENTS.md` as "Setting up with an AI assistant" and describe `docs/CLAUDE.md` as AI-assistant context, not a setup document, per this plan's `<interfaces>` contract
- `npm run docs:lint`, `npm run lint`, and `npm run typecheck` are all green
- Two manual verifications remain open for a human, recorded in `08.2-VALIDATION.md`'s Manual-Only Verifications table: (1) a fresh-clone dry run of `AGENTS.md` with a coding assistant, and (2) confirming `mise exec -- npm run dev` leaves `git status --short` clean for `AGENTS.md` after the Next.js managed block is regenerated. Neither blocks this plan's completion; both are named as the human's job, not this executor's.
- DOCS-05 stays open in `REQUIREMENTS.md` per the shared-ID gate — sibling plan 06 still declares it
- No blockers

---
*Phase: 08.2-documentation-overhaul-agent-onboarding*
*Completed: 2026-09-05*

## Self-Check: PASSED

- `AGENTS.md` — FOUND, tracked, 157 lines, contains the Next.js managed block exactly once between its exact markers, byte-identical to the installed generator's output
- `CLAUDE.md` — FOUND, tracked, 1 line (`@AGENTS.md`)
- `docs/CLAUDE.md` — FOUND, 236 lines (within the required 120-300 range), no `feature/mach-alliance-implementation`, no `Current Git Status`, no `Recent Fixes`, no `database_id`
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` — FOUND, 08.2-04-01/02/03 rows all read `✅ green`
- Commit `05afc83` — FOUND in `git log`
- Commit `1d5d2a2` — FOUND in `git log`
- Commit `7d6957e` — FOUND in `git log`
- All three tasks' `<acceptance_criteria>` re-verified via their `<verify>` commands: PASS
- Plan-level `<verification>` re-run: `AGENTS.md` exists/tracked with the full gate order, rules, do-not-edit paths, and docs map, every command and path resolving; the managed block appears exactly once and verbatim; the root pointer file is tracked and still one line; `docs/CLAUDE.md` is 236 lines with no stale content and links to every topic it stopped covering; no secret value, resource identifier, or bypass literal appears in any file this plan touched; `npm run docs:lint`, `npm run lint`, and `npm run typecheck` all green — PASS
