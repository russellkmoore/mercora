---
phase: 08.2-documentation-overhaul-agent-onboarding
plan: 01
subsystem: docs
tags: [documentation, docs-inventory, gsd-ingest-manifest, retirement-machine]

requires: []
provides:
  - "A proven retirement contract: delete + fix every inbound link + repoint the ingest manifest + log the changelog, all in one commit"
  - "08.2-DOCS-INVENTORY.md — a verdict (KEEP/KEEP-TRIM/MERGE/RETIRE) with a one-line reason for all 28 docs/*.md files and the 4 root files, a topic-to-owner map with no double-owned topic, and the retirement queue plans 02 and 03 execute"
  - "docs/CHANGELOG-docs.md — the append-only living log, header and first entry"
  - "gsd-ingest-manifest.yaml reconciled bidirectionally with the docs/ tree (28 entries = 28 files)"
  - "Both stale D1 identifiers replaced with the labeled-placeholder convention"
affects: ["08.2-02", "08.2-03", "08.2-04", "08.2-05", "08.2-06"]

actuals:
  tokens: 15000
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Retirement contract: git rm the doc, grep-fix every inbound reference (README.md, docs/README.md, docs/CLAUDE.md, any doc that lists it in an ASCII tree), remove the gsd-ingest-manifest.yaml entry, add one docs/CHANGELOG-docs.md line naming it in backticks — all in one commit"
    - "Manifest bidirectional invariant: count of `path: docs/` entries in gsd-ingest-manifest.yaml equals count of docs/*.md files, checked both directions (dangling path, unregistered file)"
    - "Labeled-placeholder convention for example Cloudflare resource IDs: `your-d1-database-id-here`, never a real-looking value"

key-files:
  created:
    - docs/CHANGELOG-docs.md
    - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md
  modified:
    - gsd-ingest-manifest.yaml
    - README.md
    - docs/README.md
    - docs/CLAUDE.md
    - docs/STRIPE_INTEGRATION.md
    - docs/DEPLOYMENT_SETUP.md
    - .planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md

key-decisions:
  - "Copied every verdict and reason from 08.2-RESEARCH.md's Docs Inventory Table verbatim rather than re-deriving any of them, per the plan's own executor_notes."
  - "docs/DEPLOYMENT_SETUP.md recorded as KEEP-TRIM (not plain KEEP) because it is the merge target for STRIPE_INTEGRATION.md in plan 03 — it keeps its path but is both trimmed and expanded."
  - "Gift cards' owning document is docs/checkout-trust-boundary.md's Optional capabilities section, not a new doc, since o07-gift-cards-plan.md (the PRD being retired in plan 02) has already had its living facts absorbed there and in docs/runtime-configuration.md."
  - "Flagged (not resolved): AGENTS.md onboarding completeness is currently defined by the fact inventory in 08.2-RESEARCH.md, not by an actual fresh-clone dry run — recorded in the inventory's notes section and in 08.2-VALIDATION.md's Manual-Only Verifications table for a human to close later."

patterns-established:
  - "Dense pipe-table verdict format (File | Lines | Manifest Type | Historical Banner | Verdict | Reason) copied from 08-QA-MATRIX.md's style, reused by the phase's inventory record."

requirements-completed: [DOCS-04]

coverage:
  - id: D1
    description: "One document (docs/api-architecture.md) carried through every layer of the retirement machine in a single commit: deleted, de-linked from README.md/docs/README.md (x2)/docs/CLAUDE.md/docs/STRIPE_INTEGRATION.md's file tree, removed from gsd-ingest-manifest.yaml, and logged in the new docs/CHANGELOG-docs.md."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 1 <verify>: dead-reference grep across README.md/docs/AGENTS.md/CONTRIBUTING.md/SECURITY.md/gsd-ingest-manifest.yaml (excluding the changelog) returns zero hits"
        status: pass
      - kind: other
        ref: "task 1 <verify>: manifest/docs bidirectional reconciliation (N=28, M=28, zero dangling/unregistered paths)"
        status: pass
      - kind: other
        ref: "task 1 <verify>: all four locked ADRs present and listed, exactly 4 locked: true markers"
        status: pass
      - kind: other
        ref: "task 1 <verify>: docs/CHANGELOG-docs.md and 08.2-DOCS-INVENTORY.md shape check"
        status: pass
    human_judgment: false
  - id: D2
    description: "All 28 docs/*.md files plus the 4 root files (README.md, AGENTS.md, CONTRIBUTING.md, SECURITY.md) carry a verdict from the fixed set and a one-line reason; a topic-to-owner map covers 14 topics with no topic double-owned; a retirement queue names plan 02's seven retirements and plan 03's one merge."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 2 <verify>: per-file classification loop (0 UNCLASSIFIED, 0 UNCLASSIFIED ROOT), verdict-row count 45 >= 28"
        status: pass
      - kind: other
        ref: "task 2 <verify>: topic-presence loop for all 14 required topics, webhook-owner check, retirement-queue check"
        status: pass
      - kind: other
        ref: "task 2 <verify>: framing-record scan (themeable/seven/sample) and notes-section presence"
        status: pass
    human_judgment: false
  - id: D3
    description: "The two stale, real-looking D1 database identifiers in docs/DEPLOYMENT_SETUP.md and docs/CLAUDE.md are replaced with the shared labeled placeholder, and a whole-docs-tree scan confirms no hex-shaped resource ID, no real-length provider key, and no admin dev-bypass literal is published anywhere."
    requirement: DOCS-04
    verification:
      - kind: other
        ref: "task 3 <verify>: UUID-shape grep across docs/README.md/AGENTS.md/CONTRIBUTING.md/SECURITY.md returns zero hits"
        status: pass
      - kind: other
        ref: "task 3 <verify>: provider-key-shape grep returns zero hits; bypass literal extracted live from lib/auth/admin-middleware.ts and grepped, zero hits"
        status: pass
      - kind: other
        ref: "task 3 <verify>: both files carry the placeholder; docs/CLAUDE.md diff is exactly 2 changed lines (1 insertion, 1 deletion)"
        status: pass
    human_judgment: false

duration: 20min
completed: 2026-09-05
status: complete
---

# Phase 08.2 Plan 01: Docs Retirement Tracer, Full Inventory, and Placeholder Sweep Summary

**Retired `docs/api-architecture.md` end to end in one commit (proving the retirement contract), classified all 28 `docs/` files plus 4 root files into a verdict-and-reason inventory with a 14-topic ownership map, and replaced two stale D1 identifiers with the repo's labeled-placeholder convention.**

## Performance

- **Duration:** 20 min
- **Tasks:** 3 completed
- **Files modified:** 9 (2 created, 6 modified, 1 deleted)

## Accomplishments

- Proved the exact retirement contract plans 02 and 03 will repeat eight more times: delete with `git rm`, fix every inbound reference (root README, docs index x2, AI-context "Important Files" list, Stripe guide's ASCII file tree — with its box-drawing connector corrected from `├──` to `└──` now that it's the last child), repoint `gsd-ingest-manifest.yaml`, and log one line in the new `docs/CHANGELOG-docs.md`
- Reconciled `gsd-ingest-manifest.yaml` bidirectionally: removed the retired entry, added the two files that existed but were never registered (`docs/theming.md` as SPEC, `docs/mobile-lighthouse-baseline.md` as DOC), and registered the new changelog file — 28 manifest entries now match 28 `docs/*.md` files exactly
- Built `08.2-DOCS-INVENTORY.md`'s full verdict table (32 rows: 28 docs files + 4 root files), copying every verdict and reason from `08.2-RESEARCH.md` rather than re-deriving them, plus a topic-to-owner map (14 topics, zero double-owned) and a 9-row retirement queue (7 to plan 02, 1 merge to plan 03)
- Replaced the two real-looking D1 database IDs in `docs/DEPLOYMENT_SETUP.md` and `docs/CLAUDE.md` with the shared `your-d1-database-id-here` placeholder and confirmed by whole-tree scan that no hex-shaped identifier, real-length provider key, or the admin dev-bypass literal appears anywhere in `docs/` or the root markdown files

## Task Commits

Each task was committed atomically:

1. **Task 1: End-to-end retirement tracer** - `d1835c8` (feat)
2. **Task 2: Classify all 28 documents, map every topic to one owner** - `28107ea` (docs)
3. **Task 3: Replace stale D1 identifiers, prove no credential in docs** - `0611bee` (fix)

**Plan metadata:** committed separately (this SUMMARY + STATE/ROADMAP/REQUIREMENTS/VALIDATION updates)

## Files Created/Modified

- `docs/CHANGELOG-docs.md` - New living log; header, status line, first dated entry naming the retirement
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md` - Full verdict table, topic-owner map, retirement queue, notes
- `gsd-ingest-manifest.yaml` - Removed the retired entry, added 3 entries (theming.md, mobile-lighthouse-baseline.md, CHANGELOG-docs.md)
- `README.md` - Removed the docs-index bullet pointing at the retired file
- `docs/README.md` - Removed the Technical Architecture bullet and reworded the "where do I look" mention to point at the owning ADRs
- `docs/CLAUDE.md` - Removed the "Important Files to Reference" bullet; separately, replaced the stale D1 identifier with the labeled placeholder
- `docs/STRIPE_INTEGRATION.md` - Removed the retired file's line from the ASCII file-structure tree, correcting the now-last child's connector glyph
- `docs/DEPLOYMENT_SETUP.md` - Replaced the stale D1 identifier example comment with the labeled placeholder
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-VALIDATION.md` - Marked this plan's three task rows green
- `docs/api-architecture.md` - Deleted (retired)

## Decisions Made

- Copied verdicts/reasons from research verbatim rather than re-deriving any (executor_notes instruction)
- `docs/DEPLOYMENT_SETUP.md` classified KEEP-TRIM, not plain KEEP, because it is plan 03's merge target
- Gift cards' owning document is `docs/checkout-trust-boundary.md`'s Optional capabilities section, since the retiring PRD's living facts already moved there
- Flagged, not resolved: AGENTS.md's onboarding completeness is currently defined by the research fact inventory, not a fresh-clone dry run — recorded for a human to close

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 02 has a proven retirement path and a 7-item work list (RETIRE rows) to execute against
- Plan 03 has its 1-item work list (the STRIPE_INTEGRATION.md merge into DEPLOYMENT_SETUP.md) and D-11's gate-list fix
- `gsd-ingest-manifest.yaml` and the `docs/` tree agree in both directions; `/gsd-ingest-docs` resolves every path
- No blockers. DOCS-04 stays open in REQUIREMENTS.md per the shared-ID gate — sibling plans in this phase still declare it

---
*Phase: 08.2-documentation-overhaul-agent-onboarding*
*Completed: 2026-09-05*

## Self-Check: PASSED

- `docs/CHANGELOG-docs.md` — FOUND
- `.planning/phases/08.2-documentation-overhaul-agent-onboarding/08.2-DOCS-INVENTORY.md` — FOUND
- `docs/api-architecture.md` — deleted as expected (no longer present)
- Commit `d1835c8` — FOUND in `git log`
- Commit `28107ea` — FOUND in `git log`
- Commit `0611bee` — FOUND in `git log`
- All three tasks' `<acceptance_criteria>` re-verified via their `<verify>` commands: PASS
- Plan-level `<verification>` re-run: manifest/docs bidirectional agreement (28=28), 4 locked ADRs intact, all 32 verdict rows present, zero credential/identifier hits — PASS
</content>
