---
phase: 06-theme-file-mechanism-presets
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - themes/midnight.css
  - themes/luxe.css
  - themes/index.generated.css
  - lib/themes/manifest.generated.ts
  - .planning/todos/pending/theme-contract-dropped-properties.md
autonomous: true
requirements: [THEME-04]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Three presets ship: volt-dark (the manifest default, unchanged in value), midnight (a second dark theme, indigo-black with a violet accent) and luxe (light, warm ivory with a champagne-gold accent and a serif display face) — D-01."
    - "Each new theme file declares exactly the 23 frozen tokens inside exactly one attribute-selector block whose name equals its filename stem, with every colour value a 6-digit lowercase hex, and passes the plan 06-01 validator unmodified (D-06, D-07)."
    - "Both new files carry the theme header comment with a label, an industry line and a synopsis, copied verbatim from the UI-SPEC preset sections so the admin cards read the intended copy (D-05, D-14)."
    - "Neither new theme file declares a font-loading at-rule or any external resource reference; luxe's display token references the CSS variable the root layout already exposes (D-02)."
    - "Every token value comes from the UI-SPEC's pre-computed conversion table, including its two deliberate accessibility corrections (luxe's focus ring darkened to reach the non-text contrast threshold, midnight's on-primary swapped to its own dark surface value to reach the text contrast threshold). No hex is re-derived from the direction document's oklch values."
    - "The direction document's extra properties (shadow, border width, image aspect, a second accent, a mono font, letter spacing) are NOT added as tokens; midnight folds the dropped second accent's cyan into its info token and luxe folds the dropped sunken surface into its border token, per the UI-SPEC rename map. The dropped properties are recorded as a backlog note (D-03)."
    - "The generated barrel and manifest regenerate to three entries, and the check-mode gate passes against the committed output."
    - "Setting the stored appearance theme to each preset name in turn makes the storefront serve that name in the html element's theme attribute and serve that theme's declaration block in the stylesheet, with no code change and no redeploy (D-01, THEME-02 wiring proof)."
    - "The whole-tree token scan stays at 0 violations — theme files are the scanner's declared source-of-truth directory and are skipped entirely, so adding two of them changes nothing about the sweep's verdict."
  artifacts:
    - path: "themes/midnight.css"
      provides: "The second dark preset: 23 tokens in one attribute-selector block with a theme header"
      contains: "midnight"
    - path: "themes/luxe.css"
      provides: "The light preset: 23 tokens in one attribute-selector block with a theme header"
      contains: "luxe"
    - path: ".planning/todos/pending/theme-contract-dropped-properties.md"
      provides: "Backlog record of the direction document's properties dropped under D-03"
  key_links:
    - from: "themes/index.generated.css"
      to: "themes/luxe.css"
      via: "generated import line, regenerated by the build script after the file is added"
      pattern: "luxe\\.css"
    - from: "lib/themes/manifest.generated.ts"
      to: "themes/midnight.css"
      via: "generated manifest entry carrying the parsed header metadata and the 23 token values"
      pattern: "midnight"
    - from: "themes/luxe.css"
      to: "app/layout.tsx"
      via: "the display-font token references the CSS variable the root layout's font declaration exposes"
      pattern: "font-cormorant-garamond"
  prohibitions:
    - statement: "The mechanism must NOT allow a theme file to inject anything beyond token values (no at-rules, no second selector, no external resource reference). A theme is data, not code."
      status: unresolved
      verification: judgment
      reason: "Recalled project prohibition, authored descriptor-less; this is the first plan that authors theme files by hand, so the validator's rules are exercised against real content here."
    - statement: "The 23-token contract must NOT be widened, narrowed, or renamed by this phase (Phase 5 D-01, one-way)."
      status: unresolved
      verification: judgment
      reason: "Recalled project prohibition, authored descriptor-less; the temptation to add a shadow or border-width token to express the direction document's presets lands squarely on this plan, and D-03 forbids it."
    - statement: "The admin theme selection must NOT let a non-manifest value reach the theme attribute or the client."
      status: unresolved
      verification: judgment
      reason: "Recalled project prohibition, authored descriptor-less; binds here because this plan grows the allow-list, and a preset that fails validation must never enter it."
    - statement: "Theme resolution must NOT be cached across requests in an isolate-level variable."
      status: unresolved
      verification: judgment
      reason: "Recalled project prohibition, authored descriptor-less; the preset-switch proof in Task 3 is the observable consequence — a cached resolution would make the switch appear not to take effect."
---

<objective>
Ship the two new presets the milestone promised: `midnight`, a second dark theme that proves the
mechanism is not volt-dark recoloured, and `luxe`, the light theme that is the token sweep's acid
test. Both are authored purely as 23-token CSS files with a metadata header, and both pass the
build-time validator with no exceptions carved for them.

Purpose: THEME-04's first half. The second half — the visual QA that decides whether a light preset
exposes dark-tuned shadows and overlays — is plan 06-05, which cannot start until these files exist.

Output: `themes/midnight.css`, `themes/luxe.css`, a regenerated three-entry manifest and barrel, and
a backlog note for the properties D-03 dropped.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-theme-file-mechanism-presets/06-CONTEXT.md
@.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md
@.planning/phases/06-theme-file-mechanism-presets/06-01-SUMMARY.md
@.planning/phases/06-theme-file-mechanism-presets/06-02-SUMMARY.md
</context>

<interfaces>
This plan authors data, not code. It consumes, unchanged:

- The validator rules and the generator CLI from plan 06-01 (`scripts/build-themes.mjs`, its
  `--check` flag, its `[build-themes]` message prefix).
- The 23 token names from `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md`
  section 1 — frozen, one-way, not extensible by this plan.
- The Cormorant Garamond CSS variable name exposed by `app/layout.tsx` from plan 06-02, which
  luxe's display-font token references.
- `APPEARANCE_THEME_SETTING_KEY` and `APPEARANCE_SETTINGS_CATEGORY` from plan 06-02, used by
  Task 3's local D1 write to prove a preset switch takes effect.

It produces two new manifest entry names — `midnight` and `luxe` — that plan 06-04's admin card grid
and plan 06-05's QA pass both consume.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: themes/midnight.css - the second dark preset</name>
  <read_first>
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 248-276 - the complete Midnight token table: every token, its value, and its provenance, including the two rows marked as deliberate deviations and the four-step radius progression. Copy these values; do not re-derive them from oklch.
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 193-214 - the rename map showing which direction-document names map to which contract token, which are dropped, and where the dropped second accent's hue is folded.
    - `themes/volt-dark.css` - the exact file structure to mirror: head comment, one attribute-selector block, the four commented token groups (main colours, inverse colours, shape, type), and the declaration order.
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` section 1 - the canonical list of the 23 custom-property names, so none is misspelled.
  </read_first>
  <files>themes/midnight.css</files>
  <action>
Create the midnight theme file mirroring the reference file's structure exactly: a head comment, the
theme header comment carrying the label, industry and synopsis strings copied verbatim from the
UI-SPEC's Midnight header line, then a single attribute-selector block keyed on the filename stem,
holding all 23 custom properties in the reference file's group order with the same group comments.

Take every value from the UI-SPEC's Midnight table. Two rows there are deliberate corrections rather
than transcriptions of the direction document, and both must be honoured as written: the on-primary
token uses the theme's own dark surface value rather than the document's near-white, because the
document's value fails the text contrast threshold against the violet accent; and the info token
carries the cyan the document assigned to a second accent that D-03 drops, since the roles overlap
and no token may be added.

The sans and display font tokens both keep the existing Geist stack, byte-identical to the reference
file's values. The direction document distinguishes them only by weight and letter spacing, and
neither is a token in the frozen contract, so there is no distinct face to load for this preset.

Write no at-rule, no second selector, no external resource reference, and no property outside the
23. Every colour value is 6-digit lowercase hex.
  </action>
  <acceptance_criteria>
    - `test "$(/usr/bin/grep -c '^\s*--store-' themes/midnight.css)" -eq 23` and `test "$(/usr/bin/grep -c '{' themes/midnight.css)" -eq 1`.
    - `mise exec -- node scripts/build-themes.mjs --path themes --json` reports zero errors for the new file.
    - Every colour declaration in the file matches the 6-digit lowercase hex form: `/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/midnight.css` is 17.
    - The file's theme header carries a label, an industry line and a synopsis, and the label string matches the UI-SPEC's Midnight header line.
    - `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts` exits 0, with the loop now covering the new file.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c '^\s*--store-' themes/midnight.css)" -eq 23 && test "$(/usr/bin/grep -c '{' themes/midnight.css)" -eq 1 && test "$(/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/midnight.css)" -eq 17 && mise exec -- node scripts/build-themes.mjs && mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts</automated>
    <fails_when>a count assertion is not equal (wrong token count, more than one block, or a colour value that is not 6-digit lowercase hex); or the generator exits non-zero and prints a build-themes file-and-line message; or the vitest summary reports a failing parity assertion between the new file and its manifest entry.</fails_when>
  </verify>
  <done>A second dark preset exists as a pure 23-token data file, passes the validator, and has a manifest entry whose values match its CSS byte for byte.</done>
</task>

<task type="auto">
  <name>Task 2: themes/luxe.css - the light preset and the sweep's acid test</name>
  <read_first>
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 216-246 - the complete Luxe token table with every value and its provenance, the flagged focus-ring deviation and its measured contrast ratio, the four radius tokens all set flat per the direction's square-corner intent, and the display-font stack that references the layout's CSS variable. Copy these values.
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 193-214 - the rename map, specifically the row folding the direction's sunken surface into the border token as the hairline-divider tone.
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 282-302 - confirms the display face is loaded once in the root layout and that no theme file may declare a font-loading at-rule.
    - `themes/volt-dark.css` and `themes/midnight.css` from Task 1 - the file structure to mirror.
    - `.planning/phases/05-token-contract-component-sweep/05-TOKEN-MAP.md` section 3 - what the five inverse tokens are actually used for (the two drawers, the Stripe host, the email templates), which is why a light preset inverts them rather than copying the dark theme's light values.
  </read_first>
  <files>themes/luxe.css</files>
  <action>
Create the luxe theme file mirroring the same structure, with its theme header strings copied
verbatim from the UI-SPEC's Luxe header line.

Take every value from the UI-SPEC's Luxe table. One row is a deliberate accessibility correction
rather than a transcription: the focus-ring token is darkened away from the literal accent value
because the accent on the ivory surface falls below the non-text contrast threshold. Use the
corrected value as written; re-deriving from the direction document's oklch would silently
reintroduce the failure the UI-SPEC already caught.

The five inverse tokens invert relative to every dark theme: because luxe's main surfaces are light,
its drawer, Stripe-host and email panels go dark, using the warm near-black values the UI-SPEC lists.
Do not copy the dark themes' light inverse values.

All four radius tokens take the flat value the UI-SPEC gives, expressing the direction's square-corner
intent through the contract's four steps rather than adding a shape token.

The sans token keeps the Geist stack. The display token is the serif stack whose first entry is the
CSS variable the root layout exposes for Cormorant Garamond, followed by the named family and the
generic serif fallbacks, exactly as the UI-SPEC writes it. The file itself loads no font.

Write no at-rule, no second selector, no external resource reference, and no property outside the 23.
Every colour value is 6-digit lowercase hex.
  </action>
  <acceptance_criteria>
    - `test "$(/usr/bin/grep -c '^\s*--store-' themes/luxe.css)" -eq 23` and `test "$(/usr/bin/grep -c '{' themes/luxe.css)" -eq 1`.
    - `test "$(/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/luxe.css)" -eq 17`.
    - The display-font declaration references the layout's Cormorant Garamond CSS variable, and the file declares no font-loading at-rule (the generator's rule 3 proves this).
    - The four radius declarations all carry the same flat value the UI-SPEC gives.
    - `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts` exits 0 with the loop covering all three theme files.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -c '^\s*--store-' themes/luxe.css)" -eq 23 && test "$(/usr/bin/grep -c '{' themes/luxe.css)" -eq 1 && test "$(/usr/bin/grep -cE '^\s*--store-[a-z-]+: #[0-9a-f]{6};$' themes/luxe.css)" -eq 17 && /usr/bin/grep -q 'font-cormorant-garamond' themes/luxe.css && mise exec -- node scripts/build-themes.mjs && mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts</automated>
    <fails_when>a count assertion is not equal; or the font-variable grep finds nothing, meaning the display token does not reference the face the layout loads; or the generator exits non-zero with a build-themes message naming luxe; or the parity test reports a mismatch between the file and its manifest entry.</fails_when>
  </verify>
  <done>A light preset exists as a pure 23-token data file with inverted inverse surfaces, a contrast-corrected focus ring, and a display face it references but does not load.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate to three entries and prove a preset switch takes effect with no redeploy</name>
  <precondition>The local D1 database is reachable through the project's dev pipeline (npm run predev completes), and lib/themes/active-theme.ts from plan 06-02 exists.</precondition>
  <read_first>
    - `scripts/build-themes.mjs` and the plan 06-01 SUMMARY - the generator's CLI, its check mode, and the committed-output rule.
    - `lib/themes/active-theme.ts` and the plan 06-02 SUMMARY - the setting key and category to write, and the resolution order this task exercises.
    - `scripts/db-local-ensure.mjs` - how the local D1 database is prepared, so the setting row can be written against the same database the dev server reads.
    - `lib/db/schema/settings.ts` - the admin settings table columns (key, value, category, data type), so the inserted row matches what the settings helper expects to read back. No migration is needed: this is a new row, not a new column.
    - `.planning/todos/pending/theme-metadata-industry-synopsis-admin.md` - the existing todo this phase resolves, and the file format for the new backlog note.
    - `.planning/phases/06-theme-file-mechanism-presets/06-UI-SPEC.md` lines 191 and 213 - the exact list of direction-document properties D-03 drops, to record in the backlog note.
  </read_first>
  <files>themes/index.generated.css, lib/themes/manifest.generated.ts, .planning/todos/pending/theme-contract-dropped-properties.md</files>
  <action>
Regenerate both generated files so they carry three entries, and commit them. Confirm check mode
passes against the committed output and that a second generator run leaves the working tree clean.

Prove the switch end to end against a running dev server: for each of the three theme names in turn,
write the appearance theme setting row into the local D1 database using the key and category
constants from plan 06-02, request the storefront home page, and confirm the served HTML carries
that name in the html element's theme attribute. Then confirm the served stylesheet contains a
declaration block for that name. Restore the setting to the manifest default when done. This is the
observable proof that a shipped preset is selectable without a code change and without a redeploy,
and it is also the practical check that nothing cached the resolution.

Write the backlog note recording the direction document's properties that D-03 drops — shadow,
border width, image aspect, the second accent, the mono font, and letter spacing — including where
two of them were folded into existing tokens rather than lost (the second accent's cyan into
midnight's info token, the sunken surface into luxe's border token), and noting that extending the
contract would touch the validator, every theme file, and the Phase 5 sweep. Follow the format of the
existing pending todo named in read_first.

Also note in the SUMMARY that the existing theme-metadata todo is resolved by this phase, so whoever
runs the next backlog review can close it.
  </action>
  <acceptance_criteria>
    - The generated manifest has exactly three entries; `/usr/bin/grep -c 'name:' lib/themes/manifest.generated.ts` is 3.
    - The generated barrel has exactly three import lines, one per theme file, in filename order.
    - `mise exec -- node scripts/build-themes.mjs --check` exits 0 and leaves `git status --porcelain` empty for both generated paths.
    - For each of the three theme names, the served home page HTML contains that name inside the html element's theme attribute while that name is the stored setting.
    - `mise exec -- npm run scan:tokens` reports 0 violations and the same 2 manual-review rows.
    - `mise exec -- npm run lint`, `mise exec -- npm run typecheck` and `mise exec -- npm test` all exit 0.
    - The backlog note exists and names all six dropped properties plus the two fold-ins.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- node scripts/build-themes.mjs && test "$(/usr/bin/grep -c 'name:' lib/themes/manifest.generated.ts)" -eq 3 && test "$(/usr/bin/grep -c '@import' themes/index.generated.css)" -eq 3 && mise exec -- node scripts/build-themes.mjs --check && test -z "$(git status --porcelain themes/index.generated.css lib/themes/manifest.generated.ts)" && test -f .planning/todos/pending/theme-contract-dropped-properties.md && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm run scan:tokens && mise exec -- npm test</automated>
    <fails_when>non-zero exit; or an entry or import count is not 3, meaning a preset did not reach the manifest or the barrel; or git status is non-empty after check mode, meaning the committed output is stale or check mode wrote a file; or the backlog note is absent.</fails_when>
    <human-check>With the dev server running, switch the stored appearance theme through all three names and load the home page each time. Confirm the page visibly changes look (black/orange, indigo/violet, ivory/gold), that the change needs only a page reload rather than a restart, and that the luxe page renders its headings in the serif face while the two dark themes do not fetch that font file at all.</human-check>
  </verify>
  <done>Three presets are in the committed manifest and barrel, each one is reachable by writing a single D1 setting row, and the properties the contract deliberately does not carry are recorded rather than forgotten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme source file -> global stylesheet | Every declaration in a theme file is emitted into the storefront's global CSS for every visitor. |
| theme header comment -> admin UI | The label, industry and synopsis strings are parsed out of a comment and later rendered on the admin Appearance page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering | `themes/luxe.css`, `themes/midnight.css` | medium | mitigate | Both files go through the plan 06-01 validator on `predev`, `build:worker` and in CI: one rule, one selector, no at-rule, no unknown property, hex-only colours. Task 1 and Task 2 each run the generator as their gate, so a file that would smuggle an external resource reference cannot be committed green. |
| T-06-10 | Information Disclosure | the header metadata strings entering the generated manifest and shipping to the browser | low | mitigate | The strings are authored in this repo and copied from the UI-SPEC; they carry product copy only. Plan 06-04 renders them as React text children, which escapes them, so even an edited header cannot inject markup into the admin page. |
| T-06-11 | Tampering | the local D1 setting row written by Task 3's switch proof | low | mitigate | The write targets the local development database prepared by the project's own dev script, uses the same key and category the admin UI will use, and is restored to the manifest default at the end of the task. No production database is touched. |
| T-06-12 | Spoofing | a preset name colliding with or shadowing an existing one | low | mitigate | The validator requires the selector name to equal the filename stem and restricts stems to lowercase alphanumerics and hyphens, and the filesystem already forbids two files with the same name, so two manifest entries cannot share a name. |
| T-06-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan; it adds only CSS data files and regenerated build output. `06-RESEARCH.md` section "Package Legitimacy Audit" records zero packages added this phase. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| Theme file | `themes/midnight.css` (manifest name `midnight`) |
| Theme file | `themes/luxe.css` (manifest name `luxe`) |
| Manifest entries | `midnight`, `luxe` — joining `volt-dark` in `THEME_MANIFEST` |
| Backlog note | `.planning/todos/pending/theme-contract-dropped-properties.md` |
| Regenerated output | `themes/index.generated.css` (3 imports), `lib/themes/manifest.generated.ts` (3 entries) |
</artifacts_this_phase_produces>

<verification>
Commands the SUMMARY must record:

1. The per-file shape assertions for each new theme (token count 23, block count 1, hex count 17).
2. `mise exec -- node scripts/build-themes.mjs` and `--check`, with the resulting entry and import counts.
3. `mise exec -- npx vitest run tests/unit/lib/themes/token-contract.test.ts` showing the loop covering three files.
4. `mise exec -- npm run scan:tokens` reporting 0 violations and 2 manual-review rows.
5. `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm test`.
6. The three-way switch proof from Task 3, recording the served theme-attribute value for each name.
7. `mise exec -- npm run screenshot:routes -- --label phase-06-03-midnight` and
   `mise exec -- npm run screenshot:routes -- --label phase-06-03-luxe`, captured while each preset is
   the stored theme. These two labels are the input plan 06-05's QA pass reads.

The SUMMARY must also list, per preset, every token whose value is a deliberate deviation from the
direction document, so the QA pass in plan 06-05 knows which values were chosen rather than derived.
</verification>

<success_criteria>
- Three presets ship, at least one light, each a pure 23-token data file passing the same validator with no exceptions.
- No token was added, renamed, or dropped from the frozen contract to accommodate a preset.
- Selecting each preset changes the storefront's look with a single D1 row and a page reload.
- The properties the direction document wanted and the contract does not carry are recorded in the backlog, not silently lost.
</success_criteria>

<output>
Create `.planning/phases/06-theme-file-mechanism-presets/06-03-SUMMARY.md` when done.
</output>
