---
phase: 07-layout-switches
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - tests/unit/app/layout-switch-contract.test.ts
  - .planning/phases/07-layout-switches/07-SCREENSHOTS.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One repo-wide contract test proves the LAYOUT-04 claim for all three switches at once: every enum member has a named component, every lookup map's keys equal its enum in order with no duplicates, and every member renders non-empty markup. [LAYOUT-04 edge: empty, adjacency, ordering]"
    - "No page or display component in the three switch paths compares a resolved value against a member name literal, and no component in the phase declares a prop named for the generic concept rather than for its own switch. [LAYOUT-04]"
    - "Every one of the eight variants has at least one render test, counted and named in the contract test's own coverage assertion so a future ninth variant without a test fails the suite (D-13). [LAYOUT-04]"
    - "The three default variants are pixel-identical to the pre-extraction baseline: the home, category and product route hashes captured under the default combination match the plan 07-01 baseline hash for hash (D-14)."
    - "Every one of the eight variants is captured on its own route under both the dark default preset and the light preset, and the manifest carries a coverage table naming, for each variant and theme, the label and the cell that evidences it (D-14). [LAYOUT-01, LAYOUT-02, LAYOUT-03]"
    - "Non-default variants are recorded as new intentional captures, not as diffs against anything — only the three defaults carry a byte-identity obligation (D-14)."
    - "The four phase requirements are marked complete in the requirements register and the roadmap's phase entry reflects the shipped plan list."
    - "The full gate suite is green at phase close: lint, typecheck, the token scan at zero violations with the two pre-existing manual-review rows, the whole unit suite, and a production build."
    - "The stored values for all three switches are left at their defaults when the phase closes, so the next phase starts from the pre-phase storefront."
  artifacts:
    - path: "tests/unit/app/layout-switch-contract.test.ts"
      provides: "The single repo-wide LAYOUT-04 proof across all three switches"
    - path: ".planning/phases/07-layout-switches/07-SCREENSHOTS.md"
      provides: "Every variant captured under both presets, the defaults parity result, and the variant-to-cell coverage table"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LAYOUT-01 through LAYOUT-04 marked complete"
  key_links:
    - from: "tests/unit/app/layout-switch-contract.test.ts"
      to: "lib/layout/variants.ts"
      via: "the three enums are the test's own source of truth for what must exist"
      pattern: "CATEGORY_LAYOUTS"
    - from: ".planning/phases/07-layout-switches/07-SCREENSHOTS.md"
      to: ".screenshots/phase-07-grid3-minimal-left-volt-dark"
      via: "the defaults capture whose hashes are compared against the pre-extraction baseline"
      pattern: "phase-07-pre-extraction-volt-dark"
  prohibitions:
    - statement: "The three switches must NOT introduce a generic layout or variant prop on any page component."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition lifted from LAYOUT-04; this plan is where it becomes a single mechanical assertion across every file the phase touched."
    - statement: "Layout resolution must NOT be cached in a module-level or isolate-level variable."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition carried from Phase 6; re-checked here at phase close across the resolver and all three call sites."
    - statement: "The hero image URL must NOT be read from settings or any other user-supplied input."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition; re-checked here as part of the repo-wide contract test rather than only inside plan 07-02's own suite."
    - statement: "This phase must NOT change the 23-token contract or the theme mechanism."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less phase prohibition carried from Phase 5 D-01 and Phase 6; the closing gate suite is where a token-contract drift would surface."
    - statement: "ThemePresetGrid.tsx must NOT be modified by this phase."
      status: unresolved
      verification: judgment
      reason: "Descriptor-less prohibition lifted from D-12; re-checked at phase close against the phase's whole diff, not just plan 07-04's."
---

<objective>
Close the phase on evidence rather than assertion: one repo-wide test that proves the LAYOUT-04
claim for all three switches, a screenshot sweep that shows every variant under both presets, a hash
comparison proving the three extractions changed nothing, and the requirement register updated.

Purpose: LAYOUT-04 in full, plus the D-14 visual evidence and the phase-close bookkeeping for
LAYOUT-01 through LAYOUT-03.

Output: `tests/unit/app/layout-switch-contract.test.ts`, a completed `07-SCREENSHOTS.md` with a
variant-to-cell coverage table and a defaults parity result, and the requirement and roadmap updates.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-layout-switches/07-CONTEXT.md
@.planning/phases/07-layout-switches/07-UI-SPEC.md
@.planning/phases/07-layout-switches/07-01-SUMMARY.md
@.planning/phases/07-layout-switches/07-02-SUMMARY.md
@.planning/phases/07-layout-switches/07-03-SUMMARY.md
@.planning/phases/07-layout-switches/07-04-SUMMARY.md
</context>

<interfaces>
Consumed from plans 07-01 through 07-04, all unchanged: the three enums and the defaults object, the
resolver, the eight variant components, the three lookup maps at their call sites, and the three
per-switch data attributes on the variant roots.

This plan creates no production interface. It adds one test file and updates planning documents.
</interfaces>

<executor_notes>
- Node 24 comes from `mise`: prefix every project command with `mise exec --`.
- The shell has `noclobber`. Write commit messages to a `mktemp` file and use the file form of
  `git commit`; redirect with `>|` when a target file may already exist.
- The screenshot script exits non-zero without the allow-missing flag, because the order-status route
  has no order id in a local fixture. Pass it on every capture, the way every prior phase did.
- Every task is committed atomically before the next one starts.
</executor_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One repo-wide contract test for the LAYOUT-04 claim</name>
  <read_first>
    - `lib/layout/variants.ts` — the three enums and the defaults, which are this test's source of truth.
    - `app/category/[slug]/page.tsx`, `app/page.tsx` and `app/product/[slug]/ProductDisplay.tsx` as they stand after plans 07-01 through 07-03 — the three lookup map declarations whose key sets the test reads out of the source.
    - `tests/unit/app/admin-appearance-source.test.ts` lines 284-310 — the source-level checks idiom for assertions a render cannot make.
    - `tests/unit/components/layout/category/category-variants.test.ts`, `.../home/home-hero-variants.test.ts` and `.../product/product-gallery-variants.test.ts` — the existing per-switch suites, so this test complements them rather than duplicating them.
  </read_first>
  <files>tests/unit/app/layout-switch-contract.test.ts</files>
  <behavior>
    - The test builds its own member-to-component table for all eight variants by importing each component directly, and asserts that table's key set equals the three enums exactly — a component added without an enum member, or an enum member added without a component, fails here.
    - Every one of the eight components renders non-empty markup with a minimal fixture, and every rendered root carries its own switch's data attribute set to its own member name.
    - For each of the three source files holding a lookup map, the declared map literal's key set equals its enum's members, in the enum's order, with no duplicate key.
    - No enum array contains a duplicate entry, and each default is a member of its own enum.
    - None of the three source files holding a map, and neither of the two display components, contains a comparison of a resolved value against a member name literal.
    - No component file created by this phase declares a prop named for the generic concept rather than for its own switch.
    - No file in the layout component tree, and not the resolver, contains a cross-request memoisation helper or a module-scope mutable declaration.
    - Neither hero variant that takes an image reads that image from a settings module or a store-config field.
    - Each of the eight variants is covered by at least one case in the three per-switch suites, asserted by reading those files and matching each member name.
  </behavior>
  <action>
Write one test file that makes the phase-level claim in one place, rather than leaving LAYOUT-04
implied by four separate suites.

Build the member-to-component table by importing all eight components directly. That table is
deliberately a second, independent statement of the same mapping the three production maps make: if
they ever disagree, one of them is wrong and this test says so. Assert its key set against the enums,
then render each entry with a minimal fixture and assert non-empty markup and the correct data
attribute value.

Read the three source files holding the production maps and extract each map literal's key set.
Compare it to its enum, including order. The compiler already refuses a map that is missing a member,
because each map is declared over the enum's record type — this assertion catches the other
direction, a stray key or a reordering that would make the source stop reading as a mirror of the
enum.

Add the source-level assertions from the remaining behavior rows: no name-literal comparison in the
files that hold a map or receive a resolved value, no generically-named prop on any component this
phase created, no cross-request memoisation and no module-scope mutable declaration anywhere in the
layout module or the layout component tree, and no settings or store-config read behind either
image-bearing hero.

Finish with the coverage assertion: read the three per-switch suite files and assert every enum
member's name appears in at least one case name or assertion in them. That is what makes D-13's
one-render-test-per-variant rule survive a ninth variant being added later.

Keep the fixtures minimal and local to this file. This test is a contract, not a second render suite
— the detailed per-variant behaviour already lives in the three suites the earlier plans wrote.
  </action>
  <acceptance_criteria>
    - `mise exec -- npx vitest run tests/unit/app/layout-switch-contract.test.ts` exits 0 with one passing case per behavior row.
    - The member-to-component table is written out member by member in the test, not derived from the production maps, so the two are genuinely independent.
    - No case asserts a hardcoded count of variants; each counts against its own enum's length.
    - `mise exec -- npm run typecheck` and `mise exec -- npm run lint` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/app/layout-switch-contract.test.ts && mise exec -- npm run typecheck && mise exec -- npm run lint</automated>
    <fails_when>vitest exits non-zero; or the summary reads "0 passed"; or a case fails because a map key set drifted from its enum, because a component branches on a member name literal, because a generically-named prop appeared, or because one of the eight variants has no case in any per-switch suite.</fails_when>
  </verify>
  <done>LAYOUT-04 is a single failing-if-broken assertion covering all three switches, all eight components, all three maps and all three call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Capture every variant under both presets and prove the three defaults changed nothing</name>
  <precondition>A local dev server can serve the storefront against a local D1 with a category that has active products and at least one product page, both resolvable from /sitemap.xml, and the admin settings endpoint accepts the documented development bypass header. The light preset must be a shipped theme in the current manifest.</precondition>
  <read_first>
    - `.planning/phases/07-layout-switches/07-SCREENSHOTS.md` — the pre-extraction baseline section written by plan 07-01, specifically its home, category and product rows and their hash column.
    - `scripts/screenshot-routes.mjs` lines 240-264 — the missing-cell exit behaviour and the exact per-label section format appended to the manifest.
    - `.planning/phases/06-theme-file-mechanism-presets/06-SCREENSHOTS.md` lines 25-62 and 283-375 — the per-preset capture write-up and the phase-close evidence roll-up shape to follow.
    - `.planning/phases/07-layout-switches/07-CONTEXT.md` — D-14, for the obligation split between the three defaults and the five new variants.
  </read_first>
  <files>.planning/phases/07-layout-switches/07-SCREENSHOTS.md</files>
  <action>
Capture the whole variant matrix in six runs rather than sixteen, using the fact that the three
switches are independent: each run stores one value per switch and the harness captures all three
routes, so three well-chosen combinations cover all eight variants under one preset.

Per preset, the three combinations are: all three defaults; the two-column category with the split
hero and the top gallery; and the list category with the full-bleed hero and the default gallery.
Between runs, set the three stored values through the admin settings endpoint with the documented
development bypass header, exactly as the earlier plans' probes did. Label each run for the values
and the preset it captured, so a label reads as its own description, and pass this phase's manifest
path and the allow-missing flag on every run.

Run the first preset as the dark default and the second as the light preset, switching the theme
through the same settings endpoint between the two sets. Confirm the switch took effect before
capturing — a mislabelled preset set is worse than a missing one.

Then do the parity comparison. Take the home, category and product rows from the pre-extraction
baseline section and from the all-defaults dark-preset section, keep the route, viewport, state and
hash columns, drop the path column since it necessarily differs by label, and compare the two lists.
They must be identical: the three extractions were supposed to change nothing, and this is the
measurement that says whether they did. If any row differs, do not record it as a tolerated
difference — open the two images, find the cause, and either fix the extraction or record a specific,
named reason with evidence, the way Phase 5 registered its snap decisions. A hash difference is a
question, not a verdict.

Write the manifest up. Above the generated tables, add a coverage table with one row per variant per
preset naming the label and the route whose cells evidence it — sixteen rows, which is the D-14
minimum expressed as coverage rather than as a run count. Add the parity result with the compared row
count and the outcome. Add a short note that the five non-default variants are new intentional
captures with nothing to diff against, so a reader does not go looking for a baseline that never
existed.

Restore all three stored values to their defaults and the theme to the dark default before finishing,
and stop the server.
  </action>
  <acceptance_criteria>
    - The manifest contains six new label sections, three per preset, each named for the values and the preset it captured.
    - Every section's home, category and product rows have real hashes; no MISSING cell among those three routes in any section.
    - The coverage table has one row per variant per preset, sixteen rows total, each naming a label and a route.
    - The parity comparison of the home, category and product rows between the pre-extraction baseline and the all-defaults dark-preset capture reports no differing row.
    - The manifest records that the five non-default variants are new captures with no baseline.
    - All three stored values and the stored theme are back at their defaults when the task finishes.
  </acceptance_criteria>
  <verify>
    <automated>M=.planning/phases/07-layout-switches/07-SCREENSHOTS.md && test "$(/usr/bin/grep -c '^## Label: `phase-07-' "$M")" -eq 7 && PRE="$(mktemp)" && POST="$(mktemp)" && /usr/bin/awk '/^## Label: `phase-07-pre-extraction-volt-dark`/{f=1;next} /^## Label: /{f=0} f' "$M" | /usr/bin/grep -E '^\| (home|category|product) \|' | /usr/bin/awk -F'|' '{print $2"|"$3"|"$4"|"$6}' >| "$PRE" && /usr/bin/awk '/^## Label: `phase-07-grid3-minimal-left-volt-dark`/{f=1;next} /^## Label: /{f=0} f' "$M" | /usr/bin/grep -E '^\| (home|category|product) \|' | /usr/bin/awk -F'|' '{print $2"|"$3"|"$4"|"$6}' >| "$POST" && test "$(/usr/bin/wc -l < "$PRE")" -eq 12 && test "$(/usr/bin/wc -l < "$POST")" -eq 12 && /usr/bin/diff "$PRE" "$POST" && ! /usr/bin/grep -E '^\| (home|category|product) \|' "$M" | /usr/bin/grep -q 'MISSING'</automated>
    <fails_when>the label-section count is not 7, meaning a capture run was skipped or an extra one was left in; or either extracted list is not 12 rows, meaning a route captured as MISSING and the comparison would be vacuous; or the comparison prints any differing line, meaning one of the three extractions changed rendered output and is not the no-op D-14 requires; or any of the three storefront routes recorded a MISSING cell in any section.</fails_when>
  </verify>
  <done>Every one of the eight variants has captured evidence under both presets, and the claim that the three extractions changed nothing is a measured hash comparison rather than a statement.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — requirement register, roadmap, and the green gate suite</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 25-35 (the four layout requirement lines) and lines 75-90 (the status table rows).
    - `.planning/ROADMAP.md` — the Phase 7 entry, its success criteria and its plan list.
    - `.planning/phases/07-layout-switches/07-01-SUMMARY.md` through `07-04-SUMMARY.md` — every recorded command, probe result and flagged decision that the roll-up cites.
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Mark the four layout requirements complete: tick each of the four requirement lines and set each of
the four status-table rows to complete. Do not touch any other requirement, and do not restate the
requirement text.

Update the roadmap's Phase 7 entry so its plan list shows all five plans as delivered. Change only
that phase's entry — edit the section in place rather than rewriting the file, because every other
phase entry lives in the same document.

Then run the full gate suite one last time from a clean tree and record every result. If anything is
red, stop and fix it rather than recording a caveat: this is the last gate before the phase is called
done.

Write the phase-close roll-up into the summary rather than into a new document: one short section per
requirement naming the evidence — for the three storefront requirements, the live probe from its own
plan plus the screenshot coverage rows from Task 2; for the fourth, the contract test from Task 1 and
the eight per-variant cases. Carry forward, in the summary's own carried-forward section, the two
items this phase deliberately did not resolve: the settings endpoint's empty-category behaviour on a
genuinely fresh install, and the product display's local media helper that a later phase may unify
with the shared product-image resolver. Also carry forward the admin browser walkthrough from plan
07-04 if it has not been run by a human yet.
  </action>
  <acceptance_criteria>
    - All four layout requirement lines are ticked and all four status-table rows read complete.
    - No other requirement line or status row changed.
    - The roadmap's Phase 7 entry lists five delivered plans, and no other phase entry changed.
    - `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm test` and `mise exec -- npm run build` all exit 0.
    - `mise exec -- npm run scan:tokens` reports 0 violations and exactly 2 manual-review rows, and those rows name the same two files as before the phase.
    - The summary contains a per-requirement evidence roll-up and a carried-forward section naming the three outstanding items.
  </acceptance_criteria>
  <verify>
    <automated>test "$(/usr/bin/grep -cE '^- \[x\] \*\*LAYOUT-0[1-4]\*\*' .planning/REQUIREMENTS.md)" -eq 4 && test "$(/usr/bin/grep -cE '^\| LAYOUT-0[1-4] \| Phase 7 \| Complete' .planning/REQUIREMENTS.md)" -eq 4 && test "$(git diff --name-only | /usr/bin/wc -l | /usr/bin/tr -d ' ')" -le 2 && SCAN="$(mktemp)" && mise exec -- npm run scan:tokens >| "$SCAN" 2>&1 && test "$(/usr/bin/grep -c '^MANUAL-REVIEW' "$SCAN")" -eq 2 && /usr/bin/grep -q 'image-placeholders.ts' "$SCAN" && /usr/bin/grep -q 'Promotion.ts' "$SCAN" && /usr/bin/grep -q '0 violations' "$SCAN" && mise exec -- npm run lint && mise exec -- npm run typecheck && mise exec -- npm test && mise exec -- npm run build</automated>
    <fails_when>either requirement count is under 4, meaning a requirement line or status row was missed; or the working tree shows more than the two planning files this task is allowed to touch, meaning production code changed during a bookkeeping task; or the scan prints a violation line, a manual-review count other than 2, or a manual-review pair that is not the two pre-existing files; or any gate command exits non-zero.</fails_when>
  </verify>
  <done>The four requirements are marked complete against named evidence, the roadmap reflects what shipped, and the whole suite is green with the storefront back on its default layouts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot harness -> captured images | The harness drives a real browser against real routes and writes images to disk; pointed at a non-local host it would capture real customer data on the account, checkout and order-status routes. |
| planning documents -> reviewers | The manifest and the summary are the evidence a reviewer trusts instead of re-running the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Information Disclosure | the screenshot harness's target host | high | mitigate | Every capture in this plan uses the default local base URL and never passes the remote-allow flag, so the harness's own guard refuses any non-local host. The account, checkout and order-status cells therefore only ever contain local fixture data. |
| T-07-18 | Repudiation | the defaults-parity claim | medium | mitigate | The parity result is a mechanical comparison of recorded hashes between two labelled sections of the committed manifest, reproducible by anyone from the file alone, rather than an executor's assertion that the pages looked the same. |
| T-07-19 | Tampering | stored layout and theme values left behind after the sweep | low | mitigate | The task restores all three layout values and the theme to their defaults before finishing and the closing gate re-runs against that state, so the next phase does not start from a half-set storefront. |
| T-07-SC | Tampering | npm / pip / cargo installs | low | accept | No package-manager install task exists in this plan or this phase. `07-RESEARCH.md` records zero packages added; there is nothing to gate. |
</threat_model>

<artifacts_this_phase_produces>
| Kind | Name / path |
|---|---|
| Test | `tests/unit/app/layout-switch-contract.test.ts` |
| Evidence | six new labelled capture sections plus a variant-to-preset coverage table in `07-SCREENSHOTS.md` |
| Evidence | the defaults parity comparison result recorded in `07-SCREENSHOTS.md` |
| Register update | LAYOUT-01 through LAYOUT-04 marked complete in `.planning/REQUIREMENTS.md` |
| Roadmap update | Phase 7's delivered plan list in `.planning/ROADMAP.md` |
</artifacts_this_phase_produces>

<verification>
Commands the SUMMARY must record:

1. `mise exec -- npx vitest run tests/unit/app/layout-switch-contract.test.ts` with the per-case pass count.
2. The six capture commands, each of the form `mise exec -- npm run screenshot:routes -- --label phase-07-<values>-<preset> --manifest .planning/phases/07-layout-switches/07-SCREENSHOTS.md --allow-missing`, with the settings posts that preceded each.
3. The parity comparison — the compared row count and the outcome.
4. `mise exec -- npm run lint`, `mise exec -- npm run typecheck`, `mise exec -- npm test`, `mise exec -- npm run build`.
5. `mise exec -- npm run scan:tokens` — the violation count and the two manual-review file names.

The SUMMARY must also carry the per-requirement evidence roll-up and the carried-forward items.
</verification>

<success_criteria>
- LAYOUT-04 is proven once, repo-wide, for all three switches and all eight components.
- Every variant has captured evidence under both the dark default preset and the light preset.
- The three extractions are proven no-ops by hash comparison against the pre-extraction baseline.
- The requirement register and the roadmap reflect what shipped, and the whole gate suite is green.
- The storefront is left on its default layouts and its default theme.
</success_criteria>

<output>
Create `.planning/phases/07-layout-switches/07-05-SUMMARY.md` when done.
</output>
