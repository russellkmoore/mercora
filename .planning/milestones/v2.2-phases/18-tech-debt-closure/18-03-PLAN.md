---
phase: 18-tech-debt-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/tax/route.ts
  - tests/unit/api/tax-route.test.ts
  - tests/unit/app/api/public-route-hardening.test.ts
  - lib/services/order-effects.ts
  - tests/unit/lib/services/order-effects-gift-card-env.test.ts
files_deleted:
  - app/api/tax/route.ts
  - tests/unit/api/tax-route.test.ts
autonomous: true
requirements: [DEBT-01, DEBT-02]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No route in the repository hardcodes a tax rate or a tax code any more; the only tax path is the authoritative pricing service (D-01)."
    - "Deleting the route leaves no dangling import or reference anywhere — the build and the whole unit suite stay green (D-01)."
    - "The gift-card effect hands the fulfilment service either the full worker environment or nothing at all, never a database-only stand-in (D-02)."
    - "A caller that supplies no environment still reaches the fulfilment service's own context lookup and fails loudly on a genuinely missing key ring, rather than being mis-attributed to a missing key ring when the environment itself was wrong (D-02)."
  artifacts:
    - tests/unit/lib/services/order-effects-gift-card-env.test.ts
    - tests/unit/app/api/public-route-hardening.test.ts
    - lib/services/order-effects.ts
  key_links:
    - "lib/services/order-effects.ts is the single call site that builds the gift-card fulfilment environment; no other caller constructs a partial one (verified: giftCardEnvironment appears in exactly three source lines repo-wide)."
    - "lib/services/gift-card-fulfillment.ts reads the delivery and HMAC key rings off that environment before any email step, so a partial environment breaks issuance itself, not just delivery."
---

<objective>
Close two unrelated, self-contained cleanups that share a wave because they share no files.

DEBT-01: delete `app/api/tax/route.ts`. It has zero non-test callers, hardcodes its own
fallback tax rate and a single Stripe tax code with no gift-card exemption, and actively
diverges from the authoritative `lib/services/checkout-pricing.ts`. It is an orphan whose only
remaining effect would be to be wrong if anyone ever called it (D-01).

DEBT-02: remove the database-only environment fallback in `lib/services/order-effects.ts`. It
constructs an object the fulfilment service's own doc comment calls "strictly worse than
handing over nothing" — it switches off the sender's context lookup while carrying none of the
key material issuance needs (D-02).

Output: one route and one test file gone, one expression simplified, one regression test added.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tech-debt-closure/18-CONTEXT.md
@.planning/phases/18-tech-debt-closure/18-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Delete the orphaned tax route and its two test surfaces</name>
  <files>app/api/tax/route.ts (delete), tests/unit/api/tax-route.test.ts (delete), tests/unit/app/api/public-route-hardening.test.ts</files>
  <read_first>
    - `app/api/tax/route.ts` — read it once before deleting so the SUMMARY can state what was lost
    - `tests/unit/api/tax-route.test.ts`
    - `tests/unit/app/api/public-route-hardening.test.ts` lines 1-95 — the mock block, the import list (line 33), the parameterised route table (line 62), and the oversized-cart case (lines 86-93)
    - `lib/services/checkout-pricing.ts` around the nontaxable code constant and the fallback-rate path — confirm only, change nothing
  </read_first>
  <precondition>`app/api/tax/route.ts` has no importer outside `tests/`. Re-confirm with a repository-wide search for the route path across `app/`, `components/`, `lib/`, `workers/`, `scripts/` and `docs/` before deleting; halt if any non-test hit appears.</precondition>
  <action>
Delete `app/api/tax/route.ts` and `tests/unit/api/tax-route.test.ts` outright. Use
`git rm` so the deletions land in the commit; do not leave empty directories behind — remove
`app/api/tax/` and, if `tests/unit/api/` becomes empty, remove it too.

Then repair `tests/unit/app/api/public-route-hardening.test.ts`, which is the only remaining
file that knows the route existed. Three edits:

  1. remove the import of the deleted route's POST handler (line 33);
  2. remove that route's row from the parameterised table the rate-limit case iterates
     (line 62), leaving the other three rows exactly as they are;
  3. delete the whole oversized-cart case that exercises the deleted handler (lines 86-93).

Do **not** replace any of these with an assertion that the path now returns a not-found
response. The route file is gone; there is no handler to import and nothing to assert against.
A test that tried would either fail to resolve its import or silently assert on Next's own
fallback behaviour, which this suite does not own.

After the edits, check whether the `calculateTax` mock declared at line 5 and wired at line 17
still has a consumer in the file. If the payment-intent route's own mock of `@/lib/stripe`
still needs it, leave it; if nothing references it any more, remove the declaration and its
wiring too rather than leaving a dead mock.

The authoritative tax path in `lib/services/checkout-pricing.ts` is not touched by this task.
No new documentation file is written explaining the deletion — the git history and this
phase's SUMMARY are the record (D-01).
  </action>
  <verify>
    <automated>test ! -e app/api/tax/route.ts && test ! -e tests/unit/api/tax-route.test.ts && mise exec -- npx vitest run tests/unit/app/api/public-route-hardening.test.ts && mise exec -- npx tsc --noEmit</automated>
    <fails_when>Either file still exists on disk, the hardening suite fails to resolve an import of the removed handler, the three surviving parameterised rows were disturbed, or type-checking finds another reference to the deleted module.</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither deleted path exists, and both deletions are staged as deletions in git.
    - `tests/unit/app/api/public-route-hardening.test.ts` still covers exactly three public routes and passes.
    - No file in the repository outside `.planning/` still names the deleted route path.
    - `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The orphaned tax route and every reference to it are gone; the authoritative pricing service is the only tax path left.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The gift-card effect passes the whole worker environment or nothing</name>
  <files>lib/services/order-effects.ts, tests/unit/lib/services/order-effects-gift-card-env.test.ts</files>
  <read_first>
    - `lib/services/order-effects.ts` lines 55-72 (`OrderEffectRuntime`, especially the `giftCardEnvironment` field and its doc comment) and lines 296-303 (the gift-card effect branch)
    - `lib/services/gift-card-fulfillment.ts` lines 38-86 (`emailEnvironmentFrom` and its doc comment, which states the exact reason this fallback is harmful) and lines 687-716 (`fulfillPaidGiftCards`, especially the context fallback and the two key-ring parses that run before any email step)
    - `lib/services/order-finalization.ts` lines 78-84 and 188-194 — the live request-path callers, which pass neither field
    - `lib/observability/scheduled.ts` line 63 — the cron caller, which passes the full environment
    - `tests/unit/components/cart-line-source.test.ts` — the source-contract test style to copy for the new test
  </read_first>
  <behavior>
    - The gift-card effect branch passes through whatever full worker environment the runtime was given, and passes nothing when it was given none.
    - A source-contract test reads `lib/services/order-effects.ts` and fails if the file ever again builds an object whose only property is the D1 binding.
    - The existing order-effects integration suite, which drains a gift-card effect with only a database supplied, still passes — the effect's gift-card branch returns before touching the environment when the order carries no gift-card line.
    - The existing gift-card fulfilment integration suite, which always passes a complete environment, still passes unchanged.
  </behavior>
  <action>
In `lib/services/order-effects.ts`, simplify the `environment` property in the
`fulfillPaidGiftCards` call inside the gift-card effect branch so it is the runtime's
gift-card environment field and nothing else. Drop the entire nullish-coalescing tail that
builds a stand-in object from the runtime's database handle.

Leave the `giftCardEnvironment` field on `OrderEffectRuntime` as it is, and leave the
`database` field alone — it is used by every other effect branch. Change nothing else in the
file.

Why this is a landmine removal rather than a live bug fix, for the SUMMARY: the two live
request-path callers in `lib/services/order-finalization.ts` supply neither field, so the
expression already resolves to nothing today and `fulfillPaidGiftCards` already falls back to
its own Cloudflare context lookup, which is correct. The cron path always supplies the complete
environment. The harm is latent: any future caller that passed a database alone would hand the
fulfilment service an object rich enough to switch off its context lookup but too poor to
satisfy the two key-ring parses that run before any email is sent — turning a wrong-environment
mistake into a misleading missing-key-ring failure at issuance time.

Then add `tests/unit/lib/services/order-effects-gift-card-env.test.ts`, a source-contract test
in the style of `tests/unit/components/cart-line-source.test.ts`:

  - read `lib/services/order-effects.ts` with `readFileSync`;
  - assert the gift-card branch passes the runtime's gift-card environment field through;
  - assert the file contains no object literal whose sole property is the D1 binding name —
    write the forbidden fragment as a string constant inside the test file and assert the
    source does not contain it;
  - assert the field's doc comment still describes it as the full worker environment, so a
    future edit that narrows the contract has to touch this test.

Do not weaken or delete any case in `tests/integration/lib/services/order-effects.test.ts`;
run it as-is to prove the change is behaviour-preserving.
  </action>
  <verify>
    <automated>mise exec -- npx vitest run tests/unit/lib/services/order-effects-gift-card-env.test.ts && mise exec -- npx vitest run --config vitest.workers.config.mts tests/integration/lib/services/order-effects.test.ts tests/integration/lib/services/gift-card-fulfillment.test.ts</automated>
    <fails_when>The source-contract test still finds a database-only object literal in the service, the order-effects drain suite regresses on the seven-effect case, or any gift-card issuance or delivery case in the fulfilment suite fails because the environment contract changed under it.</fails_when>
  </verify>
  <acceptance_criteria>
    - The gift-card effect branch's environment expression is a single property read with no conditional tail.
    - `tests/unit/lib/services/order-effects-gift-card-env.test.ts` exists and fails if the removed construction reappears.
    - Both integration suites pass with no edit to either.
    - `npm run lint` and `npx tsc --noEmit` are clean.
  </acceptance_criteria>
  <done>The only environment the gift-card effect can hand the fulfilment service is a complete one, and a test keeps it that way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → `/api/*` | Unauthenticated request handlers; one of them is being removed |
| Order-effect drain → gift-card fulfilment | Key material and bindings cross from the worker environment into issuance and delivery |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-10 | Denial of service | deleted route `app/api/tax` | high | mitigate | A deletion that leaves a dangling import turns a clean not-found into a module-resolution failure that can surface as a server error on unrelated routes. Task 1's precondition is a repository-wide search for non-test importers before deleting, and its verify runs both the hardening suite and a full type-check, which is what would catch a stale import. |
| T-18-11 | Information disclosure | removed hardcoded tax code and fallback rate | low | mitigate | The deleted route exposed a tax quote computed from its own hardcoded rate and tax code, divergent from the authoritative service and with no gift-card exemption. Removing it eliminates a public surface that could be used to read a second, wrong tax opinion about a cart. Nothing replaces it. |
| T-18-12 | Tampering | `lib/services/order-effects.ts` gift-card branch | high | mitigate | The removed construction produced an environment rich enough to suppress the sender's own context lookup but missing both key rings, so a wrong-environment caller would be reported as a key-ring configuration failure. After the change the only two outcomes are a complete environment or none, and `fulfillPaidGiftCards` fails loudly on a genuinely absent key ring through the existing encryption-configuration error. The source-contract test prevents reintroduction. |
| T-18-13 | Repudiation | `lib/services/gift-card-fulfillment.ts` | medium | accept | The clear-failure property for a missing key ring is already owned and proven by the existing gift-card fulfilment integration suite, which this plan runs unchanged as its regression proof. No new telemetry event is added for a condition that already throws. |
| T-18-14 | Elevation of privilege | `lib/services/order-effects.ts` | low | accept | The `database` field stays on the runtime for every other effect branch; this change only stops it being reshaped into a credential-bearing environment it is not. No new capability is granted to any caller. |

No package-manager install task exists in this plan, so no package-legitimacy gate applies.
</threat_model>

<verification>
- `mise exec -- npm test`
- `mise exec -- npm run test:workers`
- `npm run lint && npm run typecheck && npm run build`
</verification>

<success_criteria>
- The tax route, its dedicated test, and every reference to it are gone; nothing hardcodes a tax rate or tax code outside the authoritative pricing service.
- The gift-card effect's environment expression has no conditional tail, and a test fails if one returns.
- Both gift-card integration suites and the full unit suite pass.
</success_criteria>

<artifacts_this_phase_produces>
- `app/api/tax/route.ts` and `tests/unit/api/tax-route.test.ts` — deleted
- `tests/unit/app/api/public-route-hardening.test.ts` — three-route table, deleted-route case removed
- `lib/services/order-effects.ts` — gift-card environment expression simplified
- `tests/unit/lib/services/order-effects-gift-card-env.test.ts` — new source-contract regression test
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/18-tech-debt-closure/18-03-SUMMARY.md` when done. Record what the
deleted route contained (its hardcoded rate and tax code) so the git history is not the only
account, and state plainly that DEBT-02 removed a latent hazard rather than a live production
bug, with the caller evidence.
</output>
