---
phase: 17-saved-payment-methods
plan: 06
type: execute
wave: 4
depends_on: ["17-02", "17-03", "17-05"]
files_modified:
  - .planning/todos/pending/consolidate-stripe-customer-bindings.md
  - .planning/todos/completed/saved-payment-methods.md
  - .planning/phases/17-saved-payment-methods/17-06-SUMMARY.md
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full CI gate order passes locally in one green run before anything is pushed."
    - "Migration 0025 reaches production through the normal push-to-main deploy and the new tables and routes exist live."
    - "An anonymous request to either new account route is answered 401 in production, proving guests have no saved-method surface (D-07, D-08)."
    - "D-02's consolidation debt is written down as a Phase 18 todo rather than left in a decision log only."
    - "The four behaviours that need a real card are handed to Russell as an explicit click-through list, not silently assumed."
  artifacts:
    - .planning/todos/pending/consolidate-stripe-customer-bindings.md
    - .planning/phases/17-saved-payment-methods/17-06-SUMMARY.md
  key_links:
    - "Deploy happens by pushing main; Cloudflare Workers Builds runs deploy:ci, which is the only thing that applies remote migrations."
    - "Production verification is read-only: probes and at most one read query, no writes, no purchases."
---

<objective>
Close the phase: run the full CI-mirroring gate suite, push to main so Workers Builds deploys and
applies migration 0025, verify the live site read-only, record D-02's consolidation debt as a
Phase 18 todo, and hand Russell the short list of things only a real card can prove.

Purpose: PAY-01 through PAY-03 are only complete once the code is live and the parts a machine
cannot exercise are explicitly handed over rather than assumed.
Output: a green gate record, a deployed build, a live read-only verification record, one new
todo, and the phase close summary.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-saved-payment-methods/17-CONTEXT.md
@.planning/phases/17-saved-payment-methods/17-01-SUMMARY.md
@.planning/phases/17-saved-payment-methods/17-02-SUMMARY.md
@.planning/phases/17-saved-payment-methods/17-03-SUMMARY.md
@.planning/phases/17-saved-payment-methods/17-04-SUMMARY.md
@.planning/phases/17-saved-payment-methods/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The full CI-mirroring gate suite, in CI's order</name>
  <files>.planning/phases/17-saved-payment-methods/17-06-SUMMARY.md</files>
  <read_first>
    - `AGENTS.md` Gates section — the exact eleven commands CI runs and the order it runs them in
    - `.github/workflows/ci.yml` — confirm the order has not drifted from AGENTS.md since it was written
    - `.planning/STATE.md` Accumulated Context, the Phase 13 entry about the sandbox secret-read guard — the known trip-wire when a generated-types check has to be re-run without local environment files present
  </read_first>
  <action>
    Run the eleven CI commands in CI's order, each prefixed with `mise exec --`: the production
    dependency audit, the migration-safety check against `origin/main`, the theme-build freshness
    check, the token scan, lint, typecheck, the generated-Cloudflare-types check, the unit suite,
    the Workers suite, the observability-worker suite, and the production build. Record each
    command's result in the summary with the actual numbers it printed, not a paraphrase.

    Two known trip-wires, neither of which is a code failure. First, the generated-types check
    compares against a generation made with no local environment files present; if it reports a
    diff, confirm whether a local, untracked environment file is influencing generation before
    touching any source, and note that this repository's sandbox refuses any command containing
    that file's literal name, so the filename has to be assembled from fragments. Second,
    `check:migrations` needs `origin/main` fetched to diff against; fetch it first if the working
    copy is stale.

    If any gate is red, fix the cause in the owning plan's files and re-run the whole suite from
    the top. Do not push a partially green suite, and do not weaken a gate to make it pass.
  </action>
  <verify>
    <automated>mise exec -- npm run build:themes:check &amp;&amp; mise exec -- npm run scan:tokens &amp;&amp; mise exec -- npm run lint &amp;&amp; mise exec -- npm run typecheck &amp;&amp; mise exec -- npm run cf-typecheck</automated>
    <fails_when>non-zero exit, or the token scan reports any violation count other than 0, or the types check prints a diff</fails_when>
    <automated>mise exec -- npm test &amp;&amp; mise exec -- npm run test:workers &amp;&amp; mise exec -- npm run test:observability-worker</automated>
    <fails_when>non-zero exit, or any of the three vitest summaries reports a failed file, or the unit-suite file count is lower than the count recorded in 17-05-SUMMARY.md</fails_when>
    <automated>mise exec -- npm run check:migrations -- --base origin/main &amp;&amp; mise exec -- npm run build</automated>
    <fails_when>non-zero exit, or stdout contains "ABORT", or `0025_add_payment_customers.sql` is not printed on an "expand-only" line, or the build output lists no route entry for /account/payment-methods</fails_when>
  </verify>
  <done>All eleven CI gates pass locally in one run, with the numbers recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Push, let Workers Builds deploy, verify production read-only</name>
  <files>.planning/phases/17-saved-payment-methods/17-06-SUMMARY.md</files>
  <precondition>The working tree is on `main` with every phase-17 commit present and no unrelated staged changes; `git status --short` shows nothing outside this phase's files and `.planning/`.</precondition>
  <read_first>
    - `AGENTS.md` Rules — `npm run deploy` is never run from a developer machine, `deploy:ci` is run only by Cloudflare Workers Builds, and that is the only thing that applies remote migrations
    - `wrangler.jsonc` — the D1 `database_name` bound to `DB`, and the production hostname in the public vars block
    - `.planning/STATE.md` Phase 11 entry — the precedent that a push may carry every unpushed commit, and that a Worker version has to finish deploying before a follow-up wrangler call sees it
  </read_first>
  <action>
    Commit any remaining phase artefacts by explicit path — never `git add .` — then
    `git push origin main`. Pushing is the deploy: Cloudflare Workers Builds runs `deploy:ci`,
    which applies migration 0025 to the production database and then uploads the Worker. Do not
    run `npm run deploy`, `npm run deploy:ci`, or any wrangler deploy command from here.

    Wait for the new version to finish deploying, then verify production strictly read-only. No
    purchase, no card, no write of any kind.

    Probe one: an anonymous `GET https://voltique.russellkmoore.me/api/account/payment-methods`
    must answer 401. Probe two: an anonymous `DELETE` to the same path with a made-up
    `pm_` id must answer 401 — authentication is checked before same-origin, so an unauthenticated
    caller never even reaches the origin check. Probe three: an anonymous
    `GET https://voltique.russellkmoore.me/account/payment-methods` must answer a redirect status
    or render the sign-in page; it must not contain the Payment methods heading. Probe four: an
    anonymous `GET https://voltique.russellkmoore.me/checkout` must still answer 200, proving the
    checkout page is unharmed for a signed-out visitor.

    Confirm the migration landed with one read-only query against the production database using
    the database name from `wrangler.jsonc`: a remote `SELECT count(*) AS count FROM payment_customers`.
    A successful zero-row count proves the table exists and that nothing has written to it yet.
    If that query errors with a missing table, the deploy has not finished — wait and retry rather
    than applying anything by hand.

    Record every status code and the count in the summary.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' https://voltique.russellkmoore.me/api/account/payment-methods</automated>
    <fails_when>the printed status is anything other than 401 — a 200 would mean the route answers anonymous callers, a 404 would mean the deploy has not landed</fails_when>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' -X DELETE https://voltique.russellkmoore.me/api/account/payment-methods/pm_not_a_real_id</automated>
    <fails_when>the printed status is anything other than 401 — a 200 or 204 would mean an unauthenticated detach succeeded</fails_when>
    <automated>curl -s -o /dev/null -w '%{http_code}\n' https://voltique.russellkmoore.me/checkout</automated>
    <fails_when>the printed status is not 200 — checkout must be unchanged for a signed-out visitor</fails_when>
    <automated>mise exec -- npx wrangler d1 execute mercora-db --remote --command "SELECT count(*) AS count FROM payment_customers"</automated>
    <fails_when>non-zero exit, or the output contains "no such table", which means migration 0025 has not been applied to production yet</fails_when>
  </verify>
  <done>The phase is deployed, migration 0025 is applied in production, both new routes refuse anonymous callers, and checkout is unaffected — all verified without a single write.</done>
</task>

<task type="auto">
  <name>Task 3: Record the consolidation debt and hand Russell the card-in-hand checks</name>
  <files>.planning/todos/pending/consolidate-stripe-customer-bindings.md, .planning/todos/completed/saved-payment-methods.md</files>
  <read_first>
    - `.planning/todos/pending/saved-payment-methods.md` — the todo this phase was built from; it moves to `completed/` now
    - `.planning/todos/pending/migration-0023-duplicate-number.md` — the frontmatter shape (`title`, `created`, `resolves_phase`, `source`) every todo in this tree uses
    - `.planning/phases/17-saved-payment-methods/17-CONTEXT.md` D-02 — the decision to keep two separate Stripe Customer bindings and why, plus the explicit instruction to log the consolidation as a Phase 18 tech-debt item
  </read_first>
  <action>
    Create `.planning/todos/pending/consolidate-stripe-customer-bindings.md` following the
    existing todo frontmatter shape. It records D-02 in full: this phase deliberately added
    `payment_customers` beside the existing `subscription_provider_customers` rather than sharing
    one table, because sharing would have meant editing revenue-critical, already-shipped
    subscription code during an unattended run. Name the concrete cost — a shopper who both
    subscribes and saves a card at checkout ends up with two separate Stripe Customer objects, so
    a card saved at checkout is not offered for a subscription and vice versa. Name the wanted
    end state: one binding table both features read, with the subscription rows migrated onto it
    expand-only, and the duplicate Stripe Customers reconciled. Point at
    `lib/payments/customer-binding.ts`, `lib/subscriptions/repository.ts` and
    `lib/db/schema/subscriptions.ts` as the three places that would change.

    Move `.planning/todos/pending/saved-payment-methods.md` to `.planning/todos/completed/` with
    `git mv` — this phase is what it asked for. Leave its contents unchanged.

    Write the phase close record into the summary: the gate numbers from Task 1, the production
    status codes and row count from Task 2, and the list of open items below.
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/consolidate-stripe-customer-bindings.md &amp;&amp; test -f .planning/todos/completed/saved-payment-methods.md &amp;&amp; test ! -f .planning/todos/pending/saved-payment-methods.md</automated>
    <fails_when>non-zero exit, which means either the new todo is missing or the source todo was not moved out of pending</fails_when>
    <human-check>
      For Russell, signed in on https://voltique.russellkmoore.me with a real test card. Four
      things, in this order — nothing here can be proven from a terminal, because each one needs a
      card typed into Stripe's own iframe:

      1. Buy something small. On the payment step, tick Stripe's "save this card" box, and pay.
      2. Start a second checkout. Confirm your saved card is offered in the Payment Element before
         you type anything, and confirm the "Save my information" Link box is still absent.
      3. Go to Account → Payment methods. Confirm the card is listed by brand, last four and
         expiry, then remove it and confirm it disappears.
      4. Open a private window, not signed in, and reach the checkout payment step. Confirm you
         see the plain card form with no saved cards and no save-this-card box.

      If step 2 shows no saved card, the Customer Session is not reaching the browser — say so and
      nothing else needs debugging first.
    </human-check>
  </verify>
  <done>The consolidation debt is written down, the source todo is filed as completed, and Russell has a four-step list he can work through with a card in hand.</done>
</task>

</tasks>

<acceptance_criteria>
- All eleven CI gates pass locally in one run before the push.
- The push deployed; migration 0025 is applied in production and `payment_customers` exists and is empty.
- Anonymous callers get 401 from both new routes in production; checkout still answers 200 (D-07, D-08).
- No write of any kind was made to production during verification.
- D-02's consolidation is recorded as a pending Phase 18 todo.
- The four card-in-hand checks are handed to Russell explicitly rather than assumed passing.
</acceptance_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → production | A push to main is a deploy; a migration reaches the production database without further review |
| verification tooling → production data | Read-only probes and one read query; nothing here may write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-29 | Tampering | production database | critical | mitigate | Migration 0025 is expand-only and creates one empty table; `check:migrations` proves that before the push, and no hand-applied migration is ever run against production |
| T-17-30 | Elevation of Privilege | anonymous access to the new routes | critical | mitigate | Live probes assert 401 on both routes against the real deployment, not only against mocks (D-08) |
| T-17-31 | Information Disclosure | verification output | high | mitigate | Probes record status codes only; no response body containing a Stripe id, a client secret or a card detail is printed or committed |
| T-17-32 | Denial of Service | a bad deploy taking checkout down | high | mitigate | A live 200 check on the checkout page is part of verification, so a regression in the shared checkout path is caught immediately rather than by the next shopper |
| T-17-33 | Repudiation | unverifiable card-in-hand behaviours | medium | transfer | The four behaviours a machine cannot exercise are transferred to Russell as a written, ordered checklist with a stated first-failure diagnosis, rather than recorded as assumed-passing |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan; the production dependency audit at high severity is the first gate run in Task 1 |
</threat_model>

<artifacts_this_phase_produces>
This plan creates `.planning/todos/pending/consolidate-stripe-customer-bindings.md` and
`.planning/phases/17-saved-payment-methods/17-06-SUMMARY.md`, and moves
`.planning/todos/pending/saved-payment-methods.md` into `.planning/todos/completed/`. It changes
no source file unless a gate turns red.
</artifacts_this_phase_produces>

<verification>
Eleven green CI gates, a deployed Worker version, four production probes with the expected
status codes, one read-only production row count, and two todo-file assertions.
</verification>

<success_criteria>
PAY-01, PAY-02 and PAY-03 are live, proven as far as an unattended run can prove them, with the
remaining card-in-hand checks and the one accepted piece of debt both written down explicitly.
</success_criteria>

<output>
Create `.planning/phases/17-saved-payment-methods/17-06-SUMMARY.md` when done.
</output>
