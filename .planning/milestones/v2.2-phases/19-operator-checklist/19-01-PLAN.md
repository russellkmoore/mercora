---
phase: 19-operator-checklist
plan: 19-01
wave: 1
depends_on: []
requirements: [OPS-05, OPS-06, OPS-07]
files_modified:
  - wrangler.jsonc
autonomous: false
---

# Plan 19-01: Operator Checklist

Three items only Russell can do, each requiring dashboard access this session does not have. No code changes beyond one config line; the rest is account-level action verified from the outside.

<task id="1" type="manual">
<objective>Enable Stripe Tax on the live Stripe account (OPS-05).</objective>
<action>Russell enables Stripe Tax directly in the Stripe Dashboard.</action>
<acceptance_criteria>The next production order records `tax_source: "provider"` instead of `"configured_fallback"`.</acceptance_criteria>
<verify><human-check>Confirmed by Russell directly in conversation, 2026-09-11.</human-check></verify>
</task>

<task id="2" type="config">
<objective>Set a real, routed support email (OPS-06).</objective>
<action>Add `STORE_SUPPORT_EMAIL` to `wrangler.jsonc`'s `vars` block next to `STORE_SENDER_EMAIL`; confirm Cloudflare Email Routing delivers replies to an inbox Russell checks.</action>
<acceptance_criteria>The storefront's support link no longer shows the placeholder `support@mercora.example.com`; the address is routed.</acceptance_criteria>
<verify>
<automated>curl -s https://voltique.russellkmoore.me/ | grep -o 'mailto:support@[a-zA-Z0-9.]*'</automated>
<fails_when>output is empty or still shows support@mercora.example.com</fails_when>
</verify>
</task>

<task id="3" type="manual">
<objective>Correct production secrets (OPS-07).</objective>
<action>Generate and set `ORDER_STATUS_SECRET` and `EMAIL_UNSUBSCRIBE_SECRET_CURRENT` as Worker secrets (64 random bytes each, piped directly to `wrangler secret put`, never printed); delete the stale `ADMIN_USER_IDS` secret after confirming no code references it.</action>
<acceptance_criteria>`wrangler secret list` shows both required secrets present and `ADMIN_USER_IDS` gone.</acceptance_criteria>
<verify>
<automated>npx wrangler secret list | grep -c '"ORDER_STATUS_SECRET"\|"EMAIL_UNSUBSCRIBE_SECRET_CURRENT"'</automated>
<fails_when>count is less than 2, or ADMIN_USER_IDS still appears in the list</fails_when>
</verify>
</task>

<must_haves>
<truths>
- A production order after the toggle records tax_source "provider"
- The storefront support link shows a real, routed address
- wrangler secret list shows ORDER_STATUS_SECRET and EMAIL_UNSUBSCRIBE_SECRET_CURRENT present and ADMIN_USER_IDS absent
</truths>
<artifacts>
- wrangler.jsonc (STORE_SUPPORT_EMAIL)
</artifacts>
</must_haves>

<threat_model>
No new attack surface — a config value and two Worker secrets rotated with locally-generated random values, never logged or printed; the deleted secret was confirmed unreferenced by any code path before removal.
</threat_model>
